---
phase: 04-content-validation-storage-lifecycle-observability
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - internal/metrics/metrics.go
  - internal/metrics/queue_collector.go
  - internal/metrics/metrics_test.go
  - internal/worker/worker.go
  - internal/reconciler/reconciler.go
autonomous: false
requirements: [OBS-01]
user_setup: []
must_haves:
  truths:
    - "A job reaching a terminal state increments a job-outcome counter labeled engine+status exactly once per job, never on the transient-retry path (OBS-01/D-13, Pitfall 6)"
    - "Each conversion attempt's wall-clock duration is observed in a histogram labeled engine+status (D-14)"
    - "Each webhook delivery attempt increments a success/failure counter (OBS-01)"
    - "Each reconciler recovery and exhaustion increments a recovered/exhausted counter (D-15)"
    - "Queue depth per asynq queue and state is exposed via a pull-based prometheus.Collector (OBS-01)"
  artifacts:
    - path: internal/metrics/metrics.go
      provides: "promauto counters/histogram + Record* helpers for job/webhook/reconciler outcomes"
      contains: "package metrics"
    - path: internal/metrics/queue_collector.go
      provides: "NewQueueDepthCollector wrapping asynq.Inspector.GetQueueInfo"
      contains: "func NewQueueDepthCollector"
  key_links:
    - from: internal/worker/worker.go
      to: internal/metrics/metrics.go
      via: "metrics.RecordJobOutcome / RecordWebhookDelivery at the terminal exit points"
      pattern: "metrics\\.Record(JobOutcome|WebhookDelivery)"
    - from: internal/reconciler/reconciler.go
      to: internal/metrics/metrics.go
      via: "metrics.RecordReconcilerAction at the exhausted + recovered branches"
      pattern: "metrics\\.RecordReconcilerAction"
---

<objective>
Define the Prometheus metric set for the service and instrument the existing
terminal event points — job outcomes, conversion duration, webhook delivery
outcomes, reconciler recoveries/exhaustions — plus a pull-based queue-depth
collector, all in a new `internal/metrics` package.

Purpose: Close the instrumentation half of OBS-01. Metrics are emitted exactly
where each event already happens, without changing any Phase 2/3 behavior and
without adding logging to `internal/*` (metrics are the sanctioned visibility
mechanism the reconciler doc comment explicitly defers to Phase 4).
Output: `internal/metrics/{metrics.go,queue_collector.go,metrics_test.go}` and
increment/observe calls added at the worker and reconciler exit points. The
`/metrics` HTTP exposure and collector registration are wired in plan 05.
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-content-validation-storage-lifecycle-observability/04-CONTEXT.md
@.planning/phases/04-content-validation-storage-lifecycle-observability/04-RESEARCH.md
@.planning/phases/04-content-validation-storage-lifecycle-observability/04-PATTERNS.md

<interfaces>
<!-- Existing contracts the executor builds against. -->

Engine/queue label constants to reuse (internal/queue/queue.go:24-27) — do NOT introduce new string literals:
  const QueueImage = "image"; QueueWebhook = "webhook"

Worker terminal exit points to instrument (internal/worker/worker.go):
  HandleImageConvert (lines 128-151): after h.process(ctx, job) —
    isTerminal(err) branch -> job ends "failed"; success branch -> job ends "done".
    The transient branch (`return err` at ~line 144) must NOT be instrumented (Pitfall 6).
  HandleWebhookDeliver (lines 219-235): code, derr := h.deliverer.Deliver(...); derr==nil means success.

Reconciler branches to instrument (internal/reconciler/reconciler.go):
  sweep (lines 94-105): `if n >= s.cfg.MaxRecoveries` -> after MarkFailed = "exhausted".
  sweep (lines 126-176): after a SUCCESSFUL RequeueStale = "recovered". The
    asynq.ErrDuplicateTask `continue` (lines 113-121) is NOT a recovery — do not count it.

asynq Inspector (v0.26.0, already vendored) for the collector:
  asynq.NewInspector(asynq.RedisConnOpt) *asynq.Inspector
  (*asynq.Inspector).GetQueueInfo(queue string) (*asynq.QueueInfo, error)
  QueueInfo fields: Pending, Active, Scheduled, Retry, Archived int

New dependency (gate before install — see Task 1):
  github.com/prometheus/client_golang  (promauto, promhttp, prometheus, prometheus/testutil) — tagged [ASSUMED] in RESEARCH Package Legitimacy Audit
</interfaces>
</context>

## Phase Goal

**As an** operator, **I want to** have Prometheus metrics emitted for job outcomes, conversion durations, webhook deliveries, reconciler recoveries, and queue depth, **so that** I can see throughput and spot failures on a dashboard instead of grepping job_events.

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Gate the prometheus/client_golang install ([ASSUMED] package)</name>
  <action>Pause for human verification of the new third-party dependency before any `go get`. Do not install `github.com/prometheus/client_golang` or proceed to Task 2 until approved; this checkpoint is never auto-approved.</action>
  <what-built>Nothing yet — this checkpoint gates the one new third-party dependency this phase adds. `github.com/prometheus/client_golang` is tagged [ASSUMED] in RESEARCH.md's Package Legitimacy Audit because slopcheck could not run in the research environment; per protocol the install must be human-verified before it enters go.mod.</what-built>
  <how-to-verify>
    1. Open https://pkg.go.dev/github.com/prometheus/client_golang and confirm it is the official Prometheus-org module (repo github.com/prometheus/client_golang), actively maintained, with a v1.2x tag.
    2. Confirm the version resolves against the Go module proxy: `curl -s https://proxy.golang.org/github.com/prometheus/client_golang/@latest` returns a VCS-backed tag (RESEARCH recorded v1.23.2 / commit 8179a56).
    3. Confirm this is the intended, first-party Prometheus Go client and not a typosquat (organization must be `prometheus`).
  </how-to-verify>
  <resume-signal>Type "approved" to allow `go get github.com/prometheus/client_golang`, or describe concerns.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: metrics package — definitions, Record helpers, and queue-depth collector</name>
  <files>internal/metrics/metrics.go, internal/metrics/queue_collector.go, internal/metrics/metrics_test.go</files>
  <read_first>
    - internal/queue/queue.go (QueueImage/QueueWebhook constants; the Inspector-free parts)
    - internal/reconciler/reconciler.go (the package doc comment at lines 76-79 that explicitly defers OBS metrics to Phase 4 — confirms metrics, not logs, are the sanctioned mechanism)
    - .planning/phases/04-content-validation-storage-lifecycle-observability/04-RESEARCH.md (Code Examples job-outcome counter/histogram; Pattern 3 queue collector; D-13 engine+status only, D-14 duration histogram, D-15 reconciler counter)
    - .planning/phases/04-content-validation-storage-lifecycle-observability/04-PATTERNS.md (metrics package pattern: package doc on metrics.go, New<Type> constructor convention)
  </read_first>
  <action>
    Run `go get github.com/prometheus/client_golang` (approved in Task 1) and `go mod tidy`. Create `internal/metrics/metrics.go` in `package metrics` with the package doc comment "Package metrics defines the Prometheus counters, histograms, and collectors this service exposes." Define with `promauto.NewCounterVec`/`NewHistogramVec` (default registry): `octoconv_job_outcomes_total` labeled `["engine","status"]` (D-13 — engine+status only, no client_id/error_code); `octoconv_job_duration_seconds` HistogramVec labeled `["engine","status"]` using `prometheus.DefBuckets` (D-14); `octoconv_webhook_deliveries_total` CounterVec labeled `["result"]`; `octoconv_reconciler_actions_total` CounterVec labeled `["action"]` (D-15). Expose thin helpers so callers never touch label plumbing: `func RecordJobOutcome(engine, status string, d time.Duration)` (increments the outcome counter and observes the duration for the same labels), `func RecordWebhookDelivery(success bool)` (result label "success"/"failure"), `func RecordReconcilerAction(action string)`. Create `internal/metrics/queue_collector.go` implementing a pull-based `prometheus.Collector` per RESEARCH Pattern 3: `func NewQueueDepthCollector(inspector *asynq.Inspector, queues ...string) prometheus.Collector` with a `Describe` emitting one `octoconv_queue_depth` desc (labels `["queue","state"]`) and a `Collect` that calls `GetQueueInfo` per queue and emits `pending/active/scheduled/retry/archived` gauges, swallowing per-queue errors with `continue` (a Redis blip must not crash a scrape). Write `metrics_test.go` using `github.com/prometheus/client_golang/prometheus/testutil`: assert `RecordJobOutcome("image","done",...)` increments the counter, `RecordWebhookDelivery(true)`/`(false)` bump distinct result labels, `RecordReconcilerAction("recovered")` increments, and `NewQueueDepthCollector(...).Describe(ch)` yields exactly one descriptor without needing a live Redis.
  </action>
  <verify>
    <automated>go test ./internal/metrics/ -v && go build ./... && go vet ./internal/metrics/</automated>
  </verify>
  <done>The metrics package registers the four metric families + queue-depth collector, exposes Record* helpers, and its unit tests (via testutil, no Redis) pass; the whole module builds.</done>
  <acceptance_criteria>
    - `go test ./internal/metrics/` passes
    - `grep -c 'func RecordJobOutcome' internal/metrics/metrics.go` == 1, `RecordWebhookDelivery` == 1, `RecordReconcilerAction` == 1
    - `grep -c 'func NewQueueDepthCollector' internal/metrics/queue_collector.go` == 1
    - Metric names present: `grep -c 'octoconv_job_outcomes_total' internal/metrics/metrics.go` == 1 and `grep -c 'octoconv_queue_depth' internal/metrics/queue_collector.go` == 1
    - `github.com/prometheus/client_golang` is now a direct require in go.mod: `grep -c 'prometheus/client_golang' go.mod` >= 1
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Instrument worker and reconciler terminal exit points</name>
  <files>internal/worker/worker.go, internal/reconciler/reconciler.go</files>
  <read_first>
    - internal/worker/worker.go (HandleImageConvert lines 110-152 and HandleWebhookDeliver lines 160-236 — the exact terminal branches; queue import already present)
    - internal/reconciler/reconciler.go (sweep lines 80-177 — the exhausted branch, the ErrDuplicateTask non-recovery, the RequeueStale success point)
    - internal/metrics/metrics.go (the Record* helper signatures from Task 2)
    - .planning/phases/04-content-validation-storage-lifecycle-observability/04-RESEARCH.md (Pitfall 6 — count only genuine terminal transitions, never per-attempt on the transient path)
  </read_first>
  <action>
    In `HandleImageConvert`, capture `start := time.Now()` immediately before `h.process(ctx, job)`. In the `isTerminal(err)` branch (job becomes failed) call `metrics.RecordJobOutcome(queue.QueueImage, jobs.StatusFailed, time.Since(start))` before its `return`; on the success path (after `h.process` returns nil, job is done) call `metrics.RecordJobOutcome(queue.QueueImage, jobs.StatusDone, time.Since(start))` before `return nil`. Do NOT instrument the transient `return err` branch (Pitfall 6 — the job stays active for asynq's own retry, not a terminal outcome). In `HandleWebhookDeliver`, after `code, derr := h.deliverer.Deliver(...)`, call `metrics.RecordWebhookDelivery(derr == nil)`. In `reconciler.go` `sweep`, call `metrics.RecordReconcilerAction("exhausted")` right after the `MarkFailed` in the `n >= s.cfg.MaxRecoveries` branch (before `continue`), and `metrics.RecordReconcilerAction("recovered")` only after a `RequeueStale` call returns nil (the genuine-recovery point) — never in the `asynq.ErrDuplicateTask` continue (that is a backlogged no-op, not a recovery). Add the `github.com/apaderin/octoconv/internal/metrics` import to both files. Use `queue.QueueImage`, `jobs.StatusDone`, `jobs.StatusFailed` (already imported in worker.go) as label values — no new string literals. Add NO `log.Printf` to either package (internal/* stays log-free; metrics only).
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && go test ./internal/worker/ ./internal/reconciler/ ./internal/metrics/</automated>
  </verify>
  <done>Job outcomes + durations, webhook deliveries, and reconciler recoveries/exhaustions increment at exactly the genuine terminal points; the transient-retry path and the duplicate-task no-op are not counted; existing worker/reconciler tests still pass; no new logging added.</done>
  <acceptance_criteria>
    - `go build ./... && go vet ./...` clean and `go test ./internal/worker/ ./internal/reconciler/` pass
    - `grep -c 'metrics.RecordJobOutcome' internal/worker/worker.go` == 2 (done + failed only)
    - `grep -c 'metrics.RecordWebhookDelivery' internal/worker/worker.go` == 1
    - `grep -c 'metrics.RecordReconcilerAction' internal/reconciler/reconciler.go` == 2 (recovered + exhausted)
    - No new logging in the instrumented packages: `grep -c 'log.Printf' internal/reconciler/reconciler.go` == 0
    - Label values reuse existing constants: `grep -c 'queue.QueueImage' internal/worker/worker.go` >= 2
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build/CI → go module supply chain | A new third-party module (`prometheus/client_golang`) enters the dependency graph |
| worker/reconciler → metrics registry | Increment-only observation; metrics never gate business logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-SC | Tampering | `go get github.com/prometheus/client_golang` (npm/pip/cargo-equivalent module install) | mitigate | [ASSUMED] package gated behind the blocking-human checkpoint (Task 1): verify on pkg.go.dev + module proxy that it is the official `prometheus` org module before install. Never auto-approved. |
| T-04-08 | Denial of Service | Unbounded metric-label cardinality | mitigate | Labels are a closed set — `engine`+`status` only, no client_id/error_code (D-13); `result` and `action` are small enums; queue-depth labels are the fixed queue/state taxonomy. |
| T-04-09 | Tampering | Metric counts diverging from real job outcomes across asynq retries | mitigate | Outcome counter increments only at the two genuine terminal transitions, never on the transient-retry return (Pitfall 6); a Redis blip during a scrape is swallowed with `continue`, not a crash. |
</threat_model>

<verification>
- `go test ./internal/metrics/ ./internal/worker/ ./internal/reconciler/` pass; `go build ./... && go vet ./...` clean
- The four metric families and the queue-depth collector are defined; instrumentation sits at the genuine terminal points only
- OBS-01 instrumentation complete; HTTP exposure of `/metrics` and collector registration follow in plan 05
</verification>

<success_criteria>
- OBS-01 (instrumentation): job-outcome + duration, webhook-delivery, reconciler-action metrics emitted at the correct event points; queue-depth collector implemented
- No Phase 2/3 behavior changed; no logging added to internal/*; the one new dependency is human-verified before install
</success_criteria>

<output>
Create `.planning/phases/04-content-validation-storage-lifecycle-observability/04-03-SUMMARY.md` when done
</output>
