---
phase: 04-content-validation-storage-lifecycle-observability
plan: 05
type: execute
wave: 3
depends_on: [04-02, 04-03, 04-04]
files_modified:
  - cmd/api/main.go
  - cmd/worker/main.go
  - docker-compose.yml
  - .env.example
autonomous: false
requirements: [OBS-01, OBS-03]
user_setup:
  - service: asynqmon
    why: "Queue-inspection dashboard (OBS-03); a pinned Docker image, no application code"
    dashboard_config:
      - task: "Confirm a specific published hibiken/asynqmon tag exists (not :latest) before compose up"
        location: "Docker Hub: hub.docker.com/r/hibiken/asynqmon/tags (or `docker manifest inspect`)"
must_haves:
  truths:
    - "The API and worker each serve Prometheus metrics on a localhost-only listener (METRICS_ADDR, default 127.0.0.1:9090), separate from the public API_ADDR (OBS-01/D-19)"
    - "The worker registers the queue-depth collector so per-queue depth is scrapeable (OBS-01)"
    - "An asynqmon dashboard is reachable on 127.0.0.1 only for visual queue inspection (OBS-03/D-18)"
  artifacts:
    - path: docker-compose.yml
      provides: "asynqmon service bound to 127.0.0.1 + METRICS_ADDR on api/worker"
      contains: "asynqmon"
    - path: cmd/worker/main.go
      provides: "worker /metrics listener + queue-depth collector registration"
      contains: "promhttp"
  key_links:
    - from: cmd/worker/main.go
      to: internal/metrics/queue_collector.go
      via: "prometheus.MustRegister(metrics.NewQueueDepthCollector(asynq.NewInspector(redisOpt), queue.QueueImage, queue.QueueWebhook))"
      pattern: "NewQueueDepthCollector"
    - from: cmd/api/main.go
      to: metrics HTTP exposition
      via: "second http.Server on METRICS_ADDR serving promhttp.Handler()"
      pattern: "promhttp.Handler"
---

<objective>
Expose the metrics defined in plan 04-03 over HTTP on a localhost-only
`/metrics` listener in both the API and worker processes, register the
queue-depth collector in the worker, and deploy the asynqmon dashboard bound to
127.0.0.1.

Purpose: Close the exposure half of OBS-01 (operators can scrape Prometheus
metrics) and OBS-03 (operators can visually inspect the queue), keeping both
new surfaces off the public API port per the internal-only trust model
(D-18/D-19).
Output: a second `/metrics` `http.Server` in each of `cmd/api/main.go` and
`cmd/worker/main.go`, queue-collector registration, a pinned localhost-bound
`asynqmon` compose service, and `METRICS_ADDR` documented + set.
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-content-validation-storage-lifecycle-observability/04-CONTEXT.md
@.planning/phases/04-content-validation-storage-lifecycle-observability/04-RESEARCH.md
@.planning/phases/04-content-validation-storage-lifecycle-observability/04-PATTERNS.md

<interfaces>
<!-- Existing contracts the executor builds against. -->

metrics package (plan 04-03):
  metrics.NewQueueDepthCollector(inspector *asynq.Inspector, queues ...string) prometheus.Collector
  All Record* metrics are registered on the prometheus default registry (promauto), so promhttp.Handler() exposes them with no extra registration.

promhttp exposition (github.com/prometheus/client_golang/promhttp): promhttp.Handler() http.Handler.
prometheus.MustRegister(c prometheus.Collector) — for the custom queue collector.

asynq (already imported in cmd/worker/main.go): asynq.NewInspector(redisOpt) *asynq.Inspector; the worker already builds `redisOpt` (queue.RedisOpt) at line 41.
queue.QueueImage / queue.QueueWebhook constants for the collector's queue list.

cmd/api/main.go second-listener model (lines 66-87): existing httpSrv construction + goroutine ListenAndServe + <-ctx.Done() Shutdown block to mirror for the metrics listener.
cmd/worker/main.go (lines 82-98): asynq.Server.Start is non-blocking, then the process blocks on <-ctx.Done() and calls srv.Shutdown() — the metrics listener goroutine + shutdown slots in here. The worker currently has NO http.Server (net/http not imported).

docker-compose.yml models: the `redis` service block (simplest) and `createbucket` (auxiliary container) for the asynqmon shape; api/worker environment blocks for METRICS_ADDR. Existing services bind `0.0.0.0` via bare "host:container"; asynqmon must use the `127.0.0.1:` prefix (the only such binding in the file, deliberate per D-18).
</interfaces>
</context>

## Phase Goal

**As an** operator, **I want to** scrape Prometheus /metrics on a localhost-only listener and open an asynqmon dashboard, **so that** I can watch metrics and inspect the queue without exposing operational data to arbitrary API callers.

<tasks>

<task type="auto">
  <name>Task 1: /metrics listeners on API + worker, queue collector, METRICS_ADDR config</name>
  <files>cmd/api/main.go, cmd/worker/main.go, .env.example, docker-compose.yml</files>
  <read_first>
    - cmd/api/main.go (the httpSrv construction + goroutine + graceful-shutdown block, lines 62-87; existing imports; envInt64/firstField helpers)
    - cmd/worker/main.go (the redisOpt build at line 41, the asynq server start + <-ctx.Done() shutdown at 82-98; note net/http is NOT imported here yet)
    - internal/metrics/queue_collector.go (the NewQueueDepthCollector signature from plan 04-03)
    - internal/queue/queue.go (QueueImage/QueueWebhook)
    - .env.example (the section-comment style; existing API_ADDR entry)
    - docker-compose.yml (api + worker environment blocks, lines 77-89 / 106-115)
    - .planning/phases/04-content-validation-storage-lifecycle-observability/04-CONTEXT.md (D-19 separate localhost-only metrics listener)
  </read_first>
  <action>
    In `cmd/api/main.go`, after the primary `httpSrv` is set up, read `metricsAddr := os.Getenv("METRICS_ADDR")` defaulting to `127.0.0.1:9090`, build a second `&http.Server{Addr: metricsAddr, Handler: promhttp.Handler(), ReadHeaderTimeout: 10 * time.Second}`, start it in its own goroutine with the same `log.Printf("📊 metrics listening on %s", metricsAddr)` + `errors.Is(err, http.ErrServerClosed)` guard idiom used for httpSrv, and add its `Shutdown(shutdownCtx)` alongside the existing `httpSrv.Shutdown` in the `<-ctx.Done()` block. In `cmd/worker/main.go`, add `net/http`, the promhttp/prometheus imports, and the `internal/metrics` import; register the collector with `prometheus.MustRegister(metrics.NewQueueDepthCollector(asynq.NewInspector(redisOpt), queue.QueueImage, queue.QueueWebhook))` before starting the asynq server; build the same `METRICS_ADDR` (default `127.0.0.1:9090`) `http.Server` serving `promhttp.Handler()`, run it in a goroutine, and `Shutdown` it in the `<-ctx.Done()` path after `srv.Shutdown()`. Add to `.env.example`, under a new `# Observability` section, `METRICS_ADDR=127.0.0.1:9090   # localhost-only Prometheus /metrics listener, separate from API_ADDR; run api+worker on one host with distinct values (D-19)`. Add `METRICS_ADDR: "127.0.0.1:9090"` to BOTH the `api` and `worker` service environment blocks in `docker-compose.yml`, and do NOT publish the metrics port to the host (no `ports:` entry — it stays container-local per D-19).
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && grep -c 'promhttp.Handler' cmd/api/main.go cmd/worker/main.go && grep -c 'NewQueueDepthCollector' cmd/worker/main.go && grep -c 'METRICS_ADDR' .env.example docker-compose.yml</automated>
  </verify>
  <done>Both processes serve promhttp metrics on METRICS_ADDR (default 127.0.0.1:9090); the worker registers the queue-depth collector; METRICS_ADDR is documented and set on both compose services without a host port publish; build and vet clean.</done>
  <acceptance_criteria>
    - `go build ./... && go vet ./...` clean
    - `grep -c 'promhttp.Handler' cmd/api/main.go` == 1 and `grep -c 'promhttp.Handler' cmd/worker/main.go` == 1
    - `grep -c 'NewQueueDepthCollector' cmd/worker/main.go` == 1
    - `grep -c 'METRICS_ADDR' .env.example` == 1; appears on both compose services: `grep -c 'METRICS_ADDR' docker-compose.yml` == 2
    - Metrics port is not host-published: no `9090` appears under a `ports:` list in docker-compose (source assertion)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: asynqmon dashboard service (localhost-only) in docker-compose</name>
  <files>docker-compose.yml</files>
  <read_first>
    - docker-compose.yml (the redis service block for depends_on/healthcheck shape; the createbucket auxiliary-service shape; the REDIS_ADDR value redis:6379 already used by api/worker)
    - .planning/phases/04-content-validation-storage-lifecycle-observability/04-CONTEXT.md (D-18 asynqmon separate service, 127.0.0.1-only, no extra auth)
    - .planning/phases/04-content-validation-storage-lifecycle-observability/04-RESEARCH.md (asynqmon env vars REDIS_ADDR/PORT default 8080; supply-chain note: pin the tag, not :latest)
  </read_first>
  <action>
    Add an `asynqmon` service to `docker-compose.yml`: `image: hibiken/asynqmon:<pinned-tag>` where `<pinned-tag>` is a specific published tag confirmed to exist on Docker Hub (NOT `:latest` — supply-chain hygiene; verify via the tags page or `docker manifest inspect` at execution time, e.g. a current `v0.7.x`); `container_name: octoconv-asynqmon`; `restart: always`; `depends_on: redis: {condition: service_healthy}` (same shape as api/worker); `environment: REDIS_ADDR: redis:6379` (asynqmon reads REDIS_ADDR, container port defaults to 8080); and `ports: - "127.0.0.1:8980:8080"` — the `127.0.0.1:` host-binding prefix is required so the dashboard is reachable only from the host loopback, never `0.0.0.0` (D-18). No auth layer, no new secret (D-18).
  </action>
  <verify>
    <automated>grep -c 'hibiken/asynqmon' docker-compose.yml && grep -c '127.0.0.1:8980:8080' docker-compose.yml && ! grep -q 'hibiken/asynqmon:latest' docker-compose.yml && echo OK</automated>
  </verify>
  <done>docker-compose defines an asynqmon service pinned to a specific tag, depending on a healthy redis, reading REDIS_ADDR, and bound to 127.0.0.1 only.</done>
  <acceptance_criteria>
    - `grep -c 'hibiken/asynqmon' docker-compose.yml` == 1 and the tag is not `:latest` (`grep -c 'hibiken/asynqmon:latest' docker-compose.yml` == 0)
    - Localhost binding present: `grep -c '127.0.0.1:8980:8080' docker-compose.yml` == 1
    - asynqmon depends on redis healthy: within the asynqmon block, `redis:` under `depends_on` with `condition: service_healthy` (source assertion)
    - REDIS_ADDR set to `redis:6379` for asynqmon (source assertion)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify /metrics scrape + asynqmon dashboard reachability</name>
  <action>Pause for human verification of the two operational surfaces after the code + compose changes land. Do not close the plan until an operator confirms /metrics scrapes on both containers (localhost-only) and the asynqmon dashboard is reachable on 127.0.0.1 only.</action>
  <what-built>Both processes now expose Prometheus `/metrics` on a localhost-only listener (METRICS_ADDR=127.0.0.1:9090, container-local) and the compose stack includes an asynqmon dashboard bound to 127.0.0.1:8980. This checkpoint confirms both operational surfaces are actually reachable and correctly scoped to localhost.</what-built>
  <how-to-verify>
    1. `docker-compose up --build -d` and wait for postgres/redis/minio healthchecks to pass.
    2. Metrics (OBS-01): `docker-compose exec worker wget -qO- 127.0.0.1:9090/metrics | grep octoconv_` — confirm `octoconv_queue_depth`, `octoconv_job_outcomes_total`, `octoconv_job_duration_seconds`, `octoconv_webhook_deliveries_total`, `octoconv_reconciler_actions_total` appear. Repeat for the api container: `docker-compose exec api wget -qO- 127.0.0.1:9090/metrics | head`.
    3. Localhost scoping (D-19): confirm the metrics port is NOT reachable from the host — `curl -sS --max-time 3 localhost:9090/metrics` should fail/refuse (no host publish).
    4. asynqmon (OBS-03): open http://127.0.0.1:8980 in a browser and confirm the `image` and `webhook` queues are listed. Confirm it is NOT reachable on the host's external interface (only 127.0.0.1).
    5. Optional end-to-end: submit one conversion job (authenticated POST /v1/jobs) and confirm `octoconv_job_outcomes_total{status="done"}` increments and the job is visible in asynqmon.
  </how-to-verify>
  <resume-signal>Type "approved" once /metrics scrapes on both containers and the asynqmon dashboard is reachable on 127.0.0.1 only, or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → /metrics + asynqmon | Two new unauthenticated operational surfaces exposing queue depth, job/webhook counts, and raw task state |
| compose → asynqmon Docker image | A third-party image (not a go module) enters the deployment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13 | Information Disclosure | `/metrics` (job counts, queue depth, webhook success rates) reachable by API callers | mitigate | Served on a separate `METRICS_ADDR` listener defaulting to 127.0.0.1, distinct from the public API_ADDR, and not host-published in compose (D-19) — operational data is not reachable by arbitrary internal API callers. |
| T-04-14 | Information Disclosure / Elevation | asynqmon dashboard (full queue/task inspection, no auth) | mitigate | Bound to `127.0.0.1:8980` only, never `0.0.0.0`; not reachable outside the host, matching the internal-only trust model without new credential management (D-18). |
| T-04-15 | Tampering | asynqmon supply chain (`:latest` image drift) | mitigate | Image pinned to a specific published tag (not `:latest`); a human confirms the tag on Docker Hub before compose up (user_setup) — Docker images are outside go.mod/slopcheck, so tag-pinning + human confirmation is the applicable control. |
</threat_model>

<verification>
- `go build ./... && go vet ./...` clean
- Checkpoint: `/metrics` scrapes `octoconv_*` families on both api and worker containers (localhost-only, not host-published); asynqmon dashboard reachable at 127.0.0.1:8980 showing the image/webhook queues (OBS-01 exposure + OBS-03)
</verification>

<success_criteria>
- OBS-01 (exposure): operators can scrape Prometheus metrics (queue depth, job outcomes, durations, webhook, reconciler) from a localhost-only listener on both processes
- OBS-03: asynqmon dashboard deployed for visual queue inspection, bound to 127.0.0.1 (D-18)
- Both surfaces kept off the public API port per D-18/D-19
</success_criteria>

<output>
Create `.planning/phases/04-content-validation-storage-lifecycle-observability/04-05-SUMMARY.md` when done
</output>
