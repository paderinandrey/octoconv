---
phase: 06-reconciler-webhook-gap-sweep-staleness-soak-test
plan: 03
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - internal/reconciler/reconciler.go
  - internal/reconciler/reconciler_test.go
  - internal/metrics/metrics.go
autonomous: true
requirements: [RECON-04]

must_haves:
  truths:
    - "Each sweep tick runs a SECOND scan (FindWebhookGaps) alongside the existing queued/active staleness scan (D-03)"
    - "For each gap, the sweep attempts EnqueueWebhookDeliver first; only a successful (non-duplicate) enqueue proceeds to RecordWebhookGapRecovered + metrics (D-03, enqueue-first)"
    - "An asynq.ErrDuplicateTask result is treated as 'delivery already live/queued, not a gap' and skipped silently (D-03), with no recovery event recorded"
    - "Each successful gap recovery records a job_events row AND increments the reconciler-actions metric with action label 'webhook_gap_recovered' (D-06)"
    - "Webhook-gap recovery is uncapped/single-shot — it never counts toward MaxRecoveries (uses detailActionWebhookGapRecovered, not detailActionRecovery)"
  artifacts:
    - path: "internal/reconciler/reconciler.go"
      provides: "jobStore interface gains FindWebhookGaps + RecordWebhookGapRecovered; sweep() gains a second enqueue-first webhook-gap loop"
      contains: "FindWebhookGaps"
    - path: "internal/reconciler/reconciler_test.go"
      provides: "fakeStore/fakeEnqueuer extended; unit tests for successful gap recovery, duplicate-skip, and best-effort find error"
      contains: "TestSweepRecoversWebhookGap"
    - path: "internal/metrics/metrics.go"
      provides: "reconcilerActions Help text updated to document the webhook_gap_recovered action value"
      contains: "webhook_gap_recovered"
  key_links:
    - from: "internal/reconciler/reconciler.go sweep()"
      to: "store.FindWebhookGaps"
      via: "second scan per tick"
      pattern: "FindWebhookGaps\\(ctx, s\\.cfg\\.ActiveStaleAfter\\)"
    - from: "internal/reconciler/reconciler.go sweep()"
      to: "metrics.RecordReconcilerAction"
      via: "webhook_gap_recovered label on successful enqueue"
      pattern: "RecordReconcilerAction\\(\"webhook_gap_recovered\"\\)"
---

<objective>
Add the orchestration half of RECON-04: extend `Sweeper.sweep()` with a second enqueue-first scan over `FindWebhookGaps`, guarded by `asynq.ErrDuplicateTask` exactly like the existing image-recovery loop (D-03), with dual observability (job_events + metrics) matching the existing `"recovered"`/`"exhausted"` conventions (D-06).

Purpose: This is where the gap detection (Plan 02) and the uniqueness lock (Plan 01) combine into the working RECON-04 behavior. Per RESEARCH.md the sweep loop MUST land together with (never before) the asynq.Unique addition — which is why this plan depends on both Plan 01 and Plan 02.

Output: extended `jobStore` interface + second sweep loop in `reconciler.go`, extended fakes + 2-3 new unit tests in `reconciler_test.go`, and an updated metric Help string.
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-reconciler-webhook-gap-sweep-staleness-soak-test/06-CONTEXT.md
@.planning/phases/06-reconciler-webhook-gap-sweep-staleness-soak-test/06-RESEARCH.md
@.planning/phases/06-reconciler-webhook-gap-sweep-staleness-soak-test/06-PATTERNS.md

@internal/reconciler/reconciler.go
@internal/reconciler/reconciler_test.go

<interfaces>
From Plan 02 (internal/jobs/repo.go — already merged before this plan runs):
- `type WebhookGapJob struct { ID uuid.UUID; Status string }`
- `func (r *Repo) FindWebhookGaps(ctx context.Context, activeStaleAfter time.Duration) ([]jobs.WebhookGapJob, error)`
- `func (r *Repo) RecordWebhookGapRecovered(ctx context.Context, id uuid.UUID, status string) error`

Existing in internal/reconciler/reconciler.go:
- `type jobStore interface { FindStale(...); RecoveryCount(...); RequeueStale(...); MarkFailed(...); Get(...) }` (lines 31-37) — the interface to extend.
- `type enqueuer interface { EnqueueImageConvert(...); EnqueueWebhookDeliver(ctx, id) error }` (lines 40-43) — already has EnqueueWebhookDeliver; NO change needed.
- `func (s *Sweeper) sweep(ctx context.Context)` (lines 81-185) — the existing image-recovery loop (lines 88-184) shows the enqueue-first + errors.Is(err, asynq.ErrDuplicateTask) idiom to mirror; append the new loop after it.
- `s.cfg.ActiveStaleAfter` is the reused threshold (D-04).

Existing in internal/metrics/metrics.go:
- `func RecordReconcilerAction(action string)` (line 54) — accepts arbitrary string, NO signature change needed. The `reconcilerActions` CounterVec Help text (line 31) currently says "recovered/exhausted".

Existing in internal/reconciler/reconciler_test.go:
- `fakeStore` (lines 18-63): fields stale/findStaleErr/recoveryCount/jobs/requeueStaleErrs/markFailedCalls + methods.
- `fakeEnqueuer` (lines 66-80): fields enqueueImageErr/imageCalls/webhookCalls; EnqueueWebhookDeliver currently always returns nil.
- `TestSweepSkipsDuplicateEnqueue` (lines 113-136): the exact pattern to mirror for the webhook-gap duplicate-skip test.
- Precedent: tests in this file assert store/enqueuer side effects only, NOT metrics calls (metrics has no test hook) — follow that precedent.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend jobStore and add the webhook-gap sweep loop</name>
  <files>internal/reconciler/reconciler.go, internal/metrics/metrics.go</files>
  <read_first>
    - internal/reconciler/reconciler.go (the whole file — jobStore interface lines 31-37, the existing image-recovery loop lines 88-184 for the enqueue-first + asynq.ErrDuplicateTask idiom and the best-effort-on-finder-error handling at 82-86)
    - internal/metrics/metrics.go (reconcilerActions CounterVec at lines 29-33 and RecordReconcilerAction at 52-56)
    - .planning/phases/06-reconciler-webhook-gap-sweep-staleness-soak-test/06-RESEARCH.md (Pattern 4 — the exact second-scan block; the "hard prerequisite" note explaining why the asynq.Unique lock must be in place, which Plan 01 delivered)
  </read_first>
  <action>
    In internal/reconciler/reconciler.go:
    - Add two methods to the jobStore interface: `FindWebhookGaps(ctx context.Context, activeStaleAfter time.Duration) ([]jobs.WebhookGapJob, error)` and `RecordWebhookGapRecovered(ctx context.Context, id uuid.UUID, status string) error`.
    - Append a second scan to sweep(), AFTER the existing `for _, j := range stale { ... }` loop: call `gaps, err := s.store.FindWebhookGaps(ctx, s.cfg.ActiveStaleAfter)`; on error, best-effort skip (no return needed since it is at the end — do not disturb the existing FindStale early-return). For each gap, attempt `s.enq.EnqueueWebhookDeliver(ctx, g.ID)` FIRST; if it returns errors.Is(err, asynq.ErrDuplicateTask) then `continue` (delivery already live/queued — not a genuine gap); any other error also `continue` (transient, best-effort, retry next tick). Only after a successful non-duplicate enqueue: call `_ = s.store.RecordWebhookGapRecovered(ctx, g.ID, g.Status)` then `metrics.RecordReconcilerAction("webhook_gap_recovered")`. Match the existing loop's best-effort error style exactly (continue/discard, no panic, no propagated error). Add a comment noting webhook-gap recovery is one-shot/self-terminating and uncapped (it uses detailActionWebhookGapRecovered, which RecoveryCount never counts, so it never interacts with MaxRecoveries — per the resolved open question).
    In internal/metrics/metrics.go: update the `reconcilerActions` CounterVec Help string to list the new action value, e.g. "...labeled by action (recovered/exhausted/webhook_gap_recovered)." No signature or logic change to RecordReconcilerAction.
  </action>
  <verify>
    <automated>cd /Users/apaderin/dev/octoconv && go build ./... && go vet ./...</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` succeeds (proves *jobs.Repo satisfies the extended jobStore interface — the Plan 02 methods exist — and cmd/worker still compiles).
    - `grep -n 'FindWebhookGaps' internal/reconciler/reconciler.go` matches in BOTH the interface and sweep().
    - `grep -n 'RecordReconcilerAction("webhook_gap_recovered")' internal/reconciler/reconciler.go` matches, and it appears AFTER the EnqueueWebhookDeliver call (enqueue-first).
    - `grep -n 'errors.Is(err, asynq.ErrDuplicateTask)' internal/reconciler/reconciler.go` count is 2 (image loop + new webhook-gap loop).
    - `grep -c 'webhook_gap_recovered' internal/metrics/metrics.go` >= 1 (Help text updated).
    - `go vet ./...` clean.
  </acceptance_criteria>
  <done>sweep() runs a second enqueue-first webhook-gap scan guarded by asynq.ErrDuplicateTask; successful recoveries emit a job_events row + metric; the module builds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend fakes and add webhook-gap sweep unit tests</name>
  <files>internal/reconciler/reconciler_test.go</files>
  <read_first>
    - internal/reconciler/reconciler_test.go (the whole file — fakeStore lines 18-63, fakeEnqueuer lines 66-80, TestSweepSkipsDuplicateEnqueue lines 113-136, testConfig lines 82-89)
    - internal/reconciler/reconciler.go (the new sweep() second loop from Task 1, to know exactly which side effects to assert)
  </read_first>
  <behavior>
    - TestSweepRecoversWebhookGap: fakeStore returns one WebhookGapJob, fakeEnqueuer's webhook enqueue succeeds -> EnqueueWebhookDeliver called once for that id AND RecordWebhookGapRecovered called once for that id with the job's status.
    - TestSweepSkipsDuplicateWebhookGap: fakeEnqueuer injected with enqueueWebhookErr = asynq.ErrDuplicateTask -> EnqueueWebhookDeliver called once, but RecordWebhookGapRecovered NOT called (no recovery event on a duplicate).
    - TestSweepWebhookGapFindErrorBestEffort: fakeStore returns findWebhookGapsErr -> sweep returns without panic and without calling EnqueueWebhookDeliver for any gap (best-effort).
    - Existing tests (TestSweepRecoversUnderCap etc.) still pass unchanged — the webhook-gap loop must not perturb the existing queued/active recovery behavior when there are no gaps.
  </behavior>
  <action>
    In internal/reconciler/reconciler_test.go:
    - Add to fakeStore: `webhookGaps []jobs.WebhookGapJob`, `findWebhookGapsErr error`, `webhookGapRecoveredCalls []uuid.UUID` fields; implement `FindWebhookGaps(ctx, activeStaleAfter) ([]jobs.WebhookGapJob, error)` (returns findWebhookGapsErr if set, else webhookGaps) and `RecordWebhookGapRecovered(ctx, id, status) error` (appends id to webhookGapRecoveredCalls, returns nil), mirroring the existing FindStale/RequeueStale field+method shape.
    - Add to fakeEnqueuer: `enqueueWebhookErr error` field, and make EnqueueWebhookDeliver append to webhookCalls and return f.enqueueWebhookErr (currently it always returns nil) — mirroring EnqueueImageConvert's enqueueImageErr handling.
    - Add the three tests described in <behavior>, mirroring TestSweepSkipsDuplicateEnqueue's construction style. Assert store/enqueuer side effects only (imageCalls/webhookCalls/webhookGapRecoveredCalls lengths and ids) — do NOT assert metrics calls (no test hook; follow existing precedent).
    Ensure the fakeStore with an empty webhookGaps slice leaves all pre-existing tests green (they construct fakeStore without the new fields, which default to nil/empty).
  </action>
  <verify>
    <automated>cd /Users/apaderin/dev/octoconv && go test ./internal/reconciler/... -run 'TestSweep' -count=1 && go test ./internal/reconciler/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/reconciler/... -count=1` passes (all existing + new unit tests), with NO live DB/Redis required (pure in-memory fakes).
    - `grep -n 'TestSweepRecoversWebhookGap\|TestSweepSkipsDuplicateWebhookGap\|TestSweepWebhookGapFindErrorBestEffort' internal/reconciler/reconciler_test.go` shows all three added.
    - `grep -n 'enqueueWebhookErr' internal/reconciler/reconciler_test.go` matches (field + return).
    - The duplicate-skip test asserts len(webhookGapRecoveredCalls) == 0 when enqueueWebhookErr is asynq.ErrDuplicateTask.
  </acceptance_criteria>
  <done>The webhook-gap sweep loop is unit-tested for the recovery, duplicate-skip, and find-error paths; all reconciler unit tests pass without a live DB.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reconciler goroutine → Redis/Postgres | Internal background sweep; no externally-reachable surface added |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering / DoS | sweep() webhook-gap re-enqueue | mitigate | Enqueue-first ordering + errors.Is(asynq.ErrDuplicateTask) skip relies on Plan 01's asynq.Unique lock: between "sweep enqueues" and the asynchronous webhook_deliveries row write, the same job matches FindWebhookGaps again next tick; the lock makes those repeat enqueues collide harmlessly instead of firing duplicate outbound POSTs. Verified by TestSweepSkipsDuplicateWebhookGap. |
| T-06-05 | DoS (false-positive re-delivery) | uncapped webhook-gap recoveries | accept | Webhook-gap recovery is one-shot per job: once EnqueueWebhookDeliver succeeds, the worker writes a webhook_deliveries row and FindWebhookGaps never matches that job again (D-05). It uses detailActionWebhookGapRecovered (not detailActionRecovery), so RecoveryCount never counts it toward MaxRecoveries — intentional and documented, no cap needed. |
| T-06-SC | Tampering | go module dependencies | accept | Zero new packages this phase (no go.mod change). |
</threat_model>

<verification>
- `go build ./...` and `go vet ./...` clean.
- `go test ./internal/reconciler/... -count=1` passes (existing + 3 new unit tests, no live services needed).
- sweep() performs the second FindWebhookGaps scan with enqueue-first + asynq.ErrDuplicateTask guard and dual observability.
</verification>

<success_criteria>
The reconciler recovers done/failed jobs whose webhook was silently never enqueued (RECON-04 SC1), never re-triggers delivery for jobs that already have a webhook_deliveries row including dead-lettered ones (SC2, enforced upstream by Plan 02's query + the duplicate-skip here), and records each recovery via job_events + metrics.
</success_criteria>

<output>
Create `.planning/phases/06-reconciler-webhook-gap-sweep-staleness-soak-test/06-03-SUMMARY.md` when done.
</output>
