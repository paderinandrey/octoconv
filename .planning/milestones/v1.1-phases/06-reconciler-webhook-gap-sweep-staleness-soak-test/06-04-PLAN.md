---
phase: 06-reconciler-webhook-gap-sweep-staleness-soak-test
plan: 04
type: execute
wave: 3
depends_on: ["06-03"]
files_modified:
  - internal/reconciler/reconciler_soak_test.go
autonomous: true
requirements: [RECON-05]

must_haves:
  truths:
    - "A job left genuinely stranded in queued/active past the staleness threshold is requeued/recovered by a live, running Sweeper.Run process within the expected sweep interval, using REAL elapsed wall-clock time — not a mocked/backdated clock (D-07, RECON-05 SC3)"
    - "A job exceeding MaxRecoveries under real elapsed time is terminally failed (status=failed), with the failure recorded in job_events (D-08, RECON-05 SC4)"
    - "The soak test uses a real jobs.Repo (live Postgres) paired with the existing in-memory fakeEnqueuer — NEVER a real queue.Client/Redis (avoids the hardcoded 2-minute uniqueTTLSafetyMargin blowing the time budget)"
    - "The whole soak test completes well under a minute of wall-clock time"
  artifacts:
    - path: "internal/reconciler/reconciler_soak_test.go"
      provides: "TestSoakRecoversStrandedQueuedJob + TestSoakExhaustsAtCap, real-clock integration tests with local newSoakTestPool/createSoakTestClient helpers"
      contains: "func TestSoakRecoversStrandedQueuedJob"
  key_links:
    - from: "reconciler_soak_test.go"
      to: "Sweeper.Run (real ticker)"
      via: "go s.Run(ctx) + real time.Sleep polling"
      pattern: "go s\\.Run\\(ctx\\)"
    - from: "reconciler_soak_test.go"
      to: "jobs.Repo + fakeEnqueuer"
      via: "NewSweeper(repo, &fakeEnqueuer{}, cfg)"
      pattern: "NewSweeper\\("
---

<objective>
Add RECON-05: an automated, real-wall-clock soak test proving the existing Phase 3 staleness recovery works under genuine elapsed time (not just mocked-clock unit tests). It constructs a real `Sweeper` with short-but-real `Config` durations, runs `Sweeper.Run` in a goroutine against a live Postgres `jobs.Repo`, and polls real wall-clock time to observe recovery and exhaustion.

Purpose: RECON-05 SC3/SC4 require proof under real elapsed time. This validates the reconciler behavior that RECON-04 extends; it does NOT change any recovery logic (Phase 3, unchanged).

Output: `internal/reconciler/reconciler_soak_test.go` (NEW FILE) with two soak tests and local live-DB helpers.
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-reconciler-webhook-gap-sweep-staleness-soak-test/06-CONTEXT.md
@.planning/phases/06-reconciler-webhook-gap-sweep-staleness-soak-test/06-RESEARCH.md
@.planning/phases/06-reconciler-webhook-gap-sweep-staleness-soak-test/06-PATTERNS.md

<interfaces>
This test file lives in `package reconciler` (same package as reconciler.go / reconciler_test.go), so it can use the unexported `fakeEnqueuer`, `NewSweeper`, `Config`, `Sweeper` directly. It CANNOT import internal/jobs's unexported test helpers (newTestRepo/createTestClient are in package jobs), so it must define its own local helpers.

Existing exports it builds against:
- `func NewSweeper(store jobStore, enq enqueuer, cfg Config) *Sweeper`
- `func (s *Sweeper) Run(ctx context.Context)` — ticks every cfg.SweepInterval, returns on ctx cancel.
- `Config{ QueuedStaleAfter, ActiveStaleAfter, SweepInterval time.Duration; MaxRecoveries int }`
- `fakeEnqueuer{}` (from reconciler_test.go) — imageCalls/webhookCalls tracking, all enqueues succeed by default (no live Redis).
- `jobs.NewRepo(pool *pgxpool.Pool) *Repo` — satisfies jobStore concretely (including the Plan 02/03 methods).
- `jobs.CreateParams{ ClientID uuid.UUID; Operation, Engine, SourceFormat, TargetFormat string; Input jobs.Input }`, `jobs.Input{ ObjectKey, Filename, Format string }`.
- `(*jobs.Repo).Create(ctx, CreateParams) (uuid.UUID, error)`, `.Get(ctx, id) (*jobs.Job, error)`.
- `jobs.StatusQueued`, `jobs.StatusActive`, `jobs.StatusFailed` string constants.
- `db.Connect(ctx) (*pgxpool.Pool, error)`, `db.Migrate(ctx, pool) error`.

Live-DB helper convention to replicate (from internal/jobs/repo_test.go lines 14-43): t.Skip if DATABASE_URL unset, db.Connect + db.Migrate, t.Cleanup(pool.Close); createTestClient inserts a minimal clients row via `INSERT INTO clients (name) VALUES ($1) RETURNING id`. Since pool is needed directly, expose a *pgxpool.Pool from the helper (createSoakTestClient runs the INSERT on the pool).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Real-wall-clock soak test for stranded-job recovery and cap exhaustion</name>
  <files>internal/reconciler/reconciler_soak_test.go</files>
  <read_first>
    - internal/reconciler/reconciler.go (Run/sweep/NewSweeper/Config — to construct the sweeper correctly)
    - internal/reconciler/reconciler_test.go (fakeEnqueuer definition — reused as-is, same package)
    - internal/jobs/repo_test.go (newTestRepo + createTestClient lines 14-43 — the live-DB setup convention to replicate as local helpers; TestFindStale and TestMarkFailed for the create + job_events-query idiom)
    - internal/jobs/repo.go (Create/Get signatures, StaleJob, status constants, RequeueStale/MarkFailed behavior)
    - .planning/phases/06-reconciler-webhook-gap-sweep-staleness-soak-test/06-RESEARCH.md (Code Examples: soak test skeleton; Common Pitfalls 3, 4, 5 — ALL load-bearing)
  </read_first>
  <action>
    Create internal/reconciler/reconciler_soak_test.go in `package reconciler`. Add local helpers `newSoakTestPool(t *testing.T) *pgxpool.Pool` (t.Skip if DATABASE_URL unset, db.Connect + db.Migrate, t.Cleanup(pool.Close)) and `createSoakTestClient(t *testing.T, pool *pgxpool.Pool) uuid.UUID` (INSERT INTO clients (name) VALUES ($1) RETURNING id), mirroring internal/jobs/repo_test.go's helpers (cannot import them cross-package).

    Test 1 — TestSoakRecoversStrandedQueuedJob (D-07 / SC3): create a real job via jobs.NewRepo(pool).Create(...); pair it with `enq := &fakeEnqueuer{}` (NO real queue.Client — this is the critical constraint from Pitfall 3: a real Redis asynq.Unique lock has a hardcoded 2-min floor and would blow the time budget). Build Config with short-but-REAL durations: QueuedStaleAfter=1s, ActiveStaleAfter=1s, SweepInterval=300ms, MaxRecoveries=2. Start `go s.Run(ctx)` (context.WithCancel, defer cancel). Poll with real time.Sleep(100ms) up to a 10s deadline until repo.Get shows status back to queued AND len(enq.imageCalls) >= 1, then return; fail if the deadline passes. Do NOT backdate created_at via SQL — the recovery must come from genuine elapsed wall-clock time crossing the 1s threshold.

    Test 2 — TestSoakExhaustsAtCap (D-08 / SC4): same real-Repo + fakeEnqueuer pairing, MaxRecoveries=2, short intervals. Start the sweeper and poll (generous ~15s deadline) until repo.Get shows status == jobs.StatusFailed. Then assert a job_events row exists recording the exhaustion (query job_events for the job id via the pool, following TestMarkFailed's detail-round-trip query style). Per Pitfall 4, do NOT assert a fixed sequence of recovery `reason` values — RequeueStale never resets created_at, so after the first recovery the job trips the queued-branch staleness on subsequent ticks; assert only the final terminal state + that at least one exhaustion job_events row exists.

    Use generous polling windows (Pitfall 5) to absorb Go/Postgres clock skew; never use tight exact-boundary sleeps.
  </action>
  <verify>
    <automated>cd /Users/apaderin/dev/octoconv && go vet ./internal/reconciler/... && go test ./internal/reconciler/... -run 'TestSoak' -count=1 -timeout 90s</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/reconciler/... -run 'TestSoak' -count=1 -timeout 90s` passes with DATABASE_URL set (source ./.env first); both soak tests skip cleanly when DATABASE_URL is unset (never fail hard).
    - `grep -n 'go s.Run(ctx)' internal/reconciler/reconciler_soak_test.go` matches (real background ticker, not a direct s.sweep() call).
    - `grep -c 'queue.NewClient\|queue.Client' internal/reconciler/reconciler_soak_test.go` == 0 (uses fakeEnqueuer, never a real queue client — the load-bearing Pitfall 3 constraint).
    - `grep -c 'UPDATE jobs SET created_at\|interval' internal/reconciler/reconciler_soak_test.go` == 0 (recovery driven by real elapsed time, not SQL backdating).
    - `grep -n 'jobs.StatusFailed' internal/reconciler/reconciler_soak_test.go` matches (exhaustion assertion) and the exhaustion test queries job_events.
    - Total `TestSoak` runtime is well under a minute (the 90s -timeout is headroom, not the expected duration).
    - `go vet ./internal/reconciler/...` clean.
  </acceptance_criteria>
  <done>Two real-wall-clock soak tests prove stranded-job recovery (SC3) and cap exhaustion with a job_events record (SC4), using a live Postgres jobs.Repo + in-memory fakeEnqueuer, completing well under a minute.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → Postgres | Test-only; no production runtime surface, no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | (reliability, not security) | soak test flakiness | mitigate | Use generous polling deadlines (10-15s) not tight boundary sleeps, absorbing Go/Postgres clock skew (Pitfall 5); pair real Repo with fakeEnqueuer to avoid the 2-min asynq.Unique floor (Pitfall 3). No security surface — this is a test-only addition. |
| T-06-SC | Tampering | go module dependencies | accept | Zero new packages this phase (no go.mod change). |
</threat_model>

<verification>
- `go vet ./internal/reconciler/...` clean.
- `go test ./internal/reconciler/... -run 'TestSoak' -count=1 -timeout 90s` passes with ./.env sourced; skips cleanly otherwise.
- No real queue.Client/Redis and no SQL backdating used — recovery/exhaustion driven by genuine elapsed wall-clock time.
</verification>

<success_criteria>
RECON-05 is satisfied: staleness recovery (SC3) and cap exhaustion with a job_events failure record (SC4) are proven by an automated test under real, unmocked wall-clock time, completing well under a minute.
</success_criteria>

<output>
Create `.planning/phases/06-reconciler-webhook-gap-sweep-staleness-soak-test/06-04-SUMMARY.md` when done.
</output>
