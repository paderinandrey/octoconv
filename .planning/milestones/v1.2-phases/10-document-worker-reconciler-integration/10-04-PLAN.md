---
phase: 10-document-worker-reconciler-integration
plan: 04
type: execute
wave: 3
depends_on: ["10-03"]
files_modified:
  - Dockerfile.worker
  - Dockerfile.document-worker
  - docker-compose.yml
  - .env.example
autonomous: true
requirements: [DOC-07]
must_haves:
  truths:
    - "Dockerfile.worker is libvips-only again — LibreOffice, its fonts, and tini are REMOVED from the image worker's attack surface (D-01), genuinely reducing risk, not relocating it in place"
    - "Dockerfile.document-worker builds cmd/document-worker with LibreOffice + fonts + tini-as-PID-1, isolating the heavy footprint to the document container (D-01, DOC-07)"
    - "docker-compose.yml runs a document-worker service with its own DOCUMENT_ENGINE_TIMEOUT/DOCUMENT_WORKER_CONCURRENCY and the SAME cpus:2.0/memory:1g limits as the image worker (D-02)"
    - ".env.example documents DOCUMENT_WORKER_CONCURRENCY, DOCUMENT_ENGINE_TIMEOUT, and DOCUMENT_MAX_RETRY with the established inline-comment style"
  artifacts:
    - path: Dockerfile.worker
      provides: "reverted libvips-only image worker runtime"
      contains: "libvips-tools"
    - path: Dockerfile.document-worker
      provides: "LibreOffice document-worker runtime image"
      contains: "cmd/document-worker"
    - path: docker-compose.yml
      provides: "document-worker service definition"
      contains: "octoconv-document-worker"
    - path: .env.example
      provides: "document worker env var documentation"
      contains: "DOCUMENT_WORKER_CONCURRENCY"
  key_links:
    - from: docker-compose.yml
      to: Dockerfile.document-worker
      via: "document-worker service build.dockerfile"
      pattern: "dockerfile: Dockerfile.document-worker"
    - from: Dockerfile.document-worker
      to: cmd/document-worker
      via: "go build -o /out/document-worker ./cmd/document-worker"
      pattern: "./cmd/document-worker"
---

<objective>
Correct the Docker image split that Phase 9 pragmatically violated. Revert `Dockerfile.worker` to libvips-only (drop LibreOffice, its three font packages, and tini), and create `Dockerfile.document-worker` carrying that exact LibreOffice + font + tini-as-PID-1 provisioning but building `cmd/document-worker`. Add a `document-worker` docker-compose service with its own env and identical resource limits, and document the new env vars in `.env.example`.

Purpose: DOC-07 / D-01 — LibreOffice's heavy footprint and larger attack surface must never touch the image worker's container. This is the milestone-level locked decision (separate binary, separate image) that this phase finally realizes. The security requirement is explicit: the split must genuinely REMOVE LibreOffice from Dockerfile.worker, not just move risk around.

Output: Two clean single-purpose runtime Dockerfiles, a document-worker compose service, and documented env vars.
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-document-worker-reconciler-integration/10-CONTEXT.md
@.planning/phases/10-document-worker-reconciler-integration/10-PATTERNS.md
@.planning/phases/09-libreoffice-converter-engine/09-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Revert Dockerfile.worker to libvips-only and create Dockerfile.document-worker</name>
  <files>Dockerfile.worker, Dockerfile.document-worker</files>
  <read_first>
    - Dockerfile.worker (current state — has Phase 9's LibreOffice/tini/font additions and the tini ENTRYPOINT to remove; the LibreOffice+font+tini block to relocate verbatim)
    - Dockerfile.api (the slim single-purpose runtime shape to match: ca-certificates-only runtime, direct ENTRYPOINT, USER nobody)
    - Dockerfile.worker-test (confirm it is FROM golang:1.26-bookworm with its own LibreOffice install — independent of both runtime Dockerfiles, so it is NOT touched by this split)
    - .planning/phases/10-document-worker-reconciler-integration/10-PATTERNS.md (the Dockerfile.worker / Dockerfile.document-worker section with exact before/after contents)
  </read_first>
  <action>
    Revert Dockerfile.worker: keep the build stage building `./cmd/worker`; in the runtime stage keep only `ca-certificates` and `libvips-tools`, and REMOVE `tini`, `libreoffice-writer-nogui`, `libreoffice-calc-nogui`, `libreoffice-impress-nogui`, `fonts-crosextra-carlito`, `fonts-crosextra-caladea`, and `fonts-liberation2`. Change the ENTRYPOINT from the tini wrapper back to a direct `["/usr/local/bin/worker"]` (the image engine is a single synchronous libvips CLI invocation with no forking-daemon orphan risk, so no init/reaper is needed). Remove the tini-as-PID-1 comment block and fix the stale runtime-stage comment so it no longer mentions LibreOffice (image engine needs only libvips). Keep `USER nobody`. Then create Dockerfile.document-worker: mirror Dockerfile.api's build/runtime two-stage shape, build `-o /out/document-worker ./cmd/document-worker`, and in the runtime stage install `ca-certificates`, `tini`, `libreoffice-writer-nogui`, `libreoffice-calc-nogui`, `libreoffice-impress-nogui`, `fonts-crosextra-carlito`, `fonts-crosextra-caladea`, `fonts-liberation2` (copy this exact package set verbatim from Dockerfile.worker's pre-revert state — Phase 9's 09-RESEARCH.md already live-verified this set at 4:7.4.7-1+deb12u13 with all tracked CVEs fixed; do NOT re-verify). Copy the file to /usr/local/bin/document-worker, set USER nobody, and use `ENTRYPOINT ["/usr/bin/tini", "--", "/usr/local/bin/document-worker"]`. Preserve the tini-as-PID-1 comment verbatim (it documents the live-verified 09-02 correctness requirement that a SIGKILLed soffice.bin reparents to PID 1 and must be reaped). Do NOT modify Dockerfile.worker-test.
  </action>
  <verify>
    <automated>grep -v '^#' Dockerfile.worker | grep -c 'libreoffice\|tini\|fonts-' ; grep -v '^#' Dockerfile.document-worker | grep -c 'libreoffice-writer-nogui'</automated>
  </verify>
  <acceptance_criteria>
    - Dockerfile.worker, non-comment lines, contain ZERO occurrences of libreoffice/tini/fonts- : `grep -v '^#' Dockerfile.worker | grep -c 'libreoffice\|tini\|fonts-'` returns 0 (fail-closed attack-surface removal proof).
    - Dockerfile.worker non-comment lines still contain `libvips-tools` and its ENTRYPOINT is `["/usr/local/bin/worker"]` (no tini wrapper).
    - Dockerfile.document-worker exists, builds `./cmd/document-worker` (`grep './cmd/document-worker' Dockerfile.document-worker` matches), and its runtime installs all three libreoffice-*-nogui packages and all three font packages plus tini.
    - Dockerfile.document-worker ENTRYPOINT is `["/usr/bin/tini", "--", "/usr/local/bin/document-worker"]`.
    - Dockerfile.worker-test is byte-for-byte unchanged (`git diff --exit-code Dockerfile.worker-test` returns clean).
  </acceptance_criteria>
  <done>The image worker is libvips-only again and LibreOffice lives exclusively in the document-worker image with tini as PID 1.</done>
</task>

<task type="auto">
  <name>Task 2: Add the document-worker compose service and document the env vars</name>
  <files>docker-compose.yml, .env.example</files>
  <read_first>
    - docker-compose.yml (the worker service block lines 95-124 to mirror; insert the new service after it and before the asynqmon service at line 131)
    - .env.example (the # Worker section lines with WORKER_CONCURRENCY/ENGINE_TIMEOUT/IMAGE_MAX_RETRY — mirror the inline-comment style; note existing # Reconciler / # Storage lifecycle section-header convention)
    - internal/queue/client.go (confirm the DOCUMENT_MAX_RETRY default 3 and DOCUMENT_ENGINE_TIMEOUT default 300s chosen in Plan 10-01 so .env.example documents matching values)
    - cmd/document-worker/main.go (confirm DOCUMENT_WORKER_CONCURRENCY default 2 and that WEBHOOK_SIGNING_SECRET is not required)
  </read_first>
  <action>
    In docker-compose.yml, add a `document-worker` service after the `worker` block (before `asynqmon`), mirroring the worker service exactly with these differences: `build.dockerfile: Dockerfile.document-worker`, `container_name: octoconv-document-worker`, same postgres/redis/minio depends_on healthchecks, the same DATABASE_URL/REDIS_ADDR/S3_* environment values, but replace `WORKER_CONCURRENCY: "4"` with `DOCUMENT_WORKER_CONCURRENCY: "2"` and `ENGINE_TIMEOUT: "120s"` with `DOCUMENT_ENGINE_TIMEOUT: "300s"`, keep `METRICS_ADDR: "127.0.0.1:9090"` (separate container network namespace, no collision with the worker), and OMIT `WEBHOOK_SIGNING_SECRET` (D-06: document-worker does not deliver/sign webhooks). Set the SAME `deploy.resources.limits` of `cpus: "2.0"` and `memory: 1g` (D-02 — explicitly identical to the image worker's envelope). In .env.example, add a new `# Document worker` section (matching the file's one-section-per-concern convention) with: `DOCUMENT_WORKER_CONCURRENCY=2` (inline comment: lower than WORKER_CONCURRENCY's 4 — soffice.bin's heavier per-conversion memory footprint within the same cpus:2.0/memory:1g ceiling, D-02/D-03), `DOCUMENT_ENGINE_TIMEOUT=300s` (inline comment: Phase 9 D-01), and `DOCUMENT_MAX_RETRY=3` (inline comment: bounded retry budget for document conversion, mirrors IMAGE_MAX_RETRY; also feeds the derived per-job uniqueness-lock TTL together with DOCUMENT_ENGINE_TIMEOUT). Use the trailing `# comment` inline style already used throughout the file (tolerated by firstField).
  </action>
  <verify>
    <automated>docker compose config -q && grep -c 'DOCUMENT_WORKER_CONCURRENCY\|DOCUMENT_ENGINE_TIMEOUT\|DOCUMENT_MAX_RETRY' .env.example</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose config -q` exits 0 (valid compose after adding the service). If the docker CLI is unavailable in the execution environment, fall back to `python3 -c "import yaml,sys; yaml.safe_load(open('docker-compose.yml'))"` returning 0.
    - `grep 'octoconv-document-worker' docker-compose.yml` matches and the service's `build.dockerfile` is `Dockerfile.document-worker`.
    - The document-worker service sets `DOCUMENT_WORKER_CONCURRENCY: "2"` and `DOCUMENT_ENGINE_TIMEOUT: "300s"`, sets NO `WEBHOOK_SIGNING_SECRET`, and its resource limits are `cpus: "2.0"` / `memory: 1g`.
    - `grep -c 'DOCUMENT_WORKER_CONCURRENCY\|DOCUMENT_ENGINE_TIMEOUT\|DOCUMENT_MAX_RETRY' .env.example` returns 3 (all three documented), each with an inline `#` comment.
    - The `worker` service block is otherwise unchanged (still references Dockerfile.worker, WORKER_CONCURRENCY, ENGINE_TIMEOUT, WEBHOOK_SIGNING_SECRET).
  </acceptance_criteria>
  <done>docker-compose runs an isolated document-worker service with its own concurrency/timeout and identical resource limits, and all document env vars are documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| container image → host | Each runtime image's installed package set defines its attack surface. The image worker must not carry LibreOffice (large surface) it never uses. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-08 | Elevation of Privilege / Tampering | Dockerfile.worker attack surface | mitigate | Revert genuinely REMOVES LibreOffice + fonts + tini from the image worker image (verified fail-closed by the non-comment grep returning 0), reducing its attack surface — the heavy engine lives only in Dockerfile.document-worker. Both images run USER nobody. |
| T-10-09 | Denial of Service | document container resource envelope | accept | cpus:2.0/memory:1g limits (D-02) are a reasoned default, not empirically measured against a document corpus — accepted, adjustable later, same posture as DOCUMENT_ENGINE_TIMEOUT's 300s. |
| T-10-SC | Tampering | apt package set in Dockerfile.document-worker | accept | LibreOffice/font package set copied verbatim from Phase 9's live-verified set (09-RESEARCH.md, 4:7.4.7-1+deb12u13, all CVEs fixed) — not an npm/pip/cargo install, so the package legitimacy gate does not apply; no new packages introduced. |
</threat_model>

<verification>
- `grep -v '^#' Dockerfile.worker | grep -c 'libreoffice\|tini\|fonts-'` returns 0.
- `docker compose config -q` (or YAML parse fallback) succeeds.
- `git diff --exit-code Dockerfile.worker-test` is clean.
- Full container image build + live document conversion smoke is intentionally deferred to Phase 11's end-to-end verification (roadmap Phase 11 success criterion 4).
</verification>

<success_criteria>
- Image worker is libvips-only; document worker image carries LibreOffice + tini and builds cmd/document-worker.
- Compose runs an isolated document-worker service with its own env and D-02 resource limits; env vars documented.
</success_criteria>

<output>
Create `.planning/phases/10-document-worker-reconciler-integration/10-04-SUMMARY.md` when done.
</output>
