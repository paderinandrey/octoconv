---
phase: 11-api-routing-end-to-end-document-conversion
plan: 03
type: execute
wave: 3
depends_on: [11-02]
files_modified: []
autonomous: false
requirements: [DOC-10]

must_haves:
  truths:
    - "The full docker-compose stack (api + worker + document-worker + postgres/redis/minio) builds and reaches a healthy state"
    - "All 6 document format pairs convert live (docx/xlsx/pptx/odt/ods/odp → pdf) through upload → convert → download, each yielding a %PDF- output"
    - "The signed webhook fires for the completed document job and is received by the in-test receiver"
    - "A human confirms the live E2E run passed across all 6 pairs before the phase is considered complete"
  artifacts:
    - path: ".planning/phases/11-api-routing-end-to-end-document-conversion/11-03-SUMMARY.md"
      provides: "captured E2E run output + pass/fail per pair"
  key_links:
    - from: "internal/e2e/e2e_test.go"
      to: "live stack via E2E_BASE_URL"
      via: "go test with env pointing at the running API"
      pattern: "E2E_BASE_URL"
---

<objective>
Run the committed E2E suite against a real, freshly-built docker-compose stack and confirm the full document pipeline works end-to-end across all 6 format pairs, including the signed webhook and presigned download. This is the milestone's final acceptance gate (SC#4) — the "live e2e verified" bar every prior phase met.

Purpose: Code that compiles and self-skips proves nothing about a live LibreOffice conversion. This plan actually stands up the stack (including the heavy document-worker image), executes the gated suite, and has a human confirm the result before v1.2 is done.
Output: A captured E2E run and a human-verified pass across all 6 pairs.
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-api-routing-end-to-end-document-conversion/11-CONTEXT.md

<interfaces>
<!-- What the run needs. -->
Stack: docker compose -f docker-compose.yml -f docker-compose.e2e.yml up -d --build
  Services: postgres(:5434), redis, minio(:9100), createbucket, api(:8090), worker, document-worker.
  The document-worker image is heavy (LibreOffice) — first build can take several minutes.

Gating env for the suite (from Plan 11-02):
  E2E_BASE_URL=http://localhost:8090
  DATABASE_URL=postgres://octo:octo-pass@localhost:5434/octo_db
  API_KEY_SALT=dev-only-change-me-in-real-deploys   (MUST equal the api service's API_KEY_SALT)
  E2E_WEBHOOK_HOST=host.docker.internal             (default; the callback host containers reach)

Run: go test ./internal/e2e/ -run E2E -count=1 -v -timeout 20m
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stand up the stack and run the live E2E suite</name>
  <files>none (operational — captures output into the SUMMARY)</files>
  <read_first>
    - docker-compose.yml (service names, ports 8090/5434/9100, healthchecks)
    - docker-compose.e2e.yml (the E2E override created in Plan 11-02: SSRF opt-out + host-gateway)
    - internal/e2e/e2e_test.go (the gating env vars it reads: E2E_BASE_URL, DATABASE_URL, API_KEY_SALT, E2E_WEBHOOK_HOST)
  </read_first>
  <action>
    Bring up the full stack with the E2E override: `docker compose -f docker-compose.yml -f docker-compose.e2e.yml up -d --build`. Wait for postgres/redis/minio to report healthy and for the api/worker/document-worker containers to be running (poll `docker compose ps` / container health; do not use a blind fixed sleep). Confirm the API is reachable: `curl -fsS http://localhost:8090/healthz` returns a 200 with all three dependencies "ok". Then run the suite with the gating env exported to match the running api's values:
      E2E_BASE_URL=http://localhost:8090
      DATABASE_URL=postgres://octo:octo-pass@localhost:5434/octo_db
      API_KEY_SALT=dev-only-change-me-in-real-deploys
      E2E_WEBHOOK_HOST=host.docker.internal
    Invoke `go test ./internal/e2e/ -run E2E -count=1 -v -timeout 20m` and capture the full output.

    If a document job goes to `failed` or the webhook does not arrive, inspect `docker compose logs document-worker` and `docker compose logs worker` (the webhook consumer/deliverer), diagnose, and — if it is an environment/config issue rather than a code defect (e.g. host.docker.internal unreachable on this Linux host, or API_KEY_SALT mismatch) — correct the run configuration and retry. If it is a code defect in the routing (Plan 11-01) or the test (Plan 11-02), STOP and report it as a gap rather than editing those plans' outputs here. Record the per-pair pass/fail matrix and the webhook result in the SUMMARY. Leave the stack running (or note teardown) for the human-verify checkpoint that follows.
  </action>
  <verify>
    <automated>curl -fsS http://localhost:8090/healthz | grep -q '"status":"ok"'</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose -f docker-compose.yml -f docker-compose.e2e.yml ps` shows api, worker, and document-worker running and postgres/redis/minio healthy
    - `curl -fsS http://localhost:8090/healthz` returns HTTP 200 with postgres/redis/s3 all "ok"
    - `go test ./internal/e2e/ -run E2E -count=1 -v -timeout 20m` runs the suite (does NOT skip) and reports PASS for all 6 format pairs
    - The captured output shows a received webhook (X-OctoConv-Signature present) for the webhook-covered pair
    - The per-pair result matrix and webhook outcome are recorded in 11-03-SUMMARY.md
  </acceptance_criteria>
  <done>The live stack is healthy, the E2E suite runs (not skips) and passes all 6 pairs plus the webhook, and the run output is captured in the SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human confirms the live end-to-end document pipeline</name>
  <what-built>
    Engine-aware routing (Plan 11-01) plus a live E2E run (Task 1 above) that converted all 6 office
    document formats to PDF through the real docker-compose stack — the same upload → convert →
    download → webhook pipeline used for images — proving DOC-10 and the v1.2 milestone's final
    success criteria.
  </what-built>
  <how-to-verify>
    1. Review the per-pair result matrix in 11-03-SUMMARY.md — all six of docx/xlsx/pptx/odt/ods/odp → pdf must show PASS.
    2. Optionally re-run yourself against the running stack:
       `E2E_BASE_URL=http://localhost:8090 DATABASE_URL=postgres://octo:octo-pass@localhost:5434/octo_db API_KEY_SALT=dev-only-change-me-in-real-deploys E2E_WEBHOOK_HOST=host.docker.internal go test ./internal/e2e/ -run E2E -count=1 -v -timeout 20m`
    3. Confirm the captured output shows a signed webhook was received for the webhook-covered pair and each downloaded output began with %PDF-.
    4. If satisfied, the v1.2 Document Engine Class milestone is functionally complete; you may tear down the stack with `docker compose -f docker-compose.yml -f docker-compose.e2e.yml down -v`.
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 pairs + the webhook passed, or describe which pair(s) failed and what the logs showed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → docker-compose stack | ephemeral local/CI stack built and run for acceptance only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07 | Elevation of Privilege | E2E stack uses relaxed SSRF guard | accept | The relaxed guard comes only from docker-compose.e2e.yml (E2E override); it is applied to a throwaway local stack for the duration of the acceptance run and torn down after. Production compose defaults are unaffected. |
| T-11-08 | Denial of Service | LibreOffice document-worker resource use | accept | The document-worker container is CPU/RAM-limited (2 CPU / 1 GiB) and concurrency-capped (DOCUMENT_WORKER_CONCURRENCY=2) per Phase 10; the E2E run drives at most a handful of small fixtures. Bounded by DOCUMENT_ENGINE_TIMEOUT. |
| T-11-SC | Tampering | package installs | accept | No new packages installed; this plan only runs docker compose and `go test` against already-built images and committed code. |
</threat_model>

<verification>
- Stack builds and reaches healthy state.
- `go test ./internal/e2e/ -run E2E` passes all 6 pairs live (does not skip).
- Signed webhook received for the webhook-covered pair.
- Human approves the run.
</verification>

<success_criteria>
- All 6 document format pairs convert live through the full pipeline. [SC#4]
- GET /v1/jobs/{id} returns a working presigned download URL for completed document jobs, verified by %PDF- bytes. [SC#2]
- Webhook delivery fires for a completed document job via the existing signed pipeline. [SC#3]
- A human confirms the live run. DOC-10 satisfied; v1.2 milestone functionally complete.
</success_criteria>

<output>
Create `.planning/phases/11-api-routing-end-to-end-document-conversion/11-03-SUMMARY.md` when done, including the per-pair pass/fail matrix and the webhook result.
</output>
