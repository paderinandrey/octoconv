---
phase: 11-api-routing-end-to-end-document-conversion
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/convert/sniff.go
  - internal/convert/sniff_test.go
  - internal/api/handlers_test.go
autonomous: true
gap_closure: true
requirements: [DOC-10]

must_haves:
  truths:
    - "convert.MIMEType(\"pdf\") returns application/pdf (not application/octet-stream)"
    - "convert.MIMEType returns the canonical OOXML/ODF MIME type for each of docx/xlsx/pptx/odt/ods/odp"
    - "A completed document job's stored/served Content-Type matches its format, identically to how image jobs get a per-format Content-Type"
  artifacts:
    - path: "internal/convert/sniff.go"
      provides: "MIMEType switch cases for pdf + 6 document source formats"
      contains: "application/pdf"
    - path: "internal/convert/sniff_test.go"
      provides: "TestMIMEType coverage for pdf + 6 document formats"
      contains: "application/vnd.oasis.opendocument.text"
    - path: "internal/api/handlers_test.go"
      provides: "Content-Type parity assertions on document handler tests"
      contains: "store.contentType"
  key_links:
    - from: "internal/api/handlers.go:231"
      to: "convert.MIMEType"
      via: "contentType := convert.MIMEType(detected)"
      pattern: "convert\\.MIMEType\\(detected\\)"
    - from: "internal/worker/worker.go:409"
      to: "convert.MIMEType"
      via: "uploadFrom(... convert.MIMEType(job.TargetFormat))"
      pattern: "convert\\.MIMEType\\(job\\.TargetFormat\\)"
---

<objective>
Close the single verification gap from 11-VERIFICATION.md (SC#2, DOC-10): `convert.MIMEType` (internal/convert/sniff.go) has switch cases only for the five image formats, so document uploads (internal/api/handlers.go:231) and converted PDF outputs (internal/worker/worker.go:409,420) are stored/served with `application/octet-stream` instead of their canonical MIME types — unlike image jobs, which get a correct per-format Content-Type. This breaks SC#2's literal "identically to image jobs" claim.

Extend `convert.MIMEType` with the canonical MIME types for `pdf` and the six document source formats, and add tests that lock the parity in (unit-level in the convert package, and handler-level Content-Type assertions mirroring the existing PNG assertion). This is the exact fix already specified in 11-REVIEW.md WR-01.

Purpose: Make DOC-10's "identically to image jobs" true — a document job's presigned download serves `application/pdf`, exactly as an image job serves `image/png`.
Output: Extended MIMEType table + regression tests. No new dependencies, no production behavior change beyond the corrected Content-Type string.
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/11-api-routing-end-to-end-document-conversion/11-VERIFICATION.md
@.planning/phases/11-api-routing-end-to-end-document-conversion/11-REVIEW.md

<interfaces>
<!-- Exact identifiers the executor needs. No codebase exploration required. -->

From internal/convert/sniff.go (the function to extend):
```go
// MIMEType returns the canonical MIME type for a registered format, or
// "application/octet-stream" for anything unrecognized. This is the single
// home for format->MIME mapping shared by internal/api (stored Content-Type,
// D-06) and internal/worker (output Content-Type).
func MIMEType(format string) string {
	switch NormalizeFormat(format) {
	case "png":
		return "image/png"
	// ... four more image cases ...
	default:
		return "application/octet-stream"
	}
}
```

From internal/convert/convert.go — NormalizeFormat only aliases jpeg->jpg and tif->tiff; `pdf`, `docx`, `xlsx`, `pptx`, `odt`, `ods`, `odp` normalize to themselves, so a plain `case "pdf":` etc. matches directly.

Canonical MIME strings to add (exact, from WR-01):
- pdf  -> application/pdf
- docx -> application/vnd.openxmlformats-officedocument.wordprocessingml.document
- xlsx -> application/vnd.openxmlformats-officedocument.spreadsheetml.sheet
- pptx -> application/vnd.openxmlformats-officedocument.presentationml.presentation
- odt  -> application/vnd.oasis.opendocument.text
- ods  -> application/vnd.oasis.opendocument.spreadsheet
- odp  -> application/vnd.oasis.opendocument.presentation

Call sites (do NOT modify — they already call MIMEType correctly; they just inherit the corrected mapping):
- internal/api/handlers.go:231 — `contentType := convert.MIMEType(detected)`
- internal/worker/worker.go:409,420 — `convert.MIMEType(job.TargetFormat)`

Existing test to extend (internal/convert/sniff_test.go:141-156):
```go
func TestMIMEType(t *testing.T) {
	cases := map[string]string{
		"png":     "image/png",
		// ...
		"unknown": "application/octet-stream",
	}
	// asserts MIMEType(format) == want for each
}
```

Existing handler Content-Type assertion pattern to mirror (internal/api/handlers_test.go:336-338, in TestCreateJob_OK):
```go
if store.contentType != "image/png" {
	t.Errorf("contentType = %q, want image/png (detected format, not client header, D-06)", store.contentType)
}
```
The fakeStorage at handlers_test.go:56-66 records the Content-Type passed to Upload into `store.contentType`. The document tests to extend are `TestCreateJob_DocumentDetectedAndAccepted` (docx input, handlers_test.go:515) and `TestCreateJob_ODFDetectedAndAccepted` (odt input, handlers_test.go:554); both already use a `store *fakeStorage` and assert `store.uploaded` but not `store.contentType`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend convert.MIMEType with pdf + 6 document formats and lock it with a unit test</name>
  <files>internal/convert/sniff.go, internal/convert/sniff_test.go</files>
  <read_first>
    - internal/convert/sniff.go (the MIMEType function at lines 95-114 — the file being modified)
    - internal/convert/sniff_test.go (TestMIMEType at lines 141-156 — the test being extended)
    - internal/convert/convert.go lines 31-43 (NormalizeFormat — confirms pdf/docx/... normalize to themselves)
    - .planning/phases/11-api-routing-end-to-end-document-conversion/11-REVIEW.md (WR-01, the authoritative fix spec)
  </read_first>
  <action>
    In `internal/convert/sniff.go`, add seven `case` entries to the existing `switch NormalizeFormat(format)` in `MIMEType`, before the `default:` arm, returning the exact canonical MIME strings listed in the `<interfaces>` block (per WR-01): `pdf` -> `application/pdf`; `docx`/`xlsx`/`pptx` -> the OOXML wordprocessingml.document / spreadsheetml.sheet / presentationml.presentation types; `odt`/`ods`/`odp` -> the OASIS opendocument text / spreadsheet / presentation types. Do not alter the existing five image cases, the `default` arm, or the doc comment's intent (you may extend the doc comment to note it now also covers document formats + pdf, per project convention that MIMEType is the single format->MIME home). Keep gofmt-clean tab indentation and the existing case ordering style.

    In `internal/convert/sniff_test.go`, extend the `cases` map in `TestMIMEType` with the seven new format->MIME assertions (matching the strings added above), keeping the existing image and `"unknown"` entries. This is the parity lock at the unit level — a future regression that drops any case fails here.

    Scope guard: do NOT touch internal/api/handlers.go or internal/worker/worker.go — they already call `convert.MIMEType(...)` correctly and inherit the fix. Do NOT implement WR-02/WR-03/WR-04 (out of scope for this gap plan).
  </action>
  <verify>
    <automated>go test ./internal/convert/ -run TestMIMEType -count=1 -v && go vet ./internal/convert/ && gofmt -l internal/convert/sniff.go internal/convert/sniff_test.go</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/convert/ -run TestMIMEType -count=1` passes (PASS, no failures).
    - `MIMEType("pdf")` returns exactly `application/pdf`.
    - `MIMEType("docx")` returns exactly `application/vnd.openxmlformats-officedocument.wordprocessingml.document`; `xlsx`/`pptx` return their OOXML types; `odt`/`ods`/`odp` return their OASIS opendocument types (all exact strings per WR-01).
    - `MIMEType("unknown")` still returns `application/octet-stream` and the five image formats are unchanged.
    - `gofmt -l internal/convert/sniff.go internal/convert/sniff_test.go` prints nothing (both files gofmt-clean); `go vet ./internal/convert/` is clean.
  </acceptance_criteria>
  <done>convert.MIMEType returns canonical MIME types for pdf and all six document formats, covered by TestMIMEType, with image formats and the octet-stream default unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Assert Content-Type parity in the document handler tests</name>
  <files>internal/api/handlers_test.go</files>
  <read_first>
    - internal/api/handlers_test.go lines 56-66 (fakeStorage.Upload — records store.contentType)
    - internal/api/handlers_test.go lines 317-338 (TestCreateJob_OK — the image PNG Content-Type assertion to mirror)
    - internal/api/handlers_test.go lines 511-588 (TestCreateJob_DocumentDetectedAndAccepted and TestCreateJob_ODFDetectedAndAccepted — the two tests to extend)
    - internal/api/handlers.go line 231 (contentType := convert.MIMEType(detected) — the code path under test)
  </read_first>
  <action>
    In `internal/api/handlers_test.go`, add a `store.contentType` assertion to each of the two document tests, mirroring the existing PNG assertion at lines 336-338:
    - In `TestCreateJob_DocumentDetectedAndAccepted` (docx upload), assert `store.contentType` equals `application/vnd.openxmlformats-officedocument.wordprocessingml.document` — the canonical docx MIME the stored upload must carry (the input's detected format, D-06).
    - In `TestCreateJob_ODFDetectedAndAccepted` (odt upload), assert `store.contentType` equals `application/vnd.oasis.opendocument.text`.
    Place each assertion alongside the existing `store.uploaded` check, with an error message following the existing style (name the wanted MIME and cite D-06 / detected-format-not-client-header). These assertions exercise the real `convert.MIMEType` (not a fake), so they fail if Task 1's mapping regresses. Do not change routing/engine/queue assertions already present in these tests, and do not add new tests beyond these two assertions.
  </action>
  <verify>
    <automated>go test ./internal/api/ -run 'TestCreateJob_DocumentDetectedAndAccepted|TestCreateJob_ODFDetectedAndAccepted' -count=1 -v && go vet ./internal/api/ && gofmt -l internal/api/handlers_test.go</automated>
  </verify>
  <acceptance_criteria>
    - Both `TestCreateJob_DocumentDetectedAndAccepted` and `TestCreateJob_ODFDetectedAndAccepted` pass with the new `store.contentType` assertions present.
    - Reverting Task 1's sniff.go change would make these two tests FAIL on the contentType assertion (parity is genuinely locked to the real MIMEType mapping, not a fake) — confirm by reasoning about the code path (fakeStorage records the value handleCreateJob computed via convert.MIMEType).
    - `gofmt -l internal/api/handlers_test.go` prints nothing; `go vet ./internal/api/` is clean.
  </acceptance_criteria>
  <done>Both document handler tests assert the stored Content-Type equals the canonical docx/odt MIME type, giving handler-level parity with the existing image (image/png) assertion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (upload) | Untrusted multipart upload; detected format drives the stored Content-Type string |
| S3 → presigned download consumer | Content-Type header served to whoever holds the presigned URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04-01 | Tampering | `convert.MIMEType` mapping | accept | Content-Type is derived from server-side magic-byte detection (`convert.Sniff`), not the client-supplied header (D-06); the added cases are a fixed closed table of constant strings — no client input reaches the return value. |
| T-11-04-02 | Information Disclosure | presigned download Content-Type | accept | Correcting `application/octet-stream` → `application/pdf` reveals only the format, which the requester already knows (they submitted/own the job). Internal-services-only clients per CLAUDE.md. No new data exposed. |
| T-11-04-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — pure stdlib Go edits to two existing files plus one test file. No dependency surface added. |
</threat_model>

<verification>
- `go build ./...` succeeds.
- `go vet ./...` is clean.
- `go test ./internal/convert/ ./internal/api/ -count=1` passes (both extended tests green).
- Manual reasoning trace: `convert.MIMEType("pdf")` == `application/pdf`, so the worker's PDF output upload (worker.go:409,420) and the document input upload (handlers.go:231) now carry correct Content-Type — SC#2 "identically to image jobs" holds at the code-path level.
</verification>

<success_criteria>
- SC#2 gap from 11-VERIFICATION.md closed: document jobs' stored/served Content-Type equals their canonical MIME type (pdf → application/pdf), matching image-job behavior.
- `convert.MIMEType` covers pdf + docx/xlsx/pptx/odt/ods/odp with the exact WR-01 strings, locked by both a convert-package unit test and two handler-level assertions.
- No production code behavior changed beyond the corrected Content-Type; no new dependencies; gofmt/vet clean.
- WR-02/WR-03/WR-04 explicitly NOT addressed (advisory review findings, out of gap scope).
</success_criteria>

<output>
Create `.planning/phases/11-api-routing-end-to-end-document-conversion/11-04-SUMMARY.md` when done.
</output>
