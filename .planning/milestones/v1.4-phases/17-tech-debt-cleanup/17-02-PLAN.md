---
phase: 17-tech-debt-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/e2e/testdata/sample.png
  - internal/e2e/e2e_test.go
autonomous: true
requirements: [DEBT-08]

must_haves:
  truths:
    - "internal/e2e/testdata/sample.png is a real PNG that passes convert.Sniff (magic bytes) and the declared-dimension check"
    - "Against a live compose stack, TestImageConversionE2E RUNS (not skips) and reports `--- PASS`, driving a full upload → convert (libvips png→jpg) → download → HMAC-verified webhook cycle"
    - "TestImageConversionE2E self-skips without E2E_BASE_URL, keeping `go test ./...` green offline (additional cheap check, not the terminal proof)"
  artifacts:
    - path: "internal/e2e/testdata/sample.png"
      provides: "small real PNG fixture for the image-engine E2E test"
    - path: "internal/e2e/e2e_test.go"
      provides: "TestImageConversionE2E + assertDownloadIsImage helper"
      contains: "func TestImageConversionE2E"
  key_links:
    - from: "internal/e2e/e2e_test.go (TestImageConversionE2E)"
      to: "convert.Sniff"
      via: "download assertion sniffs the fetched bytes as the target image format"
      pattern: "convert\\.Sniff"
    - from: "internal/e2e/e2e_test.go (TestImageConversionE2E)"
      to: "assertSignedWebhook"
      via: "callback_url registered at the in-test receiver, signed webhook asserted"
      pattern: "assertSignedWebhook"
---

<objective>
Add the missing image-engine (libvips) E2E test — the last gap in `internal/e2e`'s format-matrix — so the v1.4 live-E2E CI tier exercises the image path, not just document and HTML.

The test mirrors TestDocumentConversionE2E exactly but for the image engine: multipart upload of a real PNG → poll to done → presigned download (assert the bytes sniff as the target image format) → assert a signed, HMAC-verified webhook arrives. It self-skips without E2E_BASE_URL like every other E2E test, but the terminal proof for this plan is a LIVE compose-stack run where it actually PASSES.

Purpose: unblock the Phase 19 live-E2E CI tier, which must not be enabled while the image path has no E2E coverage.
Output: a committed PNG fixture + TestImageConversionE2E driving the full image pipeline live.
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Existing E2E helpers (internal/e2e/e2e_test.go) to REUSE verbatim — do NOT
     reimplement: -->
- e2eSetup(t) e2eConfig                                  // self-skips w/o E2E_BASE_URL
- provisionClient(t) string                              // creates client row, returns raw API key
- postJob(t, baseURL, apiKey, filename, data, target, callbackURL, opts) string
- pollUntilDone(t, baseURL, apiKey, jobID, timeout) map[string]any
- startWebhookReceiver(t, webhookHost) (callbackURL string, <-chan webhookHit)
- assertSignedWebhook(t, received, jobID)                // asserts + HMAC-verifies when WEBHOOK_SIGNING_SECRET set
- downloadClient() *http.Client                          // honors E2E_S3_DIAL_ADDR

<!-- Live-run env contract (from e2e_test.go package doc, lines 7-22):
     E2E_BASE_URL=http://localhost:8090   DATABASE_URL=postgres://octo:octo-pass@localhost:5434/octo_db
     API_KEY_SALT=dev-only-change-me-in-real-deploys (MUST match running API; compose value)
     E2E_WEBHOOK_HOST=host.docker.internal   E2E_S3_DIAL_ADDR=127.0.0.1:9100
     WEBHOOK_SIGNING_SECRET=dev-only-change-me-in-real-deploys (compose worker value) -->

<!-- Format detection (internal/convert/sniff.go):
     Sniff(r io.Reader) (detected string, rest io.Reader, err error)
     returns the NORMALIZED format ("png","jpg","webp","heic","tiff") or "".
     libvips supported pairs (internal/convert/libvips.go): every ordered pair
     among {png, jpg, webp, heic, tiff}; png→jpg is the chosen pair. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate the committed PNG fixture</name>
  <files>internal/e2e/testdata/sample.png</files>
  <read_first>
    - internal/convert/sniff.go (matchPNG signature: detection is the standard 8-byte PNG magic; a real PNG passes trivially)
    - internal/convert/dimensions.go (the declared-dimension check is a MAX-pixel guard read from IHDR within the first 64 KiB — a tiny image passes; there is no minimum-dimension requirement)
    - internal/e2e/testdata/ (existing fixtures: sample.docx/.html etc. — the new file joins them as sample.png)
  </read_first>
  <action>
    Produce a small, real PNG at internal/e2e/testdata/sample.png (e.g. a 16×16 opaque solid-color image). Generate it deterministically with a throwaway Go program that uses the stdlib image + image/png packages (image.NewRGBA, fill pixels, png.Encode to the file) — do NOT hand-craft bytes and do NOT download from the network. Write the generator to a temp path outside the repo tree (or a temp dir), `go run` it to emit the file, then delete the generator so only the PNG is committed. The result MUST begin with the 8-byte PNG magic (\x89PNG\r\n\x1a\n) and carry a valid IHDR so convert.Sniff detects "png" and the dimension parser reads its width/height. Keep it tiny (well under 1 KB).
  </action>
  <acceptance_criteria>
    - `internal/e2e/testdata/sample.png` exists.
    - `file internal/e2e/testdata/sample.png` output contains `PNG image data`.
    - The first 8 bytes are the PNG magic: `printf '\x89PNG\r\n\x1a\n' | cmp -n 8 - internal/e2e/testdata/sample.png` exits 0.
    - File size is small (`wc -c < internal/e2e/testdata/sample.png` well under 4096).
    - No generator source file remains under `internal/e2e/` (`ls internal/e2e/*.go` shows only e2e_test.go).
  </acceptance_criteria>
  <verify>
    <automated>test -f internal/e2e/testdata/sample.png && file internal/e2e/testdata/sample.png | grep -q "PNG image data" && printf '\x89PNG\r\n\x1a\n' | cmp -n 8 - internal/e2e/testdata/sample.png</automated>
  </verify>
  <done>A tiny real PNG fixture is committed at internal/e2e/testdata/sample.png; no generator artifact left behind.</done>
</task>

<task type="auto">
  <name>Task 2: Author TestImageConversionE2E (png→jpg) + image download assertion</name>
  <files>internal/e2e/e2e_test.go</files>
  <read_first>
    - internal/e2e/e2e_test.go (mirror TestDocumentConversionE2E ~lines 336-386 for the upload→poll→download→webhook shape; reuse e2eSetup/provisionClient/postJob/pollUntilDone/startWebhookReceiver/assertSignedWebhook/downloadClient; the package doc comment lines 7-22 is the live-run env contract; note assertDownloadIsFormat uses convert.SniffContainer which is for ZIP/office containers, NOT images — a new image helper is needed)
    - internal/convert/sniff.go (convert.Sniff returns the normalized image format from magic bytes; use it for the image download assertion)
    - internal/convert/libvips.go (confirm png→jpg is a registered pair)
  </read_first>
  <action>
    Add `TestImageConversionE2E` to internal/e2e/e2e_test.go, structurally mirroring TestDocumentConversionE2E but for the image engine and a single pair (png→jpg):
    - Call e2eSetup(t) first (self-skip guard) and provisionClient(t).
    - Read testdata/sample.png.
    - Register a callback_url via startWebhookReceiver(t, cfg.webhookHost) so this test exercises the webhook path (the image worker enqueues the webhook; cmd/webhook-worker delivers it — the compose override already gives webhook-worker-1/-2 the host-gateway alias).
    - postJob with target "jpg" and the callback_url; pollUntilDone with a generous bound (the doc/html tests use 5*time.Minute; an image convert is fast, but keep a comfortable timeout, e.g. 2*time.Minute).
    - Extract download_url from the done body and assert it is a JPG.
    - assertSignedWebhook(t, received, jobID) to require the signed (HMAC-verified when WEBHOOK_SIGNING_SECRET is set) webhook.
    Add a small helper `assertDownloadIsImage(t *testing.T, downloadURL, wantFormat string)` that fetches the presigned URL via downloadClient() (no auth header), asserts 200, reads the body, calls convert.Sniff on it, and fails unless the detected (normalized) format equals wantFormat. Do NOT add a package import beyond what e2e_test.go already imports (convert is already imported). Run gofmt.
    Keep the deliberate no-subtest-table style acceptable here — a single-pair image test does not need the t.Run table the document test uses (though a single t.Run wrapper is fine for parity).
  </action>
  <acceptance_criteria>
    - `grep -c "func TestImageConversionE2E" internal/e2e/e2e_test.go` prints `1`.
    - `grep -c "func assertDownloadIsImage" internal/e2e/e2e_test.go` prints `1`.
    - `grep -c "convert.Sniff(" internal/e2e/e2e_test.go` is non-zero (image download asserted via Sniff, not SniffContainer).
    - `grep -c "assertSignedWebhook" internal/e2e/e2e_test.go` is >= 2 (document test + new image test both call it).
    - `go vet ./internal/e2e/...` exits 0.
    - `gofmt -l internal/e2e/e2e_test.go` prints nothing.
    - Additional cheap check (offline, NOT the terminal proof): `go test ./internal/e2e/ -run TestImageConversionE2E -v` reports `--- SKIP` (E2E_BASE_URL unset) and exits 0 — proving `go test ./...` stays green offline.
    - LIVE-COMPOSE HARD GATE (SC#3 — the terminal proof): with the compose stack up, the live run with the full env contract exits 0 and the `-v` output shows `--- PASS` for TestImageConversionE2E (it RAN, no `--- SKIP`). This proves the full upload → convert → download → HMAC-verified webhook cycle against a real stack.
    - Loud-failure rule: if `command -v docker` is genuinely absent, the task must FAIL LOUDLY (SUMMARY blocker); do NOT accept the offline self-skip path as proof of SC#3.
  </acceptance_criteria>
  <verify>
    <automated>go vet ./internal/e2e/... && gofmt -l internal/e2e/e2e_test.go | (! grep .) && test "$(grep -c 'func TestImageConversionE2E' internal/e2e/e2e_test.go)" = 1 && go test ./internal/e2e/ -run TestImageConversionE2E -v 2>&1 | grep -Eq "SKIP|E2E_BASE_URL not set"
command -v docker >/dev/null 2>&1 || { echo "BLOCKER: docker absent — SC#3 live compose E2E cannot run; do NOT accept the offline skip path"; exit 1; }
docker compose -f docker-compose.yml -f docker-compose.e2e.yml up -d --build || { echo "if build failed on 'no space left on device': docker builder prune -af && docker image prune -af, then retry (precedent 13-03-SUMMARY)"; exit 1; }
for i in $(seq 1 60); do curl -fsS http://localhost:8090/healthz >/dev/null 2>&1 && break; sleep 3; done
curl -fsS http://localhost:8090/healthz >/dev/null 2>&1 || { echo "BLOCKER: API healthz not ready after wait loop"; exit 1; }
E2E_BASE_URL=http://localhost:8090 DATABASE_URL="postgres://octo:octo-pass@localhost:5434/octo_db" API_KEY_SALT=dev-only-change-me-in-real-deploys E2E_WEBHOOK_HOST=host.docker.internal E2E_S3_DIAL_ADDR=127.0.0.1:9100 WEBHOOK_SIGNING_SECRET=dev-only-change-me-in-real-deploys go test ./internal/e2e/ -run TestImageConversionE2E -v -count=1 -timeout 30m 2>&1 | tee /tmp/17-02-e2e.log
test "${PIPESTATUS[0]:-1}" = 0 && grep -q -- "--- PASS" /tmp/17-02-e2e.log && ! grep -q -- "--- SKIP" /tmp/17-02-e2e.log</automated>
  </verify>
  <done>TestImageConversionE2E and assertDownloadIsImage exist, self-skip offline, and against a live compose stack the test RUNS (not skips) and reports `--- PASS`, driving the full image pipeline with an HMAC-verified webhook. If docker is genuinely absent the task fails loudly rather than accepting the skip path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture ↔ repo | committed sample.png is loaded and uploaded by the E2E test |
| stack → in-test webhook receiver | signed webhook crosses from webhook-worker to the host-bound receiver |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03 | Tampering | internal/e2e/testdata/sample.png fixture | mitigate | Fixture is generated deterministically from Go stdlib image/png (not fetched from any network source) and committed tiny — no supply-chain vector; reviewable as a plain PNG. |
| T-17-04 | Information Disclosure / Spoofing | E2E webhook HMAC verification (image path) | mitigate | The test reuses assertSignedWebhook, which HMAC-verifies X-OctoConv-Signature under the known dev signing secret when WEBHOOK_SIGNING_SECRET is set — extends the signed-delivery invariant proof to the image engine path. |
| T-17-SC | Tampering | go module installs | accept | No new dependencies; image/png and convert.Sniff are stdlib/existing. No package-manager install occurs, so the Package Legitimacy Gate does not apply. |
</threat_model>

<verification>
Whole-plan gate (offline, deterministic — necessary but NOT sufficient for SC#3):
- `go build ./... && go vet ./...` exit 0.
- `gofmt -l internal/e2e/e2e_test.go` prints nothing.
- `go test ./internal/e2e/ -run TestImageConversionE2E -v` reports SKIP (E2E_BASE_URL unset) and exits 0 — proving `go test ./...` stays green offline. This is an additional cheap check, NOT the terminal proof.

LIVE COMPOSE-STACK HARD GATE (the terminal proof of SC#3 — MANDATORY, unconditional; the offline self-skip above cannot prove the pipeline works):
1. `command -v docker` — if genuinely absent, FAIL LOUDLY (record a SUMMARY blocker); do NOT accept the self-skip path as proof of SC#3.
2. Bring up the stack: `docker compose -f docker-compose.yml -f docker-compose.e2e.yml up -d --build`
   - Docker disk pressure is a known pitfall building the 5-image stack (LibreOffice + Chromium). If `no space left on device` occurs, run `docker builder prune -af` / `docker image prune -af` and retry (per research PITFALLS Pitfall 1 and 13-03-SUMMARY).
3. Wait for readiness: poll `curl -fsS http://localhost:8090/healthz` until 200 (bounded loop, ~60× / 3s).
4. Run the image test live with the exact env contract from e2e_test.go's package doc:
   `E2E_BASE_URL=http://localhost:8090 \
    DATABASE_URL="postgres://octo:octo-pass@localhost:5434/octo_db" \
    API_KEY_SALT=dev-only-change-me-in-real-deploys \
    E2E_WEBHOOK_HOST=host.docker.internal \
    E2E_S3_DIAL_ADDR=127.0.0.1:9100 \
    WEBHOOK_SIGNING_SECRET=dev-only-change-me-in-real-deploys \
    go test ./internal/e2e/ -run TestImageConversionE2E -v -count=1 -timeout 30m`
   - Require: exit 0 AND `-v` output shows `--- PASS` for TestImageConversionE2E (RAN, not SKIP) — upload accepted (202), job reaches done, presigned download sniffs as jpg, and a signed webhook is HMAC-verified.
5. Teardown: leave the stack running, or `docker compose stop` at the end — executor's choice. Do NOT `docker compose down -v` (the volumes are shared with the dev stack).
</verification>

<success_criteria>
- Roadmap SC#3: a new image-engine E2E test in internal/e2e RUNS (not skips) and PASSES against a live compose stack — driving a full upload → convert (libvips) → download → HMAC-verified webhook cycle — closing the last gap in the E2E matrix. Proven by the LIVE COMPOSE-STACK HARD GATE above, not the offline self-skip path. (DEBT-08)
</success_criteria>

<output>
Create `.planning/phases/17-tech-debt-cleanup/17-02-SUMMARY.md` when done.
</output>
</content>
</invoke>
