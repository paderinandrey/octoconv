---
phase: 18-presets
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - internal/presets/optscheck.go
  - internal/presets/optscheck_test.go
  - cmd/manage-presets/main.go
autonomous: true
requirements: [PRST-01]
user_setup: []

must_haves:
  truths:
    - "D-10: cmd/manage-presets mirrors cmd/manage-clients — subcommand dispatch, direct Postgres connect (no HTTP/auth layer), fail-fast log.Fatalf on db.Connect error — with verbs create / update / list / show / deactivate"
    - "D-10: system scope is selected by the ABSENCE of --client-id; user scope requires --client-id; the DDL presets_scope_owner_chk CHECK enforces the invariant (no Go re-derivation)"
    - "D-04: `update` performs bump-on-update via presets.Repo.Update (new version, prior deactivated); `deactivate` sets is_active=false; there is no delete verb (no hard delete)"
    - "D-11: `create` and `update` validate the --opts JSON through the same convert allowlist parsers at write time (fail early for operators); empty opts is valid; D-06 re-validation at use time remains the real enforcement point"
  artifacts:
    - path: "cmd/manage-presets/main.go"
      provides: "operator CLI: create/update/list/show/deactivate"
      contains: "func main"
    - path: "internal/presets/optscheck.go"
      provides: "ValidateOptsJSON write-time allowlist check (D-11)"
      contains: "func ValidateOptsJSON"
  key_links:
    - from: "cmd/manage-presets/main.go"
      to: "internal/presets.Repo"
      via: "presets.NewRepo(pool)"
      pattern: "presets\\.NewRepo"
    - from: "cmd/manage-presets create/update"
      to: "internal/presets.ValidateOptsJSON"
      via: "write-time opts validation"
      pattern: "ValidateOptsJSON"
---

<objective>
Ship the operator CLI `cmd/manage-presets` (mirroring `cmd/manage-clients`) with verbs create / update / list / show / deactivate for both system- and client-scoped presets, plus a write-time opts validation helper so operators get early rejection of malformed opts (D-11).

Purpose: The CLI is the sole management surface for presets in v1.4 (no REST CRUD until v2). It is the operator's only way to populate the presets table that the API resolves against.
Output: `cmd/manage-presets/main.go`, `internal/presets/optscheck.go` + its unit test.
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-presets/18-CONTEXT.md

<interfaces>
<!-- From 18-01 (dependency). Executor uses these directly. -->
internal/presets.Repo methods (from 18-01):
  Resolve(ctx, clientID uuid.UUID, name string) (*Preset, error)
  Create(ctx, p CreateParams) (id uuid.UUID, version int, err error)     // CreateParams{Name, Scope, ClientID *uuid.UUID, TargetFormat, Options map[string]any, Description}
  Update(ctx, scope string, clientID *uuid.UUID, name, targetFormat string, options map[string]any, description string) (version int, err error)
  Deactivate(ctx, scope string, clientID *uuid.UUID, name string) error
  List(ctx, scope string, clientID *uuid.UUID, includeInactive bool) ([]Preset, error)
  Get(ctx, scope string, clientID *uuid.UUID, name string) (*Preset, error)
  Consts: ScopeSystem, ScopeUser, OperationConvert

convert allowlist parsers (internal/convert):
  ParseDocOpts(raw []byte) (DocOpts, error)      // opts.go — strict, DisallowUnknownFields
  ParseHTMLOpts(raw []byte) (HTMLOpts, error)    // htmlopts.go — strict, DisallowUnknownFields

cmd/manage-clients/main.go pattern: os.Args subcommand switch, db.Connect(ctx), defer pool.Close(),
  fmt.Println for user-facing output, log.Fatalf for errors, usage() helper.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write-time opts validation helper (D-11)</name>
  <files>internal/presets/optscheck.go, internal/presets/optscheck_test.go</files>
  <read_first>
    - internal/convert/opts.go (ParseDocOpts + DocOptsFromMap — the strict allowlist parser and its map round-trip)
    - internal/convert/htmlopts.go (ParseHTMLOpts — the HTML allowlist parser)
    - .planning/phases/18-presets/18-CONTEXT.md (D-06 vs D-11 distinction — D-11 is fail-early, D-06 is the enforcement point)
    - .planning/research/PITFALLS.md (Pitfall 9 — stored opts are operator-originated; write-time check is a convenience, not the trust boundary)
  </read_first>
  <behavior>
    - ValidateOptsJSON(nil) and ValidateOptsJSON(empty map) → nil (empty opts always valid).
    - A DocOpts-shaped payload {"pdf_profile":"pdf/a-2b"} → nil (passes ParseDocOpts).
    - An HTMLOpts-shaped payload {"page_size":"a4","margin_mm":10} → nil (passes ParseHTMLOpts).
    - A payload with a key unknown to BOTH schemas {"bogus":1} → non-nil error.
    - An out-of-range HTML value {"margin_mm":9999} that fails ParseHTMLOpts but is not DocOpts either → non-nil error.
  </behavior>
  <action>
    Add `ValidateOptsJSON(options map[string]any) error` to a new internal/presets/optscheck.go. The CLI cannot know the engine at write time (engine is derived from detected source + target at job time), so the helper accepts the opts if they pass AT LEAST ONE of the current allowlist parsers: marshal the map to JSON, then return nil if `convert.ParseDocOpts(raw)` succeeds OR `convert.ParseHTMLOpts(raw)` succeeds; otherwise return an error naming that the opts match no known schema. Empty/nil map short-circuits to nil (mirror DocOptsFromMap's len==0 rule). Document clearly that this is D-11 fail-early convenience only, and that D-06 re-validation in handleCreateJob (18-03) — which knows the engine — is the actual enforcement point. This makes internal/presets import internal/convert (acceptable: convert is a leaf).

    Write optscheck_test.go covering the behavior cases above (pure functions, no DB).
  </action>
  <verify>
    <automated>cd /Users/apaderin/dev/octoconv && go test ./internal/presets/ -run TestValidateOptsJSON</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/presets/ -run TestValidateOptsJSON` passes.
    - `grep -n "func ValidateOptsJSON" internal/presets/optscheck.go` matches.
    - Helper returns nil for empty opts and for both a valid DocOpts and a valid HTMLOpts payload; returns non-nil for a both-schemas-unknown key and for an out-of-range margin_mm.
  </acceptance_criteria>
  <done>ValidateOptsJSON rejects opts that pass neither current allowlist parser and accepts opts valid under either, with passing pure-function tests.</done>
</task>

<task type="auto">
  <name>Task 2: cmd/manage-presets CLI (create / update / list / show / deactivate)</name>
  <files>cmd/manage-presets/main.go</files>
  <read_first>
    - cmd/manage-clients/main.go (the CLI pattern to mirror file-for-file: subcommand dispatch, db.Connect, fmt.Println output, log.Fatalf, usage())
    - internal/presets/repo.go (the Repo methods this CLI calls — from 18-01)
    - internal/presets/optscheck.go (ValidateOptsJSON — from Task 1)
    - .planning/phases/18-presets/18-CONTEXT.md (D-10 verbs/UX; D-11 write-time validation; "Specific Ideas": system=no --client-id, user=--client-id)
    - .planning/research/ARCHITECTURE.md ("cmd/manage-presets mirrors cmd/manage-clients" structure rationale)
  </read_first>
  <action>
    Create cmd/manage-presets/main.go, package main, mirroring cmd/manage-clients/main.go's shape: connect to Postgres via db.Connect(ctx) (fail-fast log.Fatalf), defer pool.Close(), construct `repo := presets.NewRepo(pool)`, dispatch on the subcommand. No API_KEY_SALT is needed (presets carry no secrets). Use the stdlib `flag` package with a `flag.NewFlagSet` per subcommand (richer than manage-clients' positional args, justified because presets need named flags; keep the same connect/output/error conventions).

    Verbs (flag names are Claude's discretion per CONTEXT — these are the chosen set):
    - `create --name <n> --target <fmt> [--client-id <uuid>] [--opts '<json>'] [--description <d>]` — scope = ScopeSystem when --client-id absent, ScopeUser (with parsed uuid) when present (D-10). Parse --opts JSON into map[string]any; run presets.ValidateOptsJSON before insert (D-11). Call repo.Create; print the new id + version.
    - `update <same flags>` — parse+ValidateOptsJSON, call repo.Update (bump-on-update, D-04); print new version.
    - `list [--client-id <uuid>] [--all]` — call repo.List(scope, clientID, includeInactive=--all). When --client-id absent, list system scope; when present, list that client's user scope. Print a simple aligned table (name, version, scope, target, active). (Whether --all shows inactive is the documented Claude's-discretion choice; --all includes inactive.)
    - `show --name <n> [--client-id <uuid>]` — repo.Get; print full row including options JSON.
    - `deactivate --name <n> [--client-id <uuid>]` — repo.Deactivate (is_active=false, D-04 no hard delete); print confirmation. There is deliberately NO delete verb.

    For unknown/missing subcommand or missing required flags, call a usage() that log.Fatalf's the verb list (mirror manage-clients usage()). Map presets.ErrNotFound to a clear "no such preset" message on show/update/deactivate.
  </action>
  <verify>
    <automated>cd /Users/apaderin/dev/octoconv && go build ./cmd/manage-presets/ && go vet ./cmd/manage-presets/ && go run ./cmd/manage-presets 2>&1 | grep -qi usage</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./cmd/manage-presets/` and `go vet ./cmd/manage-presets/` exit 0.
    - Running with no args prints a usage line naming create/update/list/show/deactivate.
    - `grep -n "presets.NewRepo" cmd/manage-presets/main.go` and `grep -n "ValidateOptsJSON" cmd/manage-presets/main.go` both match.
    - No "delete"/"DELETE" verb exists (`grep -ci 'case "delete"' cmd/manage-presets/main.go` returns 0).
    - Scope is selected by presence/absence of --client-id (system when absent).
  </acceptance_criteria>
  <done>cmd/manage-presets builds and vets clean, mirrors manage-clients conventions, exposes create/update/list/show/deactivate with scope keyed on --client-id, validates opts at write time (D-11), and has no hard-delete path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell→CLI→Postgres | operator-supplied name/target/opts/client_id written directly to the presets table via parameterized repo methods |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04 | Tampering | CLI --opts JSON | mitigate | ValidateOptsJSON runs the same strict allowlist parsers at write time (D-11); D-06 re-validation at use time is the enforcement backstop |
| T-18-05 | Elevation | CLI creating a mis-scoped preset | mitigate | Scope derived solely from --client-id presence; DDL presets_scope_owner_chk rejects an inconsistent (scope, client_id) pair at INSERT |
| T-18-06 | Repudiation/data-loss | operator deletes a preset | mitigate | No delete verb exists; deactivate sets is_active=false, preserving history (D-04) |
| T-18-SC | Tampering | Go module deps | accept | No new third-party dependencies (flag/db/presets/convert all existing) — no package legitimacy gate applicable |
</threat_model>

<verification>
- `go build ./... && go vet ./...` clean.
- `go test ./internal/presets/ -run TestValidateOptsJSON` passes.
- CLI usage output enumerates the five verbs; no delete path.
</verification>

<success_criteria>
Operator can create/update/list/show/deactivate system- and client-scoped presets via cmd/manage-presets, mirroring manage-clients, with write-time opts validation (D-11), bump-on-update versioning and no hard delete (D-04). Satisfies PRST-01.
</success_criteria>

<output>
Create `.planning/phases/18-presets/18-02-SUMMARY.md` when done.
</output>
