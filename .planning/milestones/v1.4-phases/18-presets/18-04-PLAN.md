---
phase: 18-presets
plan: 04
type: execute
wave: 3
depends_on: ["18-02", "18-03"]
files_modified:
  - scripts/presets-acceptance.sh
autonomous: true
requirements: [PRST-01, PRST-02, PRST-03, PRST-04]
user_setup: []

must_haves:
  truths:
    - "PRST-01/SC1: the script exercises ALL FIVE CLI verbs end-to-end — create (system + client), list (asserts both presets appear), show (asserts fields incl. version), update (asserts version bumped to 2 and the old version is inactive), deactivate (asserts a subsequent POST /v1/jobs with that preset → 422) — each step asserting on actual CLI output / DB / HTTP, loud-failing on mismatch"
    - "SC2/D-08: a scripted end-to-end run creates a preset via cmd/manage-presets, POSTs /v1/jobs with preset=<name>, the job reaches done, and `SELECT preset_name, preset_version FROM jobs` proves provenance was persisted"
    - "SC3/D-02: the script proves shadowing live — client A with a user-scope preset resolves to the user target; a different client B with only the same-name system preset resolves to the system target"
    - "SC4/D-01/D-03: the script proves preset+target → 422 AND a nonexistent preset and a cross-client preset both return byte-identical 422 bodies (no existence leak)"
    - "SC5/D-06: the script inserts (via SQL) a preset whose stored opts fail current validation and proves POST /v1/jobs with that preset is rejected 422 (stored opts re-validated, not trusted)"
  artifacts:
    - path: "scripts/presets-acceptance.sh"
      provides: "hard-gate live acceptance script against the local compose stack"
      contains: "docker compose"
  key_links:
    - from: "scripts/presets-acceptance.sh"
      to: "cmd/manage-presets + POST /v1/jobs + postgres"
      via: "end-to-end CLI→API→DB assertions across all five CLI verbs"
      pattern: "manage-presets"
---

<objective>
Prove the presets feature works end-to-end on the live compose stack with a scripted hard gate: operator drives ALL FIVE CLI verbs (create/list/show/update/deactivate) with real assertions, a client submits a job by preset name, the job completes carrying provenance, shadowing resolves correctly, and every failure mode (mutual exclusivity, no-leak, stored-opts re-validation, deactivated-preset rejection) behaves as specified.

Purpose: Unit/handler tests use fakes; only a live run against real Postgres + the real image worker proves SC1/SC2/SC3/SC4/SC5 hold through the actual pipeline (precedent: Phase 16/17 scripted hard gates). The CLI verb surface (PRST-01) is functionally proven here end-to-end, not just build-checked.
Output: `scripts/presets-acceptance.sh` and a run log captured in the SUMMARY.
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/18-presets/18-CONTEXT.md

<compose_contract>
<!-- Local compose stack facts (docker-compose.yml, README.md). Executor uses these directly. -->
- Services: postgres (octoconv-db, host port 5434→5432), redis, minio (host 9100→9000), createbucket (one-shot), api (octoconv-api, host 8090→8090), worker (octoconv-worker, image/libvips).
- api & worker are BUILT images; handleCreateJob changed in 18-03, so rebuild at least the api image: `docker compose up -d --build api worker` (worker itself is unchanged but bring it up).
- Host→DB: DATABASE_URL=postgres://octo:octo-pass@localhost:5434/octo_db
- API_KEY_SALT inside the api container is "dev-only-change-me-in-real-deploys" — cmd/manage-clients (run from host) MUST use the SAME salt so minted keys authenticate against the running API.
- API base: http://localhost:8090 ; auth header: `Authorization: ApiKey <raw-key>`.
- cmd/manage-presets and cmd/manage-clients run from host via `go run ./cmd/...` with DATABASE_URL + (for clients) API_KEY_SALT set.
- Postgres inside container reachable for SQL asserts via `docker exec octoconv-db psql -U octo -d octo_db -tAc "<sql>"`.
- Health gate before POSTing: `curl -fsS http://localhost:8090/healthz` returns 200 (readiness, per PITFALLS Pitfall 2 discipline).
</compose_contract>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scripted live acceptance hard gate (all five CLI verbs + resolution/no-leak/re-validation)</name>
  <files>scripts/presets-acceptance.sh</files>
  <read_first>
    - docker-compose.yml (service names, ports, api API_KEY_SALT value, build stanzas)
    - README.md (local run / env-sourcing conventions; non-standard ports 5434/9100)
    - cmd/manage-clients/main.go (how a client + raw key are minted — `create <name>` prints "client id:" and "api key ...")
    - cmd/manage-presets/main.go (verb/flag surface from 18-02 — the exact create/list/show/update/deactivate flags and their printed output)
    - internal/api/handlers.go (the 422 error strings to assert: "unknown or inactive preset", the mutually-exclusive text)
    - .planning/phases/18-presets/18-CONTEXT.md (D-01/D-02/D-03/D-04/D-06/D-08 — the live obligations)
    - .planning/research/PITFALLS.md (Pitfall 2 readiness gate; Pitfall 8 deactivated-preset rejection; Pitfall 12 byte-identical no-leak)
  </read_first>
  <action>
    Create scripts/presets-acceptance.sh, `#!/usr/bin/env bash` with `set -euo pipefail`. It must be idempotent-enough to re-run (use uuid-suffixed preset/client names). Provide a helper `assert_eq expected actual label` that echoes and exits 1 on mismatch, and a helper `assert_http expected_code label <curl args...>` (or capture `-o body -w '%{http_code}'`) so every HTTP assertion loud-fails. Steps:

    1. Bring up the stack: `docker compose up -d --build api worker` (postgres/redis/minio/createbucket come up as dependencies). Poll `curl -fsS http://localhost:8090/healthz` with a retry loop (generous timeout, ~60s) until 200 before proceeding.
    2. Export `DATABASE_URL=postgres://octo:octo-pass@localhost:5434/octo_db` and `API_KEY_SALT=dev-only-change-me-in-real-deploys` for the host CLI invocations.
    3. Mint two clients via `go run ./cmd/manage-clients create <uuid-name>` twice; capture each printed client id + raw api key (grep the "api key" line). Call them A (id_A, key_A) and B (id_B, key_B).
    4. CREATE verb (system + client scope): create presets via `go run ./cmd/manage-presets create`:
       - system preset name=P (uuid-suffixed), target=webp, no --client-id.
       - user preset name=P for client A: --client-id id_A, target=png (shadows the system P for A).
       - system preset name=Q, target=webp (for the provenance + basic-resolution case, distinct name).
       - system preset name=R, target=webp (a lifecycle preset used to prove update + deactivate).
       Capture the id/version each `create` prints; assert the printed version == 1 for a fresh create (assert_eq).

    5. CLI VERB COVERAGE (PRST-01/SC1) — prove list / show / update / deactivate against real output + DB, each asserting loud:
       a. LIST: `go run ./cmd/manage-presets list` (system scope, no --client-id) → capture output; assert it contains BOTH P and Q and R names (grep -q each; assert_eq on presence). `go run ./cmd/manage-presets list --client-id id_A` → assert the output contains the user-scope P for A (its png target visible). This proves system-vs-user list scoping live.
       b. SHOW: `go run ./cmd/manage-presets show --name Q` → capture output; assert it reports target `webp` AND version `1` (grep both fields; assert_eq). This proves show surfaces the version field.
       c. UPDATE: `go run ./cmd/manage-presets update --name R --target png` → assert the printed new version == 2 (assert_eq). Then assert via SQL: `docker exec octoconv-db psql ... "SELECT count(*) FROM presets WHERE scope='system' AND name='R' AND is_active"` == 1 (exactly one active) AND `"SELECT version FROM presets WHERE scope='system' AND name='R' AND is_active"` == 2 (active is v2) AND `"SELECT is_active FROM presets WHERE scope='system' AND name='R' AND version=1"` == f (the old v1 is now inactive). This proves bump-on-update (D-04) live.
       d. DEACTIVATE: `go run ./cmd/manage-presets deactivate --name R` → assert the confirmation output; then assert via SQL `"SELECT count(*) FROM presets WHERE scope='system' AND name='R' AND is_active"` == 0 (no active rows) AND the rows still exist (count of all R rows >= 2 — no hard delete, D-04). Finally POST `-F preset=R -F file=@<pngfile>` with key_A → assert HTTP 422 (a deactivated preset no longer resolves — links Pitfall 8 / D-03 to the CLI deactivate verb).

    6. SC2/D-08 provenance: POST a small PNG to `http://localhost:8090/v1/jobs` with `-F preset=Q -F file=@<pngfile>` and key_A → expect 202, capture job_id. Poll `GET /v1/jobs/<job_id>` until status=done (retry loop). Then assert `docker exec octoconv-db psql ... "SELECT preset_name||'/'||preset_version FROM jobs WHERE id='<job_id>'"` equals `Q/1`. Provide a small valid PNG fixture (write bytes inline or reuse a committed test asset — a 1x1 PNG is fine; the image worker converts png→webp).
    7. SC3/D-02 shadowing: POST preset=P with key_A → after resolution the job's target_format must be `png` (user shadows). POST preset=P with key_B (client B has no override) → target_format must be `webp` (system). Assert both via `SELECT target_format FROM jobs WHERE id=...`.
    8. SC4/D-01 mutual exclusivity: POST `-F preset=Q -F target=webp -F file=@png` with key_A → expect HTTP 422. Assert.
    9. SC4/D-03 no-leak: capture the full response body of (a) POST preset=<random-nonexistent> and (b) POST preset=<name of A's user preset P> using key_B (cross-client — B cannot see A's user preset; if the system P makes this ambiguous, instead create a user-only preset for A with a name that has NO system counterpart and request it as B). Both must be HTTP 422 with byte-identical bodies equal to `{"error":"unknown or inactive preset"}`. Assert both status AND body equality.
    10. SC5/D-06 stored-opts re-validation: insert a bad preset directly via SQL — `docker exec octoconv-db psql ... "INSERT INTO presets (name, scope, operation, target_format, options) VALUES ('<badname>','system','convert','pdf','{\"margin_mm\":9999}'::jsonb)"` (a value the current ParseHTMLOpts rejects as out-of-range). POST an HTML file with `-F preset=<badname>` and key_A → expect HTTP 422 (invalid opts), proving stored opts are re-validated, not trusted. Provide a tiny valid HTML fixture. Assert 422.
    11. Print a clear PASS summary on success; `set -e` makes any failed assertion abort non-zero.

    Do NOT tear the stack down at the end (leave it for inspection / re-run), matching the project's live-gate precedent; the script's exit code IS the gate.
  </action>
  <verify>
    <automated>cd /Users/apaderin/dev/octoconv && bash scripts/presets-acceptance.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/presets-acceptance.sh` exits 0.
    - ALL FIVE CLI verbs are exercised with assertions (PRST-01/SC1): create prints version 1; list shows the created presets (system + user scope); show reports the version field; update prints version 2 and SQL confirms exactly one active row at v2 with v1 inactive; deactivate leaves zero active rows (rows still present) and a subsequent POST with that preset → 422.
    - Script output shows: a preset-created job reaching `done` with provenance `Q/1` asserted from the jobs table (SC2/D-08).
    - Shadowing asserted: preset P resolves target `png` for client A and `webp` for client B (SC3/D-02).
    - preset+target → 422 asserted (SC4/D-01).
    - Nonexistent-preset and cross-client-preset POSTs both return 422 with byte-identical `{"error":"unknown or inactive preset"}` bodies (SC4/D-03).
    - A preset with a since-invalid stored opts value (margin_mm 9999) → POST rejected 422 (SC5/D-06).
    - Script is executable / runnable via `bash` and uses `set -euo pipefail`.
  </acceptance_criteria>
  <done>The live compose stack proves SC1 (all five CLI verbs end-to-end with real output/DB/HTTP assertions), SC2 (provenance), SC3 (shadowing both directions), SC4 (mutual exclusivity + byte-identical no-leak 422), and SC5 (stored-opts re-validation); the script is a re-runnable hard gate exiting non-zero on any failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test operator→live stack | scripted run exercises the real client→API→DB trust boundaries under real auth; no new production surface introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-11 | Information disclosure | live no-leak proof | mitigate | Script asserts byte-identical 422 bodies for nonexistent vs cross-client preset — the live backstop for the SQL-filter mitigation (T-18-07) |
| T-18-12 | Tampering | live stored-opts trust proof | mitigate | Script inserts an invalid-under-current-allowlist preset via SQL and confirms job creation rejects it (live backstop for T-18-08 / D-06) |
| T-18-14 | Tampering/Elevation | live deactivated-preset rejection | mitigate | Script deactivates R via the CLI verb then confirms a POST with preset=R → 422 (live backstop for the pre-Create re-check, T-18-13 / Pitfall 8) |
| T-18-SC | Tampering | dev-only compose secrets in script | accept | Script uses the existing "dev-only-change-me-in-real-deploys" placeholder salt already in docker-compose.yml; no real secret material; no env-dump to logs |
</threat_model>

<verification>
- `bash scripts/presets-acceptance.sh` exits 0 against a freshly rebuilt api image on the local compose stack.
- All five CLI verbs and all five success criteria are asserted live, not just in unit fakes.
</verification>

<success_criteria>
End-to-end on the live stack: all five CLI verbs (create/list/show/update/deactivate) are exercised with real assertions (SC1); a preset-by-name job completes with persisted provenance (SC2); shadowing resolves both directions (SC3); mutual exclusivity and byte-identical no-leak 422s hold (SC4); and stored preset opts are re-validated on use (SC5).
</success_criteria>

<output>
Create `.planning/phases/18-presets/18-04-SUMMARY.md` when done (include the passing script run log).
</output>
