---
phase: 20-presets-rest-format-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/convert/convert.go
  - internal/convert/convert_test.go
  - internal/presets/presets.go
  - internal/presets/repo.go
  - internal/presets/repo_test.go
  - internal/api/api.go
  - internal/api/presets_handlers.go
  - internal/api/formats_handlers.go
  - internal/api/routes.go
  - internal/api/handlers_test.go
  - internal/api/routes_test.go
  - internal/api/presets_handlers_test.go
  - internal/api/formats_handlers_test.go
  - cmd/api/main.go
autonomous: true
requirements: [PRAPI-01, PRAPI-02, PRAPI-03]
must_haves:
  truths:
    - "D-01: POST/GET/GET{name}/PUT/DELETE /v1/presets are wired under the authenticated /v1 group; GET list defaults to active-only and ?all=true includes inactive versions"
    - "D-02: preset write handlers hardcode scope=presets.ScopeUser and take clientID solely from auth.ClientFromContext; the request DTO has no scope/client_id fields, so a body carrying scope=system or a foreign client_id has zero effect (P4/P6 mass-assignment structurally impossible)"
    - "D-03: create on an existing active client-scope name returns 409; update/show/deactivate on a nonexistent, cross-client, or system-scope name for a write returns 404 with a no-leak body identical to the truly-missing case (never 403, never a distinguishable message)"
    - "D-04: the preset response DTO carries name, version, scope, operation, target_format, options, description, is_active, created_at, updated_at and NEVER id or client_id"
    - "D-05: opts are validated at write time via presets.ValidateOptsJSON, returning the same 422 shape jobs use"
    - "D-06: GET /v1/formats returns a registry-derived capability map built by walking convert.Default (no hardcoded pair list that can drift)"
    - "D-07: /v1/formats sits inside the /v1 group so auth and rate-limit middleware apply exactly as for /v1/jobs"
    - "D-08: a new PresetAdmin interface lives in internal/api/api.go alongside the unchanged single-method PresetRepo; both are backed by the same *presets.Repo (interface segregation preserved)"
    - "D-09: any capability the merged view needs is added to internal/presets.Repo ONCE and shared; handlers never reimplement shadow-precedence or existence semantics"
    - "D-10: GET /v1/presets returns the merged effective view — the client's own presets plus system presets marked read-only via the scope field — the same merged set MCP-04 (Phase 21) consumes as-is"
  artifacts:
    - path: "internal/convert/convert.go"
      provides: "Registry.Classes() registry-walk method for /v1/formats (D-06)"
      contains: "func (r *Registry) Classes"
    - path: "internal/presets/repo.go"
      provides: "ListForClient + GetForClient merged shadow-precedence reads and Create returning ErrAlreadyExists (D-09/D-10/D-03)"
      contains: "func (r *Repo) ListForClient"
    - path: "internal/presets/presets.go"
      provides: "ErrAlreadyExists sentinel for the 409 mapping (D-03)"
      contains: "ErrAlreadyExists"
    - path: "internal/api/api.go"
      provides: "PresetAdmin interface + Server.presetAdmin field (D-08)"
      contains: "PresetAdmin interface"
    - path: "internal/api/presets_handlers.go"
      provides: "five /v1/presets CRUD handlers with narrow DTO + no-leak error mapping (D-01..D-05/D-10)"
      exports: ["handleCreatePreset", "handleListPresets", "handleShowPreset", "handleUpdatePreset", "handleDeactivatePreset"]
    - path: "internal/api/formats_handlers.go"
      provides: "handleListFormats registry-walk endpoint (D-06/D-07)"
      exports: ["handleListFormats"]
    - path: "internal/api/routes.go"
      provides: "/v1/presets + /v1/formats routes mounted inside the /v1 auth+ratelimit group"
      contains: "presets"
  key_links:
    - from: "internal/api/presets_handlers.go"
      to: "internal/presets.Repo (via PresetAdmin)"
      via: "s.presetAdmin.Create/Update/Deactivate/GetForClient/ListForClient"
      pattern: "s\\.presetAdmin\\."
    - from: "internal/api/presets_handlers.go"
      to: "auth.ClientFromContext"
      via: "clientID + scope derivation for every handler"
      pattern: "ClientFromContext"
    - from: "internal/api/formats_handlers.go"
      to: "convert.Default.Classes"
      via: "registry walk"
      pattern: "convert\\.Default\\.Classes|\\.Classes\\("
    - from: "internal/api/routes.go"
      to: "/v1 middleware chain"
      via: "routes mounted inside r.Route(\"/v1\", ...)"
      pattern: "presets"
---

<objective>
Implement authenticated REST self-service for client-scope presets (`/v1/presets`: create/list/show/update/deactivate) plus the registry-derived capability endpoint (`GET /v1/formats`), all under the existing `/v1` auth + rate-limit chain. Reuse the Phase 18 `internal/presets.Repo` semantics; add only the shared merged-view capability the REST surface (and Phase 21's MCP `list_presets`) needs.

Purpose: This is the discovery substrate Phase 21's MCP tools consume. Preset management and format discovery move from operator-CLI-only to authenticated REST for internal clients.
Output: `PresetAdmin` interface, five preset CRUD handlers, `/v1/formats` handler, a registry-walk method, two merged-read repo methods, an `ErrAlreadyExists` sentinel, and unit + handler tests.
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/20-presets-rest-format-discovery/20-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md

<read_first>
Files being modified or mirrored — read before editing:
- internal/api/api.go — existing PresetRepo (single-method, Resolve-only) precedent; Server struct; NewServer positional wiring (D-08 says add a SECOND interface, do NOT widen PresetRepo — ARCHITECTURE Anti-Pattern 3)
- internal/api/routes.go — the /v1 group and its ratelimit.ByIP -> auth.Middleware -> ratelimit.PerClient chain (new routes MUST sit inside this group, D-07)
- internal/api/handlers.go — writeJSON/writeError conventions, maxPresetNameBytes const, auth.ClientFromContext usage, handleGetJob's 404-not-403 no-leak ownership guard (the pattern preset handlers mirror)
- internal/api/handlers_test.go — fake wiring (fakeRepo/fakeStorage/fakeQueue/fakePresetRepo/newFakeResolver/newTestServer) and the multiple direct NewServer call sites; internal/api/routes_test.go:22 has ONE MORE direct NewServer call site (TestByIP_NotEvadedByForwardedForSpoofing) that also needs the new PresetAdmin positional arg that MUST be updated for the new positional arg
- internal/presets/repo.go — List/Get/Create/Update/Deactivate signatures the PresetAdmin interface reuses verbatim; Resolve's shadow-precedence SQL (user shadows system) that ListForClient/GetForClient must reproduce; Create's current plain "already exists" error
- internal/presets/presets.go — Preset struct, ScopeUser/ScopeSystem, OperationConvert, ErrNotFound
- internal/presets/optscheck.go — presets.ValidateOptsJSON (the D-05 write-time check)
- internal/presets/repo_test.go — DATABASE_URL-gated integration test pattern (newTestRepo skips without a DB; createTestClient/uniqueName helpers)
- internal/convert/convert.go — Registry.m (unexported), Register/Lookup/EngineFor, Converter.Engine(); Registry has NO walk method today
- internal/convert/converters.go — the three registered converters (image/document/html) Classes() must surface
- cmd/manage-presets/main.go — the CLI semantics REST must mirror (bump-on-update, no hard delete, ValidateOptsJSON)
- cmd/api/main.go — the single production NewServer call site (line ~101) that must pass the new PresetAdmin arg
</read_first>

<interfaces>
Existing repo methods PresetAdmin reuses verbatim (from internal/presets/repo.go):
  Create(ctx, p presets.CreateParams) (uuid.UUID, int, error)
  Update(ctx, scope string, clientID *uuid.UUID, name, targetFormat string, options map[string]any, description string) (int, error)
  Deactivate(ctx, scope string, clientID *uuid.UUID, name string) error
  Get(ctx, scope string, clientID *uuid.UUID, name string) (*presets.Preset, error)   // scope-specific
  List(ctx, scope string, clientID *uuid.UUID, includeInactive bool) ([]presets.Preset, error)  // scope-specific

Existing single-method interface (internal/api/api.go — leave UNCHANGED):
  PresetRepo interface { Resolve(ctx, clientID uuid.UUID, name string) (*presets.Preset, error) }

auth.ClientFromContext(ctx) (*clients.Client, bool) — client.ID is the authenticated uuid.UUID.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared foundation — registry walk + merged-view repo reads + 409 sentinel</name>
  <files>internal/convert/convert.go, internal/convert/convert_test.go, internal/presets/presets.go, internal/presets/repo.go, internal/presets/repo_test.go</files>
  <behavior>
    - convert.Registry.Classes() returns a map keyed by engine class ("image"/"document"/"html") whose values list the registered (from,to) pairs for that class, deterministically ordered; every pair comes from a registered Converter (no hardcoded literals). A pair registered by LibvipsConverter appears under "image".
    - presets.Repo.ListForClient(ctx, clientID, includeInactive) returns the client's own user-scope rows PLUS every system-scope row whose name is NOT shadowed by an active user-scope row of the same name for that client (mirrors Resolve's user-shadows-system precedence). Each returned Preset keeps its scope field so callers can mark system rows read-only. includeInactive=false returns active-only.
    - presets.Repo.GetForClient(ctx, clientID, name) returns the single effective preset for that name applying the same shadow precedence (user version wins over a same-name system preset); ErrNotFound when neither a user nor a system row exists.
    - presets.Repo.Create returns presets.ErrAlreadyExists (via errors.Is) when an active row for the same (scope, client_id, name) already exists, while preserving a human-readable message; cmd/manage-presets still compiles and its errors.Is checks still work.
  </behavior>
  <action>
    In internal/convert/convert.go add `func (r *Registry) Classes() map[string][]Pair`: iterate r.m, group each Pair under its Converter.Engine() value, and sort each class's slice by From then To for deterministic output. This is the SINGLE registry-derived source for /v1/formats (D-06) — no engine/pair string literals anywhere else. Add a convert_test.go case asserting Classes()["image"] contains a known libvips pair and that output is stable across calls.
    In internal/presets/presets.go add `var ErrAlreadyExists = errors.New("preset already exists")`. In internal/presets/repo.go change Create's existing-active branch to return a wrapped sentinel (fmt.Errorf("active preset %q already exists for scope %q; use Update instead: %w", p.Name, p.Scope, ErrAlreadyExists)) so callers map it via errors.Is — do not change the CLI (its log.Fatalf still prints the message). (D-03/D-09)
    In internal/presets/repo.go add ListForClient and GetForClient. Reproduce Resolve's precedence in SQL: user-scope rows for the client, UNION system-scope rows (client_id IS NULL) whose name has no active user-scope row for this client. Keep ALL scope/ownership/shadow filtering in the WHERE/query (no post-query Go ownership branch), consistent with Resolve/Get/List. Select the same full column set as List (id..updated_at) so the handler DTO has description/is_active/timestamps. Order by name, then scope (user before system), then version DESC. (D-09/D-10)
    Add DATABASE_URL-gated integration tests in repo_test.go (mirroring newTestRepo/createTestClient/uniqueName): (a) a client with a same-name user override sees ONLY its user row in ListForClient (system shadowed); a second client with no override sees the system row read-only; (b) GetForClient returns the user row for the overriding client and the system row for the other; (c) Create twice on the same active (scope,client,name) yields errors.Is(err, ErrAlreadyExists) on the second.
  </action>
  <verify>
    <automated>cd /Users/apaderin/dev/octoconv && go build ./... && go test ./internal/convert/... && go vet ./internal/convert/... ./internal/presets/...</automated>
  </verify>
  <done>Registry.Classes returns engine->pairs from the registry; ListForClient/GetForClient encode shadow precedence in SQL; Create returns ErrAlreadyExists; convert tests pass; presets integration tests present (DB-gated, exercised live in 20-02).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PresetAdmin interface, /v1/presets CRUD handlers, routes + wiring, handler tests</name>
  <files>internal/api/api.go, internal/api/presets_handlers.go, internal/api/routes.go, internal/api/handlers_test.go, internal/api/presets_handlers_test.go, cmd/api/main.go</files>
  <behavior>
    - POST /v1/presets with JSON {target_format, options?, description?} creates a client-scope (scope=user) preset owned by the caller and returns 201 with the response DTO at version 1; a body additionally carrying "scope":"system" or "client_id":"<foreign>" produces the SAME result (those fields are not in the DTO and are ignored) — resulting scope is user, owner is the caller.
    - POST /v1/presets on a name that already has an active client-scope preset returns 409.
    - POST /v1/presets with options failing presets.ValidateOptsJSON returns 422.
    - GET /v1/presets returns 200 with an array containing the caller's own presets AND system presets, each carrying a scope field; system entries are read-only (scope="system"). ?all=true includes inactive versions.
    - GET /v1/presets/{name} returns 200 with the effective preset (user shadows system; a system-only name returns the system preset read-only). A nonexistent or cross-client name returns 404 with body identical to the truly-missing case.
    - PUT /v1/presets/{name} with {target_format, options?, description?} bumps the version (returns the new version in the DTO). Updating a nonexistent, cross-client, or system-scope name returns 404 (no-leak).
    - DELETE /v1/presets/{name} deactivates (soft) and returns success; a second GET show returns 404; no hard delete. DELETE on a nonexistent/cross-client/system name returns 404.
    - Every response DTO omits id and client_id.
  </behavior>
  <action>
    In api.go add the PresetAdmin interface (Create/Update/Deactivate/Get/List signatures verbatim from repo.go PLUS ListForClient and GetForClient) exactly as ARCHITECTURE Pattern 2 sketches — a SECOND interface, do not touch PresetRepo (D-08, Anti-Pattern 3). Add a `presetAdmin PresetAdmin` field to Server and a positional param to NewServer (place it immediately after the existing `presets PresetRepo` param); assign it in the returned struct.
    Update cmd/api/main.go: pass the SAME presetRepo value for the new PresetAdmin arg (both interfaces, one *presets.Repo — D-08/D-09).
    Create internal/api/presets_handlers.go with a narrow request DTO `type presetRequest struct { TargetFormat string \`json:"target_format"\`; Options map[string]any \`json:"options"\`; Description string \`json:"description"\` }` — NO scope/client_id fields (D-02, P4/P6). Add a response mapper `presetResponse(p *presets.Preset) map[string]any` (or a typed struct with json tags) emitting name, version, scope, operation, target_format, options, description, is_active, created_at, updated_at — never id, never client_id (D-04). Implement the five handlers:
      - handleCreatePreset: decode JSON body (cap size, reject malformed with 400); enforce len(name?) — name comes from body? NO: for create the name is a body field. Add `Name` to presetRequest (client chooses the name; scope/owner still derived) and length-guard it with maxPresetNameBytes. Derive scope=presets.ScopeUser, clientID=&client.ID from auth.ClientFromContext. Call presets.ValidateOptsJSON(options) -> 422 on error (D-05). Call presetAdmin.Create(CreateParams{Name, Scope:ScopeUser, ClientID:&client.ID, TargetFormat, Options, Description}); map errors.Is(err, presets.ErrAlreadyExists) -> 409, other error -> 500. Return 201 with the response DTO (fetch via GetForClient or construct from returned id+version+inputs).
      - handleListPresets: parse ?all=true -> includeInactive; call presetAdmin.ListForClient(ctx, &client.ID, includeInactive); return 200 array of response DTOs (D-10). Active-only by default (D-01).
      - handleShowPreset: name = chi.URLParam(r,"name"), length-guard; call presetAdmin.GetForClient(ctx, &client.ID, name); ErrNotFound -> 404 no-leak (D-03), else 200 read-only-aware DTO (system rows keep scope=system, D-10).
      - handleUpdatePreset: name from path, length-guard; decode presetRequest; ValidateOptsJSON -> 422; call presetAdmin.Update(ctx, ScopeUser, &client.ID, name, TargetFormat, Options, Description); ErrNotFound -> 404 no-leak (covers nonexistent, cross-client, AND system-scope name because the write is scope=user only — D-03); return 200 with new version DTO (bump-on-update, D-03/PRAPI-02).
      - handleDeactivatePreset: name from path, length-guard; call presetAdmin.Deactivate(ctx, ScopeUser, &client.ID, name); ErrNotFound -> 404 no-leak; return 200/204 confirmation. Soft only (no hard delete, PRAPI-02).
    Use the SAME single no-leak 404 body string for every not-found across show/update/deactivate so nonexistent/cross-client/system are byte-identical (mirror handleGetJob's "job not found" discipline, D-03).
    In routes.go, INSIDE the existing r.Route("/v1", ...) block (so ByIP/auth/PerClient all apply — D-07/rate-limit pitfall) add: r.Route("/presets", func(r chi.Router){ r.Post("/", s.handleCreatePreset); r.Get("/", s.handleListPresets); r.Get("/{name}", s.handleShowPreset); r.Put("/{name}", s.handleUpdatePreset); r.Delete("/{name}", s.handleDeactivatePreset) }).
    In handlers_test.go update EVERY NewServer call site and newTestServer for the new positional arg; add a fakePresetAdmin (implements the five PresetAdmin methods with settable return values + a captured last-CreateParams for the mass-assignment assertion). Default it to ErrNotFound/empty so existing tests are unaffected.
    Add presets_handlers_test.go covering (all via srv.Routes().ServeHTTP with authed requests): create->201 version 1 & DTO omits id/client_id; create duplicate (fake returns ErrAlreadyExists)->409; create with scope/client_id in the JSON body -> asserts the captured CreateParams.Scope==ScopeUser and ClientID==authed client (mass-assignment ignored, P6); create with bad opts->422; list returns both a user and a read-only system entry with scope fields (D-10); show system-only name -> 200 scope=system; show cross-client/nonexistent -> 404 with the single no-leak body; update -> 200 bumped version; update system/cross-client name -> 404; delete -> success then show -> 404. Assert unauthenticated requests to /v1/presets and one to /v1/formats path shape return 401 (proving the routes sit inside the auth group; the formats 401 assertion may also live in formats_handlers_test.go).
  </action>
  <verify>
    <automated>cd /Users/apaderin/dev/octoconv && go build ./... && go test ./internal/api/... && go vet ./internal/api/...</automated>
  </verify>
  <done>PresetAdmin added without widening PresetRepo; five handlers enforce scope/owner from ctx with a narrow DTO; 409/404-no-leak/422 mappings correct; routes inside the /v1 auth+ratelimit group; handler tests (incl. mass-assignment + cross-client no-leak) pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GET /v1/formats registry-walk endpoint + route + test</name>
  <files>internal/api/formats_handlers.go, internal/api/routes.go, internal/api/formats_handlers_test.go</files>
  <behavior>
    - GET /v1/formats (authenticated) returns 200 with a registry-derived capability map: for each engine class, the supported (source,target) pairs, e.g. {"engines":{"image":{"pairs":[["png","webp"],...]},"document":{...},"html":{...}}}. The image class includes a known libvips pair.
    - The endpoint sits inside /v1: an unauthenticated GET /v1/formats returns 401.
    - The response is derived from convert.Default.Classes() — no hardcoded pair list (D-06).
  </behavior>
  <action>
    Create internal/api/formats_handlers.go with `func (s *Server) handleListFormats(w http.ResponseWriter, r *http.Request)`: call convert.Default.Classes(), reshape into the JSON envelope {"engines": {<class>: {"pairs": [[from,to],...]}}} where each pair is a two-element array; write 200 via writeJSON. Field naming is at discretion but MUST be registry-derived (D-06). In routes.go, inside the /v1 group, add r.Get("/formats", s.handleListFormats) (D-07). Add formats_handlers_test.go: authed GET /v1/formats -> 200 and the decoded body contains the image class with a known pair; unauthenticated GET /v1/formats -> 401 (auth group membership).
  </action>
  <verify>
    <automated>cd /Users/apaderin/dev/octoconv && go build ./... && go test ./internal/api/... && go vet ./...</automated>
  </verify>
  <done>GET /v1/formats returns a registry-derived engine->pairs map, requires auth, and its test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /v1/presets, /v1/formats | Authenticated-but-untrusted-with-elevated-scope JSON/requests cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Elevation of Privilege | POST/PUT /v1/presets body | mitigate | Narrow presetRequest DTO with no scope/client_id fields; scope hardcoded ScopeUser, clientID from auth.ClientFromContext — mass-assignment structurally impossible (D-02, PITFALLS P6). Test asserts captured CreateParams scope/owner ignore body fields. |
| T-20-02 | Elevation of Privilege / IDOR | show/update/deactivate by {name} | mitigate | Every repo call passes ScopeUser + &client.ID from ctx; no request-supplied client_id or id path ever reaches a repo method (PITFALLS P7). |
| T-20-03 | Information Disclosure | not-found responses | mitigate | Single byte-identical 404 body for nonexistent, cross-client, and system-scope-write misses (D-03) — no existence/manageability leak, mirrors handleGetJob. |
| T-20-04 | Information Disclosure | id / client_id in responses | mitigate | Response DTO whitelist omits id and client_id (D-04). |
| T-20-05 | Denial of Service | new /v1 routes bypassing rate limits | mitigate | /v1/presets and /v1/formats mounted INSIDE the existing r.Route("/v1") ByIP->auth->PerClient chain (D-07); test asserts 401 on unauthenticated access, proving group membership. |
| T-20-SC | Tampering | supply chain | accept | Zero new packages — implementation uses only stdlib + already-vetted internal packages; no npm/pip/cargo/go-get install step in this phase. |
</threat_model>

<verification>
- `go build ./...` clean; `go vet ./...` clean; `gofmt -l internal/ cmd/` reports no files.
- `go test ./internal/api/... ./internal/convert/...` passes (handler + registry unit tests; DB-gated presets integration tests skip without DATABASE_URL and are exercised live in 20-02).
- PresetRepo interface unchanged (grep confirms Resolve is still its only method); PresetAdmin is a separate interface.
</verification>

<success_criteria>
- All five /v1/presets verbs + GET /v1/formats implemented, wired inside the /v1 auth+rate-limit group.
- Narrow DTO + ctx-derived scope/owner (mass-assignment and IDOR closed); 409 on duplicate create; single no-leak 404; response DTO omits id/client_id.
- Merged effective list/show (system read-only) backed by shared repo methods — no shadow logic duplicated in handlers.
- /v1/formats is registry-derived. PRAPI-01, PRAPI-02, PRAPI-03 all have implementation coverage (proven live in 20-02).
</success_criteria>

<output>
Create `.planning/phases/20-presets-rest-format-discovery/20-01-SUMMARY.md` when done.
</output>
