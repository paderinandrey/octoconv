---
phase: 20-presets-rest-format-discovery
plan: 02
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - scripts/presets-rest-acceptance.sh
autonomous: true
requirements: [PRAPI-01, PRAPI-02, PRAPI-03]
must_haves:
  truths:
    - "D-01/PRAPI-01: all five /v1/presets verbs (create/list/show/update/deactivate) are proven end-to-end via curl against the live compose stack, each asserted against the real HTTP status/body and DB state"
    - "D-02/P6: a create body carrying scope=system and a foreign client_id produces a preset that the DB shows as scope=user owned by the calling client — mass-assignment has zero effect"
    - "D-03/PRAPI-02: duplicate active-name create returns 409; nonexistent, cross-client, and system-scope-write names all return a byte-identical no-leak 404"
    - "D-03: bump-on-update increases the version and there is no hard delete — a deactivated preset's rows still exist in the DB"
    - "D-04: the create/show/list response bodies never contain id or client_id"
    - "D-10: GET /v1/presets returns the caller's own presets AND system presets marked scope=system (read-only), the merged view Phase 21 consumes"
    - "D-06/PRAPI-03: GET /v1/formats returns a registry-derived engine->pairs map containing a known pair (e.g. png->webp)"
    - "D-07: /v1/formats and /v1/presets require auth — an unauthenticated request returns 401, proving they sit inside the /v1 rate-limited group"
  artifacts:
    - path: "scripts/presets-rest-acceptance.sh"
      provides: "executable live hard gate whose exit code IS the phase gate (set -euo pipefail)"
      min_lines: 120
  key_links:
    - from: "scripts/presets-rest-acceptance.sh"
      to: "live compose API (http://localhost:8090/v1/presets, /v1/formats)"
      via: "curl -w '%{http_code}' against the rebuilt api container"
      pattern: "/v1/presets|/v1/formats"
    - from: "scripts/presets-rest-acceptance.sh"
      to: "live Postgres (octoconv-db)"
      via: "docker exec psql assertions on scope/owner/row-existence"
      pattern: "docker exec.*psql"
---

<objective>
Extend the project's live-gate discipline (unconditional since Phase 17) to the new REST surface: a curl-driven hard gate that proves `/v1/presets` CRUD and `GET /v1/formats` behave correctly against the REAL docker-compose stack — not fakes. The script's exit code is the gate.

Purpose: Handler unit tests use fakes; only a live run proves the merged shadow-precedence SQL, the 409/404 no-leak mappings, mass-assignment resistance, and the registry-derived formats map against real Postgres + the built api image.
Output: `scripts/presets-rest-acceptance.sh`, executed green end-to-end.
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-presets-rest-format-discovery/20-CONTEXT.md
@.planning/phases/20-presets-rest-format-discovery/20-01-PLAN.md

<read_first>
- scripts/presets-acceptance.sh — the Phase 18 CLI live gate; reuse its exact structure verbatim where possible: assert_eq/assert_contains helpers, psql_q helper, http_post/http_get pattern (curl -s -o file -w '%{http_code}'), the compose bring-up (`docker compose -p octoconv -f docker-compose.yml -f docker-compose.e2e.yml up -d --build api` then `up -d`), the /healthz readiness loop, DATABASE_URL/API_KEY_SALT/DB_CONTAINER=octoconv-db constants, uuidgen SUFFIX naming, mktemp WORKDIR + trap cleanup, and the "stack left running" tail
- cmd/manage-clients/main.go — output format: "client id: <uuid>" and "api key (save now, shown once): <key>" (parse with awk -F': ', same as the Phase 18 script)
- cmd/manage-presets/main.go — used to seed a SYSTEM-scope preset (no --client-id) so the merged-list read-only assertion (D-10) has a system entry to find
- internal/api/presets_handlers.go + formats_handlers.go (from 20-01) — the exact JSON request shape (application/json body: name/target_format/options/description) and response field names to assert against
</read_first>

<env_notes>
- docker available (Docker 29.4.0); jq and yq available on PATH; no shellcheck (use `bash -n` for syntax).
- Compose container names: octoconv-db (Postgres, host port 5434), octoconv-api. DATABASE_URL for host tools: postgres://octo:octo-pass@localhost:5434/octo_db. API base: http://localhost:8090.
- Only the api image needs `--build`; the rest come from existing images. Always pass `-p octoconv`.
- Preset CRUD + formats need only api + postgres reachable (no image worker needed for CRUD), but bring up the full stack exactly like the Phase 18 script for parity and a working /healthz.
</env_notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author scripts/presets-rest-acceptance.sh</name>
  <files>scripts/presets-rest-acceptance.sh</files>
  <action>
    Create scripts/presets-rest-acceptance.sh (chmod +x), modeled on scripts/presets-acceptance.sh. Reuse its header discipline (set -euo pipefail; cd to repo root; assert_eq/assert_contains/psql_q helpers; http_post/http_get wrappers extended for arbitrary methods/paths; compose bring-up with -p octoconv --build api; /healthz wait loop; uuidgen SUFFIX; WORKDIR trap; PASS_COUNT tally; "stack left running" tail). Add an http_json helper that issues a method+path with a JSON body and Authorization header, capturing HTTP_STATUS and the body file.
    Mint one client A and one client B via `go run ./cmd/manage-clients create ...` (parse id + api key). Seed one SYSTEM-scope preset via `go run ./cmd/manage-presets create --name sys-<SUFFIX> --target webp` (no --client-id) so the merged read-only assertion has a target.
    Assertions (each with a labeled PASS line; any failure aborts non-zero):
      1. CREATE: POST /v1/presets (client A) JSON {"name":"pa-rest-<SUFFIX>","target_format":"webp"} -> 201; body contains "version":1 and "scope":"user"; body does NOT contain "client_id" and does NOT contain "\"id\"" (D-04). Assert absence with `grep -qv` style checks that fail loudly if present.
      2. 409: repeat the same POST -> 409 (D-03).
      3. VALIDATE-OPTS: POST with options that fail ValidateOptsJSON (e.g. {"name":"pa-bad-<SUFFIX>","target_format":"pdf","options":{"margin_mm":9999}}) -> 422 (D-05).
      4. MASS-ASSIGNMENT: POST (client A) JSON including "scope":"system" and "client_id":"<CLIENT_B_ID>" alongside name/target_format -> 201; then psql assert the created row is scope='user' AND client_id=<CLIENT_A_ID> (body fields ignored, D-02/P6).
      5. LIST: GET /v1/presets (client A) -> 200; body contains the client's own preset name AND the seeded system preset name with "scope":"system" (D-10 merged read-only view).
      6. SHOW own: GET /v1/presets/pa-rest-<SUFFIX> (client A) -> 200; contains "target_format":"webp".
      7. SHOW system read-only: GET /v1/presets/sys-<SUFFIX> (client A) -> 200; contains "scope":"system" (D-10).
      8. UPDATE bump: PUT /v1/presets/pa-rest-<SUFFIX> (client A) JSON {"target_format":"png"} -> 200; body contains "version":2 (bump-on-update, D-03); psql assert exactly one active row at version 2 and v1 inactive.
      9. NO-LEAK 404s: capture bodies of (a) GET nonexistent name, (b) GET pa-rest-<SUFFIX> as client B (cross-client), (c) PUT sys-<SUFFIX> as client A (system-scope write) — all -> 404 and all three bodies byte-identical (D-03).
      10. DELETE soft: DELETE /v1/presets/pa-rest-<SUFFIX> (client A) -> success (2xx); then GET show -> 404; psql assert rows for that name still exist (count >= 1, no hard delete, PRAPI-02).
      11. ?all: GET /v1/presets?all=true (client A) -> 200 and includes an inactive version of the deleted/bumped preset (D-01).
      12. FORMATS: GET /v1/formats (client A) -> 200; jq-parse and assert an "image"/engines entry and a known pair (png,webp) is present (registry-derived, D-06). Also assert an unauthenticated GET /v1/formats (no Authorization header) -> 401 and an unauthenticated GET /v1/presets -> 401 (D-07 auth-group membership).
    End with a summary block enumerating which D-NN/SC each block proved and "Stack left running for inspection (compose project: octoconv)."
  </action>
  <verify>
    <automated>cd /Users/apaderin/dev/octoconv && bash -n scripts/presets-rest-acceptance.sh && test -x scripts/presets-rest-acceptance.sh</automated>
  </verify>
  <done>Script parses cleanly (bash -n), is executable, and encodes every assertion block above with loud labeled PASS/FAIL lines.</done>
</task>

<task type="auto">
  <name>Task 2: Run the live hard gate against the compose stack</name>
  <files>scripts/presets-rest-acceptance.sh</files>
  <action>
    Execute scripts/presets-rest-acceptance.sh end-to-end. This UNCONDITIONALLY brings up the compose stack (rebuilding the api image with the 20-01 handlers), waits for /healthz, and runs every assertion. If any assertion fails, read the loud FAIL line + the actual body/status it prints, diagnose against the 20-01 handlers, and fix (either the script's expectation if it mis-encoded a decision, or — via a follow-up gap in 20-01 — the handler). Do NOT weaken an assertion to make it pass; the assertions encode the locked D-NN decisions. Re-run until the script exits 0 with "ALL <N> ASSERTIONS PASSED". Leave the stack running (matches Phase 16/17/18 precedent). Record the final PASS_COUNT in the SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/apaderin/dev/octoconv && bash scripts/presets-rest-acceptance.sh</automated>
  </verify>
  <done>The script exits 0 with all assertions passing, proving PRAPI-01/02/03 and D-01..D-10's observable REST behaviors against the real stack.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test client → live /v1 REST surface | The gate drives the real authenticated boundary the same way an internal client would |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-L1 | Elevation of Privilege | mass-assignment via body | mitigate | Assertion 4 posts scope=system + foreign client_id and psql-verifies the row is scope=user owned by the caller (live proof of T-20-01). |
| T-20-L2 | Information Disclosure | existence/manageability leak | mitigate | Assertion 9 proves nonexistent/cross-client/system-write 404 bodies are byte-identical (live proof of T-20-03). |
| T-20-L3 | Denial of Service | unauthenticated access | mitigate | Assertion 12 proves /v1/formats and /v1/presets return 401 without a key (live proof of /v1 group membership, T-20-05). |
| T-20-SC | Tampering | supply chain | accept | No new packages; only api image rebuild from vetted source. |
</threat_model>

<verification>
- `bash -n` passes; script is executable.
- Live run exits 0 with "ALL <N> ASSERTIONS PASSED"; stack left running for inspection.
- Every assertion label maps to a D-NN decision from 20-CONTEXT.md.
</verification>

<success_criteria>
- The live hard gate proves, against the real compose stack, all five /v1/presets verbs, the 409/no-leak-404/mass-assignment/no-hard-delete/bump semantics, the merged read-only system view, and the registry-derived authenticated /v1/formats endpoint.
- PRAPI-01, PRAPI-02, PRAPI-03 verified end-to-end.
</success_criteria>

<output>
Create `.planning/phases/20-presets-rest-format-discovery/20-02-SUMMARY.md` when done (record final PASS_COUNT and the compose invocation used).
</output>
