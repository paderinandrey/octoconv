---
phase: 22-cfb-classification
plan: 02
type: execute
wave: 2
depends_on: [22-01]
files_modified:
  - internal/api/handlers.go
  - internal/api/handlers_test.go
  - internal/api/testdata/legacy.doc
  - internal/api/testdata/encrypted.docx
  - internal/e2e/e2e_test.go
autonomous: true
requirements: [CFB-01]
must_haves:
  truths:
    - "D-06: a password-protected OOXML upload (ClassifyCFB → CFBEncrypted) gets 422 'password-protected Office file is not supported; remove the password and re-upload'"
    - "D-06: a legacy binary .doc/.xls/.ppt upload (ClassifyCFB → CFBLegacy) gets a DISTINCT 422 'legacy binary Office format (.doc/.xls/.ppt) is not supported; convert to docx/xlsx/pptx'"
    - "D-06: an unrecognized/corrupt CFB (ClassifyCFB → CFBUnknown) falls through to the EXISTING combined 422 text unchanged — fail-closed compat, no new message"
    - "D-06: the three cases log distinct reason= tags: encrypted_document / legacy_document / legacy_or_encrypted_document"
    - "D-06: all three branches reject BEFORE any S3 upload or Postgres job row — no storage write, no job created"
    - "D-07: TestOLECFBRejectionE2E asserts legacy.doc gets the legacy message and encrypted.docx gets the encrypted message, live against the compose stack (unconditional hard gate)"
  artifacts:
    - path: "internal/api/handlers.go"
      provides: "the IsOLECFB branch split into a three-way ClassifyCFB switch (D-06)"
      contains: "convert.ClassifyCFB"
    - path: "internal/api/handlers_test.go"
      provides: "handler tests asserting distinct messages/log-reasons for each CFBClass over the real fixtures, all pre-storage"
      contains: "func TestCreateJob_CFB"
    - path: "internal/e2e/e2e_test.go"
      provides: "TestOLECFBRejectionE2E updated to assert the two DISTINCT messages per fixture"
  key_links:
    - from: "internal/api/handlers.go handleCreateJob"
      to: "internal/convert.ClassifyCFB"
      via: "the existing detected=='' && IsOLECFB branch now switches on ClassifyCFB(file, header.Size)"
      pattern: "convert\\.ClassifyCFB"
    - from: "internal/e2e/e2e_test.go TestOLECFBRejectionE2E"
      to: "live compose API"
      via: "postJobExpectStatus asserting per-fixture distinct 422 substrings"
      pattern: "postJobExpectStatus"
---

<objective>
Split the single OLE-CFB 422 in `handleCreateJob` into three branches driven by `convert.ClassifyCFB` (from Plan 22-01): a distinct "password-protected" 422, a distinct "legacy binary format" 422, and — for anything unrecognized — the EXISTING combined message unchanged (fail-closed). Add handler tests over the real fixtures and update the live e2e gate to prove the two distinct messages against the running stack (D-07, unconditional).

Purpose: This is the client-facing deliverable of the phase (CFB-01) — clients can finally tell "remove the password" from "convert the file format". The live gate is the only proof the wired-in ClassifyCFB behaves correctly against real fixtures through the full HTTP path.
Output: modified `internal/api/handlers.go`, `internal/api/handlers_test.go` (+ copied fixtures), updated `internal/e2e/e2e_test.go`, and a green live TestOLECFBRejectionE2E.
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-cfb-classification/22-CONTEXT.md
@.planning/phases/22-cfb-classification/22-01-PLAN.md

<read_first>
- internal/api/handlers.go (~lines 239-259) — the EXACT branch to split. Current shape: `if detected == "" && convert.IsOLECFB(file) { log.Printf(...reason=legacy_or_encrypted_document...); writeError(w, 422, "legacy binary or password-protected Office format is not supported; convert to docx/xlsx/pptx or remove the password"); return }`. Note: `file` is the multipart `io.ReaderAt`, `header.Size` is its int64 length (the same pair passed to convert.SniffContainer two branches above), and `client.ID`/`filename` are the log fields already in use. Keep IsOLECFB as the cheap pre-gate (D-01) — call ClassifyCFB only inside the existing IsOLECFB branch.
- internal/api/handlers_test.go (~lines 560-590, TestCreateJob_OLECFBRejected) — the existing synthetic-bytes CFB test. Its `0xD0CF11E0...+"legacy sector padding"` input is NOT a valid CFB directory, so under the new code it classifies as CFBUnknown and hits the UNCHANGED combined message (still contains "password") — that test stays green as the fail-closed-compat proof. Also read multipartBody / newTestServer / fakeStorage(.uploaded) / fakeRepo(.created) helpers to mirror the assertion style (status 422, no upload, no job created, message substring).
- internal/e2e/e2e_test.go (oleCFBFixtures + TestOLECFBRejectionE2E, ~lines 518-552, and the postJobExpectStatus helper ~226-242) — the live gate to update. Currently a loose `bytes.Contains(...,"password")` substring for both fixtures; change to per-fixture DISTINCT expected substrings.
- internal/e2e/testdata/legacy.doc + encrypted.docx — the real fixtures the e2e test already loads; COPY the same two into internal/api/testdata/ for the handler test.
- .planning/research/PITFALLS.md Pitfall 11 — the fail-closed default must NOT weaken: CFBUnknown MUST map to reject (the existing generic 422), never a proceed-to-conversion path.
</read_first>

<message_contract>
The three exact client-facing strings and log reasons (D-06) — the handler tests and e2e assert on these:
  - CFBEncrypted → 422 body: "password-protected Office file is not supported; remove the password and re-upload"   | log reason=encrypted_document
  - CFBLegacy    → 422 body: "legacy binary Office format (.doc/.xls/.ppt) is not supported; convert to docx/xlsx/pptx" | log reason=legacy_document
  - CFBUnknown   → 422 body: the EXISTING string unchanged ("legacy binary or password-protected Office format is not supported; convert to docx/xlsx/pptx or remove the password") | log reason=legacy_or_encrypted_document
Distinct-substring anchors for tests: encrypted body contains "remove the password" AND "password-protected"; legacy body contains "legacy binary" AND ".doc/.xls/.ppt" but NOT "password". Keep the `writeError(w, http.StatusUnprocessableEntity, ...)` shape and the `log.Printf("content validation rejected: client_id=%s filename=%q reason=%s", ...)` shape already used in this file (D-08 scoped-logging convention).
</message_contract>

<live_gate_infra>
Bring-up (mirror Phase 20/21 live gates; only the api image embeds the new convert code, so only it needs rebuild):
  docker compose -p octoconv -f docker-compose.yml -f docker-compose.e2e.yml up -d --build api
  docker compose -p octoconv -f docker-compose.yml -f docker-compose.e2e.yml up -d   # remaining services
Run the single CFB e2e test (the OLE-CFB rejection is pre-storage — no S3 download, so no E2E_S3_DIAL_ADDR needed):
  E2E_BASE_URL=http://localhost:8090 \
  DATABASE_URL=postgres://octo:octo-pass@localhost:5434/octo_db \
  API_KEY_SALT=dev-only-change-me-in-real-deploys \
  go test ./internal/e2e/ -run 'TestOLECFBRejectionE2E' -v -timeout 5m
Hang rule: e2eHTTP already carries a 30s per-request timeout and `-timeout 5m` bounds the whole test — a hung classify would surface as a diagnosable timeout, never a silent stall. The test asserting a bounded 422 (not a hang) IS success-criterion 3's live proof. Leave the stack running for inspection (Phase 18/20 precedent).
</live_gate_infra>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Split the handleCreateJob OLE-CFB branch on ClassifyCFB + handler tests</name>
  <files>internal/api/handlers.go, internal/api/handlers_test.go, internal/api/testdata/legacy.doc, internal/api/testdata/encrypted.docx</files>
  <behavior>
    - POST a real encrypted.docx (as "in.docx"→pdf): status 422, body contains "remove the password", NO S3 upload (store.uploaded false), NO job created (repo.created nil), log reason=encrypted_document.
    - POST a real legacy.doc (as "in.doc"→pdf): status 422, body contains "legacy binary" and ".doc/.xls/.ppt", body does NOT contain "password", no upload, no job, log reason=legacy_document.
    - The existing TestCreateJob_OLECFBRejected (synthetic magic+padding = CFBUnknown) still returns 422 with the unchanged combined message containing "password" (fail-closed-compat, Pitfall 11) and still no upload/no job.
  </behavior>
  <action>
    Copy fixtures: `cp internal/e2e/testdata/legacy.doc internal/e2e/testdata/encrypted.docx internal/api/testdata/` (create the dir).
    In internal/api/handlers.go, replace the body of the existing `if detected == "" && convert.IsOLECFB(file) { ... }` branch (keep the IsOLECFB guard as the cheap pre-filter per D-01). Inside it, call `convert.ClassifyCFB(file, header.Size)` (same file/size pair passed to convert.SniffContainer above; ClassifyCFB uses io.ReaderAt and never disturbs any sequential cursor) and switch on the result into the three arms of <message_contract>: CFBEncrypted, CFBLegacy, and default/CFBUnknown → the existing string + reason. Each arm logs its distinct reason via the existing `log.Printf("content validation rejected: client_id=%s filename=%q reason=%s", client.ID, filename, <reason>)` form and `writeError(w, http.StatusUnprocessableEntity, <msg>); return`. Update the branch's leading comment to explain the three-way split and cite D-06 (replace the old "rejects both as one branch (v2 DOCV3-02)" comment, which this phase supersedes). Do NOT add an error_code field (research anti-feature) — message-only distinction, consistent with every other 422 in this handler.
    In internal/api/handlers_test.go add `func TestCreateJob_CFBEncrypted` and `func TestCreateJob_CFBLegacy` (or one table-driven `TestCreateJob_CFB`) modeled on TestCreateJob_OLECFBRejected: read the copied fixture via os.ReadFile("testdata/<name>"), post via multipartBody + authed + newTestServer, assert the exact status/message-substring/no-upload/no-job per <behavior>. Leave TestCreateJob_OLECFBRejected in place (it now documents the CFBUnknown fail-closed-compat case) — optionally add a comment noting the synthetic bytes now exercise CFBUnknown.
  </action>
  <verify>
    <automated>cd /Users/apaderin/dev/octoconv && go test ./internal/api/ -run 'TestCreateJob' && go vet ./internal/api/</automated>
  </verify>
  <done>The three CFB cases produce their distinct (or unchanged-for-unknown) 422 messages and log reasons, all before any storage write or job creation; existing OLE-CFB compat test still green; go vet clean.</done>
</task>

<task type="auto">
  <name>Task 2: Update TestOLECFBRejectionE2E to assert distinct messages + run the live hard gate (D-07)</name>
  <files>internal/e2e/e2e_test.go</files>
  <action>
    Update TestOLECFBRejectionE2E (internal/e2e/e2e_test.go): replace the shared `oleCFBFixtures []string` + loose "password" substring with a per-fixture table pairing each fixture to its DISTINCT expected substring — legacy.doc → "legacy binary" (assert the body also does NOT contain "password", proving distinctness), encrypted.docx → "remove the password". Keep the postJobExpectStatus(...422...) pre-storage assertion (no job id is produced — matches the existing helper's contract) and the case-insensitive compare style. Keep the existing comment discipline; note D-06/D-07 and that the 422 status + bounded return (never a hang) is success-criterion 3's live proof.
    Then run the UNCONDITIONAL live hard gate per <live_gate_infra>: bring up the compose stack rebuilding the api image (which now embeds the 22-01 parser + the Task 1 branch), and run `go test ./internal/e2e/ -run 'TestOLECFBRejectionE2E' -v -timeout 5m` with the env vars. Both subtests (legacy.doc, encrypted.docx) MUST pass with their distinct messages. If a subtest fails, read the actual 422 body it prints and diagnose against Task 1's message contract or 22-01's ClassifyCFB (never weaken the assertion). Re-run until green. Leave the stack running.
    Also confirm the full offline suite is unaffected: `go test ./...` (e2e self-skips without E2E_BASE_URL, so this stays green offline).
  </action>
  <verify>
    <automated>cd /Users/apaderin/dev/octoconv && go build ./... && go test ./... && docker compose -p octoconv -f docker-compose.yml -f docker-compose.e2e.yml up -d --build api && docker compose -p octoconv -f docker-compose.yml -f docker-compose.e2e.yml up -d && E2E_BASE_URL=http://localhost:8090 DATABASE_URL=postgres://octo:octo-pass@localhost:5434/octo_db API_KEY_SALT=dev-only-change-me-in-real-deploys go test ./internal/e2e/ -run 'TestOLECFBRejectionE2E' -v -timeout 5m</automated>
  </verify>
  <done>TestOLECFBRejectionE2E passes live: legacy.doc → the legacy 422 (no "password"), encrypted.docx → the "remove the password" 422, both bounded (no hang), before any job creation. Offline `go test ./...` green. Stack left running.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → handleCreateJob content validation | Untrusted multipart bytes reach ClassifyCFB pre-storage; the branch must reject every CFB case before any S3/Postgres write (same boundary as the existing Sniff/SniffContainer/IsOLECFB checks). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-05 | Tampering (fail-closed regression) | the new three-way switch | mitigate | CFBUnknown maps to the EXISTING generic reject (unchanged message); no arm reaches storage/job-create. Proven by the retained TestCreateJob_OLECFBRejected (CFBUnknown) + the handler tests asserting no upload/no job (Pitfall 11). |
| T-22-06 | Denial of Service | ClassifyCFB on request bytes | mitigate | The bounded/cycle-guarded parser (Plan 22-01, T-22-01/02) runs here; the e2e `-timeout 5m` + 30s per-request client timeout make any regression surface as a diagnosable timeout, not a silent API stall (success-criterion 3). |
| T-22-07 | Information Disclosure | 422 message wording | accept | Messages name only the file category and remediation (no internal paths/errors), consistent with every other content-validation 422; no error_code taxonomy added (research anti-feature). |
| T-22-SC | Tampering | supply chain | accept | No new packages; only the api image is rebuilt from vetted source. No package-install task, so no legitimacy gate applies. |
</threat_model>

<verification>
- `go test ./internal/api/ -run 'TestCreateJob'` passes (three distinct CFB outcomes + fail-closed-compat).
- `go build ./...` and offline `go test ./...` green (e2e self-skips offline).
- Live `TestOLECFBRejectionE2E` passes with per-fixture DISTINCT messages against the rebuilt api image (D-07 unconditional gate); stack left running.
</verification>

<success_criteria>
- A password-protected OOXML upload and a legacy binary .doc/.xls/.ppt upload receive DISTINCT, accurate 422s; an unrecognized CFB keeps the existing generic 422 unchanged — all before storage/job-create (CFB-01 integration half, success criteria 1 & 2).
- A bounded 422 (never a hang) is live-proven end-to-end for both real fixtures (success criterion 3).
</success_criteria>

<output>
Create `.planning/phases/22-cfb-classification/22-02-SUMMARY.md` when done (record the exact live 422 bodies observed for each fixture and the compose invocation used).
</output>
