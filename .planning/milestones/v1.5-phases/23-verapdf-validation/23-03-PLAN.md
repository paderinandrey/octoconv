---
phase: 23-verapdf-validation
plan: 03
type: execute
wave: 3
depends_on: ["23-01", "23-02"]
files_modified:
  - internal/e2e/e2e_test.go
  - internal/e2e/testdata/trigger.docx
  - internal/e2e/testdata/verapdf-shim/soffice
  - internal/e2e/testdata/verapdf_noncompliant_marker.pdf
  - docker-compose.e2e.yml
autonomous: true
requirements: [PDFA-01, PDFA-02]
user_setup: []

must_haves:
  truths:
    - "LIVE UNCONDITIONAL: a real PDF/A-2b export job against the rebuilt document-worker image (veraPDF active in the path) reaches status=done — no regression vs the v1.3 fixture path (D-09, ROADMAP SC#2)"
    - "LIVE UNCONDITIONAL: a deliberately non-compliant, /GTS_PDFA-marker-bearing PDF fed through the real validation path fails the job TERMINALLY (status=failed, no retry loop) with the veraPDF non-conformance reason in job_events (D-09, ROADMAP SC#1)"
    - "The document-worker image build-time delta from bundling veraPDF is recorded and the CI docker-build tier stays within its 20-minute bound (D-10)"
  artifacts:
    - path: "internal/e2e/e2e_test.go"
      provides: "TestPDFANonCompliantE2E live terminal-fail gate + pollUntilFailed helper"
      contains: "TestPDFANonCompliantE2E"
    - path: "internal/e2e/testdata/verapdf_noncompliant_marker.pdf"
      provides: "Marker-bearing (/GTS_PDFA present) but ISO-non-compliant PDF — passes the cheap pre-filter, fails real veraPDF (23-CONTEXT specifics: compliant export with corrupted XMP)"
    - path: "internal/e2e/testdata/verapdf-shim/soffice"
      provides: "E2E-only soffice interposer: transparent pass-through to the real soffice except for the trigger fixture, where it emits the non-compliant PDF into the validation path"
    - path: "docker-compose.e2e.yml"
      provides: "E2E-only document-worker override: single-file shadow mount of the shim over /usr/local/bin/soffice (test-only fault injection, confined to the .e2e.yml precedent) — NO PATH duplication"
      contains: "verapdf-shim"
  key_links:
    - from: "internal/e2e/testdata/verapdf-shim/soffice"
      to: "document-worker soffice resolution"
      via: "docker-compose.e2e.yml single-file bind mount over /usr/local/bin/soffice (ahead of /usr/bin on the image PATH — no PATH env override)"
      pattern: "verapdf-shim"
    - from: "TestPDFANonCompliantE2E"
      to: "job_events.detail"
      via: "live Postgres query (db.Connect, provisionClient precedent)"
      pattern: "job_events"
---

<objective>
Prove the whole phase live, unconditionally, against the rebuilt compose stack (D-09 LIVE HARD
GATE): a genuine PDF/A-2b export passes end-to-end with real veraPDF now in the path, and a
deliberately non-compliant (but /GTS_PDFA-marker-bearing) PDF fed through the real validation
path fails terminally with the veraPDF reason recorded in job_events. Record the document-worker
image build-time delta against the CI docker-build 20-minute bound (D-10).

Purpose: The unit fixtures in Plan 02 prove the parser; only a live run proves the wiring — the
rebuilt image, the JVM inside the resource-limited container, the terminal classification, and
the job_events diagnostics trail all working together (ROADMAP success criteria 1-4).
Output: Extended e2e suite with the non-compliant hard gate, e2e-only fault-injection shim, and
recorded build/e2e timing deltas.
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-verapdf-validation/23-CONTEXT.md
@.planning/phases/23-verapdf-validation/23-01-SUMMARY.md
@.planning/phases/23-verapdf-validation/23-02-SUMMARY.md

@internal/e2e/e2e_test.go
@docker-compose.e2e.yml

<read_first>
- `internal/e2e/e2e_test.go` — `TestPDFAExportE2E` (~577, the compliant-path gate this plan re-runs against the rebuilt image), `pollUntilDone` (~249, NOTE: it FATALS on "failed" — the non-compliant test needs a `pollUntilFailed` sibling that succeeds on "failed" and fatals on "done"/timeout), `provisionClient` (~92, the db.Connect + DATABASE_URL precedent the job_events query reuses), `postJob`/`buildJobRequest` (multipart + opts field shape), `pdfAProfileOpts` (~569).
- `docker-compose.e2e.yml` — the established E2E-ONLY override precedent ("MUST NEVER be used in production", SSRF relaxation confined here). The shim mount for document-worker follows exactly this confinement pattern.
- `Dockerfile.document-worker` (post-23-01) — the real soffice lives at `/usr/bin/soffice` (Debian libreoffice packages). The Debian base PATH puts `/usr/local/bin` AHEAD of `/usr/bin`, so a single-file bind mount of the shim at `/usr/local/bin/soffice` shadows the real binary WITHOUT any PATH env override (WARNING-6 fix — do NOT replicate/duplicate the image's full PATH in compose). The shim's pass-through invokes `/usr/bin/soffice` by absolute path, so the real binary is still reachable.
- `internal/worker/worker.go` `HandleDocumentConvert` MarkFailed call (~365) — the veraPDF reason lands in `job_events.detail` as `{"engine_stderr": err.Error()}` (D-07); the canonical terminal substring to assert is `"pdf/a non-compliant"` (defined in 23-02).
- `internal/convert/libreoffice.go` `Convert` (~109-130) — the shim must honor soffice's argv contract: `--convert-to pdf:<filter>[:<suffix>] --outdir <workDir> <inPath>`, producing `<workDir>/in.pdf` (input is always `in.docx`); exit 0 with the output file present.
- `.github/workflows/ci.yml` docker-build tier (~47-74, `timeout-minutes: 20`) and e2e tier (~80-141, 25-min cap, `go test ./internal/e2e/... -timeout 30m`) — the D-10 budget context.
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Craft the marker-bearing non-compliant fixture + trigger docx + e2e-only soffice shim (shadow-mounted, no PATH duplication)</name>
  <files>internal/e2e/testdata/verapdf_noncompliant_marker.pdf, internal/e2e/testdata/trigger.docx, internal/e2e/testdata/verapdf-shim/soffice, docker-compose.e2e.yml</files>
  <action>
Plan 01's `verapdf_noncompliant.pdf` (a plain non-A PDF) cannot exercise the live veraPDF path — it
fast-fails the `/GTS_PDFA` pre-filter (existing "output missing pdf/a outputintent marker" signature)
before any JVM spins up. The live gate needs a fixture that PASSES the pre-filter and fails veraPDF
(23-CONTEXT specifics line 58, first option): inside the built 23-01 image, generate a compliant
PDF/A-2b export from `sample.docx` (same soffice invocation as scripts/verapdf-measure.sh), then patch
it with a byte-length-preserving corruption of its XMP PDF/A identification (e.g. replace the ASCII
bytes `pdfaid` with `pdfXid` — same length, so the xref table stays valid). Verify empirically inside
the container, all three: (1) `verapdf -f 2b` reports it NON-compliant (do not assume — executor-verified);
(2) `grep -c GTS_PDFA` finds the OutputIntent marker still present; (3) the file still begins `%PDF-`.
Commit as `internal/e2e/testdata/verapdf_noncompliant_marker.pdf`. If the pdfaid patch alone does not
flip the verdict, escalate the corruption (e.g. additionally length-preserving-corrupt the xmpmeta packet
header) until veraPDF flags it — record the exact patch used.

Create `internal/e2e/testdata/trigger.docx`: a copy of sample.docx rebuilt as a STORED (uncompressed)
zip — unzip sample.docx and re-zip with `zip -X -0` — so its document text, which must include the
literal canary string `VERAPDF-E2E-NONCOMPLIANT-TRIGGER`, is greppable from raw bytes (`grep -aq`).
Verify it still sniffs as docx (`convert.SniffContainer` accepts stored-entry zips) and that a normal
docx->pdf conversion of it succeeds in the stack (it must reach the worker, not 422).

Create the shim `internal/e2e/testdata/verapdf-shim/soffice` (POSIX sh, `chmod 0755`): parse `--outdir`
and the trailing input path from its argv; if `grep -aq VERAPDF-E2E-NONCOMPLIANT-TRIGGER "$input"`,
copy the mounted non-compliant fixture (read from the fixed absolute mount path
`/e2e-fixtures/verapdf_noncompliant_marker.pdf`, see the compose mount below) to
`"$outdir/$(basename input, ext).pdf"` (i.e. `in.pdf`) and exit 0 — injecting the non-compliant bytes
exactly where soffice would have written its output, so EVERYTHING downstream (validatePDF, /GTS_PDFA
pre-filter, real veraPDF JVM, terminal classification, MarkFailed, job_events) is the genuine production
path; otherwise `exec /usr/bin/soffice "$@"` (fully transparent for every other document e2e test — the
real binary is reached by absolute path regardless of PATH).

In `docker-compose.e2e.yml`, add a `document-worker` override following the file's established
test-only confinement pattern, using a SHADOW MOUNT instead of a PATH override (WARNING-6 — no PATH
duplication): read-only bind-mount the shim file directly over `/usr/local/bin/soffice`
(`./internal/e2e/testdata/verapdf-shim/soffice:/usr/local/bin/soffice:ro`), which sits ahead of
`/usr/bin/soffice` on the image PATH so it is resolved first with NO `PATH` env change; and read-only
bind-mount the fixture to the fixed path the shim reads
(`./internal/e2e/testdata/verapdf_noncompliant_marker.pdf:/e2e-fixtures/verapdf_noncompliant_marker.pdf:ro`).
Do NOT set or replicate the container `PATH`. Add a comment mirroring the file's header discipline:
E2E-ONLY fault injection, never production. Files must be world-readable/executable (worker runs as USER nobody).
  </action>
  <verify>
    <automated>docker run --rm -v "$PWD/internal/e2e/testdata:/t" --entrypoint verapdf octoconv-document-worker:verapdf -f 2b /t/verapdf_noncompliant_marker.pdf | grep -iq 'compliant="false"\|isCompliant.*false' && grep -aq GTS_PDFA internal/e2e/testdata/verapdf_noncompliant_marker.pdf && grep -aq VERAPDF-E2E-NONCOMPLIANT-TRIGGER internal/e2e/testdata/trigger.docx && test -x internal/e2e/testdata/verapdf-shim/soffice</automated>
  </verify>
  <done>verapdf_noncompliant_marker.pdf is empirically confirmed non-compliant under `verapdf -f 2b` in the pinned image while still carrying /GTS_PDFA and %PDF-; trigger.docx is greppable for the canary string and converts normally without the shim; the shim passes through transparently for non-trigger inputs (`exec /usr/bin/soffice`) and injects the fixture for the trigger; docker-compose.e2e.yml shadow-mounts the shim over /usr/local/bin/soffice for document-worker only, with the fixture mounted read-only at /e2e-fixtures, and sets NO PATH override. (Adjust the non-compliant grep to the exact report schema recorded in 23-01-SUMMARY.)</done>
</task>

<task type="auto">
  <name>Task 2: TestPDFANonCompliantE2E — live terminal-fail gate with job_events assertion + pollUntilFailed helper</name>
  <files>internal/e2e/e2e_test.go</files>
  <action>
Add `pollUntilFailed(t, baseURL, apiKey, jobID, timeout)` — the inverse sibling of `pollUntilDone`:
same 2s poll loop, returns the terminal body on `"failed"`, fatals on `"done"` (the whole point is the
job must NOT succeed) and on timeout. Keep the two helpers adjacent with a doc comment explaining the
inversion so they never drift.

Add `TestPDFANonCompliantE2E` (e2eSetup + provisionClient, standard shape): POST `trigger.docx` ->
`pdf` with `opts=pdfAProfileOpts`, `pollUntilFailed` within 5 minutes (a retry loop burning
DOCUMENT_MAX_RETRY x backoff would blow this bound — the 5-min budget is itself the live proof the
failure was TERMINAL, not retried), then assert:
  1. the failed body's `error_code` == "engine_error" (the worker's terminal-path code);
  2. the veraPDF reason is in job_events (D-07/D-09): open the live Postgres via `db.Connect` (reusing
     the provisionClient DATABASE_URL precedent), query
     `SELECT detail->>'engine_stderr' FROM job_events WHERE job_id=$1 AND to_status='failed'`,
     and assert the value contains the canonical `"pdf/a non-compliant"` substring (23-02's terminal
     signature) — i.e. the actual rule-violation summary reached the diagnostics trail.
Also assert the compliant path is untouched: `TestPDFAExportE2E` stays as-is (it is now the compliant
live gate — real veraPDF validates the real export before MarkDone) and the shim's pass-through must
not affect it. `go vet ./internal/e2e/` and gofmt clean; the suite still self-skips without E2E_BASE_URL.
  </action>
  <verify>
    <automated>go vet ./internal/e2e/ && go test ./internal/e2e/ -run 'TestPDFANonCompliantE2E' -count=1</automated>
  </verify>
  <done>TestPDFANonCompliantE2E compiles and self-skips offline; pollUntilFailed exists; the test asserts terminal failure within the 5-min no-retry bound AND the "pdf/a non-compliant" veraPDF reason in job_events.detail via live Postgres.</done>
</task>

<task type="auto">
  <name>Task 3: LIVE HARD GATE (unconditional) — rebuilt stack, full PDF/A e2e pass, build-time delta recorded</name>
  <files>internal/e2e/e2e_test.go</files>
  <action>
Run the D-09 gate against the real stack — UNCONDITIONAL, no skip path:
  1. Time a clean rebuild of the document-worker image (`time docker compose build --no-cache document-worker`)
     and record the wall-clock; compare against the pre-phase baseline (build the pre-23-01 Dockerfile
     from `git stash`/parent commit once, or cite the last v1.4 CI docker-build duration) — the D-10
     build-time delta with raw numbers for the SUMMARY. Sanity-check the delta leaves the CI docker-build
     tier comfortably inside `timeout-minutes: 20` (ci.yml ~50).
  2. `docker compose -f docker-compose.yml -f docker-compose.e2e.yml up -d --build`, wait for
     `curl -fsS http://localhost:8090/healthz`, run migrations (`go run ./cmd/migrate` with the compose
     DATABASE_URL), then run the FULL document-relevant e2e set live:
     `go test ./internal/e2e/ -run 'TestPDFAExportE2E|TestPDFANonCompliantE2E|TestDocumentConversionE2E' -v -timeout 30m`
     with E2E_BASE_URL=http://localhost:8090, DATABASE_URL=postgres://octo:octo-pass@localhost:5434/octo_db,
     API_KEY_SALT=dev-only-change-me-in-real-deploys, E2E_S3_DIAL_ADDR=127.0.0.1:9100 (ci.yml env, verified
     against the compose ports). ALL THREE must pass: compliant PDF/A export -> done with veraPDF active
     (SC#2); non-compliant -> terminal fail with reason in job_events (SC#1); plain document conversions
     unaffected by the shim pass-through (regression).
  3. Record the PDFA subtest's wall-clock delta vs the pre-veraPDF baseline (Pitfall 8's CI-timing
     discipline: the e2e tier has a 25-min cap; note the per-PDF/A-job added latency observed live).
If ANY of the three tests fails, this plan FAILS — fix the wiring (or, if the compliant export
regresses under real veraPDF, STOP and escalate per Pitfall 9; do not loosen the check to pass the gate).
  </action>
  <verify>
    <automated>E2E_BASE_URL=http://localhost:8090 DATABASE_URL=postgres://octo:octo-pass@localhost:5434/octo_db API_KEY_SALT=dev-only-change-me-in-real-deploys E2E_S3_DIAL_ADDR=127.0.0.1:9100 go test ./internal/e2e/ -run 'TestPDFAExportE2E|TestPDFANonCompliantE2E|TestDocumentConversionE2E' -v -timeout 30m</automated>
  </verify>
  <done>All three live tests pass against the rebuilt compose stack: compliant PDF/A-2b -> done under real veraPDF; deliberately non-compliant -> terminal failed with "pdf/a non-compliant" reason in job_events; document table unaffected. Build-time delta and PDF/A job latency delta recorded with raw numbers (D-10, D-02 discipline). Stack torn down after (`docker compose ... down -v`).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docker-compose.e2e.yml shim mount -> document-worker exec path | Test-only fault injection must never leak into production compose |
| non-compliant PDF bytes -> real veraPDF JVM in the live container | Untrusted-shaped PDF parsed live inside the 1g/2cpu resource-limited worker |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-06 | Elevation of privilege | soffice shim shadow-mounted in the worker | mitigate | Confined to docker-compose.e2e.yml (the file's existing "MUST NEVER be used in production" discipline); production docker-compose.yml untouched; shim mounted read-only over /usr/local/bin/soffice, runs as nobody; no PATH override to leak |
| T-23-07 | Repudiation | non-compliant failure without diagnosable reason | mitigate | Task 2 asserts the veraPDF rule summary is present in job_events.detail (D-07) — the audit trail is a tested property, not an assumption |
| T-23-08 | Denial of Service | veraPDF latency regressing the CI e2e 25-min cap | mitigate | Task 3 records build-time and per-job latency deltas against the ci.yml bounds (D-10, Pitfall 8) |
| T-23-SC | Tampering | pinned verapdf/cli:1.30.2 (carried from Plan 01) | mitigate | No new image/package installs in this plan |
</threat_model>

<verification>
- Fixture triple-verified in the pinned image: non-compliant per veraPDF, /GTS_PDFA present, %PDF- magic present.
- TestPDFANonCompliantE2E: failed within 5 min (terminal, not retried), error_code=engine_error, "pdf/a non-compliant" in job_events.detail.
- TestPDFAExportE2E: done with real veraPDF in the path (no v1.3 regression).
- TestDocumentConversionE2E: shim pass-through transparent for all 6 pairs.
- docker-compose.e2e.yml uses a shadow mount over /usr/local/bin/soffice, NOT a duplicated PATH override.
- Build-time delta + PDF/A latency delta recorded with raw numbers.
</verification>

<success_criteria>
- ROADMAP SC#1: non-compliant PDF/A export fails terminally with veraPDF reason in job_events — proven live (PDFA-01, D-09).
- ROADMAP SC#2: genuinely compliant PDF/A-2b export still passes under the stricter check — proven live against the existing fixture (PDFA-01).
- ROADMAP SC#3 confirmation: bundled veraPDF + hardened exec + same-commit signatures all exercised live in the rebuilt image (PDFA-02).
- D-10: docker-build tier impact measured and inside the 20-min bound; e2e latency delta noted.
- Live gate ran UNCONDITIONALLY against the compose stack (D-09).
</success_criteria>

<output>
Create `.planning/phases/23-verapdf-validation/23-03-SUMMARY.md` when done. It MUST record: the exact
XMP corruption used for the fixture, the build-time delta raw numbers (D-10), the observed per-PDF/A-job
veraPDF latency in the live run, the shadow-mount mechanism used (no PATH duplication), and confirmation
that all three live tests passed.
</output>
</content>
