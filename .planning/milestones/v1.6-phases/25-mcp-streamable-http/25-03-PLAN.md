---
phase: 25-mcp-streamable-http
plan: 03
type: execute
wave: 3
depends_on: [25-01, 25-02]
files_modified:
  - internal/mcpserver/mcp_http_live_test.go
autonomous: true
requirements: [MCPH-01, MCPH-02]
must_haves:
  truths:
    - "A chart-deployed mcp-http pod on OrbStack k8s serves a real MCP-over-HTTP session: initialize + tools/list returns all 5 tools (MCPH-01, D-08)"
    - "convert_file over HTTP with a real minted caller key succeeds and returns a presigned-only result — no local_path (MCPH-02, D-04, D-08)"
    - "The presigned result URL resolves by direct host-dial from the OrbStack host (closes Phase 24's SC3 recheck, D-08)"
    - "A request without an ApiKey header is rejected 401 before any tool runs (D-03, D-08)"
    - "OrbStack discipline held: compose stopped, images built sequentially, no >120s hangs; teardown clean (D-09)"
  artifacts:
    - path: "internal/mcpserver/mcp_http_live_test.go"
      provides: "offline-skip-guarded live MCP-over-HTTP session driver"
      contains: "tools/list"
  key_links:
    - from: "host driver"
      to: "mcp-http pod :8070"
      via: "kubectl port-forward + streamable-HTTP client"
      pattern: "port-forward"
    - from: "convert_file result"
      to: "presigned S3 URL"
      via: "direct host-dial returns 200 (SC3 recheck)"
      pattern: "presigned"
---

<objective>
THE LIVE HARD GATE (D-08). Deploy the mcp-http pod via the Phase 24 chart on OrbStack k8s and prove, against the live cluster, that the streamable-HTTP endpoint works end-to-end with per-request caller-key auth and presigned-only results — then tear down. This gate also closes Phase 24's deferred SC3 recheck by directly host-dialing a presigned URL.

Reuse the Phase 24 install discipline verbatim (24-03 SUMMARY): compose stopped, sequential builds with fixed non-`latest` tags, `helm install` WITHOUT `--wait` (post-install createbucket hook chicken-egg), hang rule (kill any docker/kubectl command silent >120s and run the restart runbook rather than waiting it out).

Purpose: The live acceptance for MCPH-01 (5 tools over HTTP, real conversion, per-request key) and MCPH-02 (presigned-only remote result).
Output: A driven live session with a loud PASS/FAIL verdict, plus the offline-skip-guarded driver committed for reuse.
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-mcp-streamable-http/25-CONTEXT.md

# Install-flow discipline + SC3 recheck note + OrbStack hazards to reuse verbatim:
@.planning/phases/24-helm-chart-core/24-03-SUMMARY.md

# The endpoint under test + the stdio live-test offline-skip pattern to mirror:
@internal/mcpserver/mcpserver.go
@internal/mcpserver/mcp_live_test.go

<existing-behavior>
- 24-03 proved: `helm install` must run WITHOUT `--wait` (apps crash until the post-install createbucket hook creates the bucket; `--wait` blocks hooks → chicken-egg). Gate pod readiness separately via `kubectl wait --for=condition=Available`.
- 24-03 deviation #2: kubelet image GC deletes locally-built :dev tags under disk pressure; if an image vanishes, `docker builder prune -af` then rebuild sequentially.
- 24-03: mint client rows with the project's own `cmd/manage-clients` CLI over a postgres port-forward (the sanctioned mechanism — kubectl exec writes were permission-denied). DATABASE_URL to in-cluster postgres via port-forward.
- 24-03 SC3 (presigned-from-host): PASSED via port-forward + `curl --connect-to` preserving the signed Host header when OrbStack's host→cluster proxy was wedged; the direct-dial claim could not be re-verified then — this plan attempts it again (D-08 SC3 recheck).
- 24-03 SC4 (NetworkPolicy negative test): OrbStack ships no NetworkPolicy controller — do NOT rely on NetworkPolicy enforcement locally; it is not part of this gate.
- Plan 25-01 SUMMARY records the D-02 stateless verdict and the exact StreamableHTTPOptions the binary uses — read it so the driver connects with matching expectations.
</existing-behavior>
</context>

<tasks>

<task type="auto">
  <name>Task 1: OrbStack pre-flight + sequential build + helm install</name>
  <files>(no source files — live cluster operations)</files>
  <action>Pre-flight (Pitfall 7 discipline): confirm the compose stack is DOWN (`docker compose ps` empty / stopped) — never run compose and k8s hot together; confirm `kubectl get nodes` shows Ready; confirm the OrbStack kube-context. Sequentially (never in parallel) build any missing app images with fixed `:dev` tags, and build `octoconv-mcp-http:dev` from Dockerfile.mcp-http (25-02). Verify with `docker images` that all required images (api, 4 workers, mcp-http, plus the pinned postgres/redis/minio) are present; if kubelet GC evicted any (24-03 dev #2), `docker builder prune -af` and rebuild. Run `helm install octoconv deploy/chart/octoconv -f deploy/chart/octoconv/values-local.yaml` (plus any overlay carrying dev creds) WITHOUT `--wait`, with `mcpHttp.enabled=true`. Then `kubectl wait --for=condition=Available deployment/mcp-http --timeout=300s` and confirm all app Deployments (incl. api) reach Available. Apply the hang rule: any docker/kubectl/helm command silent >120s is a wedge — run the proven restart runbook (quit OrbStack → pkill helper → open -a OrbStack → poll `docker version`), do not wait it out.</action>
  <verify>
    <automated>kubectl get deployment mcp-http -o jsonpath='{.status.availableReplicas}' | grep -q '^1$' && echo MCPHTTP_READY</automated>
  </verify>
  <done>mcp-http pod (single replica) and all app Deployments Available on OrbStack k8s; images built sequentially with non-latest tags; no unresolved >120s hang.</done>
</task>

<task type="auto">
  <name>Task 2: Scripted MCP-over-HTTP session, SC3 recheck, 401 case, teardown, verdict</name>
  <files>internal/mcpserver/mcp_http_live_test.go</files>
  <action>Mint a real caller key: port-forward the in-cluster postgres and run `cmd/manage-clients` create against it (sanctioned mechanism, 24-03), capturing the raw key. Port-forward the mcp-http Service (`kubectl port-forward svc/mcp-http 8070:8070`). Write `internal/mcpserver/mcp_http_live_test.go` as an offline-skip-guarded live driver (mirror mcp_live_test.go's skip pattern: skip unless an env flag like `OCTOCONV_MCP_HTTP_LIVE=1` and the endpoint/key envs are set — so `go test ./...` offline stays green). The driver connects a streamable-HTTP MCP client (SDK client transport, matching the StreamableHTTPOptions recorded in 25-01 SUMMARY) to `http://127.0.0.1:8070` and asserts: (1) initialize succeeds; (2) tools/list returns exactly the 5 tools (convert_file, get_job_status, download_result, list_supported_formats, list_presets) — MCPH-01; (3) convert_file on a small fixture image with `Authorization: ApiKey <minted-key>` succeeds and the result carries a non-empty presigned_url and an EMPTY local_path — MCPH-02 / D-04 presigned-only; (4) a connection/request WITHOUT the ApiKey header is rejected 401 before any tool runs — D-03. Separately, directly host-dial the presigned URL returned in (3) with a plain HTTP GET from the OrbStack host and assert HTTP 200 + valid image bytes — the Phase 24 SC3 recheck (D-08); if OrbStack's host→cluster proxy is wedged again, fall back to the 24-03 `--connect-to` Host-preserving path and record which path was used. Capture the full transcript. Then teardown: `helm uninstall octoconv`; confirm pods gone. Write a loud PASS/FAIL verdict table into the SUMMARY (mirror 24-03's verdict format) — never silently downgrade a failure to a warning.</action>
  <verify>
    <automated>OCTOCONV_MCP_HTTP_LIVE=1 go test ./internal/mcpserver/ -run MCPHTTPLive -v (executor supplies endpoint+key envs from the port-forward+minted key; offline `go test ./internal/mcpserver/` must skip and stay green)</automated>
  </verify>
  <done>Live session verdict recorded: tools/list=5, convert_file presigned-only (no local_path), 401-without-key, presigned host-dial 200 (path noted). Teardown clean. Driver committed and offline-skips.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host driver → mcp-http (port-forward) | Caller key crosses; validates the per-request auth path live |
| host → presigned S3 URL | Direct-dial reachability of the returned URL (SC3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-08 | Spoofing | live 401 path | mitigate | Live-assert no-header request → 401 before tool exec (D-03, D-08) |
| T-25-09 | Information disclosure | minted test key | mitigate | Key minted for the gate via manage-clients; teardown removes the release; do not print the raw key into the committed SUMMARY |
| T-25-SC | Tampering | build/deploy supply chain | accept | Reuses images built from already-pinned bases; no new package installs; OrbStack shared image store, non-latest tags (D-09) |
</threat_model>

<verification>
- mcp-http Deployment Available on OrbStack k8s (single replica).
- Live MCP-over-HTTP: initialize OK, tools/list=5, convert_file presigned-only, 401-without-key.
- Presigned URL host-dial 200 (SC3 recheck) — record direct vs `--connect-to` fallback.
- Offline `go test ./internal/mcpserver/...` skips the live test and stays green (stdio + unit tests unaffected).
- Teardown clean; OrbStack discipline held (no unresolved wedge).
</verification>

<success_criteria>
- MCPH-01: live streamable-HTTP session against the in-cluster Service lists all 5 tools and runs a real conversion with a per-request caller key.
- MCPH-02: the convert_file result is remote-usable — presigned-only, local_path omitted (D-04).
- D-08 satisfied incl. the Phase 24 SC3 presigned-from-host recheck; D-09 teardown done.
</success_criteria>

<output>
Create `.planning/phases/25-mcp-streamable-http/25-03-SUMMARY.md` with a loud verdict table (mirror 24-03). Record: tools count, presigned-only assertion, 401 result, SC3 host-dial path (direct vs fallback) and status, and any OrbStack incident. Do not include the raw minted key.
</output>
