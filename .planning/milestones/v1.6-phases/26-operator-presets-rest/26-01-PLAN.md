---
phase: 26-operator-presets-rest
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/api/api.go
  - internal/api/system_presets_handlers.go
  - internal/api/system_presets_handlers_test.go
  - internal/api/routes.go
  - cmd/api/main.go
  - deploy/chart/octoconv/values.yaml
  - deploy/chart/octoconv/templates/configmap.yaml
  - .env.example
autonomous: true
requirements: [OPER-01]

must_haves:
  truths:
    - "An operator client (ID in OPERATOR_CLIENT_IDS) can create/list/show/update/deactivate system-scope presets via /v1/system/presets"
    - "A non-operator client gets a byte-identical no-leak 404 (never 403) on every /v1/system/presets route"
    - "Empty/unset OPERATOR_CLIENT_IDS = zero operators (fail-closed): even the resolved caller gets 404"
    - "A malformed OPERATOR_CLIENT_IDS aborts API startup loudly (fail-loud), never silently skipping the bad entry"
    - "Operator-ness is never surfaced in any response body"
  artifacts:
    - path: "internal/api/system_presets_handlers.go"
      provides: "ParseOperatorClientIDs, requireOperator middleware, 5 system-scope handlers"
      contains: "func (s \\*Server) requireOperator"
    - path: "internal/api/system_presets_handlers_test.go"
      provides: "operator vs non-operator vs unset-allowlist matrix + parser strictness tests"
    - path: "internal/api/routes.go"
      provides: "/v1/system/presets subtree gated by requireOperator"
      contains: "system/presets"
    - path: "internal/api/api.go"
      provides: "Config.OperatorClientIDs + Server.operators set"
      contains: "OperatorClientIDs"
    - path: "deploy/chart/octoconv/templates/configmap.yaml"
      provides: "OPERATOR_CLIENT_IDS env key"
      contains: "OPERATOR_CLIENT_IDS"
  key_links:
    - from: "cmd/api/main.go"
      to: "api.ParseOperatorClientIDs"
      via: "os.Getenv(\"OPERATOR_CLIENT_IDS\") parsed once at startup, fail-loud"
      pattern: "OPERATOR_CLIENT_IDS"
    - from: "internal/api/routes.go"
      to: "s.requireOperator"
      via: "chi subtree middleware after auth.Middleware"
      pattern: "requireOperator"
    - from: "internal/api/system_presets_handlers.go"
      to: "s.presetAdmin"
      via: "Create/Update/Deactivate/Get/List with presets.ScopeSystem + nil clientID"
      pattern: "presets.ScopeSystem"
---

<objective>
Add operator-only REST management of system-scope presets under a new
`/v1/system/presets` route group, gated by an `OPERATOR_CLIENT_IDS` env
allowlist parsed once at API startup. Non-operators receive the codebase's
uniform no-leak 404 (never 403). Zero migrations, zero schema change, no
second auth system — pure additive `internal/api` change reusing the already
scope-agnostic `PresetAdmin` interface unchanged.

Purpose: Satisfies OPER-01 and Phase 26 SC 1-3. Implements the resolved Key
Decision (D-01): env-allowlist + 404-no-leak over an `is_operator` column +
403 (the column stays the documented future option, K8SV2-03).

Output: operator gate wiring (D-01), five system-scope handlers + routing
(D-02/D-03/D-04), handler test matrix (D-06), and chart/env additions (D-05)
with a helm-template assert.
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-operator-presets-rest/26-CONTEXT.md

# Canonical implementation references (read_first — do NOT re-explore the tree)
@internal/api/presets_handlers.go
@internal/api/api.go
@internal/api/routes.go
@internal/presets/repo.go
@cmd/manage-presets/main.go
@cmd/api/main.go
@internal/api/presets_handlers_test.go

<interfaces>
<!-- Contracts the executor needs; extracted from the codebase. Use directly, no exploration. -->

The system-scope write paths ALREADY exist and are exercised today by
cmd/manage-presets (system scope = no --client-id). REST reuses EXACTLY these.
`PresetAdmin` (internal/api/api.go) is already scope-agnostic — do NOT widen or
change it:

```go
type PresetAdmin interface {
    Create(ctx context.Context, p presets.CreateParams) (uuid.UUID, int, error)
    Update(ctx context.Context, scope string, clientID *uuid.UUID, name, targetFormat string, options map[string]any, description string) (int, error)
    Deactivate(ctx context.Context, scope string, clientID *uuid.UUID, name string) error
    Get(ctx context.Context, scope string, clientID *uuid.UUID, name string) (*presets.Preset, error)
    List(ctx context.Context, scope string, clientID *uuid.UUID, includeInactive bool) ([]presets.Preset, error)
    // ListForClient / GetForClient are the MERGED-view reads used by /v1/presets — NOT used here.
}
```

System-scope call form (verified against internal/presets/repo.go: every
method's SQL handles `($N::uuid IS NULL AND client_id IS NULL)` for the system
row, and Create inserts scope='system', client_id NULL — DDL
presets_scope_owner_chk enforces the invariant):
- Create: `presets.CreateParams{Name, Scope: presets.ScopeSystem, ClientID: nil, TargetFormat, Options, Description}` → returns ErrAlreadyExists on active dup (map to 409)
- Update: `Update(ctx, presets.ScopeSystem, nil, name, targetFormat, options, description)` → ErrNotFound → 404
- Deactivate: `Deactivate(ctx, presets.ScopeSystem, nil, name)` → ErrNotFound → 404
- Get: `Get(ctx, presets.ScopeSystem, nil, name)` → ErrNotFound → 404 (returns full column set for the DTO)
- List: `List(ctx, presets.ScopeSystem, nil, includeInactive)`

Reusable from internal/api/presets_handlers.go (do NOT duplicate):
- `presetRequest` DTO — narrow, NO scope/client_id field (mass-assignment structurally impossible); reuse for system create/update bodies
- `presetResponse` + `newPresetResponse(*presets.Preset)` — reuse for all system responses
- `decodePresetRequest(w, r)` (size-capped JSON, 400 on bad body), `validPresetName(name)`, consts `noSuchPreset` = "preset not found", `invalidPresetName`
- `presets.ValidateOptsJSON(opts)` → non-nil err maps to 422
- `presets.ErrAlreadyExists`, `presets.ErrNotFound`, `presets.ScopeSystem`

Auth context (internal/auth/context.go, internal/clients/clients.go):
```go
func auth.ClientFromContext(ctx context.Context) (*clients.Client, bool) // set by auth.Middleware
type clients.Client struct { ID uuid.UUID; Name string }
```

Server/Config to extend (internal/api/api.go): NewServer signature already
carries resolver, health, and Config; extend Config with the operator set and
store it on Server (Config is the right home — it is already the tunables/data
bag, resolver/health stay the positional interfaces).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Operator env allowlist parse (fail-closed + fail-loud) and Server gate wiring</name>
  <files>internal/api/api.go, internal/api/system_presets_handlers.go, internal/api/system_presets_handlers_test.go, cmd/api/main.go</files>
  <behavior>
    ParseOperatorClientIDs(raw string) (map[uuid.UUID]struct{}, error):
    - "" or all-whitespace → empty non-nil map, nil error (fail-closed: zero operators, NOT an error)
    - "uuid1,uuid2" → set of both; trims surrounding whitespace per entry; ignores empty entries between commas (e.g. trailing comma)
    - any malformed UUID entry → returns a non-nil error naming the bad token (fail-loud); caller (main) log.Fatalf's — startup aborts, never silently skips
    requireOperator middleware:
    - resolved caller in OPERATOR_CLIENT_IDS → next handler runs
    - caller NOT in the set → writes the SAME body as noSuchPreset at 404 (byte-identical to a foreign-preset 404), short-circuits (D-02 no-leak); never 403
    - empty operator set → every caller (incl. the resolved test client) → 404
  </behavior>
  <action>Per D-01: add `OperatorClientIDs map[uuid.UUID]struct{}` to api.Config and an `operators map[uuid.UUID]struct{}` field to api.Server; in NewServer, copy cfg.OperatorClientIDs into the Server (nil → treat as empty set). Create internal/api/system_presets_handlers.go with: (a) exported `ParseOperatorClientIDs(raw string) (map[uuid.UUID]struct{}, error)` — pure function, splits on ',', strings.TrimSpace each token, skips empty tokens, uuid.Parse each, returns error naming the first bad token; (b) method `func (s *Server) requireOperator(next http.Handler) http.Handler` returning an http.Handler that reads auth.ClientFromContext, checks membership in s.operators, and on miss calls `writeError(w, http.StatusNotFound, noSuchPreset)` and returns without calling next. In cmd/api/main.go (env-only-in-main convention), read `os.Getenv("OPERATOR_CLIENT_IDS")`, pass through `api.ParseOperatorClientIDs`, `log.Fatalf("OPERATOR_CLIENT_IDS: %v", err)` on error, and set the result on `api.Config.OperatorClientIDs` in the existing NewServer call. Do NOT reformat unrelated lines.</action>
  <verify>
    <automated>go build ./... && go vet ./internal/api/... ./cmd/api/... && go test ./internal/api/ -run 'TestParseOperatorClientIDs|TestRequireOperator' -v</automated>
  </verify>
  <done>ParseOperatorClientIDs unit tests pass for empty/valid-CSV/whitespace/trailing-comma/bad-UUID cases; requireOperator returns byte-identical noSuchPreset 404 on non-operator and on empty set; build + vet clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Five system-scope handlers, routing, and operator-vs-non-operator-vs-unset test matrix</name>
  <files>internal/api/system_presets_handlers.go, internal/api/routes.go, internal/api/system_presets_handlers_test.go</files>
  <behavior>
    Handlers (all hardcode presets.ScopeSystem + nil clientID; reuse presetRequest/newPresetResponse/decodePresetRequest/validPresetName):
    - handleCreateSystemPreset: valid → 201 + DTO(version 1); presets.ErrAlreadyExists → 409; bad opts → 422; bad name → 400 invalidPresetName (D-04)
    - handleListSystemPresets: 200 + array of system presets (List(ScopeSystem,nil,?all=true))
    - handleShowSystemPreset: 200 + DTO; ErrNotFound → 404 noSuchPreset
    - handleUpdateSystemPreset: 200 + DTO(bumped version); ErrNotFound → 404
    - handleDeactivateSystemPreset: 200 + {name,is_active:false}; ErrNotFound → 404
    Matrix (D-06): with operator set = {resolver client ID} → each verb succeeds; with operator set = {some OTHER uuid} → each verb 404 byte-identical to a foreign-preset noSuchPreset 404; with EMPTY operator set → each verb 404 (fail-closed). No response body contains an operator flag/marker.
  </behavior>
  <action>Per D-02/D-03/D-04: add the five `handle*SystemPreset` methods to internal/api/system_presets_handlers.go, each following the exact error-mapping discipline of the existing user-scope handlers in presets_handlers.go but calling presetAdmin with `presets.ScopeSystem` and `nil` clientID (create uses CreateParams{Scope: presets.ScopeSystem, ClientID: nil}; show/list use Get/List not GetForClient/ListForClient — this subtree manages system rows directly, it is NOT the merged view). Reuse presetRequest, newPresetResponse, decodePresetRequest, validPresetName, and the noSuchPreset/invalidPresetName consts — do not redeclare them. In routes.go, inside the existing `/v1` group (so ByIP→auth.Middleware→PerClient already apply), add `r.Route("/system/presets", func(r chi.Router){ r.Use(s.requireOperator); POST "/", GET "/", GET/PUT/DELETE "/{name}" })`. Confirm no chi path conflict: `/v1/system/presets` is a distinct literal segment under /v1, not under the existing `/v1/presets/{name}` subtree. Add the test matrix to system_presets_handlers_test.go, reusing fakePresetAdmin/fakeResolver/authed from handlers_test.go; build the Server via NewServer with Config.OperatorClientIDs set AFTER creating the resolver (so the operator-success case can include resolver.client.ID). Per Claude's Discretion (D): operator-ness is NOT exposed in responses — assert response bodies contain no operator field. Add the D-discretion log line `reason=system_preset_write` on successful system writes (create/update/deactivate), consistent with existing log patterns (library code does not log — emit via the existing request logger path only if a handler already logs; otherwise omit rather than introduce logging into internal/api, matching the CLAUDE.md logging convention).</action>
  <verify>
    <automated>go test ./internal/api/ -run 'SystemPreset|Operator' -v && go vet ./internal/api/... && gofmt -l internal/api cmd/api</automated>
  </verify>
  <done>All five verbs pass for an operator; every verb returns a byte-identical noSuchPreset 404 for a non-operator AND for an empty allowlist; no response body leaks operator-ness; gofmt reports no files.</done>
</task>

<task type="auto">
  <name>Task 3: Chart values + ConfigMap + .env.example, with helm-template assert</name>
  <files>deploy/chart/octoconv/values.yaml, deploy/chart/octoconv/templates/configmap.yaml, .env.example</files>
  <action>Per D-05: in values.yaml add `operatorClientIds: ""` under the existing `api:` block (empty default, with a one-line comment: comma-separated operator client UUIDs; empty = no operators, fail-closed; changing requires an API restart). In templates/configmap.yaml add `OPERATOR_CLIENT_IDS: {{ .Values.api.operatorClientIds | default "" | quote }}` (the ConfigMap is the shared non-secret env surface carried by every binary per the DEBT-05 full-surface convention already documented in that file; api-only keys like RATE_LIMIT_* already live here, so this matches precedent and needs no api-Deployment edit). In .env.example add `OPERATOR_CLIENT_IDS=` with a trailing comment matching the file's style (comma-separated client UUIDs authorized for /v1/system/presets; empty/unset = no operators, fail-closed; parsed once at API startup, restart to change; D-01/OPER-01).</action>
  <verify>
    <automated>helm template octoconv deploy/chart/octoconv -f deploy/chart/octoconv/values-local.yaml | grep -q 'OPERATOR_CLIENT_IDS' && helm template octoconv deploy/chart/octoconv -f deploy/chart/octoconv/values-local.yaml --set api.operatorClientIds=11111111-2222-3333-4444-555555555555 | grep -q '11111111-2222-3333-4444-555555555555' && grep -q '^OPERATOR_CLIENT_IDS=' .env.example</automated>
  </verify>
  <done>helm template renders OPERATOR_CLIENT_IDS with the empty default AND surfaces an overridden value via --set api.operatorClientIds; .env.example carries the OPERATOR_CLIENT_IDS entry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (/v1) | Untrusted internal-service callers cross here; already authenticated by API key via auth.Middleware before any handler runs |
| operator subset | The operator allowlist is a second, narrower authorization gate INSIDE the authenticated boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Elevation of Privilege | /v1/system/presets writes by a non-operator | mitigate | requireOperator middleware runs after auth.Middleware; membership check against s.operators; empty set = deny-all (fail-closed) |
| T-26-02 | Information disclosure | Existence/shape of the operator surface | mitigate | Non-operator → writeError(404, noSuchPreset), byte-identical to the foreign-preset 404 (never 403); operator-ness never in any response body (D discretion) |
| T-26-03 | Tampering / DoS | Malformed OPERATOR_CLIENT_IDS at startup | mitigate | ParseOperatorClientIDs returns error on any bad UUID; main log.Fatalf aborts startup (fail-loud) — never silently skip a bad token (would shrink the intended operator set) |
| T-26-04 | Spoofing | Bypassing the operator gate | mitigate | No new auth path; reuses the existing API-key auth (auth.Middleware) and the resolved clients.Client.ID; requireOperator has no independent identity source |
| T-26-05 | Tampering (mass-assignment) | Body-supplied scope/client_id on system create | mitigate | Reuses presetRequest (no scope/client_id fields); scope hardcoded ScopeSystem, clientID hardcoded nil — smuggled fields ignored by json.Unmarshal |

No package-manager installs in this phase (Go stdlib + already-pinned deps only) — no supply-chain (T-*-SC) checkpoint required.
</threat_model>

<verification>
- `go build ./...` and `go vet ./...` clean
- `go test ./internal/api/...` passes (existing suite + new operator/system matrix)
- `gofmt -l internal/api cmd/api` reports no files
- `helm template ... -f values-local.yaml` renders OPERATOR_CLIENT_IDS (empty default and overridable)
</verification>

<success_criteria>
- OPER-01 / SC 1: an operator can create/list/show/update/deactivate system-scope presets via /v1/system/presets, reusing PresetAdmin unchanged (unit-proven).
- SC 2: a non-operator receives the uniform no-leak 404 (never 403) on every route — byte-identical to the foreign-preset 404.
- SC 3: the gate is env-only (OPERATOR_CLIENT_IDS parsed once at startup), zero migrations, no second auth system; empty/unset = fail-closed; bad UUID = fail-loud startup.
- D-05: chart values + ConfigMap + .env.example carry OPERATOR_CLIENT_IDS; helm-template assert green.
</success_criteria>

<output>
Create `.planning/phases/26-operator-presets-rest/26-01-SUMMARY.md` when done
</output>
