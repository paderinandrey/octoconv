---
phase: 29-v1-6-hardening-tail
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - deploy/chart/octoconv/templates/scaledobject-image.yaml
  - deploy/chart/octoconv/templates/scaledobject-document.yaml
  - deploy/chart/octoconv/templates/scaledobject-html.yaml
  - deploy/chart/octoconv/templates/prometheus.yaml
  - deploy/chart/octoconv/templates/_helpers.tpl
  - deploy/chart/octoconv/values.yaml
autonomous: true
requirements: [HARD-01, HARD-03]

must_haves:
  truths:
    - "D-01: an empty/absent PromQL result no longer reads as queue-empty — ignoreNullValues is false on all three ScaledObjects so sustained metric absence becomes a scaler error and fallback.replicas holds 1 per class"
    - "D-03: all three ScaledObject trigger queries count state=~\"pending|active|retry\" so a retry backlog scales a zeroed class back up before the reconciler sweep"
    - "D-02 (WR-05): the Prometheus pod-template carries a checksum/config annotation that hashes the SHARED octoconv.prometheusScrapeConfig named-template (not the whole self-referential prometheus.yaml file) so a helm upgrade that changes scrape-config rolls the pod — AND helm template/lint render clean without the infinite template recursion a whole-file self-hash would trigger"
    - "D-06 (explicit 0): an explicit scaleDownStabilizationSeconds of 0 renders stabilizationWindowSeconds: 0 (instant downscale) instead of being silently dropped to the k8s 300s default"
    - "D-06 (null default): the production default scaleDownStabilizationSeconds: null (values.yaml) renders ZERO stabilizationWindowSeconds — the guard pairs hasKey with a not-nil check so the unset/null default never emits a broken block (the WR-01-class silent bug this phase exists to close)"
    - "Chart still lints and renders under values-local and values-loadproof after all edits"
    - "D-08 (plan grouping): this plan is the SOLE owner of the ScaledObject/prometheus/values/_helpers.tpl chart edits — HARD-01 and the HARD-03 stabilization template fix are colocated here so no other wave-1 plan touches these templates (29-02 has zero chart-file overlap, preserving A‖B parallelism)"
  artifacts:
    - path: "deploy/chart/octoconv/templates/scaledobject-image.yaml"
      provides: "image-class scaler with ignoreNullValues:false + retry-inclusive PromQL"
      contains: "ignoreNullValues: \"false\""
    - path: "deploy/chart/octoconv/templates/scaledobject-document.yaml"
      provides: "document-class scaler + hasKey-AND-not-nil-guarded stabilization block"
      contains: "ignoreNullValues: \"false\""
    - path: "deploy/chart/octoconv/templates/scaledobject-html.yaml"
      provides: "html-class scaler with ignoreNullValues:false + retry-inclusive PromQL"
      contains: "ignoreNullValues: \"false\""
    - path: "deploy/chart/octoconv/templates/_helpers.tpl"
      provides: "octoconv.prometheusScrapeConfig named template shared by the Prometheus ConfigMap data and the pod-template checksum annotation (the recursion-safe checksum source)"
      contains: "define \"octoconv.prometheusScrapeConfig\""
    - path: "deploy/chart/octoconv/templates/prometheus.yaml"
      provides: "Prometheus Deployment whose pod-template checksum/config hashes the shared scrape-config named-template, and whose ConfigMap data includes that same named-template"
      contains: "checksum/config"
    - path: "deploy/chart/octoconv/values.yaml"
      provides: "cooldownPeriod invariant comment tying keda.*.cooldownPeriod to queue.go retry backoff"
      contains: "cooldownPeriod"
  key_links:
    - from: "deploy/chart/octoconv/templates/scaledobject-*.yaml"
      to: "KEDA prometheus scaler"
      via: "ignoreNullValues field"
      pattern: "ignoreNullValues: \"false\""
    - from: "deploy/chart/octoconv/templates/prometheus.yaml"
      to: "pod-template restart on config drift"
      via: "checksum/config annotation"
      pattern: "checksum/config:"
    - from: "deploy/chart/octoconv/templates/prometheus.yaml"
      to: "octoconv.prometheusScrapeConfig in _helpers.tpl"
      via: "include in BOTH the ConfigMap data block AND the pod-template checksum annotation (single source, no whole-file self-hash)"
      pattern: "octoconv.prometheusScrapeConfig"
---

<objective>
Close HARD-01 (WR-01/WR-05/WR-06 KEDA robustness) and the HARD-03 gate-tooling fix #1 (falsy-0 stabilization) — every change here touches the ScaledObject trio / Prometheus / _helpers.tpl / values, so this plan is the single owner of those templates. All work and verification is OFFLINE (helm lint / helm template); no cluster.

Purpose: The audio ScaledObject authored in Phase 33 must be copied from a fixed chart substrate. A known-bad `ignoreNullValues: "true"` pattern or a retry-blind PromQL query would otherwise propagate into the new audio class from its first commit (STATE.md blocker: "WR-01 must precede authoring scaledobject-audio.yaml").
Output: Three ScaledObject templates flipped to fail-safe null handling and retry-inclusive queries, a recursion-safe Prometheus config-drift checksum annotation backed by a shared named-template in _helpers.tpl, a hasKey-AND-not-nil-guarded stabilization block, and a values.yaml invariant comment — all rendering clean under values-local and values-loadproof.
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-v1-6-hardening-tail/29-CONTEXT.md
@.planning/phases/29-v1-6-hardening-tail/29-PATTERNS.md
@.planning/milestones/v1.6-phases/27-keda-autoscaling/27-REVIEW.md
@deploy/chart/octoconv/templates/scaledobject-image.yaml
@deploy/chart/octoconv/templates/scaledobject-document.yaml
@deploy/chart/octoconv/templates/scaledobject-html.yaml
@deploy/chart/octoconv/templates/prometheus.yaml
@deploy/chart/octoconv/templates/_helpers.tpl
@deploy/chart/octoconv/values.yaml

<interfaces>
<!-- Extracted from the live templates; use directly, no exploration needed. -->

ScaledObject trigger block shape (scaledobject-image.yaml:32-41, identical in document/html except queue name and the document-only advanced block):
  triggers:
    - type: prometheus
      metadata:
        serverAddress: http://prometheus.{{ .Values.global.namespace }}.svc.cluster.local:9090
        query: sum(octoconv_queue_depth{queue="image", state=~"pending|active"})
        threshold: {{ .Values.keda.image.threshold | quote }}
        # <3-line comment currently justifying "true" via "genuinely empty queue">
        ignoreNullValues: "true"

Co-dependency guard that must NOT break (scaledobject-image.yaml:13, all three):
  {{- if and .Values.keda.enabled .Values.prometheus.enabled }}

Document-only stabilization block with the falsy-0 bug (scaledobject-document.yaml:38-48):
  {{- if .Values.keda.document.scaleDownStabilizationSeconds }}
  advanced:
    horizontalPodAutoscalerConfig:
      behavior:
        scaleDown:
          stabilizationWindowSeconds: {{ .Values.keda.document.scaleDownStabilizationSeconds }}

Production default this guard must NOT render (values.yaml:157):
  scaleDownStabilizationSeconds: null   # a bare hasKey is TRUE for null → must be paired with `ne ... nil`

Prometheus pod-template metadata insertion point (prometheus.yaml:29-33):
  template:
    metadata:
      labels:
        {{- include "octoconv.labels" . | nindent 8 }}
        {{- include "octoconv.selectorLabels" (dict "component" "prometheus" "root" $) | nindent 8 }}

Prometheus ConfigMap data block whose CONTENT becomes the named-template (prometheus.yaml:65-74) — this is the block to extract into _helpers.tpl and `include` back, NOT to leave inline:
  data:
    prometheus.yml: |
      global:
        scrape_interval: 15s
      scrape_configs:
        - job_name: octoconv-api
          metrics_path: /metrics
          static_configs:
            - targets:
                - api.{{ .Values.global.namespace }}.svc.cluster.local:9090

Existing _helpers.tpl define style to mirror for the new named-template (_helpers.tpl:4-6, 49-55):
  {{- define "octoconv.name" -}}
  {{- .Chart.Name | trunc 63 | trimSuffix "-" }}
  {{- end }}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip ScaledObject trio to fail-safe null handling + retry-inclusive PromQL + hasKey-AND-not-nil-guarded stabilization</name>
  <files>deploy/chart/octoconv/templates/scaledobject-image.yaml, deploy/chart/octoconv/templates/scaledobject-document.yaml, deploy/chart/octoconv/templates/scaledobject-html.yaml</files>
  <read_first>
    - deploy/chart/octoconv/templates/scaledobject-image.yaml (whole file, 43 lines)
    - deploy/chart/octoconv/templates/scaledobject-document.yaml (whole file, 63 lines — has the document-only advanced/stabilization block)
    - deploy/chart/octoconv/templates/scaledobject-html.yaml (whole file, 41 lines)
    - deploy/chart/octoconv/values.yaml (line 157 — the `scaleDownStabilizationSeconds: null` production default the guard must NOT render)
    - .planning/phases/29-v1-6-hardening-tail/29-PATTERNS.md (section "scaledobject-{image,document,html}.yaml" — D-01/D-03/D-06#1 targets with exact line numbers)
    - .planning/milestones/v1.6-phases/27-keda-autoscaling/27-REVIEW.md (WR-01, WR-06 full text)
  </read_first>
  <action>
    Apply three edits to all relevant ScaledObject templates.
    (D-01) Change `ignoreNullValues: "true"` → `ignoreNullValues: "false"` on all three (image:41, document:61, html:39). REWRITE the 3-line `#` comment immediately above the field on each: remove the "genuinely empty queue → treat as 0" justification (no longer valid), replace with the new fail-safe intent — sustained metric absence (api unavailable/unscrapeable) is now a scaler error, so `fallback.replicas: 1` holds one replica per class rather than false scale-to-zero with a live backlog; note the benign fallback blip on fresh install / short api restart before first enqueue is accepted (fail-safe toward availability, same philosophy as webhook-worker fail-closed). Keep the `#`-prefixed 8-space-indented comment style.
    (D-03) Change the trigger query `state=~"pending|active"` → `state=~"pending|active|retry"` on all three (image:36, document:56, html:34) — retry tasks are imminent work; without them a class at 0 replicas never scales up on a retry backlog until the reconciler sweep (~12.6 min worst case). Update each file's HEADER doc comment (the `(D-04 — pending+active only, never retry/scheduled/archived)` phrasing near lines 1-6) to state the new retry-inclusive intent per D-03.
    (D-06 fix #1, scaledobject-document.yaml ONLY) Replace the truthy `{{- if .Values.keda.document.scaleDownStabilizationSeconds }}` guard around the advanced/stabilization block with a guard that requires BOTH conditions — the key is present AND its value is not nil: `{{- if and (hasKey .Values.keda.document "scaleDownStabilizationSeconds") (ne .Values.keda.document.scaleDownStabilizationSeconds nil) }}` (or the nested two-`{{- if }}` equivalent, each with its own `{{- end }}`). BOTH conditions are MANDATORY — `hasKey` alone is TRUE for the production default `scaleDownStabilizationSeconds: null` (values.yaml:157) and would emit a broken `stabilizationWindowSeconds: <no value>` block on every default render, which is exactly the WR-01-class silent bug this phase exists to close. The pair distinguishes three states cleanly: unset/null → block NOT rendered (k8s 300s default preserved); explicit `0` → `stabilizationWindowSeconds: 0` (instant downscale); explicit N → N. Keep the 2-space Helm indent and matching `{{- end }}`. The exact `and`-form vs nested-`if` form is your discretion per D-08.
    Do NOT touch the `{{- if and .Values.keda.enabled .Values.prometheus.enabled }}` co-dependency guard, the `fallback: {failureThreshold: 3, replicas: 1}` block, or the `octoconv.labels` include.
  </action>
  <verify>
    <automated>bash -c 'set -e; cd /Users/apaderin/dev/octoconv; F="deploy/chart/octoconv/templates/scaledobject-image.yaml deploy/chart/octoconv/templates/scaledobject-document.yaml deploy/chart/octoconv/templates/scaledobject-html.yaml"; NC=$(cat $F | grep -v "^[[:space:]]*#"); [ "$(printf "%s\n" "$NC" | grep -c "ignoreNullValues: \"false\"")" -eq 3 ] && [ "$(printf "%s\n" "$NC" | grep -c "ignoreNullValues: \"true\"")" -eq 0 ] && [ "$(printf "%s\n" "$NC" | grep -c "state=~\"pending|active|retry\"")" -eq 3 ] && [ "$(printf "%s\n" "$NC" | grep -c "pending|active\"}")" -eq 0 ] && grep -q "hasKey" deploy/chart/octoconv/templates/scaledobject-document.yaml && grep -q "ne .Values.keda.document.scaleDownStabilizationSeconds nil" deploy/chart/octoconv/templates/scaledobject-document.yaml'</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: across the three ScaledObject templates, non-comment lines contain exactly 3 occurrences of `ignoreNullValues: "false"` and 0 of `ignoreNullValues: "true"`.
    - Source assertion: exactly 3 non-comment occurrences of `state=~"pending|active|retry"` and 0 remaining `pending|active"}` (old query form) across the trio.
    - Source assertion: scaledobject-document.yaml guards the stabilization block with BOTH `hasKey` AND `ne .Values.keda.document.scaleDownStabilizationSeconds nil` (not `hasKey` alone).
    - Behavior assertion: each per-class comment above `ignoreNullValues` no longer justifies the value via "genuinely empty queue".
  </acceptance_criteria>
  <done>All three ScaledObjects use ignoreNullValues:false with rewritten fail-safe comments and retry-inclusive queries; document.yaml's stabilization guard distinguishes explicit 0 from unset/null via paired hasKey + not-nil checks.</done>
</task>

<task type="auto">
  <name>Task 2: Recursion-safe Prometheus checksum/config annotation (D-02 / WR-05) via a shared _helpers.tpl named-template + values.yaml cooldownPeriod invariant comment (WR-06)</name>
  <files>deploy/chart/octoconv/templates/_helpers.tpl, deploy/chart/octoconv/templates/prometheus.yaml, deploy/chart/octoconv/values.yaml</files>
  <read_first>
    - deploy/chart/octoconv/templates/_helpers.tpl (whole file — existing `define` style at 4-6 and 49-55 to mirror for the new named-template)
    - deploy/chart/octoconv/templates/prometheus.yaml (whole file — pod-template metadata ~29-33, and the ConfigMap `data.prometheus.yml` scrape-config block ~65-74; single-file Deployment+ConfigMap+Service convention)
    - deploy/chart/octoconv/values.yaml (keda.* block, especially cooldownPeriod values ~lines 148-162 and the comment-style examples at 138-144, 157)
    - internal/queue/queue.go (retry backoff schedules — the invariant the comment must reference)
    - .planning/phases/29-v1-6-hardening-tail/29-PATTERNS.md (sections "prometheus.yaml (D-02)" and "values.yaml (comment-only)")
    - .planning/milestones/v1.6-phases/27-keda-autoscaling/27-REVIEW.md (WR-05, WR-06 full text)
  </read_first>
  <action>
    (D-02 / WR-05 — recursion-safe checksum) The prometheus.yaml file holds the Deployment, the ConfigMap, AND the Service in ONE file. A `checksum/config` that hashes the whole file (`include (print $.Template.BasePath "/prometheus.yaml") .`) self-references the same file that contains the annotation → infinite template recursion → `helm template`/`helm lint` hard-fail (exit 1). DO NOT use the whole-file self-hash. Instead hash only the scrape-config CONTENT:
      1. In `_helpers.tpl`, add a new named-template `{{- define "octoconv.prometheusScrapeConfig" -}}...{{- end -}}` whose body is the exact scrape-config YAML currently inline at the ConfigMap's `data.prometheus.yml` block (prometheus.yaml:66-74: `global:` / `scrape_interval` / `scrape_configs:` / the `octoconv-api` job with the `api.{{ .Values.global.namespace }}.svc.cluster.local:9090` target). Preserve the `{{ .Values.global.namespace }}` templating inside the named-template. Mirror the existing define style in this file (define/end, no leading blank line inside the body).
      2. In prometheus.yaml, REPLACE the inline `data.prometheus.yml` body with `{{- include "octoconv.prometheusScrapeConfig" . | nindent 4 }}` (keep the `prometheus.yml: |` key and its indentation so the rendered ConfigMap is byte-equivalent to today).
      3. In prometheus.yaml, add an `annotations:` block to the pod-template metadata (prometheus.yaml:29-33, sibling of the existing `labels:`) containing `checksum/config: {{ include "octoconv.prometheusScrapeConfig" . | sha256sum }}`. Now BOTH the ConfigMap data and the pod-template annotation derive from the SAME named-template — a scrape-config change rolls the pod, and there is no self-referential recursion.
      Preserve the `{{- if .Values.prometheus.enabled }}` guard, the `app.kubernetes.io/component: prometheus` labeling discipline (deliberately NOT octoconv.io/tier), and the `---` doc separators.
    (WR-06 doc) Add an invariant comment next to the `keda.*.cooldownPeriod` values in values.yaml stating each class's cooldownPeriod must exceed that class's max retry backoff (reference internal/queue/queue.go schedules) or retry-state tasks strand at 0 replicas until the reconciler recovers them. Use this file's established style: multi-line `#`-prefixed block comment ABOVE the key for structural rationale (as at values.yaml:138-144), or inline trailing `#` for a single-value call-out (as at :157). Do not invent a new convention.
  </action>
  <verify>
    <automated>bash -c 'set -e; cd /Users/apaderin/dev/octoconv; grep -q "define \"octoconv.prometheusScrapeConfig\"" deploy/chart/octoconv/templates/_helpers.tpl && [ "$(grep -c "octoconv.prometheusScrapeConfig" deploy/chart/octoconv/templates/prometheus.yaml)" -eq 2 ] && [ "$(grep -c "Template.BasePath" deploy/chart/octoconv/templates/prometheus.yaml)" -eq 0 ]; R=$(helm template oc deploy/chart/octoconv -f deploy/chart/octoconv/values-local.yaml --set keda.enabled=true --set prometheus.enabled=true | grep -v "^[[:space:]]*#"); [ "$(printf "%s\n" "$R" | grep -c "checksum/config:")" -ge 1 ] && [ "$(printf "%s\n" "$R" | grep -c "job_name: octoconv-api")" -ge 1 ] && [ "$(grep -ci "cooldownPeriod" deploy/chart/octoconv/values.yaml)" -ge 1 ] && grep -Eiq "retry backoff|queue.go|max retry" deploy/chart/octoconv/values.yaml'</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `_helpers.tpl` defines `octoconv.prometheusScrapeConfig`; prometheus.yaml references it exactly twice (ConfigMap data include + pod-template checksum annotation) and contains ZERO `Template.BasePath` whole-file self-hash.
    - Behavior assertion: `helm template` with values-local (keda+prometheus enabled) succeeds (no recursion, exit 0), renders at least one `checksum/config:` annotation AND still renders the `job_name: octoconv-api` scrape target from the shared named-template (ConfigMap unchanged).
    - Source assertion: values.yaml contains a comment near cooldownPeriod referencing the retry backoff / queue.go invariant (matches one of `retry backoff|queue.go|max retry`).
    - Behavior assertion: the Prometheus `{{- if .Values.prometheus.enabled }}` guard and component labeling are unchanged.
  </acceptance_criteria>
  <done>Prometheus pod-template rolls on config drift via a checksum/config that hashes the shared octoconv.prometheusScrapeConfig named-template (no whole-file recursion); the ConfigMap data is that same named-template; values.yaml documents the cooldownPeriod > max-retry-backoff invariant.</done>
</task>

<task type="auto">
  <name>Task 3: Offline chart proof — helm lint + render under values-local (incl. null-default stabilization absence) and values-loadproof with enforcing assertions</name>
  <files>deploy/chart/octoconv (rendered and asserted; no file modifications in this task)</files>
  <read_first>
    - deploy/chart/octoconv/values-local.yaml (the standard render overlay — sets keda.enabled/prometheus.enabled, does NOT override scaleDownStabilizationSeconds, so the real null default from values.yaml applies)
    - deploy/chart/octoconv/values-loadproof.yaml (sets document scaleDownStabilizationSeconds — exercises the fix #1 path)
    - .planning/ROADMAP.md (Phase 29 Success Criteria SC1, SC3)
  </read_first>
  <action>
    Run the offline chart gate that proves SC1 (KEDA no-empty-as-empty) and the D-06 fix #1 falsy-0 behavior without a cluster. Confirm `helm lint` passes with values-local (this alone catches the whole-file-self-hash recursion — a recursive template fails lint). Render with values-local (keda+prometheus enabled) and assert: exactly 3 `ignoreNullValues: "false"` and 0 `"true"`; exactly 3 retry-inclusive queries and 0 old-form queries; at least one `checksum/config:`; and CRITICALLY, ZERO `stabilizationWindowSeconds` — because values-local applies the real `scaleDownStabilizationSeconds: null` production default, the paired hasKey/not-nil guard must leave the whole advanced block UNRENDERED (a bare hasKey would wrongly emit a broken block here; this assertion is what catches BLOCKER-2's WR-01-class regression). Then render with `--set keda.document.scaleDownStabilizationSeconds=0` and assert `stabilizationWindowSeconds: 0` appears exactly once (the falsy-0 proof — previously this silently dropped to the k8s 300s default). Also render under values-loadproof to confirm no template break. This task modifies no files; if any assertion fails, return to Task 1/Task 2.
  </action>
  <verify>
    <automated>bash -c 'set -e; cd /Users/apaderin/dev/octoconv; helm lint deploy/chart/octoconv -f deploy/chart/octoconv/values-local.yaml; R=$(helm template oc deploy/chart/octoconv -f deploy/chart/octoconv/values-local.yaml --set keda.enabled=true --set prometheus.enabled=true | grep -v "^[[:space:]]*#"); [ "$(printf "%s\n" "$R" | grep -c "ignoreNullValues: \"false\"")" -eq 3 ] && [ "$(printf "%s\n" "$R" | grep -c "ignoreNullValues: \"true\"")" -eq 0 ] && [ "$(printf "%s\n" "$R" | grep -c "state=~\"pending|active|retry\"")" -eq 3 ] && [ "$(printf "%s\n" "$R" | grep -c "pending|active\"}")" -eq 0 ] && [ "$(printf "%s\n" "$R" | grep -c "checksum/config:")" -ge 1 ] && [ "$(printf "%s\n" "$R" | grep -c "stabilizationWindowSeconds")" -eq 0 ]; Z=$(helm template oc deploy/chart/octoconv -f deploy/chart/octoconv/values-local.yaml --set keda.enabled=true --set prometheus.enabled=true --set keda.document.scaleDownStabilizationSeconds=0 | grep -v "^[[:space:]]*#"); [ "$(printf "%s\n" "$Z" | grep -c "stabilizationWindowSeconds: 0")" -eq 1 ]; helm template oc deploy/chart/octoconv -f deploy/chart/octoconv/values-loadproof.yaml >/dev/null'</automated>
  </verify>
  <acceptance_criteria>
    - CLI output: `helm lint` with values-local exits 0 (also proves no template recursion).
    - Behavior assertion: values-local render yields 3 `ignoreNullValues: "false"`, 0 `"true"`, 3 retry-inclusive queries, 0 old-form queries, ≥1 `checksum/config:`.
    - Behavior assertion (BLOCKER-2 null-default guard): the values-local render (real null default, NO stabilization override) produces ZERO `stabilizationWindowSeconds` — the advanced block stays unrendered.
    - Behavior assertion: rendering with `keda.document.scaleDownStabilizationSeconds=0` produces exactly one `stabilizationWindowSeconds: 0` (falsy-0 fixed).
    - Behavior assertion: values-loadproof render succeeds (exit 0).
  </acceptance_criteria>
  <done>The chart lints and renders clean under both overlays; the null default leaves stabilization unrendered, an explicit 0 renders instant downscale, and all SC1 assertions are green — the substrate is safe for the Phase 33 audio ScaledObject.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prometheus scrape → KEDA scaler → HPA | Metric availability drives autoscaling decisions; absence must not be silently interpreted as zero backlog |
| helm upgrade → running Prometheus pod | A ConfigMap change that never rolls the pod leaves a stale scraper feeding KEDA |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Denial of Service | scaledobject-*.yaml trigger | mitigate | `ignoreNullValues: "false"` (D-01) converts sustained metric absence to a scaler error so `fallback.replicas: 1` holds a busy class up instead of false scale-to-zero during an api outage |
| T-29-02 | Tampering | prometheus.yaml pod-template | mitigate | `checksum/config` annotation over the shared `octoconv.prometheusScrapeConfig` named-template (D-02 / WR-05) rolls the Prometheus pod on scrape-config drift, preventing a stale scrape that degrades into T-29-01's empty result — without the whole-file self-hash recursion |
| T-29-03 | Denial of Service | retry-state backlog | mitigate | PromQL `state=~"pending\|active\|retry"` (D-03) lets a zeroed class scale up on retry backlog before the ~12.6 min reconciler sweep |
| T-29-04 | Tampering | falsy-0 stabilization | mitigate | paired `hasKey` + `ne ... nil` guard (D-06 fix #1) preserves an operator's explicit `stabilizationWindowSeconds: 0` while keeping the production `null` default unrendered (no broken block) |
| T-29-SC | Tampering | package installs | accept | No npm/pip/cargo installs introduced; only Helm template/values edits, rendered and asserted offline via helm lint/template |
</threat_model>

<verification>
- `helm lint deploy/chart/octoconv -f deploy/chart/octoconv/values-local.yaml` exits 0 (also proves the checksum named-template introduces no recursion).
- Offline render (Task 3) proves SC1 (no empty-as-empty), retry-inclusive queries, checksum annotation, the null-default stabilization absence, and the falsy-0 fix under both values-local and values-loadproof.
- No cluster interaction in this plan — the WR-06 behavior change is re-verified live by the Wave-2 keda-gate.sh run in plan 29-03.
</verification>

<success_criteria>
- SC1: all three ScaledObjects use `ignoreNullValues: "false"` with rewritten fail-safe comments (HARD-01 / D-01).
- All three trigger queries include the `retry` state (HARD-01 / D-03) and their header comments reflect the retry-inclusive intent.
- Prometheus pod-template carries a `checksum/config` annotation backed by the shared `octoconv.prometheusScrapeConfig` named-template, with no whole-file self-hash recursion (D-02 / WR-05).
- Explicit `scaleDownStabilizationSeconds: 0` renders `stabilizationWindowSeconds: 0` AND the `null` default renders none (HARD-03 fix #1 / D-06).
- values.yaml documents the cooldownPeriod > max-retry-backoff invariant (WR-06).
</success_criteria>

<output>
Create `.planning/phases/29-v1-6-hardening-tail/29-01-SUMMARY.md` when done (commit with `git add -f`).
</output>
