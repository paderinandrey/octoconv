---
phase: 29-v1-6-hardening-tail
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - scripts/presets-rest-acceptance.sh
autonomous: true
requirements: [HARD-02]

must_haves:
  truths:
    - "D-05: docker-compose.yml api service passes OPERATOR_CLIENT_IDS through with a ${VAR:-} default so an operator UUID set in the shell env reaches cmd/api/main.go"
    - "D-04: scripts/presets-rest-acceptance.sh gains a system-scope section that mints an operator + a regular client, exports the operator UUID into OPERATOR_CLIENT_IDS, restarts the compose api to load it, and drives operator CRUD on /v1/system/presets"
    - "D-04: a non-operator gets a byte-identical no-leak 404 from the system-preset endpoints (no existence oracle vs a genuinely-nonexistent name)"
    - "D-04: a system preset created by the operator is usable in a job by any (non-operator) client"
    - "The extended acceptance script passes end-to-end against the live compose stack (exit 0)"
  artifacts:
    - path: "docker-compose.yml"
      provides: "OPERATOR_CLIENT_IDS passthrough on the api service"
      contains: "OPERATOR_CLIENT_IDS"
    - path: "scripts/presets-rest-acceptance.sh"
      provides: "operator system-scope acceptance section"
      contains: "OPERATOR_CLIENT_IDS"
  key_links:
    - from: "scripts/presets-rest-acceptance.sh"
      to: "docker-compose.yml api service env"
      via: "OPERATOR_CLIENT_IDS export + compose recreate"
      pattern: "OPERATOR_CLIENT_IDS"
    - from: "scripts/presets-rest-acceptance.sh"
      to: "/v1/system/presets"
      via: "operator http_json CRUD + non-operator 404 assertion"
      pattern: "/v1/system/presets"
---

<objective>
Close HARD-02 (OPER-01): make the operator system-presets path exercisable by a live acceptance script against the compose stack, and wire the `OPERATOR_CLIENT_IDS` compose passthrough that was missing (WR-03 from 26-REVIEW). Go code is NOT touched — only docker-compose.yml and the existing acceptance script.

Purpose: Phase 26 shipped the operator system-presets REST surface but the acceptance intent never materialized into a runnable script, and compose had no way to declare operators. This plan makes the operator authorization + no-leak-404 behavior provable on the cheaper compose stack (no k8s).
Output: A compose api service that reads `OPERATOR_CLIENT_IDS` from the shell env, and an extended `presets-rest-acceptance.sh` whose new system-scope section proves operator CRUD, byte-identical non-operator 404, and cross-client usability of a system preset.
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-v1-6-hardening-tail/29-CONTEXT.md
@.planning/phases/29-v1-6-hardening-tail/29-PATTERNS.md
@scripts/presets-rest-acceptance.sh
@docker-compose.yml
@internal/api/system_presets_handlers.go
@cmd/manage-presets/main.go

<interfaces>
<!-- Extracted from PATTERNS.md + the live files; use directly. -->

Existing harness primitives in presets-rest-acceptance.sh to REUSE (do not reinvent):
  - setup (lines 29-43): set -euo pipefail; cd repo root; API_BASE=http://localhost:8090; DATABASE_URL=postgres://octo:octo-pass@localhost:5434/octo_db; DB_CONTAINER=octoconv-db; WORKDIR mktemp + EXIT trap
  - assert_eq / assert_contains / assert_not_contains (lines 50-82): echo PASS/FAIL, exit 1 loud, increment PASS_COUNT
  - http_json helper (lines 93-105): parameterized method/path/out_file/key/body, captures %{http_code}
  - client-mint pattern (lines 135-153): go run ./cmd/manage-clients create <name>; parse `client id:` and `api key` via awk -F': '
  - system-preset seeding already used (line ~160): go run ./cmd/manage-presets create --name <n> --target webp  (no --client-id = system scope)
  - byte-identical no-leak 404 assertion pattern (lines 258-276): assert_eq of two saved 404 bodies

Operator authorization (internal/api/system_presets_handlers.go):
  - requireOperator + ParseOperatorClientIDs gate all 5 system-preset handlers
  - a non-operator always gets the SAME noSuchPreset body via writeError(w, http.StatusNotFound, noSuchPreset) — this is the byte-identical target for the no-leak assertion (system_presets_handlers.go:54-67)

docker-compose.yml api env block (lines 77-107): all literal values today; NO ${VAR:-} entry exists yet.
${VAR:-} idiom precedent (shell, not compose): scripts/keda-gate.sh:97 CALIBRATE="${CALIBRATE:-0}".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add OPERATOR_CLIENT_IDS passthrough to the compose api service (D-05 / WR-03)</name>
  <files>docker-compose.yml</files>
  <read_first>
    - docker-compose.yml (api service environment block ~lines 77-107; note the DEBT-05 inline-comment convention at ~92-94)
    - deploy/chart/octoconv/values.yaml (the operatorClientIds fail-closed comment at ~34-37 — mirror its "empty = no operators, fail-closed" wording)
    - .planning/phases/29-v1-6-hardening-tail/29-PATTERNS.md (section "docker-compose.yml api service (D-05)")
    - .planning/milestones/v1.6-phases/26-operator-presets-rest/26-REVIEW.md (WR-03 compose-passthrough gap)
  </read_first>
  <action>
    Insert `OPERATOR_CLIENT_IDS: "${OPERATOR_CLIENT_IDS:-}"` into the api service `environment:` map in docker-compose.yml, near the auth-adjacent `API_KEY_SALT` (~line 89). This is the file's FIRST shell-variable-passthrough entry — follow the `${VAR:-default}` bash parameter-expansion idiom (precedent scripts/keda-gate.sh:97). Add an inline `#` comment above the line explaining fail-closed-on-empty (empty = no operators), matching the Go-side wording at values.yaml:34-37 and this file's DEBT-05 comment-above-the-var convention. Do not alter any other env value.
  </action>
  <verify>
    <automated>bash -c 'set -e; cd /Users/apaderin/dev/octoconv; grep -q "OPERATOR_CLIENT_IDS: \"\${OPERATOR_CLIENT_IDS:-}\"" docker-compose.yml && [ "$(OPERATOR_CLIENT_IDS=probe-uuid docker compose config 2>/dev/null | grep -c "OPERATOR_CLIENT_IDS: probe-uuid")" -ge 1 ] && [ "$(docker compose config 2>/dev/null | grep -c "OPERATOR_CLIENT_IDS:")" -ge 1 ]'</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: docker-compose.yml contains the literal `OPERATOR_CLIENT_IDS: "${OPERATOR_CLIENT_IDS:-}"` line.
    - Behavior assertion: `OPERATOR_CLIENT_IDS=probe-uuid docker compose config` renders `OPERATOR_CLIENT_IDS: probe-uuid` (env interpolates through).
    - Behavior assertion: `docker compose config` with the var unset still renders a valid config (fail-closed empty, no error).
  </acceptance_criteria>
  <done>The compose api service reads OPERATOR_CLIENT_IDS from the shell env, defaulting to empty (no operators) when unset.</done>
</task>

<task type="auto">
  <name>Task 2: Extend presets-rest-acceptance.sh with the operator system-scope section (D-04) and run it live against compose</name>
  <files>scripts/presets-rest-acceptance.sh</files>
  <read_first>
    - scripts/presets-rest-acceptance.sh (whole file — reuse setup, assert_*, http_json, client-mint, no-leak-404 patterns per the interfaces block)
    - internal/api/system_presets_handlers.go (all 5 handlers + requireOperator/ParseOperatorClientIDs; the byte-identical noSuchPreset 404 at ~54-67)
    - cmd/manage-presets/main.go (system-preset create — no --client-id = system scope)
    - cmd/manage-clients/main.go (create output format the awk parse depends on)
    - .planning/phases/29-v1-6-hardening-tail/29-PATTERNS.md (section "presets-rest-acceptance.sh (D-04)")
  </read_first>
  <action>
    Add a new system-scope section to presets-rest-acceptance.sh reusing the existing harness verbatim (setup, assert_eq/assert_contains/assert_not_contains, http_json, client-mint, no-leak-404 patterns). The section must:
    1. Mint TWO clients via `go run ./cmd/manage-clients create ...` (an operator and a regular non-operator), parsing `client id:` / `api key` with the existing awk pattern; loud-fail if either id/key is empty.
    2. Export the operator's UUID into `OPERATOR_CLIENT_IDS` and recreate the compose api service so cmd/api/main.go re-reads the env — `docker compose up -d --force-recreate api` vs down/up is your discretion per D-05/CONTEXT; wait for the api to be healthy again (poll `GET /healthz` or the existing readiness pattern) before asserting.
    3. As the operator, exercise CRUD on `/v1/system/presets` and `/v1/system/presets/{name}` via http_json: create → list (contains the new name) → show → update → deactivate, asserting the expected 2xx codes.
    4. As the non-operator, hit the same system-preset endpoints and assert a 404 whose body is BYTE-IDENTICAL to a genuinely-nonexistent-name 404 (reuse the assert_eq-of-two-saved-bodies pattern at lines 258-276) — proving requireOperator leaks no existence oracle.
    5. Prove a system preset (seeded via `go run ./cmd/manage-presets create --name <n> --target <fmt>` with no --client-id) is usable in a job submitted by the regular (non-operator) client.
    Append the new assertions to the summary footer in the existing `echo "D-04/... : PASS"` style; preserve the "stack left running for inspection" no-teardown-on-success behavior (Phase 16/17/18 precedent). Do NOT modify Go code.
  </action>
  <verify>
    <automated>bash -c 'set -e; cd /Users/apaderin/dev/octoconv; bash -n scripts/presets-rest-acceptance.sh && grep -q "OPERATOR_CLIENT_IDS" scripts/presets-rest-acceptance.sh && grep -q "/v1/system/presets" scripts/presets-rest-acceptance.sh && grep -Eq "force-recreate|up -d api|down" scripts/presets-rest-acceptance.sh && [ "$(grep -c "manage-clients create" scripts/presets-rest-acceptance.sh)" -ge 2 ]'</automated>
  </verify>
  <acceptance_criteria>
    - Structural (offline): `bash -n` clean; script references `OPERATOR_CLIENT_IDS`, `/v1/system/presets`, a compose recreate command, and mints ≥2 clients.
    - LIVE GATE (run against an up compose stack, OrbStack discipline — ensure no k8s stack is hot first): `bash scripts/presets-rest-acceptance.sh` exits 0, with the new section printing PASS for operator CRUD, the byte-identical non-operator 404, and cross-client system-preset job usage.
    - Behavior assertion: the non-operator 404 body equals the nonexistent-name 404 body (assert_eq PASS, no leak).
  </acceptance_criteria>
  <done>The acceptance script proves the operator system-presets path end-to-end on compose: operator CRUD works, non-operator gets a no-leak 404, and a system preset is usable by any client.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internal client (API key) → api /v1/system/presets | Only allowlisted operator client IDs may CRUD system presets; all others must be indistinguishable from "not found" |
| shell env → compose api service | OPERATOR_CLIENT_IDS declares who is an operator; an empty/absent value must fail closed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-10 | Elevation of Privilege | /v1/system/presets CRUD | mitigate | requireOperator gate; the acceptance section asserts a non-operator cannot access/mutate system presets |
| T-29-11 | Information Disclosure | system-preset existence oracle | mitigate | Byte-identical no-leak 404 asserted (D-04) — non-operator response is identical to a genuinely-nonexistent name, exposing no existence signal |
| T-29-12 | Spoofing | OPERATOR_CLIENT_IDS misconfig | mitigate | `${OPERATOR_CLIENT_IDS:-}` fail-closed default (D-05) — unset means zero operators, never "everyone is operator" |
| T-29-SC | Tampering | package installs | accept | No npm/pip/cargo installs introduced; only a compose env line and a bash script section, exercised via `go run` of existing in-repo commands |
</threat_model>

<verification>
- Offline: `bash -n scripts/presets-rest-acceptance.sh` clean; `docker compose config` interpolates OPERATOR_CLIENT_IDS.
- Live gate (compose only, no k8s): `bash scripts/presets-rest-acceptance.sh` exits 0 with the new system-scope PASS lines.
- OrbStack discipline: ensure no k8s stack is running before bringing compose up; the script leaves the stack up on success for inspection.
</verification>

<success_criteria>
- SC2: an operator can run the acceptance script against compose exercising `/v1/system/presets` CRUD plus a byte-identical no-leak 404 for a non-operator, with `OPERATOR_CLIENT_IDS` passed through the compose api service (HARD-02 / D-04 / D-05).
- A system preset created by the operator is usable in a job by any client.
</success_criteria>

<output>
Create `.planning/phases/29-v1-6-hardening-tail/29-02-SUMMARY.md` when done (commit with `git add -f`).
</output>
