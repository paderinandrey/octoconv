---
phase: 31-queue-worker-routing-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/db/migrations/0006_audio_engine.sql
  - internal/convert/converters.go
  - internal/convert/whisper.go
  - internal/queue/queue.go
  - internal/queue/client.go
  - internal/queue/queue_test.go
autonomous: true
requirements: [AUD-05]

must_haves:
  truths:
    - "A jobs row with engine='audio' can be INSERTed (CHECK constraint accepts it)"
    - "convert.Default.EngineFor(<audio source>, <transcript target>) returns 'audio' and /v1/formats lists audio"
    - "AudioUniqueTTL is derived fresh from AUDIO_MAX_RETRY/AUDIO_ENGINE_TIMEOUT and strictly exceeds the worst-case audio attempt lifetime"
    - "A local dev run resolves the whisper model path from AUDIO_MODEL_PATH without container-only /models/ggml-base.bin"
  artifacts:
    - path: "internal/db/migrations/0006_audio_engine.sql"
      provides: "jobs.engine CHECK constraint extended to accept 'audio'"
      contains: "jobs_engine_check"
    - path: "internal/convert/converters.go"
      provides: "AudioConverter registered in Default registry"
      contains: "Default.Register(AudioConverter{})"
    - path: "internal/convert/whisper.go"
      provides: "SetAudioModelPath setter + 3-tier model() fallback"
      contains: "func SetAudioModelPath"
    - path: "internal/queue/queue.go"
      provides: "TypeAudioConvert, QueueAudio, NewAudioConvertTask, AudioUniqueTTL, RetryDelayFunc audio case"
      contains: "AudioUniqueTTL"
    - path: "internal/queue/client.go"
      provides: "audioMaxRetry/audioUniqueTTL fields + EnqueueAudioConvert"
      contains: "func (c *Client) EnqueueAudioConvert"
    - path: "internal/queue/queue_test.go"
      provides: "TestAudioUniqueTTL asserting strict-exceeds-worst-case (SC3)"
      contains: "func TestAudioUniqueTTL"
  key_links:
    - from: "internal/convert/converters.go"
      to: "convert.Default registry"
      via: "init() Default.Register(AudioConverter{})"
      pattern: "Default\\.Register\\(AudioConverter\\{\\}\\)"
    - from: "internal/queue/queue.go RetryDelayFunc"
      to: "AudioRetryDelay"
      via: "case TypeAudioConvert"
      pattern: "case TypeAudioConvert"
    - from: "internal/queue/client.go EnqueueAudioConvert"
      to: "NewAudioConvertTask"
      via: "audioMaxRetry/audioUniqueTTL"
      pattern: "NewAudioConvertTask"
---

<objective>
Lay the foundation substrate for the fourth (audio) engine class: the Postgres CHECK-constraint migration that unblocks every audio job INSERT, the one-line converter registration that makes `EngineFor`/`/v1/formats`/`Lookup` audio-aware automatically, the local-dev model-path setter, and the full queue/task-type/UniqueTTL/client layer (including the SC3 `TestAudioUniqueTTL` proof). Nothing downstream (worker, API, reconciler) compiles or runs until `queue.TypeAudioConvert` and `*queue.Client.EnqueueAudioConvert` exist — this plan produces them.

Purpose: Wave 1 must produce the substrate all Wave 2 plans link against. The migration is the single most likely "looks done in code review, 500s on first live test" gap (jobs.engine CHECK constraint does not yet accept 'audio').
Output: 0006 migration, registered converter, model-path setter, audio queue/TTL/client block, TestAudioUniqueTTL.
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-queue-worker-routing-integration/31-RESEARCH.md
@.planning/phases/31-queue-worker-routing-integration/31-PATTERNS.md

<interfaces>
<!-- Verified from codebase reads. Executor uses these directly. -->

internal/convert/convert.go:23  — const EngineAudio = "audio"
internal/convert/whisper.go:28  — const defaultAudioModelPath = "/models/ggml-base.bin"
internal/convert/whisper.go:47-52 — AudioConverter.model() (current 2-tier: c.modelPath || defaultAudioModelPath)
internal/convert/verapdf.go:10-36 — SetVeraPDFTimeout / verapdfTimeout / effectiveVeraPDFTimeout (setter shape to mirror; internal/convert NEVER calls os.Getenv directly)
internal/queue/queue.go:18-36 — TypeImageConvert/TypeDocumentConvert/TypeHTMLConvert consts + QueueImage/QueueDocument/QueueHTML consts
internal/queue/queue.go:296-299 — const uniqueTTLSafetyMargin = 2 * time.Minute (REUSE verbatim, never a per-class margin)
internal/queue/queue.go:281-294 — RetryDelayFunc dispatch switch (cases: TypeImageConvert/TypeWebhookDeliver/TypeDocumentConvert/TypeHTMLConvert, default asynq.DefaultRetryDelayFunc)
internal/queue/queue.go document block — NewDocumentConvertTask, documentRetrySchedule, DocumentRetryDelay, documentBackoffSum, DocumentUniqueTTL (formula: (maxRetry+1)*engineTimeout + backoffSum + uniqueTTLSafetyMargin)
internal/queue/client.go:34-45,69-82,112-123 — documentMaxRetry/documentUniqueTTL fields, NewClient construction block, EnqueueDocumentConvert method
internal/queue/queue_test.go:262-283 — TestDocumentUniqueTTL (exact template)
ConvertPayload / ParseConvertPayload — reused as-is for audio; NO new payload type
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0006 + register AudioConverter + AUDIO_MODEL_PATH setter</name>
  <files>internal/db/migrations/0006_audio_engine.sql, internal/convert/converters.go, internal/convert/whisper.go</files>
  <read_first>
    - internal/db/migrations/0005_html_engine.sql (exact template, 12 lines)
    - internal/db/migrations/0001_init.sql lines 40-55 (original jobs.engine inline CHECK)
    - internal/convert/converters.go (current 3-line init body)
    - internal/convert/verapdf.go lines 10-36 (SetVeraPDFTimeout setter shape to mirror)
    - internal/convert/whisper.go lines 20-52 (defaultAudioModelPath const + model() accessor)
  </read_first>
  <action>
    Create internal/db/migrations/0006_audio_engine.sql mirroring 0005_html_engine.sql's DROP CONSTRAINT / ADD CONSTRAINT shape verbatim: `ALTER TABLE jobs DROP CONSTRAINT jobs_engine_check;` then `ALTER TABLE jobs ADD CONSTRAINT jobs_engine_check CHECK (engine IN ('image', 'document', 'av', 'cad', 'archive', 'probe', 'html', 'audio'));`. Include a doc comment header noting AUD-05 and that jobs_engine_check is Postgres's auto-generated name for the unnamed inline CHECK in 0001_init.sql. The embedded-migration runner (db.Migrate, called at cmd/api/main.go on startup) picks it up by the 0006_ prefix glob — no separate deploy step.
    In internal/convert/converters.go, add exactly one line to init(): `Default.Register(AudioConverter{})` after the ChromiumConverter registration (zero-value modelPath — resolved via the setter below, NOT here; init() runs before main() parses env).
    In internal/convert/whisper.go, add a package-level `var audioModelPath string` plus `func SetAudioModelPath(path string)` following SetVeraPDFTimeout's single-write-before-any-concurrent-reader contract (doc comment must state: call once at process startup from cmd/audio-worker/main.go BEFORE srv.Start; no mutex). Change AudioConverter.model() from 2-tier to 3-tier: injected c.modelPath (unchanged, test-only) → audioModelPath (new middle tier) → defaultAudioModelPath (unchanged final fallback). This is a strict superset — no existing test-injection behavior changes.
  </action>
  <verify>
    <automated>gofmt -l internal/db internal/convert && go vet ./internal/convert/... && go build ./... && grep -c "jobs_engine_check" internal/db/migrations/0006_audio_engine.sql && grep -c "Default.Register(AudioConverter{})" internal/convert/converters.go && grep -c "func SetAudioModelPath" internal/convert/whisper.go</automated>
  </verify>
  <acceptance_criteria>
    - internal/db/migrations/0006_audio_engine.sql exists; its ADD CONSTRAINT list is exactly ('image', 'document', 'av', 'cad', 'archive', 'probe', 'html', 'audio')
    - `grep -c "Default.Register(AudioConverter{})" internal/convert/converters.go` == 1 (was 0 before this plan)
    - SetAudioModelPath is defined and internal/convert calls no os.Getenv (grep -c "os.Getenv" internal/convert/whisper.go == 0)
    - AudioConverter.model() consults audioModelPath between c.modelPath and defaultAudioModelPath
    - go build ./... succeeds; existing internal/convert tests still pass (go test ./internal/convert/)
  </acceptance_criteria>
  <done>Migration 0006 admits 'audio'; AudioConverter is registered; model path resolves from AUDIO_MODEL_PATH via SetAudioModelPath with defaultAudioModelPath fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Audio task-type/queue/retry/AudioUniqueTTL block in queue.go</name>
  <files>internal/queue/queue.go</files>
  <read_first>
    - internal/queue/queue.go lines 18-36 (task-type + queue-name const blocks)
    - internal/queue/queue.go document block: NewDocumentConvertTask, documentRetrySchedule, DocumentRetryDelay, documentBackoffSum, DocumentUniqueTTL (copy shape + doc-comment discipline)
    - internal/queue/queue.go lines 281-299 (RetryDelayFunc switch + uniqueTTLSafetyMargin const)
    - 31-PATTERNS.md "internal/queue/queue.go (additions)" section (naming: TypeAudioConvert/QueueAudio, NOT *Transcribe*)
  </read_first>
  <action>
    Add `TypeAudioConvert = "audio:convert"` to the task-type const block and `QueueAudio = convert.EngineAudio` to the queue-name const block — uniform `{class}:convert` shape (do NOT use *Transcribe* naming; the ARCHITECTURE.md TypeAudioTranscribe suggestion is explicitly superseded per RESEARCH.md Pattern 6, preserves DEBT-02 single-source-of-truth).
    Add `NewAudioConvertTask(jobID uuid.UUID, maxRetry int, uniqueTTL time.Duration) (*asynq.Task, error)` mirroring NewDocumentConvertTask exactly (reuse the existing ConvertPayload — no new payload type; asynq.Queue(QueueAudio), asynq.MaxRetry(maxRetry), asynq.Unique(uniqueTTL)).
    Add `var audioRetrySchedule = []time.Duration{...}` (no jitter, document/html shape — schedule is discretion: use 5s/15s/30s matching documentRetrySchedule as a defensible default; document the choice in a comment), `AudioRetryDelay(n int, e error, t *asynq.Task) time.Duration` (index-clamp shape), `audioBackoffSum(maxRetry int) time.Duration`.
    Add `AudioUniqueTTL(maxRetry int, engineTimeout time.Duration) time.Duration` using the formula `time.Duration(maxRetry+1)*engineTimeout + audioBackoffSum(maxRetry) + uniqueTTLSafetyMargin` — REUSE the shared uniqueTTLSafetyMargin const verbatim (no audio-specific margin). Copy DocumentUniqueTTL's doc-comment discipline. Per the AudioUniqueTTL binding decision (STATE.md): derive fresh from AUDIO_ENGINE_TIMEOUT/AUDIO_MAX_RETRY, never reuse image/document TTL.
    Add a `case TypeAudioConvert: return AudioRetryDelay(n, e, t)` to the RetryDelayFunc dispatch switch BEFORE default (a missed case silently falls through to asynq.DefaultRetryDelayFunc — not a compile error).
  </action>
  <verify>
    <automated>gofmt -l internal/queue && go vet ./internal/queue/... && go build ./... && grep -c "TypeAudioConvert\|QueueAudio\|func NewAudioConvertTask\|func AudioUniqueTTL\|case TypeAudioConvert" internal/queue/queue.go</automated>
  </verify>
  <acceptance_criteria>
    - TypeAudioConvert == "audio:convert" and QueueAudio == convert.EngineAudio ("audio")
    - AudioUniqueTTL formula = (maxRetry+1)*engineTimeout + audioBackoffSum(maxRetry) + uniqueTTLSafetyMargin, reusing the shared margin const (grep confirms uniqueTTLSafetyMargin referenced, no new margin const introduced)
    - RetryDelayFunc has a `case TypeAudioConvert` arm (not falling through to default)
    - No *Transcribe* identifier anywhere: `grep -c "Transcribe" internal/queue/queue.go` == 0
    - go build ./... succeeds
  </acceptance_criteria>
  <done>Audio task-type/queue-name/retry-schedule/AudioUniqueTTL all defined and wired into RetryDelayFunc, following the document block shape with fresh audio-derived TTL.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Client audio fields + EnqueueAudioConvert + TestAudioUniqueTTL</name>
  <files>internal/queue/client.go, internal/queue/queue_test.go</files>
  <behavior>
    - TestAudioUniqueTTL: worked-example assertion equals (maxRetry+1)*engineTimeout + audioBackoffSum + uniqueTTLSafetyMargin
    - Monotonic in maxRetry: AudioUniqueTTL(maxRetry+1, t) > AudioUniqueTTL(maxRetry, t)
    - Monotonic in engineTimeout: AudioUniqueTTL(m, t+1s) > AudioUniqueTTL(m, t)
    - SC3-specific: AudioUniqueTTL strictly EXCEEDS (maxRetry+1)*engineTimeout + audioBackoffSum(maxRetry) (the zero-margin worst-case retry lifetime) — proving the safety-margin term is load-bearing, not accidentally zero
  </behavior>
  <read_first>
    - internal/queue/client.go lines 34-45 (documentMaxRetry/documentUniqueTTL field docs)
    - internal/queue/client.go lines 69-82 (NewClient construction block: envInt/envDuration reads + struct-literal fields)
    - internal/queue/client.go lines 112-123 (EnqueueDocumentConvert method)
    - internal/queue/queue_test.go lines 262-283 (TestDocumentUniqueTTL — exact template)
  </read_first>
  <action>
    In internal/queue/client.go add `audioMaxRetry int` and `audioUniqueTTL time.Duration` fields on Client (mirror documentMaxRetry/documentUniqueTTL doc-comment shape). In NewClient add `audioMaxRetry := envInt("AUDIO_MAX_RETRY", 3)` and `audioEngineTimeout := envDuration("AUDIO_ENGINE_TIMEOUT", 600*time.Second)` (600s is an [ASSUMED] placeholder — Phase 32 re-derives from RTF measurement; comment it as such), and populate `audioMaxRetry: audioMaxRetry, audioUniqueTTL: AudioUniqueTTL(audioMaxRetry, audioEngineTimeout)` in the returned struct literal. Add `func (c *Client) EnqueueAudioConvert(ctx context.Context, jobID uuid.UUID) error` copying EnqueueDocumentConvert exactly (NewAudioConvertTask with c.audioMaxRetry/c.audioUniqueTTL; error wrap "enqueue audio convert %s: %w").
    In internal/queue/queue_test.go add TestAudioUniqueTTL mirroring TestDocumentUniqueTTL, and add the SC3 strict-exceeds-zero-margin-lifetime assertion described in the behavior block.
  </action>
  <verify>
    <automated>gofmt -l internal/queue && go vet ./internal/queue/... && go test ./internal/queue/ -run TestAudioUniqueTTL -v && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - *queue.Client has method EnqueueAudioConvert (grep -c "func (c \*Client) EnqueueAudioConvert" internal/queue/client.go == 1)
    - TestAudioUniqueTTL passes and includes an explicit assertion that AudioUniqueTTL > (maxRetry+1)*engineTimeout + audioBackoffSum(maxRetry)
    - AUDIO_MAX_RETRY and AUDIO_ENGINE_TIMEOUT are the env vars read (grep confirms in client.go NewClient)
    - go test ./internal/queue/ passes fully (not just the new test)
  </acceptance_criteria>
  <done>*queue.Client can enqueue audio tasks with a fresh audio-derived UniqueTTL; TestAudioUniqueTTL proves SC3's strict-exceeds-worst-case property.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client audio job → jobs table | engine='audio' value crosses into the persisted CHECK-constrained column |
| Go module graph | no new external packages cross into go.mod/go.sum this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-05 | Denial of Service / correctness (availability) | jobs.engine CHECK constraint | mitigate | 0006_audio_engine.sql extends the allow-list to include 'audio'; without it every audio INSERT 500s with a raw jobs_engine_check violation (RESEARCH.md Pitfall 1). Verified by the migration file's constraint list + downstream live E2E (Plan 04). |
| T-31-06 | Tampering (path traversal, future) | whisper-cli -m model path via AUDIO_MODEL_PATH | accept | AUDIO_MODEL_PATH is an operator-set process env var (server-controlled, never client bytes), consumed once at startup via SetAudioModelPath. Same trust class as defaultAudioModelPath (T-30-10 closed). No client input reaches model(). |
| T-31-SC | Tampering (supply chain) | go module installs | accept | No new external packages this phase (RESEARCH.md Package Legitimacy Audit: N/A — verified no new go.mod/go.sum entries needed). All libraries (chi/asynq/pgx/minio-go) already present and used identically by three prior engine classes. |
</threat_model>

<verification>
- `go build ./...` green (all packages compile against the widened *queue.Client)
- `go test ./internal/queue/ ./internal/convert/` pass
- `gofmt -l internal/db internal/convert internal/queue` prints nothing; `go vet ./...` clean
- Migration 0006 present and numbered correctly for the embed glob
</verification>

<success_criteria>
- jobs.engine CHECK accepts 'audio' (migration 0006)
- AudioConverter registered → EngineFor/Classes()/Lookup audio-aware automatically
- queue.TypeAudioConvert, QueueAudio, NewAudioConvertTask, AudioUniqueTTL, *queue.Client.EnqueueAudioConvert all exist
- TestAudioUniqueTTL proves SC3 (fresh derivation, strictly exceeds worst-case attempt lifetime)
- Local dev model-path resolution via AUDIO_MODEL_PATH (SetAudioModelPath)
</success_criteria>

<output>
Create `.planning/phases/31-queue-worker-routing-integration/31-01-SUMMARY.md` when done
</output>
