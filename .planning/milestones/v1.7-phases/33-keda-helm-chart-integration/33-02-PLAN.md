---
phase: 33-keda-helm-chart-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/keda-audio-loadproof.sh
autonomous: true
requirements: [AUD-08]

must_haves:
  truths:
    - "A new scripts/keda-audio-loadproof.sh exists that is self-contained: installs KEDA, layers values-local, tears down via EXIT trap, refuses to run if compose is up"
    - "The script triggers the audio class with the committed jfk.wav fixture (11s) and captures a Phase-28-style timestamped kubectl-describe-pod event timeline (Scheduled→Pulling→Pulled→Created→Started)"
    - "keda-load-proof.sh and keda-gate.sh remain byte-unchanged (audio logic lives only in the new file)"
    - "The teardown trap kills any watcher via process-group + pkill belt-and-suspenders (Phase-29 WR-01 pattern, no orphaned kubectl -w)"
  artifacts:
    - path: "scripts/keda-audio-loadproof.sh"
      provides: "audio scale-from-zero live-proof gate (structural clone of keda-load-proof.sh, no CALIBRATE mode)"
      contains: "keda-audio-loadproof"
  key_links:
    - from: "scripts/keda-audio-loadproof.sh"
      to: "internal/e2e/testdata/jfk.wav"
      via: "trigger fixture for the audio scale-from-zero event"
      pattern: "jfk.wav"
    - from: "scripts/keda-audio-loadproof.sh"
      to: "deploy/chart/octoconv (helm install)"
      via: "helm install with values-local + EXIT-trap teardown"
      pattern: "helm (install|upgrade)"
---

<objective>
Write scripts/keda-audio-loadproof.sh: a self-contained, single-purpose live-cluster gate that proves audio scale-from-zero and separates image-pull time from orchestration time via a timestamped pod-event timeline. It is a structural clone of scripts/keda-load-proof.sh (byte-unchanged) with a NARROWER scope — no CALIBRATE mode (AUDIO_ENGINE_TIMEOUT=742s is already RTF-measured, nothing to calibrate) — and a NEW scope: kubectl-describe-pod event-timeline separation for the 650MB audio image, using the committed jfk.wav (11s) as the trigger fixture.

Purpose: Build the instrument Plan 03 runs live. Authoring and static-verifying the script here (parallel with the chart work) de-risks the expensive live run.
Output: One new executable script, verified by `bash -n`, the byte-unchanged constraint on the two frozen scripts, and grep gates for the teardown/process-group/fixture patterns.
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-keda-helm-chart-integration/33-RESEARCH.md
@.planning/phases/33-keda-helm-chart-integration/33-PATTERNS.md

<decisions>
Open Question 2 (cold image-pull on OrbStack) — RESOLVED for the script's measurement design:
- Measure what is honestly measurable locally: audio scale-from-zero (0→1) with the image already in OrbStack's shared Docker store (imagePullPolicy IfNotPresent, a pre-built :dev/:local tag). This is the production-relevant local answer — orchestration time from Scheduled to Started.
- Capture the FULL `kubectl describe pod` event timeline with real timestamps (Scheduled → Pulling → Pulled → Created → Started). If Pulling→Pulled is near-zero, that IS the measured evidence answering SC3 for THIS environment: bake-in imposes ~0 extra pull cost on OrbStack's shared store.
- Do NOT engineer an artificial cold-pull (no fresh-tag + imagePullPolicy Always against a non-existent registry — it would ImagePullBackOff, not measure). `docker rmi` of the local tag before install only yields ImagePullBackOff (there is no registry to pull from), not a measurement — so it is NOT done. Record the caveat in the script's header and evidence output: "image-pull ≈ 0 on OrbStack shared store; a genuine registry-backed 650MB cold pull is unmeasurable locally and is deferred to a real-registry environment." Bake-vs-volume stays baked and reversible, per Key Decision 3.

Open Question 1 (keda-gate.sh audio smoke) — RESOLVED: do NOT touch keda-gate.sh (byte-unchanged discipline, same as keda-load-proof.sh). This new dedicated keda-audio-loadproof.sh provides the audio-class live proof. Trade-off recorded explicitly: keda-gate.sh's fast 3-class regression smoke will not cover audio 0→1; accepted because the flagship keda-audio-loadproof.sh covers audio cold-start in depth, mirroring how keda-gate.sh and keda-load-proof.sh already split fast-smoke vs flagship-proof roles for the other three classes.

Pitfall 4: NO CALIBRATE mode — AUDIO_ENGINE_TIMEOUT is a hard committed constant; there is nothing to calibrate.
Pitfall 7: any fresh-install path that expects audio-worker to settle to genuine 0 must seed `SADD asynq:queues ... audio ...` (or accept waiting for the first real audio enqueue to register the queue).
</decisions>

<interfaces>
Reuse (do not re-invent) these keda-load-proof.sh helper shapes — clone their structure, rename the fixture path to internal/e2e/testdata/jfk.wav:
- Header self-containment convention (lines 1-46): shebang, `set -euo pipefail`, `cd "$(dirname "$0")/.."`, self-contained KEDA-install + values-local layering + refuse-if-compose-up.
- teardown() EXIT trap (lines 153-208): SAMPLER_PID/SNAPSHOT_PID kill, process-group kill `kill -- -"$SNAPSHOT_PID"`, belt-and-suspenders `pkill -f "kubectl get pod ${BUSY_POD} .* -w"`, `helm uninstall octoconv` + `helm uninstall keda`. Copy the process-group-kill discipline verbatim (Phase-29 WR-01/WR-02/WR-03, commits db14b42/5440263) — any `kubectl get pod ... -w` watcher this script launches MUST use `set -m` + process-group-kill + pkill.
- postJob() (lines 332-360): curl POST /v1/jobs with ApiKey auth, expect 202, extract job_id.
- waitForReplicasAtLeast()/waitForReplicasAtMost() (lines 361-399): bounded kubectl jsonpath poll loops.
Un-read sections of keda-load-proof.sh (sampleLoop 497+, evidence-render block 634-773, snapshotLoop 751+, summary 999+) must be read at implementation time — do NOT read the full 1010-line file up front; target the sections named above plus whatever the event-timeline capture needs.
Evidence render helper (optional PNG): scripts/fixtures/render_evidence.py (pin the interpreter, per Phase-29 gate-tooling fix).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author scripts/keda-audio-loadproof.sh (structural clone, narrower scope, event-timeline capture)</name>
  <files>scripts/keda-audio-loadproof.sh</files>
  <read_first>
    - scripts/keda-load-proof.sh — header (1-60), teardown() (150-208), postJob/waitForReplicasAtLeast/waitForReplicasAtMost (330-399); read the sampleLoop/snapshot/summary sections only as needed for the event-timeline capture (do NOT read the full 1010-line file up front)
    - scripts/audio-rtf-measure.sh header (1-40) — the codebase's canonical "Structural clone of <script>" self-documentation convention to model this script's header on
    - .planning/phases/33-keda-helm-chart-integration/33-PATTERNS.md "scripts/keda-audio-loadproof.sh" section (verbatim teardown + helper excerpts)
    - .planning/milestones/v1.6-phases/28-autoscale-load-proof/evidence/ (the 4-artifact naming convention to emit)
  </read_first>
  <action>
    Create scripts/keda-audio-loadproof.sh, executable, `set -euo pipefail`, `cd "$(dirname "$0")/.."`. Model the header on audio-rtf-measure.sh's "Structural clone of scripts/keda-load-proof.sh" self-documentation, listing: (a) narrower scope — NO CALIBRATE mode (Pitfall 4); (b) new scope — image-pull-vs-cold-start separation via kubectl-describe-pod event-timeline for the ~650MB audio image; (c) trigger fixture internal/e2e/testdata/jfk.wav (11s, already committed); (d) the Open-Question-2 caveat verbatim ("image-pull ≈ 0 on OrbStack shared store; genuine registry-backed cold pull deferred to a real-registry environment"); (e) the byte-unchanged note that keda-load-proof.sh and keda-gate.sh are NOT modified.

    Structure: refuse to run if the docker-compose stack is up (clone keda-load-proof.sh's compose-up guard). Self-contained KEDA install + `helm install octoconv deploy/chart/octoconv -f values-local.yaml` (KEDA + prometheus enabled). Seed `SADD asynq:queues image document html audio webhook` before expecting scale-to-zero (Pitfall 7 — include audio in the seed set). Trigger flow: confirm audio-worker at 0 replicas, postJob jfk.wav to target txt, then capture the winning pod's `kubectl describe pod` event timeline with real timestamps and extract the Scheduled/Pulling/Pulled/Created/Started deltas into an sc3-audio-scale-from-zero-<UTC-ts>.txt artifact; use waitForReplicasAtLeast to confirm 0→1. If a `kubectl get pod ... -w` watcher is used, launch it under `set -m` and kill it by process-group + pkill in teardown (WR-01 discipline). Emit evidence into .planning/phases/33-keda-helm-chart-integration/evidence/ using the Phase-28 naming convention (gate-transcript-<ts>.log, sc3-audio-scale-from-zero-<ts>.txt, and a burst CSV/PNG if a sampler is included). teardown() EXIT trap ALWAYS runs: kill sampler/snapshot process-groups, `helm uninstall octoconv` + `helm uninstall keda`, leaving OrbStack cold. Do NOT add a CALIBRATE branch. Do NOT modify keda-load-proof.sh or keda-gate.sh.
  </action>
  <verify>
    <automated>cd /Users/apaderin/dev/octoconv && bash -n scripts/keda-audio-loadproof.sh && test -x scripts/keda-audio-loadproof.sh && grep -q 'jfk.wav' scripts/keda-audio-loadproof.sh && grep -q 'trap .*teardown.* EXIT' scripts/keda-audio-loadproof.sh && grep -Eq 'kill -- -|pkill -f' scripts/keda-audio-loadproof.sh && grep -q 'SADD asynq:queues' scripts/keda-audio-loadproof.sh && grep -q 'audio' <(grep 'SADD asynq:queues' scripts/keda-audio-loadproof.sh) && ! grep -qi 'CALIBRATE' scripts/keda-audio-loadproof.sh</automated>
  </verify>
  <done>scripts/keda-audio-loadproof.sh exists, is executable, passes `bash -n`, references jfk.wav, has an EXIT teardown with process-group/pkill watcher-kill, seeds asynq:queues including audio, and contains NO CALIBRATE mode.</done>
</task>

<task type="auto">
  <name>Task 2: Assert the two frozen scripts remain byte-unchanged</name>
  <files>scripts/keda-audio-loadproof.sh</files>
  <read_first>
    - git log for scripts/keda-load-proof.sh and scripts/keda-gate.sh (confirm last-touched commit predates this phase)
    - .planning/phases/33-keda-helm-chart-integration/33-PATTERNS.md "Byte-unchanged constraint (critical)" note
  </read_first>
  <action>
    Verification-only task (no new production file beyond Task 1). Confirm that this phase's work has NOT modified scripts/keda-load-proof.sh or scripts/keda-gate.sh — the audio proof logic lives exclusively in the new keda-audio-loadproof.sh. This byte-unchanged discipline is load-bearing: Plan 03 re-runs the UNMODIFIED keda-load-proof.sh to close Phase 29's deferred human-verification item, so any edit here would invalidate that "closes Phase 29's gap" claim. If `git status` shows either frozen script as modified, revert it.
  </action>
  <verify>
    <automated>cd /Users/apaderin/dev/octoconv && git diff --quiet HEAD -- scripts/keda-load-proof.sh scripts/keda-gate.sh && echo BYTE-UNCHANGED-OK</automated>
  </verify>
  <done>git diff shows zero changes to scripts/keda-load-proof.sh and scripts/keda-gate.sh; the only new/changed script from this plan is scripts/keda-audio-loadproof.sh.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| load-proof script → live OrbStack cluster/daemon | The script starts/installs/uninstalls cluster resources; a failed teardown leaves OrbStack hot (four daemon-wedge incidents on record) |
| watcher subprocess → parent script | A reparented `kubectl -w` pipeline can survive the EXIT trap and orphan |
| script edits → frozen gate scripts | Editing keda-load-proof.sh/keda-gate.sh would break the Phase-29 deferred re-run's validity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-05 | Denial of Service | teardown()/orphaned watcher | mitigate | EXIT trap always runs; watcher launched under `set -m`, killed by process-group + `pkill -f` belt-and-suspenders (Phase-29 WR-01/WR-02/WR-03 pattern); grep gate asserts the kill discipline exists |
| T-33-06 | Denial of Service | fresh-install scale-to-zero | mitigate | Seed `SADD asynq:queues ... audio ...` (Pitfall 7) so the WR-01 absent-metric fallback resolves to genuine 0 instead of a stuck fallback.replicas 1 |
| T-33-07 | Tampering | frozen gate scripts | mitigate | `git diff --quiet` gate on keda-load-proof.sh/keda-gate.sh guarantees they stay byte-unchanged; audio logic isolated to the new file |
| T-33-SC | Tampering | supply chain (installs) | accept | No package installs — shell authoring only; the script installs KEDA via the same pinned kedacore/keda helm repo the frozen scripts already use (re-verified live at run time) |
</threat_model>

<verification>
- `bash -n scripts/keda-audio-loadproof.sh` passes; script is executable.
- Header self-documents as a structural clone of keda-load-proof.sh with the no-CALIBRATE + event-timeline + Open-Question-2 caveat notes.
- EXIT teardown with process-group + pkill watcher-kill; asynq:queues seed includes audio; no CALIBRATE branch.
- `git diff --quiet HEAD -- scripts/keda-load-proof.sh scripts/keda-gate.sh` (frozen scripts unchanged).
</verification>

<success_criteria>
The live-proof instrument (scripts/keda-audio-loadproof.sh) exists, statically verifies clean, and preserves the byte-unchanged discipline on the two frozen scripts — ready for Plan 03's live execution. SC3 itself is proven live in Plan 03.
</success_criteria>

<output>
Create `.planning/phases/33-keda-helm-chart-integration/33-02-SUMMARY.md` when done.
</output>
