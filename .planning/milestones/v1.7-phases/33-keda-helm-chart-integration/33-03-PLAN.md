---
phase: 33-keda-helm-chart-integration
plan: 03
type: execute
wave: 2
depends_on: [33-01, 33-02]
files_modified:
  - .planning/phases/33-keda-helm-chart-integration/evidence/
autonomous: false
requirements: [AUD-08]

must_haves:
  truths:
    - "Audio scale-from-zero (0→1) is live-proven on the OrbStack cluster with the model baked into the image (SC3)"
    - "Timestamped Phase-28-style evidence separates image-pull time from scale-from-zero orchestration time (SC3)"
    - "The unmodified keda-load-proof.sh is re-run live and confirms zero orphaned kubectl -w watchers survive teardown (closes Phase 29 deferred human-verification item)"
    - "Both stacks (compose and k8s) are DOWN at the end — OrbStack returned to the safe resting state"
    - "The bake-vs-volume decision is recorded as reversible+measured per Key Decision 3"
  artifacts:
    - path: ".planning/phases/33-keda-helm-chart-integration/evidence/"
      provides: "timestamped load-proof artifacts (gate transcript, sc3 event-timeline, burst CSV/PNG)"
      contains: "sc3-audio-scale-from-zero"
  key_links:
    - from: "scripts/keda-audio-loadproof.sh (live run)"
      to: ".planning/phases/33-keda-helm-chart-integration/evidence/"
      via: "timestamped artifact emission"
      pattern: "sc3-audio-scale-from-zero-.*\\.txt"
    - from: "scripts/keda-load-proof.sh (unmodified re-run)"
      to: "Phase 29 deferred human-verification item"
      via: "zero orphaned watcher confirmation post-teardown"
      pattern: "keda-load-proof"
---

<objective>
Run the live scale-from-zero proof for the audio class on the OrbStack cluster, capturing timestamped Phase-28-style evidence that measures image-pull vs scale-from-zero cold-start with the model baked in (SC3). Then re-run the UNMODIFIED keda-load-proof.sh to close Phase 29's deferred human-verification item (zero orphaned kubectl -w watchers). Both stacks start DOWN and MUST end DOWN — OrbStack is never left hot, and compose and k8s are never up simultaneously (four daemon-wedge incidents on record).

Purpose: Deliver SC3 — the only success criterion with no non-live substitute — and discharge the Phase-29 deferred obligation, closing v1.7's final phase.
Output: Timestamped evidence artifacts under the phase evidence/ directory; a recorded, measured bake-vs-volume decision; OrbStack back at rest.
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-keda-helm-chart-integration/33-RESEARCH.md
@.planning/phases/33-keda-helm-chart-integration/33-PATTERNS.md
@.planning/phases/33-keda-helm-chart-integration/33-01-SUMMARY.md
@.planning/phases/33-keda-helm-chart-integration/33-02-SUMMARY.md

<decisions>
Execution-environment discipline (STATE.md Key Decision 3 + OrbStack discipline):
- Both stacks START down. Bring up k8s with `orb start k8s`; verify daemon health before any build/install.
- Pre-build/tag images SEQUENTIALLY with non-:latest tags (OrbStack shared-store discipline — never parallel builds, never :latest). The audio-worker image is ~650MB; build it and the api image the chart needs, sequentially.
- NEVER run compose and k8s hot simultaneously. If compose is up, the load-proof scripts refuse to run — confirm compose is down first.
- ALWAYS tear down (helm uninstall via the scripts' EXIT traps) and leave k8s DOWN at the end (`orb stop k8s` or equivalent) — return to the safe resting state.

SC3 measurement (Open Question 2, resolved in Plan 02): measure audio 0→1 with the image already in OrbStack's shared store; capture the full kubectl-describe-pod event timeline (Scheduled→Pulling→Pulled→Created→Started). Pulling→Pulled ≈ 0 IS the honest local answer (bake-in imposes ~0 extra pull on OrbStack). Record the caveat: a genuine registry-backed 650MB cold pull is unmeasurable locally and deferred to a real-registry environment. Bake-vs-volume stays baked, reversible.

Phase-29 deferred obligation: one live re-run of the UNMODIFIED scripts/keda-load-proof.sh (document class) confirming zero orphaned `kubectl get pod -w` processes survive teardown and correct BUSY_POD selection — this closes 29-VERIFICATION's approved-with-deferral human-verification item.
</decisions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bring up k8s, sequential image builds, run keda-audio-loadproof.sh, capture SC3 evidence</name>
  <files>.planning/phases/33-keda-helm-chart-integration/evidence/</files>
  <read_first>
    - scripts/keda-audio-loadproof.sh (the instrument authored in Plan 02 — read its env-var knobs and expected image tags)
    - scripts/keda-load-proof.sh (its image-build/helm-install expectations, so the audio run mirrors them)
    - .planning/milestones/v1.6-phases/28-autoscale-load-proof/evidence/ (the 4-artifact naming + content convention to match)
    - Dockerfile.audio-worker (build target + baked model path /models/ggml-base.bin)
  </read_first>
  <action>
    Confirm the compose stack is DOWN (refuse otherwise). `orb start k8s`; verify the daemon is healthy (kubectl cluster-info / node Ready) before proceeding. Build the required images SEQUENTIALLY with non-:latest tags (audio-worker ~650MB and the api image the chart deploys) — one build at a time per OrbStack discipline. Run scripts/keda-audio-loadproof.sh: it self-installs KEDA, helm-installs the chart with values-local, seeds asynq:queues (incl. audio), triggers the audio class with jfk.wav, and captures the kubectl-describe-pod event timeline into a timestamped sc3-audio-scale-from-zero-<UTC-ts>.txt plus a gate-transcript-<ts>.log (and burst CSV/PNG if the sampler is included). The script's EXIT trap tears down helm/KEDA. Verify the emitted evidence separates Pulling→Pulled from Scheduled→Started; if Pulling→Pulled ≈ 0, that is the recorded honest local answer (with the registry-cold-pull caveat noted in Plan 03's SUMMARY). Do NOT engineer an artificial cold-pull. Leave k8s in whatever state the next task needs (it re-runs another script), but ensure this script's own teardown left no octoconv/keda release behind.
  </action>
  <verify>
    <automated>cd /Users/apaderin/dev/octoconv && ls .planning/phases/33-keda-helm-chart-integration/evidence/sc3-audio-scale-from-zero-*.txt >/dev/null 2>&1 && ls .planning/phases/33-keda-helm-chart-integration/evidence/gate-transcript-*.log >/dev/null 2>&1 && grep -Eq 'Pulling|Pulled|Started' .planning/phases/33-keda-helm-chart-integration/evidence/sc3-audio-scale-from-zero-*.txt</automated>
  </verify>
  <done>keda-audio-loadproof.sh ran to completion (exit 0), audio scaled 0→1, and timestamped evidence exists showing the pod-event timeline with pull-vs-orchestration timings; helm/KEDA torn down by the script's trap.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run the UNMODIFIED keda-load-proof.sh (Phase-29 deferred), confirm no orphaned watchers, leave both stacks down</name>
  <files>.planning/phases/33-keda-helm-chart-integration/evidence/</files>
  <read_first>
    - scripts/keda-load-proof.sh (byte-unchanged — the exact script Phase 29 deferred a live re-run of)
    - .planning/phases/29-v1-6-hardening-tail/29-VERIFICATION.md and 29-HUMAN-UAT.md (the deferred human-verification item's exact acceptance wording: zero orphaned `kubectl get pod -w`, correct BUSY_POD selection)
  </read_first>
  <action>
    With compose still DOWN, run the UNMODIFIED scripts/keda-load-proof.sh live (the document-class flagship gate). After it completes and its EXIT trap tears down, confirm zero orphaned `kubectl get pod ... -w` processes survive (e.g. `pgrep -f 'kubectl get pod'` returns nothing) and that BUSY_POD selection behaved correctly per its transcript — this closes Phase 29's approved-with-deferral human-verification item. Do NOT modify the script. Then return OrbStack to the safe resting state: confirm no octoconv/keda helm releases remain, and stop k8s (`orb stop k8s` or equivalent). Final invariant: both compose and k8s are DOWN. Copy/keep the keda-load-proof transcript alongside the phase evidence for the audit trail.
  </action>
  <verify>
    <automated>cd /Users/apaderin/dev/octoconv && ! pgrep -f 'kubectl get pod' >/dev/null && git diff --quiet HEAD -- scripts/keda-load-proof.sh scripts/keda-gate.sh</automated>
  </verify>
  <done>keda-load-proof.sh re-ran unmodified to completion; no orphaned kubectl -w watchers remain; the two frozen scripts are byte-unchanged; k8s is stopped and both stacks are down.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — SC3 evidence, Phase-29 closure, resting state</name>
  <action>Operator reviews the captured evidence and confirms the resting state after the two live runs (verification-only checkpoint; all automation completed in Tasks 1-2).</action>
  <what-built>The audio class autoscales in the cluster: SC3 timestamped scale-from-zero evidence captured with the model baked in, the Phase-29 deferred keda-load-proof.sh re-run completed with no orphaned watchers, and OrbStack returned to rest (both stacks down).</what-built>
  <how-to-verify>
    1. Open .planning/phases/33-keda-helm-chart-integration/evidence/sc3-audio-scale-from-zero-*.txt — confirm the pod-event timeline (Scheduled/Pulling/Pulled/Created/Started) has real timestamps and that image-pull time is isolated from orchestration time (expect Pulling→Pulled ≈ 0 on OrbStack's shared store; confirm the registry-cold-pull caveat is recorded).
    2. Skim gate-transcript-*.log — confirm audio-worker scaled 0→1 and the EXIT trap uninstalled helm/KEDA.
    3. Confirm the Phase-29 deferred item is closed: keda-load-proof.sh re-ran unmodified with zero orphaned kubectl -w processes.
    4. Confirm resting state: `kubectl cluster-info` unreachable / k8s stopped, `docker compose ps` empty — both stacks down.
    5. Confirm the bake-vs-volume decision is recorded in the SUMMARY as measured + reversible (Key Decision 3): bake-in stays.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. evidence missing a timing, a stack left hot, or the bake-vs-volume decision not recorded).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator/scripts → live OrbStack daemon | Bringing k8s up + building a 650MB image + hot compose simultaneously can wedge the daemon (four incidents on record) |
| load-proof run → evidence artifacts | An unauditable/informal proof cannot substantiate SC3's live claim |
| watcher subprocess → teardown | An orphaned kubectl -w is the exact Phase-29 defect being re-verified as closed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-08 | Denial of Service | OrbStack daemon (build/hot-stack) | mitigate | Sequential non-:latest builds; compose confirmed DOWN before any k8s work; never both hot; both scripts' EXIT traps + explicit `orb stop k8s` leave OrbStack at rest |
| T-33-09 | Repudiation | load-proof auditability | mitigate | Phase-28-style timestamped evidence dir (gate-transcript, sc3 event-timeline, burst CSV/PNG) provides an auditable trail for the SC3 live claim |
| T-33-10 | Denial of Service | orphaned kubectl -w watcher | mitigate | Re-run of the WR-01-fixed keda-load-proof.sh + explicit `pgrep -f 'kubectl get pod'` post-teardown check closes the Phase-29 deferred item and proves no reparented watcher survives |
| T-33-SC | Tampering | supply chain (installs) | accept | No package installs — the scripts install KEDA via the pinned kedacore/keda helm repo re-verified live at run time; images built from the already-pinned Dockerfile.audio-worker |
</threat_model>

<verification>
- keda-audio-loadproof.sh ran to completion; audio scaled 0→1; sc3-audio-scale-from-zero-<ts>.txt + gate-transcript-<ts>.log exist with real timestamps separating pull from orchestration.
- keda-load-proof.sh re-ran UNMODIFIED; `pgrep -f 'kubectl get pod'` empty post-teardown; frozen scripts byte-unchanged.
- Both stacks DOWN at the end (k8s stopped, compose empty).
- SUMMARY records the measured bake-vs-volume decision (baked, reversible, Key Decision 3) with the OrbStack pull-time caveat.
- Human checkpoint approved.
</verification>

<success_criteria>
SC3 is live-proven: audio scale-from-zero with the baked model, with timestamped evidence measuring image-pull vs scale-from-zero cold-start. The Phase-29 deferred human-verification item is closed. OrbStack is at rest (both stacks down). AUD-08 fully delivered across Plans 01-03; v1.7's final phase complete.
</success_criteria>

<output>
Create `.planning/phases/33-keda-helm-chart-integration/33-03-SUMMARY.md` when done.
</output>
