---
phase: 37-keda-helm-chart-integration
plan: 03
type: execute
wave: 2
depends_on: [37-01, 37-02]
files_modified:
  - .planning/phases/37-keda-helm-chart-integration/evidence/
autonomous: false
requirements: [AVE-05]

must_haves:
  truths:
    - "av scale-from-zero (0→1→N→0) is live-proven on the OrbStack cluster with timestamped Phase-33-style evidence separating image-pull from scale-from-zero orchestration time (SC3, D-07 proof #1)"
    - "A long in-flight av transcode survives a genuine N→N-1 HPA downscale — the pod's SIGTERM is honored under terminationGracePeriodSeconds=783 and the job reaches done with container exit 0, NOT exit 137 (SC4, D-07 proof #2)"
    - "IN-02 AV_* env-parity is confirmed across every queue-client-constructing service in the rendered chart (SC4 static half re-confirmed live)"
    - "Both stacks (compose and k8s) are DOWN at the end — OrbStack returned to the safe resting state (never both hot simultaneously)"
  artifacts:
    - path: ".planning/phases/37-keda-helm-chart-integration/evidence/"
      provides: "timestamped av load-proof artifacts (scale-from-zero event timeline + downscale-survival SIGTERM→done timeline + gate transcripts)"
      contains: "sc3-av-scale-from-zero"
  key_links:
    - from: "scripts/keda-av-loadproof.sh (live run)"
      to: ".planning/phases/37-keda-helm-chart-integration/evidence/"
      via: "timestamped artifact emission"
      pattern: "sc3-av-scale-from-zero-.*\\.txt"
    - from: "scripts/keda-av-downscale-survival.sh (live run)"
      to: ".planning/phases/37-keda-helm-chart-integration/evidence/"
      via: "timestamped SIGTERM→done + exit-code evidence"
      pattern: "sc4-av-downscale-survival-.*\\.txt"
---

<objective>
Run the two live av proofs D-07 requires on the OrbStack cluster, capturing timestamped Phase-33/28-style evidence: (1) scale-from-zero 0→1→N→0 (SC3) via scripts/keda-av-loadproof.sh, and (2) long-transcode survival across a genuine N→N-1 downscale (SC4) via scripts/keda-av-downscale-survival.sh — proving terminationGracePeriodSeconds=783 is honored (job reaches done, container exit 0, no exit-137/SIGKILL). Re-confirm IN-02 AV_* env-parity in the rendered chart. Both stacks start DOWN and MUST end DOWN — OrbStack is never left hot, and compose and k8s are never up simultaneously (four daemon-wedge incidents on record).

Purpose: Deliver SC3 and the live half of SC4 — the only success criteria with no non-live substitute — closing Phase 37, milestone v1.8, and requirement AVE-05.
Output: Timestamped evidence artifacts under the phase evidence/ directory; OrbStack back at rest (both stacks down).
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-keda-helm-chart-integration/37-CONTEXT.md
@.planning/phases/37-keda-helm-chart-integration/37-01-SUMMARY.md
@.planning/phases/37-keda-helm-chart-integration/37-02-SUMMARY.md

<decisions>
Execution-environment discipline (STATE.md OrbStack discipline + Phase 28/33 precedent):
- Both stacks START down. Bring up k8s with `orb start k8s`; verify daemon health (kubectl cluster-info / node Ready) before any build/install.
- Pre-build/tag images SEQUENTIALLY with non-:latest tags (OrbStack shared-store discipline — never parallel builds, never :latest). The av-worker image is heavy (ffmpeg + codec libs); build it and the api image the chart needs, one at a time.
- NEVER run compose and k8s hot simultaneously. If compose is up, the load-proof scripts refuse to run — confirm compose is down first.
- ALWAYS tear down (helm uninstall via the scripts' EXIT traps) and leave k8s DOWN at the end (`orb stop k8s` or equivalent) — return to the safe resting state.

SC3 measurement (Phase-33 precedent): measure av 0→1 with the image already in OrbStack's shared store; capture the full kubectl-describe-pod event timeline (Scheduled→Pulling→Pulled→Created→Started). Pulling→Pulled ≈ 0 IS the honest local answer; a genuine registry-backed cold pull of the heavy av-worker image is unmeasurable locally and deferred to a real-registry environment (record the caveat in the SUMMARY, carries AVX-02).

SC4 measurement (D-07 proof #2, load-bearing): with a long in-flight av transcode on the victim pod, the values-loadproof.yaml 15s stabilization override fires a prompt N→N-1 downscale; the victim pod receives SIGTERM and MUST complete the job under the PRODUCTION terminationGracePeriodSeconds=783 (NOT lowered in any overlay). Success = job reaches `done`, container exits 0; failure = exit 137 (SIGKILL) or the job requeued/failed mid-transcode.
</decisions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bring up k8s, sequential image builds, run keda-av-loadproof.sh, capture SC3 evidence</name>
  <files>.planning/phases/37-keda-helm-chart-integration/evidence/</files>
  <read_first>
    - scripts/keda-av-loadproof.sh (the instrument authored in Plan 02 — read its env-var knobs, expected image tags, and fixture-synthesis flow)
    - scripts/keda-audio-loadproof.sh (its image-build/helm-install expectations, so the av run mirrors them)
    - .planning/milestones/v1.7-phases/33-keda-helm-chart-integration/evidence/ (the artifact naming + content convention to match)
    - Dockerfile.av-worker (build target; confirm no --platform pin)
  </read_first>
  <action>
    Confirm the compose stack is DOWN (refuse otherwise). `orb start k8s`; verify the daemon is healthy (kubectl cluster-info / node Ready) before proceeding. Build the required images SEQUENTIALLY with non-:latest tags (av-worker heavy image + the api image the chart deploys) — one build at a time per OrbStack discipline. Run scripts/keda-av-loadproof.sh: it self-installs KEDA, helm-installs the chart with values-local, seeds asynq:queues (incl. av), synthesizes the short lavfi fixture, triggers the av class, and captures the kubectl-describe-pod event timeline into a timestamped sc3-av-scale-from-zero-<UTC-ts>.txt plus a gate-transcript-<ts>.log (and burst CSV/PNG if the sampler is included). The script's EXIT trap tears down helm/KEDA. Verify the emitted evidence separates Pulling→Pulled from Scheduled→Started and shows the 0→1(→N)→0 progression; if Pulling→Pulled ≈ 0, that is the recorded honest local answer (with the registry-cold-pull caveat noted in the SUMMARY). Do NOT engineer an artificial cold-pull. Ensure this script's own teardown left no octoconv/keda release behind.
  </action>
  <verify>
    <automated>cd /Users/apaderin/dev/octoconv && ls .planning/phases/37-keda-helm-chart-integration/evidence/sc3-av-scale-from-zero-*.txt >/dev/null 2>&1 && ls .planning/phases/37-keda-helm-chart-integration/evidence/gate-transcript-*.log >/dev/null 2>&1 && grep -Eq 'Pulling|Pulled|Started' .planning/phases/37-keda-helm-chart-integration/evidence/sc3-av-scale-from-zero-*.txt</automated>
  </verify>
  <done>keda-av-loadproof.sh ran to completion (exit 0), av scaled 0→1(→N)→0, and timestamped evidence exists showing the pod-event timeline with pull-vs-orchestration timings; helm/KEDA torn down by the script's trap.</done>
</task>

<task type="auto">
  <name>Task 2: Run keda-av-downscale-survival.sh (SC4), confirm env-parity, leave both stacks down</name>
  <files>.planning/phases/37-keda-helm-chart-integration/evidence/</files>
  <read_first>
    - scripts/keda-av-downscale-survival.sh (the SC4 instrument authored in Plan 02 — read its scenario flow, victim-pod selection, and exit-code checks)
    - deploy/chart/octoconv/values-loadproof.yaml (the keda.av.scaleDownStabilizationSeconds:15 override it layers; confirm NO terminationGracePeriodSeconds override)
    - .planning/milestones/v1.6-phases/28-autoscale-load-proof/28-CONTEXT.md (the downscale-victim/survival evidence pattern)
  </read_first>
  <action>
    With compose still DOWN, run scripts/keda-av-downscale-survival.sh live: it helm-installs with `-f values-local.yaml -f values-loadproof.yaml`, synthesizes the long lavfi fixture, scales av to maxReplicaCount (2) with a long in-flight transcode on the victim pod, drains the backlog so KEDA wants N→N-1, and after the 15s stabilization the HPA downscales and SIGTERMs the victim. Confirm from the emitted sc4-av-downscale-survival-<UTC-ts>.txt that the victim pod's terminationGracePeriodSeconds is 783, the in-flight job reached `done`, and the container exited 0 (NOT exit 137/SIGKILL). Then re-confirm IN-02 AV_* env-parity in the rendered chart: `helm template ... -f values-local.yaml` shows every queue-client Deployment (api + all workers) inheriting AV_ENGINE_TIMEOUT and AV_MAX_RETRY via the shared octoconv-config ConfigMap (envFrom). Return OrbStack to the safe resting state: confirm no octoconv/keda helm releases remain, stop k8s (`orb stop k8s` or equivalent). Final invariant: both compose and k8s are DOWN. Do NOT modify any frozen script or lower the production grace.
  </action>
  <verify>
    <automated>cd /Users/apaderin/dev/octoconv && ls .planning/phases/37-keda-helm-chart-integration/evidence/sc4-av-downscale-survival-*.txt >/dev/null 2>&1 && ! grep -Eq 'exit.?code.*137|Exit Code:\s*137|OOMKilled|SIGKILL' .planning/phases/37-keda-helm-chart-integration/evidence/sc4-av-downscale-survival-*.txt && helm template octoconv deploy/chart/octoconv -f deploy/chart/octoconv/values-local.yaml | grep -c 'name: octoconv-config' | awk '{exit !($1>=5)}' && ! pgrep -f 'kubectl get pod' >/dev/null</automated>
  </verify>
  <done>keda-av-downscale-survival.sh ran to completion; the long transcode survived the N→N-1 downscale (job done, exit 0, no exit-137) with terminationGracePeriodSeconds=783 honored; IN-02 AV_* parity confirmed across all queue-client Deployments via the shared ConfigMap; no orphaned kubectl -w watchers; k8s stopped and both stacks down.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — SC3 + SC4 live evidence, env-parity, resting state</name>
  <action>Operator reviews the captured evidence and confirms the resting state after the two live runs (verification-only checkpoint; all automation completed in Tasks 1-2).</action>
  <what-built>The av class autoscales in the cluster with production parity: SC3 timestamped scale-from-zero evidence (0→1→N→0), SC4 long-transcode survival across a genuine N→N-1 downscale (job done, exit 0, no premature SIGTERM under grace 783), IN-02 AV_* env-parity confirmed, and OrbStack returned to rest (both stacks down).</what-built>
  <how-to-verify>
    1. Open .planning/phases/37-keda-helm-chart-integration/evidence/sc3-av-scale-from-zero-*.txt — confirm the pod-event timeline (Scheduled/Pulling/Pulled/Created/Started) has real timestamps, image-pull is isolated from orchestration (expect Pulling→Pulled ≈ 0 on OrbStack; confirm the registry-cold-pull caveat is recorded in the SUMMARY, carries AVX-02), and the class scaled 0→1(→N)→0.
    2. Open .planning/phases/37-keda-helm-chart-integration/evidence/sc4-av-downscale-survival-*.txt — confirm the victim pod's terminationGracePeriodSeconds is 783, the SIGTERM→job-done timeline shows the in-flight transcode completing gracefully, and the container exited 0 (NO exit 137 / OOMKilled / SIGKILL). This is the evidence that validates D-04's grace formula.
    3. Skim gate-transcript-*.log — confirm both scripts' EXIT traps uninstalled helm/KEDA.
    4. Confirm IN-02 AV_* env-parity: `helm template ... -f values-local.yaml` shows api + all workers inheriting AV_ENGINE_TIMEOUT / AV_MAX_RETRY via the shared octoconv-config ConfigMap.
    5. Confirm resting state: `kubectl cluster-info` unreachable / k8s stopped, `docker compose ps` empty — both stacks down.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. an exit-137 in the downscale evidence, a missing timeline, a stack left hot, or env-parity not confirmed).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator/scripts → live OrbStack daemon | Bringing k8s up + building a heavy av image + hot compose simultaneously can wedge the daemon (four incidents on record) |
| HPA downscale → in-flight transcode | The N→N-1 SIGTERM crosses here; if grace 783 is not honored the live transcode is SIGKILLed (exit 137) mid-job — the exact defect SC4 proves is closed |
| load-proof run → evidence artifacts | An unauditable/informal proof cannot substantiate SC3/SC4's live claims |
| watcher subprocess → teardown | An orphaned kubectl -w survives the EXIT trap |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-10 | Denial of Service | HPA downscale premature SIGKILL of live transcode | mitigate | SC4 run proves terminationGracePeriodSeconds=783 is honored — grep gate asserts NO exit-137/OOMKilled/SIGKILL in the sc4 evidence; the job reaches done with exit 0 |
| T-37-11 | Denial of Service | OrbStack daemon (build/hot-stack) | mitigate | Sequential non-:latest builds; compose confirmed DOWN before any k8s work; never both hot; both scripts' EXIT traps + explicit `orb stop k8s` leave OrbStack at rest |
| T-37-12 | Repudiation | load-proof auditability | mitigate | Phase-28/33-style timestamped evidence dir (gate-transcript, sc3 event-timeline, sc4 SIGTERM→done timeline) provides an auditable trail for the SC3/SC4 live claims |
| T-37-13 | Denial of Service | orphaned kubectl -w watcher | mitigate | Both scripts' process-group + pkill teardown discipline + explicit `pgrep -f 'kubectl get pod'` post-teardown check proves no reparented watcher survives |
| T-37-SC | Tampering | supply chain (installs) | accept | No package installs — the scripts install KEDA via the pinned kedacore/keda helm repo re-verified live; images built from the already-pinned Dockerfile.av-worker |
</threat_model>

<verification>
- keda-av-loadproof.sh ran to completion; av scaled 0→1(→N)→0; sc3-av-scale-from-zero-<ts>.txt + gate-transcript-<ts>.log exist with real timestamps separating pull from orchestration.
- keda-av-downscale-survival.sh ran to completion; sc4-av-downscale-survival-<ts>.txt shows grace 783 honored, job done, container exit 0, NO exit-137/OOMKilled/SIGKILL.
- IN-02 AV_* env-parity confirmed across all queue-client Deployments via the shared ConfigMap.
- `pgrep -f 'kubectl get pod'` empty post-teardown; both stacks DOWN at the end (k8s stopped, compose empty).
- Human checkpoint approved.
</verification>

<success_criteria>
SC3 is live-proven (av scale-from-zero with timestamped evidence) and the live half of SC4 is proven (a long transcode survives a genuine N→N-1 downscale under grace 783, no premature SIGTERM). IN-02 AV_* env-parity is confirmed live. OrbStack is at rest (both stacks down). AVE-05 fully delivered across Plans 01-03; Phase 37 and milestone v1.8 complete.
</success_criteria>

<output>
Create `.planning/phases/37-keda-helm-chart-integration/37-03-SUMMARY.md` when done.
</output>
