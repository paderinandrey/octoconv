---
phase: 04-content-validation-storage-lifecycle-observability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/convert/sniff.go
  - internal/convert/sniff_test.go
  - internal/api/handlers.go
  - internal/api/handlers_test.go
  - internal/worker/worker.go
autonomous: true
requirements: [VALID-01, VALID-02]
must_haves:
  truths:
    - "A POST /v1/jobs upload whose magic bytes match a known-but-different format than the declared extension is rejected with 422 before any S3 write (D-01)"
    - "A POST /v1/jobs upload whose magic bytes match no known signature is rejected with 422 before any S3 write (D-02)"
    - "A well-formed upload whose detected format matches its declared extension is stored with the canonical detected-format Content-Type, not the client-supplied header (D-06)"
    - "Every magic-byte-mismatch rejection is logged with the resolved client_id (D-08)"
  artifacts:
    - path: internal/convert/sniff.go
      provides: "Magic-byte signature table, Sniff(peek+re-stitch), and MIMEType() shared format->MIME mapping"
      contains: "func Sniff"
    - path: internal/convert/sniff_test.go
      provides: "Per-format detection, no-match, stream-preservation, and MIMEType unit tests"
      contains: "func TestSniff"
  key_links:
    - from: internal/api/handlers.go
      to: internal/convert/sniff.go
      via: "convert.Sniff + convert.MIMEType called in handleCreateJob before the pair-check and before s3.Upload"
      pattern: "convert\\.Sniff|convert\\.MIMEType"
    - from: internal/worker/worker.go
      to: internal/convert/sniff.go
      via: "convert.MIMEType replaces the private contentTypeFor helper"
      pattern: "convert\\.MIMEType"
---

<objective>
Add magic-byte content validation to the job-intake path so a file's declared
extension must match its actual content before anything is written to S3, and
centralize the format→MIME mapping in the convert package.

Purpose: Close VALID-01/VALID-02 — replace "trust the filename/Content-Type
header" with "verify the actual bytes," rejecting mislabeled or corrupt uploads
fast with an actionable 422 instead of failing deep inside the worker.
Output: `internal/convert/sniff.go` (signature table + `Sniff` + `MIMEType`),
a reordered `handleCreateJob`, updated handler tests, and a `worker.go` that
uses the shared `convert.MIMEType`.
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-content-validation-storage-lifecycle-observability/04-CONTEXT.md
@.planning/phases/04-content-validation-storage-lifecycle-observability/04-RESEARCH.md
@.planning/phases/04-content-validation-storage-lifecycle-observability/04-PATTERNS.md

<interfaces>
<!-- Existing contracts the executor builds against. Do NOT re-derive from the codebase. -->

Registered image formats (internal/convert/libvips.go:9) — the exact closed set the signature table must cover:
  var imageFormats = []string{"png", "jpg", "webp", "heic", "tiff"}

Existing normalizer to reuse (internal/convert/convert.go:29) — detected output MUST pass through it so it composes with Registry.Supports:
  func NormalizeFormat(f string) string   // "jpeg"->"jpg", "tif"->"tiff", lowercases, strips leading "."

Existing MIME mapping to promote (internal/worker/worker.go:348-363) — currently a private `contentTypeFor(format string) string`; identical switch over png/jpg/webp/heic/tiff -> image/png,image/jpeg,image/webp,image/heic,image/tiff, default application/octet-stream.

Existing handler helpers to reuse unchanged (internal/api/handlers.go:198-209):
  func writeJSON(w http.ResponseWriter, status int, v any)
  func writeError(w http.ResponseWriter, status int, msg string)

Client identity in request context (internal/auth):
  client, _ := auth.ClientFromContext(ctx)   // resolved by auth.Middleware before handler runs; client.ID is a uuid.UUID
</interfaces>
</context>

## Phase Goal

**As an** operator of a trusted-internal conversion service, **I want to** have uploads verified by their real content bytes before they reach storage, **so that** a mislabeled or corrupt file is rejected immediately with an actionable 422 instead of failing deep in the worker.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Magic-byte signature table, Sniff, and shared MIMEType in internal/convert</name>
  <files>internal/convert/sniff.go, internal/convert/sniff_test.go</files>
  <read_first>
    - internal/convert/libvips.go (the exact registered format set `imageFormats`)
    - internal/convert/convert.go (package doc comment lives here; `NormalizeFormat` to compose with; `Lookup` (from,to)(_,bool) shape to mirror)
    - internal/worker/worker.go (the `contentTypeFor` switch at lines 348-363 to promote verbatim)
    - .planning/phases/04-content-validation-storage-lifecycle-observability/04-RESEARCH.md (Pattern 1 peek/re-stitch; Pitfall 1 HEIC brand detection; the exact byte signatures)
    - .planning/phases/04-content-validation-storage-lifecycle-observability/04-PATTERNS.md (sniff.go pattern assignment)
  </read_first>
  <behavior>
    - Test: PNG bytes (89 50 4E 47 0D 0A 1A 0A) detect as "png"
    - Test: JPEG bytes (FF D8 FF ...) detect as "jpg"
    - Test: WebP bytes ("RIFF"+4 arbitrary size bytes+"WEBP") detect as "webp"
    - Test: little-endian TIFF (49 49 2A 00) and big-endian TIFF (4D 4D 00 2A) both detect as "tiff"
    - Test: HEIC (4-byte size + "ftyp" at offset 4 + brand in {heic,heix,hevc,hevx,mif1,msf1} at offset 8) detects as "heic"; a "ftyp"+non-HEIF brand (e.g. "mp42") does NOT detect as heic (returns "")
    - Test: content matching no signature (e.g. "notanimage") returns detected == "" (D-02 unrecognized)
    - Test: a short input (< 12 bytes) never panics and returns "" when no signature fits
    - Test: the `rest` io.Reader returned by Sniff yields the FULL original byte stream (peeked prefix + remainder), byte-for-byte, so nothing is consumed
    - Test: MIMEType("png")=="image/png", ("jpg")=="image/jpeg", ("webp")=="image/webp", ("heic")=="image/heic", ("tiff")=="image/tiff", unknown=="application/octet-stream"
  </behavior>
  <action>
    Create `internal/convert/sniff.go` in `package convert` (no second package doc comment — convert.go already carries it). Define an unexported signature table keyed to the five registered formats and a `const sniffLen = 12` (WebP and HEIC both need offset 8-11). Implement `func Sniff(r io.Reader) (detected string, rest io.Reader, err error)`: read up to `sniffLen` bytes with `io.ReadFull`, tolerate `io.ErrUnexpectedEOF` (short files match fewer signatures, not an error), match against the table, normalize the winner through `NormalizeFormat`, return `""` when nothing matches (D-02), and re-stitch the peeked prefix onto the remainder with `io.MultiReader(bytes.NewReader(buf), r)` so callers upload the intact file (D-07 — never fully buffer). Real read errors (not ErrUnexpectedEOF) propagate as `err`. HEIC detection is NOT a fixed magic string: match bytes 4-7 == ASCII "ftyp" AND bytes 8-11 in the six-brand allow-list `{heic,heix,hevc,hevx,mif1,msf1}` (per D-03 hardcoded table + RESEARCH Pitfall 1); TIFF matches either endian marker. Promote the worker's `contentTypeFor` into this file as exported `func MIMEType(format string) string` with the identical switch (png/jpg/webp/heic/tiff and `application/octet-stream` default), routing the argument through `NormalizeFormat` first exactly as the original does. Write `sniff_test.go` covering every case in `<behavior>` using minimal literal byte fixtures.
  </action>
  <verify>
    <automated>go test ./internal/convert/ -run 'TestSniff|TestMIMEType' -v && go vet ./internal/convert/</automated>
  </verify>
  <done>Sniff detects each of png/jpg/webp/heic/tiff from minimal fixtures, returns "" for unrecognized/foreign-brand content, preserves the full stream through `rest`, and MIMEType returns the canonical MIME for each format; all new tests pass.</done>
  <acceptance_criteria>
    - `go test ./internal/convert/ -run 'TestSniff|TestMIMEType'` passes
    - `internal/convert/sniff.go` defines exported `Sniff` and `MIMEType`; `grep -c 'func Sniff' internal/convert/sniff.go` == 1 and `grep -c 'func MIMEType' internal/convert/sniff.go` == 1
    - `sniffLen` == 12 (source assertion: `grep -n 'sniffLen = 12' internal/convert/sniff.go` returns a line)
    - HEIC brand allow-list present: `grep -oE 'heic|heix|hevc|hevx|mif1|msf1' internal/convert/sniff.go | sort -u | wc -l` == 6
    - Sniff returns detected=="" for input "notanimage" (asserted by a passing test case)
    - `rest` stream equals the original bytes for a WebP fixture (asserted by a passing test case)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reorder handleCreateJob to detect-then-validate and adopt shared MIMEType</name>
  <files>internal/api/handlers.go, internal/api/handlers_test.go, internal/worker/worker.go</files>
  <read_first>
    - internal/api/handlers.go (the full `handleCreateJob`, lines 34-133 — current extension-first order, the `contentType := header.Header.Get("Content-Type")` at line 89, the upload at line 91, and client resolution at line 97)
    - internal/api/handlers_test.go (existing fakes and the four TestCreateJob_* / TestHealthz cases that will break when content sniffing runs — TestCreateJob_OK uses []byte("fakepng"), TestCreateJob_UnsupportedPair uses "fakepng")
    - internal/worker/worker.go (the two `contentTypeFor(job.TargetFormat)` call sites at lines 282 and 293, and the private func at 348-363 to remove)
    - internal/convert/sniff.go (the `Sniff`/`MIMEType` contract from Task 1)
    - .planning/phases/04-content-validation-storage-lifecycle-observability/04-CONTEXT.md (D-01,D-02,D-04,D-05,D-06,D-08)
    - .planning/phases/04-content-validation-storage-lifecycle-observability/04-PATTERNS.md (handleCreateJob pattern assignment; the D-08 logging exception; D-04 detailed-422 exception)
  </read_first>
  <behavior>
    - Test: multipart upload filename "in.jpg" carrying real PNG magic bytes, target "webp" -> 422, response error body contains both "jpg" and "png" (D-04 detailed message), storage.Upload NOT called, repo.Create NOT called
    - Test: multipart upload filename "in.png" carrying content "notanimage", target "webp" -> 422 (D-02 unrecognized), no upload, no create
    - Test: multipart upload filename "in.png" carrying real PNG magic bytes, target "webp" -> 202; storage.Upload called with contentType "image/png" (D-06, not the client multipart header); CreateParams.SourceFormat == "png"
    - Test (updated existing): TestCreateJob_UnsupportedPair now sends real PNG bytes with filename "in.png" target "mp3" -> still 422, reaching the pair-check after detection succeeds
    - Test (updated existing): TestCreateJob_OK sends real PNG bytes -> still 202
  </behavior>
  <action>
    Reorder `handleCreateJob` per D-05 without changing its Postgres-first double-write structure. Keep parsing the declared source from the filename extension via `convert.NormalizeFormat(path.Ext(...))` — it is still needed for the D-01 honesty comparison. Move the `client, _ := auth.ClientFromContext(ctx)` resolution UP so it precedes the detection step (the D-08 rejection log needs `client.ID`). Call `detected, rest, err := convert.Sniff(file)`: on a real read error return the existing generic `writeError(w, http.StatusBadRequest, "invalid multipart form")`; on `detected == ""` return `writeError(w, http.StatusUnprocessableEntity, ...)` with a detailed message about unrecognized content AND `log.Printf` a rejection line including `client.ID` (D-08 — the one sanctioned internal log exception); on `detected != source` return 422 with the D-04 detailed message that names both formats (e.g. "declared format "+source+" does not match detected content "+detected) AND the same D-08 `log.Printf`. Feed the DETECTED format (not the extension) into `convert.Default.Supports(detected, target)` for the pair-check (D-05). Replace `contentType := header.Header.Get("Content-Type")` with `contentType := convert.MIMEType(detected)` (D-06) and use it for both `s3.Upload` and `jobs.Input.ContentType`; pass the `rest` reader (not the raw `file`) to `s.storage.Upload` so the peeked prefix is included. Set `jobs.Input.Format` and `CreateParams.SourceFormat` to `detected`. In `internal/worker/worker.go`, delete the private `contentTypeFor` func and replace both call sites with `convert.MIMEType(job.TargetFormat)`. Update `internal/api/handlers_test.go`: add a valid-PNG-header fixture helper and the new mismatch/unrecognized/happy-path assertions from `<behavior>`, and fix the existing cases (TestCreateJob_OK, TestCreateJob_UnsupportedPair) to use real magic bytes so they still exercise their intended 202/422 paths. Add `"log"` to the handlers.go import set.
  </action>
  <verify>
    <automated>go test ./internal/api/ ./internal/worker/ ./internal/convert/ && go build ./... && go vet ./...</automated>
  </verify>
  <done>handleCreateJob detects content before the pair-check and before any S3 write, rejects declared/detected mismatch and unrecognized content with 422 + a client_id-tagged log, stores the detected-format MIME, and the worker shares convert.MIMEType; full build, vet, and package tests pass.</done>
  <acceptance_criteria>
    - `go test ./internal/api/ ./internal/worker/ ./internal/convert/` passes
    - `go build ./... && go vet ./...` clean
    - handleCreateJob no longer trusts the client header: `grep -c 'header.Header.Get("Content-Type")' internal/api/handlers.go` == 0
    - Detection precedes the pair-check: in `internal/api/handlers.go`, the line number of `convert.Sniff` is less than the line number of `convert.Default.Supports` (source-order assertion)
    - The private worker helper is gone: `grep -c 'func contentTypeFor' internal/worker/worker.go` == 0 and `grep -c 'convert.MIMEType' internal/worker/worker.go` >= 2
    - Mismatch path emits a client-scoped log: `grep -vE '^\s*//' internal/api/handlers.go | grep -c 'log.Printf'` >= 1
    - A mismatch request returns 422 with a body naming both declared and detected formats (asserted by a passing test)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (`POST /v1/jobs`) | Untrusted multipart upload: filename, extension, and Content-Type header are all attacker-controllable and must not be trusted for format identity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Spoofing/Tampering | `handleCreateJob` upload format identity | mitigate | Magic-byte `Sniff` runs before the pair-check and before any S3 write; declared-vs-detected mismatch and unrecognized content both reject with 422 (D-01/D-02). This IS the VALID-01/02 control (ASVS V12 file-type verification). |
| T-04-02 | Tampering | S3-stored object `Content-Type` metadata | mitigate | Stored Content-Type is `convert.MIMEType(detected)`, never the client multipart header (D-06) — removes the residual trust-the-client path (RESEARCH Pitfall 5). |
| T-04-03 | Information Disclosure | D-04 detailed 422 message + D-08 rejection log | accept | Declared-vs-detected format names are not sensitive internal state (unlike stack traces / temp paths / engine stderr); the exception is deliberate and scoped to these two sites only (CONTEXT D-04/D-08). |
| T-04-04 | Denial of Service | decompression-bomb / oversized-dimension image | accept | Explicitly deferred (D-09) — out of scope this phase; `MAX_UPLOAD_BYTES` byte cap remains the only size guard. |
</threat_model>

<verification>
- `go test ./internal/convert/ ./internal/api/ ./internal/worker/` all pass
- `go build ./... && go vet ./...` clean
- A `.jpg`-named upload containing PNG bytes is rejected 422 before storage (VALID-02); an unrecognized-content upload is rejected 422 (VALID-01/D-02); a truthfully-named PNG uploads with `image/png` stored Content-Type (D-06)
</verification>

<success_criteria>
- VALID-01: upload content is checked by magic bytes before storage/processing
- VALID-02: content/declared-format mismatch is rejected with 422 before the S3 write
- Detection is the source of truth fed into the pair-check (D-05); stored Content-Type is the detected-format MIME (D-06); rejections are logged with client_id (D-08); the format→MIME mapping has a single home in `internal/convert`
</success_criteria>

<output>
Create `.planning/phases/04-content-validation-storage-lifecycle-observability/04-01-SUMMARY.md` when done
</output>
