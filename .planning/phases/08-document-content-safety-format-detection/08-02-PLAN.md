---
phase: 08-document-content-safety-format-detection
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - internal/api/api.go
  - cmd/api/main.go
  - .env.example
  - internal/api/handlers.go
  - internal/api/handlers_test.go
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03]

must_haves:
  truths:
    - "handleCreateJob calls convert.SniffContainer on the original multipart.File + header.Size when Sniff returns '' and the file's first 4 bytes are PK\\x03\\x04 (D-01)"
    - "A well-formed docx/xlsx/pptx/odt/ods/odp is structurally detected as its true format before any S3 write (D-01)"
    - "An office document whose declared uncompressed total exceeds MAX_DOCUMENT_UNCOMPRESSED_BYTES is rejected 422 before storage/create (D-02)"
    - "An office document containing a macro part is rejected 422 before storage/create (D-03)"
    - "A duplicate-root-part office document is not accepted (fail-closed, falls through to unrecognized 422)"
    - "Document uploads skip the image-only dimension check entirely (dimension-regression fixed via convert.HasDimensionLimit)"
    - "No upload that fails a document safety check is written to S3 or recorded in Postgres"
    - "MAX_DOCUMENT_UNCOMPRESSED_BYTES is configurable, defaults to 500 MiB, and is documented in .env.example (D-04)"
  artifacts:
    - path: "internal/api/api.go"
      provides: "MaxDocumentUncompressedBytes Config field + maxDocumentUncompressedBytes Server field + 500 MiB default"
      contains: "MaxDocumentUncompressedBytes"
    - path: "cmd/api/main.go"
      provides: "MAX_DOCUMENT_UNCOMPRESSED_BYTES env wiring via envInt64"
      contains: "MAX_DOCUMENT_UNCOMPRESSED_BYTES"
    - path: ".env.example"
      provides: "Documented MAX_DOCUMENT_UNCOMPRESSED_BYTES line"
      contains: "MAX_DOCUMENT_UNCOMPRESSED_BYTES"
    - path: "internal/api/handlers.go"
      provides: "SniffContainer branch + zip-bomb/macro 422 rejections + HasDimensionLimit-guarded dimension check"
      contains: "convert.SniffContainer"
  key_links:
    - from: "internal/api/handlers.go"
      to: "convert.SniffContainer"
      via: "call on original multipart.File + header.Size when detected=='' and prefix is PK header"
      pattern: "convert\\.SniffContainer\\(file, header\\.Size\\)"
    - from: "internal/api/handlers.go"
      to: "convert.HasDimensionLimit"
      via: "guard wrapping the existing Dimensions() block"
      pattern: "convert\\.HasDimensionLimit\\(detected\\)"
    - from: "cmd/api/main.go"
      to: "api.Config.MaxDocumentUncompressedBytes"
      via: "envInt64 wiring"
      pattern: "MAX_DOCUMENT_UNCOMPRESSED_BYTES"
---

<objective>
Wire the Plan 08-01 detection layer into the request pipeline: `handleCreateJob` gains a container-inspection branch (structural office-format disambiguation, zip-bomb rejection, macro rejection) in the existing "reject before any storage write" position, the confirmed dimension-check regression is fixed with a `HasDimensionLimit` guard, and the new `MAX_DOCUMENT_UNCOMPRESSED_BYTES` limit is threaded through Config → env → docs following the exact `MAX_IMAGE_PIXELS` precedent.

Purpose: Detection logic is inert until the handler enforces it. This plan turns `SniffContainer`'s signals into HTTP 422 rejections and makes document uploads survive the existing pipeline (which today 422s every document on the unconditional `convert.Dimensions` call — RESEARCH.md Pitfall 5).

Output: modified `internal/api/api.go`, `cmd/api/main.go`, `.env.example`, `internal/api/handlers.go`, and new test cases in `internal/api/handlers_test.go`.

Scope boundary: This plan does NOT route documents to a document queue and does NOT register any document Converter — `convert.Default.Supports(...)` still returns false for every document pair, so a well-formed docx correctly 422s with "unsupported conversion" AFTER passing the safety gate. Engine routing is Phase 11. Do not add it here.
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-document-content-safety-format-detection/08-CONTEXT.md
@.planning/phases/08-document-content-safety-format-detection/08-RESEARCH.md
@.planning/phases/08-document-content-safety-format-detection/08-PATTERNS.md

<interfaces>
<!-- Contracts consumed FROM Plan 08-01 (internal/convert). Use directly — no exploration needed. -->
```go
// internal/convert/docsniff.go (delivered by Plan 08-01)
type ContainerResult struct {
    Format            string // "docx"|"xlsx"|"pptx"|"odt"|"ods"|"odp"|""
    TotalUncompressed uint64
    HasMacro          bool
    DuplicateRootPart bool
}
func SniffContainer(r io.ReaderAt, size int64) (ContainerResult, error)
func HasDimensionLimit(format string) bool
```

Existing surface this plan modifies:
```go
// internal/api/api.go — existing MaxImagePixels field pattern to mirror
type Config struct { MaxUploadBytes int64; MaxImagePixels uint64; PresignTTL time.Duration; IPRateLimitRPM int; ClientRateLimitRPM int }
// Server has: maxImagePixels uint64 (default 100_000_000 set in NewServer)

// internal/api/handlers.go — existing handleCreateJob sequence (lines 116-170):
//   Sniff -> detected=="" (unrecognized 422) -> detected!=source (mismatch 422)
//   -> Supports() pair-check -> convert.Dimensions() (CURRENTLY UNCONDITIONAL — the bug) -> maxImagePixels check
// `file` (multipart.File, implements io.ReaderAt) and `header.Size` are both in scope.
// `rest` is Sniff's io.MultiReader (does NOT implement io.ReaderAt — never pass it to SniffContainer).
// Structured rejection log convention: log.Printf("content validation rejected: client_id=%s filename=%q reason=<snake_case> ...", client.ID, filename, ...)

// cmd/api/main.go — existing wiring (line 101):
//   MaxImagePixels: uint64(envInt64("MAX_IMAGE_PIXELS", 100_000_000)),
// envInt64(key, def) helper already exists (line 157).
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire MaxDocumentUncompressedBytes config (three touch-points)</name>
  <files>internal/api/api.go, cmd/api/main.go, .env.example</files>
  <read_first>
    - internal/api/api.go (Server struct lines 50-65, Config struct 68-74, NewServer defaulting 81-95, struct-literal wiring 96-107 — the MaxImagePixels field is the exact precedent)
    - cmd/api/main.go (api.Config{...} literal at lines 99-104; envInt64 helper at 157-165)
    - .env.example (the `# API` section; MAX_IMAGE_PIXELS line 17 and its inline-comment style)
    - .planning/phases/08-document-content-safety-format-detection/08-PATTERNS.md (api.go / cmd main.go / .env.example MODIFY sections — exact field/comment text)
  </read_first>
  <action>
Follow the existing `MaxImagePixels` pattern exactly, four touch-points in three files:
1. `internal/api/api.go`: add `maxDocumentUncompressedBytes uint64` field to the `Server` struct (adjacent to `maxImagePixels`); add `MaxDocumentUncompressedBytes uint64` to the `Config` struct; in `NewServer` add a default block `if cfg.MaxDocumentUncompressedBytes == 0 { cfg.MaxDocumentUncompressedBytes = 500 << 20 }` with a comment citing D-04 (500 MiB); add `maxDocumentUncompressedBytes: cfg.MaxDocumentUncompressedBytes,` to the returned `&Server{...}` literal. Use `uint64` (matches the `UncompressedSize64` sum it is compared against).
2. `cmd/api/main.go`: add `MaxDocumentUncompressedBytes: uint64(envInt64("MAX_DOCUMENT_UNCOMPRESSED_BYTES", 500<<20)), // D-04: 500 MiB default` to the existing `api.Config{...}` literal. Reuse the existing `envInt64` helper unchanged — no new parsing helper.
3. `.env.example`: add `MAX_DOCUMENT_UNCOMPRESSED_BYTES=524288000   # zip-bomb guard: max total declared uncompressed size summed across all ZIP entries in an office document (default 500 MiB)` immediately below the `MAX_IMAGE_PIXELS` line, matching the existing inline-comment style.
  </action>
  <verify>
    <automated>cd /Users/apaderin/dev/octoconv && go build ./... && go vet ./internal/api/ ./cmd/api/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'MaxDocumentUncompressedBytes uint64' internal/api/api.go` (Config field)
    - `grep -q 'maxDocumentUncompressedBytes' internal/api/api.go` (Server field + literal wiring)
    - `grep -q '500 << 20' internal/api/api.go` (500 MiB default block)
    - `grep -q 'MAX_DOCUMENT_UNCOMPRESSED_BYTES' cmd/api/main.go` and it uses `envInt64`
    - `grep -q 'MAX_DOCUMENT_UNCOMPRESSED_BYTES=524288000' .env.example`
    - `go build ./...` succeeds; `go vet ./internal/api/ ./cmd/api/` clean
  </acceptance_criteria>
  <done>The 500 MiB-default document uncompressed-size limit is configurable via MAX_DOCUMENT_UNCOMPRESSED_BYTES, wired through Config → NewServer default → env → .env.example, and the server field is available to handleCreateJob as `s.maxDocumentUncompressedBytes`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate SniffContainer + zip-bomb/macro rejections + dimension-check guard into handleCreateJob</name>
  <files>internal/api/handlers.go, internal/api/handlers_test.go</files>
  <read_first>
    - internal/api/handlers.go (handleCreateJob lines 73-228; the Sniff→detected==""→mismatch→Supports→Dimensions→maxImagePixels sequence at 116-170 is the exact insertion region; note `file`, `header`, `rest`, `client`, `filename`, `source`, `detected` in-scope names)
    - internal/api/handlers_test.go (fixture helpers lines 122-169: oversizedPNGFixture, truncatedIHDRPNGFixture, jpegBytesFixture, multipartBody, newTestServer, authed; TestCreateJob_OK / _ContentMismatch / _UnrecognizedContent / _UnsupportedPair / _DimensionLimitExceeded shapes; fakeRepo/fakeStorage `.uploaded`/`.created` invariant fields)
    - internal/convert/docsniff.go (SniffContainer signature + ContainerResult fields — delivered by Plan 08-01)
    - internal/convert/dimensions.go (HasDimensionLimit — delivered by Plan 08-01)
    - .planning/phases/08-document-content-safety-format-detection/08-PATTERNS.md (handlers.go MODIFY section: exact branch shape, structured-log tags, dimension-guard wrapping; handlers_test.go MODIFY section: fixture-builder helpers + assertion invariants)
    - .planning/phases/08-document-content-safety-format-detection/08-RESEARCH.md (Pattern 5 read-from-original-file rule; Handler integration sketch; Pitfall 5 regression)
  </read_first>
  <behavior>
    - TestCreateJob_DocumentDetectedButUnsupported: a well-formed docx fixture with target "pdf" → 422 "unsupported conversion: docx -> pdf" (passes the safety gate, then correctly fails Supports() since no document Converter is registered this phase); store.uploaded==false, repo.created==nil
    - TestCreateJob_ODFDetectedButUnsupported: a well-formed odt fixture → same 422 unsupported-conversion outcome (proves ODF disambiguation path is reached)
    - TestCreateJob_ZipBombRejected: a docx-shaped fixture whose declared uncompressed total exceeds a small MaxDocumentUncompressedBytes set in Config → 422; store.uploaded==false, repo.created==nil
    - TestCreateJob_MacroRejected: a docx-shaped fixture containing word/vbaProject.bin → 422 macro rejection; store.uploaded==false, repo.created==nil
    - TestCreateJob_DuplicateRootPartRejected: docx-shaped fixture with two word/document.xml entries → NOT accepted as docx (falls through to unrecognized/mismatch 422); store.uploaded==false, repo.created==nil
    - TestCreateJob_BareZipUnrecognized: a plain .zip (no office parts) uploaded as "in.zip"/target → 422 unrecognized/unsupported; store.uploaded==false, repo.created==nil
    - Existing image tests (TestCreateJob_OK, _DimensionLimitExceeded, _UnrecognizedContent, _ContentMismatch, _UnsupportedPair) must all still pass unchanged — the dimension check still runs for images
  </behavior>
  <action>
Modify `handleCreateJob` in `internal/api/handlers.go`, following the existing idiom precisely (RESEARCH.md Pattern 5 + Handler integration sketch):

1. Container-inspection branch: after `convert.Sniff(file)` returns and BEFORE the existing `detected == ""` unrecognized rejection fires, add a branch: when `detected == ""`, read the first 4 bytes via `file.ReadAt(prefix[:], 0)` (positional read — MUST use the original `file`, which implements io.ReaderAt, NOT `rest` which is an io.MultiReader; per Pattern 5 `ReadAt` does not disturb Sniff's sequential cursor, so `rest` stays valid for the later Upload). If those 4 bytes equal `{'P','K',3,4}`, call `cr, cerr := convert.SniffContainer(file, header.Size)`. When `cerr == nil && cr.Format != "" && !cr.DuplicateRootPart`: set `detected = cr.Format`; then if `cr.TotalUncompressed > s.maxDocumentUncompressedBytes` reject 422 "declared uncompressed size exceeds configured limit"; then if `cr.HasMacro` reject 422 "macro-carrying documents are not accepted". A DuplicateRootPart result (or ErrNotAZip, or Format=="") intentionally leaves `detected` empty so the existing unrecognized-content 422 below handles it (fail-closed). Each new rejection uses the structured log convention with a distinct snake_case reason tag: `reason=zip_bomb declared_uncompressed=%d limit=%d` and `reason=macro_detected` (mirror the existing `reason=dimension_limit ...` format exactly). Do NOT re-stitch or reassign `rest` — SniffContainer's ReadAt reads never advance the sequential cursor.

2. Dimension-check regression fix: wrap the EXISTING dimension-check block (current lines 156-170: `convert.Dimensions(...)` → err handling → `rest = dimRest` → `totalPixels` → `maxImagePixels` check) in `if convert.HasDimensionLimit(detected) { ... }`. Documents (HasDimensionLimit false) skip the entire block; `rest` remains Sniff's stream unmodified. Image formats behave exactly as before. This is the fix for the confirmed regression (Pitfall 5) — documents skip the check, they do NOT get a document-specific dimension check.

Leave the `detected != source` mismatch check, `Supports()` pair-check, callback_url validation, upload, Create, and enqueue UNCHANGED. `bytes.Equal` for the prefix compare requires the `bytes` import — add it if not already imported.

Then add the `TestCreateJob_*` cases above to `internal/api/handlers_test.go` using the existing test shape: build via `NewServer(repo, store, &fakeQueue{}, resolver, healthyDeps(), Config{MaxUploadBytes: 1<<20, MaxDocumentUncompressedBytes: <small-for-bomb-test>})`, construct the body via the existing `multipartBody(t, filename, target, data)` helper, POST through `srv.Routes().ServeHTTP`, and assert status + `store.uploaded == false` + `repo.created == nil` for every rejection. Add fixture-builder helpers using `archive/zip.Writer` into a buffer (mirroring Plan 08-01's test fixtures): `docxFixture(t)`, `odtFixture(t)`, `zipBombDocxFixture(t, declaredSize)`, `macroDocxFixture(t)`, `duplicateRootPartDocxFixture(t)`, `bareZipFixture(t)`. Note the multipart filename extension must match the target/source-honesty check for the format tests (e.g. `in.docx` with detected docx passes the mismatch check, then hits Supports()).
  </action>
  <verify>
    <automated>cd /Users/apaderin/dev/octoconv && go test ./internal/api/ -run TestCreateJob -count=1 && go vet ./internal/api/</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/api/ -run TestCreateJob -count=1` passes, including all new document cases AND all pre-existing image cases (no regression)
    - `grep -q 'convert.SniffContainer(file, header.Size)' internal/api/handlers.go` (reads original file + size, per Pattern 5)
    - handlers.go does NOT pass `rest` to SniffContainer: `grep -c 'SniffContainer(rest' internal/api/handlers.go` returns 0
    - `grep -q 'convert.HasDimensionLimit(detected)' internal/api/handlers.go` guarding the dimension block
    - `grep -q 'reason=zip_bomb' internal/api/handlers.go` and `grep -q 'reason=macro_detected' internal/api/handlers.go` (structured rejection logs)
    - The zip-bomb and macro test cases assert `store.uploaded == false` and `repo.created == nil` (reject-before-storage invariant)
    - `go vet ./internal/api/` clean
  </acceptance_criteria>
  <done>handleCreateJob structurally detects office documents, rejects oversized-declared and macro-carrying documents with 422 before any S3 write, fails closed on duplicate-root-part/non-zip input, and skips the image-only dimension check for documents — with the full existing image path unchanged and all tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API `POST /v1/jobs` | Untrusted multipart file bytes; `handleCreateJob` enforces every safety decision here, before any S3 write or Postgres row (the "reject before storage" invariant) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Spoofing | handleCreateJob format acceptance | mitigate | SniffContainer's structural `Format` becomes the trusted `detected`; a bare zip / renamed archive yields Format=="" and hits the existing unrecognized-content 422 — extension is never trusted (extends Phase 4 D-01) |
| T-08-08 | Denial of Service | zip-bomb enforcement | mitigate | Reject 422 when `cr.TotalUncompressed > s.maxDocumentUncompressedBytes` (default 500 MiB, D-04) BEFORE upload/Create — no decompression, no storage write for a bomb |
| T-08-09 | Elevation of Privilege / Tampering | macro enforcement | mitigate | Reject 422 when `cr.HasMacro`, unconditional, no opt-out (D-05) — a macro-carrying document never reaches storage or the (future) engine |
| T-08-10 | Tampering (parser confusion / smuggling) | duplicate-root-part handling | mitigate | `cr.DuplicateRootPart` leaves `detected` empty → fails closed to the unrecognized-content 422; the handler never accepts an ambiguous archive (RESEARCH.md Pitfall 3) |
| T-08-11 | Information Disclosure | rejection error responses | mitigate | All new rejections use fixed short `writeError` strings and never echo parse-error internals; detail goes to the structured `content validation rejected: ... reason=<tag>` log only (existing D-08 convention) |
| T-08-12 | Tampering | ReaderAt vs sequential-read cursor coupling | accept | Reads via `file.ReadAt`/`SniffContainer` do not advance Sniff's sequential cursor (verified in RESEARCH.md Pattern 5), so `rest` remains the correct, intact stream for `s.storage.Upload` — no double-restitch, no truncation risk |
| T-08-SC | Tampering | package installs | accept | Zero external packages installed this phase — stdlib only. No install task, no legitimacy checkpoint required |
</threat_model>

<verification>
- `go test ./... -count=1` — whole repo green (no cross-package regression)
- `go build ./...` — compiles
- `go vet ./internal/api/ ./cmd/api/` — clean
- Manual reasoning check: a well-formed docx returns 422 "unsupported conversion" (NOT "unrecognized content" and NOT "cannot determine declared image dimensions") — proving detection + dimension-skip both work while engine routing correctly remains absent (Phase 11)
</verification>

<success_criteria>
- Office documents are structurally detected and the three safety checks (format match, zip-bomb, macro) reject unsafe uploads with 422 before storage (DOC-01/DOC-02/DOC-03)
- The dimension-check regression is fixed: documents skip the image-only check; images are unaffected
- `MAX_DOCUMENT_UNCOMPRESSED_BYTES` is configurable (default 500 MiB) and documented
- All existing image-path tests still pass; `go build ./...` and `go vet` clean
</success_criteria>

<output>
Create `.planning/phases/08-document-content-safety-format-detection/08-02-SUMMARY.md` when done
</output>
