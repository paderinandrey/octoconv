---
phase: 09-libreoffice-converter-engine
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - Dockerfile.worker
  - Dockerfile.worker-test
autonomous: true
requirements: [DOC-04, DOC-05, DOC-06]

must_haves:
  truths:
    - "The worker runtime image installs LibreOffice -nogui + fonts so soffice is on PATH at runtime, unprivileged as USER nobody (DOC-04)"
    - "A repeatable test image (Go toolchain + LibreOffice + procps) exists that runs the soffice-gated tests live, not skipped (DOC-06)"
    - "Running the gated tests inside the LibreOffice image proves zero surviving soffice/oosplash processes after a timeout kill (DOC-06)"
    - "Running Convert inside the LibreOffice image produces a valid %PDF- output for a real document fixture (DOC-04, DOC-05)"
  artifacts:
    - path: "Dockerfile.worker"
      provides: "runtime worker image with LibreOffice + fonts"
      contains: "libreoffice-writer-nogui"
    - path: "Dockerfile.worker-test"
      provides: "Go + LibreOffice + procps image that runs internal/convert tests live"
      contains: "golang:1.26-bookworm"
  key_links:
    - from: "Dockerfile.worker"
      to: "soffice on PATH"
      via: "apt-get install libreoffice -nogui packages"
      pattern: "libreoffice-(writer|calc|impress)-nogui"
    - from: "Dockerfile.worker-test"
      to: "go test ./internal/convert/"
      via: "container test run proving DOC-06 live"
      pattern: "go test"
---

<objective>
Provision LibreOffice into the worker's Docker image so `soffice` is present at runtime, add a repeatable Go+LibreOffice test image, and execute the soffice-gated tests from plan 09-01 live inside that image to prove the launch-blocking DOC-06 guarantee (a timed-out conversion leaves zero surviving `soffice`/`soffice.bin`/`oosplash` processes) and the live DOC-04/DOC-05 conversion path.

Purpose: DOC-04 requires LibreOffice available in the worker image; DOC-06 (per CONTEXT.md D-03) requires the process-kill guarantee to be proven by a LIVE integration test against a real installed LibreOffice, not assumed. This plan turns plan 09-01's gated (skipping) tests into an actually-executed, passing proof.

Output: `Dockerfile.worker` (LibreOffice packages added), `Dockerfile.worker-test` (new), and a captured live test run showing PASS (not SKIP) for both gated tests.

NOTE: Docker is a hard requirement for this plan (RESEARCH.md "Environment Availability": there is no fallback for proving a real OS process-group kill against a real binary). If `docker` is unavailable at execution time, stop and surface it as a blocker rather than marking DOC-06 satisfied.
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-libreoffice-converter-engine/09-CONTEXT.md
@.planning/phases/09-libreoffice-converter-engine/09-RESEARCH.md
@.planning/phases/09-libreoffice-converter-engine/09-PATTERNS.md

<interfaces>
<!-- Live-verified facts from RESEARCH.md (2026-07-09), reuse directly. -->
LibreOffice apt package set (installs cleanly on debian:bookworm-slim, version 4:7.4.7-1+deb12u13,
all currently-tracked important CVEs fixed):
  libreoffice-writer-nogui libreoffice-calc-nogui libreoffice-impress-nogui
  fonts-crosextra-carlito fonts-crosextra-caladea fonts-liberation2
Image size delta measured: 97.2MB base -> 521MB with LibreOffice+fonts (~424MB, arm64).
Existing Dockerfile.worker runtime stage installs (single RUN, --no-install-recommends):
  ca-certificates libvips-tools   (USER nobody, CGO_ENABLED=0 static build — all locked, preserve).
Gated tests authored in plan 09-01 (internal/convert/libreoffice_test.go):
  TestLibreOfficeConverter_TimeoutKillsRealProcess  (DOC-06: zero survivors after timeout)
  TestLibreOfficeConverter_ConvertProducesValidPDF  (DOC-04/DOC-05: real conversion -> valid %PDF-)
Both skip via exec.LookPath("soffice") when soffice is absent; the survivor check shells out to `ps`
(so the test image must include procps).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add LibreOffice to Dockerfile.worker and create Dockerfile.worker-test</name>
  <files>Dockerfile.worker, Dockerfile.worker-test</files>
  <read_first>
    - Dockerfile.worker (current single-RUN apt-get install line; multi-stage shape; USER nobody)
    - Dockerfile.api (comparison for the shared build-stage conventions)
    - .planning/phases/09-libreoffice-converter-engine/09-RESEARCH.md (Standard Stack install block, Package Legitimacy Audit, Pitfall 3 on HOME/fontconfig)
    - .planning/phases/09-libreoffice-converter-engine/09-PATTERNS.md (Dockerfile.worker MODIFIED section)
  </read_first>
  <action>
    Edit `Dockerfile.worker`: extend the SINGLE existing runtime-stage `RUN apt-get update && apt-get install -y --no-install-recommends ...` line (do NOT add a second RUN layer) so the install list becomes, in order: `ca-certificates`, `libvips-tools`, `libreoffice-writer-nogui`, `libreoffice-calc-nogui`, `libreoffice-impress-nogui`, `fonts-crosextra-carlito`, `fonts-crosextra-caladea`, `fonts-liberation2`, keeping the trailing `&& rm -rf /var/lib/apt/lists/*`. Update the block comment `# Runtime stage: image engine needs the libvips CLI.` to also mention the document engine (soffice). Preserve exactly: the two-stage `golang:1.26-bookworm` -> `debian:bookworm-slim` build, `CGO_ENABLED=0 go build`, the `COPY --from=build`, `USER nobody`, and the ENTRYPOINT. Do not pin package versions (bookworm apt resolves 4:7.4.7-1+deb12u13, verified). Do NOT add HOME/fontconfig provisioning — RESEARCH.md Pitfall 3 confirmed USER nobody degrades gracefully (exit 0, correct PDF); any HOME cleanup, if ever wanted, is a code-level cmd.Env concern, not a Dockerfile one.

    Create `Dockerfile.worker-test` (a dedicated, committed, repeatable test harness for the D-03 live proof, since the runtime debian-slim image has no Go toolchain and the build stage has no soffice): `FROM golang:1.26-bookworm`; a single `RUN apt-get update && apt-get install -y --no-install-recommends` line installing the same LibreOffice + font packages as above PLUS `procps` (provides `ps`, needed by the survivor assertion) and `libvips-tools` (so the full `./internal/convert/` suite runs), then `&& rm -rf /var/lib/apt/lists/*`; `WORKDIR /src`; `COPY go.mod go.sum ./`; `RUN go mod download`; `COPY . .`; and a default `CMD ["go", "test", "./internal/convert/", "-v"]`. Add a top comment explaining this image exists solely to run the soffice-gated convert tests live (DOC-06 per CONTEXT.md D-03) and is not a deployment artifact.
  </action>
  <verify>
    <automated>cd /Users/apaderin/dev/octoconv && grep -q libreoffice-writer-nogui Dockerfile.worker && grep -q 'USER nobody' Dockerfile.worker && grep -qE '^FROM golang:1.26-bookworm' Dockerfile.worker-test && grep -q procps Dockerfile.worker-test && docker build -f Dockerfile.worker -t octoconv-worker:phase9 . && docker build -f Dockerfile.worker-test -t octoconv-lo-test:phase9 .</automated>
  </verify>
  <acceptance_criteria>
    - `Dockerfile.worker` install list contains all three `libreoffice-*-nogui` packages and all three font packages, still on a single `RUN apt-get` line: `grep -c 'RUN apt-get' Dockerfile.worker` returns `1`.
    - `USER nobody` and the two-stage build are preserved: `grep -q 'USER nobody' Dockerfile.worker` and `grep -q 'CGO_ENABLED=0' Dockerfile.worker`.
    - `Dockerfile.worker-test` exists, is `FROM golang:1.26-bookworm`, installs the LibreOffice packages + `procps`, and ends with a `go test ./internal/convert/` CMD (`grep -q 'go.*test' Dockerfile.worker-test`).
    - `docker build -f Dockerfile.worker .` succeeds (exit 0).
    - `docker build -f Dockerfile.worker-test .` succeeds (exit 0).
  </acceptance_criteria>
  <done>The worker runtime image ships LibreOffice + fonts (soffice on PATH, USER nobody preserved), and a repeatable Go+LibreOffice+procps test image builds successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Prove DOC-06 (and live DOC-04/DOC-05) by running the gated tests inside the LibreOffice image</name>
  <files></files>
  <read_first>
    - .planning/phases/09-libreoffice-converter-engine/09-RESEARCH.md (D-03 methodology: poll ps for soffice.bin, SIGKILL group, assert zero survivors)
    - internal/convert/libreoffice_test.go (the two gated tests that must now RUN, not skip)
    - Dockerfile.worker-test (the image built in Task 1)
  </read_first>
  <action>
    Build the test image (`docker build -f Dockerfile.worker-test -t octoconv-lo-test:phase9 .`) if not already built in Task 1, then run the soffice-gated tests live inside it: `docker run --rm octoconv-lo-test:phase9 go test ./internal/convert/ -run 'TestLibreOfficeConverter' -v`. Confirm from the captured output that BOTH `TestLibreOfficeConverter_TimeoutKillsRealProcess` and `TestLibreOfficeConverter_ConvertProducesValidPDF` report `--- PASS` and that NEITHER reports `--- SKIP` (soffice is present in this image, so they must actually execute). The kill test executing to PASS is the DOC-06 launch-blocking live proof (zero surviving soffice/oosplash after the timeout fired mid-conversion); the conversion test executing to PASS is the live DOC-04/DOC-05 proof (a real fixture -> valid %PDF- output).

    Then run the full package suite live to confirm nothing regresses: `docker run --rm octoconv-lo-test:phase9 go test ./internal/convert/ -v` and confirm exit 0. Record the relevant PASS lines (and the absence of SKIP for the two gated tests) in the plan SUMMARY as the DOC-06 evidence, mirroring the project's live-verification discipline. Do not weaken or `-short`-skip the gated tests; if the kill test fails (a survivor is found), treat it as a launch blocker and stop — DOC-06 is not satisfied.
  </action>
  <verify>
    <automated>cd /Users/apaderin/dev/octoconv && docker build -f Dockerfile.worker-test -t octoconv-lo-test:phase9 . && docker run --rm octoconv-lo-test:phase9 go test ./internal/convert/ -run 'TestLibreOfficeConverter' -v 2>&1 | tee /tmp/lo-test.out && grep -q -- '--- PASS: TestLibreOfficeConverter_TimeoutKillsRealProcess' /tmp/lo-test.out && grep -q -- '--- PASS: TestLibreOfficeConverter_ConvertProducesValidPDF' /tmp/lo-test.out && ! grep -q -- '--- SKIP: TestLibreOfficeConverter' /tmp/lo-test.out</automated>
  </verify>
  <acceptance_criteria>
    - The container test run exits 0.
    - Output contains `--- PASS: TestLibreOfficeConverter_TimeoutKillsRealProcess` (DOC-06 zero-survivors proof executed live, not skipped).
    - Output contains `--- PASS: TestLibreOfficeConverter_ConvertProducesValidPDF` (live DOC-04/DOC-05 real-conversion -> valid %PDF-).
    - Output does NOT contain `--- SKIP:` for either gated test (proving soffice was present and the tests actually ran).
    - `docker run --rm octoconv-lo-test:phase9 go test ./internal/convert/` (full suite) exits 0.
  </acceptance_criteria>
  <done>DOC-06 is proven by an executed (not skipped) live integration test showing zero surviving soffice/oosplash processes after a timeout kill; DOC-04/DOC-05 are additionally validated live via a real document->PDF conversion inside the LibreOffice image.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external process (soffice/oosplash/soffice.bin) -> host resources | A hung/crashed conversion must not outlive its timeout or orphan children |
| Debian apt mirror -> worker/test image contents | Installed packages must come from a trusted, integrity-checked source |
| untrusted document -> LibreOffice import filter (in a running container) | A malformed document could trigger a filter bug at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Denial of Service | runCommand Setpgid + group-SIGKILL vs. oosplash forking soffice.bin | mitigate | Live integration test (executed in Task 2) proves the existing exec wrapper kills the whole process group with zero survivors, even mid-conversion — CONFIRMED live 2026-07-09 against 4:7.4.7-1+deb12u13, formalized here as a regression guard |
| T-09-05 | Elevation of Privilege / Denial of Service | malicious document triggers LibreOffice import-filter memory-corruption at runtime | mitigate | Debian security-patched package line (all tracked important CVEs fixed at 4:7.4.7-1+deb12u13); USER nobody unprivileged execution limits blast radius; DOCUMENT_ENGINE_TIMEOUT (Phase 10) bounds runtime |
| T-09-SC | Tampering | apt package supply chain (Dockerfile installs) | mitigate | Packages are Debian GPG-signed and pulled from the official bookworm/bookworm-security mirrors (a stronger trust model than unauthenticated npm/PyPI/crates). The slopcheck/blocking-human legitimacy gate is N/A: no npm/pip/cargo language-registry packages and no new Go module deps are introduced (RESEARCH.md Package Legitimacy Audit). No [ASSUMED]/[SUS] packages. |
</threat_model>

<verification>
- `docker build -f Dockerfile.worker .` and `docker build -f Dockerfile.worker-test .` both succeed.
- `Dockerfile.worker` keeps a single `RUN apt-get` line, `USER nobody`, and the two-stage CGO_ENABLED=0 build.
- The gated tests execute (not skip) inside the LibreOffice image and PASS; full `./internal/convert/` suite passes live.
</verification>

<success_criteria>
- Worker runtime image installs LibreOffice + fonts and preserves the unprivileged, static-binary, two-stage build shape.
- A repeatable Go+LibreOffice test image exists and runs the convert suite.
- DOC-06's process-kill guarantee is proven by an executed live test (zero survivors), and DOC-04/DOC-05 are validated live via a real document->PDF conversion.
</success_criteria>

<output>
Create `.planning/phases/09-libreoffice-converter-engine/09-02-SUMMARY.md` when done, including the captured PASS/no-SKIP evidence for the two gated tests as the DOC-06 proof.
</output>
