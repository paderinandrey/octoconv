---
phase: 12-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/convert/convert.go
  - internal/convert/libvips.go
  - internal/convert/libreoffice.go
  - internal/api/handlers.go
  - internal/reconciler/reconciler.go
  - internal/queue/queue.go
  - internal/queue/queue_test.go
  - internal/e2e/e2e_test.go
  - docker-compose.e2e.yml
  - docker-compose.yml
autonomous: true
requirements: [DEBT-01, DEBT-02, DEBT-03, DEBT-04, DEBT-05]

must_haves:
  truths:
    - "docker-compose.e2e.yml renders extra_hosts host.docker.internal:host-gateway on the api service"
    - "Engine-class values exist as exported constants in internal/convert; no raw \"image\"/\"document\" engine literals remain outside internal/convert (excluding _test.go and the SQL migration)"
    - "E2E HTTP clients enforce a per-request timeout so a hung endpoint yields a diagnosable timeout error, not a go-test binary panic"
    - "gofmt -l . returns zero files and go vet ./... is clean"
    - "Every variable in .env.example is either wired into its consuming docker-compose.yml service or carries an explicit inline comment explaining the deliberate omission"
  artifacts:
    - path: "internal/convert/convert.go"
      provides: "Exported EngineImage / EngineDocument engine-class constants"
      contains: "EngineImage"
    - path: "docker-compose.e2e.yml"
      provides: "api-service extra_hosts host-gateway alias for Linux docker"
      contains: "extra_hosts"
    - path: "internal/e2e/e2e_test.go"
      provides: "Shared HTTP client(s) with explicit per-request Timeout"
      contains: "Timeout:"
    - path: "docker-compose.yml"
      provides: "env wiring reconciled against .env.example with logged omissions"
      contains: "MAX_IMAGE_PIXELS"
  key_links:
    - from: "internal/api/handlers.go"
      to: "convert.EngineImage / convert.EngineDocument"
      via: "engine routing switch references the shared constants"
      pattern: "convert\\.Engine(Image|Document)"
    - from: "internal/reconciler/reconciler.go"
      to: "convert.EngineImage / convert.EngineDocument"
      via: "recovery-routing switch references the shared constants"
      pattern: "convert\\.Engine(Image|Document)"
    - from: "internal/queue/queue.go"
      to: "convert.EngineImage / convert.EngineDocument"
      via: "queue-name constants defined from the engine constants"
      pattern: "convert\\.Engine(Image|Document)"
---

<objective>
Close the inherited advisory tech debt (v1.0 docker-compose audit + v1.2 11-REVIEW.md WR-02/WR-03/WR-04 + a lingering gofmt nit) before v1.3 engine work begins, so new phases do not inherit known E2E/observability weak spots.

Purpose: DEBT-01..05 are all mechanical hygiene fixes with zero feature dependency. Clearing them first (per the milestone's tech-debt-first convention) keeps the runway clean for the riskier engine phases (13-16).
Output: Engine-class constants centralized in internal/convert; E2E harness hardened (api extra_hosts + per-request HTTP timeouts); gofmt clean; docker-compose.yml reconciled against .env.example.
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# Source-of-truth review that raised WR-02/WR-03/WR-04
@.planning/milestones/v1.2-phases/11-api-routing-end-to-end-document-conversion/11-REVIEW.md

<interfaces>
<!-- Executor should use these directly — no codebase exploration needed. -->

Engine-class routing lives on the Converter interface (internal/convert/convert.go):
  type Converter interface { Pairs() []Pair; Convert(...) error; Engine() string }
  func (r *Registry) EngineFor(from, to string) (string, bool)  // returns c.Engine()

Current raw engine literals to be replaced (grep-verified against the repo at revision time —
`grep -rn '"image"\|"document"' --include='*.go' internal/ cmd/ | grep -v '_test.go' | grep -v '^internal/convert/convert.go'`
returns exactly these 10 lines, ALL of which must be eliminated for the DEBT-02 gate to reach zero):
  internal/convert/libvips.go:37      // Engine reports the "image" engine class (D-01).   <-- DOC COMMENT, must be reworded
  internal/convert/libvips.go:38      func (LibvipsConverter) Engine() string { return "image" }
  internal/convert/libreoffice.go:70  // Engine reports the "document" engine class (D-01). <-- DOC COMMENT, must be reworded
  internal/convert/libreoffice.go:71  func (LibreOfficeConverter) Engine() string { return "document" }
  internal/api/handlers.go:27-28      engineImage = "image"; engineDocument = "document" (local unexported consts)
  internal/api/handlers.go:267,269    switch engine { case engineImage / case engineDocument }
  internal/reconciler/reconciler.go:133,135  switch j.Engine { case "image" / case "document" }
  internal/queue/queue.go:26,28       QueueImage = "image"; QueueDocument = "document" (queue-name consts)

The two DOC COMMENTS above are the trap the checker caught: Task 1 must reword them (drop the quoted
literal), not only rewrite the `return`/`case`/const lines — otherwise a fully correct implementation
still leaves a quoted "image"/"document" and fails the gate.

Task-type strings ("image:convert", "document:convert") and QueueWebhook ("webhook") are NOT engine-class
literals and MUST be left untouched — the grep gate matches only the whole quoted words "image"/"document".

The convert package imports no internal packages (verified), so queue -> convert introduces no import cycle.

Env-var -> consuming binary -> docker-compose service map (grep-verified at revision time), for DEBT-05:
  cmd/api/main.go            -> service `api`             (also constructs queue.NewClient at main.go:63)
  cmd/worker/main.go         -> service `worker`          (reconciler sweeper + webhook consumer; queue.NewClient at main.go:56)
  cmd/document-worker/main.go-> service `document-worker` (queue.NewClient at main.go:56; reads WEBHOOK_SIGNING_SECRET at :54)
  internal/queue/client.go   -> queue.NewClient() reads REDIS_ADDR, IMAGE_MAX_RETRY, ENGINE_TIMEOUT,
                                DOCUMENT_MAX_RETRY, DOCUMENT_ENGINE_TIMEOUT UNCONDITIONALLY at every one
                                of the THREE construction sites above (api, worker, document-worker).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Centralize engine-class constants + clear the gofmt nit (DEBT-02, DEBT-04)</name>
  <files>internal/convert/convert.go, internal/convert/libvips.go, internal/convert/libreoffice.go, internal/api/handlers.go, internal/reconciler/reconciler.go, internal/queue/queue.go, internal/queue/queue_test.go</files>
  <read_first>
    - internal/convert/convert.go (Converter interface, Registry, EngineFor — the documented "single source of truth")
    - internal/convert/libvips.go lines 37-38 (Engine() returns bare "image", with a doc comment that also quotes "image")
    - internal/convert/libreoffice.go lines 70-71 (Engine() returns bare "document", with a doc comment that also quotes "document")
    - internal/api/handlers.go lines 23-30 and 262-282 (local engineImage/engineDocument consts + routing switch)
    - internal/reconciler/reconciler.go lines 120-160 (engine-routing switch on j.Engine)
    - internal/queue/queue.go lines 16-29 (Type* and Queue* constant blocks)
    - 11-REVIEW.md WR-03 (the finding this task closes)
  </read_first>
  <action>
    Per DEBT-02 (WR-03): export canonical engine-class constants from internal/convert and reference them everywhere the engine class is named, so no hand-duplicated literal can drift.

    1. In internal/convert/convert.go add an exported const block: EngineImage = "image" and EngineDocument = "document". Give it a doc comment naming it the single compile-time source of truth for engine-class identifiers (referenced by Converter.Engine implementations, the API routing switch, the reconciler recovery switch, and the queue-name constants). This is the ONLY file permitted to hold the raw string values (the DEBT-02 gate excludes convert.go).
    2. internal/convert/libvips.go: change Engine() to return the bare identifier EngineImage (same package). ALSO reword its doc comment (line 37) to drop the quoted literal — e.g. `// Engine reports the image engine class (D-01).` (no quotes around image). internal/convert/libreoffice.go: return EngineDocument, and reword its doc comment (line 70) the same way (e.g. `// Engine reports the document engine class (D-01).`). Rewording the two comments is REQUIRED — the DEBT-02 grep gate matches the quoted words in comments too, and these two comments are the only quoted engine literals the rest of this task does not otherwise remove.
    3. internal/api/handlers.go: delete the local engineImage/engineDocument consts (lines 27-28); change the routing switch cases to convert.EngineImage / convert.EngineDocument. handlers.go already imports internal/convert.
    4. internal/reconciler/reconciler.go: add the internal/convert import; change the `switch j.Engine` cases "image"/"document" to convert.EngineImage / convert.EngineDocument. Leave the fail-closed default branch and its unroutable_engine metric untouched.
    5. internal/queue/queue.go: add the internal/convert import; define QueueImage = convert.EngineImage and QueueDocument = convert.EngineDocument (tie queue names to engine constants so they cannot drift). Do NOT touch QueueWebhook, the Type* task-type strings, or any retry-schedule code.
    6. DEBT-04: run `gofmt -w internal/queue/queue_test.go` to clear the pre-existing formatting nit (confirmed still flagged by `gofmt -l .` at revision time). If gofmt flags any file you touched above, format it too.

    Do not change any runtime string value — EngineImage/EngineDocument/QueueImage/QueueDocument all still equal "image"/"document" exactly; this is a pure de-duplication so behavior and the DB CHECK constraint stay consistent.
  </action>
  <acceptance_criteria>
    - `grep -rn '"image"\|"document"' --include='*.go' internal/ cmd/ | grep -v '_test.go' | grep -v '^internal/convert/convert.go'` returns ZERO lines. NOTE: this requires the two doc comments in libvips.go/libreoffice.go to be reworded (step 2) in addition to the const/case/return rewrites — all 10 current matches must be gone; the only permitted raw engine literals are the const definitions in the excluded internal/convert/convert.go. Task-type strings like "image:convert" do not match the whole-word pattern.
    - `grep -rn 'convert.EngineImage\|convert.EngineDocument' internal/api/handlers.go internal/reconciler/reconciler.go internal/queue/queue.go` shows all three files referencing the shared constants.
    - `go build ./...` succeeds and `go vet ./...` is clean.
    - `go test ./internal/convert/... ./internal/api/... ./internal/reconciler/... ./internal/queue/...` passes.
    - `gofmt -l .` returns empty (DEBT-04 satisfied).
  </acceptance_criteria>
  <verify>
    <automated>[ -z "$(gofmt -l .)" ] && go vet ./... && go test ./internal/convert/... ./internal/api/... ./internal/reconciler/... ./internal/queue/... && [ -z "$(grep -rn '\"image\"\|\"document\"' --include='*.go' internal/ cmd/ | grep -v '_test.go' | grep -v '^internal/convert/convert.go')" ]</automated>
  </verify>
  <done>Engine-class values are defined once in internal/convert; api, reconciler, and queue reference the constants; the two Engine() doc comments no longer quote the literal; no raw engine literal survives outside internal/convert (tests/migration excepted); gofmt and vet are clean; all touched-package unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Harden the E2E harness — api extra_hosts + per-request HTTP timeouts (DEBT-01, DEBT-03)</name>
  <files>docker-compose.e2e.yml, internal/e2e/e2e_test.go</files>
  <read_first>
    - docker-compose.e2e.yml (extra_hosts currently on worker + document-worker only, NOT api — confirmed at revision time: merged config shows api.extra_hosts == null, worker/document-worker carry host.docker.internal:host-gateway)
    - internal/e2e/e2e_test.go lines 1-27 (package doc), 112-150 (postJob → http.DefaultClient), 171-190 (pollUntilDone → http.DefaultClient), 305-320 (download via downloadClient), 385-404 (downloadClient builder)
    - 11-REVIEW.md WR-02 and WR-04 (the findings this task closes)
  </read_first>
  <action>
    DEBT-01 (WR-02): the E2E callback host defaults to host.docker.internal, and validateCallbackURL runs inside the api process at job creation (net.LookupHost). On plain-Linux docker (standard CI) that name does not resolve without an extra_hosts alias, so the sample.docx subtest fails with 400 invalid callback_url before any conversion runs. Add to docker-compose.e2e.yml under the existing `api:` service (which currently only sets WEBHOOK_ALLOW_* env) an `extra_hosts:` entry `- "host.docker.internal:host-gateway"`, matching the alias already present on worker/document-worker. Keep the existing api environment block intact.

    DEBT-03 (WR-04): postJob and pollUntilDone use http.DefaultClient (zero Timeout) and downloadClient() builds a client with no Timeout, so a hung endpoint blocks forever inside Do() and the suite dies with the go-test binary-timeout panic instead of its intended per-job diagnostic. Introduce a single shared package-level client with an explicit per-request timeout (e.g. `var e2eHTTP = &http.Client{Timeout: 30 * time.Second}`) and use it in postJob and pollUntilDone in place of http.DefaultClient. In downloadClient(): set an explicit Timeout (e.g. 60 * time.Second) on BOTH return branches — the E2E_S3_DIAL_ADDR custom-transport client AND the fallback (replace the bare `return http.DefaultClient` with a client that carries the timeout). Pick timeout values comfortably above a healthy single request but well under pollUntilDone's 5-minute between-poll deadline so a genuinely hung request surfaces as a diagnosable client timeout error, not a suite-wide panic.

    Do not change the presigned-download Host/dial-rewrite logic in downloadClient (the V4 signature covers the Host header — only add Timeout, leave Transport/DialContext untouched). Update the e2e package doc header only if a value it documents changes; a note about the client timeout is optional.
  </action>
  <acceptance_criteria>
    - The merged `docker compose -f docker-compose.yml -f docker-compose.e2e.yml config` places `host.docker.internal:host-gateway` specifically in the `api` service's extra_hosts (scoped to api — NOT merely "present somewhere in the file", since worker/document-worker already carry it). Live plain-Linux run is environment-dependent and out of scope on the macOS dev machine.
    - `grep -c 'http.DefaultClient' internal/e2e/e2e_test.go` returns 0 (no un-timed default client remains in the suite).
    - `grep -c 'Timeout:' internal/e2e/e2e_test.go` returns >= 2 (shared client + downloadClient branches carry explicit timeouts).
    - `go vet ./internal/e2e/...` is clean and `go test ./internal/e2e/...` still compiles + self-skips without E2E_BASE_URL (offline `go test ./...` stays green).
  </acceptance_criteria>
  <verify>
    <automated>docker compose -f docker-compose.yml -f docker-compose.e2e.yml config --format json 2>/dev/null | node -e 'const c=JSON.parse(require("fs").readFileSync(0));const h=(c.services.api&&c.services.api.extra_hosts)||[];process.exit(JSON.stringify(h).includes("host.docker.internal")?0:1)' && test "$(grep -c 'http.DefaultClient' internal/e2e/e2e_test.go)" = "0" && go vet ./internal/e2e/... && go test ./internal/e2e/...</automated>
  </verify>
  <done>The e2e compose override gives the api service (specifically) the host-gateway alias so the suite works on Linux docker; every E2E HTTP call carries an explicit per-request timeout; the suite still compiles and self-skips offline.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile docker-compose.yml against .env.example (DEBT-05)</name>
  <files>docker-compose.yml</files>
  <read_first>
    - .env.example (authoritative list of documented variables + their defaults and inline rationale)
    - docker-compose.yml (current per-service environment blocks: api, worker, document-worker)
    - cmd/api/main.go, cmd/worker/main.go, cmd/document-worker/main.go, internal/queue/client.go (to confirm which binary consumes each variable)
  </read_first>
  <action>
    DEBT-05 (v1.0 carry-over): reconcile every variable documented in .env.example against docker-compose.yml so no silent gap remains — each documented var must be either wired into its consuming service or carry an explicit inline comment stating why it is deliberately omitted.

    Confirm the consuming binary for each var by grepping cmd/*/main.go and internal/queue/client.go, then map binary→service (cmd/api→api, cmd/worker→worker, cmd/document-worker→document-worker). Known gaps identified at revision time (verify before acting):
    - MAX_IMAGE_PIXELS, MAX_DOCUMENT_UNCOMPRESSED_BYTES → consumed by cmd/api → add to `api` env using the .env.example defaults.
    - IMAGE_MAX_RETRY, DOCUMENT_MAX_RETRY, ENGINE_TIMEOUT, DOCUMENT_ENGINE_TIMEOUT → read UNCONDITIONALLY by internal/queue/client.go (queue.NewClient), and queue.NewClient is constructed at THREE sites — cmd/api/main.go:63, cmd/worker/main.go:56, AND cmd/document-worker/main.go:56. So every one of these vars is a live input to all three services (api, worker, document-worker) even where it isn't obviously "about" that binary. Add each var (with its .env.example default) to every service that constructs NewClient, unless it is already present. Verify the current state first: ENGINE_TIMEOUT is already on `worker`; DOCUMENT_ENGINE_TIMEOUT is already on `document-worker`; fill in whatever each NewClient site is missing so no site silently falls back to a hardcoded default.
    - WEBHOOK_PRESIGN_TTL → consumed by cmd/worker AND cmd/document-worker → add to both `worker` and `document-worker` env.
    - WEBHOOK_SIGNING_SECRET → currently set ONLY on `worker` (docker-compose.yml:118) but ALSO read by cmd/document-worker/main.go:54 to sign document webhooks. This is a real gap. DECISION: wire it into the `document-worker` env too (document-worker genuinely signs its own callbacks — it must not fall back to an empty secret). Use the same dev placeholder value already on `worker`.
    - RECONCILER_QUEUED_STALE_AFTER, RECONCILER_ACTIVE_STALE_AFTER, RECONCILER_SWEEP_INTERVAL, RECONCILER_MAX_RECOVERIES → consumed by cmd/worker (sweeper runs in the worker process) → add all four to `worker` env with .env.example defaults.
    - WEBHOOK_ALLOW_PRIVATE_IPS, WEBHOOK_ALLOW_INSECURE_HTTP → consumed by cmd/api. These MUST NOT be wired into base compose (they default false and are the production SSRF guard; only docker-compose.e2e.yml is permitted to relax them). Instead add a short inline comment on the `api` service documenting the deliberate omission ("SSRF guard stays default-off in base compose; relaxed only in docker-compose.e2e.yml").

    Use the exact default values and units from .env.example (e.g. MAX_IMAGE_PIXELS "100000000", RECONCILER_SWEEP_INTERVAL "1m", WEBHOOK_PRESIGN_TTL "6h"). Quote every value as a string (compose convention already used in the file). Do not remove or renumber any existing wired variable. Do not add a reconciler/webhook-worker service (out of scope this phase). Leave .env.example itself unchanged unless you discover a var in compose that .env.example fails to document — in that case add the doc line to .env.example.

    Every remaining unwired var must have a corresponding inline `#` comment in docker-compose.yml explaining the omission — no silent gaps.
  </action>
  <acceptance_criteria>
    - Every variable name in .env.example either appears in the correct docker-compose.yml service environment block OR has an explicit inline comment in docker-compose.yml explaining its deliberate omission. Verify by walking the .env.example var list against the rendered config.
    - `docker compose config` renders successfully (valid YAML/compose after edits): exit code 0.
    - WEBHOOK_ALLOW_PRIVATE_IPS and WEBHOOK_ALLOW_INSECURE_HTTP are NOT set to true anywhere in base docker-compose.yml (SSRF guard preserved); their omission is documented by comment.
    - WEBHOOK_SIGNING_SECRET appears under `document-worker` (not only `worker`), since document-worker signs its own webhooks.
    - MAX_IMAGE_PIXELS, MAX_DOCUMENT_UNCOMPRESSED_BYTES appear under the `api` service; RECONCILER_* (4 vars) and WEBHOOK_PRESIGN_TTL appear under `worker`; WEBHOOK_PRESIGN_TTL also under `document-worker`.
  </acceptance_criteria>
  <verify>
    <automated>docker compose config >/dev/null 2>&1 && grep -q 'MAX_IMAGE_PIXELS' docker-compose.yml && grep -q 'RECONCILER_SWEEP_INTERVAL' docker-compose.yml && grep -q 'WEBHOOK_PRESIGN_TTL' docker-compose.yml && test "$(docker compose config --format json 2>/dev/null | node -e 'const c=JSON.parse(require("fs").readFileSync(0));const e=(c.services["document-worker"]&&c.services["document-worker"].environment)||{};process.stdout.write(e.WEBHOOK_SIGNING_SECRET?"1":"0")')" = "1" && ! grep -E 'WEBHOOK_ALLOW_(PRIVATE_IPS|INSECURE_HTTP):\s*"?true' docker-compose.yml</automated>
  </verify>
  <done>Every .env.example variable is either wired into its consuming docker-compose.yml service (with the documented default) or carries an inline comment explaining the deliberate omission; WEBHOOK_SIGNING_SECRET is wired into document-worker; the base-compose SSRF guard remains default-off; `docker compose config` renders cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API (webhook callback_url) | The only security-relevant surface touched: SSRF guard on callback_url validation. |

## STRIDE Threat Register

This phase is mechanical refactoring + config hygiene with no new code paths, no new dependencies (no npm/pip/cargo installs — package legitimacy gate N/A), and no change to any runtime string value. Threat surface is essentially unchanged; the two config-adjacent items are called out explicitly below.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information Disclosure / SSRF | docker-compose.yml env audit (DEBT-05) | mitigate | Base compose MUST keep WEBHOOK_ALLOW_PRIVATE_IPS / WEBHOOK_ALLOW_INSECURE_HTTP default-off; Task 3 documents the omission and the verify gate asserts neither is set to true. |
| T-12-02 | Spoofing / SSRF (test-only relaxation) | docker-compose.e2e.yml extra_hosts (DEBT-01) | accept | The host-gateway alias + SSRF relaxation is confined to the e2e override file (verified absent from base compose per 11-REVIEW.md); it exists solely so the httptest webhook receiver is reachable during E2E. Accepted, test-only. |
| T-12-03 | Tampering (drift) | engine-class constants (DEBT-02) | mitigate | Centralizing the literals removes the compile-clean typo/drift surface WR-03 flagged; the fail-closed default branches in api/reconciler are preserved unchanged. |
</threat_model>

<verification>
Whole-phase gates (run after all three tasks):
- `[ -z "$(gofmt -l .)" ]` (DEBT-04 — whitespace-tolerant; `wc -l | grep -qx '0'` is a false-fail on this platform because wc pads with leading spaces).
- `go build ./... && go vet ./...` clean.
- `go test ./...` passes (e2e self-skips offline).
- `[ -z "$(grep -rn '"image"\|"document"' --include='*.go' internal/ cmd/ | grep -v '_test.go' | grep -v '^internal/convert/convert.go')" ]` returns zero lines (DEBT-02 — requires the two Engine() doc comments to be reworded, not just the code lines).
- `docker compose -f docker-compose.yml -f docker-compose.e2e.yml config --format json | node` check of `.services.api.extra_hosts` contains host.docker.internal (DEBT-01 — scoped to the api service; a file-wide grep is a false-pass because worker/document-worker already carry the alias) and `docker compose config` renders cleanly (DEBT-05).
</verification>

<success_criteria>
- DEBT-01: docker-compose.e2e.yml api service has extra_hosts host.docker.internal:host-gateway (api-scoped compose config confirms).
- DEBT-02: EngineImage/EngineDocument exported from internal/convert; api, reconciler, queue reference them; grep finds no engine literals (including the two reworded doc comments) outside internal/convert (tests/migration excepted).
- DEBT-03: e2e HTTP clients carry explicit per-request timeouts; no http.DefaultClient remains.
- DEBT-04: gofmt -l . returns zero files.
- DEBT-05: every .env.example var wired into its docker-compose.yml service or explicitly commented as a deliberate omission; WEBHOOK_SIGNING_SECRET wired into document-worker; base-compose SSRF guard preserved.
</success_criteria>

<output>
Create `.planning/phases/12-tech-debt-cleanup/12-01-SUMMARY.md` when done. Record the DEBT-05 audit result (which vars were wired vs deliberately omitted, and why — including the WEBHOOK_SIGNING_SECRET document-worker wiring and the three queue.NewClient construction sites) in the summary since this phase writes no separate report file.
</output>
</content>
</invoke>
