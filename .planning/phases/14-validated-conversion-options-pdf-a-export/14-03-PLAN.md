---
phase: 14-validated-conversion-options-pdf-a-export
plan: 03
type: execute
wave: 3
depends_on: ["14-01", "14-02"]
files_modified:
  - internal/api/handlers.go
  - internal/api/handlers_test.go
  - internal/e2e/e2e_test.go
autonomous: false
requirements: [OPTS-01, OPTS-02]
must_haves:
  truths:
    - "POST /v1/jobs parses an optional opts form field, strict-validates it (syntax + applicability) BEFORE any S3 write, and 422s on unknown keys, bad values, inapplicable opts, or an oversized opts field"
    - "Accepted opts are normalized, stored, and echoed back in the 201 response and GET /v1/jobs/{id}"
    - "A live docker-compose run produces a real PDF/A-2b document→pdf output carrying the /GTS_PDFA OutputIntent marker, real 422s for invalid/inapplicable opts, and an unchanged no-opts document→pdf conversion"
  artifacts:
    - path: "internal/api/handlers.go"
      provides: "opts form-field parse + two-step validation + normalized persist + response echo"
      contains: "formFieldOpts"
    - path: "internal/api/handlers_test.go"
      provides: "unit tests: accept, unknown-key 422, inapplicable 422, oversize 422, API-level injection attempt"
      contains: "opts"
    - path: "internal/e2e/e2e_test.go"
      provides: "PDF/A happy-path + negative opts + no-opts regression; opts-threaded helpers; assertDownloadIsPDFA"
      contains: "pdf_profile"
  key_links:
    - from: "internal/api/handlers.go handleCreateJob"
      to: "convert.ParseDocOpts / convert.ValidateApplicability"
      via: "two-step validation after EngineFor, before storage.Upload"
      pattern: "ValidateApplicability"
    - from: "internal/api/handlers.go handleCreateJob"
      to: "jobs.CreateParams.Opts"
      via: "normalized DocOpts map threaded into Create"
      pattern: "Opts:"
    - from: "internal/e2e/e2e_test.go"
      to: "/GTS_PDFA marker in downloaded PDF"
      via: "assertDownloadIsPDFA"
      pattern: "GTS_PDFA"
---

<objective>
Complete the opts chain at the API boundary: parse the optional `opts` multipart field, run the two-step validation (syntax via `convert.ParseDocOpts`, then applicability via `convert.ValidateApplicability` once engine/source/target are known), fail closed with 422 before any storage write, normalize and persist the accepted opts, and echo them back in the create and get responses (D-02/D-03/D-04/D-08/D-09). Then prove the whole phase live against a fresh docker-compose stack — real PDF/A-2b export with an OutputIntent marker, real 422s, and a no-opts regression pass (the project's "live e2e verified" acceptance bar, OPTS-01/OPTS-02).

Purpose: This is the client-facing surface and the acceptance gate. It turns the validated-opts core (Plans 01-02) into an end-to-end feature and confirms it on the real LibreOffice 7.4 engine, including confirming the actual OutputIntent marker string emitted by the deployed engine.

Output: `internal/api/handlers.go`, `internal/api/handlers_test.go`, `internal/e2e/e2e_test.go` modified; a captured live-run matrix; a human sign-off.
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-validated-conversion-options-pdf-a-export/14-CONTEXT.md
@.planning/phases/14-validated-conversion-options-pdf-a-export/14-PATTERNS.md
@.planning/phases/13-cross-format-conversion-input-safety/13-03-PLAN.md

<interfaces>
<!-- Contracts consumed from Plans 01-02 (already implemented). -->

internal/convert/opts.go (Plan 02):
    func ParseDocOpts(raw []byte) (DocOpts, error)
    func ValidateApplicability(engine, source, target string, o DocOpts) error
    type DocOpts struct { PDFProfile string `json:"pdf_profile,omitempty"` }

internal/jobs (Plan 01):
    CreateParams.Opts map[string]any   // normalized, persisted to jobs.options
    Job.Opts map[string]any            // read back by Get

Existing handleCreateJob anchor points (internal/api/handlers.go):
    const block lines 23-28 (add formFieldOpts, maxOptsBytes here)
    engine, ok := convert.Default.EngineFor(detected, target)   // lines 193-198 — opts validation goes AFTER this
    s.storage.Upload(...)                                        // line 243 — all validation must precede this
    jobs.CreateParams{...}                                       // lines 250-266 — add Opts
    writeJSON(w, http.StatusAccepted, map[string]any{...})       // lines 294-297 — echo opts (201)
    handleGetJob resp map lines 331-334                          // echo opts (GET)

Live E2E harness (internal/e2e/e2e_test.go):
    buildJobRequest / postJob / postJobExpectStatus              // lines 120-205 (add opts param, WriteField "opts")
    assertDownloadIsPDF lines 354-372                            // model assertDownloadIsPDFA on this
    e2eSetup gates on E2E_BASE_URL (self-skips offline)          // lines 62-88
    sample.docx fixture available in internal/e2e/testdata/
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parse, validate, persist, and echo opts in the API layer</name>
  <read_first>
    - internal/api/handlers.go (const block lines 23-28; handleCreateJob whole body lines 70-298 — especially the EngineFor block 193-198, the callback_url optional-field block 226-235 as the copy template, the Upload at 243, CreateParams 250-266, the 201 response 294-297; handleGetJob resp map 331-334 and its conditional-key appends 336-358)
    - internal/api/handlers_test.go (existing handleCreateJob test setup: mock repo/storage/queue, multipart request construction — reuse these helpers)
    - internal/convert/opts.go (ParseDocOpts, ValidateApplicability — Plan 02)
    - .planning/phases/14-validated-conversion-options-pdf-a-export/14-CONTEXT.md (D-02/D-03/D-04/D-08/D-09 and Claude's-Discretion notes on opts size limit and empty-vs-absent semantics)
  </read_first>
  <behavior>
    - POST with no opts field (or `opts={}`) → job created exactly as today; response has no `opts` key; stored Opts is empty.
    - POST with `opts={"pdf_profile":"pdf/a-2b"}` on a docx→pdf request → 202/created; CreateParams.Opts carries the normalized `{"pdf_profile":"pdf/a-2b"}`; the create response echoes `opts`.
    - POST with `opts` containing an unknown key (e.g. `{"EncryptFile":true}`) → 422, nothing written to storage.
    - POST with `opts={"pdf_profile":"pdf/a-2b"}` on an image job (e.g. png→webp) → 422 (inapplicable), nothing written.
    - POST with `opts={"pdf_profile":"pdf/a-2b"}` on a docx→odt request → 422 (target not pdf), nothing written.
    - POST with an opts field larger than the size cap → 422, nothing written.
    - GET /v1/jobs/{id} for a job with stored opts echoes `opts`; a job without opts omits the key (omitempty).
  </behavior>
  <action>
    internal/api/handlers.go — const block: add `formFieldOpts = "opts"` and `maxOptsBytes = 4096` (a conservative cap for the JSON opts field; Claude's-Discretion size limit per CONTEXT — 4 KiB comfortably fits the closed schema while bounding the DoS surface).

    handleCreateJob — insert an opts-validation block immediately AFTER the `engine, ok := convert.Default.EngineFor(detected, target)` block (lines 193-198) and BEFORE `s.storage.Upload` (line 243), mirroring the callback_url optional-field discipline (lines 226-235): read `raw := r.FormValue(formFieldOpts)`. Treat both an empty string and the literal `{}` as "no opts" (D-09 empty semantics) → leave a nil normalized value, skip validation. Otherwise: (1) if `len(raw) > maxOptsBytes` → `writeError(w, http.StatusUnprocessableEntity, "opts field too large")` and return (log a content-validation rejection line matching the existing `log.Printf("content validation rejected: client_id=%s ...")` style). (2) syntax step — `docOpts, err := convert.ParseDocOpts([]byte(raw))`; on error → `writeError(w, http.StatusUnprocessableEntity, "invalid opts")` and return. (3) applicability step (D-04, engine/detected/target now known) — `convert.ValidateApplicability(engine, detected, target, docOpts)`; on error → `writeError(w, http.StatusUnprocessableEntity, "opts not applicable to this conversion")` and return. On success, build the normalized persisted value by round-tripping the validated struct: `json.Marshal(docOpts)` then `json.Unmarshal` into a `map[string]any` (D-08 — store the normalized struct, never the raw client bytes); keep it nil when no opts were supplied.

    handleCreateJob — CreateParams literal (lines 250-266): add `Opts: normalizedOpts,`.

    handleCreateJob — 201 response (lines 294-297): when `normalizedOpts` is non-empty, add `"opts": normalizedOpts` to the response map (D-09; omit the key entirely when empty).

    handleGetJob — resp map (lines 331-334): after building `resp`, when `len(job.Opts) > 0`, add `resp["opts"] = job.Opts` (D-09 omitempty; existing no-opts responses are byte-for-byte unchanged).

    Do not add validation logic to the API that duplicates internal/convert — the API only calls ParseDocOpts/ValidateApplicability (single validation authority, D-04/D-10).
  </action>
  <verify>
    <automated>go vet ./internal/api/... && go test ./internal/api/... -run 'CreateJob|Opts|GetJob' -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'formFieldOpts' internal/api/handlers.go` and `grep -n 'maxOptsBytes' internal/api/handlers.go` match.
    - Opts validation appears after the `EngineFor` block and before `s.storage.Upload` (source review — fail-closed-before-storage ordering).
    - `grep -n 'convert.ParseDocOpts' internal/api/handlers.go` and `grep -n 'convert.ValidateApplicability' internal/api/handlers.go` match.
    - `grep -n 'Opts:' internal/api/handlers.go` matches in the CreateParams literal.
    - New unit tests in handlers_test.go cover: accept valid opts (assert CreateParams.Opts captured), unknown-key → 422, inapplicable (image + pdf_profile) → 422, docx→odt + pdf_profile → 422, oversize → 422, and an API-level injection attempt (opts value embedding a second filter property) → 422. `go test ./internal/api/... -run 'CreateJob|Opts|GetJob' -count=1` passes.
    - A test asserts that a no-opts POST response and a no-opts GET response contain NO `opts` key (regression: existing responses unchanged).
  </acceptance_criteria>
  <done>The API parses and strict-validates opts before storage, persists the normalized form, echoes it in create/get responses, and 422s on every invalid/inapplicable/oversized case — all covered by unit tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend the E2E suite — PDF/A pair, negative opts, no-opts regression, opts-threaded helpers</name>
  <read_first>
    - internal/e2e/e2e_test.go (buildJobRequest/postJob/postJobExpectStatus lines 120-205; TestCrossFormatConversionE2E happy-path shape lines 388-420; TestOLECFBRejectionE2E 422 shape lines 461-486; assertDownloadIsPDF lines 354-372; e2eSetup gating lines 62-88)
    - .planning/phases/13-cross-format-conversion-input-safety/13-03-PLAN.md (the live-run env/command precedent this plan reuses)
    - internal/convert/opts.go (the pdf_profile shape being exercised)
  </read_first>
  <behavior>
    - `sample.docx` → pdf with `opts={"pdf_profile":"pdf/a-2b"}` polls to done and the downloaded bytes contain the `/GTS_PDFA` OutputIntent marker.
    - `sample.docx` → odt with `opts={"pdf_profile":"pdf/a-2b"}` → 422 (inapplicable target).
    - A POST with a malformed/unknown-key opts value → 422.
    - `sample.docx` → pdf with NO opts still polls to done and downloads a valid `%PDF-` file (no-opts regression).
    - Whole suite still compiles and self-skips cleanly with no E2E_BASE_URL set.
  </behavior>
  <action>
    internal/e2e/e2e_test.go — thread an `opts` parameter through the request helpers: add an `opts string` parameter to `buildJobRequest`, `postJob`, and `postJobExpectStatus`, and in the multipart body add `if opts != "" { w.WriteField("opts", opts) }` mirroring the existing `callback_url` WriteField block. Update all existing call sites to pass `""` for opts (keeping their current behavior). (If updating the shared signatures touches too many call sites cleanly, the alternative is sibling `...WithOpts` helpers — Claude's Discretion; prefer extending the existing signatures for consistency.)

    Add `assertDownloadIsPDFA(t, downloadURL)` modeled on `assertDownloadIsPDF` (lines 354-372): download the bytes, assert the `%PDF-` prefix AND that the body contains the `/GTS_PDFA` OutputIntent marker substring.

    Add `TestPDFAExportE2E`: `sample.docx` → pdf with `opts={"pdf_profile":"pdf/a-2b"}`, `pollUntilDone`, then `assertDownloadIsPDFA`. Also assert the create/get response echoes the `opts` field.

    Add `TestOptsRejectionE2E` (table-driven, modeled on TestOLECFBRejectionE2E): (a) `sample.docx` → odt with pdf_profile opts → 422; (b) a malformed/unknown-key opts value on `sample.docx` → pdf → 422. Assert `http.StatusUnprocessableEntity` for each.

    Add a no-opts regression assertion: `sample.docx` → pdf with `opts=""` polls to done and `assertDownloadIsPDF` passes (existing path unaffected). This may be a subtest of TestPDFAExportE2E or a small dedicated test.

    All new tests use `e2eSetup(t)` first so they self-skip without E2E_BASE_URL and the package compiles offline.
  </action>
  <verify>
    <automated>go vet ./internal/e2e/... && go test ./internal/e2e/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/e2e/... -count=1` compiles and self-skips cleanly with no E2E_BASE_URL set (no failure).
    - `grep -n 'func assertDownloadIsPDFA' internal/e2e/e2e_test.go` matches and its body checks for `GTS_PDFA`.
    - `grep -n 'func TestPDFAExportE2E' internal/e2e/e2e_test.go` and `grep -n 'func TestOptsRejectionE2E' internal/e2e/e2e_test.go` match.
    - `buildJobRequest`/`postJob`/`postJobExpectStatus` accept an opts argument and write an `opts` multipart field when non-empty (grep for `WriteField("opts"`).
    - A no-opts document→pdf regression assertion exists.
  </acceptance_criteria>
  <done>The E2E suite exercises the PDF/A happy path (OutputIntent marker), negative opts (422), and the no-opts regression, with opts-threaded helpers; it compiles and self-skips offline.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live acceptance — fresh docker-compose stack, real PDF/A export + 422s + no-opts regression</name>
  <read_first>
    - .planning/phases/13-cross-format-conversion-input-safety/13-03-PLAN.md (the identical live-run env/command sequence and health-gating this reuses)
    - docker-compose.yml and docker-compose.e2e.yml (services: postgres, redis, minio, api, worker, document-worker; the e2e override's api extra_hosts / SSRF relaxation from Phase 12)
    - internal/convert/libreoffice.go (the `/GTS_PDFA` family match — confirm against the real LO 7.4 output here; if the deployed engine emits a more specific `/GTS_PDFA2` and the family match still holds, no change; if the family match is wrong, correct it and re-run)
  </read_first>
  <what-built>
    A validated-opts pipeline end to end: the API parses/validates `opts`, persists the normalized form, the document worker threads it into LibreOffice 7.4, builds the PDF/A-2b FilterOptions from server constants, and validates the produced PDF carries the OutputIntent marker before marking the job done. The E2E suite (Task 2) exercises the PDF/A happy path, negative opts 422s, and the no-opts regression.
  </what-built>
  <how-to-verify>
    1. Build and start a fresh stack:
       `docker compose -f docker-compose.yml -f docker-compose.e2e.yml up -d --build`
       Wait for health: `curl -fsS http://localhost:8090/healthz` returns 200 with postgres/redis/s3 "ok". Confirm api, worker, and document-worker are up (`docker compose -f docker-compose.yml -f docker-compose.e2e.yml ps`).
    2. Run the full E2E suite live (must NOT skip):
       `E2E_BASE_URL=http://localhost:8090 DATABASE_URL=postgres://octo:octo-pass@localhost:5434/octo_db API_KEY_SALT=dev-only-change-me-in-real-deploys E2E_S3_DIAL_ADDR=127.0.0.1:9100 E2E_WEBHOOK_HOST=host.docker.internal WEBHOOK_SIGNING_SECRET=dev-only-change-me-in-real-deploys go test ./internal/e2e/ -run 'E2E' -count=1 -v -timeout 30m`
    3. Confirm `TestPDFAExportE2E` passes: the docx→pdf-with-pdf_profile job reaches done and the downloaded PDF contains the `/GTS_PDFA` OutputIntent marker. If the marker string differs on the real LO 7.4 engine (e.g. `/GTS_PDFA2` only), update the match in internal/convert/libreoffice.go and assertDownloadIsPDFA to the confirmed string, rebuild, and re-run. Record the exact marker observed.
    4. Confirm `TestOptsRejectionE2E` passes: docx→odt-with-pdf_profile and malformed-opts both return 422.
    5. Confirm the no-opts document→pdf regression still succeeds.
    6. Optionally spot-check that the raw stored value in `jobs.options` for the PDF/A job is the normalized `{"pdf_profile": "pdf/a-2b"}`, not any raw client bytes.
    7. Record the pass/fail matrix (which tests passed, the observed OutputIntent marker, any filter/marker correction made). Tear down when satisfied: `docker compose -f docker-compose.yml -f docker-compose.e2e.yml down -v`.
  </how-to-verify>
  <resume-signal>Type "approved" once the live PDF/A export (with OutputIntent marker), the opts 422s, and the no-opts regression are all confirmed — or describe the failing case.</resume-signal>
  <files>docker-compose.yml, docker-compose.e2e.yml, internal/convert/libreoffice.go, internal/e2e/e2e_test.go</files>
  <action>Bring up a fresh stack (`docker compose -f docker-compose.yml -f docker-compose.e2e.yml up -d --build`), run the live E2E suite with the env from how-to-verify, confirm the PDF/A export carries the OutputIntent marker (correcting the `/GTS_PDFA` match in libreoffice.go/assertDownloadIsPDFA if the real LO 7.4 marker string differs), confirm the opts 422s and the no-opts regression, capture the result matrix into the SUMMARY, then pause for human sign-off.</action>
  <verify>
    <human-check>Fresh docker-compose stack: TestPDFAExportE2E passes with a real `/GTS_PDFA` OutputIntent marker in the downloaded PDF, TestOptsRejectionE2E returns 422 for inapplicable + malformed opts, and the no-opts document->pdf regression still succeeds. Human confirms and the result matrix (with the observed marker string) is recorded in the SUMMARY.</human-check>
  </verify>
  <done>A human has confirmed, against a freshly built docker-compose stack, a real PDF/A-2b export carrying the OutputIntent marker, real opts 422s, and an unchanged no-opts conversion; the live-run matrix and observed marker are recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (opts field) | Untrusted `opts` JSON crosses the multipart boundary; this is where client bytes are first validated and normalized before persistence. |
| operator → ephemeral docker-compose stack | Acceptance-only stack; the e2e override relaxes the SSRF guard ONLY in docker-compose.e2e.yml (Phase 12), never base compose. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering (filter-property injection at ingress) | handleCreateJob opts parse | mitigate | `convert.ParseDocOpts` (DisallowUnknownFields + single-value allow-list) rejects unknown keys/values with 422 before storage; only the normalized struct is persisted (D-08). The API-level injection-attempt unit test + live 422 confirm no raw bytes propagate. |
| T-14-02 | Denial of Service (oversized opts field) | handleCreateJob opts size cap | mitigate | `maxOptsBytes` (4 KiB) rejects an oversized opts field with 422 before parsing/marshaling; the outer request body is already bounded by `maxUploadByte`. |
| T-14-03 | Integrity (false PDF/A claim) | live acceptance OutputIntent check | mitigate | The live run proves a real PDF/A-2b output carries `/GTS_PDFA` and confirms the exact marker string emitted by the deployed LibreOffice 7.4 (non-authoritative sanity check; full veraPDF is accepted residual risk DOCV3-01). |
| T-14-04 | Information Disclosure (opts echo) | handleGetJob / create response | accept | Only the server-normalized opts (a closed schema, no secrets) are echoed; cross-client access is already guarded by the existing not-found ownership check. |
| T-14-SC | Tampering | Go module dependencies | accept | No new packages: stdlib `encoding/json` and the existing test/http imports only. No install occurs; the Package Legitimacy Gate does not apply. |
</threat_model>

<verification>
- `go build ./...` compiles; `go vet ./internal/api/... ./internal/e2e/...` clean.
- `go test ./internal/api/... -count=1` passes (opts accept/reject/echo unit tests).
- `go test ./internal/e2e/... -count=1` compiles and self-skips offline.
- Live: the E2E suite passes against a fresh docker-compose stack — real PDF/A-2b export with OutputIntent marker, real opts 422s, no-opts regression (human-confirmed).
- No new dependency in go.mod / go.sum.
</verification>

<success_criteria>
- `POST /v1/jobs` accepts an `opts` field validated against the closed allow-list; unrecognized/invalid/inapplicable/oversized opts → 422 before any S3 write (OPTS-01, success criterion 1).
- No client-supplied bytes reach the engine argv/filter-JSON (proven by Plan 02's builder test + this plan's API-level injection test + live run).
- A client can request PDF/A-2b for document→pdf via opts; the produced PDF is live-verified to carry the PDF/A OutputIntent marker (OPTS-02, success criterion 2).
- Existing document→pdf jobs without opts convert with no regression — live e2e verified (success criterion 3).
- Accepted opts are normalized, persisted, and echoed in create/get responses (D-08/D-09).
</success_criteria>

<output>
Create `.planning/phases/14-validated-conversion-options-pdf-a-export/14-03-SUMMARY.md` when done.
</output>
