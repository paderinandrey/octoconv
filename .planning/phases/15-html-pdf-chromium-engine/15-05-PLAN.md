---
phase: 15-html-pdf-chromium-engine
plan: 05
type: execute
wave: 5
depends_on: ["15-01", "15-02", "15-03", "15-04"]
files_modified:
  - internal/e2e/e2e_test.go
  - internal/e2e/testdata/sample.html
  - internal/e2e/testdata/canary.html
  - internal/e2e/testdata/nothtml.html
autonomous: false
requirements: [HTML-01, HTML-02, HTML-03]
user_setup: []

must_haves:
  truths:
    - "Uploading an HTML file to POST /v1/jobs routes to the html worker/container and converts to PDF end-to-end (upload→convert→download), live-verified against a freshly built stack (success criterion 1, HTML-01)"
    - "HTML referencing external/loopback/file:// resources produces ZERO connections on a live canary listener AND the job still completes within the timeout — network-block PROVEN by a live test, not code review (success criterion 2, HTML-02, D-04)"
    - "page_size / margin / print_background set via opts are reflected in the resulting PDF, live-verified (success criterion 3, HTML-03)"
    - "An HTML render that exceeds HTML_ENGINE_TIMEOUT is classified terminal (failed), and a non-HTML file uploaded as .html is rejected 422 (success criterion 4 timeout + D-07 input safety)"
  artifacts:
    - path: "internal/e2e/e2e_test.go"
      provides: "TestHTMLConversionE2E + TestHTMLNetworkBlockE2E + TestHTMLContentRejectionE2E + canary receiver helper"
      contains: "TestHTMLNetworkBlockE2E"
    - path: "internal/e2e/testdata/canary.html"
      provides: "fixture referencing external IP + loopback literal + file:// + compose hosts"
      contains: "169.254.169.254"
  key_links:
    - from: "TestHTMLNetworkBlockE2E"
      to: "startCanaryReceiver (host.docker.internal-addressed)"
      via: "zero-hits assertion + job completes"
      pattern: "startCanaryReceiver"
    - from: "TestHTMLConversionE2E"
      to: "assertDownloadIsPDF"
      via: "magic-bytes check on downloaded output"
      pattern: "assertDownloadIsPDF"
---

<objective>
Prove all four Phase 15 success criteria live, against a freshly built docker-compose stack: end-to-end html→pdf, network-block by canary (zero connections + job completes), print-opts round-trip, timeout-terminal, and non-HTML-rejection — then a human-verify checkpoint confirms the acceptance run.

Purpose: The phase's acceptance bar is "live e2e verified," and success criterion 2 explicitly demands network-block "proven by a live test, not asserted by code review alone." This plan builds the adversarial canary test (external IP + loopback literal + file:// + compose hostnames per RESEARCH Pattern 2/3) and the happy-path/opts/rejection tests, then gates on a human acceptance run mirroring Phase 14-03.

Output: The three E2E tests + canary receiver helper + fixtures, plus a recorded live acceptance run.
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-html-pdf-chromium-engine/15-CONTEXT.md
@.planning/phases/15-html-pdf-chromium-engine/15-RESEARCH.md
@.planning/phases/15-html-pdf-chromium-engine/15-PATTERNS.md

<interfaces>
<!-- Exact analogs. -->

From internal/e2e/e2e_test.go:
  e2eSetup (69), provisionClient (90), postJob/postJobExpectStatus/postJobFull, pollUntilDone (~250s)
  startWebhookReceiver (299-332) — net.Listen("tcp","0.0.0.0:0") + host.docker.internal URL + buffered chan;
    generalize into startCanaryReceiver accepting hits on arbitrary paths (/canary-img, /canary-fetch)
  TestDocumentConversionE2E (334-384) — upload→pollUntilDone→assertDownloadIsPDF; analog for happy path
  assertDownloadIsPDF (386-407) — magic-bytes check, REUSE verbatim (target always pdf)
  TestOLECFBRejectionE2E (529-550) — fixture-table + postJobExpectStatus(422) + loose substring; analog for rejection
  TestPDFAExportE2E — opts form-field usage analog (how a job sets the opts field)
  downloadClient() (391)

From docker-compose.e2e.yml: chromium-worker extra_hosts (Plan 04) — enables host.docker.internal canary reachability
D-04 canary fixture: <img src="http://{host}:{port}/canary-img"> + <script>fetch('.../canary-fetch')</script>
  + literal 169.254.169.254 + 127.0.0.1/localhost + redis/postgres refs + file:///etc/hostname; assert ZERO hits AND job done.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: E2E fixtures + happy-path + print-opts round-trip + non-HTML rejection tests</name>
  <read_first>
    - internal/e2e/e2e_test.go (e2eSetup, provisionClient, postJob*, pollUntilDone, TestDocumentConversionE2E, assertDownloadIsPDF, TestOLECFBRejectionE2E, TestPDFAExportE2E for the opts-form-field usage)
    - .planning/phases/15-html-pdf-chromium-engine/15-PATTERNS.md (e2e_test.go Analog A + Analog C sections)
    - .planning/phases/15-html-pdf-chromium-engine/15-CONTEXT.md (HTML-03 options; acceptance bar)
  </read_first>
  <action>
    Add testdata fixtures: sample.html (a valid `<!doctype html>` document with a heading, a paragraph, and a colored `<div style="background:red">` block so print_background is observable); nothtml.html (binary/non-HTML bytes — e.g. a PNG header or a NUL-containing blob — saved with a .html name).
    Add TestHTMLConversionE2E mirroring TestDocumentConversionE2E: upload sample.html with target=pdf, pollUntilDone (generalize the "LibreOffice cold start" comment to chromium), assertDownloadIsPDF. Add subtests that set the `opts` form field (mirror TestPDFAExportE2E's opts usage) for page_size a4 vs a5 and print_background true vs false, asserting the job completes and the downloaded PDF is valid — where feasible assert the page-geometry/background difference structurally (e.g. differing PDF byte size / MediaBox), otherwise assert both variants succeed and record the visual confirmation in the acceptance run (Task 3).
    Add TestHTMLContentRejectionE2E mirroring TestOLECFBRejectionE2E: post nothtml.html with target=pdf, expect 422 via postJobExpectStatus, loose substring assertion on the body (not exact-string, per the existing rejection-test rationale).
  </action>
  <verify>
    <automated>go vet ./internal/e2e/... && go test ./internal/e2e/ -run 'TestHTMLConversionE2E|TestHTMLContentRejectionE2E' -list '.*'</automated>
  </verify>
  <acceptance_criteria>
    - Fixtures sample.html and nothtml.html exist in internal/e2e/testdata.
    - TestHTMLConversionE2E compiles and includes page_size + print_background opts subtests using the `opts` form field.
    - TestHTMLContentRejectionE2E compiles and asserts 422 with a loose body substring.
    - `go vet ./internal/e2e/...` passes; tests are discoverable via `-list` (live execution happens in Task 3).
  </acceptance_criteria>
  <done>The happy-path, print-opts round-trip, and non-HTML-rejection E2E tests + fixtures exist, ready to run against the live stack.</done>
</task>

<task type="auto">
  <name>Task 2: Canary network-block test — startCanaryReceiver + TestHTMLNetworkBlockE2E</name>
  <read_first>
    - internal/e2e/e2e_test.go (startWebhookReceiver 299-332 — the exact net.Listen + host.docker.internal-URL + buffered-chan shape to generalize)
    - docker-compose.e2e.yml (chromium-worker extra_hosts from Plan 04)
    - .planning/phases/15-html-pdf-chromium-engine/15-CONTEXT.md (D-04 canary mechanics)
    - .planning/phases/15-html-pdf-chromium-engine/15-RESEARCH.md (Pattern 2 loopback literal + Pattern 3 file://; Pitfall B — must include loopback + file:// targets, not just external)
    - .planning/phases/15-html-pdf-chromium-engine/15-PATTERNS.md (e2e_test.go Analog B — canary receiver)
  </read_first>
  <action>
    Add `startCanaryReceiver(t *testing.T, host string) (baseURL string, hits <-chan canaryHit)` by generalizing startWebhookReceiver: same `net.Listen("tcp","0.0.0.0:0")` + httptest server bound to that listener + host.docker.internal-addressed base URL + buffered channel, but the handler records a canaryHit for ANY path (so /canary-img and /canary-fetch both register).
    Add testdata canary.html (templated so the canary base URL is injected at test time; the IP/loopback/host/file refs are literal): a valid `<!doctype html>` referencing the canary base URL via `<img src=".../canary-img">` AND `<script>fetch('.../canary-fetch')</script>` (inert since JS is off — kept as defense-in-depth per D-05), PLUS additional `<img>` refs to `http://169.254.169.254/` (cloud metadata), loopback literals `http://127.0.0.1:9/` and `http://localhost:9/` (Pattern 2 — exercises --proxy-bypass-list=<-loopback>), `http://redis:6379/` and `http://postgres:5432/` (compose hosts), and a `file:///etc/hostname` ref (Pattern 3 exfil attempt).
    Add TestHTMLNetworkBlockE2E: start the canary receiver on host.docker.internal, render the templated canary.html to pdf (target=pdf), pollUntilDone, then assert (a) the job reached `done` within the engine timeout (NOT stuck/failed-by-hang) — positive proof the render completed, and (b) ZERO canaryHit received on the channel over the render window (drain with a short timeout after done). This is the success-criterion-2 live proof: direct evidence no fetch occurred, not "the render didn't crash." If the file:/// ref content leaks into the PDF, that is the Plan 04 file:// residual risk — assert per the Plan 04 recorded decision (block-and-fail vs documented-residual).
  </action>
  <verify>
    <automated>go vet ./internal/e2e/... && grep -q 'startCanaryReceiver' internal/e2e/e2e_test.go && grep -q '169.254.169.254' internal/e2e/testdata/canary.html && grep -Eq '127.0.0.1|localhost' internal/e2e/testdata/canary.html && grep -q 'file:///etc/hostname' internal/e2e/testdata/canary.html && go test ./internal/e2e/ -run TestHTMLNetworkBlockE2E -list '.*'</automated>
  </verify>
  <acceptance_criteria>
    - startCanaryReceiver generalizes startWebhookReceiver to record hits on any path; TestHTMLNetworkBlockE2E compiles.
    - canary.html references all four target classes: external IP (169.254.169.254), loopback literal (127.0.0.1 + localhost), compose hosts (redis/postgres), and file:///etc/hostname — grep-confirmed.
    - The test asserts BOTH zero canary hits AND job==done (positive completion, not hang) — the success-criterion-2 direct-proof discipline.
    - `go vet ./internal/e2e/...` passes.
  </acceptance_criteria>
  <done>A live adversarial canary test proves the render is network-blocked against external, loopback, compose-host, and file:// targets while still completing successfully.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live acceptance run against a freshly built stack (all 4 success criteria)</name>
  <read_first>
    - .planning/phases/15-html-pdf-chromium-engine/15-CONTEXT.md (acceptance bar: fresh stack, real conversion, real canary, real 422)
    - internal/e2e/e2e_test.go (the four new tests)
    - .planning/phases/15-html-pdf-chromium-engine/15-04-SUMMARY.md (the file:// residual-risk + tini decisions to reference)
  </read_first>
  <what-built>
    The full html engine (Plans 01-04) plus the four E2E tests (happy path, print-opts round-trip, network-block canary, non-HTML rejection). This checkpoint runs the acceptance suite against a freshly built docker-compose stack and confirms all four Phase 15 success criteria live.
  </what-built>
  <action>
    Bring up a freshly built stack (`docker compose -f docker-compose.yml -f docker-compose.e2e.yml up --build` including the chromium-worker service). Run the E2E suite: TestHTMLConversionE2E (SC1 end-to-end + SC3 print-opts round-trip), TestHTMLNetworkBlockE2E (SC2 zero canary hits + job done), TestHTMLContentRejectionE2E (D-07 422), and a timeout-terminal check (SC4 — a fixture/short HTML_ENGINE_TIMEOUT forcing a render timeout classified terminal/failed, mirroring the document timeout-terminal acceptance). Confirm the html job's engine column is 'html' (proves the Plan 01 migration applied and routed to the distinct worker). Record pass/fail per success criterion in the SUMMARY; if the file:// ref leaked (Plan 04 finding), confirm it matches the recorded residual-risk decision.
  </action>
  <how-to-verify>
    1. Confirm the stack builds and the chromium-worker container is healthy alongside api/worker/document-worker.
    2. SC1: TestHTMLConversionE2E passes — an uploaded HTML becomes a downloadable valid PDF; the job row's engine=='html'.
    3. SC2: TestHTMLNetworkBlockE2E passes — zero canary connections AND the job completed (not hung).
    4. SC3: the page_size/print_background opts subtests pass and the PDFs differ as expected (visually confirm one background-on vs background-off PDF if structural assertion is inconclusive).
    5. SC4: the render-timeout case is classified terminal (job failed, no retry storm); TestHTMLContentRejectionE2E returns 422 for non-HTML-as-.html.
    6. Confirm the SUMMARY records all four criteria as verified against the fresh stack.
  </how-to-verify>
  <verify>
    <automated>go vet ./internal/e2e/...</automated>
  </verify>
  <acceptance_criteria>
    - All four success criteria verified live against a freshly built stack and recorded in the SUMMARY.
    - The html job's engine column is 'html' (migration + distinct-worker routing proven end-to-end).
    - Network-block is proven by the canary (zero hits + job done), not by code review.
    - Any file:// leak matches the Plan 04 recorded residual-risk decision (no undocumented behavior).
  </acceptance_criteria>
  <resume-signal>Type "approved" once all four success criteria pass against the fresh stack, or describe the failing criterion.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| attacker HTML → chromium renderer | The canary fixture is a deliberate adversarial input (external/loopback/compose-host/file:// refs); this plan proves the containment holds live. |
| chromium-worker container → canary listener | The canary listener is reachable from the container (via host.docker.internal); a fetch would register — zero hits is the positive proof of the network block. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05 | Information Disclosure | SSRF via rendered refs | mitigate | Live canary proves zero connections for external IP, loopback literal, compose hosts, and file:// — direct evidence, success criterion 2. |
| T-15-15 | Denial of Service | render hang beyond timeout | mitigate | SC4 acceptance confirms HTML_ENGINE_TIMEOUT expiry is terminal (isHTMLTerminal), no retry storm. |
| T-15-09 | Spoofing | binary-as-.html | mitigate | TestHTMLContentRejectionE2E confirms 422 pre-storage for non-HTML content. |
| T-15-07 | Information Disclosure | file:// residual read | accept/verify | Canary includes a file:///etc/hostname ref; outcome matches the Plan 04 recorded residual-risk decision (block-and-fail or documented-residual). |
</threat_model>

<verification>
- `go vet ./internal/e2e/...` passes; the four tests compile and are discoverable.
- Live acceptance run against a freshly built stack confirms SC1-SC4, recorded in the SUMMARY.
- Network-block is proven by the canary listener (zero hits + job done), satisfying success criterion 2's "live test, not code review" requirement.
</verification>

<success_criteria>
- HTML-01: html upload routes to the distinct html worker/container and converts end-to-end (engine=='html' proven).
- HTML-02: network-block proven live by the canary (external + loopback + compose-host + file:// targets, zero connections, job completes).
- HTML-03: page size / margins / printBackground reflected in the output PDF, live-verified.
- Timeout: HTML render bounded by HTML_ENGINE_TIMEOUT, classified terminal on expiry.
</success_criteria>

<output>
Create `.planning/phases/15-html-pdf-chromium-engine/15-05-SUMMARY.md` when done, recording the live acceptance outcome per success criterion.
</output>
