---
phase: 16-webhook-delivery-decoupling
plan: 03
type: execute
wave: 3
depends_on: [16-02]
files_modified:
  - docker-compose.yml
  - docker-compose.e2e.yml
  - .env.example
autonomous: true
requirements: [WEBH-01]
must_haves:
  truths:
    - "Two named webhook-worker services (webhook-worker-1, webhook-worker-2) run from Dockerfile.webhook-worker, giving real ≥2 redundancy, not deploy.replicas (D-05, PITFALLS Pitfall 13)"
    - "The image worker service no longer carries webhook/sweeper env (WEBHOOK_SIGNING_SECRET/WEBHOOK_PRESIGN_TTL/RECONCILER_* removed) — webhook role fully off image-worker (D-03/D-04)"
    - "Each webhook-worker service depends_on postgres+redis+minio (storage required by PresignGet, D-07 corrected) and carries S3_* + WEBHOOK_SIGNING_SECRET + RECONCILER_* env"
    - "docker-compose.e2e.yml wires host.docker.internal for the webhook-worker services so the live E2E receiver is reachable from the delivering process (WEBHOOK_ALLOW_PRIVATE_IPS stays an API-side var)"
    - ".env.example documents WEBHOOK_WORKER_CONCURRENCY and marks signing-secret + RECONCILER_* as webhook-worker-only"
  artifacts:
    - path: "docker-compose.yml"
      provides: "webhook-worker-1 and webhook-worker-2 services; image worker stripped of webhook env"
      contains: "webhook-worker-2"
    - path: "docker-compose.e2e.yml"
      provides: "extra_hosts host-gateway override for both webhook-worker services"
      contains: "webhook-worker-1"
    - path: ".env.example"
      provides: "WEBHOOK_WORKER_CONCURRENCY + webhook-worker section"
      contains: "WEBHOOK_WORKER_CONCURRENCY"
  key_links:
    - from: "docker-compose.yml webhook-worker-1/-2"
      to: "Dockerfile.webhook-worker"
      via: "build.dockerfile"
      pattern: "Dockerfile.webhook-worker"
    - from: "docker-compose.e2e.yml webhook-worker-1/-2"
      to: "host.docker.internal"
      via: "extra_hosts host-gateway"
      pattern: "host.docker.internal"
---

<objective>
Wire the deployment topology: add two named webhook-worker services (D-05 real redundancy) to docker-compose, strip the webhook/sweeper env from the image worker (D-03/D-04), give the webhook-workers their host-gateway override in the E2E compose, and document the new env in .env.example. After this plan the stack is runnable for the Plan 16-04 live acceptance.

Purpose: Provide the ≥2-consumer redundant deployment shape and the E2E-reachability wiring the live acceptance (SC1/SC2/SC3) needs.
Output: two webhook-worker services, cleaned image-worker env, E2E overrides, env docs.
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-webhook-delivery-decoupling/16-CONTEXT.md
@.planning/phases/16-webhook-delivery-decoupling/16-PATTERNS.md
@.planning/phases/16-webhook-delivery-decoupling/16-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add webhook-worker-1/-2 services; strip webhook env from image worker</name>
  <files>docker-compose.yml</files>
  <read_first>
    - docker-compose.yml (the worker: block ~:111-153 and document-worker: block ~:155-199 — document-worker is the closest analog for a no-webhook-env, but webhook-worker needs the webhook+reconciler env back)
    - .planning/phases/16-webhook-delivery-decoupling/16-PATTERNS.md ("docker-compose.yml — MODIFY" section — the exact two-service block and the worker/document-worker/chromium env edits)
    - Dockerfile.webhook-worker (from Plan 16-02 — the image these services build)
  </read_first>
  <action>
    In docker-compose.yml add two services, webhook-worker-1 and webhook-worker-2, both built from Dockerfile.webhook-worker (build.context ".", build.dockerfile "Dockerfile.webhook-worker"), each restart: always, each with distinct container_name (octoconv-webhook-worker-1 / -2) — everything else identical (symmetric replicas, not primary/secondary). depends_on: postgres, redis, AND minio all condition: service_healthy (minio required because HandleWebhookDeliver PresignGets download URLs, D-07 corrected). Environment for each: DATABASE_URL, REDIS_ADDR, the full S3_* block (S3_ENDPOINT minio:9000, S3_ACCESS_KEY minioadmin, S3_SECRET_KEY minioadmin, S3_BUCKET octoconv, S3_USE_SSL "false"), WEBHOOK_WORKER_CONCURRENCY "4", WEBHOOK_SIGNING_SECRET "dev-only-change-me-in-real-deploys", WEBHOOK_PRESIGN_TTL "6h", the four RECONCILER_* vars (QUEUED_STALE_AFTER 90s, ACTIVE_STALE_AFTER 5m, SWEEP_INTERVAL 1m, MAX_RECOVERIES 3), and METRICS_ADDR "127.0.0.1:9090". Add a comment on the pair noting the two named services (not deploy.replicas, which docker-compose without swarm ignores) provide the real ≥2 redundancy required by D-05/SC2.

    In the existing worker: block, REMOVE the webhook/sweeper env now that cmd/worker (image) no longer runs those roles (D-03/D-04): delete WEBHOOK_SIGNING_SECRET, WEBHOOK_PRESIGN_TTL, and all four RECONCILER_* lines. KEEP the S3_* block (image conversion still needs storage), WORKER_CONCURRENCY, ENGINE_TIMEOUT, IMAGE_MAX_RETRY, the DEBT-05 cross-engine retry/timeout vars, and METRICS_ADDR.

    In the document-worker: and chromium-worker: blocks, remove WEBHOOK_SIGNING_SECRET and WEBHOOK_PRESIGN_TTL and the DEBT-05 "inert here / revisited by the v1.3 webhook-decoupling phase" comment paragraph — this phase IS that revisit; those vars are no longer read for any webhook purpose in those binaries. Do not touch their other env.
  </action>
  <verify>
    <automated>cd /Users/apaderin/dev/octoconv && docker compose -f docker-compose.yml config >/dev/null && grep -q "webhook-worker-1:" docker-compose.yml && grep -q "webhook-worker-2:" docker-compose.yml && grep -c "Dockerfile.webhook-worker" docker-compose.yml | grep -q 2 && awk '/^  worker:/{f=1} /^  [a-z]/{if(f&&$0!~/^  worker:/)f=0} f&&/RECONCILER_/{print "LEAK"}' docker-compose.yml | grep -q LEAK && echo WORKER_HAS_RECON || echo WORKER_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose -f docker-compose.yml config` parses without error.
    - `grep 'webhook-worker-1:' docker-compose.yml` and `grep 'webhook-worker-2:' docker-compose.yml` both match; both reference Dockerfile.webhook-worker (grep count == 2).
    - Both webhook-worker services list minio under depends_on (grep in each block).
    - Image worker cleaned: within the `worker:` service block there are NO `RECONCILER_`, `WEBHOOK_SIGNING_SECRET`, or `WEBHOOK_PRESIGN_TTL` lines (the verify awk prints WORKER_CLEAN).
    - document-worker and chromium-worker blocks no longer contain WEBHOOK_SIGNING_SECRET / WEBHOOK_PRESIGN_TTL.
  </acceptance_criteria>
  <done>Two symmetric webhook-worker services exist with storage+webhook+reconciler env; image/document/chromium workers no longer carry webhook signing/presign env; compose config is valid.</done>
</task>

<task type="auto">
  <name>Task 2: E2E compose override + .env.example webhook-worker section</name>
  <files>docker-compose.e2e.yml, .env.example</files>
  <read_first>
    - docker-compose.e2e.yml (full — the header comment claiming "worker delivers webhooks" is now false; the worker:/document-worker:/chromium-worker: extra_hosts overrides)
    - .env.example (the "# Worker" and "# Reconciler" sections)
    - .planning/phases/16-webhook-delivery-decoupling/16-PATTERNS.md ("docker-compose.e2e.yml — MODIFY" and ".env.example — MODIFY" sections)
  </read_first>
  <action>
    In docker-compose.e2e.yml: add webhook-worker-1 and webhook-worker-2 override blocks, each with `extra_hosts: ["host.docker.internal:host-gateway"]`, because these are now the processes that actually dial the E2E test's host-bound webhook receiver. Remove the `worker:` and `document-worker:` extra_hosts override blocks (neither delivers webhooks anymore). KEEP the `api:` override unchanged (validateCallbackURL still runs in the api process and needs host-gateway DNS) and KEEP the `chromium-worker:` override unchanged (its host-gateway alias is for the Plan 15 network-block canary, unrelated to webhooks — do not remove). Update the file's header comment and the stale inline comment that says "worker delivers webhooks (HandleWebhookDeliver)" to state that webhook delivery now happens in the webhook-worker services. Note that WEBHOOK_ALLOW_PRIVATE_IPS/WEBHOOK_ALLOW_INSECURE_HTTP remain ONLY on the api service (creation-time SSRF guard) — do NOT add them to the webhook-worker services.

    In .env.example: add `WEBHOOK_WORKER_CONCURRENCY=4   # asynq concurrency for cmd/webhook-worker; mirrors WORKER_CONCURRENCY's default` under a new `# Webhook worker` section. Relocate WEBHOOK_SIGNING_SECRET and WEBHOOK_PRESIGN_TTL from the `# Worker` section into this new section (they are now consumed by cmd/webhook-worker, not the image worker), and add a note on the `# Reconciler` section that RECONCILER_* are now consumed only by cmd/webhook-worker (D-04, sweeper relocated). Leave WEBHOOK_ALLOW_INSECURE_HTTP / WEBHOOK_ALLOW_PRIVATE_IPS where they are but annotate them as API-side (enforced in the api process's validateCallbackURL, not the webhook-worker). Keep all existing values/comments otherwise intact.
  </action>
  <verify>
    <automated>cd /Users/apaderin/dev/octoconv && docker compose -f docker-compose.yml -f docker-compose.e2e.yml config >/dev/null && grep -q "webhook-worker-1:" docker-compose.e2e.yml && grep -q "webhook-worker-2:" docker-compose.e2e.yml && grep -q "WEBHOOK_WORKER_CONCURRENCY" .env.example && ! grep -q "worker delivers webhooks" docker-compose.e2e.yml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose -f docker-compose.yml -f docker-compose.e2e.yml config` parses without error (layered override valid).
    - docker-compose.e2e.yml has webhook-worker-1 and webhook-worker-2 override blocks, each with `host.docker.internal:host-gateway`; the stale "worker delivers webhooks" comment is gone (grep returns no match); api and chromium-worker overrides retained.
    - .env.example contains `WEBHOOK_WORKER_CONCURRENCY`; WEBHOOK_SIGNING_SECRET/WEBHOOK_PRESIGN_TTL appear under a webhook-worker section; RECONCILER_* annotated as webhook-worker-only.
  </acceptance_criteria>
  <done>E2E compose reaches the host receiver via the webhook-worker services; env docs describe the webhook-worker-only config surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator compose config → deployed fleet | env placement decides which binary signs/delivers webhooks and which holds the sweep lock |
| E2E test host ← webhook-worker containers | webhook-worker dials host.docker.internal (private) — only relaxed in the E2E override, never production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-08 | Elevation / Repudiation (SSRF relaxation leaks to prod) | docker-compose.e2e.yml | mitigate | WEBHOOK_ALLOW_PRIVATE_IPS/INSECURE_HTTP stay ONLY on the api service in the E2E override file (never the base file, never webhook-worker); the E2E header comment already warns it must never be used in production. Base docker-compose.yml keeps the guard engaged (vars unset → default deny). |
| T-16-09 | Denial of Service (single webhook consumer = same SPOF moved) | docker-compose.yml | mitigate | Two named services (not deploy.replicas, which compose ignores without swarm) guarantee ≥2 live consumers so killing one still drains the queue (D-05, addresses PITFALLS Pitfall 13). Verified live in Plan 16-04 SC2. |
| T-16-10 | Spoofing (webhook-worker cannot sign — missing secret in compose) | webhook-worker-1/-2 env | mitigate | Both services set WEBHOOK_SIGNING_SECRET; the binary fails closed at startup if it were missing (Plan 16-02 T-16-04), so a mis-wired service crashes visibly rather than delivering unsigned. |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs introduced; services build the existing Go binary; base images are Debian/golang official. No legitimacy audit required. |
</threat_model>

<verification>
- `docker compose -f docker-compose.yml config` valid; `-f docker-compose.yml -f docker-compose.e2e.yml config` valid.
- webhook-worker-1 and webhook-worker-2 present in both compose files; both build Dockerfile.webhook-worker; both depend on minio.
- Image worker service carries no webhook/reconciler env.
- .env.example documents WEBHOOK_WORKER_CONCURRENCY and the webhook-worker-only config surface.
</verification>

<success_criteria>
The deployment topology is the D-05 shape: two symmetric webhook-worker services with storage+webhook+reconciler env, image/document/chromium workers stripped of webhook roles, and E2E reachability wired for the webhook-workers. Ready for live acceptance in Plan 16-04.
</success_criteria>

<output>
Create `.planning/phases/16-webhook-delivery-decoupling/16-03-SUMMARY.md` when done.
</output>
