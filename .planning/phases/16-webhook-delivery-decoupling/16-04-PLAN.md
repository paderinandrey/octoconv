---
phase: 16-webhook-delivery-decoupling
plan: 04
type: execute
wave: 4
depends_on: [16-03]
files_modified: []
autonomous: false
requirements: [WEBH-01]
must_haves:
  truths:
    - "SC1 (D-03): with the image worker (octoconv-worker) stopped, a document- or html-engine job's completion webhook is still delivered by a webhook-worker — live verified"
    - "SC2 (D-05/D-06): killing one of the two webhook-workers mid-delivery loses zero and duplicates zero in-flight webhooks; the surviving worker drains the queue (relies on asynq at-least-once + webhook_deliveries/asynq.Unique idempotency, no new tracking)"
    - "SC3 (D-01/D-02): with both webhook-workers live, exactly one holds the reconciler advisory lock (pg_locks advisory count == 1); killing the holder auto-releases it and another worker acquires it within one sweep interval — no duplicate-sweep race, auto-failover"
    - "Baseline: the existing E2E suite passes against the new topology (webhook delivered by webhook-worker, not by any engine worker)"
  artifacts:
    - path: ".planning/phases/16-webhook-delivery-decoupling/16-04-SUMMARY.md"
      provides: "Captured live evidence for SC1/SC2/SC3 (commands run, webhook receipts, pg_locks counts, webhook_deliveries row counts)"
      contains: "SC1"
  key_links:
    - from: "docker stop octoconv-worker"
      to: "webhook still delivered"
      via: "webhook-worker consumes TypeWebhookDeliver independent of engine workers"
      pattern: "SC1"
    - from: "SELECT count(*) FROM pg_locks WHERE locktype='advisory'"
      to: "exactly one sweeper fleet-wide"
      via: "session-level advisory lock held by a single webhook-worker backend"
      pattern: "advisory"
---

<objective>
Live end-to-end acceptance of the decoupled webhook topology against a freshly built two-webhook-worker stack. Prove SC1 (image-worker-independent delivery), SC2 (kill-one-mid-delivery no loss/dup), and SC3 (exactly-one-sweeper + auto-failover). This is the phase's acceptance bar — "live e2e verified", the project's engine-phase standard (mirrors Phase 14/15 live-acceptance checkpoints).

Purpose: Confirm WEBH-01 is actually satisfied on live infrastructure, not by code review.
Output: captured evidence for the three success criteria and a human sign-off.
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-webhook-delivery-decoupling/16-CONTEXT.md
@.planning/phases/16-webhook-delivery-decoupling/16-02-SUMMARY.md
@.planning/phases/16-webhook-delivery-decoupling/16-03-SUMMARY.md

<interfaces>
E2E harness (internal/e2e/e2e_test.go header, ~:1-20) — env-gated live suite:
  E2E_BASE_URL       e.g. http://localhost:8090 (running API; unset → self-skip)
  E2E_WEBHOOK_HOST   host containers use to reach the test process (default host.docker.internal)
  E2E_S3_DIAL_ADDR   host:port to dial for the presigned download
Run stack: docker compose -f docker-compose.yml -f docker-compose.e2e.yml up -d --build
Container names (from compose container_name): octoconv-api, octoconv-worker (image engine),
  octoconv-document-worker, octoconv-chromium-worker, octoconv-webhook-worker-1, octoconv-webhook-worker-2,
  and the postgres/redis/minio services (derive exact postgres container name via `docker compose ps`).
Sweep evidence markers (internal/jobs/repo.go): job_events.detail->>'action' == 'reconciler_recovery' /
  'webhook_gap_recovered'. Advisory lock: the reconciler is the ONLY advisory-lock user in the codebase,
  so `SELECT count(*) FROM pg_locks WHERE locktype='advisory'` reflects sweeper leadership directly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the stack and verify SC1 (webhook survives image-worker stop)</name>
  <files>none — live acceptance drill, no repo files modified</files>
  <read_first>
    - internal/e2e/e2e_test.go (:1-90 — env gating, required env vars, host.docker.internal defaults)
    - docker-compose.yml + docker-compose.e2e.yml (the webhook-worker-1/-2 services from Plan 16-03)
    - .planning/phases/16-webhook-delivery-decoupling/16-CONTEXT.md (D-03/SC1 acceptance definition)
  </read_first>
  <action>
    Bring up the full stack fresh: `docker compose -f docker-compose.yml -f docker-compose.e2e.yml up -d --build`. Confirm all services (api, worker, document-worker, chromium-worker, webhook-worker-1, webhook-worker-2, postgres, redis, minio) reach healthy/running via `docker compose ps`.

    First establish baseline: run the existing E2E suite with the documented env (E2E_BASE_URL=http://localhost:8090, E2E_WEBHOOK_HOST + E2E_S3_DIAL_ADDR per the e2e_test.go header) — `go test ./internal/e2e/ -count=1 -v` — and confirm it passes with the new topology (webhooks now delivered by webhook-worker, not any engine worker). Capture the pass output.

    Then SC1: stop the image worker with `docker stop octoconv-worker`. Submit a DOCUMENT (or HTML) conversion job with a callback_url pointing at a host-reachable receiver (reuse the e2e harness's receiver mechanism, or a manual submission using the compose dev signing secret) and drive it to done. Confirm the completion webhook is delivered by one of the webhook-worker services WHILE the image worker is stopped — verify via (a) the receiver actually received the signed webhook, and (b) `docker logs octoconv-webhook-worker-1` / `-2` show the delivery, and (c) a `webhook_deliveries` row exists for that job (query Postgres via `docker exec <postgres> psql -U octo -d octo_db -c "SELECT job_id, delivered FROM webhook_deliveries WHERE job_id='<id>'"`). Capture all three evidence points. Restart the image worker afterward (`docker start octoconv-worker`) to restore the stack for Task 2.
  </action>
  <verify>
    <automated>cd /Users/apaderin/dev/octoconv && docker compose ps --format '{{.Name}} {{.State}}' | grep -q "octoconv-webhook-worker-1 running" && docker compose ps --format '{{.Name}} {{.State}}' | grep -q "octoconv-webhook-worker-2 running" && echo STACK_UP</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose ps` shows both webhook-worker-1 and webhook-worker-2 running.
    - The existing E2E suite passes against the new topology (test output captured; webhook delivery no longer depends on any engine worker).
    - With octoconv-worker stopped, a document/html job's completion webhook is delivered: receiver received it AND a webhook-worker log shows the delivery AND a webhook_deliveries row with delivered=true exists for the job.
    - Image worker restarted and running before Task 2.
  </acceptance_criteria>
  <done>SC1 live-verified: webhook delivery is independent of the image worker; baseline E2E green on the new topology. Evidence captured for the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Verify SC2 (kill-one-mid-delivery no loss/dup) and SC3 (exactly-one-sweeper + failover)</name>
  <files>none — live acceptance drill, no repo files modified</files>
  <read_first>
    - .planning/phases/16-webhook-delivery-decoupling/16-CONTEXT.md (D-05/D-06 for SC2; D-01/D-02 for SC3)
    - internal/reconciler/reconciler.go (advisory-lock behavior from Plan 16-01: session-level lock, auto-release on session close)
    - internal/worker/worker.go:484-560 (HandleWebhookDeliver — asynq retry semantics that give at-least-once)
  </read_first>
  <action>
    SC2 (no loss / no dup on mid-delivery kill): submit a job whose webhook will be delivered, and while a delivery is in flight kill exactly one webhook-worker (`docker kill octoconv-webhook-worker-1`). Confirm the surviving webhook-worker-2 completes delivery: the receiver ends up with the webhook, and — crucially — assert exactly ONE successful delivery, not a duplicate: query `SELECT count(*) FROM webhook_deliveries WHERE job_id='<id>' AND delivered=true` and confirm the receiver observed the payload exactly once (the at-least-once redelivery + asynq.Unique/webhook_deliveries idempotency of D-06 means at most one delivered row and one receiver hit). No webhook is lost (count ≥ 1 delivered) and none duplicated (receiver saw it once). Restart webhook-worker-1 (`docker start octoconv-webhook-worker-1`).

    SC3 (exactly one sweeper + auto-failover): with BOTH webhook-workers running, query the advisory-lock holder count: `docker exec <postgres> psql -U octo -d octo_db -tAc "SELECT count(*) FROM pg_locks WHERE locktype='advisory'"` and confirm it equals exactly 1 (the reconciler is the only advisory-lock user, so this proves exactly one fleet-wide sweeper). Also capture which backend pid holds it (`SELECT pid FROM pg_locks WHERE locktype='advisory'`). Then test auto-failover: identify and kill the webhook-worker holding the lock (correlate via container — if ambiguous, kill webhook-worker-1, then webhook-worker-2 if the count did not change, observing the lock move); after the killed worker's session closes, wait longer than RECONCILER_SWEEP_INTERVAL (1m; allow up to ~90s) and re-query — confirm the advisory-lock count is back to exactly 1, now held by a DIFFERENT backend pid (auto-failover to the surviving worker). Optionally corroborate active sweeping by inducing a webhook-gap (a done job whose delivery task never enqueued) and confirming a single `webhook_gap_recovered` job_events row appears (not two). Capture the before/after pg_locks counts and pids.

    Tear down or leave the stack per the executor's discretion after evidence capture; record exact commands and outputs for the checkpoint.
  </action>
  <verify>
    <automated>cd /Users/apaderin/dev/octoconv && PG=$(docker compose ps --format '{{.Name}}' | grep -i postgres | head -1) && docker exec "$PG" psql -U octo -d octo_db -tAc "SELECT count(*) FROM pg_locks WHERE locktype='advisory'" | tr -d '[:space:]' | grep -qx 1 && echo "SC3_EXACTLY_ONE_SWEEPER"</automated>
  </verify>
  <acceptance_criteria>
    - SC2: after killing one webhook-worker mid-delivery, the surviving worker delivers the webhook; `webhook_deliveries` shows exactly one delivered=true row for the job and the receiver observed the payload exactly once (no loss, no duplicate).
    - SC3 steady state: `SELECT count(*) FROM pg_locks WHERE locktype='advisory'` == 1 with both workers live (verify command prints SC3_EXACTLY_ONE_SWEEPER).
    - SC3 failover: after killing the lock holder and waiting > sweep interval, the advisory-lock count returns to 1 held by a different backend pid (before/after pids captured and differ).
    - All commands and outputs captured verbatim for the checkpoint/SUMMARY.
  </acceptance_criteria>
  <done>SC2 and SC3 live-verified: no loss/dup on mid-delivery kill; exactly one sweeper fleet-wide with working auto-failover. Evidence captured.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human sign-off on live SC1/SC2/SC3 evidence</name>
  <action>Present the captured live evidence from Tasks 1-2 (commands, receiver hits, webhook_deliveries rows, pg_locks counts/pids) and pause for human review; do not auto-approve. Resume only on explicit approval.</action>
  <what-built>A decoupled webhook-delivery topology: cmd/webhook-worker (×2 named compose services) is the sole webhook consumer and sole advisory-lock-gated sweeper; cmd/worker is image-only. Tasks 1-2 captured live evidence for all three success criteria.</what-built>
  <how-to-verify>
    Review the captured evidence (commands + outputs) from Tasks 1 and 2:
    1. SC1: with octoconv-worker stopped, a document/html job's completion webhook was still delivered by a webhook-worker (receiver hit + webhook-worker log + webhook_deliveries row). Confirm delivery genuinely happened while the image worker was down.
    2. SC2: killing one webhook-worker mid-delivery lost zero and duplicated zero webhooks — exactly one delivered=true row and one receiver hit; the survivor drained the queue.
    3. SC3: `SELECT count(*) FROM pg_locks WHERE locktype='advisory'` == 1 with both workers up; after killing the lock holder and waiting one sweep interval, the count returned to 1 held by a different backend pid (auto-failover). Optionally the single webhook_gap_recovered event.
    Spot-check that no signing secret appears in any captured log output.
  </how-to-verify>
  <resume-signal>Type "approved" if all three success criteria are convincingly demonstrated live, or describe which criterion's evidence is insufficient.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator ← live acceptance evidence | the human sign-off gates phase completion on genuine live behavior, not code review |
| webhook-worker fleet → Postgres advisory lock | the pg_locks count is the direct fleet-wide single-sweeper assertion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-11 | Repudiation (unverified acceptance claim) | acceptance checkpoint | mitigate | Blocking human-verify checkpoint; each SC requires captured live evidence (receiver hits, pg_locks counts + pids, webhook_deliveries rows), not assertions. |
| T-16-12 | Denial of Service (silent no-sweeper after failover) | advisory-lock auto-failover | mitigate | SC3 explicitly re-queries pg_locks after killing the holder and requires the count to return to exactly 1 (a DIFFERENT pid) — proves auto-failover, not a stuck/lost lock. |
| T-16-13 | Information Disclosure (secret in captured logs) | evidence capture | mitigate | Checkpoint spot-checks that no signing secret appears in any captured log/output before sign-off. |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs; the stack builds the existing Go binaries via the phase's Dockerfiles. No legitimacy audit required. |
</threat_model>

<verification>
- Full stack builds and runs (both webhook-worker services running).
- SC1: image-worker-stopped document/html webhook delivered (three evidence points).
- SC2: exactly one delivered row + one receiver hit after mid-delivery kill.
- SC3: pg_locks advisory count == 1 steady state; == 1 with different pid after failover.
- Human checkpoint approved.
</verification>

<success_criteria>
All three ROADMAP success criteria for Phase 16 are live-verified and human-approved: webhook delivery survives image-worker absence (SC1), survives a mid-delivery consumer kill without loss/dup (SC2), and runs exactly one fleet-wide sweeper with auto-failover (SC3). WEBH-01 satisfied.
</success_criteria>

<output>
Create `.planning/phases/16-webhook-delivery-decoupling/16-04-SUMMARY.md` when done, embedding the captured SC1/SC2/SC3 evidence.
</output>
