---
phase: 19-ci-pipeline
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - .github/workflows/ci.yml
autonomous: false
requirements: [CI-01, CI-02, CI-03, CI-04]

must_haves:
  truths:
    - "The workflow is committed and pushed to origin/main, triggering a live GitHub Actions run"
    - "The live run's 4 tiers are confirmed green — gate, race, docker-build, e2e — either via a scripted `gh run watch` hard gate (if gh is authenticated at execution time) or by operator confirmation at the Actions URL (if not)"
    - "A second identical docker-build run shows CACHED layers / reduced build time, proving the per-target gha cache actually hits (SC3's 'CACHED on second run')"
    - "The branch-protection operational follow-up is documented in the SUMMARY with the exact gh api / UI steps to make gate+race required checks on main"
  artifacts:
    - path: ".planning/phases/19-ci-pipeline/19-02-SUMMARY.md"
      provides: "Live-run evidence for CI-01..04 + branch-protection follow-up doc"
      contains: "branch protection"
  key_links:
    - from: "origin/main"
      to: "GitHub Actions"
      via: "git push triggers ci.yml on: push"
      pattern: "actions"
---

<objective>
Push the workflow authored in 19-01 to `origin/main` and prove all 4 tiers run GREEN on a real GitHub Actions run — the authoritative proof of CI-01..CI-04 that only a live run can give (SC1 red-on-violation determinism, SC2 -race green, SC3 CACHED-on-second-run, SC4 live E2E advisory/required + teardown/artifact). Then document the manual branch-protection follow-up that the workflow file alone cannot express.

Purpose: A workflow file that parses locally is not the same as a pipeline proven green on GitHub's runners with real disk/network/runner-sizing constraints. This plan closes that gap and records the one remaining operational step (branch protection).
Output: A green live Actions run (all 4 tiers), a proven cache hit on a second docker-build, and `19-02-SUMMARY.md` capturing run URLs/evidence + the branch-protection follow-up.
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/ARCHITECTURE.md
@.github/workflows/ci.yml
</context>

<constraints>
- `git push origin main` IS authorized by the operator for this plan.
- `gh` CLI is currently NOT authenticated (`gh auth status` fails) — but the operator was asked to `gh auth login`, so it MAY be authenticated by execution time. Detect at runtime; do not assume.
- Live Actions URL: https://github.com/paderinandrey/octoconv/actions
- THE gh-auth conditional in Task 1 is the ONLY conditional allowed in this entire phase. Everything else stays unconditional.
- Branch protection required-checks canNOT be configured without gh auth (needs the API) — it is a documented operator follow-up, never auto-applied here (ARCHITECTURE Anti-Pattern 4).
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Commit + push to main; observe the live run (scripted hard gate if gh auth, else prepare checkpoint)</name>
  <read_first>
    - .github/workflows/ci.yml — confirm it is staged/committed exactly as authored in 19-01 (no drift).
    - .planning/research/STACK.md — "Runner Sizing" (lines 187-193): what to watch for if the e2e tier is flaky/OOM (informs how to read a red run).
    - .planning/research/PITFALLS.md — Pitfall 3/4 verification note: a warm second run must show CACHED layers (SC3).
    - .planning/research/ARCHITECTURE.md — Anti-Pattern 4 (branch protection is a separate operator step).
  </read_first>
  <files>.github/workflows/ci.yml</files>
  <action>
    Ensure `.github/workflows/ci.yml` is committed on `main` (the file was created in 19-01; commit it if not already), then `git push origin main`. Capture the pushed commit SHA.

    Then run THE ONE ALLOWED CONDITIONAL for live observation:

    `if command -v gh >/dev/null && gh auth status >/dev/null 2>&1; then` → AUTHENTICATED PATH (scripted hard gate):
      - `gh run watch` the run for the pushed SHA to completion (`gh run list --commit <SHA>` → get run id → `gh run watch <id> --exit-status`). `--exit-status` makes gh exit non-zero if the run concludes failure — that IS the hard gate for CI-01/02/03 (gate/race/docker-build) plus the main-push-required e2e tier (SC4).
      - Prove SC3 (CACHED on second run): trigger an identical re-run of at least the docker-build tier — `gh run rerun <id> --job docker-build` OR push a trivial no-op commit — then inspect the docker-build logs (`gh run view <new-id> --log`) and assert `CACHED` appears on layers and build time dropped materially vs. the first run. Record both run URLs.
      - If any tier concludes failure, do NOT mark done — capture the failing job's logs (and the compose-logs artifact for e2e via `gh run download`) into the SUMMARY for diagnosis, and stop for triage.

    `else` → UNAUTHENTICATED PATH (prepare for the Task 2 checkpoint):
      - Print the exact commit SHA and the direct URL https://github.com/paderinandrey/octoconv/actions (and, if derivable, the per-run URL). Write these into the SUMMARY-in-progress. The human-verify checkpoint (Task 2) is where the operator confirms green — proceed to it.

    Record which path was taken in the SUMMARY.
  </action>
  <verify>
    <automated>git log origin/main -1 --pretty=%H >/dev/null 2>&1 && git ls-files --error-unmatch .github/workflows/ci.yml && echo PUSHED_AND_TRACKED</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` is committed and `git push origin main` succeeded (pushed SHA recorded).
    - AUTHENTICATED path: `gh run watch <id> --exit-status` exited 0 (run concluded success) for all 4 tiers; a second docker-build run's log shows `CACHED` layers with reduced build time; both run URLs recorded. OR
    - UNAUTHENTICATED path: the commit SHA + Actions URL are recorded in the SUMMARY and the checkpoint (Task 2) is queued. Which path was taken is stated explicitly.
  </acceptance_criteria>
  <done>The workflow is pushed to main and the live run is either proven green via scripted gh watch (+ CACHED second run) or handed to the human-verify checkpoint with the exact URL/SHA recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The 4-tier CI workflow (.github/workflows/ci.yml) was pushed to `main`, triggering a live GitHub Actions run. This checkpoint confirms the run is GREEN when `gh` was not authenticated at execution time (the automated `gh run watch` hard gate in Task 1 could not run). If Task 1 took the authenticated path and already hard-gated the run green, state that and approve — no manual check needed.
  </what-built>
  <how-to-verify>
    1. Open https://github.com/paderinandrey/octoconv/actions and select the `ci` run for the commit SHA recorded in Task 1 / the SUMMARY.
    2. Confirm all four jobs are green: `gate`, `race`, `docker-build`, `e2e`. (On a main-branch push, `e2e` is REQUIRED — it must be green, not merely advisory.)
    3. Open the `docker-build` job logs and confirm the images build. Then re-run the workflow once (top-right "Re-run all jobs", or "Re-run" on docker-build) and confirm the SECOND `docker-build` shows `CACHED` layers and a shorter run time — this proves SC3's per-target gha cache actually hits.
    4. On the first (or any) run, optionally open the `e2e` job to confirm the stack came up, `/healthz` was reached, `internal/e2e` ran, and the teardown step ran under "always()".
    5. Paste the run URL(s) and pass/fail per tier back here.
  </how-to-verify>
  <resume-signal>Type "approved" (with the run URL) once all four tiers are green and the second docker-build shows CACHED layers — or describe which tier failed and paste its log/artifact.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write live-run evidence + branch-protection follow-up into the SUMMARY</name>
  <read_first>
    - .planning/research/ARCHITECTURE.md — Anti-Pattern 4 (exact branch-protection rationale + the `gh api repos/{owner}/{repo}/branches/main/protection` shape).
    - .planning/ROADMAP.md — Phase 19 "Operational follow-up (not a code deliverable)" note (line 118): branch protection must be configured manually after the workflow proves green.
    - The SUMMARY-in-progress from Tasks 1-2 (run URLs, per-tier results, path taken).
  </read_first>
  <files>.planning/phases/19-ci-pipeline/19-02-SUMMARY.md</files>
  <action>
    Finalize `19-02-SUMMARY.md` with: (a) the observation path taken (scripted gh vs. human-verify); (b) the live run URL(s) and per-tier green results mapped to CI-01 (gate), CI-02 (race), CI-03 (docker-build + CACHED-second-run evidence), CI-04 (e2e advisory/required, teardown, artifact); (c) actual runner sizing observed if visible (confirms the private-2c/8GB vs public-4c/16GB assumption from STACK) and any e2e-tier timing/flakiness notes.

    Then document the BRANCH-PROTECTION FOLLOW-UP as an explicit operator action (NOT automatable here without gh auth, and NOT a code deliverable — ARCHITECTURE Anti-Pattern 4): the operator must, after this green run, configure branch protection on `main` requiring at least the `gate` and `race` status checks (ideally also `docker-build`) before merge. Provide BOTH paths: the GitHub UI route (Settings → Branches → Add rule → require status checks → select gate/race) AND the CLI route (`gh api -X PUT repos/paderinandrey/octoconv/branches/main/protection ...` with `required_status_checks.contexts` naming the job checks). State clearly that e2e is advisory-on-PR by design, so it should NOT be a required check for PRs.
  </action>
  <verify>
    <automated>grep -qi 'branch protection' .planning/phases/19-ci-pipeline/19-02-SUMMARY.md && grep -q 'paderinandrey/octoconv/branches/main/protection' .planning/phases/19-ci-pipeline/19-02-SUMMARY.md && echo SUMMARY_OK</automated>
  </verify>
  <acceptance_criteria>
    - 19-02-SUMMARY.md records the observation path, live run URL(s), and per-tier green results mapped to CI-01..CI-04.
    - SC3 CACHED-on-second-run evidence is captured (second-run URL or operator confirmation).
    - The branch-protection follow-up is documented with BOTH the UI and `gh api .../branches/main/protection` CLI routes, naming gate+race (not e2e) as the required checks, and explicitly framed as a manual operator step.
  </acceptance_criteria>
  <done>SUMMARY captures full live-run evidence for CI-01..04 and the branch-protection operational follow-up (UI + CLI), completing Phase 19.</done>
</task>

</tasks>

<verification>
- The workflow is pushed to origin/main and a live GitHub Actions run is confirmed green across all 4 tiers (scripted gh hard gate OR operator human-verify).
- SC3's CACHED-on-second-run is proven on a real re-run.
- The branch-protection follow-up is documented (UI + CLI), not silently assumed done.
</verification>

<success_criteria>
- Live GitHub Actions run is green: gate (CI-01), race (CI-02), docker-build with CACHED second run (CI-03), e2e advisory-on-PR/required-on-main with teardown + failure-artifact (CI-04).
- 19-02-SUMMARY.md records run URLs, per-tier evidence, and the manual branch-protection follow-up.
</success_criteria>

<output>
Create/finalize `.planning/phases/19-ci-pipeline/19-02-SUMMARY.md` when done, with live-run evidence for CI-01..04 and the branch-protection operational follow-up.
</output>
