---
phase: 21-mcp-server
plan: 03
type: execute
wave: 3
depends_on: [21-02]
files_modified:
  - cmd/mcp-server/main.go
  - internal/mcpserver/mcp_live_test.go
  - README.md
autonomous: true
requirements: [MCP-01, MCP-02, MCP-03, MCP-04, MCP-05]
user_setup: []

must_haves:
  truths:
    - "D-01: cmd/mcp-server is a thin main that loads Config, builds the client + mcp.Server, and runs the stdio transport (mcp.StdioTransport) to completion"
    - "D-03: the binary fails fast with a non-zero exit and a stderr message when OCTOCONV_BASE_URL or OCTOCONV_API_KEY is missing"
    - "D-10: log output is redirected to stderr (log.SetOutput(os.Stderr)) BEFORE any SDK init, and stdout carries ONLY JSON-RPC — no fmt.Print* anywhere in cmd/mcp-server"
    - "D-13: a live hard gate drives the REAL built binary over stdio JSON-RPC against the running compose stack — initialize handshake, tools/list returns the five tools, convert_file on a real PNG (png→jpg) returns a result with presigned_url + an existing local_path whose bytes are JPEG magic, list_presets + list_supported_formats round-trip, a bad-input call returns isError"
    - "D-10: the live gate asserts stdout purity — every line the binary writes to stdout parses as a JSON-RPC message; logs appear only on stderr"
    - "D-14: README gains an 'MCP server' section with a claude_desktop/claude-code config JSON example"
  artifacts:
    - path: "cmd/mcp-server/main.go"
      provides: "thin stdio MCP entrypoint with stderr logging + fail-fast env"
      contains: "StdioTransport"
    - path: "internal/mcpserver/mcp_live_test.go"
      provides: "E2E_BASE_URL-gated live stdio JSON-RPC hard gate"
      contains: "E2E_BASE_URL"
    - path: "README.md"
      provides: "MCP server section with client config JSON"
      contains: "mcp-server"
  key_links:
    - from: "cmd/mcp-server/main.go"
      to: "internal/mcpserver.NewServer"
      via: "wires Config + Client and runs StdioTransport"
      pattern: "mcpserver"
    - from: "internal/mcpserver/mcp_live_test.go"
      to: "cmd/mcp-server (built binary)"
      via: "exec.Command over stdio, JSON-RPC frames"
      pattern: "exec.Command"
---

<objective>
Ship the `cmd/mcp-server` binary (thin stdio entrypoint with fail-fast env config and stderr-only logging), document it in the README with an MCP client config example, and prove the whole surface end-to-end with a live stdio JSON-RPC hard gate that drives the real binary against the running docker-compose stack.

Purpose: D-13 is the phase's acceptance gate — a scripted JSON-RPC session over the real binary's stdio (not a mocked transport) converting a real PNG through the live API, asserting the result contract, the five tools, and stdout purity (D-10). This is what makes "agents convert files via a stdio MCP server" true rather than merely unit-plausible.

Output: `cmd/mcp-server/main.go`, `internal/mcpserver/mcp_live_test.go`, README section.
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/21-mcp-server/21-CONTEXT.md
@internal/mcpserver/mcpserver.go
@internal/mcpserver/config.go

# Live-gate infrastructure references (READ ONLY):
@internal/e2e/e2e_test.go

<live_gate_infra>
<!-- How the existing E2E suite runs against the live stack — the live gate mirrors this. -->

Env-gating (internal/e2e/e2e_test.go): tests self-skip when E2E_BASE_URL is unset, so `go test ./...` stays green offline.

Provisioning a client API key (mirror provisionClient): a _test.go file may import internal/clients + internal/auth + internal/db to insert a client row and return a raw key. This is a TEST-ONLY import — it does NOT violate D-02 (which constrains the PRODUCTION package internal/mcpserver). Requires DATABASE_URL + API_KEY_SALT to match the running API.

CI e2e job (.github/workflows/ci.yml, Tier 4): the ONLY job referencing docker-compose.e2e.yml:
  docker compose -f docker-compose.yml -f docker-compose.e2e.yml up -d
  go test ./internal/e2e/... -timeout 30m
    E2E_BASE_URL=http://localhost:8090
    DATABASE_URL=postgres://octo:octo-pass@localhost:5434/octo_db
    API_KEY_SALT=dev-only-change-me-in-real-deploys
    E2E_S3_DIAL_ADDR=127.0.0.1:9100   # host:port to dial the presigned download (minio:9000 does not resolve from host)

Base compose (docker-compose.yml) brings up the IMAGE `worker` (libvips) — png→jpg needs no document/JVM engine, so the gate uses a fast image conversion.
</live_gate_infra>
</context>

<tasks>

<task type="auto">
  <name>Task 1: cmd/mcp-server thin main (stderr logging, fail-fast) + README section</name>
  <files>cmd/mcp-server/main.go, README.md</files>
  <read_first>
    - cmd/api/main.go + cmd/worker/main.go (thin-main + log.Printf-to-stderr conventions, emoji startup log)
    - internal/mcpserver/mcpserver.go + config.go (NewServer + Config.Load signatures)
    - .planning/research/PITFALLS.md Pitfall 5 (stdout corruption)
    - README.md (existing structure — find the section layout to insert "MCP server" consistently)
  </read_first>
  <action>
    Create cmd/mcp-server/main.go: FIRST statement in main() is `log.SetOutput(os.Stderr)` (D-10, before any SDK init) — Go's log already defaults to stderr, but set it explicitly and add a comment tying it to Pitfall 5. Load Config via mcpserver.Config.Load(); on error, log.Fatalf (writes to stderr, exit non-zero) — D-03 fail-fast. Build the mcpserver.Client and mcpserver.NewServer(cfg, client), then `srv.Run(ctx, &mcp.StdioTransport{})` with a context cancelled on SIGINT/SIGTERM (mirror cmd/api's signal.NotifyContext). Use log.Printf (never fmt.Print*) for the single startup line to stderr. Keep the binary thin — all logic lives in internal/mcpserver. Then add a "## MCP server" section to README: what it is (zero-privilege stdio HTTP client of the API), the env vars (OCTOCONV_BASE_URL, OCTOCONV_API_KEY, optional OCTOCONV_OUTPUT_DIR/OCTOCONV_CONVERT_TIMEOUT/OCTOCONV_POLL_INTERVAL), a `go build -o octoconv-mcp ./cmd/mcp-server` line, and a claude-code / claude_desktop MCP config JSON example (command + args + env) — D-14.
  </action>
  <acceptance_criteria>
    - Objective: `go build ./cmd/mcp-server` produces a binary; `go vet ./cmd/mcp-server` clean.
    - No stdout writes: `grep -rn 'fmt.Print' cmd/mcp-server/` finds nothing (D-10).
    - Running the binary with OCTOCONV_API_KEY unset exits non-zero and writes the error to stderr, not stdout.
    - README contains an "MCP server" heading and a fenced JSON config block naming the binary.
  </acceptance_criteria>
  <verify>
    <automated>go build -o /tmp/octoconv-mcp ./cmd/mcp-server && go vet ./cmd/mcp-server && ! grep -rn 'fmt\.Print' cmd/mcp-server/ && OCTOCONV_BASE_URL= OCTOCONV_API_KEY= /tmp/octoconv-mcp 2>/tmp/mcp-stderr.txt 1>/tmp/mcp-stdout.txt; test $? -ne 0 && test ! -s /tmp/mcp-stdout.txt && grep -qi 'octoconv' README.md && grep -qi 'mcp' README.md</automated>
  </verify>
  <done>Binary builds, fails fast to stderr with empty stdout, no fmt.Print*, README documents the server with a config example.</done>
</task>

<task type="auto">
  <name>Task 2: LIVE stdio JSON-RPC hard gate (D-13) + stdout purity (D-10)</name>
  <files>internal/mcpserver/mcp_live_test.go</files>
  <read_first>
    - internal/e2e/e2e_test.go (e2eSetup env-gate + provisionClient — mirror both)
    - cmd/mcp-server/main.go (the binary this test builds and drives — from Task 1)
    - internal/mcpserver/testdata/sample.png (fixture added in Plan 21-01)
    - The <live_gate_infra> block above
  </read_first>
  <behavior>
    - Env-gated on E2E_BASE_URL: self-skip when unset (green offline, mirrors e2eSetup).
    - Builds the real binary (`go build -o <tmp> ./cmd/mcp-server`), provisions a client API key (test-only import of internal/clients+auth+db, mirroring provisionClient; requires DATABASE_URL + API_KEY_SALT), then launches the binary with env OCTOCONV_BASE_URL=$E2E_BASE_URL, OCTOCONV_API_KEY=<provisioned>, OCTOCONV_OUTPUT_DIR=t.TempDir(), and (for the presigned download) whatever S3 dial redirection the stack needs (the binary downloads via the presigned URL; if the URL host is minio:9000, document that the gate host must resolve it — reuse the E2E_S3_DIAL_ADDR contract or run inside the compose network).
    - Drives a JSON-RPC session over the binary's stdin/stdout: initialize → notifications/initialized → tools/list → tools/call. Assertions: (1) initialize handshake returns a result; (2) tools/list returns exactly the five tools convert_file, get_job_status, download_result, list_supported_formats, list_presets; (3) tools/call convert_file with the testdata PNG and target_format=jpg returns a NON-isError result whose structured content has a non-empty presigned_url and a local_path that exists on disk and begins with JPEG magic 0xFF 0xD8 0xFF; (4) tools/call list_presets and list_supported_formats return non-isError results with plausible content; (5) a bad-input call (e.g. target_format=jpg AND preset=whatever, or an unsupported pair) returns isError=true.
    - Stdout purity (D-10): capture the binary's raw stdout and assert every non-empty line parses as a JSON object carrying "jsonrpc":"2.0" — i.e. no startup banner or stray log leaked onto stdout; logs must be observable only on the separately-captured stderr.
  </behavior>
  <action>
    Write mcp_live_test.go (build-tag-free; env-gated at runtime like e2e). Choose the driving mechanism: EITHER hand-write a minimal JSON-RPC-over-stdio client (write newline-delimited request frames to the binary's stdin, scan stdout for matching-id responses) — which makes stdout-purity assertion natural since the test owns the raw pipe — OR drive functional assertions (1–5) via the go-sdk's own client over a CommandTransport wrapping the binary, plus a SEPARATE minimal raw-pipe run (feeding a fixed initialize+tools/list sequence on stdin) purely to assert stdout purity (D-10). Prefer the first (single raw pipe) for a self-contained gate. Provision the key exactly as provisionClient does. On timeout, kill the process group so a hung binary surfaces as a test failure, never a wedged CI job.
  </action>
  <acceptance_criteria>
    - Objective offline: `go test ./internal/mcpserver/... -count=1` still passes with E2E_BASE_URL unset (the live test self-skips).
    - Objective live (operator/CI): with the compose stack up and E2E_BASE_URL/DATABASE_URL/API_KEY_SALT set, the gate passes — five tools listed, png→jpg convert_file returns presigned_url + an existing JPEG-magic local_path, list tools round-trip, bad input is isError, and every stdout line is valid JSON-RPC (D-10/D-13).
    - The gate is unconditional where docker is available; a real Claude Code client session is OPTIONAL operator corroboration only, not the gate.
  </acceptance_criteria>
  <verify>
    <automated>go vet ./internal/mcpserver/... && go test ./internal/mcpserver/... -count=1</automated>
    <human-check>
      Bring up the stack and run the live gate (docker discipline: detached `up -d`, `-p octoconv` project name, never a blocking foreground `up`, tear down with `down -v`):
        docker compose -p octoconv -f docker-compose.yml -f docker-compose.e2e.yml up -d --build
        E2E_BASE_URL=http://localhost:8090 \
        DATABASE_URL=postgres://octo:octo-pass@localhost:5434/octo_db \
        API_KEY_SALT=dev-only-change-me-in-real-deploys \
        E2E_S3_DIAL_ADDR=127.0.0.1:9100 \
        go test ./internal/mcpserver/... -run Live -count=1 -v -timeout 15m
        docker compose -p octoconv -f docker-compose.yml -f docker-compose.e2e.yml down -v
      Expect: the Live test PASSes (5 tools, png→jpg result contract, stdout purity). Rebuild api with --build since main has moved since Phase 20.
    </human-check>
  </verify>
  <done>Live gate exists, self-skips offline, and passes against the compose stack: five tools, real png→jpg convert with presigned_url + local JPEG, list round-trips, bad-input isError, stdout JSON-RPC-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Binary stdout ↔ MCP client transport | Any non-JSON-RPC byte on stdout corrupts the session |
| Binary env ↔ process start | Missing/invalid required env must fail fast, not run half-configured |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03 | Tampering | stdout JSON-RPC stream | mitigate | log.SetOutput(os.Stderr) before SDK init; no fmt.Print* in cmd/mcp-server (static grep); live stdout-purity assertion (D-10) |
| T-21-08 | Denial of Service | misconfigured binary | mitigate | Config.Load fail-fast → log.Fatalf non-zero exit; empty-env verify check (D-03) |
| T-21-SC | Tampering | go-sdk supply chain | mitigate | Pin verified in Plan 21-01 (T-21-SC); this plan links the SDK into the shipped binary only |
| T-21-05 | Elevation of Privilege | binary privileges | accept | Binary holds only a client API key + FS access to OUTPUT_DIR; no DB/S3/Redis credentials (D-02) |
</threat_model>

<verification>
- `go build ./cmd/mcp-server && go vet ./cmd/mcp-server` clean; no fmt.Print* (D-10).
- Fail-fast: empty required env → non-zero exit, empty stdout (D-03).
- `go test ./internal/mcpserver/... -count=1` green offline (live gate self-skips).
- Live: five tools, png→jpg convert_file result contract, list round-trips, bad-input isError, stdout purity — all pass against the compose stack (D-13/D-10).
- README has an MCP server section with a client config JSON (D-14).
</verification>

<success_criteria>
The stdio MCP binary ships thin and fail-fast with stderr-only logging; the README documents it for agent/client wiring; and a live JSON-RPC hard gate proves an agent can convert a real file and discover capabilities through the real binary against the live stack, with stdout carrying only protocol framing.
</success_criteria>

<output>
Create `.planning/phases/21-mcp-server/21-03-SUMMARY.md` when done.
</output>
