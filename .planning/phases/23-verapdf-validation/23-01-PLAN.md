---
phase: 23-verapdf-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Dockerfile.document-worker
  - docker-compose.yml
  - .env.example
  - internal/convert/testdata/verapdf_compliant.mrr.xml
  - internal/convert/testdata/verapdf_noncompliant.mrr.xml
  - internal/convert/testdata/verapdf_noncompliant.pdf
  - scripts/verapdf-measure.sh
autonomous: false
requirements: [PDFA-02]
user_setup: []

must_haves:
  truths:
    - "The built document-worker image runs `verapdf` and a java process loads successfully (D-03 glibc-compat verified live, one of the two documented paths recorded)"
    - "veraPDF cold-start cost over >=10 runs on a REAL PDF/A-2b export is measured (nearest-rank p95); p95 <= 10s or the phase STOPS at the go/no-go gate (D-01)"
    - "A genuine LibreOffice PDF/A-2b export validates isCompliant=true under real veraPDF (Pitfall 9 regression canary) — otherwise escalate, do not silently downgrade"
    - "The exact veraPDF CLI flags, exit codes, and machine-readable report schema are captured from the pinned image and recorded for Plan 02 to consume"
  artifacts:
    - path: "Dockerfile.document-worker"
      provides: "veraPDF bundled via multi-stage COPY from pinned verapdf/cli:1.30.2 (D-03)"
      contains: "verapdf/cli:1.30.2"
    - path: "internal/convert/testdata/verapdf_compliant.mrr.xml"
      provides: "Real captured machine-readable report for a compliant PDF/A-2b (parser fixture for Plan 02, D-08)"
    - path: "internal/convert/testdata/verapdf_noncompliant.mrr.xml"
      provides: "Real captured machine-readable report for a non-compliant PDF (parser fixture for Plan 02, D-08)"
    - path: "internal/convert/testdata/verapdf_noncompliant.pdf"
      provides: "Deliberately non-compliant PDF fixture for the Plan 03 live terminal-fail gate (D-09 note)"
  key_links:
    - from: "Dockerfile.document-worker runtime stage"
      to: "verapdf on PATH"
      via: "COPY --from + ENV PATH"
      pattern: "COPY --from=verapdf"
    - from: "measured p95"
      to: "10s budget (D-01)"
      via: "go/no-go checkpoint gate"
      pattern: "p95"
---

<objective>
Bundle the veraPDF CLI into `Dockerfile.document-worker` and — BEFORE any Go wiring —
measure its JVM cold-start cost on a real PDF/A-2b export inside the built image. This
plan is the phase's go/no-go GATE (D-01): if measured p95 exceeds the 10s budget, or a
genuine LibreOffice PDF/A-2b export fails real veraPDF validation, the phase STOPS here
with a loud report and the `verapdf/rest` daemon fallback is presented to the operator —
Plan 02 (wiring) MUST NOT begin until this gate passes.

Purpose: De-risk the highest-uncertainty item in the milestone (new JVM runtime, unverified
glibc compat, unverified per-job latency, unverified CLI report format) with LIVE data before
touching the job hot path (Pitfalls 8/9, ROADMAP cost gate).
Output: A veraPDF-bearing document-worker image, a recorded measurement + verified CLI contract,
committed report/PDF fixtures for downstream plans, and an explicit operator go/no-go decision.
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-verapdf-validation/23-CONTEXT.md

# Packaging target + research on the glibc risk and the pinned tag:
@Dockerfile.document-worker
@.planning/research/STACK.md

<read_first>
- `Dockerfile.document-worker` — current two-stage build (golang builder -> debian:bookworm-slim runtime, USER nobody, tini PID1). veraPDF is bundled INTO the runtime stage.
- `.planning/research/STACK.md` lines 42-61 — the recommended multi-stage snippet: `FROM verapdf/cli:1.30.2 AS verapdf`; `COPY --from=verapdf /opt/java/openjdk /opt/java/openjdk`; `COPY --from=verapdf /opt/verapdf /opt/verapdf`; `ENV PATH="/opt/java/openjdk/bin:/opt/verapdf:${PATH}"`.
- `.planning/research/STACK.md` line 99 — the FLAGGED glibc risk: source image final stage is `alpine:3` (musl), target is `debian:bookworm-slim` (glibc). The copied jlink JRE's native libs (`libjvm.so`) must be verified to load LIVE, not assumed.
- `docker-compose.yml` lines 214-251 — document-worker service: env block + `deploy.resources.limits` (cpus 2.0, memory 1g). VERAPDF_TIMEOUT env is added here; the 1g limit is the veraPDF-alongside-LibreOffice memory ceiling to observe during measurement.
- `.planning/research/PITFALLS.md` Pitfall 8 (JVM cost) and Pitfall 9 (false-fail / re-validate existing v1.3 fixtures BEFORE shipping).
- `internal/e2e/e2e_test.go` `TestPDFAExportE2E` (~577) + `pdfAProfileOpts` (~569) — the exact opts (`{"pdf_profile":"pdf/a-2b"}`) and the `sample.docx` fixture that produce a real PDF/A-2b export; reuse this shape to generate the measurement input.
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bundle veraPDF into Dockerfile.document-worker and verify java loads across the glibc boundary</name>
  <files>Dockerfile.document-worker, docker-compose.yml, .env.example</files>
  <action>
Add a veraPDF build stage `FROM verapdf/cli:1.30.2 AS verapdf` (pinned tag, D-03) and, in the
existing debian:bookworm-slim runtime stage, `COPY --from=verapdf /opt/java/openjdk /opt/java/openjdk`
and `COPY --from=verapdf /opt/verapdf /opt/verapdf`, then extend PATH to include
`/opt/java/openjdk/bin` and `/opt/verapdf` (per STACK.md lines 52-58). Keep USER nobody and the tini
entrypoint unchanged.

Build the image and verify LIVE that java/verapdf actually run across the musl->glibc boundary
(STACK.md line 99 flagged this as NOT assumed-safe). Two documented outcomes — record which path was taken:
  (A) PRIMARY: the copied jlink JRE loads — `verapdf --version` (or `--help`) succeeds inside the container.
  (B) FALLBACK: if the jlink JRE fails to load its native libs against bookworm glibc, replace the
      `COPY --from=verapdf /opt/java/openjdk` with `apt-get install -y --no-install-recommends
      openjdk-17-jre-headless` and COPY only veraPDF's app jars (`/opt/verapdf`), invoking veraPDF via the
      Debian JRE. Record in the SUMMARY which path (A or B) was used and the resulting image size delta.

Add `VERAPDF_TIMEOUT: "60s"` to the document-worker service env in docker-compose.yml (D-04 own timeout,
default 60s) and document `VERAPDF_TIMEOUT` in .env.example alongside DOCUMENT_ENGINE_TIMEOUT. Do NOT
add any new compose service or topology change — veraPDF lives inside the existing document-worker container only (D-04).
  </action>
  <verify>
    <automated>docker build -f Dockerfile.document-worker -t octoconv-document-worker:verapdf . && docker run --rm --entrypoint verapdf octoconv-document-worker:verapdf --version</automated>
  </verify>
  <done>The document-worker image builds; `verapdf --version` (or `--help`) prints successfully inside the container via the recorded path (A jlink JRE or B Debian JRE). VERAPDF_TIMEOUT=60s is present in docker-compose.yml document-worker env and documented in .env.example.</done>
</task>

<task type="auto">
  <name>Task 2: Measure JVM cold-start cost on a real PDF/A-2b export, capture the CLI contract, and commit report + PDF fixtures</name>
  <files>scripts/verapdf-measure.sh, internal/convert/testdata/verapdf_compliant.mrr.xml, internal/convert/testdata/verapdf_noncompliant.mrr.xml, internal/convert/testdata/verapdf_noncompliant.pdf</files>
  <action>
First determine the exact veraPDF CLI contract against the PINNED image (do NOT assume from training data —
SUMMARY.md line 146 flags this): run `docker run --rm --entrypoint verapdf octoconv-document-worker:verapdf --help`
and record the real flags for flavour selection (expected `-f 2b`, D-06) and machine-readable output (verify the
real `--format` option value and its report schema — MRR/XML vs JSON), plus the actual exit codes for
compliant vs non-compliant vs validator-error. Record all of this verbatim in the SUMMARY for Plan 02 to consume.

Generate a REAL PDF/A-2b compliant sample: run `soffice` PDF/A-2b export inside the built image on
`internal/e2e/testdata/sample.docx` (the FilterOptions suffix `{"SelectPdfVersion":{"type":"long","value":"2"},
"EmbedStandardFonts":{"type":"boolean","value":true}}` — mirror `internal/convert/opts.go` `pdfaFilterOptionsSuffix`
and libreoffice.go's `--convert-to pdf:writer_pdf_Export:<suffix>` shape). This is the same output the live pipeline produces.

Write `scripts/verapdf-measure.sh` (throwaway-but-committed measurement harness, mirrors the scripts/*-acceptance.sh
precedent) that, inside the built image: (1) runs `verapdf -f 2b <compliant.pdf>` N>=10 times capturing per-run
wall-clock, prints each run and the computed p95 — use the NEAREST-RANK method, stated explicitly: sort the N
per-run durations ascending and take p95 = the value at 1-based rank ceil(0.95*N) (for N=10, rank 10, i.e. the
slowest of the 10 runs) — and asserts p95 <= 10s (D-01 budget; exit non-zero if exceeded);
(2) asserts the compliant export's report says isCompliant=true — the Pitfall 9 regression canary (a real LibreOffice
PDF/A-2b that veraPDF rejects is a STOP condition, not a downgrade); (3) captures the machine-readable report of the
compliant run to `verapdf_compliant.mrr.xml`; (4) creates a deliberately non-compliant PDF (a plain non-PDF/A PDF —
D-09 note: the simplest reliable non-compliant fixture is a plain non-A PDF through `-f 2b`), saves it as
`verapdf_noncompliant.pdf`, runs `verapdf -f 2b` on it, asserts the report says non-compliant, and captures that
report to `verapdf_noncompliant.mrr.xml`. Also observe/record peak memory inside the 1g container limit (JVM +
idle LibreOffice footprint) so a future OOM regression is visible. Commit the two `.mrr.xml` reports and the
`.pdf` fixture — these are Plan 02's offline parser fixtures (D-08) and Plan 03's live terminal-fail fixture (D-09).
  </action>
  <verify>
    <automated>bash scripts/verapdf-measure.sh && test -s internal/convert/testdata/verapdf_compliant.mrr.xml && test -s internal/convert/testdata/verapdf_noncompliant.mrr.xml && test -s internal/convert/testdata/verapdf_noncompliant.pdf</automated>
  </verify>
  <done>scripts/verapdf-measure.sh runs green: nearest-rank p95 <= 10s over >=10 runs on a real PDF/A-2b export, the compliant export validates isCompliant=true (Pitfall 9 canary passes), the non-compliant fixture is confirmed flagged non-compliant, and all three fixtures are committed. The verified CLI flags, machine-report schema, exit codes, and peak-memory observation are recorded in the SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: GO/NO-GO gate — operator reviews the measurement against the D-01 budget</name>
  <action>Present the recorded measurement (raw per-run numbers, nearest-rank p95, glibc path, compliant-canary verdict, CLI contract) to the operator and BLOCK: Plan 23-02 wiring must not start without an explicit "approved" (D-01 — never silently switch architectures).</action>
  <what-built>A veraPDF-bearing document-worker image plus a live measurement of veraPDF cold-start cost on a real PDF/A-2b export, with the compliant-fixture regression canary and captured CLI contract.</what-built>
  <how-to-verify>
Review the recorded measurement in the SUMMARY:
  1. GO signal (all must hold): measured nearest-rank p95 <= 10s over >=10 runs; the real LibreOffice PDF/A-2b export came back
     isCompliant=true (no v1.3 regression, Pitfall 9); the glibc path (A jlink JRE or B Debian JRE) is recorded;
     the exact `-f 2b` + machine-report flags/exit codes are captured. -> Type "approved" to proceed to Plan 23-02 (wiring).
  2. NO-GO signal (any of): p95 > 10s, OR the compliant export validated as non-compliant, OR java would not load
     across the glibc boundary under either path. -> Do NOT approve. The phase STOPS here (D-01). Re-plan toward the
     documented `verapdf/rest` daemon fallback (deferred idea in 23-CONTEXT.md), which amortizes JVM cold-start across
     a long-lived warm process, and/or a severity-policy escalation if the regression canary failed. Record the raw numbers before stopping (D-02).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to wiring (Plan 23-02), or describe the budget/canary failure to trigger the daemon-fallback re-plan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verapdf/cli Docker image -> document-worker image | Third-party image contents bundled into our runtime (supply chain) |
| uploaded office doc -> soffice PDF export -> veraPDF JVM parse | Untrusted-content-derived PDF bytes parsed by a new JVM process inside the worker |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-SC | Tampering | verapdf/cli image COPY into Dockerfile.document-worker | mitigate | Pin the exact tag `verapdf/cli:1.30.2` (D-03, not `latest`); COPY only `/opt/java/openjdk` + `/opt/verapdf`; no arbitrary network fetch at build time |
| T-23-01 | Denial of Service | JVM heap + idle LibreOffice in the shared 1g/2cpu document-worker container | mitigate | Measure peak memory in Task 2 within the 1g limit; the per-invocation VERAPDF_TIMEOUT (60s) bound is wired in Plan 02; container already runs USER nobody |
| T-23-02 | Denial of Service | veraPDF cold-start cost on the CI e2e budget and job latency | mitigate | This plan's go/no-go gate: measured nearest-rank p95 <= 10s before wiring; daemon fallback documented if the budget fails (D-01) |
</threat_model>

<verification>
- `docker build -f Dockerfile.document-worker` succeeds with the pinned veraPDF stage.
- `verapdf --version` runs inside the built image (glibc path recorded).
- `scripts/verapdf-measure.sh` exits 0: nearest-rank p95 <= 10s over >=10 runs, compliant canary true, non-compliant fixture flagged.
- Three fixtures committed under internal/convert/testdata/.
- Operator go/no-go checkpoint reached with raw numbers recorded (D-02).
</verification>

<success_criteria>
- veraPDF bundled into Dockerfile.document-worker via pinned `verapdf/cli:1.30.2` multi-stage COPY (PDFA-02).
- JVM cold-start nearest-rank p95 measured <= 10s over >=10 runs on a REAL PDF/A-2b export, or the phase STOPS at the gate (D-01).
- Real LibreOffice PDF/A-2b export confirmed isCompliant=true under veraPDF (Pitfall 9 canary).
- Verified CLI contract (flags, report schema, exit codes) + peak-memory observation recorded for Plan 02.
- Compliant/non-compliant report fixtures + non-compliant PDF fixture committed.
</success_criteria>

<output>
Create `.planning/phases/23-verapdf-validation/23-01-SUMMARY.md` when done. It MUST record: the
measured per-run and nearest-rank p95 numbers (D-02), the glibc path taken (A/B) + image size delta, the exact
verified veraPDF flags/report-schema/exit-codes, the peak-memory observation, and the go/no-go verdict.
</output>
</content>
</invoke>
