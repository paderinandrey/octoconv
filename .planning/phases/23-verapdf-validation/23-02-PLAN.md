---
phase: 23-verapdf-validation
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - internal/convert/verapdf.go
  - internal/convert/verapdf_test.go
  - internal/convert/libreoffice.go
  - internal/worker/worker.go
  - internal/worker/worker_test.go
autonomous: true
requirements: [PDFA-01, PDFA-02]
user_setup: []

must_haves:
  truths:
    - "For a wantPDFA job, after the cheap /GTS_PDFA pre-filter passes, veraPDF runs full ISO 19005-2b validation on the output (D-05)"
    - "A non-compliant report (isCompliant=false) OR any veraPDF error/timeout on a PDF/A job returns a terminal-classified error — fail-closed (D-06)"
    - "veraPDF is invoked through the existing hardened runCommand (Setpgid + process-group kill) with its own VERAPDF_TIMEOUT bound (D-04)"
    - "terminalVeraPDFSignatures are added to worker.go in the SAME COMMIT that introduces the verapdf.go error strings (D-06/D-04 discipline)"
    - "The veraPDF non-conformance reason (first N chars of rule violations) reaches job_events via the existing MarkFailed detail path — no new logging in internal/ (D-07)"
    - "The machine-report parser is unit-tested against the committed compliant/non-compliant fixtures from Plan 01 (D-08)"
  artifacts:
    - path: "internal/convert/verapdf.go"
      provides: "Hardened veraPDF invocation + machine-report parser + fail-closed terminal error strings"
      exports: ["ValidatePDFA"]
    - path: "internal/convert/verapdf_test.go"
      provides: "Offline parser + fail-closed unit tests over Plan 01 report fixtures (D-08)"
      contains: "verapdf_compliant.mrr.xml"
    - path: "internal/worker/worker.go"
      provides: "terminalVeraPDFSignatures consumed by isTerminal, same-commit (D-06)"
      contains: "terminalVeraPDFSignatures"
  key_links:
    - from: "internal/convert/libreoffice.go validateDocumentOutput wantPDFA branch"
      to: "ValidatePDFA"
      via: "call after the /GTS_PDFA pre-filter"
      pattern: "ValidatePDFA"
    - from: "internal/convert/verapdf.go ValidatePDFA"
      to: "runCommand"
      via: "hardened exec with VERAPDF_TIMEOUT sub-context"
      pattern: "runCommand\\(.*verapdf"
    - from: "internal/worker/worker.go isTerminal"
      to: "terminalVeraPDFSignatures"
      via: "substring classification loop"
      pattern: "terminalVeraPDFSignatures"
---

<objective>
Wire real veraPDF ISO 19005-2b validation into the document PDF/A export path. Add
`internal/convert/verapdf.go` (hardened runCommand invocation + machine-report parser),
call it from `validateDocumentOutput`'s existing wantPDFA branch AFTER the cheap `/GTS_PDFA`
pre-filter, and add `terminalVeraPDFSignatures` to `worker.go` in the SAME commit so a
non-compliant or unverifiable archival claim fails the job terminally (fail-closed, D-06).

Purpose: Replace the v1.3 OutputIntent-marker heuristic with genuine ISO conformance — a client
who asked for PDF/A gets a real guarantee or a terminal failure with the reason recorded (PDFA-01).
Output: Offline-testable validation logic gated behind the JVM-cost measurement that Plan 01 passed.
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-verapdf-validation/23-CONTEXT.md
@.planning/phases/23-verapdf-validation/23-01-SUMMARY.md

# The integration seam, the hardened runner, the opts flow, and the terminal-classifier:
@internal/convert/libreoffice.go
@internal/convert/exec.go
@internal/convert/opts.go
@internal/worker/worker.go

<read_first>
- `.planning/phases/23-verapdf-validation/23-01-SUMMARY.md` — the VERIFIED veraPDF CLI contract from Plan 01: exact `-f 2b` + machine-report flags, the report schema (element/attribute that carries the compliant/isCompliant verdict and the rule-violation summary), and the real exit codes. Build the parser against THIS, not training-data assumptions.
- `internal/convert/libreoffice.go` `validateDocumentOutput` (~226) — the seam. Current wantPDFA branch: `validatePDF` (magic) then the `/GTS_PDFA` marker `bytes.Contains` check (~239). veraPDF runs AFTER the marker passes (D-05: marker stays as the fast pre-filter before JVM spin-up). NOTE: `validateDocumentOutput` currently has NO ctx param; `Convert` (~130) calls it with `(outPath, targetFormat, isPDFA)` and DOES have `ctx`. Thread `ctx` into `validateDocumentOutput` and its call site so ValidatePDFA can derive its timeout sub-context.
- `internal/convert/exec.go` `runCommand(ctx, name, args...)` — Setpgid + process-group SIGKILL on ctx cancel; stderr captured into the returned error. Reuse verbatim (D-04) — no new exec abstraction.
- `internal/convert/opts.go` — `PDFAFilterOptions(docOpts) (suffix string, isPDFA bool)` and `DocOptsFromMap`. This is how Convert already knows a job wanted PDF/A: `isPDFA` flows from the persisted opts map into `validateDocumentOutput`'s `wantPDFA` arg. veraPDF runs ONLY when `wantPDFA` is true (D-05).
- `internal/worker/worker.go` `terminalLibreOfficeSignatures` (~51) and `isTerminal` (~104, the substring loops). Add a `terminalVeraPDFSignatures` slice (Claude's discretion per 23-CONTEXT.md: separate slice vs append to the existing one — keep the worker.go conventions) and consume it in `isTerminal` the SAME way. `isDocumentTerminal` (~185) already treats a wrapped `context.DeadlineExceeded` as terminal, so a VERAPDF_TIMEOUT expiry is already fail-closed — no signature needed for the timeout case.
- `internal/worker/worker.go` `MarkFailed(... detail map[string]any)` call in `HandleDocumentConvert` (~365): it already passes `{"engine_stderr": err.Error()}` into job_events.detail. The veraPDF reason rides this existing path (D-07) — do NOT add new logging.
- `internal/convert/verapdf_test.go` fixtures: `internal/convert/testdata/verapdf_compliant.mrr.xml` and `verapdf_noncompliant.mrr.xml` (committed by Plan 01).
</read_first>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: internal/convert/verapdf.go — hardened invocation + machine-report parser (fail-closed), with offline unit tests</name>
  <files>internal/convert/verapdf.go, internal/convert/verapdf_test.go</files>
  <behavior>
    - Parsing the committed `verapdf_compliant.mrr.xml` yields a compliant verdict -> ValidatePDFA returns nil.
    - Parsing the committed `verapdf_noncompliant.mrr.xml` yields a non-compliant verdict -> ValidatePDFA returns a terminal-signatured error whose message includes a bounded (first-N-chars) rule-violation summary (D-06/D-07).
    - A veraPDF process error (non-zero exit that is not a clean compliant/non-compliant report, or unparseable report output) -> ValidatePDFA returns a terminal-signatured "validation error" — fail-closed (D-06): an unverifiable archival claim is a failed archival claim.
    - The parser reads the verdict from the exact report element/attribute recorded in 23-01-SUMMARY (do not trust exit code alone — D-09/specifics line 59).
  </behavior>
  <action>
Create `internal/convert/verapdf.go` (one-engine-per-file convention) exporting
`ValidatePDFA(ctx context.Context, path string) error`. Derive a bounded sub-context
`context.WithTimeout(ctx, verapdfTimeout)` (whichever of the outer DOCUMENT_ENGINE_TIMEOUT
remainder or VERAPDF_TIMEOUT fires first) and invoke veraPDF via `runCommand` with the VERIFIED
`-f 2b` + machine-report flags from 23-01-SUMMARY. Because the LibreOfficeConverter is a zero-field
package singleton (converters.go init) with no DI seam, resolve `verapdfTimeout` from a package-level
var reading `VERAPDF_TIMEOUT` once (default 60s) — document this as a deliberate, narrowly-scoped
exception to the "env only in cmd/*" convention (the singleton has no constructor to thread it through).

Split the report-parsing into a pure helper (e.g. `parseVeraPDFReport(raw []byte) (compliant bool,
summary string, err error)`) that operates on the machine-readable report bytes — this keeps the parser
unit-testable offline with zero binary dependency. ValidatePDFA returns:
  - nil when compliant;
  - a terminal error wrapping a STABLE substring for non-compliant (canonical: `"pdf/a non-compliant"`) plus the bounded rule summary;
  - a terminal error wrapping a STABLE substring for validator failure (canonical: `"pdf/a validation error"`) on any veraPDF invocation/parse failure (fail-closed).
Runtime output text (stderr, temp paths) must stay OUT of the sanitized client message — it already
lands in job_events.detail via the worker's existing MarkFailed call (D-07).

Write `internal/convert/verapdf_test.go` covering the four behaviors above using the committed
`.mrr.xml` fixtures (D-08). No veraPDF binary is invoked in these tests — they parse the captured
report bytes, so they run in CI's offline gate tier.
  </action>
  <verify>
    <automated>go test ./internal/convert/ -run VeraPDF -count=1 && go vet ./internal/convert/</automated>
  </verify>
  <done>ValidatePDFA + parseVeraPDFReport exist; the fixture-driven unit tests pass; compliant->nil, non-compliant->terminal error with bounded summary, validator error->terminal error (fail-closed). The two canonical terminal substrings are defined and used.</done>
</task>

<task type="auto">
  <name>Task 2: Wire ValidatePDFA into the PDF/A branch + add terminalVeraPDFSignatures SAME-COMMIT + worker classification tests</name>
  <files>internal/convert/libreoffice.go, internal/worker/worker.go, internal/worker/worker_test.go</files>
  <action>
Thread `ctx context.Context` into `validateDocumentOutput` (new first param) and update its only call
site in `Convert` (~130) to pass the `ctx` it already holds. In the wantPDFA pdf branch, AFTER the
existing `/GTS_PDFA` marker `bytes.Contains` pre-filter passes (D-05: marker is the fast fail before
JVM spin-up), call `ValidatePDFA(ctx, path)` and return its error. The marker check MUST stay and MUST
run first — a plain PDF fast-fails on the marker without paying JVM cost.

In `internal/worker/worker.go`, add `terminalVeraPDFSignatures` containing the two canonical stable
substrings from Task 1 (`"pdf/a non-compliant"`, `"pdf/a validation error"`) and consume it in
`isTerminal` with the same substring-loop pattern as `terminalLibreOfficeSignatures` — in the SAME
COMMIT as Task 1's verapdf.go error strings (D-06/D-04 discipline, matching the v1.2 convention comment
already in worker.go ~46-50). This makes a non-compliant/unverifiable PDF/A output fail terminally
(MarkFailed + SkipRetry) instead of burning DOCUMENT_MAX_RETRY retries. A VERAPDF_TIMEOUT expiry needs
no new signature — isDocumentTerminal already classifies wrapped context.DeadlineExceeded as terminal.

Add worker unit tests (worker_test.go, alongside the existing isDocumentTerminal/isTerminal tests)
asserting: an error carrying `"pdf/a non-compliant"` classifies terminal; an error carrying
`"pdf/a validation error"` classifies terminal; and (fail-closed) both take the MarkFailed+SkipRetry
path, so the veraPDF reason reaches job_events.detail via the existing `{"engine_stderr": err.Error()}` payload (D-07).
  </action>
  <verify>
    <automated>go test ./internal/convert/ ./internal/worker/ -count=1 && go vet ./... && test -z "$(gofmt -l internal/convert/verapdf.go internal/convert/libreoffice.go internal/worker/worker.go)"</automated>
  </verify>
  <done>validateDocumentOutput takes ctx and calls ValidatePDFA after the /GTS_PDFA pre-filter; terminalVeraPDFSignatures exists and is consumed by isTerminal in the same commit; worker tests prove both veraPDF terminal substrings classify terminal and route through MarkFailed+SkipRetry; go vet + gofmt clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| uploaded office doc -> soffice PDF -> veraPDF JVM parse | Untrusted-content-derived PDF parsed by the JVM inside the worker |
| veraPDF verdict -> job terminal/retry decision | A wrong verdict interpretation could mark a bad PDF/A "done" or retry-loop a deterministic failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03 | Spoofing (of compliance) | ValidatePDFA verdict parsing | mitigate | Parse the report verdict element, not exit code alone (D-09); fail-closed to terminal on any parse/invocation error (D-06) |
| T-23-04 | Denial of Service | veraPDF JVM run inside the document-worker | mitigate | VERAPDF_TIMEOUT sub-context bound + runCommand Setpgid process-group SIGKILL (D-04); timeout classified terminal (no retry loop) |
| T-23-05 | Information disclosure | veraPDF stderr / temp paths leaking to the client | mitigate | Sanitized error_message only; raw detail confined to job_events.detail via existing MarkFailed path (D-07, T-03-03 precedent) |
| T-23-SC | Tampering | pinned verapdf/cli:1.30.2 (carried from Plan 01) | mitigate | Pinned tag; no new install task in this plan |
</threat_model>

<verification>
- `go test ./internal/convert/ ./internal/worker/` passes.
- verapdf_test.go drives the committed compliant/non-compliant `.mrr.xml` fixtures (offline).
- `terminalVeraPDFSignatures` present in worker.go and consumed by isTerminal in the same commit as verapdf.go.
- `go vet ./...` and `gofmt -l` clean on the touched files.
</verification>

<success_criteria>
- Real veraPDF ISO 19005-2b validation gates the wantPDFA export path, after the /GTS_PDFA pre-filter (PDFA-01, D-05).
- Non-compliant OR veraPDF error/timeout on a PDF/A job -> terminal failure, fail-closed (PDFA-01, D-06).
- Invocation via hardened runCommand with VERAPDF_TIMEOUT bound (PDFA-02, D-04).
- terminalVeraPDFSignatures added same-commit (PDFA-02, D-06/D-04 discipline).
- veraPDF reason reaches job_events via existing MarkFailed detail (D-07); no new internal/ logging.
- Parser unit-tested over committed fixtures (D-08).
</success_criteria>

<output>
Create `.planning/phases/23-verapdf-validation/23-02-SUMMARY.md` when done. Record the chosen
terminalVeraPDFSignatures shape (separate slice vs append), the VERAPDF_TIMEOUT resolution mechanism,
and the exact report element/attribute the parser keys on.
</output>
