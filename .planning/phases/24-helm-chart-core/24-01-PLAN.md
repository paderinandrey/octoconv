---
phase: 24-helm-chart-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - deploy/chart/octoconv/Chart.yaml
  - deploy/chart/octoconv/.helmignore
  - deploy/chart/octoconv/values.yaml
  - deploy/chart/octoconv/values-local.yaml
  - deploy/chart/octoconv/templates/_helpers.tpl
  - deploy/chart/octoconv/templates/NOTES.txt
  - deploy/chart/octoconv/templates/configmap.yaml
  - deploy/chart/octoconv/templates/secret.yaml
  - deploy/chart/octoconv/templates/postgres.yaml
  - deploy/chart/octoconv/templates/redis.yaml
  - deploy/chart/octoconv/templates/minio.yaml
autonomous: true
requirements: [K8S-01, K8S-02]
must_haves:
  truths:
    - "helm lint deploy/chart/octoconv passes with 0 errors on Helm v4"
    - "helm template renders postgres, redis, minio Deployment+Service (+PVCs) plus a ConfigMap and Secret"
    - "kubectl --context orbstack apply --dry-run=server accepts every rendered manifest against the live cluster"
    - "S3_ENDPOINT in the rendered ConfigMap is the FQDN minio.octoconv.svc.cluster.local:9000 (D-06)"
    - "METRICS_ADDR in the rendered ConfigMap is 0.0.0.0:9090 (D-04)"
    - "MinIO image is a concrete RELEASE tag, never :latest (D-02)"
  artifacts:
    - path: "deploy/chart/octoconv/Chart.yaml"
      provides: "Helm chart metadata (apiVersion v2, name octoconv, no chart deps)"
      contains: "apiVersion: v2"
    - path: "deploy/chart/octoconv/values.yaml"
      provides: "Full per-service value contract consumed by plans 02/03"
      contains: "imageTag"
    - path: "deploy/chart/octoconv/templates/configmap.yaml"
      provides: "Non-secret env contract mirroring docker-compose (S3_ENDPOINT FQDN, METRICS_ADDR 0.0.0.0, DEBT-05 vars)"
      contains: "S3_ENDPOINT"
    - path: "deploy/chart/octoconv/templates/secret.yaml"
      provides: "Dev credentials Secret (DATABASE_URL, API_KEY_SALT, WEBHOOK_SIGNING_SECRET, S3 creds)"
      contains: "API_KEY_SALT"
    - path: "deploy/chart/octoconv/templates/minio.yaml"
      provides: "MinIO Deployment+PVC+Service named minio (repinned tag)"
      contains: "kind: Service"
    - path: "deploy/chart/octoconv/templates/postgres.yaml"
      provides: "Postgres Deployment+PVC+Service named postgres (pg_isready probes)"
      contains: "pg_isready"
  key_links:
    - from: "templates/configmap.yaml"
      to: "minio Service FQDN"
      via: "S3_ENDPOINT value"
      pattern: "minio\\.octoconv\\.svc\\.cluster\\.local:9000"
    - from: "templates/postgres.yaml Service"
      to: "DATABASE_URL host"
      via: "Service metadata.name postgres"
      pattern: "name: postgres"
---

<objective>
Build the Helm chart skeleton and the stateful backbone (Postgres, Redis, MinIO) plus the shared env contract that every app template in plan 02 and the E2E overlay in plan 03 consume. This plan authors the COMPLETE `values.yaml` contract up front (interface-first) so downstream plans only ADD templates that consume these values — they never edit `values.yaml`.

Purpose: Establish the single flat chart (D-01) with hand-rolled stateful deps (D-02), the committed dev-cred trust model (D-03), the FQDN S3_ENDPOINT landmine fix (D-06), and the 0.0.0.0 METRICS_ADDR value (D-04) — all gated offline (helm lint / helm template) and against the live OrbStack cluster (kubectl apply --dry-run=server), which is cheap and available. NO app Deployments, NO hooks, NO NetworkPolicy yet (plan 02); NO live install (plan 03).

Output: `deploy/chart/octoconv/` chart directory with Chart.yaml, values.yaml, values-local.yaml, _helpers.tpl, NOTES.txt, configmap.yaml, secret.yaml, and postgres/redis/minio templates — all lint-clean and server-dry-run-clean.

Zero Go-code changes in this plan (values/YAML only).
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-helm-chart-core/24-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md

<read_first>
- docker-compose.yml — THE env/healthcheck/image contract being ported (every service block: postgres:18, redis:8, minio command `server /data --console-address ":9001"`, healthchecks, dev creds octo/octo-pass/minioadmin)
- .env.example — full env surface with inline-comment semantics
- cmd/api/main.go — confirms METRICS_ADDR os.Getenv default 127.0.0.1:9090 (line 129-132); DATABASE_URL/REDIS_ADDR/S3_* reads
</read_first>

<facts>
Live environment verified 2026-07-14:
- helm v4.2.3 installed; kubectl context `orbstack` is CURRENT; OrbStack k8s node Ready, server v1.34.8+orb1.
- Namespace `octoconv` does NOT yet exist (clean). Cluster is 9 days old with unrelated resources — always scope every command with `--context orbstack` and `-n octoconv`; never rely on current-context.
- OrbStack shares its image store with k8s (no registry) but re-pulls `:latest` — hence `imagePullPolicy: IfNotPresent` + concrete tags (D-02/D-03).
- Concrete MinIO repin tags (verified pullable on Docker Hub, re-confirm at execution): `minio/minio:RELEASE.2025-09-07T16-13-09Z`, `minio/mc:RELEASE.2025-08-13T08-35-41Z`.
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chart scaffold + full values contract + shared config/secret</name>
  <files>deploy/chart/octoconv/Chart.yaml, deploy/chart/octoconv/.helmignore, deploy/chart/octoconv/values.yaml, deploy/chart/octoconv/values-local.yaml, deploy/chart/octoconv/templates/_helpers.tpl, deploy/chart/octoconv/templates/NOTES.txt, deploy/chart/octoconv/templates/configmap.yaml, deploy/chart/octoconv/templates/secret.yaml</files>
  <action>
Create the single flat chart (D-01, no subcharts/deps). `Chart.yaml`: apiVersion v2, name `octoconv`, version 0.1.0, appVersion "1.6", description one-liner, type application.

Author `values.yaml` as the COMPLETE contract for the whole phase (interface-first — plans 02/03 consume these keys and MUST NOT need to edit this file). Include these top-level blocks with sane OrbStack-local defaults:
  - `global:` { imageTag: "dev", namespace: "octoconv" }
  - `image:` { registry: "", pullPolicy: "IfNotPresent" }  (D-03 — IfNotPresent so OrbStack local builds are used, never re-pulled)
  - `metrics:` { addr: "0.0.0.0:9090" }  (D-04)
  - `s3:` { endpoint: "minio.octoconv.svc.cluster.local:9000", bucket: "octoconv", useSSL: "false" }  (D-06 FQDN — string form matching S3_ENDPOINT contract)
  - per-service blocks with `image.repository` + `replicas` + `resources` + per-service tunables: `api:` (repo octoconv-api, replicas 1), `worker:` (octoconv-worker, replicas 1, gracePeriod 150, resources cpu 2/mem 1Gi), `documentWorker:` (octoconv-document-worker, replicas 1, gracePeriod 330, cpu 2/mem 1Gi), `chromiumWorker:` (octoconv-chromium-worker, replicas 1, gracePeriod 90, cpu 2/mem 2Gi, shmSize 256Mi), `webhookWorker:` (octoconv-webhook-worker, replicas 2, gracePeriod 60), `asynqmon:` (enabled false, image hibiken/asynqmon:0.7.2). These grace-period/resource values come straight from D-09/D-10 and are consumed by plan 02; define them now so plan 02 does not touch values.yaml.
  - `postgres:` { image "postgres:18", storage 2Gi, db octo_db, user octo }
  - `redis:` { image "redis:8" }  (no PVC — asynq treats Redis as transient broker, Postgres is system of record)
  - `minio:` { image "minio/minio:RELEASE.2025-09-07T16-13-09Z", mcImage "minio/mc:RELEASE.2025-08-13T08-35-41Z", storage 5Gi, rootUser minioadmin, rootPassword minioadmin }  (D-02 — concrete tags, verify pullable at execution)
  - `e2e:` { image octoconv-e2e, enabled false }  (consumed by plan 03 overlay; declare the key now)

Author `values-local.yaml` (D-03): committed dev credentials exactly like docker-compose (same internal-only trust model — NOT a leak regression). Put the loud-fake secrets here with a top-of-file comment: "DEV-ONLY credentials, committed on purpose to mirror docker-compose's internal trust model — MUST be overridden for any non-local deploy." Include `secrets:` { databaseUrl, apiKeySalt "dev-only-change-me-in-real-deploys", webhookSigningSecret "dev-only-change-me-in-real-deploys", s3AccessKey minioadmin, s3SecretKey minioadmin, postgresPassword octo-pass }.

`.helmignore`: standard ignores (.git, *.md, etc.) — do NOT ignore values-local.yaml (it is meant to be committed).

`_helpers.tpl`: define `octoconv.fullname`, `octoconv.labels` (app.kubernetes.io/name, app.kubernetes.io/part-of: octoconv, app.kubernetes.io/managed-by), and a `octoconv.selectorLabels` helper keyed by a `component` arg. Add a shared `octoconv.commonEnv` template that emits `envFrom` referencing the ConfigMap and Secret (so every Deployment in plan 02 pulls the identical env contract from one place).

`configmap.yaml` (name `octoconv-config`): NON-secret env mirroring docker-compose exactly, keyed for ALL binaries (DEBT-05 requires every process to carry the full retry/timeout surface even when it only enqueues). Include: S3_ENDPOINT (from .Values.s3.endpoint — the FQDN, D-06), S3_BUCKET, S3_USE_SSL, API_ADDR ":8090", METRICS_ADDR (from .Values.metrics.addr = 0.0.0.0:9090, D-04), MAX_UPLOAD_BYTES, MAX_IMAGE_PIXELS, MAX_DOCUMENT_UNCOMPRESSED_BYTES, RATE_LIMIT_IP_RPM, RATE_LIMIT_CLIENT_RPM, WORKER_CONCURRENCY, DOCUMENT_WORKER_CONCURRENCY, HTML_WORKER_CONCURRENCY, WEBHOOK_WORKER_CONCURRENCY, ENGINE_TIMEOUT 120s, IMAGE_MAX_RETRY 4, DOCUMENT_ENGINE_TIMEOUT 300s, DOCUMENT_MAX_RETRY 3, HTML_ENGINE_TIMEOUT 60s, HTML_MAX_RETRY 3, VERAPDF_TIMEOUT 60s, WEBHOOK_PRESIGN_TTL 6h, RECONCILER_QUEUED_STALE_AFTER 90s, RECONCILER_ACTIVE_STALE_AFTER 5m, RECONCILER_SWEEP_INTERVAL 1m, RECONCILER_MAX_RECOVERIES 3, STORAGE_TTL 168h. Use raw values WITHOUT the inline `# comment` suffixes that .env.example carries (k8s env values are literal — the firstField helper only tolerates comments, it does not require them).

`secret.yaml` (name `octoconv-secret`, type Opaque, stringData): DATABASE_URL (postgres://octo:{{ .Values.secrets.postgresPassword }}@postgres.octoconv.svc.cluster.local:5432/octo_db — FQDN host), REDIS_ADDR redis:6379, API_KEY_SALT, WEBHOOK_SIGNING_SECRET, S3_ACCESS_KEY, S3_SECRET_KEY, POSTGRES_PASSWORD. Pull every value from `.Values.secrets.*` so real values live only in values-local.yaml, never in the committed template. Do NOT set WEBHOOK_ALLOW_PRIVATE_IPS / WEBHOOK_ALLOW_INSECURE_HTTP here — they default off (production SSRF guard) and are relaxed ONLY in plan 03's e2e overlay.

`NOTES.txt`: print the in-cluster api URL (http://api.octoconv.svc.cluster.local:8090), a reminder that /metrics is NetworkPolicy-scoped (not public), and the `kubectl --context orbstack -n octoconv get pods` command.
  </action>
  <verify>
    <automated>helm lint deploy/chart/octoconv --values deploy/chart/octoconv/values-local.yaml 2>&1 | grep -q "0 chart(s) linted, 0 chart(s) failed" || helm lint deploy/chart/octoconv --values deploy/chart/octoconv/values-local.yaml</automated>
  </verify>
  <done>helm lint passes; helm template renders a ConfigMap whose S3_ENDPOINT is minio.octoconv.svc.cluster.local:9000 and METRICS_ADDR is 0.0.0.0:9090; Secret pulls all values from .Values.secrets.*; values.yaml carries every per-service block plans 02/03 will consume.</done>
</task>

<task type="auto">
  <name>Task 2: Stateful dependency templates (postgres, redis, minio)</name>
  <files>deploy/chart/octoconv/templates/postgres.yaml, deploy/chart/octoconv/templates/redis.yaml, deploy/chart/octoconv/templates/minio.yaml</files>
  <action>
Hand-roll three flat Deployment+Service (+PVC where stateful) templates (D-02) mirroring docker-compose's non-HA shape. CRITICAL: each Service `metadata.name` MUST be the literal short name (`postgres`, `redis`, `minio`) so the FQDN contract in the ConfigMap/Secret (`<name>.octoconv.svc.cluster.local`) resolves exactly per D-06 — do NOT prefix with the chart fullname on the Service objects (labels may use fullname, but metadata.name stays literal).

`postgres.yaml`: Deployment (replicas 1, strategy Recreate — single PVC writer), image .Values.postgres.image (postgres:18), env POSTGRES_USER/POSTGRES_DB from values and POSTGRES_PASSWORD from the Secret, mount a PVC (.Values.postgres.storage, ReadWriteOnce) at /var/lib/postgresql/data with a subPath (or /var/lib/postgresql per compose — match compose's mount path), readiness+liveness probes = exec `pg_isready -U octo -d octo_db` (interval/timeout mirroring compose 5s/3s). Service name `postgres`, ClusterIP, port 5432.

`redis.yaml`: Deployment (replicas 1), image redis:8, readiness+liveness probes = exec `redis-cli ping`. Service name `redis`, ClusterIP, port 6379. No PVC.

`minio.yaml`: Deployment (replicas 1, strategy Recreate), image .Values.minio.image (the concrete RELEASE tag — verify pullable at execution: `docker pull minio/minio:RELEASE.2025-09-07T16-13-09Z`), args `server /data --console-address ":9001"`, env MINIO_ROOT_USER/MINIO_ROOT_PASSWORD, PVC (.Values.minio.storage, RWO) at /data, readiness probe = HTTP GET /minio/health/ready on 9000 (mc is not in the minio image — use the HTTP health endpoint, not the compose `mc ready local` exec). Service name `minio`, ClusterIP, ports 9000 (api) and 9001 (console). imagePullPolicy from .Values.image.pullPolicy on all three.
  </action>
  <verify>
    <automated>helm template deploy/chart/octoconv --values deploy/chart/octoconv/values-local.yaml 2>&1 | grep -E "name: (postgres|redis|minio)$" | sort -u | tr '\n' ' ' | grep -q "minio" && helm template deploy/chart/octoconv --values deploy/chart/octoconv/values-local.yaml | grep -q "RELEASE.2025-09-07"</automated>
  </verify>
  <done>helm template renders postgres/redis/minio Deployments with probes, PVCs for postgres+minio, and Services named exactly postgres/redis/minio; minio uses the concrete RELEASE tag (no :latest anywhere in the render — verify with `helm template ... | grep -c ':latest'` returning 0).</done>
</task>

<task type="auto">
  <name>Task 3: Offline + server-dry-run gate</name>
  <files>deploy/chart/octoconv/templates/NOTES.txt</files>
  <action>
Wire the full offline safety net and validate against the live cluster (cheap, available — no install). Steps:
  1. `helm lint deploy/chart/octoconv --values deploy/chart/octoconv/values-local.yaml` — 0 failures.
  2. `helm template octoconv deploy/chart/octoconv --namespace octoconv --values deploy/chart/octoconv/values-local.yaml` renders with no template errors; capture output.
  3. Ensure namespace exists for server dry-run (idempotent): `kubectl --context orbstack create namespace octoconv --dry-run=client -o yaml | kubectl --context orbstack apply -f -`.
  4. Pipe the rendered manifests through server-side dry-run: `helm template octoconv deploy/chart/octoconv --namespace octoconv --values deploy/chart/octoconv/values-local.yaml | kubectl --context orbstack -n octoconv apply --dry-run=server -f -` — every object (ConfigMap, Secret, 3 Deployments, 3 Services, 2 PVCs) must be accepted by the live API server with zero schema/validation errors.
  5. Assert no `:latest` image tag leaked: `helm template ... | grep -c ':latest'` returns 0.
  6. Confirm the FQDN + metrics values one more time in the render (S3_ENDPOINT FQDN, METRICS_ADDR 0.0.0.0:9090).
If any kubectl command hangs >120s (OrbStack wedge history), kill it, run the restart runbook (quit OrbStack → pkill helper → open -a OrbStack → poll `docker version`), retry once, and stop loudly if it recurs. Refine NOTES.txt wording if the render surfaced anything unclear.
  </action>
  <verify>
    <automated>kubectl --context orbstack create namespace octoconv --dry-run=client -o yaml | kubectl --context orbstack apply -f - >/dev/null 2>&1; helm template octoconv deploy/chart/octoconv --namespace octoconv --values deploy/chart/octoconv/values-local.yaml | kubectl --context orbstack -n octoconv apply --dry-run=server -f - 2>&1 | grep -vi "created (server dry run)" | grep -i error; test $? -ne 0</automated>
  </verify>
  <done>helm lint clean; helm template clean; server-side dry-run accepts all rendered manifests against the live orbstack cluster with zero errors; zero `:latest` tags in the render.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed chart → git | Dev credentials in values-local.yaml enter version control |
| container registry → cluster | Stateful-dep images pulled/resolved by the node |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Information Disclosure | values-local.yaml dev creds in git | accept | Same internal-only trust model docker-compose already accepts; loud-fake values + explicit DEV-ONLY header comment (D-03). Never used outside local OrbStack. |
| T-24-02 | Tampering | MinIO image supply chain | mitigate | Repin `:latest` → concrete RELEASE tags (D-02); grep-gate asserts zero `:latest` in the render; verify pullable at execution. |
| T-24-03 | Spoofing | Secret template hardcoding real values | mitigate | secret.yaml pulls every value from `.Values.secrets.*`; committed template contains no literal secret. |
| T-24-SC | Tampering | container image installs (postgres/redis/minio/mc/asynqmon) | mitigate | All pinned to concrete tags (postgres:18, redis:8, minio RELEASE, mc RELEASE, asynqmon:0.7.2); no npm/pip/cargo package installs in this phase, so no package-legitimacy checkpoint applies. |
</threat_model>

<verification>
- `helm lint` and `helm template` clean on Helm v4 with values-local.yaml.
- Server-side dry-run against live orbstack accepts all manifests.
- Rendered ConfigMap: S3_ENDPOINT = minio.octoconv.svc.cluster.local:9000 (D-06), METRICS_ADDR = 0.0.0.0:9090 (D-04).
- Zero `:latest` tags rendered (D-02).
- Services named exactly postgres/redis/minio (FQDN contract).
</verification>

<success_criteria>
The chart skeleton, full values contract, shared config/secret, and stateful-dep templates render lint-clean and pass server-side dry-run against the live OrbStack cluster, with the FQDN S3_ENDPOINT and 0.0.0.0 METRICS_ADDR landmine values baked into the ConfigMap. Downstream plans consume values.yaml without editing it.
</success_criteria>

<output>
Create `.planning/phases/24-helm-chart-core/24-01-SUMMARY.md` when done.
</output>
