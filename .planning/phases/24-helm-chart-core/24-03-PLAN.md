---
phase: 24-helm-chart-core
plan: 03
type: execute
wave: 3
depends_on: [24-02]
files_modified:
  - Dockerfile.e2e
  - deploy/chart/octoconv/templates/job-e2e.yaml
  - deploy/chart/octoconv/values-e2e.yaml
autonomous: true
requirements: [K8S-01, K8S-02]
must_haves:
  truths:
    - "helm install octoconv into namespace octoconv reaches all-pods-Ready (api, worker, document-worker, chromium-worker, webhook-worker×2, postgres, redis, minio; migrate+createbucket Jobs Completed)"
    - "the in-cluster E2E Job completes with exit 0"
    - "E2E_WEBHOOK_HOST is the Job pod's own IP via Downward API status.podIP (closes host-gateway landmine); E2E_S3_DIAL_ADDR is omitted (FQDN resolves in-cluster)"
    - "a presigned result URL resolves from the OrbStack host (SC3, FQDN minio.octoconv.svc.cluster.local:9000)"
    - "curl of :9090 /metrics from an unrelated pod is denied by the NetworkPolicy (SC4 negative test)"
    - "helm upgrade re-runs both hook Jobs with no name collision (idempotence)"
    - "all 6 first-party images built sequentially with non-latest tags before install (D-11)"
  artifacts:
    - path: "Dockerfile.e2e"
      provides: "In-cluster E2E test-binary image (go test -c ./internal/e2e + testdata fixtures)"
      contains: "go test -c"
    - path: "deploy/chart/octoconv/templates/job-e2e.yaml"
      provides: "In-cluster E2E Job (Downward API podIP, secret env, gated .Values.e2e.enabled)"
      contains: "status.podIP"
    - path: "deploy/chart/octoconv/values-e2e.yaml"
      provides: "E2E overlay: api SSRF relaxations + rate-limit relaxations + e2e.enabled true"
      contains: "WEBHOOK_ALLOW_PRIVATE_IPS"
  key_links:
    - from: "job-e2e.yaml env E2E_WEBHOOK_HOST"
      to: "the Job pod's own IP"
      via: "Downward API fieldRef status.podIP"
      pattern: "status.podIP"
    - from: "job-e2e.yaml env E2E_BASE_URL"
      to: "api Service"
      via: "http://api.octoconv.svc.cluster.local:8090"
      pattern: "api.octoconv.svc.cluster.local:8090"
    - from: "values-e2e.yaml api env"
      to: "validateCallbackURL SSRF guard"
      via: "WEBHOOK_ALLOW_PRIVATE_IPS/INSECURE_HTTP true"
      pattern: "WEBHOOK_ALLOW_PRIVATE_IPS"
---

<objective>
Ship the in-cluster E2E path (Dockerfile.e2e + E2E Job template + values-e2e overlay) and execute THE LIVE HARD GATE (D-12): a full, unconditional bring-up of the chart on the live OrbStack cluster proving the stack deploys, the in-cluster E2E Job passes, presigned URLs resolve from the host, the metrics NetworkPolicy denies unauthorized scrapes, and `helm upgrade` re-runs hooks idempotently — then teardown.

Purpose: Close K8S-01 (full stack via `helm install`, in-cluster E2E as a Job) and the remaining K8S-02 landmines (Downward-API pod-IP closes both the host-gateway trick AND the S3-dial-redirect at once; FQDN S3_ENDPOINT proven host-reachable). This is the phase's acceptance gate — it is unconditional and must run against the live cluster (verified available: helm v4.2.3, context orbstack current, node Ready).

Output: Dockerfile.e2e, job-e2e.yaml, values-e2e.yaml, and a green live-gate run recorded in the SUMMARY (install → wait → E2E exit 0 → presigned-from-host → NetworkPolicy negative test → upgrade idempotence → teardown).

Go-code diff: ZERO. The E2E suite (internal/e2e/e2e_test.go) is UNCHANGED — E2E_WEBHOOK_HOST and E2E_S3_DIAL_ADDR are already env-driven escape hatches; only the Job manifest wiring changes.

## Scoped E2E run (surfaced, not silent)
The in-cluster E2E Job runs the full suite EXCEPT `TestPDFANonCompliantE2E`, which depends on the compose-only veraPDF soffice fault-injection shim (a read-only bind mount into document-worker in docker-compose.e2e.yml, feeding a crafted trigger.docx). That test validates terminal CLASSIFICATION behaviour of the worker code — a deployment-independent property already gated green by the compose E2E suite in CI. Reproducing the shim in-cluster would require a binaryData ConfigMap + subPath exec mount of a shim script AND a binary marker PDF into document-worker for the e2e overlay — real surface for a fault-injection test whose subject is not the k8s deployment. Exclude it surgically via `go test -skip TestPDFANonCompliantE2E`. TestPDFAExportE2E (the real PDF/A happy path) DOES run in-cluster and proves veraPDF works. If the operator later wants the shim in-cluster too, it is a values-e2e ConfigMap follow-up (noted, not silently dropped).
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/24-helm-chart-core/24-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@deploy/chart/octoconv/values.yaml
@deploy/chart/octoconv/templates/_helpers.tpl

<read_first>
- internal/e2e/e2e_test.go — package doc env contract (E2E_BASE_URL, DATABASE_URL, API_KEY_SALT required; E2E_WEBHOOK_HOST/E2E_S3_DIAL_ADDR optional); the webhook/canary receiver binds 0.0.0.0:0 and is reached at E2E_WEBHOOK_HOST; provisionClient uses DATABASE_URL to create a client row; 10 Test funcs incl TestPDFANonCompliantE2E (shim-dependent, excluded) and TestHTMLNetworkBlockE2E (canary via podIP — works in-cluster)
- docker-compose.e2e.yml — the exact relaxations to port: api WEBHOOK_ALLOW_PRIVATE_IPS=true, WEBHOOK_ALLOW_INSECURE_HTTP=true, RATE_LIMIT_IP_RPM/CLIENT_RPM=6000; extra_hosts host-gateway is MOOT in k8s (podIP replaces it)
- internal/e2e/testdata/ — fixtures the compiled test binary reads at runtime via filepath.Join("testdata", ...); the image MUST ship these next to the binary
- Dockerfile.api — base-image pattern (golang:1.26-bookworm build → debian:bookworm-slim runtime) to mirror for Dockerfile.e2e
- .planning/research/PITFALLS.md Pitfall 7 — OrbStack wedge history + kill-after-120s + restart runbook (bake into the gate)
</read_first>

<facts>
- Context orbstack is current; node Ready; server v1.34.8+orb1; namespace octoconv created (client-dry-run) in plan 01 — plan 03 install uses `--create-namespace` idempotently.
- OrbStack shares its image store with k8s: locally-built non-latest tags are pod-usable with imagePullPolicy IfNotPresent — NO registry push (D-11).
- go test -skip is supported (Go 1.20+; toolchain is 1.26).
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dockerfile.e2e + E2E Job template + values-e2e overlay</name>
  <files>Dockerfile.e2e, deploy/chart/octoconv/templates/job-e2e.yaml, deploy/chart/octoconv/values-e2e.yaml</files>
  <action>
`Dockerfile.e2e` (multi-stage, mirror Dockerfile.api base images): build stage golang:1.26-bookworm — `COPY . .`, then `RUN CGO_ENABLED=0 go test -c -o /out/e2e.test ./internal/e2e`. Runtime stage debian:bookworm-slim with ca-certificates — `COPY --from=build /out/e2e.test /usr/local/bin/e2e.test` and `COPY --from=build /src/internal/e2e/testdata /work/testdata`, `WORKDIR /work` (the compiled test reads ./testdata via filepath.Join). ENTRYPOINT runs the test binary with the skip filter: `["/usr/local/bin/e2e.test", "-test.v", "-test.timeout=20m", "-test.skip=TestPDFANonCompliantE2E"]`. No libvips/LibreOffice/chromium needed — the e2e binary is an HTTP client + Postgres client, it drives the stack, it does not run conversions itself. USER nobody.

`job-e2e.yaml` (gated `{{- if .Values.e2e.enabled }}`): a plain Job (NOT a Helm hook — run on demand), backoffLimit 0, restartPolicy Never, activeDeadlineSeconds ~1500. Container image octoconv-e2e:{tag}, imagePullPolicy IfNotPresent. env:
  - E2E_BASE_URL = http://api.octoconv.svc.cluster.local:8090
  - E2E_WEBHOOK_HOST = valueFrom fieldRef status.podIP (Downward API — the Job pod's own IP; closes the host-gateway landmine; the in-cluster webhook-worker dials this pod directly on the flat pod network)
  - DATABASE_URL, API_KEY_SALT, WEBHOOK_SIGNING_SECRET = valueFrom secretKeyRef octoconv-secret (provisionClient + signature verification need them; API_KEY_SALT MUST equal the api's)
  - E2E_S3_DIAL_ADDR intentionally OMITTED (S3_ENDPOINT is the in-cluster-resolvable MinIO FQDN — no redirect needed; documents the second half of the landmine closure)
Add an initContainer wait-for-api that polls http://api.octoconv.svc.cluster.local:8090/healthz until 200 so the Job starts only against a Ready api.

`values-e2e.yaml` (E2E overlay, layered ON TOP of values-local.yaml — never standalone): set `e2e.enabled: true` and inject api-only SSRF/rate relaxations mirroring docker-compose.e2e.yml. Provide an `api.extraEnv` map (WEBHOOK_ALLOW_PRIVATE_IPS "true", WEBHOOK_ALLOW_INSECURE_HTTP "true", RATE_LIMIT_IP_RPM "6000", RATE_LIMIT_CLIENT_RPM "6000") that the api Deployment renders as additional `env:` entries AFTER envFrom (env overrides envFrom on key collision). NOTE: this requires deployment-api.yaml (plan 02) to already iterate `.Values.api.extraEnv` — if that hook is absent, add the minimal `{{- range $k,$v := .Values.api.extraEnv }}` block to deployment-api.yaml as the ONE small template touch (call it out in the SUMMARY; it is chart-template only, zero Go). These relaxations stay ONLY on the api (validateCallbackURL runs there) — do NOT put them on webhook-worker.
  </action>
  <verify>
    <automated>grep -q "go test -c" Dockerfile.e2e && helm template octoconv deploy/chart/octoconv --namespace octoconv --values deploy/chart/octoconv/values-local.yaml --values deploy/chart/octoconv/values-e2e.yaml 2>&1 | grep -q "status.podIP" && helm template octoconv deploy/chart/octoconv --namespace octoconv --values deploy/chart/octoconv/values-local.yaml --values deploy/chart/octoconv/values-e2e.yaml | grep -q "WEBHOOK_ALLOW_PRIVATE_IPS"</automated>
  </verify>
  <done>Dockerfile.e2e compiles the test binary + ships testdata; job-e2e renders with E2E_WEBHOOK_HOST from status.podIP, E2E_BASE_URL to the api FQDN, secret-sourced DATABASE_URL/API_KEY_SALT/WEBHOOK_SIGNING_SECRET, no E2E_S3_DIAL_ADDR, and a wait-for-api initContainer; values-e2e injects api SSRF+rate relaxations and e2e.enabled true; full render still server-dry-run clean.</done>
</task>

<task type="auto">
  <name>Task 2: THE LIVE HARD GATE (D-12) — build, install, E2E, upgrade, teardown</name>
  <files>deploy/chart/octoconv/values-e2e.yaml</files>
  <action>
Execute the unconditional live gate against the live OrbStack cluster. EVERY helm/kubectl command MUST pin `--kube-context orbstack` (helm) / `--context orbstack` (kubectl) and `-n octoconv` — never rely on current-context. OrbStack discipline (Pitfall 7): if ANY docker/kubectl/helm command is silent >120s, KILL it, run the restart runbook (quit OrbStack → pkill the helper → `open -a OrbStack` → poll `docker version` until responsive), retry ONCE, and STOP LOUDLY if it recurs. Do NOT run this while the docker-compose stack is up.

STEP 0 — Preflight:
  - Confirm the compose stack is STOPPED (D-11): `docker compose ps` shows nothing running (or `docker compose down`). Never run compose + k8s hot simultaneously (three confirmed VM wedges).
  - `kubectl --context orbstack get nodes` → node Ready; `kubectl config current-context` = orbstack.

STEP 1 — Sequential image build (D-11, one at a time, non-latest tags, build context = repo root inside the worktree, NEVER /tmp):
  Build each with `docker build --platform linux/amd64` where the image is amd64-pinned (document-worker), tag `<name>:dev`:
  octoconv-api (-f Dockerfile.api), octoconv-worker (-f Dockerfile.worker), octoconv-document-worker (-f Dockerfile.document-worker, --platform linux/amd64), octoconv-chromium-worker (-f Dockerfile.chromium-worker), octoconv-webhook-worker (-f Dockerfile.webhook-worker), octoconv-e2e (-f Dockerfile.e2e). Build strictly sequentially (no `&`), confirming `docker images | grep octoconv-` lists all 6 with tag dev before proceeding. `docker builder prune` between builds only if disk pressure appears.

STEP 2 — Install:
  `helm install octoconv deploy/chart/octoconv --kube-context orbstack -n octoconv --create-namespace -f deploy/chart/octoconv/values-local.yaml --wait --timeout 10m`. The --wait + post-install migrate/createbucket hooks must complete; if install blocks, inspect `kubectl --context orbstack -n octoconv get pods` and hook Job logs.

STEP 3 — Wait all pods Ready (bounded, generous — first LibreOffice/chromium cold start under amd64 emulation is slow):
  `kubectl --context orbstack -n octoconv wait --for=condition=Ready pod --all --timeout=600s`. Confirm: api, worker, document-worker, chromium-worker, webhook-worker×2, postgres, redis, minio all Ready; migrate + createbucket Jobs show Completed (`kubectl get jobs`).

STEP 4 — In-cluster E2E Job (SC1):
  Render+apply the E2E Job with the overlay: `helm upgrade octoconv deploy/chart/octoconv --kube-context orbstack -n octoconv -f deploy/chart/octoconv/values-local.yaml -f deploy/chart/octoconv/values-e2e.yaml --wait --timeout 10m` (this both enables the api relaxations and creates the gated Job) — OR render just the Job via `helm template ... -f values-e2e.yaml -s templates/job-e2e.yaml | kubectl --context orbstack -n octoconv apply -f -`. Then `kubectl --context orbstack -n octoconv wait --for=condition=complete job/<e2e-job> --timeout=1200s` and confirm exit 0 via `kubectl logs`. The suite exercises document/cross-format/PDFA/HTML/image conversions, webhook delivery+signature (podIP receiver), and the chromium network-block canary — all in-cluster.

STEP 5 — Presigned-from-host (SC3, D-06): create one job through the api (port-forward api or exec a curl pod), obtain a download_url (host = minio.octoconv.svc.cluster.local:9000), and `curl` it FROM the OrbStack host — it must return 200 (OrbStack resolves cluster.local from the Mac host). This proves the FQDN closes the landmine for the host consumer class, not just in-cluster.

STEP 6 — NetworkPolicy negative test (SC4, D-04): from an unrelated throwaway pod in the namespace (`kubectl --context orbstack -n octoconv run probe --rm -it --image=busybox --restart=Never -- sh -c "wget -T5 -qO- http://<a-worker-pod-ip>:9090/metrics"`), the scrape MUST fail/time out (denied). Also confirm kubelet probes still pass (pods stay Ready) — if OrbStack's CNI blocks node-origin probes, add the node/hostNetwork allow rule to networkpolicy-metrics and re-verify.

STEP 7 — Upgrade idempotence (SC2): run `helm upgrade octoconv ... -f values-local.yaml --wait --timeout 10m` a second time; the pre-existing completed migrate/createbucket hook Jobs must be recreated (before-hook-creation delete policy) and re-complete with NO name-collision failure. Confirm `helm status octoconv` = deployed and pods stay Ready.

STEP 8 — Teardown (D-12): `helm uninstall octoconv --kube-context orbstack -n octoconv`. Note whether PVCs (postgres/minio) remain (`kubectl --context orbstack -n octoconv get pvc`) — D-12 says PVCs may remain; record the observed behaviour (do NOT assume compose-volume-like persistence). Optionally leave the namespace.

Record the full timestamped gate outcome in the SUMMARY. If ANY step fails, the phase is not done — capture logs and stop.
  </action>
  <verify>
    <automated>test -f deploy/chart/octoconv/values-e2e.yaml && grep -q "e2e" deploy/chart/octoconv/values-e2e.yaml && helm lint deploy/chart/octoconv --values deploy/chart/octoconv/values-local.yaml --values deploy/chart/octoconv/values-e2e.yaml 2>&1 | grep -q "0 chart(s) failed"</automated>
  </verify>
  <done>Live gate green end-to-end: 6 images built sequentially; helm install reaches all-pods-Ready with migrate+createbucket Completed; in-cluster E2E Job exits 0; presigned URL resolves from the OrbStack host; :9090 scrape from an unrelated pod is denied while pods stay Ready; helm upgrade re-runs hooks with no collision; teardown clean with PVC persistence behaviour recorded — all with zero Go-code changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-cluster E2E Job → api/webhook | Test-driven traffic; SSRF guard relaxed ONLY on api for the overlay |
| OrbStack host → cluster | Presigned URL dialed from the Mac host into MinIO |
| unrelated pod → :9090 | Unauthorized metrics scrape attempt (negative test) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-09 | Spoofing/SSRF | api WEBHOOK_ALLOW_PRIVATE_IPS relaxation | accept | Relaxation lives ONLY in values-e2e overlay, only on api, only for the in-cluster E2E receiver (podIP, RFC1918 pod network); production values keep the guard default-on. Mirrors the already-accepted docker-compose.e2e.yml trust model. Loopback/link-local stay hard-blocked regardless. |
| T-24-10 | Information Disclosure | :9090 scrape from an unauthorized pod | mitigate | networkpolicy-metrics (plan 02) denies it; this plan's STEP 6 negatively tests it live — a successful scrape is a hard-gate FAILURE, not a warning. |
| T-24-11 | Tampering | OrbStack daemon wedge mid-gate corrupting state | mitigate | kill-after-120s rule + proven restart runbook + strictly sequential builds + compose-stopped precondition (D-11/Pitfall 7). |
| T-24-12 | Tampering | build context xattr failure from /tmp | mitigate | Stage all build contexts inside the worktree, never /tmp (Pitfall 7 / 23-03 precedent). |
| T-24-SC | Tampering | e2e/mc image supply chain | mitigate | e2e image built locally from repo source; mc pinned to concrete RELEASE tag (plan 01). No npm/pip/cargo installs — no package-legitimacy checkpoint applies. |
</threat_model>

<verification>
- helm lint clean with both value files layered.
- LIVE (unconditional): install reaches all-pods-Ready; migrate+createbucket Completed; in-cluster E2E Job exit 0 (SC1); presigned URL 200 from the OrbStack host (SC3); :9090 scrape denied from an unrelated pod while pods stay Ready (SC4); helm upgrade re-runs hooks with no collision (SC2); teardown records PVC behaviour.
- E2E_WEBHOOK_HOST=status.podIP and no E2E_S3_DIAL_ADDR (both halves of the host-gateway/S3-dial landmine closed, K8S-02).
- Zero Go-code changes; compose E2E path remains untouched (CI stays green).
</verification>

<success_criteria>
`helm install octoconv ./deploy/chart` on the live OrbStack cluster brings the full stack to all-pods-Ready and the in-cluster E2E Job passes with exit 0; presigned URLs resolve from the host via the FQDN; the metrics NetworkPolicy denies unauthorized scrapes; `helm upgrade` re-runs the migrate/createbucket hooks idempotently; teardown is clean. All four SEED-004 landmines are closed with zero Go-code changes.
</success_criteria>

<output>
Create `.planning/phases/24-helm-chart-core/24-03-SUMMARY.md` when done, including the timestamped live-gate transcript (install/wait/E2E/presigned/negative-test/upgrade/teardown) and the observed PVC persistence behaviour.
</output>
