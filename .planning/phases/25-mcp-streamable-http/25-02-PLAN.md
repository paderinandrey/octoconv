---
phase: 25-mcp-streamable-http
plan: 02
type: execute
wave: 2
depends_on: [25-01]
files_modified:
  - Dockerfile.mcp-http
  - deploy/chart/octoconv/values.yaml
  - deploy/chart/octoconv/templates/deployment-mcp-http.yaml
  - deploy/chart/octoconv/templates/service-mcp-http.yaml
  - README.md
autonomous: true
requirements: [MCPH-01]
must_haves:
  truths:
    - "Dockerfile.mcp-http builds the cmd/mcp-http binary in the same multi-stage shape as Dockerfile.api and runs as USER nobody exposing :8070 (D-05)"
    - "helm install with mcpHttp.enabled renders a single-replica mcp-http Deployment + ClusterIP Service on :8070, gated so mcpHttp.enabled=false renders neither (D-05)"
    - "The mcp-http pod receives OCTOCONV_BASE_URL (api FQDN) and MCP_HTTP_ADDR only — no API key env, no DB/S3 secret env (D-06, D-03)"
    - "helm lint + helm template + kubectl apply --dry-run=server pass with the mcp-http templates both enabled and disabled"
    - "README documents the MCP HTTP transport config (OCTOCONV_BASE_URL, MCP_HTTP_ADDR, per-request ApiKey header)"
  artifacts:
    - path: "Dockerfile.mcp-http"
      provides: "container image for cmd/mcp-http"
      contains: "cmd/mcp-http"
    - path: "deploy/chart/octoconv/templates/deployment-mcp-http.yaml"
      provides: "gated single-replica mcp-http Deployment with /healthz probes"
      contains: "mcpHttp.enabled"
    - path: "deploy/chart/octoconv/templates/service-mcp-http.yaml"
      provides: "ClusterIP Service on 8070"
      contains: "8070"
  key_links:
    - from: "deployment-mcp-http.yaml"
      to: "api Service FQDN"
      via: "OCTOCONV_BASE_URL env = http://api.<ns>.svc.cluster.local:8090"
      pattern: "OCTOCONV_BASE_URL"
    - from: "service-mcp-http.yaml"
      to: "mcp-http pod"
      via: "ClusterIP selector on component=mcp-http, port 8070"
      pattern: "component: mcp-http"
---

<objective>
Package `cmd/mcp-http` for the cluster: a `Dockerfile.mcp-http` mirroring `Dockerfile.api`'s multi-stage shape, plus chart additions (a gated single-replica Deployment and a ClusterIP Service on :8070) and the `mcpHttp` values block. Wire only the env the pod actually needs — `OCTOCONV_BASE_URL` and `MCP_HTTP_ADDR` — never an API key and never the DB/S3 secret surface (D-06, zero-privilege). Prove it all offline (helm lint / template gating / server dry-run) and document the HTTP transport in the README.

Purpose: Serves MCPH-01's "container + Service in the chart, gated by mcpHttp.enabled, Stateless, single replica" clause (D-05).
Output: Dockerfile.mcp-http, mcp-http Deployment + Service templates, mcpHttp values, README section.
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-mcp-streamable-http/25-CONTEXT.md

# Multi-stage pattern to mirror:
@Dockerfile.api

# Chart conventions — copy the structure, helpers, and label discipline:
@deploy/chart/octoconv/values.yaml
@deploy/chart/octoconv/templates/_helpers.tpl
@deploy/chart/octoconv/templates/deployment-api.yaml
@deploy/chart/octoconv/templates/service-api.yaml

<existing-behavior>
- octoconv.labels (helpers) applies common labels to every object; selectorLabels is keyed by a `component` dict arg. deployment-api.yaml is the reference for template + selector + probe wiring.
- `octoconv.io/tier: app` is added ONLY on pod templates that must be reachable by the metrics NetworkPolicy podSelector. mcp-http exposes no /metrics this plan (D-05 makes metrics optional/nice-to-have) — do NOT add the tier label, matching the postgres/redis/minio boundary note in _helpers.tpl.
- octoconv.commonEnv (envFrom configmap+secret) injects the FULL DB/S3/secret surface. mcp-http MUST NOT use commonEnv — it needs only OCTOCONV_BASE_URL + MCP_HTTP_ADDR. Use an explicit `env:` block instead (D-06).
- values.yaml is described as the "complete per-service contract"; mcp-http was deferred to Phase 25, so this plan ADDS the `mcpHttp` block to values.yaml (this is the sanctioned edit — Phase 24 plan-02/03 did not own mcp-http).
- 24-03 SUMMARY: `helm install` must NOT use `--wait` with this chart (post-install createbucket hook chicken-egg). Not relevant to offline gates here but relevant to Plan 03.
</existing-behavior>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dockerfile.mcp-http + mcpHttp values block</name>
  <files>Dockerfile.mcp-http, deploy/chart/octoconv/values.yaml</files>
  <action>Create Dockerfile.mcp-http as a two-stage build mirroring Dockerfile.api exactly: `golang:1.26-bookworm` build stage with `CGO_ENABLED=0 go build -o /out/mcp-http ./cmd/mcp-http`, then a `debian:bookworm-slim` runtime with only `ca-certificates`, `USER nobody`, `EXPOSE 8070`, `ENTRYPOINT ["/usr/local/bin/mcp-http"]`. The mcp-http binary shells out to nothing, so the slim base with no extra tools is sufficient (same rationale as api). Add an `mcpHttp` block to values.yaml near the other per-service app blocks: `enabled: true` (default true in the committed values per D-05 "default true in values-local" — keep it true so `helm install` brings it up by default), `image.repository: "octoconv-mcp-http"`, `replicas: 1` (single replica, PITFALLS P6 / D-05), and `addr: ":8070"` (MCP_HTTP_ADDR, D-01). Also add `mcpHttp.baseURL: "http://api.octoconv.svc.cluster.local:8090"` so OCTOCONV_BASE_URL is values-driven and matches the s3.endpoint FQDN convention already in values.yaml (D-06). Keep the file comment style consistent with the existing header.</action>
  <verify>
    <automated>docker build -f Dockerfile.mcp-http -t octoconv-mcp-http:planlint . >/dev/null 2>&1 && echo BUILD_OK; grep -q "mcpHttp" deploy/chart/octoconv/values.yaml && echo VALUES_OK</automated>
  </verify>
  <done>Dockerfile.mcp-http builds the cmd/mcp-http image as USER nobody on :8070; values.yaml carries the mcpHttp block (enabled, image.repository, replicas:1, addr, baseURL).</done>
</task>

<task type="auto">
  <name>Task 2: mcp-http Deployment + Service templates, README, offline gates</name>
  <files>deploy/chart/octoconv/templates/deployment-mcp-http.yaml, deploy/chart/octoconv/templates/service-mcp-http.yaml, README.md</files>
  <action>Create deployment-mcp-http.yaml modeled on deployment-api.yaml but: wrap the entire manifest in `{{- if .Values.mcpHttp.enabled }} ... {{- end }}` (gating, D-05); metadata.name `mcp-http`; component selector `mcp-http` via octoconv.selectorLabels; `replicas: {{ .Values.mcpHttp.replicas }}` (1); DO NOT add `octoconv.io/tier: app` (no metrics NP this plan). DO NOT use octoconv.commonEnv — instead an explicit `env:` block with exactly `OCTOCONV_BASE_URL: {{ .Values.mcpHttp.baseURL | quote }}` and `MCP_HTTP_ADDR: {{ .Values.mcpHttp.addr | quote }}` (D-06, no key/secret env). Container image `"{{ .Values.mcpHttp.image.repository }}:{{ .Values.global.imageTag }}"`, `imagePullPolicy: {{ .Values.image.pullPolicy }}`; single containerPort named `http` = 8070; readiness + liveness `httpGet /healthz` on 8070 (the cheap endpoint from 25-01 Task 3, D-05) with the same tolerant/tight split shape api uses; terminationGracePeriodSeconds 30 (request/response service, no long in-flight work). Create service-mcp-http.yaml modeled on service-api.yaml: `type: ClusterIP`, selector on component=mcp-http, port 8070 → targetPort http, gated by the same `mcpHttp.enabled`. Add a README section "MCP over HTTP (in-cluster)" documenting: the endpoint is streamable-HTTP via cmd/mcp-http on MCP_HTTP_ADDR (:8070); auth is per-request `Authorization: ApiKey <key>` pass-through (the pod stores no key); OCTOCONV_BASE_URL points at the api Service; results are presigned-only in HTTP mode (D-04); ClusterIP-only, reach it via `kubectl port-forward`. Run offline gates: `helm lint`, `helm template` with mcpHttp.enabled=true (assert the Deployment+Service render and carry OCTOCONV_BASE_URL but NO secretRef/DATABASE_URL), `helm template` with mcpHttp.enabled=false (assert neither renders), and `kubectl apply --dry-run=server` on the full render.</action>
  <verify>
    <automated>helm lint deploy/chart/octoconv && helm template deploy/chart/octoconv --set mcpHttp.enabled=true | grep -q "name: mcp-http" && helm template deploy/chart/octoconv --set mcpHttp.enabled=true | grep -A40 "name: mcp-http" | grep -q "OCTOCONV_BASE_URL" && ! ( helm template deploy/chart/octoconv --set mcpHttp.enabled=false | grep -q "name: mcp-http" ) && echo GATES_OK</automated>
  </verify>
  <done>Deployment+Service render only when mcpHttp.enabled; pod env is OCTOCONV_BASE_URL + MCP_HTTP_ADDR with no secret/DB env; /healthz probes on 8070; ClusterIP :8070; README documents the HTTP transport; helm lint + dry-run clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-cluster pod → mcp-http Service | Any pod that can route to the ClusterIP can reach the endpoint; auth still enforced per-request (Plan 01) |
| chart values → pod env | Committed values must not carry secrets for this pod |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-05 | Information disclosure | mcp-http pod env | mitigate | Explicit env block (OCTOCONV_BASE_URL + MCP_HTTP_ADDR only); no commonEnv/secretRef — offline gate asserts absence of DATABASE_URL/secretRef (D-06) |
| T-25-06 | Elevation of privilege | container runtime | mitigate | USER nobody, slim base, no extra tooling (mirrors Dockerfile.api) |
| T-25-07 | Spoofing | Service exposure | accept | ClusterIP-only (no Ingress); reachable in-cluster; a restricting NetworkPolicy is optional this milestone (D-05) and — per 24-03 — unenforced on OrbStack's CNI regardless. Per-request ApiKey auth (Plan 01) is the active control |
| T-25-SC | Tampering | base images | accept | Reuses already-pinned golang:1.26-bookworm / debian:bookworm-slim; no new package installs |
</threat_model>

<verification>
- `docker build -f Dockerfile.mcp-http` succeeds (image built for Plan 03's live gate).
- `helm lint` clean; template gating verified both ways; `kubectl apply --dry-run=server` clean.
- No secretRef/DATABASE_URL in the rendered mcp-http pod.
- Compose path untouched (no docker-compose.yml / Go changes) — CI regression gate unaffected.
</verification>

<success_criteria>
- MCPH-01 (deployment clause): mcp-http ships as a chart template gated by mcpHttp.enabled, with its ClusterIP Service, single replica, /healthz probes, and key-free env (D-05, D-06).
</success_criteria>

<output>
Create `.planning/phases/25-mcp-streamable-http/25-02-SUMMARY.md` when done. Note the exact image tag/name and the port-forward command so Plan 03 can drive the live session.
</output>
