---
phase: 27-keda-autoscaling
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - deploy/chart/octoconv/templates/prometheus.yaml
  - deploy/chart/octoconv/templates/service-api.yaml
  - deploy/chart/octoconv/templates/networkpolicy-metrics.yaml
  - deploy/chart/octoconv/templates/scaledobject-image.yaml
  - deploy/chart/octoconv/templates/scaledobject-document.yaml
  - deploy/chart/octoconv/templates/scaledobject-html.yaml
  - deploy/chart/octoconv/values.yaml
  - deploy/chart/octoconv/values-local.yaml
autonomous: true
requirements: [KEDA-02]
must_haves:
  truths:
    - "With keda.enabled=false and prometheus.enabled=false (defaults), helm template renders zero ScaledObjects and zero Prometheus objects — base chart still installs on a cluster without KEDA CRDs"
    - "With the local overlay (both enabled), helm template renders exactly three ScaledObjects (image/document/html) and the Prometheus Deployment+ConfigMap+Service"
    - "The api Service exposes a metrics port 9090 so Prometheus can scrape it"
    - "networkpolicy-metrics.yaml admits the in-chart Prometheus pod (octoconv namespace, component=prometheus) to :9090 — the monitoring-namespace mismatch is fixed"
    - "webhook-worker has NO ScaledObject at all (D-09) and keeps replicas: 2"
    - "Each ScaledObject scales its own worker from minReplicaCount 0 on sum(octoconv_queue_depth{queue,state=~pending|active}) via the Prometheus scaler"
  artifacts:
    - path: "deploy/chart/octoconv/templates/prometheus.yaml"
      provides: "in-chart Prometheus Deployment + scrape ConfigMap + Service, gated prometheus.enabled"
      contains: "prom/prometheus"
    - path: "deploy/chart/octoconv/templates/scaledobject-image.yaml"
      provides: "KEDA ScaledObject for the image worker"
      contains: "kind: ScaledObject"
    - path: "deploy/chart/octoconv/values.yaml"
      provides: "keda.* and prometheus.* value blocks (default enabled:false)"
      contains: "keda:"
  key_links:
    - from: "scaledobject-*.yaml"
      to: "prometheus service :9090"
      via: "trigger serverAddress FQDN"
      pattern: "serverAddress:.*prometheus"
    - from: "prometheus.yaml scrape config"
      to: "api :9090/metrics"
      via: "static scrape target"
      pattern: "api.*:9090"
    - from: "networkpolicy-metrics.yaml"
      to: "prometheus pod"
      via: "namespaceSelector + podSelector component=prometheus on :9090"
      pattern: "component: prometheus"
---

<objective>
KEDA-02 (chart half): Author the offline-verifiable Kubernetes manifests for KEDA autoscaling — a minimal in-chart Prometheus (Deployment+ConfigMap+Service), the api Service metrics-port addition, the networkpolicy-metrics.yaml Prometheus-admission fix, and three per-class ScaledObjects (image/document/html) — all gated behind `keda.enabled`/`prometheus.enabled` flags. The live cluster proof is plan 03; this plan lands correct, lint-clean, dry-run-clean templates.

Purpose: Give KEDA a metric to scale on. Prometheus scrapes api's relocated `octoconv_queue_depth` (plan 01); KEDA's Prometheus scaler queries Prometheus; the ScaledObjects drive each worker Deployment 0→N→0. Two load-bearing gaps found in RESEARCH.md MUST be fixed here, not discovered at the live gate: (1) networkpolicy-metrics.yaml currently admits :9090 only from a `monitoring` namespace that D-11 never creates; (2) service-api.yaml exposes only :8090, not the :9090 Prometheus needs.

Output: 3 new templates (prometheus.yaml, scaledobject-{image,document,html}.yaml), 3 modified files (service-api.yaml, networkpolicy-metrics.yaml, values.yaml), and values-local.yaml enablement. No Go changes.
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-keda-autoscaling/27-CONTEXT.md
@.planning/phases/27-keda-autoscaling/27-RESEARCH.md
@.planning/phases/27-keda-autoscaling/27-PATTERNS.md

<interfaces>
<!-- Chart conventions extracted from the codebase. Use directly. -->

Helm helpers (deploy/chart/octoconv/templates/_helpers.tpl):
```
{{- include "octoconv.labels" . }}                                          # common labels (NO tier label)
{{- include "octoconv.selectorLabels" (dict "component" "<name>" "root" $) }}  # selector labels keyed by component
{{- include "octoconv.commonEnv" . }}                                       # envFrom configmap+secret (app pods only)
```

Feature-gate pattern (deployment-mcp-http.yaml:16, networkpolicy-mcp-http.yaml):
```
{{- if .Values.<component>.enabled }} ... {{- end }}
```

Worker Deployment names (scaleTargetRef targets — confirmed):
```
worker            (image class,    grace 150s)
document-worker   (document class, grace 330s)
chromium-worker   (html class,     grace 90s)
webhook-worker    (FIXED replicas:2, NO ScaledObject — D-09)
```

Existing networkpolicy-metrics.yaml (the BROKEN rule to replace, lines 44-50):
```
- from:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: monitoring   # <-- never created under D-11
  ports:
    - protocol: TCP
      port: 9090
```
The podSelector at the top matches exactly `octoconv.io/tier: app`; the :8090 rule (lines 51-57) uses `namespaceSelector: kubernetes.io/metadata.name: {{ .Values.global.namespace }}` — mirror its namespaceSelector shape for the fix.

Existing service-api.yaml ports block (lines 19-22): only `name: http, port: 8090, targetPort: 8090`.

values.yaml conventions: `global.namespace: "octoconv"`, `image.pullPolicy`, per-service blocks like `mcpHttp:` (enabled + image.repository + config keys).

PromQL signal (D-04, confirmed): `sum(octoconv_queue_depth{queue="image", state=~"pending|active"})` — pending+active only, never retry/scheduled/archived.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: In-chart Prometheus + api metrics-port + NetworkPolicy fix</name>
  <files>deploy/chart/octoconv/templates/prometheus.yaml, deploy/chart/octoconv/templates/service-api.yaml, deploy/chart/octoconv/templates/networkpolicy-metrics.yaml, deploy/chart/octoconv/values.yaml, deploy/chart/octoconv/values-local.yaml</files>
  <read_first>
    - deploy/chart/octoconv/templates/redis.yaml (combined Deployment+Service single-file pattern — the analog for prometheus.yaml)
    - deploy/chart/octoconv/templates/configmap.yaml (ConfigMap shape/labels for the scrape config)
    - deploy/chart/octoconv/templates/deployment-mcp-http.yaml (gate pattern; NO octoconv.io/tier:app precedent for a non-app pod)
    - deploy/chart/octoconv/templates/service-api.yaml (ports block to extend)
    - deploy/chart/octoconv/templates/networkpolicy-metrics.yaml (full file incl. header rationale; the :8090 rule to mirror; Pitfall 1 warning)
    - deploy/chart/octoconv/templates/_helpers.tpl (labels/selectorLabels helpers + tier-label boundary note)
    - deploy/chart/octoconv/values.yaml (per-service block conventions; global.namespace)
    - deploy/chart/octoconv/values-local.yaml (dev overlay; currently secrets-only)
    - .planning/phases/27-keda-autoscaling/27-RESEARCH.md (Pitfall 1 NetworkPolicy fix; "Minimal In-Chart Prometheus"; Pitfall 4 ignoreNullValues)
    - .planning/phases/27-keda-autoscaling/27-PATTERNS.md (prometheus.yaml / networkpolicy / service-api assignments — exact target YAML)
  </read_first>
  <action>
Create deploy/chart/octoconv/templates/prometheus.yaml as one file containing a Deployment, a ConfigMap, and a Service, ALL wrapped in `{{- if .Values.prometheus.enabled }} ... {{- end }}`. Deployment: name `prometheus`, image `{{ .Values.prometheus.image }}` (`prom/prometheus:v3.13.1`), `imagePullPolicy: {{ .Values.image.pullPolicy }}`, replicas 1, containerPort 9090 (Prometheus's own HTTP/API port), resource requests/limits from `.Values.prometheus.resources`, and a volume mounting the ConfigMap's `prometheus.yml` at `/etc/prometheus/prometheus.yml` (standard `--config.file` location). Use `include "octoconv.labels" .` + `app.kubernetes.io/component: prometheus` on metadata and pod template, and `include "octoconv.selectorLabels" (dict "component" "prometheus" "root" $)` for the selector. CRITICAL (Pitfall 1): do NOT add `octoconv.io/tier: app` to the Prometheus pod — it is a scrape SOURCE, not a metrics target; giving it the tier label would subject it to the metrics NetworkPolicy's own :9090 ingress restriction. ConfigMap: name `prometheus-config`, key `prometheus.yml` holding a minimal scrape config with a single static job scraping `api.{{ .Values.global.namespace }}.svc.cluster.local:9090` at a modest interval (e.g. global scrape_interval 15s; one job named `octoconv-api`, metrics_path `/metrics`, static_configs target the api FQDN:9090). Service: name `prometheus`, ClusterIP, port 9090 targetPort 9090, selector via the component=prometheus selectorLabels — literal un-prefixed name so `prometheus.{{ .Values.global.namespace }}.svc.cluster.local:9090` resolves (D-06 FQDN convention).

Modify deploy/chart/octoconv/templates/service-api.yaml: add a second port entry `name: metrics, port: 9090, targetPort: 9090` after the existing `http` 8090 entry, so Prometheus's scrape target has a reachable Service port (RESEARCH.md "api-Service exposure gap"). Add a one-line comment noting this is the Prometheus scrape port.

Modify deploy/chart/octoconv/templates/networkpolicy-metrics.yaml: REPLACE the broken `monitoring`-namespace :9090 rule (lines 44-50) with a single `from` entry combining a `namespaceSelector` (`kubernetes.io/metadata.name: {{ .Values.global.namespace }}`) AND a `podSelector` (`app.kubernetes.io/component: prometheus`) — the two are ANDed inside one `from` entry (standard "this specific pod in this namespace" selector). Keep the :8090 rule unchanged. Update the file's header comment block: the current text says "allowed ONLY from a monitoring namespace ... Prometheus lands in Phase 27" — replace that with the accurate statement that :9090 is now admitted from the in-chart Prometheus pod (component=prometheus) in the octoconv namespace, and retain the existing note that OrbStack ships no NetworkPolicy controller so this is environmentally unenforced locally but correct for any conformant CNI (verify via `helm template` rendering, not a live negative test — same limitation Phase 24 recorded).

Add value blocks to deploy/chart/octoconv/values.yaml (default DISABLED per D-10): a `prometheus:` block with `enabled: false`, `image: "prom/prometheus:v3.13.1"`, and `resources` (requests cpu 100m / memory 128Mi, limits cpu 500m / memory 256Mi). Also add the `keda:` block in this task or task 2 — put it in task 2 to keep concerns separate; here add ONLY `prometheus:`. In deploy/chart/octoconv/values-local.yaml add `prometheus:\n  enabled: true` (top-level override layered by `-f values-local.yaml`).
  </action>
  <verify>
    <automated>helm lint deploy/chart/octoconv && test $(helm template octoconv deploy/chart/octoconv | grep -c "kind: Deployment.*prometheus\|component: prometheus") -ge 0 && helm template octoconv deploy/chart/octoconv | grep -qv "prom/prometheus"; helm template octoconv deploy/chart/octoconv -f deploy/chart/octoconv/values-local.yaml | grep -q "prom/prometheus" && helm template octoconv deploy/chart/octoconv -f deploy/chart/octoconv/values-local.yaml | grep -q "component: prometheus"</automated>
  </verify>
  <acceptance_criteria>
    - `helm lint deploy/chart/octoconv` passes.
    - `helm template octoconv deploy/chart/octoconv` (defaults) renders NO `prom/prometheus` image and NO object labeled `component: prometheus` (prometheus.enabled=false gate holds).
    - `helm template ... -f deploy/chart/octoconv/values-local.yaml` renders the Prometheus Deployment (`prom/prometheus:v3.13.1`), the `prometheus-config` ConfigMap with a scrape target `api.octoconv.svc.cluster.local:9090`, and a `prometheus` Service on port 9090.
    - The Prometheus pod template does NOT carry `octoconv.io/tier: app`.
    - Rendered `service-api.yaml` output contains a `metrics` port `9090`.
    - Rendered `networkpolicy-metrics.yaml` :9090 ingress rule uses `namespaceSelector` matching `.Values.global.namespace` AND `podSelector` `app.kubernetes.io/component: prometheus`; the string `monitoring` no longer appears in that rule.
  </acceptance_criteria>
  <done>Prometheus scrapes api:9090, the api Service exposes :9090, and the NetworkPolicy admits Prometheus — all gated and rendering only when prometheus.enabled=true.</done>
</task>

<task type="auto">
  <name>Task 2: Three per-class ScaledObjects + keda values block (D-04..D-10)</name>
  <files>deploy/chart/octoconv/templates/scaledobject-image.yaml, deploy/chart/octoconv/templates/scaledobject-document.yaml, deploy/chart/octoconv/templates/scaledobject-html.yaml, deploy/chart/octoconv/values.yaml, deploy/chart/octoconv/values-local.yaml</files>
  <read_first>
    - deploy/chart/octoconv/templates/deployment-worker.yaml (scaleTargetRef name `worker`; label conventions)
    - deploy/chart/octoconv/templates/deployment-webhook-worker.yaml (Pitfall 2 header — confirms NO ScaledObject for webhook; do NOT create one)
    - deploy/chart/octoconv/templates/deployment-mcp-http.yaml (gate pattern for keda.enabled)
    - deploy/chart/octoconv/values.yaml (block conventions; add keda: block)
    - deploy/chart/octoconv/values-local.yaml (add keda.enabled: true)
    - .planning/phases/27-keda-autoscaling/27-RESEARCH.md (Pattern 3 full ScaledObject YAML; PromQL signal shape; Pitfall 4 ignoreNullValues; Pitfall 5 metric-name discovery)
    - .planning/phases/27-keda-autoscaling/27-PATTERNS.md (scaledobject assignment — templated values, gate, labels)
  </read_first>
  <action>
Create three ScaledObject templates (or, at executor discretion, one templated file looping per class — but three files is simplest and matches the per-service flat-chart convention). Each wrapped in `{{- if .Values.keda.enabled }} ... {{- end }}` (D-10). Each `apiVersion: keda.sh/v1alpha1`, `kind: ScaledObject`, `metadata.name` = `worker-<class>-scaledobject`, `metadata.namespace: {{ .Values.global.namespace }}`, labels via `include "octoconv.labels" .` + `app.kubernetes.io/component: worker`. Spec per class:
- image → scaleTargetRef.name `worker`; document → `document-worker`; html → `chromium-worker`.
- `minReplicaCount: 0`; `maxReplicaCount: {{ .Values.keda.<class>.maxReplicaCount }}` (D-06: image 4 / document 2 / html 2).
- `pollingInterval: {{ .Values.keda.<class>.pollingInterval }}`; `cooldownPeriod: {{ .Values.keda.<class>.cooldownPeriod }}` (D-08 demo values: image 5/60 fast-bursty; document 15/120 long-task; html 10/90).
- `fallback: { failureThreshold: 3, replicas: 1 }` (D-07 — hold 1 worker if Prometheus is unreachable after 3 failed polls; failureThreshold 3 is discretion).
- `triggers:` one `type: prometheus` with `serverAddress: http://prometheus.{{ .Values.global.namespace }}.svc.cluster.local:9090`, `query: sum(octoconv_queue_depth{queue="<class-queue>", state=~"pending|active"})` where class-queue is `image`/`document`/`html` (D-04 — pending+active ONLY, never retry/scheduled/archived), and `threshold: {{ .Values.keda.<class>.threshold | quote }}` (D-05: image "5" / document "1" / html "2"). Explicitly set `ignoreNullValues: "true"` in the trigger metadata with a one-line comment: an empty PromQL result (genuinely empty queue) is treated as 0, NOT a scaler failure — setting it false would make a routine api restart force fallback.replicas across all classes (Pitfall 4). Do NOT set `activationThreshold` (leave default 0 so any pending/active task wakes the class from zero — the literal per-class 0→1 proof).
Do NOT create a scaledobject-webhook.yaml (D-09/Pitfall 2 — webhook-worker is hard-excluded).

Add the `keda:` block to deploy/chart/octoconv/values.yaml (default `enabled: false`, D-10) with nested per-class keys `image`/`document`/`html`, each carrying `threshold`, `maxReplicaCount`, `pollingInterval`, `cooldownPeriod` set to the D-05/D-06/D-08 values above. Add a comment that these are demo starting values; production tuning is Phase 28. In deploy/chart/octoconv/values-local.yaml add `keda:\n  enabled: true`.
  </action>
  <verify>
    <automated>helm lint deploy/chart/octoconv && test $(helm template octoconv deploy/chart/octoconv | grep -c "kind: ScaledObject") -eq 0 && test $(helm template octoconv deploy/chart/octoconv -f deploy/chart/octoconv/values-local.yaml | grep -c "kind: ScaledObject") -eq 3 && test $(helm template octoconv deploy/chart/octoconv -f deploy/chart/octoconv/values-local.yaml | grep -c "scaledobject-webhook\|worker-webhook-scaledobject") -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `helm template ... ` with defaults renders 0 ScaledObjects (keda.enabled=false gate holds — base chart installs on a cluster without KEDA CRDs).
    - `helm template ... -f values-local.yaml` renders exactly 3 ScaledObjects, named `worker-image-scaledobject`, `worker-document-scaledobject`, `worker-html-scaledobject`, targeting Deployments `worker`, `document-worker`, `chromium-worker` respectively.
    - No webhook ScaledObject exists in either render.
    - Each ScaledObject has `minReplicaCount: 0`, class-correct `maxReplicaCount` (4/2/2) and `threshold` (5/1/2), a prometheus trigger with `query: sum(octoconv_queue_depth{queue="<class>", state=~"pending|active"})` (no retry/scheduled/archived), `serverAddress` pointing at the in-chart prometheus Service, `ignoreNullValues: "true"`, and `fallback.replicas: 1`.
    - `helm lint` passes.
  </acceptance_criteria>
  <done>Three gated ScaledObjects scale image/document/html workers from zero on their own pending+active queue depth; webhook-worker is untouched; defaults render nothing.</done>
</task>

<task type="auto">
  <name>Task 3: Offline gate — dry-run render + idempotence sanity across flag states</name>
  <files>deploy/chart/octoconv/values.yaml</files>
  <read_first>
    - deploy/chart/octoconv/values.yaml (final combined keda + prometheus blocks from tasks 1-2)
    - deploy/chart/octoconv/values-local.yaml (enablement overlay)
    - .planning/phases/24-helm-chart-core/24-03-SUMMARY.md (Phase 24 offline/dry-run gate conventions the executor extends)
  </read_first>
  <action>
Run the offline verification matrix (no live cluster — this is the pre-flight before plan 03's live gate) and record the results in the SUMMARY. Confirm: (1) `helm lint deploy/chart/octoconv` clean; (2) `helm template octoconv deploy/chart/octoconv` (defaults, both flags off) renders zero ScaledObjects and zero Prometheus objects and still renders the full existing app stack (api, 5 workers incl. webhook-worker at replicas 2, postgres/redis/minio) — i.e. the KEDA additions are purely additive and gated; (3) `helm template octoconv deploy/chart/octoconv -f deploy/chart/octoconv/values-local.yaml` renders 3 ScaledObjects + Prometheus Deployment/ConfigMap/Service + api :9090 metrics port + the corrected NetworkPolicy :9090 rule; (4) a `helm template` client-side render succeeds with no template errors in both flag states (server-dry-run against a live cluster is plan 03's job). If `kubectl` is configured against the OrbStack cluster, optionally run `helm template ... | kubectl apply --dry-run=client -f -` for both states to catch schema issues early, but do NOT install anything (OrbStack discipline D-13 — no live install until plan 03). This task makes no file edits unless a render error surfaces; if one does, fix the offending template and re-run.
  </action>
  <verify>
    <automated>helm lint deploy/chart/octoconv && test $(helm template octoconv deploy/chart/octoconv | grep -c "kind: ScaledObject") -eq 0 && test $(helm template octoconv deploy/chart/octoconv -f deploy/chart/octoconv/values-local.yaml | grep -c "kind: ScaledObject") -eq 3 && helm template octoconv deploy/chart/octoconv | grep -q "name: webhook-worker" && test $(helm template octoconv deploy/chart/octoconv | grep -c "kind: NetworkPolicy") -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - Flags-off render: 0 ScaledObjects, 0 Prometheus objects, full existing app stack still present (webhook-worker Deployment renders with `replicas: {{ ... }}` → 2 from values), NetworkPolicy still present.
    - Flags-on render: 3 ScaledObjects + Prometheus trio + api metrics port + corrected NetworkPolicy :9090 rule.
    - Both renders complete with no `helm template` error.
    - SUMMARY records the exact commands and their pass/fail, so plan 03's live gate starts from a known-good offline baseline.
  </acceptance_criteria>
  <done>The chart is offline-proven: additive and fully gated with flags off; complete KEDA topology with flags on; ready for the live gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-namespace pod → api :9090/metrics | Metrics NetworkPolicy governs who may scrape the queue-depth signal |
| Prometheus HTTP API :9090 → in-namespace callers | Prometheus stores/serves queue-depth; its query API is reachable within the octoconv namespace |
| KEDA operator (keda ns) → Prometheus :9090 (octoconv ns) | Cross-namespace scaler query path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-04 | Information Disclosure | api :9090 metrics port newly exposed on the Service | mitigate | networkpolicy-metrics.yaml (fixed this plan) admits :9090 only from the component=prometheus pod in the octoconv namespace; all other pods are default-denied once the Ingress policy selects api's tier=app pod. (Environmentally unenforced on OrbStack — no CNI policy controller — but correct for any conformant CNI; verified via render, per Phase 24 precedent.) |
| T-27-05 | Information Disclosure | Prometheus HTTP API :9090 queryable in-namespace | accept | Prometheus holds only operational queue-depth/job-outcome counters (no PII, no secrets); it ships behind `prometheus.enabled` (off by default) and has its own component label distinct from tier=app. Locking down Prometheus's own query API to only the KEDA namespace is a future hardening item, disproportionate for this internal dev cluster. |
| T-27-06 | Elevation of Privilege / Tampering | ScaledObject can drive Deployment replica counts | mitigate | ScaledObjects are chart-owned, gated, and bounded: minReplicaCount 0 / maxReplicaCount ≤4 per class (D-06, sized for the OrbStack VM). webhook-worker is hard-excluded (no ScaledObject) so the sweeper singleton can never be scaled to zero (Pitfall 2). A compromised metric could at most scale a class to its ceiling, not unbounded. |
| T-27-07 | Denial of Service | Prometheus unreachable → workers can't scale up | mitigate | `fallback: { failureThreshold: 3, replicas: 1 }` on every ScaledObject (D-07) holds 1 worker per scaled class and keeps processing if Prometheus queries fail — fail-safe toward availability. `ignoreNullValues: true` prevents routine api restarts from spuriously tripping fallback (Pitfall 4). |
| T-27-SC | Tampering (supply chain) | KEDA Helm chart + prom/prometheus image (external artifacts) | accept | Both verified against authoritative sources in RESEARCH.md Package Legitimacy Audit: KEDA chart `keda` v2.20.1 (official kedacore Helm repo + GitHub Releases API, live-fetched) and `prom/prometheus:v3.13.1` (official `prom/` Docker Hub org + Prometheus GitHub Releases API). Both pinned to concrete non-`latest` versions in values.yaml / plan 03. No npm/pip/cargo installs — slopcheck not applicable. KEDA install itself is a plan-03 gate step. |
</threat_model>

<verification>
- `helm lint deploy/chart/octoconv` clean.
- Flags-off: 0 ScaledObjects, 0 Prometheus objects, existing app stack intact.
- Flags-on: exactly 3 ScaledObjects (image/document/html), Prometheus trio, api :9090 metrics port, corrected NetworkPolicy :9090 rule (no `monitoring`).
- No webhook ScaledObject in any render.
</verification>

<success_criteria>
- Roadmap SC2/SC3/SC4 (manifest half): gated ScaledObjects scale image/document/html from 0 on the Prometheus scaler; webhook-worker excluded with fixed replicas 2; keda.enabled gates the whole feature.
- The two RESEARCH.md landmines (NetworkPolicy monitoring-namespace mismatch, api Service :9090 gap) are closed in-chart.
</success_criteria>

<output>
Create `.planning/phases/27-keda-autoscaling/27-02-SUMMARY.md` when done.
</output>
