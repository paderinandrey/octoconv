---
phase: 27-keda-autoscaling
plan: 03
type: execute
wave: 3
depends_on: ["27-01", "27-02"]
files_modified:
  - scripts/keda-gate.sh
autonomous: false
requirements: [KEDA-02]
user_setup: []
must_haves:
  truths:
    - "KEDA v2.20.1 helm-installs into its own keda namespace and its external.metrics.k8s.io API becomes Available"
    - "octoconv_queue_depth resolves via kubectl get --raw on the external metrics API while the target worker Deployment is verifiably at 0 replicas (SC1)"
    - "All three scaled classes (image/document/html) scale 0→1 from a single real job of their own type (SC2 literally)"
    - "The image class returns to 0 replicas after its cooldown (full cycle proof on the fastest class)"
    - "webhook-worker holds replicas: 2 throughout with no ScaledObject referencing it (D-09 fail-closed gate)"
    - "Teardown leaves OrbStack clean (helm uninstall octoconv + keda), honoring OrbStack discipline"
  artifacts:
    - path: "scripts/keda-gate.sh"
      provides: "reproducible Phase-27 live hard gate on OrbStack k8s"
      contains: "external.metrics.k8s.io"
  key_links:
    - from: "scripts/keda-gate.sh"
      to: "KEDA external metrics API"
      via: "kubectl get scaledobject -o jsonpath externalMetricNames then kubectl get --raw"
      pattern: "externalMetricNames"
    - from: "scripts/keda-gate.sh"
      to: "worker Deployments"
      via: "job submit → replica-count poll"
      pattern: "kubectl get deployment"
---

<objective>
KEDA-02 (live proof): Author and run the Phase-27 live hard gate on OrbStack Kubernetes — helm-install KEDA v2.20.1, install the octoconv chart with `keda.enabled=true prometheus.enabled=true`, and prove D-12: (a) `octoconv_queue_depth` resolves on the external metrics API while a worker is at genuinely 0 replicas; (b) all three classes scale 0→1 from a single real job of their type; (c) the image class cycles back to 0 by cooldown; (d) webhook-worker holds 2 replicas with no ScaledObject throughout. Teardown after, per OrbStack discipline.

Purpose: The offline render (plan 02) proves the manifests are correct; only a live cluster proves KEDA actually reads the relocated metric and drives real Deployments from zero. This is the milestone-critical scale-from-zero proof (burst 0→N→0 with timestamps is Phase 28 — NOT here).

Output: `scripts/keda-gate.sh` (reproducible, self-tearing-down) plus a captured gate transcript. No app-code or chart changes (if the gate surfaces a chart defect, fix it in plan 02's files and note the regression).
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-keda-autoscaling/27-CONTEXT.md
@.planning/phases/27-keda-autoscaling/27-RESEARCH.md
@.planning/phases/27-keda-autoscaling/27-PATTERNS.md
@.planning/phases/24-helm-chart-core/24-03-SUMMARY.md
@.planning/phases/25-mcp-streamable-http/25-03-SUMMARY.md

<interfaces>
<!-- Live-gate command shapes extracted from RESEARCH.md (KEDA-docs verified) and Phase 24/25 summaries. -->

KEDA install (D-11 — re-verify version live before running):
```
helm repo add kedacore https://kedacore.github.io/charts && helm repo update
helm search repo kedacore/keda --versions | head -5   # confirm 2.20.1 still current
helm install keda kedacore/keda --namespace keda --create-namespace --version 2.20.1 --wait
kubectl get apiservice v1beta1.external.metrics.k8s.io -o jsonpath='{.status.conditions}'  # poll for Available:True
```

External-metric verification (Pitfall 5 — discover name live, never hardcode):
```
kubectl get scaledobject worker-image-scaledobject -n octoconv -o jsonpath='{.status.externalMetricNames}'
kubectl get --raw "/apis/external.metrics.k8s.io/v1beta1/namespaces/octoconv/<name>?labelSelector=scaledobject.keda.sh%2Fname%3Dworker-image-scaledobject"
```

Worker Deployment names / classes / fixtures:
```
worker           image     — image fixture (internal/e2e/testdata, e.g. a PNG→JPEG job)
document-worker  document  — document fixture (e.g. a DOCX→PDF job)
chromium-worker  html      — html fixture (HTML→PDF job)
webhook-worker   FIXED replicas:2, NO ScaledObject — assert throughout
```

Bash gate conventions (scripts/presets-acceptance.sh): `#!/usr/bin/env bash`, `set -euo pipefail`, `cd "$(dirname "$0")/.."`, loud PASS/FAIL echos, exit code IS the gate.

Phase 24 install-flow decision (24-03-SUMMARY): `helm install` WITHOUT `--wait` for the octoconv release (createbucket post-install hook ↔ app-readiness chicken-egg), then `kubectl wait` per Deployment. OrbStack has NO NetworkPolicy controller (SC4 negative test not locally enforceable).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author scripts/keda-gate.sh (the reproducible live gate)</name>
  <files>scripts/keda-gate.sh</files>
  <read_first>
    - scripts/presets-acceptance.sh (bash gate-script conventions: shebang, set -euo pipefail, assert helpers, PASS/FAIL echo, exit-code-is-gate)
    - .planning/phases/24-helm-chart-core/24-03-SUMMARY.md (proven install flow: install WITHOUT --wait, kubectl wait per Deployment, teardown discipline)
    - .planning/phases/25-mcp-streamable-http/25-03-SUMMARY.md (k8s live-gate command sequence, port-forward/exec assertion patterns)
    - .planning/phases/27-keda-autoscaling/27-RESEARCH.md (KEDA Helm Install Mechanics; Pitfall 5 live metric-name discovery; external-metrics-empty-until-a-ScaledObject-exists note)
    - .planning/phases/27-keda-autoscaling/27-PATTERNS.md (Gate script assignment — the 9-step sequence to implement)
    - deploy/chart/octoconv/values-local.yaml (the keda/prometheus enablement overlay this gate layers)
    - internal/e2e/testdata (available conversion fixtures for the per-class jobs)
  </read_first>
  <action>
Write scripts/keda-gate.sh following presets-acceptance.sh conventions (`#!/usr/bin/env bash`, `set -euo pipefail`, `cd "$(dirname "$0")/.."`, named `assert_*` helpers with loud FAIL, a final `✅ PASS` banner; the script's exit code IS the gate). Header comment: Phase 27 live hard gate, proves D-12 on OrbStack k8s, and states OrbStack discipline (D-13) — the caller must ensure the compose stack is DOWN first (`docker compose ... down -v`), images are pre-built sequentially, and the script tears down at the end. Implement the 9-step sequence from 27-PATTERNS.md:
1. Preflight: assert `kubectl` reaches the OrbStack cluster and the compose stack is not hot (e.g. warn/fail if compose containers are running); re-verify KEDA version live (`helm search repo kedacore/keda --versions | head -5`) and abort if 2.20.1 is no longer resolvable, printing the current latest so the operator can repin.
2. `helm repo add kedacore https://kedacore.github.io/charts && helm repo update`; `helm install keda kedacore/keda --namespace keda --create-namespace --version 2.20.1 --wait` (idempotent — tolerate "already exists" by upgrading or skipping).
3. Poll `kubectl get apiservice v1beta1.external.metrics.k8s.io -o jsonpath='{.status.conditions}'` until `Available: True` (bounded retry loop, NOT a fixed sleep). Note the external metrics list is empty until a ScaledObject exists — that is expected, not a failure.
4. `helm install octoconv deploy/chart/octoconv -f deploy/chart/octoconv/values-local.yaml -n octoconv --create-namespace` WITHOUT `--wait` (Phase 24 decision), then `kubectl wait --for=condition=Available deployment/<name> -n octoconv --timeout=...` for api, prometheus, webhook-worker (the always-on / min-1 pods). The three scaled workers may already be at 0 — do NOT wait on them being Available.
5. SC1 (the load-bearing proof): pick one scaled class (image), confirm its Deployment is at 0 replicas (`kubectl get deployment worker -n octoconv -o jsonpath='{.spec.replicas}'` / status.replicas is 0), then discover the external metric name live via `kubectl get scaledobject worker-image-scaledobject -n octoconv -o jsonpath='{.status.externalMetricNames}'` (Pitfall 5 — never hardcode), then `kubectl get --raw "/apis/external.metrics.k8s.io/v1beta1/namespaces/octoconv/<name>?labelSelector=scaledobject.keda.sh%2Fname%3Dworker-image-scaledobject"` and assert it returns a value (metric resolves at genuinely 0 replicas — SC1).
6. SC2 per class (image/document/html): submit ONE real conversion job of that class's type via the api (provision a client key, POST the class's fixture — reuse the internal/e2e fixtures and the api's create-job route), then poll the target Deployment (`worker`/`document-worker`/`chromium-worker`) until `status.replicas >= 1` within a bounded timeout. Assert all three scale 0→1 (catches doc/html cold-start issues now, not in Phase 28).
7. Full-cycle (image only, fastest class): after the image job drains, poll `worker` back down to 0 replicas within a bounded window (cooldownPeriod 60s + margin). Assert it returns to 0.
8. webhook-worker gate (assert THROUGHOUT — check at start, mid, end): `kubectl get deployment webhook-worker -n octoconv -o jsonpath='{.spec.replicas}'` reads `2`, AND `kubectl get scaledobject -n octoconv` lists NO object whose scaleTargetRef is webhook-worker (`kubectl get scaledobject -n octoconv -o jsonpath` / grep -c returns 0 webhook references). D-09 fail-closed.
9. Teardown (always, even on failure via trap): `helm uninstall octoconv -n octoconv`; `helm uninstall keda -n keda`; optionally delete the namespaces. Print a final PASS/FAIL banner. Do NOT leave the k8s stack hot (D-13).
Make each assertion print what it checked and the observed value so the transcript is self-documenting evidence. Use bounded retry loops with clear timeout messages (distinguish "never scaled" from "timed out waiting").
  </action>
  <verify>
    <automated>bash -n scripts/keda-gate.sh && test -x scripts/keda-gate.sh && grep -q "externalMetricNames" scripts/keda-gate.sh && grep -q "set -euo pipefail" scripts/keda-gate.sh && test $(grep -c "webhook-worker" scripts/keda-gate.sh) -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/keda-gate.sh` parses cleanly; the file is executable (`chmod +x`).
    - Script uses `set -euo pipefail`, `cd "$(dirname "$0")/.."`, and a teardown trap that runs on both success and failure.
    - It discovers the external metric name live via `externalMetricNames` (no hardcoded `s0-prometheus`-style literal).
    - It re-verifies the KEDA version live and aborts with a clear message if 2.20.1 is no longer resolvable.
    - It asserts SC1 (metric resolves at 0 replicas), SC2 (all three classes 0→1), image cycle back to 0, and webhook-worker replicas==2 with no ScaledObject — each with a printed observed value.
    - It installs the octoconv release WITHOUT `--wait` and uses `kubectl wait` for the always-on pods only.
  </acceptance_criteria>
  <done>A reproducible, self-tearing-down gate script encodes the full D-12 proof; runnable from a clean OrbStack cluster.</done>
</task>

<task type="auto">
  <name>Task 2: Pre-build images sequentially and execute the live gate</name>
  <files>scripts/keda-gate.sh</files>
  <read_first>
    - scripts/keda-gate.sh (the gate authored in task 1)
    - .planning/phases/24-helm-chart-core/24-03-SUMMARY.md (the exact sequential build commands + non-latest tag discipline that proved out on OrbStack)
    - .planning/phases/27-keda-autoscaling/27-CONTEXT.md (D-13 OrbStack discipline)
  </read_first>
  <action>
Honor OrbStack discipline (D-13) end to end. First ensure the compose stack is fully down (`docker compose -f docker-compose.yml -f docker-compose.e2e.yml down -v` if anything from plan 01's validation is still up) — compose and k8s must never be hot simultaneously (three documented daemon wedges). Then pre-build all required images SEQUENTIALLY with fixed non-`latest` tags matching `global.imageTag: "dev"` (api, worker, document-worker, chromium-worker, webhook-worker, and mcp-http if the release includes it), one at a time, not in parallel — reuse the exact build invocations recorded in 24-03-SUMMARY. Prometheus and KEDA images are pulled by their charts, not built. Then run `bash scripts/keda-gate.sh` and capture the full transcript. The script's non-zero exit on any failed assertion IS the gate failure. If it fails on a chart defect (not an environment flake), fix the offending template in plan 02's files, re-render offline, rebuild only what changed, and re-run. Record the final transcript (KEDA version confirmed, SC1 raw-metric value at 0 replicas, per-class 0→1 replica observations, image cycle-to-0, webhook-worker replicas==2) in the SUMMARY as timestamped-adjacent evidence (full burst 0→N→0 timeline is Phase 28's deliverable, not this gate's).
  </action>
  <verify>
    <automated>echo "Executed live — evidence captured in SUMMARY and gate transcript; confirmed at the human-verify checkpoint below."</automated>
  </verify>
  <acceptance_criteria>
    - Compose stack confirmed DOWN before any k8s install (no simultaneous hot stacks).
    - All app images built sequentially with the `dev` (non-latest) tag before `helm install`.
    - `scripts/keda-gate.sh` runs to a `✅ PASS` banner (exit 0): SC1 metric resolves at 0 replicas, all three classes scale 0→1, image returns to 0, webhook-worker stays at 2 with no ScaledObject.
    - Teardown ran (helm uninstall octoconv + keda); `kubectl get deployment -n octoconv` shows the release gone (or namespaces deleted).
    - Gate transcript captured in the SUMMARY.
  </acceptance_criteria>
  <done>The live gate passed on OrbStack: scale-from-zero proven for all three classes, webhook-worker held at 2, cluster torn down clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of the live gate evidence</name>
  <action>Present the captured gate transcript and confirm each D-12 assertion with the operator before the phase is marked complete. This is a verification-only checkpoint — no files are modified.</action>
  <what-built>
The Phase-27 live hard gate (`scripts/keda-gate.sh`) was authored and executed on OrbStack Kubernetes. It helm-installed KEDA v2.20.1, installed the octoconv chart with keda/prometheus enabled, and asserted the full D-12 proof, then tore the stack down.
  </what-built>
  <how-to-verify>
1. Review the captured gate transcript in the SUMMARY (or re-run `bash scripts/keda-gate.sh` from a clean cluster with the compose stack DOWN).
2. Confirm SC1: `octoconv_queue_depth` resolved via `kubectl get --raw` on the external metrics API while the image `worker` Deployment was verifiably at 0 replicas (the load-bearing scale-from-zero property — a value must have come back at true 0).
3. Confirm SC2: all three classes (image/document/html) scaled 0→1 from a single real job of their own type — check the per-class replica-count observations.
4. Confirm the image class returned to 0 replicas after cooldown.
5. Confirm webhook-worker showed `replicas: 2` throughout with NO ScaledObject referencing it (D-09 fail-closed).
6. Confirm teardown left OrbStack clean (no octoconv/keda release still installed) — OrbStack discipline (D-13).
  </how-to-verify>
  <resume-signal>Type "approved" if the gate transcript proves SC1/SC2, the image cycle-to-zero, the webhook-worker fixed-2 gate, and a clean teardown; otherwise describe which assertion failed or looked wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → KEDA helm install (cluster-scoped operator + CRDs) | Installing KEDA grants a cluster-scoped controller the ability to scale Deployments and register an aggregated metrics API |
| KEDA metrics-apiserver → external.metrics.k8s.io | KEDA registers an aggregated APIService the HPA controller reads |
| local image build store → OrbStack pods | Locally-built `:dev` images become pod-visible with no registry (OrbStack shared store) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-08 | Elevation of Privilege | KEDA operator RBAC (cluster-scoped: scale Deployments, register external.metrics.k8s.io) | accept | KEDA is the off-the-shelf, community-standard autoscaler installed into its own dedicated `keda` namespace from the official kedacore chart (verified, RESEARCH.md audit); its cluster-scoped RBAC is inherent to how HPA-integrated autoscaling works and is out of scope to re-implement. Local OrbStack dev cluster, internal-only. Teardown removes the operator after the gate. |
| T-27-09 | Denial of Service | Scaling ceilings on a resource-tight OrbStack VM (documented daemon wedges under load) | mitigate | maxReplicaCount capped per class (image 4 / document 2 / html 2, D-06) sized for the VM; the gate submits ONE job per class (not a burst — burst is Phase 28) and tears down after. D-13 discipline: compose stack confirmed down first, sequential builds, no simultaneous hot stacks. |
| T-27-10 | Spoofing / Tampering | webhook-worker scaled to zero would silently kill the fleet-wide sweeper singleton | mitigate | The gate explicitly asserts webhook-worker replicas==2 and that NO ScaledObject targets it, at start/mid/end — a fail-closed hard gate (D-09/Pitfall 2). A regression that added a webhook ScaledObject fails the gate. |
| T-27-SC | Tampering (supply chain) | KEDA Helm chart install + prom/prometheus image pull (external artifacts pulled live by the gate) | accept | Both pinned to concrete versions verified in RESEARCH.md Package Legitimacy Audit against authoritative sources (kedacore official Helm repo + GitHub Releases; official `prom/` Docker Hub org + GitHub Releases). The gate re-verifies the KEDA version live before install and aborts on drift. No npm/pip/cargo installs — slopcheck not applicable; no auto-approvable legitimacy checkpoint needed since artifacts are [VERIFIED], not [ASSUMED]/[SUS]. |
</threat_model>

<verification>
- `bash -n scripts/keda-gate.sh` clean; script executable; live discovery of metric name; version re-check present.
- Live gate exit 0: SC1 metric at 0 replicas, SC2 all-three 0→1, image cycle→0, webhook-worker fixed 2 with no ScaledObject.
- Clean teardown (no octoconv/keda release remaining).
- Human-verify checkpoint approved.
</verification>

<success_criteria>
- Roadmap SC1 (live half): metric resolves at genuinely 0 replicas.
- Roadmap SC2: image/document/html scale 0→1 from their own job type.
- Roadmap SC3: webhook-worker fixed at 2, no ScaledObject — proven, not assumed.
- OrbStack left clean; burst 0→N→0 timeline deferred to Phase 28.
</success_criteria>

<output>
Create `.planning/phases/27-keda-autoscaling/27-03-SUMMARY.md` when done.
</output>
