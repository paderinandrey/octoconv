---
phase: 28-autoscale-load-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - deploy/chart/octoconv/templates/deployment-worker.yaml
  - deploy/chart/octoconv/templates/deployment-document-worker.yaml
  - deploy/chart/octoconv/templates/deployment-chromium-worker.yaml
  - deploy/chart/octoconv/templates/scaledobject-document.yaml
  - deploy/chart/octoconv/values.yaml
  - deploy/chart/octoconv/values-loadproof.yaml
  - scripts/keda-gate.sh
autonomous: true
requirements: [KEDA-03]
user_setup: []

must_haves:
  truths:
    - "D-10 (WR-02): the three KEDA-scaled Deployments (worker, document-worker, chromium-worker) omit spec.replicas when keda.enabled AND prometheus.enabled are both true, so a helm upgrade never resets a scaled-to-zero class back to 1"
    - "D-10: with keda.enabled=false the same three Deployments still render spec.replicas (production-inert default preserved), and webhook-worker renders spec.replicas unconditionally in every mode (never touched)"
    - "The document ScaledObject gains a values-gated advanced.horizontalPodAutoscalerConfig.behavior.scaleDown.stabilizationWindowSeconds block that only renders when keda.document.scaleDownStabilizationSeconds is set — supporting the SC3 2→1 downscale timing per RESEARCH Pitfall 1 (D-07/D-08 depend on this)"
    - "values.yaml keeps scaleDownStabilizationSeconds null by default (production HPA 300s default untouched); values-loadproof.yaml is the only file that turns it on (15s) and forces DOCUMENT_WORKER_CONCURRENCY=1 for deterministic SC3 pod↔job mapping"
    - "The Phase 27 gate scripts/keda-gate.sh still passes bash -n and its live assertions are logically unchanged (D-10 requires the Phase 27 gate remain working)"
  artifacts:
    - path: "deploy/chart/octoconv/values-loadproof.yaml"
      provides: "load-proof-only overlay: scaleDownStabilizationSeconds=15 + documentWorker.extraEnv.DOCUMENT_WORKER_CONCURRENCY=1"
      contains: "scaleDownStabilizationSeconds"
    - path: "deploy/chart/octoconv/templates/scaledobject-document.yaml"
      provides: "values-gated HPA scaleDown stabilization override"
      contains: "stabilizationWindowSeconds"
    - path: "deploy/chart/octoconv/templates/deployment-document-worker.yaml"
      provides: "field-level replicas conditional + extraEnv override block"
      contains: "extraEnv"
  key_links:
    - from: "deploy/chart/octoconv/values-loadproof.yaml"
      to: "deploy/chart/octoconv/templates/scaledobject-document.yaml"
      via: "keda.document.scaleDownStabilizationSeconds"
      pattern: "scaleDownStabilizationSeconds"
    - from: "deploy/chart/octoconv/values-loadproof.yaml"
      to: "deploy/chart/octoconv/templates/deployment-document-worker.yaml"
      via: "documentWorker.extraEnv"
      pattern: "extraEnv"
---

<objective>
Ship the two Phase 28 chart fixes that must exist before the live load-proof can run:

1. **D-10 / WR-02** — field-level omission of `spec.replicas` on the three KEDA-scaled Deployments (image `worker`, `document-worker`, `chromium-worker`) when KEDA owns them, so a `helm upgrade` never fights the HPA by resetting a scaled-to-zero class back to 1. `webhook-worker` is deliberately NOT touched (it is hard-excluded from KEDA).
2. **SC3 downscale-timing determinism** — a values-gated `advanced.horizontalPodAutoscalerConfig.behavior.scaleDown.stabilizationWindowSeconds` override on the document ScaledObject (RESEARCH Pitfall 1: KEDA `cooldownPeriod` governs only 1→0; the 2→1 step SC3 exercises is standard-HPA-governed with a 300s default that would push the downscale past the long job's own `DOCUMENT_ENGINE_TIMEOUT`=300s and silently invalidate SC3). Plus a document-worker `extraEnv` hook so the load-proof overlay can force `DOCUMENT_WORKER_CONCURRENCY=1` for deterministic pod↔job victim selection (D-08).

Explicitly OUT of scope: **D-11 (WR-01)** — empty-PromQL/`ignoreNullValues` semantics stay unchanged this phase (deferred to backlog; risky to alter trigger semantics right before the flagship acceptance).

Purpose: Establish the deterministic, production-safe chart substrate the load-proof gate (plans 28-02/28-03) depends on, without altering any production default.
Output: 6 chart files modified + 1 new overlay; the Phase 27 gate left working.
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-autoscale-load-proof/28-CONTEXT.md
@.planning/phases/28-autoscale-load-proof/28-RESEARCH.md
@.planning/phases/28-autoscale-load-proof/28-PATTERNS.md

<interfaces>
<!-- Concrete chart state the executor must respect. Extracted from the repo this planning session. -->

Current unconditional replicas render (identical shape in all three scaled-class Deployments):
  deployment-worker.yaml:23           spec:\n  replicas: {{ .Values.worker.replicas }}
  deployment-document-worker.yaml:25  spec:\n  replicas: {{ .Values.documentWorker.replicas }}
  deployment-chromium-worker.yaml:25  spec:\n  replicas: {{ .Values.chromiumWorker.replicas }}

DO NOT TOUCH (non-scaled contrast, must stay unconditional):
  deployment-webhook-worker.yaml:27   replicas: {{ .Values.webhookWorker.replicas }}

Existing co-dependency guard idiom already used across scaledobject-*.yaml:
  {{- if and .Values.keda.enabled .Values.prometheus.enabled }}

Existing extraEnv idiom to mirror (deployment-api.yaml:37-48): after `{{- include "octoconv.commonEnv" . | nindent 10 }}`,
an `{{- if .Values.api.extraEnv }} env: {{- range $k,$v := .Values.api.extraEnv }}...{{- end }}{{- end }}` block.
commonEnv (_helpers.tpl:49) is `envFrom: configMapRef octoconv-config + secretRef octoconv-secret`; explicit `env:` entries
override envFrom for the same key (this is how DOCUMENT_WORKER_CONCURRENCY, set "2" in configmap.yaml:26, gets overridden).

Current keda.document values block (values.yaml:152-156):
  document: { threshold: "1", maxReplicaCount: 2, pollingInterval: 15, cooldownPeriod: 120 }

document ScaledObject baseline (scaledobject-document.yaml): minReplicaCount 0, maxReplicaCount/pollingInterval/cooldownPeriod
from values, fallback {failureThreshold:3, replicas:1}, single prometheus trigger with ignoreNullValues:"true" (leave the trigger untouched — that is WR-01/D-11 territory).

keda-gate.sh settling workaround: STEP 6, lines ~220-240 (comment lines 222-227 + the poll loop 228-240).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Field-level spec.replicas omission on the three KEDA-scaled Deployments (D-10/WR-02)</name>
  <files>deploy/chart/octoconv/templates/deployment-worker.yaml, deploy/chart/octoconv/templates/deployment-document-worker.yaml, deploy/chart/octoconv/templates/deployment-chromium-worker.yaml, scripts/keda-gate.sh</files>
  <read_first>
    - deploy/chart/octoconv/templates/deployment-worker.yaml (current spec.replicas at line 23)
    - deploy/chart/octoconv/templates/deployment-document-worker.yaml (line 25)
    - deploy/chart/octoconv/templates/deployment-chromium-worker.yaml (line 25)
    - deploy/chart/octoconv/templates/deployment-webhook-worker.yaml (the DO-NOT-TOUCH contrast — read to confirm you are not editing it)
    - deploy/chart/octoconv/templates/scaledobject-image.yaml (for the exact `{{- if and .Values.keda.enabled .Values.prometheus.enabled }}` guard wording)
    - scripts/keda-gate.sh lines 215-240 (STEP 6 settling comment + poll)
    - .planning/phases/28-autoscale-load-proof/28-PATTERNS.md section "deployment-{worker,document-worker,chromium-worker}.yaml"
  </read_first>
  <action>
    In each of the three scaled-class Deployment templates (deployment-worker.yaml, deployment-document-worker.yaml, deployment-chromium-worker.yaml), replace the single unconditional `spec.replicas` line with a field-level conditional: under `spec:`, wrap in `{{- if and .Values.keda.enabled .Values.prometheus.enabled }}` a comment explaining spec.replicas is intentionally omitted because KEDA/HPA owns the scale subresource (rendering a fixed value makes every helm upgrade reset a scaled-to-zero class back to the default, fighting the HPA — WR-02, D-10), `{{- else }}` render the original `replicas: {{ .Values.<class>.replicas }}` line, `{{- end }}`. The Deployment object itself MUST always render (only the replicas FIELD is conditional) — KEDA needs the Deployment to exist before it can take ownership. Use `.Values.worker.replicas`, `.Values.documentWorker.replicas`, `.Values.chromiumWorker.replicas` respectively. Do NOT edit deployment-webhook-worker.yaml — it must keep rendering `replicas: {{ .Values.webhookWorker.replicas }}` unconditionally.

    Then, in scripts/keda-gate.sh STEP 6, update ONLY the stale explanatory comment (lines ~222-227) that currently says "The worker Deployment's chart-owned spec.replicas defaults to 1 (helm renders it that way...)". Post-WR-02 the chart no longer renders spec.replicas for the image worker under keda+prometheus, so Kubernetes defaults an unset replicas to 1 on fresh Deployment CREATE, and KEDA then scales it to 0 after observing the empty queue across its cooldownPeriod. Rewrite the comment to reflect this. KEEP the poll-for-settling loop (lines 228-240) UNCHANGED — a fresh `helm install` still starts the worker at the k8s default of 1 before KEDA scales it down, so the settling poll is still required; only its rationale comment changes. Do not alter any assertion or control flow in keda-gate.sh. (Rationale for the conservative scope: a functional removal is not safe because fresh-install settling is inherent to omitted-replicas semantics, not the WR-02 upgrade bug; D-10 asks to "simplify" the workaround and requires the Phase 27 gate remain working — updating the now-incorrect comment is the correct, non-breaking simplification.)
  </action>
  <verify>
    <automated>bash -n scripts/keda-gate.sh && helm lint deploy/chart/octoconv -f deploy/chart/octoconv/values-local.yaml >/dev/null && for f in deployment-worker deployment-document-worker deployment-chromium-worker; do n=$(helm template octoconv deploy/chart/octoconv -f deploy/chart/octoconv/values-local.yaml --show-only templates/$f.yaml | grep -cE '^[[:space:]]*replicas:'); [ "$n" -eq 0 ] || { echo "FAIL keda-on $f replicas=$n want 0"; exit 1; }; m=$(helm template octoconv deploy/chart/octoconv -f deploy/chart/octoconv/values-local.yaml --set keda.enabled=false --show-only templates/$f.yaml | grep -cE '^[[:space:]]*replicas:'); [ "$m" -eq 1 ] || { echo "FAIL keda-off $f replicas=$m want 1"; exit 1; }; done && w=$(helm template octoconv deploy/chart/octoconv -f deploy/chart/octoconv/values-local.yaml --show-only templates/deployment-webhook-worker.yaml | grep -cE '^[[:space:]]*replicas:'); [ "$w" -eq 1 ] && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `helm template ... -f values-local.yaml --show-only templates/deployment-worker.yaml` (and document-worker, chromium-worker) contains ZERO lines matching `^[[:space:]]*replicas:` (keda.enabled+prometheus.enabled both true via values-local).
    - The same three, rendered with `--set keda.enabled=false`, each contain EXACTLY ONE `^[[:space:]]*replicas:` line.
    - `deployment-webhook-worker.yaml` renders EXACTLY ONE `^[[:space:]]*replicas:` line in every mode (unchanged).
    - `bash -n scripts/keda-gate.sh` exits 0; `git diff scripts/keda-gate.sh` shows ONLY comment-line changes in STEP 6 (no changed assertion/loop/echo executable lines).
    - `helm lint deploy/chart/octoconv -f values-local.yaml` reports no errors.
  </acceptance_criteria>
  <done>All three scaled Deployments omit spec.replicas under KEDA, render it when KEDA is off, webhook-worker is untouched, and the Phase 27 gate still parses with only its stale comment corrected.</done>
</task>

<task type="auto">
  <name>Task 2: Document ScaledObject stabilization override + document-worker extraEnv + load-proof overlay (D-07/D-08 timing)</name>
  <files>deploy/chart/octoconv/templates/scaledobject-document.yaml, deploy/chart/octoconv/templates/deployment-document-worker.yaml, deploy/chart/octoconv/values.yaml, deploy/chart/octoconv/values-loadproof.yaml</files>
  <read_first>
    - deploy/chart/octoconv/templates/scaledobject-document.yaml (full — baseline spec to extend)
    - deploy/chart/octoconv/templates/deployment-api.yaml lines 36-48 (the extraEnv idiom to mirror verbatim)
    - deploy/chart/octoconv/templates/deployment-document-worker.yaml lines 37-45 (where commonEnv is included — extraEnv goes right after it)
    - deploy/chart/octoconv/templates/configmap.yaml lines 25-28 (DOCUMENT_WORKER_CONCURRENCY "2" — the value being overridden)
    - deploy/chart/octoconv/values.yaml lines 138-161 (keda block + inline-comment convention)
    - deploy/chart/octoconv/values-e2e.yaml (the "overlay layered ON TOP of values-local.yaml, NEVER standalone" header convention to copy)
    - .planning/phases/28-autoscale-load-proof/28-RESEARCH.md "Pattern 1" + "Pitfall 1"
    - .planning/phases/28-autoscale-load-proof/28-PATTERNS.md sections "scaledobject-document.yaml", "values.yaml", "values-loadproof.yaml"
  </read_first>
  <action>
    (a) scaledobject-document.yaml: after the `cooldownPeriod:` line and before `fallback:`, add a values-gated block `{{- if .Values.keda.document.scaleDownStabilizationSeconds }}` rendering `advanced.horizontalPodAutoscalerConfig.behavior.scaleDown` with `stabilizationWindowSeconds: {{ .Values.keda.document.scaleDownStabilizationSeconds }}` and one policy `{ type: Pods, value: 1, periodSeconds: 15 }`, then `{{- end }}`. Add a header-comment note that this key defaults to null (production keeps the Kubernetes 300s default) and that `cooldownPeriod` governs ONLY the 1→0 transition while THIS knob governs the N→N-1 (N>1) transition SC3 exercises — do not conflate them. Leave the existing prometheus trigger + `ignoreNullValues: "true"` untouched (that is D-11/WR-01, out of scope).

    (b) deployment-document-worker.yaml: directly after the `{{- include "octoconv.commonEnv" . | nindent 10 }}` line, add the extraEnv block mirroring deployment-api.yaml (gated on `{{- if .Values.documentWorker.extraEnv }}`, `env:` with a `{{- range $k, $v := .Values.documentWorker.extraEnv }}` name/value loop, `{{ $v | quote }}`). Include the same one-line comment that explicit env overrides envFrom for the same key. Default (no documentWorker.extraEnv) leaves concurrency at ConfigMap "2" — production behavior unchanged.

    (c) values.yaml: under `keda.document:` add `scaleDownStabilizationSeconds: null` with an inline comment "NEW (D-10/Pattern 1) — production default OFF (K8s 300s HPA default preserved); load-proof gate overrides via values-loadproof.yaml". Change nothing else in the keda block. Do NOT add a documentWorker.extraEnv default (leave documentWorker as-is; the overlay supplies it).

    (d) values-loadproof.yaml (NEW): copy the values-e2e.yaml header convention (overlay layered on top of values-local.yaml, never standalone; show the `helm install ... -f values-local.yaml -f values-loadproof.yaml -n octoconv` invocation). Body sets exactly two things: `keda.document.scaleDownStabilizationSeconds: 15` (deterministic fast 2→1 downscale for SC3, per RESEARCH Pitfall 1) and `documentWorker.extraEnv.DOCUMENT_WORKER_CONCURRENCY: "1"` (one job per pod so the long-first/short-second submission order in the load-proof gate deterministically maps pod1=long-job → the annotated downscale victim, D-08). Comment WHY each knob exists and that this file is never applied to a production values file.
  </action>
  <verify>
    <automated>helm lint deploy/chart/octoconv -f deploy/chart/octoconv/values-local.yaml >/dev/null && base=$(helm template octoconv deploy/chart/octoconv -f deploy/chart/octoconv/values-local.yaml --show-only templates/scaledobject-document.yaml | grep -c 'stabilizationWindowSeconds'); [ "$base" -eq 0 ] || { echo "FAIL base stabilization=$base want 0"; exit 1; }; over=$(helm template octoconv deploy/chart/octoconv -f deploy/chart/octoconv/values-local.yaml -f deploy/chart/octoconv/values-loadproof.yaml --show-only templates/scaledobject-document.yaml | grep -c 'stabilizationWindowSeconds'); [ "$over" -eq 1 ] || { echo "FAIL overlay stabilization=$over want 1"; exit 1; }; conc=$(helm template octoconv deploy/chart/octoconv -f deploy/chart/octoconv/values-local.yaml -f deploy/chart/octoconv/values-loadproof.yaml --show-only templates/deployment-document-worker.yaml | grep -A1 'DOCUMENT_WORKER_CONCURRENCY' | grep -c '"1"'); [ "$conc" -eq 1 ] && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Rendered `scaledobject-document.yaml` with ONLY values-local.yaml contains ZERO `stabilizationWindowSeconds` (production default off).
    - Rendered with `-f values-local.yaml -f values-loadproof.yaml` contains EXACTLY ONE `stabilizationWindowSeconds: 15` under `advanced.horizontalPodAutoscalerConfig.behavior.scaleDown`.
    - Rendered `deployment-document-worker.yaml` with the overlay contains an explicit `env:` entry `DOCUMENT_WORKER_CONCURRENCY` with value `"1"`; without the overlay it contains no `env:` DOCUMENT_WORKER_CONCURRENCY entry (falls through to ConfigMap "2").
    - `values.yaml` keda.document has `scaleDownStabilizationSeconds: null`; the prometheus trigger + `ignoreNullValues: "true"` in scaledobject-document.yaml are byte-identical to before (D-11 untouched — `git diff` shows no change to the `triggers:` block).
    - `helm lint` passes with the overlay.
  </acceptance_criteria>
  <done>The document class has a values-gated fast-downscale override (off in production, 15s in the load-proof overlay) and a concurrency=1 override path, both proven via helm template; WR-01/D-11 trigger semantics are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Helm values → rendered k8s manifests | Chart templating decides replica ownership and HPA timing; a mis-scoped conditional could affect production defaults |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Tampering | deployment-webhook-worker.yaml | accept | webhook-worker is explicitly NOT edited; WR-02 fix scoped to the 3 KEDA-scaled classes only; verify grep asserts webhook renders replicas unconditionally in every mode |
| T-28-02 | Denial of Service | scaled Deployments | mitigate | replicas omitted ONLY when `keda.enabled AND prometheus.enabled`; helm template proves replicas still render with keda off, so a non-KEDA deploy is never left with an undefined replica count |
| T-28-03 | Information Disclosure | values-loadproof.yaml | mitigate | overlay carries no secrets — only timing/concurrency knobs; header documents it is layered on values-local.yaml and never a production values file; scaleDownStabilizationSeconds defaults null in values.yaml so production HPA behavior is unchanged |
| T-28-SC | Tampering | npm/pip/cargo installs | accept | this plan installs NO packages and touches no dependency manifest (chart YAML + one bash comment only) |
</threat_model>

<verification>
- `helm lint deploy/chart/octoconv -f values-local.yaml` clean.
- `helm template` render checks (both tasks' automated verify) pass.
- `bash -n scripts/keda-gate.sh` exits 0 and its diff is comment-only.
- No change to the document ScaledObject `triggers:`/`ignoreNullValues` block (D-11 out of scope).
</verification>

<success_criteria>
- Three KEDA-scaled Deployments omit spec.replicas under KEDA ownership; webhook-worker untouched; production (keda off) still renders replicas.
- Document ScaledObject supports a values-gated fast-downscale window, off by default, on via the load-proof overlay.
- values-loadproof.yaml exists with the stabilization + concurrency-1 overrides.
- Phase 27 gate remains working (parses; logic unchanged).
</success_criteria>

<output>
Create `.planning/phases/28-autoscale-load-proof/28-01-SUMMARY.md` when done.
</output>
