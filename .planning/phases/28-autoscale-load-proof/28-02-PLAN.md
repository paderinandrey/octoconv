---
phase: 28-autoscale-load-proof
plan: 02
type: execute
wave: 2
depends_on: [28-01]
files_modified:
  - scripts/fixtures/gen_heavy_docx.py
  - scripts/fixtures/render_evidence.py
  - scripts/keda-load-proof.sh
autonomous: true
requirements: [KEDA-03]
user_setup: []

must_haves:
  truths:
    - "D-07: scripts/fixtures/gen_heavy_docx.py generates a heavy multi-page/table docx into a caller-chosen path with a --page-units calibration knob, using ephemeral `uv run --with python-docx` (never persisted to any dependency manifest)"
    - "D-02: scripts/fixtures/render_evidence.py turns the sampler CSV into a dual-axis PNG (queue depth + pod count on one time axis) via ephemeral `uv run --with matplotlib`, headless (Agg)"
    - "D-01: scripts/keda-load-proof.sh contains a ~5s-cadence CSV sampler loop (ISO timestamp, queue_depth via live-discovered external metric, ready-replica counts per Deployment) runnable as a background job across steady→burst→drain→zero"
    - "D-04/D-05: the gate submits a burst of 20 parallel image jobs (png→jpg) against the image worker at true zero, asserts SC1 (≥2 replicas within 60s) literally, records reaching maxReplicas=4 as a fact not an assertion; burst is image-class only"
    - "D-08: the gate submits the long docx first then the short docx (deterministic pod1=long/pod2=short mapping under DOCUMENT_WORKER_CONCURRENCY=1), annotates the busy pod (pod1) with controller.kubernetes.io/pod-deletion-cost=-1000 BEFORE the 2→1 downscale fires, and never uses kubectl delete pod"
    - "D-09: the gate reads the SIGTERM timestamp from the kubelet `Killing` event (never deletionTimestamp), the pod exit timestamp from containerStatuses.lastState.terminated, and runs the two psql triple-check queries (exactly one queued→active in job_events; jobs.finished_at) through the existing svc/postgres port-forward"
    - "D-12: keda-load-proof.sh is self-contained (installs KEDA itself, layers -f values-local.yaml -f values-loadproof.yaml, tears down via EXIT trap, refuses to run if the compose stack is up) and leaves scripts/keda-gate.sh untouched"
  artifacts:
    - path: "scripts/keda-load-proof.sh"
      provides: "Phase 28 live load-proof gate (SC1/SC2 burst + SC3 soak + evidence generation)"
      min_lines: 250
    - path: "scripts/fixtures/gen_heavy_docx.py"
      provides: "calibrated heavy-docx generator"
      contains: "page-units"
    - path: "scripts/fixtures/render_evidence.py"
      provides: "CSV→PNG dual-axis renderer"
      contains: "twinx"
  key_links:
    - from: "scripts/keda-load-proof.sh"
      to: "deploy/chart/octoconv/values-loadproof.yaml"
      via: "helm install -f values-local.yaml -f values-loadproof.yaml"
      pattern: "values-loadproof"
    - from: "scripts/keda-load-proof.sh"
      to: "scripts/fixtures/render_evidence.py"
      via: "uv run --with matplotlib after CSV sampler completes"
      pattern: "render_evidence"
    - from: "scripts/keda-load-proof.sh"
      to: "scripts/fixtures/gen_heavy_docx.py"
      via: "uv run --with python-docx (calibration + SC3 long job)"
      pattern: "gen_heavy_docx"
---

<objective>
Author the three evidence/load-generation tools the live run (plan 28-03) executes. NONE of these touch application code or the Go module graph — they are one-off test/evidence scripts in the `scripts/*` family (RESEARCH Architectural Responsibility Map: "Test/Evidence tooling").

1. `scripts/fixtures/gen_heavy_docx.py` — python-docx generator for a calibrated ~200s LibreOffice job (D-07).
2. `scripts/fixtures/render_evidence.py` — matplotlib CSV→PNG dual-axis timeline (D-02).
3. `scripts/keda-load-proof.sh` — the Phase 28 gate, built across two tasks: (Task 2) the reusable skeleton from keda-gate.sh + calibration mode (D-07) + the SC1/SC2 image-burst sampler scenario (D-01/D-04/D-05/D-06) with PNG evidence; (Task 3) the independent SC3 downscale-soak scenario with deterministic victim selection (D-08) and the D-09 triple-check.

Purpose: Produce runnable, offline-validated tooling so plan 28-03 is a pure execution/observation run against live infrastructure.
Output: 2 new Python scripts + 1 new gate script.
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-autoscale-load-proof/28-CONTEXT.md
@.planning/phases/28-autoscale-load-proof/28-RESEARCH.md
@.planning/phases/28-autoscale-load-proof/28-PATTERNS.md
@scripts/keda-gate.sh
@deploy/chart/octoconv/values-loadproof.yaml

<interfaces>
<!-- Concrete reusable shapes from scripts/keda-gate.sh (read in full). -->
- Constants: NAMESPACE=octoconv, KEDA_NAMESPACE=keda, KEDA_VERSION=2.20.1, CHART_DIR=deploy/chart/octoconv,
  VALUES_LOCAL=deploy/chart/octoconv/values-local.yaml, API_LOCAL_PORT=18090, DB_LOCAL_PORT=15434.
- Helpers to copy verbatim: assert_eq, assert_nonempty, log; teardown() + `trap teardown EXIT`.
- postJob(filename,target,content_type) → echoes job_id; POSTs multipart to $API_BASE/v1/jobs with `Authorization: ApiKey $CLIENT_KEY`, expects HTTP 202.
- waitForReplicasAtLeast(deployment,floor,timeout_s) / waitForReplicasAtMost(...) → bounded poll of status.replicas.
- Live external-metric discovery: `kubectl get scaledobject worker-image-scaledobject -n $NS -o jsonpath='{.status.externalMetricNames[0]}'` then
  `kubectl get --raw "/apis/external.metrics.k8s.io/v1beta1/namespaces/$NS/$METRIC?labelSelector=scaledobject.keda.sh%2Fname%3Dworker-image-scaledobject"` (NEVER hardcode the metric name — Pitfall 5).
- Client mint: port-forward svc/api + svc/postgres; export DATABASE_URL, API_KEY_SALT=dev-only-change-me-in-real-deploys; `go run ./cmd/manage-clients create NAME`; parse `api key: ...`.
- document ScaledObject name (for SC3 metric discovery): worker-document-scaledobject; document trigger counts state=~"pending|active".
- Deployment names: worker (image), document-worker, chromium-worker, webhook-worker.
- Existing image fixture: internal/e2e/testdata/sample.png; existing docx fixture: internal/e2e/testdata/sample.docx (the SHORT job).
- SC3 grace ceiling: document-worker terminationGracePeriodSeconds=330 (values.yaml:53); cmd/document-worker ShutdownTimeout=DOCUMENT_ENGINE_TIMEOUT(300s)+10s=310s.
- bash `local` multi-assignment pitfall (27-03-SUMMARY deviation 2): split `local a; a=...; local b=...${a}` across statements when a later var references an earlier one.

RESEARCH.md Code Examples are the authoritative source skeletons for gen_heavy_docx.py (§"Heavy docx generator") and render_evidence.py (§"PNG evidence render") and the D-09 psql queries (§"D-09 triple-check queries").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: python-docx heavy-docx generator + matplotlib CSV→PNG renderer</name>
  <files>scripts/fixtures/gen_heavy_docx.py, scripts/fixtures/render_evidence.py</files>
  <read_first>
    - .planning/phases/28-autoscale-load-proof/28-RESEARCH.md §"Code Examples" (both skeletons, verbatim starting points) and §"Standard Stack" (uv/python-docx/matplotlib versions verified live)
    - .planning/phases/28-autoscale-load-proof/28-PATTERNS.md sections "gen_heavy_docx.py" and "render_evidence.py"
    - internal/e2e/testdata/ (confirm sample.png exists for optional image embedding)
  </read_first>
  <action>
    gen_heavy_docx.py: build on RESEARCH §"Heavy docx generator". Use argparse for `--page-units N` (calibration knob, default e.g. 300) and `--out PATH`. Per page-unit: a level-1 heading, a paragraph of repeated filler, an 8×6 table of filled cells, and every 10th unit embed `internal/e2e/testdata/sample.png` (reuse the existing fixture so no new binary is committed). Insert a TOC field (the OxmlElement/qn pattern from RESEARCH) so LibreOffice does real layout work. Save to `--out`. Print the output path and the chosen page-units to stdout. The script itself must import only `docx` + stdlib (invoked via `uv run --with python-docx python3 ...`) — do NOT add a requirements.txt or touch go.mod. Per RESEARCH Security Domain: the script is committed but the generated docx is NOT (generated to /tmp at run time by the gate).

    render_evidence.py: build on RESEARCH §"PNG evidence render". argparse `--csv PATH --png PATH` (plus optional `--title`). Force `matplotlib.use("Agg")` before importing pyplot (headless). Read the CSV with csv.DictReader; parse `timestamp` via datetime.fromisoformat; plot `queue_depth` on ax1 and a pod-count series on ax2 via `twinx()`; format the x-axis with mdates.DateFormatter("%H:%M:%S"); label both axes and the title; `savefig(--png, dpi=120)`. Handle the CSV column names your sampler in Task 2 actually writes (keep them consistent: `timestamp,queue_depth,worker_replicas` at minimum; you may add doc/html replica columns). Invoked via `uv run --with matplotlib python3 ...` — stdlib + matplotlib only.
  </action>
  <verify>
    <automated>uv run --with python-docx python3 scripts/fixtures/gen_heavy_docx.py --page-units 3 --out /tmp/loadproof-smoke.docx && test -s /tmp/loadproof-smoke.docx && printf 'timestamp,queue_depth,worker_replicas\n2026-07-17T00:00:00Z,20,0\n2026-07-17T00:00:05Z,15,2\n2026-07-17T00:00:10Z,0,4\n' > /tmp/loadproof-smoke.csv && uv run --with matplotlib python3 scripts/fixtures/render_evidence.py --csv /tmp/loadproof-smoke.csv --png /tmp/loadproof-smoke.png && test -s /tmp/loadproof-smoke.png && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --with python-docx python3 scripts/fixtures/gen_heavy_docx.py --page-units 3 --out /tmp/loadproof-smoke.docx` exits 0 and produces a non-empty .docx.
    - The generator accepts `--page-units` and `--out` and its heaviness scales with page-units (larger N → larger file / more content).
    - `render_evidence.py` on a 3-row CSV produces a non-empty PNG with two y-axes (queue depth + pod count) and a time x-axis, headless (no DISPLAY required).
    - Neither script adds any file to the Go module graph or a persistent Python requirements file (`git status` shows only the two new scripts under scripts/fixtures/).
  </acceptance_criteria>
  <done>Both fixtures run via ephemeral uv, produce their artifacts on smoke inputs, and add no persistent dependency.</done>
</task>

<task type="auto">
  <name>Task 2: keda-load-proof.sh skeleton + calibration mode + SC1/SC2 image-burst sampler scenario</name>
  <files>scripts/keda-load-proof.sh</files>
  <read_first>
    - scripts/keda-gate.sh (FULL — copy the skeleton: config block, assert_*/log helpers, teardown+trap, preflight compose-down guard, KEDA install, port-forward+client-mint, postJob, waitForReplicasAtLeast/AtMost, live metric discovery)
    - .planning/phases/28-autoscale-load-proof/28-RESEARCH.md §"Common Pitfalls" (1,4,5), §"Code Examples" (heavy-docx invocation)
    - .planning/phases/28-autoscale-load-proof/28-PATTERNS.md section "scripts/keda-load-proof.sh" (sampleLoop shape)
    - deploy/chart/octoconv/values-loadproof.yaml (the overlay this gate layers on)
  </read_first>
  <action>
    Create scripts/keda-load-proof.sh (`set -euo pipefail`, `cd "$(dirname "$0")/.."`). Header doc-comment maps scenarios to SC1-SC4 and D-01..D-12. Reuse keda-gate.sh's constants, assert_*/log, teardown+trap (extend teardown to also kill the sampler PID and clear any pod-deletion-cost annotation — the annotation cleanup is a no-op here but keeps teardown shared with Task 3), preflight (refuse if compose stack up — D-12), KEDA install, and the octoconv install BUT with `helm install octoconv "$CHART_DIR" -f "$VALUES_LOCAL" -f deploy/chart/octoconv/values-loadproof.yaml -n "$NAMESPACE" --create-namespace`. Add `EVIDENCE_DIR=".planning/phases/28-autoscale-load-proof/evidence"` and a `RUN_TS=$(date -u +%Y%m%dT%H%M%SZ)` for filenames; `mkdir -p "$EVIDENCE_DIR"`. Tee the whole run to `$EVIDENCE_DIR/gate-transcript-$RUN_TS.log`.

    CALIBRATION MODE (D-07): support `CALIBRATE=1` (env or `--calibrate` arg) that, after install + port-forward + client mint, generates a heavy docx via `uv run --with python-docx python3 scripts/fixtures/gen_heavy_docx.py --page-units "${PAGE_UNITS:-300}" --out /tmp/heavy-$RUN_TS.docx`, submits ONLY that one docx→pdf job, polls jobs until done, prints the observed conversion duration (from jobs.started_at/finished_at via psql), then returns (teardown via trap). This is the live in-cluster trial run — there is NO local soffice (RESEARCH Pitfall 4).

    SC1/SC2 BURST SCENARIO (D-04/D-05/D-06 + D-01 sampler + SC4): live-discover the image external metric name (worker-image-scaledobject); confirm worker at true 0 via a bounded poll (Rule-1 fix — KEDA settles after cooldown, poll don't assert). Define a sampleLoop() that every ~5s appends `timestamp,queue_depth,worker_replicas` (queue_depth from `kubectl get --raw` on the discovered metric; worker_replicas from status.replicas) to `$CSV_FILE=$EVIDENCE_DIR/sc1-sc2-burst-$RUN_TS.csv`; run it in the background (`&`, capture SAMPLER_PID) starting a few samples BEFORE the burst (steady state). Generate a medium image fixture at run time so each conversion takes a couple seconds and the backlog stays above threshold long enough for the HPA to add replicas toward maxReplicas=4 (planner discretion per D-05: e.g. `uv run --with matplotlib python3 -c` or ImageMagick/Pillow to synthesize a ~3-5MP PNG into /tmp; if unavailable, fall back to 20× sample.png). Fire 20 parallel `postJob` calls in background subshells, `wait`, collect the 20 job_ids (heed the `local` multi-assignment pitfall). ASSERT SC1 literally: `waitForReplicasAtLeast worker 2 60` succeeds. Record (not assert) the peak replicas observed (up to 4) into the transcript. Then let the queue drain and ASSERT SC2/N→0: `waitForReplicasAtMost worker 0 180` after the metric returns to 0. Stop the sampler cleanly (kill SAMPLER_PID) so the CSV has a clean end. Render the PNG: `uv run --with matplotlib python3 scripts/fixtures/render_evidence.py --csv "$CSV_FILE" --png "$EVIDENCE_DIR/sc1-sc2-burst-$RUN_TS.png"`.

    Leave a clearly-marked insertion point (comment marker) after the SC1/SC2 scenario and BEFORE any final GATE_OK/summary — the SC3 downscale-soak scenario and the ALL-PASSED summary are added by Task 3 in this same file. keda-gate.sh must remain byte-unchanged (D-12: separate script).
  </action>
  <verify>
    <automated>bash -n scripts/keda-load-proof.sh && grep -q 'values-loadproof.yaml' scripts/keda-load-proof.sh && grep -q 'trap teardown EXIT' scripts/keda-load-proof.sh && grep -q 'render_evidence.py' scripts/keda-load-proof.sh && grep -q 'gen_heavy_docx.py' scripts/keda-load-proof.sh && grep -q 'sc1-sc2-burst' scripts/keda-load-proof.sh && grep -q 'waitForReplicasAtLeast worker 2' scripts/keda-load-proof.sh && grep -Eq 'CALIBRATE|--calibrate' scripts/keda-load-proof.sh && git diff --quiet scripts/keda-gate.sh && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/keda-load-proof.sh` exits 0 (syntactically valid even before Task 3 appends SC3).
    - Script installs with `-f values-local.yaml -f values-loadproof.yaml`, has `trap teardown EXIT`, and references `render_evidence.py` + `gen_heavy_docx.py`.
    - CALIBRATE mode (env or `--calibrate`) exists and generates the heavy docx via `gen_heavy_docx.py --page-units`.
    - The SC1 assertion is `waitForReplicasAtLeast worker 2 60` (literal ≥2 in 60s); reaching 4 is only recorded, not asserted (the burst assertion uses floor 2, not 4).
    - SC2 asserts the image worker returns to 0 (`waitForReplicasAtMost worker 0 ...`); the sampler writes `sc1-sc2-burst-<RUN_TS>.csv` and the PNG is rendered from it.
    - `git diff --quiet scripts/keda-gate.sh` is true (Phase 27 gate untouched — D-12).
  </acceptance_criteria>
  <done>keda-load-proof.sh parses, self-installs against the load-proof overlay, supports live calibration, implements the SC1/SC2 image-burst sampler scenario with CSV+PNG evidence, and leaves keda-gate.sh untouched. A marked insertion point awaits the SC3 scenario.</done>
</task>

<task type="auto">
  <name>Task 3: keda-load-proof.sh SC3 downscale-soak scenario + D-09 triple-check + ALL-PASSED summary</name>
  <files>scripts/keda-load-proof.sh</files>
  <read_first>
    - scripts/keda-load-proof.sh (the Task 2 skeleton + SC1/SC2 scenario + the marked SC3 insertion point)
    - .planning/phases/28-autoscale-load-proof/28-RESEARCH.md §"Architecture Patterns" (SC3 downscale diagram), §"Common Pitfalls" (1,2,3), §"Code Examples" (D-09 psql queries)
    - .planning/phases/28-autoscale-load-proof/28-PATTERNS.md section "scripts/keda-load-proof.sh" (pod-deletion-cost, SIGTERM read)
  </read_first>
  <action>
    At the marked SC3 insertion point in scripts/keda-load-proof.sh (after SC1/SC2, before the final summary), add the SC3 DOWNSCALE-SOAK SCENARIO (D-07/D-08/D-09), independent of the image burst (document queue only). Ensure document-worker at 0. Generate the calibrated heavy docx to /tmp. Submit the LONG heavy docx→pdf FIRST; wait document-worker 0→1 (`waitForReplicasAtLeast document-worker 1 180`) and poll the long job to status=active — this pod (oldest by .metadata.creationTimestamp) is pod1/the busy pod. Submit the SHORT sample.docx→pdf; wait document-worker 1→2 (pod2=newest, picks the short job because DOCUMENT_WORKER_CONCURRENCY=1 keeps pod1 full). Identify pod1 = the document-worker pod with the EARLIEST creationTimestamp; immediately `kubectl annotate pod "$pod1" -n "$NAMESPACE" controller.kubernetes.io/pod-deletion-cost=-1000 --overwrite` — this MUST land before the short job completes / before the 2→1 downscale decision (Pitfall: best-effort, must precede the ReplicaSet deletion decision). Do NOT use kubectl delete pod. Throughout the SC3 window, poll+persist pod1's `kubectl get pod -o json` snapshots (Pitfall 3: terminated pods get GC'd — capture continuously, not once at the end) into `$EVIDENCE_DIR/sc3-timestamps-$RUN_TS.txt` with local read-timestamps. After the short job finishes, wait for document-worker to drop to 1 (`waitForReplicasAtMost document-worker 1 <window>`, window derived from scaleDownStabilizationSeconds=15 + margin). Then capture the D-09 triple check into sc3-timestamps: (1) long job status=done AND its result downloads (GET /v1/jobs/{id} then the presigned/result URL returns bytes); (2) `psql "postgres://octo:octo-pass@127.0.0.1:${DB_LOCAL_PORT}/octo_db" -tAc "SELECT count(*) FROM job_events WHERE job_id='$LONG' AND from_status='queued' AND to_status='active'"` equals 1 (no false retry); (3) SIGTERM timestamp = `kubectl get events -n "$NAMESPACE" --field-selector involvedObject.name=$pod1,reason=Killing -o jsonpath='{.items[0].firstTimestamp}'` (NEVER deletionTimestamp — Pitfall 2), pod exit = `containerStatuses[0].lastState.terminated.finishedAt` + exit reason, and jobs.finished_at via psql. ASSERT: SIGTERM_ts < job finished_at AND pod terminated with reason Completed / exit 0 before the 330s grace deadline (graceful, not SIGKILL/OOMKilled). Print all three raw timestamps into the transcript and sc3-timestamps file for the human-verify checkpoint in 28-03.

    Finally, replace the Task-2 insertion marker's trailing region with: set GATE_OK=1 and a loud ALL-PASSED summary ONLY after every SC1/SC2/SC3 assertion has passed. keda-gate.sh must remain byte-unchanged (D-12: separate script).
  </action>
  <verify>
    <automated>bash -n scripts/keda-load-proof.sh && grep -q 'reason=Killing' scripts/keda-load-proof.sh && grep -q 'pod-deletion-cost' scripts/keda-load-proof.sh && grep -q 'sc3-timestamps' scripts/keda-load-proof.sh && grep -q 'waitForReplicasAtMost document-worker 1' scripts/keda-load-proof.sh && [ "$(grep -c 'deletionTimestamp' scripts/keda-load-proof.sh)" -eq 0 ] && ! grep -q 'kubectl delete pod' scripts/keda-load-proof.sh && grep -q 'GATE_OK' scripts/keda-load-proof.sh && git diff --quiet scripts/keda-gate.sh && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/keda-load-proof.sh` exits 0 (the full script with SC3 appended is syntactically valid).
    - SC3 uses long-first/short-second submission and annotates the earliest-creationTimestamp document-worker pod with `controller.kubernetes.io/pod-deletion-cost=-1000`; no `kubectl delete pod` appears anywhere in the script.
    - Script contains ZERO occurrences of `deletionTimestamp` (Pitfall 2 avoided — SIGTERM read from the `reason=Killing` event) and writes `sc3-timestamps-<RUN_TS>.txt`.
    - The D-09 triple-check is present: long job done+download, exactly-one `queued→active` psql count, and SIGTERM(Killing) < completion with a graceful non-SIGKILL exit before the 330s grace.
    - `GATE_OK`/ALL-PASSED summary is set only after all assertions; `git diff --quiet scripts/keda-gate.sh` is true (D-12).
  </acceptance_criteria>
  <done>keda-load-proof.sh's SC3 downscale-soak with deterministic victim selection, Killing-event SIGTERM reads, and the D-09 triple-check is appended and parses; the final ALL-PASSED summary gates on every assertion; keda-gate.sh stays untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate script → committed evidence files | evidence/ is committed (D-03); must never capture secrets |
| ephemeral uv packages → host | python-docx/matplotlib pulled for one-off scripts, never persisted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04 | Information Disclosure | keda-load-proof.sh evidence writes | mitigate | never echo $CLIENT_KEY / $DATABASE_URL / $API_KEY_SALT into $EVIDENCE_DIR files; reuse keda-gate.sh's dev-only throwaway credential pattern (dev-only-change-me-in-real-deploys); evidence rows carry only timestamps, queue depth, replica counts, job/pod names |
| T-28-SC | Tampering | pip/uv installs (python-docx, matplotlib) | accept | consumed ONLY via ephemeral `uv run --with`; never written to go.mod/go.sum/requirements.txt/Dockerfile — not part of the shipped dependency graph. Both are decades-established, very-high-download libraries (RESEARCH Package Legitimacy Audit: python-docx github.com/python-openxml/python-docx, matplotlib github.com/matplotlib/matplotlib). Well-known-package exception applies; no blocking checkpoint required because nothing is persisted |
</threat_model>

<verification>
- Both python fixtures run via ephemeral uv and produce artifacts (Task 1 automated verify).
- keda-load-proof.sh passes bash -n after Task 2 (skeleton + calibration + SC1/SC2 + PNG) and again after Task 3 (SC3 appended), references the overlay + evidence tooling, avoids deletionTimestamp, and leaves keda-gate.sh untouched.
- No persistent dependency added (git status shows only scripts/fixtures/*.py + scripts/keda-load-proof.sh).
</verification>

<success_criteria>
- gen_heavy_docx.py (calibratable) and render_evidence.py (dual-axis PNG) exist and run.
- keda-load-proof.sh implements the SC1/SC2 burst 0→N→0 sampler scenario (Task 2) and the SC3 downscale-soak with deterministic victim selection and the D-09 triple-check (Task 3), gated by a single ALL-PASSED summary.
- Phase 27 gate untouched; no dependency-graph changes.
</success_criteria>

<output>
Create `.planning/phases/28-autoscale-load-proof/28-02-SUMMARY.md` when done.
</output>
</content>
