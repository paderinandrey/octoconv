---
phase: 28-autoscale-load-proof
plan: 03
type: execute
wave: 3
depends_on: [28-01, 28-02]
files_modified:
  - .planning/phases/28-autoscale-load-proof/evidence/
autonomous: false
requirements: [KEDA-03]
user_setup: []

must_haves:
  truths:
    - "D-07: one live in-cluster calibration run tunes gen_heavy_docx.py --page-units so the heavy docx→pdf conversion lands with margin on both ends — comfortably > the 15s scaleDownStabilizationSeconds override and comfortably < DOCUMENT_ENGINE_TIMEOUT 300s (targeting ~200s per D-07); there is NO local soffice, so calibration is a real submitted job"
    - "SC1 (D-04): with the image worker at genuinely 0 replicas and a freshly filled queue, the 20-job burst scales it to ≥2 replicas within 60s — asserted live by keda-load-proof.sh"
    - "SC2 (D-06): after the queue drains the image worker returns to 0 within the cooldown window — asserted live"
    - "SC3 (D-08/D-09): a ~200s document conversion in flight survives a genuine KEDA/HPA 2→1 downscale (the annotated busy pod is the victim) — the job reaches done and downloads, job_events has exactly one queued→active, and the Killing-event SIGTERM timestamp precedes job completion with a graceful (non-SIGKILL) pod exit before the 330s grace deadline"
    - "SC4 (D-01/D-02/D-03): the full 0→N→0 timeline is captured as a committed CSV + dual-axis PNG (queue depth + pod count on one time axis showing steady→burst→time-to-first-replica→time-to-drain→time-to-scale-to-zero) plus a timestamped gate transcript and the SC3 triple-check evidence, all committed under evidence/ so acceptance survives the session"
    - "D-12: the run obeys OrbStack discipline — compose stack down, images pre-built sequentially, gate self-installs KEDA and tears down via EXIT trap, k8s never hot alongside compose"
  artifacts:
    - path: ".planning/phases/28-autoscale-load-proof/evidence/"
      provides: "committed load-proof evidence: sc1-sc2-burst CSV+PNG, gate transcript, sc3-timestamps"
      contains: "sc1-sc2-burst"
  key_links:
    - from: "scripts/keda-load-proof.sh"
      to: ".planning/phases/28-autoscale-load-proof/evidence/"
      via: "CSV sampler + PNG render + transcript tee + sc3 timestamps"
      pattern: "evidence"
---

<objective>
Execute the flagship live acceptance of milestone v1.6: run the load-proof gate against the real OrbStack cluster, calibrate the long document job, capture timestamped 0→N→0 evidence, prove the graceful downscale-soak, and present it all for human sign-off.

This plan runs the tooling from 28-02 against the chart from 28-01 — it produces no new code, only committed evidence (D-03) and a human-verified acceptance.

Purpose: Close KEDA-03 with "доказано с таймстампами" — a committed causal chain, not merely "it scaled."
Output: Evidence artifacts under `.planning/phases/28-autoscale-load-proof/evidence/` (git add -f) + a human-verify checkpoint.
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-autoscale-load-proof/28-CONTEXT.md
@.planning/phases/28-autoscale-load-proof/28-RESEARCH.md
@scripts/keda-load-proof.sh
@scripts/keda-gate.sh
@deploy/chart/octoconv/values-loadproof.yaml

<interfaces>
<!-- Operational facts for the live run. -->
- D-12 / keda-gate.sh header: the CALLER pre-builds all app images SEQUENTIALLY with the pinned `dev` tag and ensures `docker compose ... down -v` BEFORE running the gate; the gate refuses to run if the compose stack is up and tears the k8s stack down on EXIT.
- Calibration target (D-07 re-derived per RESEARCH Pitfall 1): long job duration comfortably > scaleDownStabilizationSeconds (15s, values-loadproof.yaml) and < DOCUMENT_ENGINE_TIMEOUT (300s); ~200s target. Tune via CALIBRATE=1 / PAGE_UNITS.
- Open Question (RESEARCH): the effective HPA sync floor on OrbStack is assumed 15s; confirm the observed 2→1 transition timing during calibration and adjust PAGE_UNITS from the OBSERVED number if it diverges.
- evidence filenames (from keda-load-proof.sh): sc1-sc2-burst-<RUN_TS>.csv/.png, gate-transcript-<RUN_TS>.log, sc3-timestamps-<RUN_TS>.txt.
- `.planning/` is gitignored (.gitignore:6 `/.planning/`) but tracked by convention — evidence files need `git add -f`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live calibration of the SC3 long docx job (D-07)</name>
  <files>.planning/phases/28-autoscale-load-proof/evidence/</files>
  <read_first>
    - scripts/keda-load-proof.sh (the CALIBRATE mode + PAGE_UNITS knob)
    - .planning/phases/28-autoscale-load-proof/28-RESEARCH.md §"Common Pitfalls" Pitfall 1 (re-derive against stabilization, not cooldown) and Pitfall 4 (no local soffice — calibrate live)
    - .planning/phases/28-autoscale-load-proof/28-CONTEXT.md D-07
  </read_first>
  <action>
    Confirm OrbStack discipline first (D-12): ensure the compose stack is down (`docker compose ps` shows no octoconv-* containers) and app images are pre-built sequentially with the pinned `dev` tag (per keda-gate.sh header — the gate will refuse otherwise). Run the calibration path: `CALIBRATE=1 PAGE_UNITS=300 scripts/keda-load-proof.sh` (adjust PAGE_UNITS between runs). Read the observed heavy-docx conversion duration the gate prints (from jobs.started_at/finished_at). Tune PAGE_UNITS across at most a few trial runs until the observed duration lands in the target window: comfortably above the 15s scaleDownStabilizationSeconds override and comfortably below the 300s DOCUMENT_ENGINE_TIMEOUT (target ~200s, per D-07). Also note the observed 2→1 downscale settling time during a trial to confirm the ~15s assumption (RESEARCH Open Question 1); if it diverges, re-derive the target duration from the OBSERVED settling number. Record the final PAGE_UNITS and observed duration in the SUMMARY and as a note the SC3 run will reuse. Each calibration invocation tears down via the EXIT trap — never leave the cluster hot between runs (D-12).
  </action>
  <verify>
    <automated>MISSING — live-cluster calibration; verified by the recorded observed conversion duration falling in (15s, 300s) and captured in the SUMMARY. No offline automated check possible.</automated>
    <human-check>The calibration transcript shows a document-worker conversion of the heavy docx completing in a duration comfortably between 15s and 300s (≈200s target), and the chosen PAGE_UNITS is recorded.</human-check>
  </verify>
  <acceptance_criteria>
    - A live calibration run submitted the heavy docx to the real in-cluster document-worker (not a local dry run) and reported an observed conversion duration.
    - The final chosen PAGE_UNITS yields a duration strictly > 15s and strictly < 300s (recorded in the SUMMARY).
    - The cluster was torn down after calibration (no octoconv/keda release left installed).
  </acceptance_criteria>
  <done>PAGE_UNITS is calibrated so the SC3 long job reliably straddles the 15s downscale window and stays under the 300s engine timeout, verified by a real in-cluster conversion.</done>
</task>

<task type="auto">
  <name>Task 2: Full load-proof gate run + commit evidence (SC1/SC2/SC3/SC4)</name>
  <files>.planning/phases/28-autoscale-load-proof/evidence/</files>
  <read_first>
    - scripts/keda-load-proof.sh (full run path)
    - .planning/phases/28-autoscale-load-proof/28-CONTEXT.md D-01, D-03, D-06, D-09
    - .planning/phases/28-autoscale-load-proof/28-RESEARCH.md §"Security Domain" (no creds in committed evidence)
  </read_first>
  <action>
    With OrbStack discipline confirmed (compose down, images pre-built), run the full gate at the calibrated PAGE_UNITS: `PAGE_UNITS=<calibrated> scripts/keda-load-proof.sh`. The gate must: (SC1/D-04) assert the image worker scales 0→≥2 within 60s under the 20-job burst and record the peak toward 4; (SC2/D-06) assert the image worker returns to 0 after drain; (SC4/D-01/D-02) write the sampler CSV and render the dual-axis PNG showing steady→burst→time-to-first-replica→time-to-drain→time-to-scale-to-zero; (SC3/D-08/D-09) run the downscale-soak and assert the triple check (long job done + downloads; exactly one queued→active in job_events; Killing-event SIGTERM before job completion with a graceful non-SIGKILL exit before the 330s grace). If any assertion fails, this is a real failure — capture the transcript, diagnose (common: WR-01-style empty-metric read, or a mis-timed downscale per Pitfall 1), fix within the tooling/overlay only if it is a tooling defect (NOT by weakening an assertion), and re-run. Do not touch application code or trigger semantics (D-11 out of scope). After a clean PASS, verify the evidence files exist (sc1-sc2-burst CSV+PNG, gate-transcript log, sc3-timestamps txt) and that NONE contain the client key, DATABASE_URL, or API_KEY_SALT (grep the committed files). Then `git add -f` the evidence directory (`.planning/` is gitignored) and confirm the files are staged.
  </action>
  <verify>
    <automated>ls .planning/phases/28-autoscale-load-proof/evidence/sc1-sc2-burst-*.png .planning/phases/28-autoscale-load-proof/evidence/sc1-sc2-burst-*.csv .planning/phases/28-autoscale-load-proof/evidence/gate-transcript-*.log .planning/phases/28-autoscale-load-proof/evidence/sc3-timestamps-*.txt >/dev/null 2>&1 && ! grep -rIl -e 'dev-only-change-me' -e 'DATABASE_URL=postgres' -e 'ApiKey ' .planning/phases/28-autoscale-load-proof/evidence/ && git ls-files --error-unmatch .planning/phases/28-autoscale-load-proof/evidence/sc1-sc2-burst-*.png >/dev/null 2>&1 || git diff --cached --name-only | grep -q 'evidence/sc1-sc2-burst' && echo PASS</automated>
    <human-check>The gate transcript ends with an ALL-PASSED summary listing SC1 (≥2 in 60s), SC2 (back to 0), and SC3 (triple-check) as PASS.</human-check>
  </verify>
  <acceptance_criteria>
    - A single gate run produced all four evidence files (CSV, PNG, transcript log, sc3-timestamps) under evidence/ with the run timestamp.
    - The transcript shows SC1 asserted ≥2 replicas within 60s, SC2 asserted return to 0, and SC3 asserting the D-09 triple-check (done+download, exactly one queued→active, SIGTERM < completion, graceful exit before 330s).
    - No committed evidence file contains `dev-only-change-me`, a `DATABASE_URL=postgres...` string, or an `ApiKey ` header value.
    - The evidence files are staged via `git add -f` (visible in `git diff --cached --name-only`).
  </acceptance_criteria>
  <done>The full 0→N→0 timeline and the SC3 graceful-downscale proof are captured as committed, credential-free evidence with all gate assertions passing.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human acceptance of the timestamped load-proof evidence</name>
  <action>Pause for human review of the committed load-proof evidence. Present the SC4 timeline PNG, the gate transcript, and the SC3 triple-check timestamps; do not proceed to phase completion until the reviewer accepts (or routes gaps back to gap closure). No automated work occurs in this task — automation completed in Tasks 1-2.</action>
  <what-built>
    A full live load-proof run against OrbStack k8s: image worker autoscaled 0→N→0 under a 20-job burst (SC1/SC2), a ~200s document conversion survived a genuine KEDA/HPA downscale (SC3), and the whole 0→N→0 timeline plus the SC3 triple-check were captured as committed evidence (SC4/D-03).
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/28-autoscale-load-proof/evidence/sc1-sc2-burst-<RUN_TS>.png` — confirm the dual-axis chart shows queue depth and pod count on one time axis with the five markers visible: steady state → burst → time-to-first-replica → time-to-drain → time-to-scale-to-zero.
    2. Skim `evidence/gate-transcript-<RUN_TS>.log` — confirm the ALL-PASSED summary: SC1 (image 0→≥2 within 60s, peak toward 4 recorded), SC2 (image back to 0 after drain).
    3. Open `evidence/sc3-timestamps-<RUN_TS>.txt` — confirm the D-09 triple-check: (a) long job reached done and the result downloaded; (b) job_events has exactly one queued→active row (no false retry); (c) the Killing-event SIGTERM timestamp precedes the job completion timestamp and the pod exited gracefully (reason Completed / exit 0) before the 330s grace deadline.
    4. Confirm the evidence is committed (git-tracked) so it survives the session.
  </how-to-verify>
  <resume-signal>Type "approved" to accept the load-proof and close KEDA-03, or describe any gap (e.g. a marker missing from the chart, or an SC3 timestamp ordering that looks wrong).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live cluster run → committed evidence | evidence is committed (D-03); must be credential-free |
| OrbStack daemon | heavy concurrent load has wedged this VM before (three on record) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-05 | Information Disclosure | committed CSV/PNG/log/timestamp evidence | mitigate | Task 2 greps every committed evidence file for `dev-only-change-me`, `DATABASE_URL=postgres`, and `ApiKey ` before commit; evidence rows carry only timestamps, queue depth, replica counts, job/pod names |
| T-28-06 | Denial of Service | OrbStack daemon wedge | mitigate | D-12 discipline enforced: compose stack down before the run (gate preflight refuses otherwise), images pre-built sequentially, EXIT-trap teardown, k8s never hot alongside compose; calibration and full run are separate sequential installs, never parallel |
| T-28-SC | Tampering | pip/uv installs | accept | no new installs in this plan; the ephemeral uv packages were audited in plan 28-02 (well-known, never persisted) |
</threat_model>

<verification>
- Live gate transcript ends ALL-PASSED (SC1/SC2/SC3).
- All four evidence files present under evidence/, credential-free, and git add -f staged (Task 2 automated verify).
- Human reviewer accepts the PNG timeline + SC3 triple-check (Task 3 checkpoint).
</verification>

<success_criteria>
- SC1: image 0→≥2 within 60s at true zero — asserted live.
- SC2: image → 0 after drain — asserted live.
- SC3: ~200s doc job survives a genuine KEDA downscale gracefully — triple-check passes.
- SC4: committed timestamped CSV+PNG+transcript+sc3-timestamps evidence.
- KEDA-03 closed with human sign-off.
</success_criteria>

<output>
Create `.planning/phases/28-autoscale-load-proof/28-03-SUMMARY.md` when done. Record the calibrated PAGE_UNITS, observed long-job duration, observed 2→1 settling time, and any threshold/cooldown tuning observations as input for the deferred production-tuning work (D-11-adjacent, next milestone).
</output>
