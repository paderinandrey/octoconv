---
phase: 29-v1-6-hardening-tail
plan: 03
type: execute
wave: 2
depends_on: [29-01]
files_modified:
  - scripts/keda-load-proof.sh
  - scripts/fixtures/render_evidence.py
  - scripts/fixtures/gen_heavy_docx.py
  - scripts/keda-gate.sh
autonomous: true
requirements: [HARD-03, HARD-04]

must_haves:
  truths:
    - "D-06 fix #2: keda-load-proof.sh busy-pod selection excludes Terminating pods (Running + empty deletionTimestamp) so the pod-deletion-cost annotation lands on the live long-job pod, not a dying one"
    - "D-06 fix #3: the D-09(1) result-download check captures the HTTP status and fails unless it is 200, so an S3 error body is no longer a false PASS"
    - "D-06 fix #4: the SC3 watcher runs in its own process group and the whole group is killed, so no orphaned kubectl -w survives EXIT"
    - "D-06 fix #5: render_evidence.py parses the trailing-Z timestamp defensively and its uv-run call sites pin the interpreter (uv run --python) at all three fixture invocations"
    - "D-06 fix #6: gen_heavy_docx.py resolves SAMPLE_IMAGE relative to __file__ and warns loudly to stderr when the fixture is absent"
    - "D-07: keda-gate.sh has a presigned-from-host step with an OrbStack daemon health pre-flight (loud-fail) and a DIRECT curl on the presigned FQDN URL with bounded retry, no --connect-to and no kubectl port-forward"
    - "The keda-gate.sh live run passes end-to-end, re-verifying the 29-01 WR-06 retry-inclusive query (retry-backlog scale-up) and HARD-04 direct-dial"
  artifacts:
    - path: "scripts/keda-load-proof.sh"
      provides: "gate-tooling fixes #2/#3/#4 + interpreter pins at fixture call sites"
      contains: "deletionTimestamp"
    - path: "scripts/fixtures/render_evidence.py"
      provides: "defensive trailing-Z timestamp parse"
      contains: "replace(\"Z\""
    - path: "scripts/fixtures/gen_heavy_docx.py"
      provides: "__file__-relative SAMPLE_IMAGE + absent-fixture warning"
      contains: "__file__"
    - path: "scripts/keda-gate.sh"
      provides: "presigned direct-dial step with daemon pre-flight"
      contains: "presign"
  key_links:
    - from: "scripts/keda-gate.sh"
      to: "MinIO presigned FQDN URL"
      via: "direct curl without --connect-to"
      pattern: "curl"
    - from: "scripts/keda-load-proof.sh"
      to: "SC3 busy-pod selection"
      via: "Running + empty deletionTimestamp filter"
      pattern: "deletionTimestamp"
---

<objective>
Close the remaining five HARD-03 gate-tooling warnings from 28-REVIEW (fixes #2–#6) and HARD-04 (K8S-02 presigned direct-dial recheck / D-07). This plan is Wave 2 — it depends on 29-01 because the keda-gate.sh live run is the re-verification vehicle for 29-01's behavior-changing WR-06 retry-inclusive PromQL query.

Purpose: The gate scripts are the proof surface for the whole KEDA program; a false-PASS download check, a stale-pod race, or an orphaned watcher undermines every future load-proof (including the Phase 33 audio scale-from-zero). HARD-04 closes the 24-VERIFICATION degraded-transport caveat by proving a presigned URL resolves via a direct host dial rather than the port-forward workaround.
Output: Three surgical fixes in keda-load-proof.sh, two portability fixes in the Python fixtures, a new direct-dial presigned step in keda-gate.sh, and a passing live keda-gate.sh run.
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-v1-6-hardening-tail/29-CONTEXT.md
@.planning/phases/29-v1-6-hardening-tail/29-PATTERNS.md
@.planning/milestones/v1.6-phases/28-autoscale-load-proof/28-REVIEW.md
@scripts/keda-load-proof.sh
@scripts/fixtures/render_evidence.py
@scripts/fixtures/gen_heavy_docx.py
@scripts/keda-gate.sh

<interfaces>
<!-- Extracted from PATTERNS.md + 28-REVIEW; exact line numbers for the surgical fixes. -->

keda-load-proof.sh fix targets:
  - #2 stale-pod (695-705): BUSY_POD chosen by earliest creationTimestamp; must exclude Terminating (field-selector status.phase=Running AND deletionTimestamp empty). Keep the two-tier label fallback (app.kubernetes.io/component=document-worker then app=document-worker).
  - #3 download check (836-844): curl -s ... -w '%{size_download}' with no -f; add %{http_code} capture (suggested RESULT_META/RESULT_CODE), FAIL unless code==200 AND bytes>0. Existing http_code idiom lives at postJob (:329).
  - #4 orphaned watcher (735-752, killed at 885-887 and in teardown 162-165): `snapshotLoop &` + inner `kubectl get pod -w | while read` pipeline reparents; kill the process GROUP (set -m / setsid + `kill -- -PID`, or `pkill -P "$SNAPSHOT_PID"`). Insertion point for the effective kill is the existing teardown SNAPSHOT_PID sequence — fix the kill, don't restructure the trap.
  - #5 call sites: render_evidence.py at :622 (`uv run --with matplotlib python3 ...`); gen_heavy_docx.py at :407 and :657 (`uv run --with python-docx python3 ...`). Add `--python <ver>` to all three.

render_evidence.py: docstring contract at 8-11; datetime.fromisoformat(row["timestamp"]) at :72 needs the trailing 'Z' handled (`.replace("Z","+00:00")`).

gen_heavy_docx.py: SAMPLE_IMAGE at :31-33 is CWD-relative; resolve via os.path.dirname(os.path.abspath(__file__)) + ../../internal/e2e/testdata/sample.png (two levels up). Graceful-degradation call site have_sample_image at :63 stays; add a stderr WARNING when absent.

keda-gate.sh (currently ZERO presigned code — grep presign|connect-to|download_url = no matches):
  - log() helper (:79); assert_nonempty/assert_eq (:59-77); teardown()+trap (:86-119, add any new bg PID here).
  - Attachment point: SC2 image-class job IMAGE_JOB_ID (:365-373); drain-wait pattern (:416-426); download_url extraction shape `grep -o '"download_url":"[^"]*"'` (mirrors keda-load-proof.sh:812).
  - Reference the block being adapted-minus-workaround: keda-load-proof.sh:816-844 (port-forward + --connect-to). D-07 REMOVES that workaround: pre-flight OrbStack daemon health (loud-fail if wedged) + direct curl on the presigned FQDN with bounded retry.
  - Closes: .planning/milestones/v1.6-phases/24-helm-chart-core/24-VERIFICATION.md:11,31 (direct host-dial re-validation).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: keda-load-proof.sh gate-tooling fixes #2 (stale-pod), #3 (download check), #4 (orphaned watcher) + interpreter pins (#5 call sites)</name>
  <files>scripts/keda-load-proof.sh</files>
  <read_first>
    - scripts/keda-load-proof.sh (read the fix regions: 695-705, 735-752, 816-844, 885-887, teardown 153-202, and the fixture call sites 407, 622, 657)
    - .planning/milestones/v1.6-phases/28-autoscale-load-proof/28-REVIEW.md (WR-02, WR-03, WR-04, WR-05 full text with suggested fixes)
    - .planning/phases/29-v1-6-hardening-tail/29-PATTERNS.md (section "keda-load-proof.sh" — fixes #2/#3/#4 with exact excerpts)
  </read_first>
  <action>
    Apply four surgical fixes to keda-load-proof.sh.
    (#2 stale-pod, ~695-705) Change BUSY_POD selection to exclude Terminating pods — add `--field-selector=status.phase=Running` and/or select `.items[?(@.metadata.deletionTimestamp=="")]` so `pod-deletion-cost=-1000` lands on the genuinely-busy long-job pod, not a dying earlier-creationTimestamp pod. Keep the existing `--sort-by=.metadata.creationTimestamp -o jsonpath=...` shape and the two-tier label-selector fallback.
    (#3 download check, ~836-844) Capture the HTTP status alongside bytes — e.g. `-w '%{http_code} %{size_download}'` into RESULT_META, split into RESULT_CODE/RESULT_BYTES, and FAIL loudly unless `RESULT_CODE == 200 && RESULT_BYTES > 0`. This rejects a MinIO 403/404 XML error body (nonzero bytes) that currently reads as a false PASS. Match the existing `%{http_code}` idiom used by postJob (~:329).
    (#4 orphaned watcher, ~735-752 + teardown) Run the SC3 watcher (`snapshotLoop`) in its own process group (`set -m`/`setsid`) and kill the whole group (`kill -- -"$SNAPSHOT_PID"` or `pkill -P "$SNAPSHOT_PID"`) at both the STEP-8 stop and in teardown, so the reparented `kubectl get pod -w | while read` child does not survive EXIT and keep appending to SC3_TIMESTAMPS_FILE. This is the project's "kill the whole tree, not the direct child" idiom (exec.go Setpgid precedent). Fix the kill; do not restructure the trap architecture.
    (#5 call sites) Add `uv run --python <ver>` (a 3.11+ pin) to all three fixture invocations: render_evidence.py at ~:622, gen_heavy_docx.py at ~:407 and ~:657, so a host `python3` below 3.11 cannot silently break a fully-passed live run at render time.
    Keep every assertion group `&&`-chained / loud-fail; do not introduce a `;` that masks an earlier failure.
  </action>
  <verify>
    <automated>bash -c 'set -e; cd /Users/apaderin/dev/octoconv; bash -n scripts/keda-load-proof.sh && grep -q "deletionTimestamp\|status.phase=Running" scripts/keda-load-proof.sh && grep -Eq "RESULT_CODE|RESULT_META|http_code.*size_download|%\{http_code\} %\{size_download\}" scripts/keda-load-proof.sh && grep -Eq "pkill -P|kill -- -|setsid|set -m" scripts/keda-load-proof.sh && [ "$(grep -c "uv run --python" scripts/keda-load-proof.sh)" -ge 3 ]'</automated>
  </verify>
  <acceptance_criteria>
    - Structural: `bash -n scripts/keda-load-proof.sh` clean.
    - Source assertion (#2): busy-pod selection references `deletionTimestamp` or `status.phase=Running`.
    - Source assertion (#3): the result-download path captures HTTP status (RESULT_CODE/RESULT_META or `%{http_code} %{size_download}`) and gates on 200.
    - Source assertion (#4): a process-group kill (`pkill -P` / `kill -- -` / `setsid` / `set -m`) guards the SC3 watcher.
    - Source assertion (#5): at least 3 `uv run --python` pinned invocations (render_evidence + gen_heavy_docx ×2).
  </acceptance_criteria>
  <done>keda-load-proof.sh selects only live busy pods, fails on a non-200 result download, leaves no orphaned watcher, and pins the fixture interpreter at every call site.</done>
</task>

<task type="auto">
  <name>Task 2: Python fixture portability fixes — render_evidence.py defensive timestamp (#5) + gen_heavy_docx.py __file__-relative SAMPLE_IMAGE (#6)</name>
  <files>scripts/fixtures/render_evidence.py, scripts/fixtures/gen_heavy_docx.py</files>
  <read_first>
    - scripts/fixtures/render_evidence.py (docstring 8-11; datetime.fromisoformat call ~:72)
    - scripts/fixtures/gen_heavy_docx.py (SAMPLE_IMAGE construction ~:31-33; have_sample_image ~:63; usage ~:72)
    - .planning/milestones/v1.6-phases/28-autoscale-load-proof/28-REVIEW.md (WR-05, WR-06 full text)
    - .planning/phases/29-v1-6-hardening-tail/29-PATTERNS.md (sections "render_evidence.py (#5)" and "gen_heavy_docx.py (#6)")
  </read_first>
  <action>
    (#5 render_evidence.py) Make the timestamp parse defensive regardless of interpreter version: parse `row["timestamp"].replace("Z", "+00:00")` through `datetime.fromisoformat(...)` at ~:72 (the sampler writes a trailing `Z`, which pre-3.11 `fromisoformat` rejects). Update the docstring invocation example (8-11) to include the `uv run --python <ver>` pin so the documented contract matches the pinned call site from Task 1. Belt-and-suspenders with Task 1's call-site pin.
    (#6 gen_heavy_docx.py) Change SAMPLE_IMAGE (~:31-33) from a CWD-relative join to one resolved relative to the script: `os.path.dirname(os.path.abspath(__file__))` plus two `..` up to repo root, then `internal/e2e/testdata/sample.png` (or the pathlib `Path(__file__).resolve().parents[2] / ...` form). Add a loud `WARNING: ... not found -- generating WITHOUT images; calibration will not match` to stderr when `have_sample_image` is False (the existing graceful-skip at ~:63 stays; only the constant's construction and the warning are added), so a silently image-free fixture can never invalidate D-07 calibration without signal.
  </action>
  <verify>
    <automated>bash -c 'set -e; cd /Users/apaderin/dev/octoconv; python3 -c "import ast; ast.parse(open(\"scripts/fixtures/render_evidence.py\").read()); ast.parse(open(\"scripts/fixtures/gen_heavy_docx.py\").read())" && grep -q "replace(\"Z\"" scripts/fixtures/render_evidence.py && grep -q "__file__" scripts/fixtures/gen_heavy_docx.py && grep -Eq "parents\[2\]|dirname" scripts/fixtures/gen_heavy_docx.py && grep -qi "warning" scripts/fixtures/gen_heavy_docx.py'</automated>
  </verify>
  <acceptance_criteria>
    - Structural: both files parse as valid Python (`ast.parse`).
    - Source assertion (#5): render_evidence.py normalizes the trailing `Z` (`replace("Z"...)`) before `fromisoformat`.
    - Source assertion (#6): gen_heavy_docx.py builds SAMPLE_IMAGE from `__file__` (via `parents[2]` or `dirname(abspath(__file__))`) and emits a `WARNING` to stderr when the fixture is absent.
  </acceptance_criteria>
  <done>render_evidence.py parses timestamps on any 3.x interpreter; gen_heavy_docx.py finds its fixture regardless of CWD and warns loudly when it cannot.</done>
</task>

<task type="auto">
  <name>Task 3: keda-gate.sh presigned direct-dial step (D-07 / HARD-04) + live gate run</name>
  <files>scripts/keda-gate.sh</files>
  <read_first>
    - scripts/keda-gate.sh (log() :79; assert_nonempty/assert_eq :59-77; teardown()+trap :86-119; SC2 IMAGE_JOB_ID :365-373; drain-wait :416-426)
    - scripts/keda-load-proof.sh (:793-844 — the port-forward/--connect-to presigned block being adapted-minus-workaround; download_url extraction :812)
    - .planning/milestones/v1.6-phases/24-helm-chart-core/24-VERIFICATION.md (K8S-02 degraded-transport caveat, lines 11 and 31)
    - .planning/phases/29-v1-6-hardening-tail/29-PATTERNS.md (section "keda-gate.sh (D-07)")
  </read_first>
  <action>
    Add a new presigned-from-host step to keda-gate.sh (a STEP inside the existing gate, not a separate install cycle), announced via the `log "STEP N: ..."` idiom. keda-gate.sh currently has NO presigned code — this ADDS a step adapted from keda-load-proof.sh's block with the workaround REMOVED.
    1. Pre-flight: check the OrbStack daemon/proxy is healthy and LOUD-FAIL (exit nonzero with diagnostics) if wedged — do NOT mask a wedge with a workaround. This is what distinguishes "daemon wedged (investigate)" from "presign genuinely broken (real failure)".
    2. Attach to the existing SC2 image-class job (IMAGE_JOB_ID, ~:365-373): after `waitForReplicasAtLeast worker 1 120`, poll it to `done` using the existing drain-wait pattern (~:416-426), then extract its `download_url` with the `grep -o '"download_url":"[^"]*"'` shape.
    3. Direct dial: a bare `curl` against the presigned FQDN URL from the OrbStack host — NO `--connect-to`, NO `kubectl port-forward` — with bounded retry; capture `%{http_code}` and assert 200 plus nonzero bytes (reuse assert_eq/assert_nonempty).
    4. Register any new background/port-forward PID in teardown()'s kill sequence (matching the API_PF_PID/DB_PF_PID pattern), and keep the EXIT-trap teardown intact.
    Keep all assertion groups `&&`-chained / loud-fail; never introduce a `;` that masks an earlier failure. Then RUN the gate live (see acceptance_criteria) — the run re-verifies 29-01's WR-06 retry-inclusive query (retry-backlog scale-up) as well as HARD-04.
  </action>
  <verify>
    <automated>bash -c 'set -e; cd /Users/apaderin/dev/octoconv; bash -n scripts/keda-gate.sh && grep -Eq "presign|download_url|PRESIGNED" scripts/keda-gate.sh && [ "$(grep -c -- "--connect-to" scripts/keda-gate.sh)" -eq 0 ] && [ "$(grep -c "kubectl port-forward" scripts/keda-gate.sh)" -ge 0 ] && grep -Eiq "daemon|orbstack|pre-flight|health" scripts/keda-gate.sh'</automated>
  </verify>
  <acceptance_criteria>
    - Structural: `bash -n scripts/keda-gate.sh` clean; the new step references a presigned/download_url fetch and a daemon/OrbStack health pre-flight.
    - Source assertion: keda-gate.sh contains ZERO `--connect-to` (the direct dial must not use the port-forward rewrite workaround).
    - LIVE GATE (OrbStack discipline — compose down first, sequential builds, teardown after): `bash scripts/keda-gate.sh` exits 0, with the presigned direct-dial step PASS (HTTP 200, nonzero bytes) and the WR-06 retry-backlog scale-up behavior confirmed.
    - Behavior assertion: if the OrbStack daemon is wedged, the step fails LOUD with diagnostics rather than passing via a workaround.
  </acceptance_criteria>
  <done>keda-gate.sh proves a presigned MinIO URL resolves via a direct host dial against a health-checked daemon (no port-forward / --connect-to), and the live run re-verifies the 29-01 retry-query behavior change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OrbStack host → MinIO presigned FQDN URL | The gate must prove genuine host-reachability, not a transport workaround; a wedged daemon must be distinguishable from a broken presign |
| gate script → committed evidence / exit code | The script's exit code IS the gate; a false PASS or an orphaned process corrupts the proof surface for every future load-proof |
| fixture host interpreter → render/generate step | An unpinned interpreter or CWD-relative asset silently degrades a fully-passed run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-20 | Information Disclosure / false validation | D-09(1) download check | mitigate | Capture `%{http_code}` and gate on 200 (fix #3) so a MinIO error-body is not accepted as a successful download |
| T-29-21 | Repudiation / evidence integrity | SC3 orphaned watcher | mitigate | Process-group kill (fix #4) stops the reparented `kubectl -w` so it cannot append to SC3_TIMESTAMPS_FILE after capture is believed stopped |
| T-29-22 | Tampering | presigned host-reachability masked by workaround | mitigate | Direct dial without `--connect-to` (D-07) plus a loud-fail daemon pre-flight distinguishes a wedged daemon from a genuinely broken presign |
| T-29-23 | Denial of Service (test flake) | stale Terminating busy-pod | mitigate | Running + empty-deletionTimestamp selection (fix #2) prevents the pod-deletion-cost annotation landing on a dying pod |
| T-29-SC | Tampering | uv `--with matplotlib` / `--with python-docx` PyPI pulls | accept | No NEW package installs introduced — matplotlib and python-docx are pre-existing, established PyPI packages already used by these fixtures; this phase only pins the interpreter version |
</threat_model>

<verification>
- Offline: `bash -n` clean on both scripts; `ast.parse` clean on both fixtures; source assertions for all five HARD-03 fixes; ZERO `--connect-to` in keda-gate.sh.
- Live gate (HARD-04 + WR-06 re-verify): `bash scripts/keda-gate.sh` exits 0 — presigned direct-dial step PASS and retry-backlog scale-up PASS. Depends on 29-01 being landed (the retry-inclusive query is what the run re-verifies).
- OrbStack discipline: compose down before the k8s gate; sequential image builds; teardown after (EXIT-trap).
</verification>

<success_criteria>
- SC3 (remainder): the five gate-tooling warnings #2–#6 are closed, each with its script/fixture diff and a gate re-run (HARD-03 / D-06).
- SC4: a presigned result URL resolves from the OrbStack host via a direct dial against a verified-healthy daemon, with no `kubectl port-forward` / `curl --connect-to` workaround (HARD-04 / D-07).
- The live keda-gate.sh run re-verifies 29-01's WR-06 retry-inclusive PromQL behavior change.
</success_criteria>

<output>
Create `.planning/phases/29-v1-6-hardening-tail/29-03-SUMMARY.md` when done (commit with `git add -f`).
</output>
