---
phase: 31-queue-worker-routing-integration
plan: 04
type: execute
wave: 3
depends_on: [31-01, 31-02, 31-03]
files_modified:
  - cmd/audio-worker/main.go
  - .env.example
autonomous: false
requirements: [AUD-05]

must_haves:
  truths:
    - "cmd/audio-worker consumes the audio queue, calls SetAudioModelPath from AUDIO_MODEL_PATH, and threads AUDIO_MAX_DURATION_SECONDS into NewHandler"
    - "An uploaded audio job flows queued → active → done end-to-end via go run ./cmd/audio-worker against compose infra with local ffmpeg/whisper-cli"
    - ".env.example documents AUDIO_ENGINE_TIMEOUT (placeholder), AUDIO_MAX_RETRY, AUDIO_WORKER_CONCURRENCY, AUDIO_MAX_DURATION_SECONDS, AUDIO_MODEL_PATH, the RECONCILER_ACTIVE_STALE_AFTER global-raise tradeoff, and the MAX_UPLOAD_BYTES long-audio decision"
  artifacts:
    - path: "cmd/audio-worker/main.go"
      provides: "audio-worker process entry point (asynq server bound to the audio queue)"
      contains: "HandleAudioConvert"
    - path: ".env.example"
      provides: "audio worker env block + documented operator levers"
      contains: "AUDIO_ENGINE_TIMEOUT"
  key_links:
    - from: "cmd/audio-worker/main.go"
      to: "queue.TypeAudioConvert / h.HandleAudioConvert"
      via: "mux.HandleFunc"
      pattern: "mux.HandleFunc\\(queue.TypeAudioConvert"
    - from: "cmd/audio-worker/main.go"
      to: "convert.SetAudioModelPath"
      via: "os.Getenv(AUDIO_MODEL_PATH) before srv.Start"
      pattern: "SetAudioModelPath"
    - from: "cmd/audio-worker/main.go"
      to: "worker.NewHandler audioMaxDuration"
      via: "envDuration(AUDIO_MAX_DURATION_SECONDS)"
      pattern: "AUDIO_MAX_DURATION_SECONDS"
---

<objective>
Ship the `cmd/audio-worker` process entry point (mirroring `cmd/document-worker` verbatim, one class swap) and the `.env.example` audio block, then prove the whole phase end-to-end with a live run: an uploaded audio job flows queued → active → done through `go run ./cmd/audio-worker` against the compose infra (postgres/redis/minio) with locally-installed ffmpeg + whisper-cli. This is where the migration, converter registration, worker classifier, duration guard, API routing, and reconciler routing all get exercised together for the first time against real infrastructure — the single most likely place a "code-complete but never wired" gap surfaces.

Purpose: SC1's live end-to-end proof + the operator-facing env documentation (including the two deliberate deferred decisions: RECONCILER_ACTIVE_STALE_AFTER global-raise tradeoff and the MAX_UPLOAD_BYTES long-audio ceiling).
Output: cmd/audio-worker/main.go, .env.example audio block, live E2E verification.
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-queue-worker-routing-integration/31-RESEARCH.md
@.planning/phases/31-queue-worker-routing-integration/31-PATTERNS.md

<interfaces>
<!-- Verified from codebase reads. -->

cmd/document-worker/main.go (166 lines) — EXACT template: db.Connect → storage.New → queue.RedisOpt → queue.NewClient → jobs.NewRepo → worker.NewHandler(...) → mux.HandleFunc(queue.TypeDocumentConvert, h.HandleDocumentConvert) → asynq.NewServer{Concurrency, Queues:{QueueDocument:1}, RetryDelayFunc: queue.RetryDelayFunc, ShutdownTimeout} → localhost /metrics listener → graceful shutdown. Private helpers envInt/envDuration/firstField are duplicated per-package (no shared config pkg).
cmd/document-worker/main.go:79 — convert.SetVeraPDFTimeout(envDuration(...)) called before srv.Start — the setter-in-main precedent to mirror for SetAudioModelPath.
worker.NewHandler — AFTER Plan 02 this has a 10th positional param audioMaxDuration time.Duration (audio worker passes envDuration("AUDIO_MAX_DURATION_SECONDS", ...); non-audio params webhookRepo/deliverer/signingSecret/presignTTL passed as nil/nil/nil/0).
queue.TypeAudioConvert / queue.QueueAudio / h.HandleAudioConvert — from Plans 01 & 02.
convert.SetAudioModelPath(path string) — from Plan 01; call with os.Getenv("AUDIO_MODEL_PATH") BEFORE srv.Start.

Local dev deps (Phase 30-provisioned, verified present): /opt/homebrew/bin/ffmpeg, /opt/homebrew/bin/ffprobe, /opt/homebrew/bin/whisper-cli, ~/.cache/whisper/ggml-base.bin.
Compose infra ports: postgres 5434, minio 9100/9101 (README). Operator discipline (STATE.md): never run compose + k8s stacks hot simultaneously.
.env.example existing blocks to mirror: "# Document worker" / "# Chromium (html) worker" blocks; RECONCILER_ACTIVE_STALE_AFTER line.

Scope fence: do NOT add QueueAudio to cmd/api/main.go NewQueueDepthCollector (that is AUD-08 / Phase 33). No Dockerfile.audio-worker, no compose service (Phase 32).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: cmd/audio-worker/main.go + .env.example audio block</name>
  <files>cmd/audio-worker/main.go, .env.example</files>
  <read_first>
    - cmd/document-worker/main.go (full 166-line template)
    - cmd/document-worker/main.go lines 55-95 (NewHandler call + asynq.NewServer config + helpers) and line 79 (SetVeraPDFTimeout-in-main precedent)
    - .env.example "# Document worker" and "# Chromium (html) worker" blocks + the RECONCILER_ACTIVE_STALE_AFTER line
    - 31-PATTERNS.md cmd/audio-worker/main.go section (full annotated template) + .env.example section
    - 31-RESEARCH.md Pitfall 2 (stale-after tradeoff) + Pitfall 3 (MAX_UPLOAD_BYTES long audio)
  </read_first>
  <action>
    Create cmd/audio-worker/main.go copying cmd/document-worker/main.go's structure, swapping every Document/document identifier for Audio/audio: worker.NewHandler with envDuration("AUDIO_ENGINE_TIMEOUT", 600*time.Second) as engineTimeout (comment: [ASSUMED] placeholder, Phase 32 re-derives from RTF measurement — do NOT copy DOCUMENT_ENGINE_TIMEOUT's 300s), nil/nil for webhookRepo/deliverer, qc for enqueuer, nil/0 for signingSecret/presignTTL, and envDuration("AUDIO_MAX_DURATION_SECONDS", <sane default e.g. 4*time.Hour>) as the new 10th audioMaxDuration param. Call convert.SetAudioModelPath(os.Getenv("AUDIO_MODEL_PATH")) BEFORE srv.Start (mirroring SetVeraPDFTimeout at document-worker:79). mux.HandleFunc(queue.TypeAudioConvert, h.HandleAudioConvert). asynq.NewServer with Concurrency: envInt("AUDIO_WORKER_CONCURRENCY", 2), Queues: map[string]int{queue.QueueAudio: 1}, RetryDelayFunc: queue.RetryDelayFunc, ShutdownTimeout: envDuration("AUDIO_ENGINE_TIMEOUT", 600*time.Second) + 10*time.Second. Own localhost /metrics listener + graceful shutdown, exactly like document-worker. Duplicate envInt/envDuration/firstField helpers verbatim (per-package convention). Startup log: "🐙 audio-worker starting (queue=%s)". Do NOT construct any sweeper here (the sweep runs solely in cmd/webhook-worker under the advisory lock — document-worker's own comment explains why; a second sweeper would race). Do NOT touch cmd/api/main.go NewQueueDepthCollector (Phase 33 scope).
    In .env.example add a "# Audio worker" block: AUDIO_ENGINE_TIMEOUT=600s (comment: [ASSUMED] Phase-32-RTF-measurement placeholder, do not copy DOCUMENT_ENGINE_TIMEOUT verbatim), AUDIO_MAX_RETRY=3, AUDIO_WORKER_CONCURRENCY=2, AUDIO_MAX_DURATION_SECONDS (with the value matching the code default), AUDIO_MODEL_PATH (local-dev example ~/.cache/whisper/ggml-base.bin; container path /models/ggml-base.bin noted). Update the RECONCILER_ACTIVE_STALE_AFTER line/comment to document the global raise (now 15m default) and its tradeoff: raising it for audio also raises image/document/html staleness-detection latency; accepted per Pitfall 2 option 1 because AudioUniqueTTL + ErrDuplicateTask is the actual double-processing safety mechanism. Add a comment near MAX_UPLOAD_BYTES documenting the deliberate decision: keep the global 100MiB default, note the operator lever, and note that oversized long audio returns a predictable 413 (per-format ceiling deferred to Phase 32 alongside RTF sizing — do not fail silently).
  </action>
  <verify>
    <automated>gofmt -l cmd/audio-worker && go vet ./cmd/audio-worker/... && go build ./cmd/audio-worker/ && grep -c "mux.HandleFunc(queue.TypeAudioConvert" cmd/audio-worker/main.go && grep -c "SetAudioModelPath\|AUDIO_MAX_DURATION_SECONDS" cmd/audio-worker/main.go && grep -c "AUDIO_ENGINE_TIMEOUT\|AUDIO_MODEL_PATH\|MAX_UPLOAD_BYTES" .env.example</automated>
  </verify>
  <acceptance_criteria>
    - cmd/audio-worker/main.go builds; binds asynq server to queue.QueueAudio only (Queues map has exactly QueueAudio)
    - mux registers queue.TypeAudioConvert → h.HandleAudioConvert
    - convert.SetAudioModelPath(os.Getenv("AUDIO_MODEL_PATH")) called before srv.Start
    - NewHandler passes envDuration("AUDIO_MAX_DURATION_SECONDS", ...) as the 10th param and nil/nil/nil/0 for webhook-only fields
    - No sweeper constructed; cmd/api/main.go NewQueueDepthCollector untouched (grep confirms no QueueAudio added there)
    - .env.example documents all five AUDIO_* vars, the RECONCILER_ACTIVE_STALE_AFTER tradeoff, and the MAX_UPLOAD_BYTES long-audio decision
  </acceptance_criteria>
  <done>cmd/audio-worker is a runnable audio-queue consumer with local-dev model-path resolution; .env.example documents every audio lever and the two deferred-decision tradeoffs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live end-to-end audio conversion (queued → active → done)</name>
  <files>(verification only — no files modified)</files>
  <what-built>
    The full audio vertical is now wired: migration 0006 (jobs.engine accepts 'audio'), AudioConverter registered, audio queue/task/TTL, HandleAudioConvert + stage-aware classifier + duration guard, API SniffAudio/opts/enqueue routing, reconciler routing, and cmd/audio-worker. This checkpoint proves it works against real infrastructure — the automatable prep (build, script) is done by Claude; the operator confirms the environment precondition (compose up, k8s down) and the transcript result.
  </what-built>
  <read_first>
    - README.md (compose bring-up, env sourcing: `set -a && . ./.env && set +a`, non-standard ports)
    - .planning/phases/31-queue-worker-routing-integration/31-RESEARCH.md Environment Availability section (compose not currently running; never run compose + k8s hot simultaneously)
    - internal/convert/testdata/audio/ (existing fixtures: jfk.wav, sample.wav, sample.m4a, sample-id3.mp3)
  </read_first>
  <action>
    Before pausing for the operator, Claude performs every automatable step and reports the output: `go build ./...` green; ask the operator to confirm the k8s stack is DOWN, then (only on approval, per the "never run both hot" discipline — four confirmed daemon wedges on record) `docker compose up -d postgres redis minio createbucket`; source env with the audio vars set (AUDIO_MODEL_PATH=~/.cache/whisper/ggml-base.bin, AUDIO_MAX_DURATION_SECONDS large enough for the fixture); run `go run ./cmd/migrate` (or start the API which auto-migrates) and confirm the 0006 constraint applied via `\d jobs` (correct the migration DROP name now if the live jobs_engine_check name differs); start `go run ./cmd/api` and `go run ./cmd/audio-worker` in the background; POST internal/convert/testdata/audio/jfk.wav to /v1/jobs with target=txt (optionally opts {"language":"en"}), capture the job_id, and assert 202 (NOT 500 — a 500 means the migration or routing is broken); poll GET /v1/jobs/{id} for queued → active → done; download the presigned transcript URL. Report the create status code, observed status transitions, and transcript text. Only the environment precondition and the transcript-quality judgment require a human.
  </action>
  <how-to-verify>
    1. Confirm the compose infra is up and the k8s stack is DOWN (operator precondition).
    2. Confirm 0006 applied: `\d jobs` shows jobs_engine_check includes 'audio'.
    3. Create job returns 202 Accepted (not 500).
    4. GET /v1/jobs/{id} status transitions queued → active → done.
    5. The presigned transcript URL returns non-empty, plausible transcript text (not empty/garbage).
  </how-to-verify>
  <verify>
    <human-check>Operator confirms the audio job reached done with a plausible transcript and no jobs_engine_check 500 on create.</human-check>
  </verify>
  <done>An uploaded audio job flows queued → active → done via go run ./cmd/audio-worker against compose infra, producing a valid transcript; create returns 202 (migration + routing proven live).</done>
  <resume-signal>Type "approved" once the audio job reached done with a valid transcript, or describe the failure (status stuck, 500 on create, empty transcript, wrong queue).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AUDIO_MODEL_PATH env → whisper-cli -m | operator-set process env (server-controlled), never client bytes |
| uploaded audio → live worker pipeline | first real client-facing exercise of the full audio vertical |
| compose vs k8s daemon | operator discipline: never both hot (four confirmed daemon wedges) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-05 | Denial of Service / correctness | jobs.engine CHECK constraint at live INSERT | mitigate | Live E2E asserts 202 (not 500) on create — proves 0006 applied and the constraint accepts 'audio'; confirms jobs_engine_check via \d jobs and corrects the DROP name if the live name differs. |
| T-31-11 | Denial of Service | audio-worker binds/consumes an unbounded queue | mitigate | Queues map limited to {QueueAudio:1}; ShutdownTimeout = AUDIO_ENGINE_TIMEOUT + 10s for graceful in-flight completion; Concurrency bounded by AUDIO_WORKER_CONCURRENCY (Phase 32 re-sizes from measured RSS). |
| T-31-06 | Tampering (path) | AUDIO_MODEL_PATH → whisper-cli -m | accept | Operator-set env var, same trust class as defaultAudioModelPath (T-30-10 closed); no client input reaches model(). |
| T-31-SC | Tampering (supply chain) | go module installs | accept | No new external packages this phase (RESEARCH.md Package Legitimacy Audit N/A). |
</threat_model>

<verification>
- `go build ./...` green (cmd/audio-worker compiles against the 10-param NewHandler + audio queue symbols)
- `gofmt -l cmd/audio-worker` prints nothing; `go vet ./...` clean
- Live E2E: audio job reaches `done` with a valid transcript; create returns 202 (no jobs_engine_check 500)
- .env.example documents all AUDIO_* vars + the two deferred-decision tradeoffs
</verification>

<success_criteria>
- SC1 (live): uploaded audio job routed to the audio queue, consumed by cmd/audio-worker end-to-end (queued → active → done)
- cmd/audio-worker runnable locally with AUDIO_MODEL_PATH resolution
- Operator-facing env documentation complete, including RECONCILER_ACTIVE_STALE_AFTER and MAX_UPLOAD_BYTES decisions
</success_criteria>

<output>
Create `.planning/phases/31-queue-worker-routing-integration/31-04-SUMMARY.md` when done
</output>
