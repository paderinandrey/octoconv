---
phase: 32-containerization-local-e2e-rtf-gate
plan: 04
type: execute
wave: 3
depends_on: ["32-01", "32-02", "32-03"]
files_modified:
  - docker-compose.yml
  - .github/workflows/ci.yml
  - .env.example
autonomous: true
requirements: [AUD-06, AUD-07]
must_haves:
  truths:
    - "An audio-worker compose service exists with the RTF-measured AUDIO_ENGINE_TIMEOUT/AUDIO_WORKER_CONCURRENCY and matching cpus/memory limits"
    - "AUDIO_ENGINE_TIMEOUT + AUDIO_MAX_RETRY are set IDENTICALLY across all 7 queue.NewClient()-constructing services in ONE commit (IN-02)"
    - "grep -c AUDIO_ENGINE_TIMEOUT docker-compose.yml == 7"
    - "The CI bake matrix builds audio-worker (docker-build cache-to/from + e2e cache-from)"
    - ".env.example ships the measured AUDIO_ENGINE_TIMEOUT (no [ASSUMED] placeholder) with a derivation comment"
  artifacts:
    - path: "docker-compose.yml"
      provides: "audio-worker service + IN-02 cross-process env consistency"
      contains: "audio-worker"
  key_links:
    - from: "docker-compose.yml audio-worker service"
      to: "Dockerfile.audio-worker"
      via: "build.dockerfile"
      pattern: "dockerfile: Dockerfile.audio-worker"
    - from: ".github/workflows/ci.yml bake targets"
      to: "audio-worker"
      via: "cache-to/cache-from scope lines (auto-derived target from compose build block)"
      pattern: "audio-worker.cache-from"
---

<objective>
Wire the measured audio configuration into the deployable/CI substrate: add the
`audio-worker` compose service (closing IN-04 — the API already enqueues audio jobs
that today sit queued forever with no consumer), propagate the RTF-measured
`AUDIO_ENGINE_TIMEOUT`/`AUDIO_MAX_RETRY` IDENTICALLY across all seven
`queue.NewClient()`-constructing services in one commit (IN-02 — divergence reopens the
T-03-10 double-processing race), add the CI bake-matrix cache scopes, and replace the
`.env.example` `[ASSUMED]` placeholder with the derived value. This plan consumes Plan
03's decided numbers verbatim.

Purpose: Close AUD-06 (compose + CI) and the IN-02/IN-04 cross-cutting gaps.
Output: docker-compose.yml audio-worker service + 7-way env consistency; ci.yml bake entries; .env.example final values.
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/32-containerization-local-e2e-rtf-gate/32-RESEARCH.md
@.planning/phases/32-containerization-local-e2e-rtf-gate/32-PATTERNS.md
@.planning/phases/32-containerization-local-e2e-rtf-gate/32-03-SUMMARY.md

<read_first>
Files to read before editing:
- docker-compose.yml (document-worker block lines 220-266 = the structural analog for the new service; api lines 98-106, worker lines 141-147, document-worker 250-258, chromium-worker 291-298 = the DEBT-05 cross-engine env blocks to extend; webhook-worker-1/2 lines 173-218 = the two services currently omitting all engine-class MAX_RETRY/TIMEOUT vars)
- .github/workflows/ci.yml (docker-build set: lines 62-74; e2e set: lines 101-107)
- .env.example lines 50-63 (the AUDIO_* block: replace the AUDIO_ENGINE_TIMEOUT [ASSUMED] value + AUDIO_WORKER_CONCURRENCY placeholder)
- 32-03-SUMMARY.md (the decided AUDIO_ENGINE_TIMEOUT, AUDIO_WORKER_CONCURRENCY, AUDIO_MAX_DURATION_SECONDS, cpus/memory, -t values)
- 31-REVIEW.md IN-02 + IN-04 (the two findings this plan closes)
</read_first>

<interfaces>
<!-- DEBT-05 cross-engine block already duplicated in api/worker/document-worker/chromium-worker. -->
IMAGE_MAX_RETRY: "4"
ENGINE_TIMEOUT: "120s"
DOCUMENT_MAX_RETRY: "3"
DOCUMENT_ENGINE_TIMEOUT: "300s"
HTML_MAX_RETRY: "3"
HTML_ENGINE_TIMEOUT: "60s"

<!-- IN-02: queue.NewClient() (internal/queue/client.go:87-88) reads these UNCONDITIONALLY in
     every process (api, worker, document-worker, chromium-worker, webhook-worker-1, webhook-worker-2,
     audio-worker) and derives audioUniqueTTL from them. All 7 MUST carry the same value. -->
AUDIO_ENGINE_TIMEOUT: "<measured>"
AUDIO_MAX_RETRY: "3"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the audio-worker service block to docker-compose.yml (measured values, no platform pin)</name>
  <files>docker-compose.yml</files>
  <read_first>docker-compose.yml document-worker block lines 220-266 (copy shape); 32-RESEARCH.md "Compose Service Wiring" (fully drafted block); 32-03-SUMMARY.md (values)</read_first>
  <action>
    Add an `audio-worker` service, structurally copying the document-worker block but WITHOUT the
    `platform: linux/amd64` line (whisper.cpp is source-built portable — chromium-worker precedent).
    `build.dockerfile: Dockerfile.audio-worker`; `container_name: octoconv-audio-worker`;
    `restart: always`; the standard postgres/redis/minio `depends_on: condition: service_healthy`.
    Environment: the standard DATABASE_URL/REDIS_ADDR/S3_* block; own-engine vars
    `AUDIO_WORKER_CONCURRENCY` / `AUDIO_ENGINE_TIMEOUT` / `AUDIO_MAX_RETRY` / `AUDIO_MAX_DURATION_SECONDS`
    / `AUDIO_MODEL_PATH: "/models/ggml-base.bin"` using the DECIDED values from 32-03-SUMMARY.md
    (do NOT set AUDIO_THREADS — leave unset so the container auto-detects from cgroup, per Plan 02;
     only set it if the measurement explicitly recommended an override); the DEBT-05 cross-engine block
    (IMAGE_MAX_RETRY/ENGINE_TIMEOUT/DOCUMENT_MAX_RETRY/DOCUMENT_ENGINE_TIMEOUT/HTML_MAX_RETRY/HTML_ENGINE_TIMEOUT)
    identical to the sibling worker services; `METRICS_ADDR: "127.0.0.1:9090"`.
    `deploy.resources.limits.cpus`/`memory` = the exact values the RTF measurement container used
    (32-03-SUMMARY.md) — this is load-bearing: the measured timeout is only valid at those limits.
    Add a comment noting the AUDIO_ENGINE_TIMEOUT value is RTF-measured (Phase 32, AUD-07), not copied.
  </action>
  <verify>
    <automated>docker compose config >/dev/null 2>&1 && docker compose config | grep -q 'octoconv-audio-worker'</automated>
  </verify>
  <done>audio-worker service present and compose config validates; uses Dockerfile.audio-worker; no platform pin; measured AUDIO_ENGINE_TIMEOUT/CONCURRENCY/cpus/memory; AUDIO_MODEL_PATH=/models/ggml-base.bin.</done>
</task>

<task type="auto">
  <name>Task 2: IN-02 — propagate AUDIO_ENGINE_TIMEOUT/AUDIO_MAX_RETRY to the 6 existing services; update .env.example (grep==7 gate)</name>
  <files>docker-compose.yml, .env.example</files>
  <read_first>docker-compose.yml api/worker/document-worker/chromium-worker/webhook-worker-1/webhook-worker-2 environment blocks; 31-REVIEW.md IN-02; 32-RESEARCH.md "CRITICAL — IN-02 propagation"; .env.example lines 50-54</read_first>
  <action>
    In the SAME commit as Task 1's new service, add `AUDIO_ENGINE_TIMEOUT: "<measured>"` and
    `AUDIO_MAX_RETRY: "3"` (or the decided retry value) to the environment block of ALL SIX existing
    queue.NewClient()-constructing services: `api`, `worker`, `document-worker`, `chromium-worker`,
    `webhook-worker-1`, `webhook-worker-2` — using the SAME value as the audio-worker service and the
    SAME DEBT-05 comment style ("queue.NewClient() reads these unconditionally"). webhook-worker-1/2
    currently omit ALL engine-class MAX_RETRY/TIMEOUT vars; this phase closes that gap for AUDIO_* on
    those two (it does not retrofit the pre-existing DOCUMENT_*/HTML_* omission — out of scope).
    Rationale (record inline): queue.NewClient() derives audioUniqueTTL from these envs in EVERY process;
    a stale value anywhere reopens the T-03-10 double-processing race AudioUniqueTTL exists to close.

    In .env.example, replace the `AUDIO_ENGINE_TIMEOUT=600s   # [ASSUMED] placeholder...` line (line 51)
    with the measured value and a terse derivation comment (mirror `DOCUMENT_ENGINE_TIMEOUT=300s # Phase 9 D-01`
    style: e.g. "# Phase 32 AUD-07: RTF-measured p95=<x>, timeout = maxDuration×RTF×2.0 safety"). Update
    `AUDIO_WORKER_CONCURRENCY` to the decided value with a comment noting it is measured (drop the "Phase 32
    re-sizes" placeholder note). If the NO-GO lever lowered AUDIO_MAX_DURATION_SECONDS (Plan 03), update that
    line too and note why. Add the IN-02 sentence to the .env.example audio block: "must be set identically
    in the api, webhook-worker, and audio-worker environments — the uniqueness-lock TTL is derived from these
    values in every enqueuing process."

    Acceptance gate: `grep -c '^\s*AUDIO_ENGINE_TIMEOUT' docker-compose.yml` must be exactly 7 (api, worker,
    document-worker, chromium-worker, webhook-worker-1, webhook-worker-2, audio-worker).
  </action>
  <verify>
    <automated>[ "$(grep -c 'AUDIO_ENGINE_TIMEOUT:' docker-compose.yml)" -eq 7 ] && [ "$(grep -c 'AUDIO_MAX_RETRY:' docker-compose.yml)" -eq 7 ] && ! grep -q 'ASSUMED' .env.example && docker compose config >/dev/null</automated>
  </verify>
  <done>All 7 services carry identical AUDIO_ENGINE_TIMEOUT + AUDIO_MAX_RETRY (grep==7 each); .env.example ships the measured value with derivation comment + IN-02 note and no [ASSUMED] placeholder; compose config validates.</done>
</task>

<task type="auto">
  <name>Task 3: Add audio-worker to the CI bake matrix (docker-build + e2e jobs)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>.github/workflows/ci.yml docker-build set: lines 62-74 (cache-to + cache-from per service), e2e set: lines 101-107 (cache-from only); 32-RESEARCH.md "CI Bake Matrix"</read_first>
  <action>
    In the `docker-build` job's `set:` block, add two lines following the existing per-service pattern:
    `audio-worker.cache-to=type=gha,mode=max,scope=audio-worker` and
    `audio-worker.cache-from=type=gha,scope=audio-worker`.
    In the `e2e` job's `set:` block, add one line: `audio-worker.cache-from=type=gha,scope=audio-worker`.
    No other ci.yml change is needed: docker/bake-action auto-derives the audio-worker target from the
    compose `build:` block (Task 1), and the `docker compose up -d` step has no explicit service list so
    audio-worker is included automatically. Note in the SUMMARY (do NOT pre-emptively bump) whether the
    docker-build `timeout-minutes: 20` (whisper.cpp compile is a real new cost) and the e2e
    `-timeout 30m` look tight after the first live run — flag only if evidence shows it.
  </action>
  <verify>
    <automated>[ "$(grep -c 'audio-worker.cache-from=type=gha,scope=audio-worker' .github/workflows/ci.yml)" -eq 2 ] && grep -q 'audio-worker.cache-to=type=gha,mode=max,scope=audio-worker' .github/workflows/ci.yml</automated>
  </verify>
  <done>docker-build job has audio-worker cache-to + cache-from; e2e job has audio-worker cache-from; the cache-from scope line appears exactly twice (once per job).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| enqueuing process ↔ enqueuing process | audioUniqueTTL is derived independently per process from these envs; drift = double-processing window |
| compose config → deployable stack | A missing/mis-typed service block leaves audio jobs with no consumer (IN-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-10 | Tampering | Divergent AUDIO_ENGINE_TIMEOUT across processes → stale audioUniqueTTL → T-03-10 double-processing (the most expensive engine class) | mitigate | Identical value across all 7 services in one commit; `grep -c == 7` acceptance gate |
| T-32-11 | Denial of Service | API accepts audio jobs with no compose consumer (IN-04) → jobs queue until reconciler exhaustion | mitigate | audio-worker service added to docker-compose.yml (this plan) |
| T-32-12 | Repudiation | .env.example still advertising an [ASSUMED] placeholder as if tuned | mitigate | Placeholder replaced with the measured value + derivation comment; no ASSUMED string remains |
</threat_model>

<verification>
- `grep -c AUDIO_ENGINE_TIMEOUT: docker-compose.yml` == 7 AND `grep -c AUDIO_MAX_RETRY: docker-compose.yml` == 7.
- `docker compose config` validates and shows octoconv-audio-worker.
- ci.yml: audio-worker cache-from present in both jobs; cache-to present in docker-build.
- No `ASSUMED` string remains in .env.example.
</verification>

<success_criteria>
- audio-worker compose service exists with measured values + matching cpus/memory (IN-04 closed).
- IN-02: 7-way identical AUDIO_ENGINE_TIMEOUT/AUDIO_MAX_RETRY propagation in one commit.
- CI bake matrix builds audio-worker; .env.example ships the measured, non-placeholder value.
</success_criteria>

<output>
Create `.planning/phases/32-containerization-local-e2e-rtf-gate/32-04-SUMMARY.md` when done.
</output>
