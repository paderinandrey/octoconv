---
phase: 32-containerization-local-e2e-rtf-gate
plan: 05
type: execute
wave: 4
depends_on: ["32-04"]
files_modified:
  - internal/e2e/testdata/jfk.wav
  - internal/e2e/e2e_test.go
autonomous: true
requirements: [AUD-06]
must_haves:
  truths:
    - "TestAudioConversionE2E drives an audio job end-to-end through the LIVE containerized audio-worker: upload → poll to done → presigned transcript download → non-empty transcript"
    - "A signed webhook for the audio job is confirmed (assertSignedWebhook)"
    - "The transcript assertion is non-emptiness only (Pitfall 9 — ASR output is non-deterministic; no exact-string match)"
    - "The conversion runs through the CONTAINERIZED audio-worker (compose), not go run"
  artifacts:
    - path: "internal/e2e/e2e_test.go"
      provides: "TestAudioConversionE2E + assertDownloadIsNonEmptyTranscript"
      contains: "TestAudioConversionE2E"
    - path: "internal/e2e/testdata/jfk.wav"
      provides: "Committed audio E2E fixture (copied from internal/convert/testdata/audio/jfk.wav)"
  key_links:
    - from: "TestAudioConversionE2E"
      to: "compose audio-worker service"
      via: "postJob → audio queue → containerized whisper-cli → presigned download"
      pattern: "TestAudioConversionE2E"
    - from: "TestAudioConversionE2E"
      to: "assertSignedWebhook"
      via: "webhook-worker signed callback confirmation"
      pattern: "assertSignedWebhook"
---

<objective>
Add the USER-AGREED MUST-HAVE end-to-end test: a repeatable, env-gated
`TestAudioConversionE2E` that drives an audio job through the LIVE compose stack —
multipart upload → poll to done → presigned transcript download → non-empty-transcript
assertion → signed-webhook confirmation — through the CONTAINERIZED `audio-worker`
service (Plan 04), NOT a local `go run`. Mirrors `TestImageConversionE2E` exactly, with
Pitfall-9 non-determinism handling (structural/non-empty assertions only). This is the
phase's live acceptance gate for SC2.

Purpose: Prove the containerized audio pipeline works end-to-end; close the AUD-06 live-E2E acceptance criterion.
Output: internal/e2e/testdata/jfk.wav + TestAudioConversionE2E + assertDownloadIsNonEmptyTranscript, green against the live stack.
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/32-containerization-local-e2e-rtf-gate/32-RESEARCH.md
@.planning/phases/32-containerization-local-e2e-rtf-gate/32-PATTERNS.md
@.planning/phases/32-containerization-local-e2e-rtf-gate/32-04-SUMMARY.md

<read_first>
Files to read before editing:
- internal/e2e/e2e_test.go — TestImageConversionE2E (lines 1096-1121) is the copy template; assertDownloadIsImage (lines 1128-1150) is the assertion analog to adapt into assertDownloadIsNonEmptyTranscript; reuse unchanged: e2eSetup (74), provisionClient (95), postJob (167), pollUntilDone (252), startWebhookReceiver (347), assertSignedWebhook (763), downloadClient (824). NOTE the package header (lines 7-21) documents the exact required/optional live-run env vars: REQUIRED E2E_BASE_URL, DATABASE_URL, API_KEY_SALT; OPTIONAL E2E_WEBHOOK_HOST, E2E_S3_DIAL_ADDR, WEBHOOK_SIGNING_SECRET — Task 2's live command must supply these exact names
- 32-RESEARCH.md "TestAudioConversionE2E" section (the fully drafted test body + assertion helper, written against this exact analog — copy it directly) + "Fixture note"/"E2E_WEBHOOK_HOST" (no docker-compose.e2e.yml override needed; audio-worker delivers no webhooks — only webhook-worker-1/2 dial the receiver)
</read_first>

<interfaces>
<!-- Reused e2e helpers (internal/e2e/e2e_test.go), call unchanged. -->
func e2eSetup(t *testing.T) e2eConfig            // env-gated skip when E2E_BASE_URL unset
func provisionClient(t *testing.T) string        // returns apiKey
func postJob(t, baseURL, apiKey, filename, data, target, callbackURL, opts) string  // returns jobID
func pollUntilDone(t, baseURL, apiKey, jobID string, timeout time.Duration) map[string]any
func startWebhookReceiver(t, webhookHost) (callbackURL string, received <-chan webhookHit)
func assertSignedWebhook(t, received <-chan webhookHit, jobID string)
func downloadClient() *http.Client
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy jfk.wav fixture + add TestAudioConversionE2E and assertDownloadIsNonEmptyTranscript</name>
  <files>internal/e2e/testdata/jfk.wav, internal/e2e/e2e_test.go</files>
  <read_first>internal/e2e/e2e_test.go TestImageConversionE2E + assertDownloadIsImage; 32-RESEARCH.md "TestAudioConversionE2E" (drafted body to copy verbatim)</read_first>
  <action>
    Copy the committed fixture `internal/convert/testdata/audio/jfk.wav` →
    `internal/e2e/testdata/jfk.wav` as a REAL file (no symlink — no symlinks exist anywhere in
    internal/e2e/testdata/).
    Add `TestAudioConversionE2E` to internal/e2e/e2e_test.go, copying the 32-RESEARCH.md drafted body:
    `e2eSetup(t)`; `provisionClient(t)`; read `testdata/jfk.wav`; `startWebhookReceiver(t, cfg.webhookHost)`;
    `postJob(..., "jfk.wav", data, "txt", callbackURL, "")`; `pollUntilDone(..., 5*time.Minute)` (the
    document/html cold-start allowance, NOT the 2-minute image bound — whisper cold start + real
    transcription); extract `download_url`, fatal if empty; `assertDownloadIsNonEmptyTranscript(t, downloadURL)`;
    `assertSignedWebhook(t, received, jobID)`.
    Add `assertDownloadIsNonEmptyTranscript(t, downloadURL)` mirroring assertDownloadIsImage's GET/status
    shape but asserting `len(bytes.TrimSpace(body)) == 0` → fatal (structural non-empty check ONLY —
    Pitfall 9: ASR output is the project's first non-deterministic engine output; content/substring checks
    belong to the unit suite, never here). Confirm `bytes` is already imported in e2e_test.go (add if not).
    No docker-compose.e2e.yml change: audio-worker delivers no webhooks (decoupled to webhook-worker-1/2
    which already have extra_hosts), so no new extra_hosts entry is needed.
  </action>
  <verify>
    <automated>test -f internal/e2e/testdata/jfk.wav && go vet ./internal/e2e/... && go test ./internal/e2e/... -run TestAudioConversionE2E -count=1 2>&1 | grep -qiE 'SKIP|no tests to run|ok'</automated>
  </verify>
  <done>jfk.wav copied into internal/e2e/testdata/; TestAudioConversionE2E + assertDownloadIsNonEmptyTranscript compile and self-skip cleanly when E2E_BASE_URL is unset; non-emptiness assertion only (no exact-transcript match); go vet clean.</done>
</task>

<task type="auto">
  <name>Task 2: Live E2E run through the containerized audio-worker with signed-webhook confirmation</name>
  <files>internal/e2e/e2e_test.go</files>
  <read_first>.github/workflows/ci.yml e2e job (the exact live-run recipe: bake load → compose up -f docker-compose.yml -f docker-compose.e2e.yml → wait healthz → migrate → go test with E2E_BASE_URL/DATABASE_URL/API_KEY_SALT/WEBHOOK_SIGNING_SECRET/E2E_S3_DIAL_ADDR); internal/e2e/e2e_test.go header (lines 7-21) for the authoritative required/optional env-var names; STATE.md OrbStack compose/k8s mutual-exclusion discipline</read_first>
  <action>
    Run the live E2E through the CONTAINERIZED stack (NOT go run). Ensure k8s is down first (OrbStack
    mutual exclusion). Bring up the full stack including the new audio-worker service:
    `docker compose -f docker-compose.yml -f docker-compose.e2e.yml up -d --build` (build so the new
    audio-worker image is present), wait for `/healthz`, run migrations (`go run ./cmd/migrate`), then:
    `go test ./internal/e2e/... -run TestAudioConversionE2E -count=1 -v -timeout 15m` with the exact env
    the e2e suite header documents as required/optional: `E2E_BASE_URL=http://localhost:8090`,
    `DATABASE_URL=postgres://octo:octo-pass@localhost:5434/octo_db`,
    `API_KEY_SALT=dev-only-change-me-in-real-deploys`,
    `WEBHOOK_SIGNING_SECRET=dev-only-change-me-in-real-deploys`, `E2E_S3_DIAL_ADDR=127.0.0.1:9100`.
    Confirm the test PASSES: the job reaches done via the containerized audio-worker (check
    `docker compose logs audio-worker` shows it consuming the job), the presigned transcript downloads
    non-empty, and the signed webhook is confirmed. Record in the SUMMARY: pass/fail, observed wall-clock
    for the audio job (feeds the CI -timeout headroom note from Plan 04 Task 3), and whether the 30m e2e
    suite timeout looks tight. Tear down (`docker compose ... down -v`).
    If the test flakes on the 5-minute pollUntilDone bound, record the observed cold-start time and flag a
    bound adjustment rather than silently loosening it.
  </action>
  <verify>
    <automated>docker compose -f docker-compose.yml -f docker-compose.e2e.yml up -d --build && (for i in $(seq 1 60); do curl -fsS http://localhost:8090/healthz >/dev/null 2>&1 && break; [ "$i" = 60 ] && { docker compose -f docker-compose.yml -f docker-compose.e2e.yml down -v; exit 1; }; sleep 5; done) && go run ./cmd/migrate && E2E_BASE_URL=http://localhost:8090 DATABASE_URL=postgres://octo:octo-pass@localhost:5434/octo_db API_KEY_SALT=dev-only-change-me-in-real-deploys WEBHOOK_SIGNING_SECRET=dev-only-change-me-in-real-deploys E2E_S3_DIAL_ADDR=127.0.0.1:9100 go test ./internal/e2e/... -run TestAudioConversionE2E -count=1 -v -timeout 15m; rc=$?; docker compose -f docker-compose.yml -f docker-compose.e2e.yml down -v; exit $rc</automated>
  </verify>
  <done>TestAudioConversionE2E passes against the live compose stack through the containerized audio-worker; audio-worker logs show it consumed the job; presigned transcript non-empty; signed webhook confirmed; observed wall-clock + CI-timeout-headroom note recorded in SUMMARY; stack torn down. The `<automated>` command exits non-zero on any failure (health-wait timeout, migrate error, or test failure) — no always-green echo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test client → live API | E2E exercises the real public HTTP surface + presigned S3 download + webhook receiver |
| non-deterministic engine output → test assertion | ASR transcript content varies run-to-run (Pitfall 9) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-13 | Repudiation | A flaky exact-transcript assertion masking real regressions or failing on ASR non-determinism | mitigate | Non-emptiness/structural assertion only (Pitfall 9); content checks confined to the unit suite |
| T-32-14 | Spoofing | Unsigned/forged webhook accepted as delivery confirmation | mitigate | assertSignedWebhook validates the HMAC-SHA256 signature (reused unchanged from the existing suite) |
| T-32-15 | Tampering | Test passing against a local `go run` worker instead of the containerized service (false green for SC2) | mitigate | Live run brings up the compose audio-worker; SUMMARY confirms `docker compose logs audio-worker` consumed the job |
</threat_model>

<verification>
- `go test ./internal/e2e/... -run TestAudioConversionE2E` self-skips offline and compiles clean.
- Live run: test PASSES against `docker compose up` (audio-worker included); logs confirm the containerized worker consumed the job; presigned transcript non-empty; signed webhook confirmed. The Task 2 `<automated>` command is a real fail-closed sequence (non-zero exit on any failure), not an echo.
- Observed wall-clock + CI-timeout headroom recorded in SUMMARY.
</verification>

<success_criteria>
- Repeatable env-gated TestAudioConversionE2E exists, mirroring TestImageConversionE2E, with non-empty-transcript + signed-webhook assertions.
- The test passes live through the CONTAINERIZED audio-worker (not go run).
- jfk.wav fixture committed under internal/e2e/testdata/.
</success_criteria>

<output>
Create `.planning/phases/32-containerization-local-e2e-rtf-gate/32-05-SUMMARY.md` when done — record the live pass, observed job wall-clock, and CI-timeout headroom assessment.
</output>
</content>
</invoke>
