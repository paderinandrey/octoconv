---
phase: 33-keda-helm-chart-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - deploy/chart/octoconv/templates/configmap.yaml
  - deploy/chart/octoconv/values.yaml
  - deploy/chart/octoconv/templates/deployment-audio-worker.yaml
  - deploy/chart/octoconv/templates/scaledobject-audio.yaml
  - cmd/api/main.go
autonomous: true
requirements: [AUD-08]

must_haves:
  truths:
    - "The chart renders an audio-worker Deployment and a worker-audio-scaledobject ScaledObject (SC1)"
    - "The audio ScaledObject carries the WR-01 fail-safe triad verbatim (ignoreNullValues false, fallback.replicas 1, retry-inclusive PromQL) from its first commit (SC1)"
    - "The audio Deployment's terminationGracePeriodSeconds (772s) exceeds AUDIO_ENGINE_TIMEOUT (742s) (SC4)"
    - "scaleDownStabilizationSeconds is a non-null production value above the 742s worst-case job duration (SC1)"
    - "QueueAudio is registered in the api queue-depth collector so audio backlog is exposed at zero replicas (SC2)"
    - "The chart ConfigMap carries all 5 AUDIO_* keys and RECONCILER_ACTIVE_STALE_AFTER is 15m (not stale 5m)"
  artifacts:
    - path: "deploy/chart/octoconv/templates/deployment-audio-worker.yaml"
      provides: "audio-worker Deployment template (clone of document-worker, no Rosetta/platform framing, grace 772s)"
      contains: "terminationGracePeriodSeconds"
    - path: "deploy/chart/octoconv/templates/scaledobject-audio.yaml"
      provides: "audio KEDA ScaledObject (clone of document, WR-01 triad, non-null stabilization)"
      contains: "worker-audio-scaledobject"
    - path: "deploy/chart/octoconv/templates/configmap.yaml"
      provides: "5 AUDIO_* env keys + RECONCILER_ACTIVE_STALE_AFTER 15m fix"
      contains: "AUDIO_ENGINE_TIMEOUT"
    - path: "deploy/chart/octoconv/values.yaml"
      provides: "audioWorker block + keda.audio block with non-null scaleDownStabilizationSeconds"
      contains: "audioWorker"
    - path: "cmd/api/main.go"
      provides: "QueueAudio in NewQueueDepthCollector registration"
      contains: "queue.QueueAudio"
  key_links:
    - from: "deploy/chart/octoconv/templates/scaledobject-audio.yaml"
      to: "cmd/api/main.go collector"
      via: "PromQL queue=audio series exposed by the QueueAudio-registered collector"
      pattern: "octoconv_queue_depth\\{queue=\"audio\""
    - from: "deploy/chart/octoconv/templates/deployment-audio-worker.yaml"
      to: "deploy/chart/octoconv/templates/configmap.yaml"
      via: "octoconv.commonEnv envFrom pulls the AUDIO_* keys"
      pattern: "octoconv.commonEnv"
---

<objective>
Author the static chart substrate for the audio engine class: a new audio-worker Deployment and audio KEDA ScaledObject, the 5 missing AUDIO_* ConfigMap keys plus the stale-reconciler fix, the values.yaml tuning blocks, and the one-line QueueAudio collector splice in cmd/api/main.go. This is the 4th repetition of an established 3-class chart pattern — every value has a correct sibling precedent already in the repo; the failure mode is a silently dropped invariant, not novel code.

Purpose: Deliver SC1, SC2, and SC4 as renderable, grep-verifiable chart artifacts before any live cluster work (Plan 03 consumes these).
Output: Two new template files, three edited files, all verified by `helm template` render + grep gates + `go build`/`go vet`.
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-keda-helm-chart-integration/33-RESEARCH.md
@.planning/phases/33-keda-helm-chart-integration/33-PATTERNS.md
@.planning/phases/32-containerization-local-e2e-rtf-gate/32-03-SUMMARY.md

<decisions>
Locked numeric values for this phase (do NOT re-derive; rationale below):
- terminationGracePeriodSeconds = 772s  (AUDIO_ENGINE_TIMEOUT 742s + 30s, the ENGINE_TIMEOUT+30s chart convention shared by image/document/html; stricter than compose's 762s and satisfies SC4 with more margin — RESEARCH Pattern 4, Assumption A1 resolved in favor of the chart convention)
- scaleDownStabilizationSeconds = 900s  (NON-NULL production value; must exceed the 742s worst-case in-flight audio job so a genuine N→N-1 downscale never SIGTERMs a live transcription; 900s = 742s + 158s margin (~21%) and aligns with the 15m/900s reconciler CAP the whole phase reasons in; unlike document's production null, audio is the first class where the k8s 300s HPA default is provably insufficient — RESEARCH Pitfall 3 / State of the Art)
- keda.audio.threshold = "1"          (one job per replica: AUDIO_WORKER_CONCURRENCY=1, so each pending job warrants a replica — mirrors document's "1")
- keda.audio.maxReplicaCount = 2      (each replica costs 2 CPU / 1 GiB; on a single-node OrbStack cluster 2 replicas = 4 CPU / 2 GiB — a deliberate conservative local cap matching document's; open-question-3 resolved)
- keda.audio.pollingInterval = 15     (742s jobs do not need aggressive 5s polling; match document's 15s)
- keda.audio.cooldownPeriod = 180     (must exceed audioRetrySchedule's 30s max backoff step per WR-06 invariant, and exceed document's 120s to reflect audio's longer jobs; open-question-3 resolved)
- audioWorker.terminationGracePeriodSeconds = 772, audioWorker.replicas = 1, resources.limits cpu "2" / memory "1Gi" (must match the RTF-measurement container exactly — 742s is only valid at these limits)
- image repository: "octoconv-audio-worker"
</decisions>

<interfaces>
QueueAudio is already defined (no new constant): internal/queue/queue.go:38 `QueueAudio = convert.EngineAudio` ("audio").
audioRetrySchedule max backoff step = 30s (internal/queue/queue.go:303-307) — cooldownPeriod 180 clears it comfortably (WR-06 invariant).
octoconv.commonEnv (_helpers.tpl) makes every Deployment pull the full ConfigMap+Secret via envFrom — the 5 AUDIO_* keys need no per-Deployment env wiring beyond that single include.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ConfigMap AUDIO_* keys + stale-reconciler fix, and QueueAudio collector splice</name>
  <files>deploy/chart/octoconv/templates/configmap.yaml, cmd/api/main.go</files>
  <read_first>
    - deploy/chart/octoconv/templates/configmap.yaml (target — currently has ZERO AUDIO_* keys; RECONCILER_ACTIVE_STALE_AFTER at line 38 reads stale "5m")
    - cmd/api/main.go lines 88-95 (current 4-arg NewQueueDepthCollector call at 91-92: QueueImage, QueueDocument, QueueHTML, QueueWebhook)
    - docker-compose.yml lines 389-399 (audio-worker env — the exact source-of-truth values the ConfigMap keys must match byte-for-byte)
    - .planning/phases/33-keda-helm-chart-integration/33-PATTERNS.md "configmap.yaml" section (exact 5 keys + verification commands)
  </read_first>
  <action>
    Add exactly these 5 keys to configmap.yaml's `data:` block near the other per-class keys, values matching docker-compose.yml exactly: AUDIO_WORKER_CONCURRENCY "1", AUDIO_ENGINE_TIMEOUT "742s", AUDIO_MAX_RETRY "3", AUDIO_MAX_DURATION_SECONDS "1800", AUDIO_MODEL_PATH "/models/ggml-base.bin". In the SAME edit, change RECONCILER_ACTIVE_STALE_AFTER from "5m" to "15m" (Pitfall 2 — deploying 742s against a 5m stale-after reopens the reconciler double-processing risk Phase 32 IN-16 closed in compose; the two changes MUST land together). Add a brief inline comment on the fixed key noting it mirrors compose's Phase-32 IN-16 fix and that AUDIO_ENGINE_TIMEOUT (742s) < RECONCILER_ACTIVE_STALE_AFTER (15m/900s) must hold. Then in cmd/api/main.go, insert `queue.QueueAudio` between `queue.QueueHTML` and `queue.QueueWebhook` in the NewQueueDepthCollector variadic call — this is the only change to that file (Pitfall 6: without it octoconv_queue_depth{queue="audio"} is never registered and the ScaledObject permanently holds fallback.replicas 1, never scaling to genuine 0).
  </action>
  <verify>
    <automated>cd /Users/apaderin/dev/octoconv && test "$(grep -c 'AUDIO_' deploy/chart/octoconv/templates/configmap.yaml)" = "5" && grep -q 'RECONCILER_ACTIVE_STALE_AFTER: "15m"' deploy/chart/octoconv/templates/configmap.yaml && ! grep -q 'RECONCILER_ACTIVE_STALE_AFTER: "5m"' deploy/chart/octoconv/templates/configmap.yaml && grep -q 'queue.QueueAudio' cmd/api/main.go && go build ./... && go vet ./...</automated>
  </verify>
  <done>ConfigMap has exactly 5 AUDIO_* keys with compose-matching values, RECONCILER_ACTIVE_STALE_AFTER is 15m (no 5m remaining), the collector call includes queue.QueueAudio, and go build/vet pass clean.</done>
</task>

<task type="auto">
  <name>Task 2: values.yaml audioWorker + keda.audio blocks</name>
  <files>deploy/chart/octoconv/values.yaml</files>
  <read_first>
    - deploy/chart/octoconv/values.yaml lines 49-57 (documentWorker block — clone shape) and lines 146-171 (keda block: WR-06 cooldown invariant comment + document/html blocks incl. scaleDownStabilizationSeconds precedent)
    - docker-compose.yml lines 414-420 (audio-worker deploy.resources.limits: cpus "2.0", memory 1g — the values.yaml resources MUST match to keep the 742s timeout valid)
    - .planning/phases/33-keda-helm-chart-integration/33-PATTERNS.md "values.yaml" section
  </read_first>
  <action>
    Add an `audioWorker` top-level block cloned from documentWorker: image.repository "octoconv-audio-worker", replicas 1, terminationGracePeriodSeconds 772, resources.limits cpu "2" and memory "1Gi". Add an `audio` block under `keda:` cloned from `keda.document` with threshold "1", maxReplicaCount 2, pollingInterval 15, cooldownPeriod 180, and scaleDownStabilizationSeconds 900 (NON-NULL — the divergence from document's production null; add an inline comment stating audio is the first class whose 742s worst-case exceeds the k8s 300s HPA default, so this knob is load-bearing in production, and that 900s > 742s with ~21% margin). Extend the WR-06 cooldown-invariant comment to note audio's cooldownPeriod 180s clears audioRetrySchedule's 30s max backoff step (internal/queue/queue.go). Do NOT add an extraEnv block (Assumption A5 — AUDIO_WORKER_CONCURRENCY is hardcoded to the production-safe 1). All decision values are locked in this plan's <decisions>; do not re-derive.
  </action>
  <verify>
    <automated>cd /Users/apaderin/dev/octoconv && grep -q 'audioWorker:' deploy/chart/octoconv/values.yaml && grep -q 'octoconv-audio-worker' deploy/chart/octoconv/values.yaml && grep -q 'terminationGracePeriodSeconds: 772' deploy/chart/octoconv/values.yaml && grep -A6 '^  audio:' deploy/chart/octoconv/values.yaml | grep -q 'scaleDownStabilizationSeconds: 900'</automated>
  </verify>
  <done>values.yaml has an audioWorker block (repo octoconv-audio-worker, grace 772, cpu 2 / mem 1Gi) and a keda.audio block (threshold 1, maxReplicaCount 2, pollingInterval 15, cooldownPeriod 180, scaleDownStabilizationSeconds 900 non-null with rationale comment).</done>
</task>

<task type="auto">
  <name>Task 3: deployment-audio-worker.yaml + scaledobject-audio.yaml templates, full render gate</name>
  <files>deploy/chart/octoconv/templates/deployment-audio-worker.yaml, deploy/chart/octoconv/templates/scaledobject-audio.yaml</files>
  <read_first>
    - deploy/chart/octoconv/templates/deployment-document-worker.yaml (full — structural clone base for the Deployment)
    - deploy/chart/octoconv/templates/scaledobject-document.yaml (full — structural clone base for the ScaledObject; ONLY sibling with the scaleDownStabilizationSeconds block)
    - deploy/chart/octoconv/templates/scaledobject-image.yaml (contrast only — has NO stabilization block; do NOT clone this, Pitfall 3)
    - .planning/phases/33-keda-helm-chart-integration/33-PATTERNS.md "deployment-audio-worker.yaml" and "scaledobject-audio.yaml" sections (verbatim blocks + divergences)
  </read_first>
  <action>
    Create deployment-audio-worker.yaml by cloning deployment-document-worker.yaml, renaming every document-worker/documentWorker token to audio-worker/audioWorker. CRITICAL divergences: (1) do NOT copy the amd64/Rosetta/platform framing in the header comment — audio-worker is built -DGGML_NATIVE=OFF (source-built, portable, no --platform pin in Dockerfile.audio-worker); write a header noting portability and grace 772s ≥ AUDIO_ENGINE_TIMEOUT 742s + margin. (2) Keep the spec.replicas KEDA-omission block (`{{- if and .Values.keda.enabled .Values.prometheus.enabled }}` ... else `replicas: {{ .Values.audioWorker.replicas }}`). (3) Keep the startupProbe (Assumption A4 — 650MB image + baked model is heavy; defensive cold-start budget). (4) Drop the extraEnv block. (5) No volumeMounts/volumes (that is chromium-worker's /dev/shm special-case). Use octoconv.commonEnv include for env, containerPort 9090, resources from .Values.audioWorker.resources.limits.

    Create scaledobject-audio.yaml by cloning scaledobject-document.yaml, renaming document→audio throughout: metadata.name worker-audio-scaledobject, scaleTargetRef.name audio-worker, PromQL `sum(octoconv_queue_depth{queue="audio", state=~"pending|active|retry"})`. Copy the WR-01 fail-safe triad VERBATIM (ignoreNullValues "false", fallback failureThreshold 3 / replicas 1, retry-inclusive PromQL). Keep the `hasKey` + `ne nil` guard around the advanced/stabilization block exactly as document has it (defensive consistency even though keda.audio.scaleDownStabilizationSeconds is always non-null in production). Reference .Values.keda.audio.* for all tunables. Keep the whole file wrapped in the `{{- if and .Values.keda.enabled .Values.prometheus.enabled }}` co-dependency guard.

    After authoring, render the chart three ways to prove correctness: default values, values-local.yaml (keda+prometheus enabled), and confirm the rendered ScaledObject carries the WR-01 triad and non-null stabilization 900.
  </action>
  <verify>
    <automated>cd /Users/apaderin/dev/octoconv && helm template octoconv deploy/chart/octoconv >/dev/null && helm template octoconv deploy/chart/octoconv -f deploy/chart/octoconv/values-local.yaml > /tmp/33-render.yaml && grep -q 'name: worker-audio-scaledobject' /tmp/33-render.yaml && grep -q 'queue="audio", state=~"pending|active|retry"' /tmp/33-render.yaml && grep -q 'ignoreNullValues: "false"' /tmp/33-render.yaml && grep -q 'stabilizationWindowSeconds: 900' /tmp/33-render.yaml && grep -q 'terminationGracePeriodSeconds: 772' /tmp/33-render.yaml && ! grep -qi 'rosetta\|platform:' deploy/chart/octoconv/templates/deployment-audio-worker.yaml</automated>
  </verify>
  <done>Both templates render under default and values-local; the audio ScaledObject shows the WR-01 triad verbatim, retry-inclusive audio PromQL, non-null stabilization 900, and the Deployment renders grace 772 with no Rosetta/platform framing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| KEDA/HPA → audio-worker Deployment | The autoscaler owns the 0↔N replica decision; a wrong grace/stabilization value here lets it SIGTERM a live transcription |
| Prometheus → ScaledObject PromQL | An absent/empty metric series (api down, or QueueAudio unregistered) crosses here and, mishandled, scales a busy class to zero |
| Chart ConfigMap vs docker-compose env | Two independently-maintained env surfaces that can silently drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Denial of Service | deployment-audio-worker.yaml terminationGracePeriodSeconds | mitigate | Set 772s (> AUDIO_ENGINE_TIMEOUT 742s) so a genuine N→N-1 downscale never premature-SIGTERMs a live transcription (SC4); grep gate asserts 772 in render |
| T-33-02 | Denial of Service | scaledobject-audio.yaml scaleDownStabilizationSeconds | mitigate | Non-null 900s (> 742s worst-case) prevents a scale-down race against an in-flight job; grep gate asserts stabilizationWindowSeconds 900 in render |
| T-33-03 | Denial of Service | scaledobject-audio.yaml WR-01 triad + QueueAudio collector | mitigate | ignoreNullValues "false" + fallback.replicas 1 + retry-inclusive PromQL copied verbatim; QueueAudio registered so the audio series exists — together they stop an absent-metric scale-to-zero of a busy class |
| T-33-04 | Tampering | ConfigMap vs compose config drift | mitigate | 5 AUDIO_* keys sourced byte-for-byte from docker-compose.yml + RECONCILER_ACTIVE_STALE_AFTER 15m fix in the same commit; grep gates assert count=5 and 15m/no-5m |
| T-33-SC | Tampering | supply chain (installs) | accept | No npm/pip/cargo/go-get installs this phase — chart YAML + one-line Go wiring against an in-repo constant + shell only; Package Legitimacy Audit not applicable (RESEARCH) |
</threat_model>

<verification>
- `helm template octoconv deploy/chart/octoconv` and `... -f values-local.yaml` both render without error.
- Rendered audio ScaledObject: name worker-audio-scaledobject, audio PromQL with state=~"pending|active|retry", ignoreNullValues "false", fallback.replicas 1, stabilizationWindowSeconds 900.
- Rendered audio Deployment: terminationGracePeriodSeconds 772, startupProbe present, no Rosetta/platform framing.
- ConfigMap: exactly 5 AUDIO_* keys, RECONCILER_ACTIVE_STALE_AFTER 15m (no 5m).
- cmd/api/main.go: QueueAudio in collector; `go build ./...` and `go vet ./...` clean.
</verification>

<success_criteria>
SC1 (Deployment + ScaledObject with scaleDownStabilizationSeconds above worst-case + WR-01 fix from first commit), SC2 (QueueAudio registered), and SC4 (grace > AUDIO_ENGINE_TIMEOUT) are delivered as renderable, grep-verified chart artifacts. SC3 (live scale-from-zero proof) is deferred to Plan 03, which consumes these artifacts.
</success_criteria>

<output>
Create `.planning/phases/33-keda-helm-chart-integration/33-01-SUMMARY.md` when done.
</output>
