---
phase: 34-av-engine-foundation
plan: 03
type: execute
wave: 2
depends_on: [34-02]
files_modified:
  - internal/convert/av.go
  - internal/convert/av_test.go
autonomous: true
requirements: [AVC-01, AVC-02, AVC-03, AVC-04, AVC-05, AVE-02]
must_haves:
  truths:
    - "AVConverter transcodes mov/avi/mkv/webm to mp4 (H.264/AAC, +faststart) verified against a real ffmpeg binary"
    - "AVConverter transcodes mp4 to webm (VP9/Opus) as a full re-encode"
    - "AVConverter extracts an audio track to mp3/wav/m4a, using -c:a copy when source is AAC and target is m4a"
    - "AVConverter extracts a thumbnail via input-side -ss at a client or 1.0s-default timecode, clamped to duration, failing closed when no output file is produced"
    - "The transcode stream-copy fast path remuxes only when the source codec is legal in the target container per a project-owned allowlist, never per ffmpeg muxer acceptance"
    - "-protocol_whitelist file,crypto blocks a crafted HLS remote reference, proven by an offline canary test"
    - "The duration and resolution guards run before any ffmpeg encode/decode"
  artifacts:
    - path: "internal/convert/av.go"
      provides: "AVConverter (Pairs/Convert/Engine), transcode/extract/thumbnail argv builders, avStreamCopyLegal, validateAVOutput, ffprobe codec probes"
      contains: "func (AVConverter) Convert"
      min_lines: 150
    - path: "internal/convert/av_test.go"
      provides: "argv-pinning unit tests, requireLiveAVBinaries skip-gate, live transcode/extract/thumbnail tests, protocol-whitelist offline canary, self-disjointness Pairs() test"
      contains: "func requireLiveAVBinaries"
  key_links:
    - from: "AVConverter.Convert"
      to: "EnforceMaxDuration + EnforceMaxResolution"
      via: "guard stage invoked BEFORE any ffmpeg decode/encode"
      pattern: "EnforceMaxDuration|EnforceMaxResolution"
    - from: "AVConverter.Convert transcode path"
      to: "avStreamCopyLegal"
      via: "ffprobe codec check gating -c copy on the project allowlist"
      pattern: "avStreamCopyLegal"
    - from: "every ffmpeg/ffprobe argv builder in av.go"
      to: "-protocol_whitelist file,crypto"
      via: "hardening argv element on each invocation (AVE-02)"
      pattern: "protocol_whitelist"
    - from: "AVConverter.Convert"
      to: "AVOptsFromMap"
      via: "strict opts parse on the converter read path"
      pattern: "AVOptsFromMap"
---

<objective>
Build the standalone `AVConverter` (the AVC-01..05 and AVE-02-ffmpeg-hardening surface of Phase 34): three target-driven ffmpeg argv builders (transcode, audio-extract, thumbnail) dispatched by output format, an ffprobe-gated stream-copy fast path keyed on a project-owned codec allowlist, the duration+resolution guard stage wired before any decode, `-protocol_whitelist file,crypto` on every ffmpeg/ffprobe invocation, and fail-closed output post-validation. Mirrors `whisper.go`'s `AudioConverter` shape 1:1. Built and tested directly against a real ffmpeg/ffprobe binary but left UNREGISTERED in `convert.Default` (scope fence — registration is Phase 35).

Purpose: This is the end-to-end converter clients will use for video. It must honor the codec contract (mp4=H.264/AAC, webm=VP9/Opus) even when ffmpeg's muxer would silently accept an illegal remux, and must block SSRF/LFI (V12) and decode bombs (V12) before the expensive stage.

Output: `internal/convert/av.go`, `internal/convert/av_test.go`.
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-av-engine-foundation/34-RESEARCH.md
@.planning/phases/34-av-engine-foundation/34-PATTERNS.md
@.planning/phases/34-av-engine-foundation/34-02-SUMMARY.md

<interfaces>
<!-- Contracts this plan consumes. AVOpts/EngineAV/resolution guard are delivered by Plan 34-02. -->

From internal/convert/convert.go (Plan 34-02 added EngineAV):
- const EngineAV = "av"
- type Pair struct { From string; To string }
- func NormalizeFormat(f string) string  // folds jpeg->jpg, tif->tiff, htm->html

From internal/convert/avopts.go (Plan 34-02):
- type AVOpts struct { Timecode float64; ResolutionHeight int; Codec string }
- func AVOptsFromMap(m map[string]any) (AVOpts, error)
- const x264DefaultCRF = 23
- const x265DefaultCRF = 28

From internal/convert/avduration.go (Plan 34-02):
- func probeVideoStream(ctx, path) (codec string, width, height int, err error)
- func EnforceMaxResolution(ctx, path string, maxHeight int) error
- var ErrAVResolutionExceeded error

From internal/convert/audioduration.go (existing, reuse VERBATIM — do NOT re-derive):
- func ProbeDuration(ctx, path) (time.Duration, error)
- func EnforceMaxDuration(ctx, path, max time.Duration) error

From internal/convert/exec.go (existing, reuse):
- func runCommand(ctx context.Context, name string, args ...string) ([]byte, error)

From internal/convert/sniff.go (existing, reuse for thumbnail output re-check):
- func Sniff(r io.Reader) (detected string, rest io.Reader, err error)  // detects jpg/png/webp output image

Reference analog (copy shape, do NOT import differently):
- internal/convert/whisper.go — AudioConverter: Pairs()/Engine()/Convert(), ffmpegNormalizeArgs "file:" isolation, validateAudioOutput, fail-fast-on-unsupported-target
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AVConverter struct, Pairs(), and the three ffmpeg argv builders</name>
  <files>internal/convert/av.go, internal/convert/av_test.go</files>
  <read_first>
    - internal/convert/whisper.go (AudioConverter struct, Pairs() nested-loop shape, whisperOutputFlag target-driven dispatch, ffmpegNormalizeArgs "file:" isolation for argv-pinning tests)
    - internal/convert/whisper_test.go (requireLiveAudioBinaries skip-gate lines 26-42; TestWhisperArgs/TestFfmpegNormalizeArgs_FilePrefix argv-pinning style)
    - .planning/phases/34-av-engine-foundation/34-RESEARCH.md (Pattern 1 transcodeToMP4Args/transcodeToWebMArgs live-verified argv; Code Examples audio-extract and thumbnail argv lines 511-528; Pitfall 3 explicit -c:v per thumbnail target; Pitfall 4 x265 CRF; Assumptions Log A4 -nostdin; Open Question 1 Pairs() definition)
    - .planning/phases/34-av-engine-foundation/34-PATTERNS.md (av.go section — target-driven dispatch, argv isolation, Test File Patterns)
  </read_first>
  <behavior>
    - transcodeToMP4Args produces argv containing -c:v libx264, -preset veryfast, -crf (x264DefaultCRF), -c:a aac, -b:a 128k, -movflags +faststart, and (leading) -y -nostdin -protocol_whitelist file,crypto with a file:-prefixed -i path
    - transcodeToWebMArgs produces argv containing -c:v libvpx-vp9, -b:v 1M, -c:a libopus, always full re-encode, same leading hardening flags
    - a HEVC-selected transcode (AVOpts.Codec=="hevc", mp4 target) produces -c:v libx265 with -crf x265DefaultCRF (NOT x264DefaultCRF)
    - audio-extract argv: -vn plus the correct target codec (-c:a libmp3lame for mp3, -c:a pcm_s16le for wav, -c:a aac for m4a), same leading hardening flags
    - thumbnail argv: input-side -ss BEFORE -i, -frames:v 1, and an EXPLICIT -c:v per target (-c:v mjpeg for jpg, -c:v png for png, -c:v libwebp for webp) — never relying on extension auto-selection
    - Pairs() advertises: transcode {mov,avi,mkv,webm}->mp4 and mp4->webm; audio-extract {mp4,mov,avi,mkv,webm}->{mp3,wav,m4a}; thumbnail {mp4,mov,avi,mkv,webm}->{jpg,png,webp}; no duplicate (from,to) pair
    - Engine() returns EngineAV
  </behavior>
  <action>
    Create `internal/convert/av.go` in package convert. Define `AVConverter` as a plain struct (no injected fields needed — no model path analog) with `Engine() string { return EngineAV }` and `Pairs() []Pair` built from explicit source/target slices per the locked Open Question 1 decision: define `avTranscodeToMP4Sources = {"mov","avi","mkv","webm"}`, the mp4->webm transcode pair, `avExtractSources = {"mp4","mov","avi","mkv","webm"}` × `avAudioTargets = {"mp3","wav","m4a"}`, and avExtractSources × `avThumbnailTargets = {"jpg","png","webp"}`. Document this pair set in a comment (it shapes Phase 35's disjointness test). Implement the argv builders as isolated pure functions mirroring ffmpegNormalizeArgs: `transcodeToMP4Args`, `transcodeToWebMArgs` (verbatim from RESEARCH.md Pattern 1, referencing x264DefaultCRF constant, not a literal), a HEVC variant or a codec parameter on the mp4 builder that selects `-c:v libx265 -crf {x265DefaultCRF}` when AVOpts.Codec=="hevc" (Pitfall 4 — use x265DefaultCRF, never x264's), `extractAudioArgs(inPath, outPath, target, streamCopy bool)` (from Code Examples audio-extract argv; when streamCopy is true use `-c:a copy`), and `thumbnailArgs(inPath, outPath, target string, timecode float64)` with input-side `-ss` before `-i` and an explicit per-target `-c:v` (Pitfall 3). EVERY builder's argv must lead with `-y -nostdin -protocol_whitelist file,crypto` and use `file:`+inPath on the -i element (AVE-02 / A4). Add a `-threads` element sized from a hardcoded-safe value or a package var if the executor chooses (env wiring is Phase 36 — do not read os.Getenv). No package-level doc comment; every exported identifier documented starting with its own name.
    Create `internal/convert/av_test.go` with `requireLiveAVBinaries(t)` skip-gating on exec.LookPath("ffmpeg") and exec.LookPath("ffprobe") (mirrors requireLiveAudioBinaries), plus a SEPARATE narrower helper that skips only the webp thumbnail test when `ffmpeg -encoders` output lacks `libwebp` (Pitfall 3 / Environment Availability — local dev may lack it). Add unconditional argv-pinning tests (`TestTranscodeToMP4Args`, `TestTranscodeToWebMArgs`, `TestThumbnailArgs_ExplicitCodec`, `TestExtractAudioArgs`, `TestHEVCUsesX265CRF`) asserting the exact returned []string including the leading `-protocol_whitelist file,crypto` and `file:` prefix, and asserting the HEVC builder uses x265DefaultCRF. Add `TestAVConverter_PairsSelfDisjoint` asserting no duplicate (from,to) pair within Pairs() (Open Question 3 — cheap insurance; cross-converter test is Phase 35).
  </action>
  <verify>
    <automated>cd /Users/apaderin/dev/octoconv && go test ./internal/convert/ -run 'TestTranscode|TestThumbnailArgs|TestExtractAudioArgs|TestHEVCUsesX265CRF|TestAVConverter_PairsSelfDisjoint' -v && gofmt -l internal/convert/av.go && go vet ./internal/convert/ && grep -c 'os.Getenv' internal/convert/av.go</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/convert/ -run 'TestTranscode|TestThumbnailArgs|TestExtractAudioArgs|TestHEVCUsesX265CRF|TestAVConverter_PairsSelfDisjoint'` passes with all argv-pinning functions run (not skipped — they are pure functions)
    - Every argv-pinning test asserts the presence of `-protocol_whitelist`,`file,crypto` and a `file:`-prefixed input path (AVE-02)
    - `TestHEVCUsesX265CRF` proves the HEVC branch references x265DefaultCRF (28), never x264DefaultCRF (23)
    - `TestThumbnailArgs_ExplicitCodec` proves each of jpg/png/webp gets an explicit `-c:v` (mjpeg/png/libwebp) and that `-ss` precedes `-i`
    - `TestAVConverter_PairsSelfDisjoint` passes (no duplicate pairs)
    - `grep -c 'os.Getenv' internal/convert/av.go` returns 0
    - `gofmt -l internal/convert/av.go` prints nothing; `go vet ./internal/convert/` exits 0
  </acceptance_criteria>
  <done>AVConverter advertises the locked Pairs() set with no duplicates, Engine() returns EngineAV, and all three argv builders produce live-verified hardened argv (protocol-whitelist + file: prefix + explicit per-target codecs, x265 CRF distinct from x264), proven by unconditional pinning tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert() orchestration — guards, stream-copy fast path, protocol-whitelist canary, output validation</name>
  <files>internal/convert/av.go, internal/convert/av_test.go</files>
  <read_first>
    - internal/convert/av.go (current state after Task 1 — the argv builders and Pairs()/Engine() being orchestrated)
    - internal/convert/whisper.go (Convert lines 230-295 orchestration: AVOptsFromMap->wrap "av: %w", fail-fast on unsupported target, per-stage error prefixes "av: ffmpeg:", validateAudioOutput; the fail-fast-before-expensive-work shape)
    - internal/convert/audioduration.go (ProbeDuration/EnforceMaxDuration reused verbatim for the duration axis)
    - internal/convert/avduration.go (EnforceMaxResolution/probeVideoStream from Plan 34-02, for the resolution axis and stream-copy codec check)
    - .planning/phases/34-av-engine-foundation/34-RESEARCH.md (Pattern 2 avStreamCopyLegal + Anti-Pattern 2 the VP9/Opus-into-mp4 muxer footgun; Pitfall 2 out-of-range -ss produces NO file; Code Examples "Protocol-whitelist offline canary" lines 586-609 for the required canary test; ffprobe audio-codec probe line 547)
    - .planning/phases/34-av-engine-foundation/34-PATTERNS.md (av.go Convert orchestration and output post-validation sections)
  </read_first>
  <behavior>
    - Convert dispatches on NormalizeFormat(ext(outPath)): mp4/webm->transcode, mp3/wav/m4a->audio-extract, jpg/png/webp->thumbnail; an unsupported target fails fast with "av: unsupported target format" BEFORE any subprocess
    - Convert runs the guard stage (EnforceMaxDuration reused + EnforceMaxResolution) BEFORE any ffmpeg encode/decode; a duration or resolution over ceiling aborts before the expensive stage
    - transcode path: probe source video+audio codec via ffprobe; call -c copy only when avStreamCopyLegal(targetContainer, srcVideoCodec, srcAudioCodec) is true; otherwise full re-encode. avStreamCopyLegal returns true only for mp4<-h264/aac and webm<-vp9/opus
    - a VP9/Opus source targeting mp4 is NOT stream-copied (it re-encodes to h264/aac) — proven by test, because ffmpeg's muxer would otherwise silently accept the illegal remux
    - audio-extract path: when source audio codec is aac and target is m4a, use -c:a copy (AVC-03); otherwise re-encode per target
    - thumbnail path: default timecode 1.0s when AVOpts.Timecode is zero; the timecode is bounds-checked against ProbeDuration (clamped/rejected if past EOF, Pitfall 2) before invoking ffmpeg; output post-validation treats a MISSING file (os.Stat error) and a zero-byte file identically as "no output"; a produced thumbnail is additionally re-Sniff()'d to confirm it decodes as the expected image format
    - a crafted HLS m3u8 with an http:// segment reference is rejected by ffmpeg due to -protocol_whitelist file,crypto (offline canary, non-zero exit, no outbound connection)
    - every subprocess-stage error carries a distinguishable prefix ("av: ffmpeg:", "av: ffprobe:")
  </behavior>
  <action>
    Extend `internal/convert/av.go` with `avStreamCopyLegal(targetContainer, srcVideoCodec, srcAudioCodec string) bool` verbatim from RESEARCH.md Pattern 2 (mp4<-h264/aac, webm<-vp9/opus, default false) with the load-bearing comment explaining ffmpeg's mp4 muxer silently accepts VP9/Opus remux (Anti-Pattern 2). Add an ffprobe audio-codec probe argv builder (`ffprobeAudioCodecArgs`, `-select_streams a:0 -show_entries stream=codec_name -of csv=p=0`, WITH `-protocol_whitelist file,crypto` and `file:` prefix) and a small helper returning the source audio codec. Implement `validateAVOutput(path string) error` copying validateAudioOutput but mapping BOTH an os.Stat error AND Size()==0 to the same "av: output missing or empty" class (Pitfall 2 — thumbnail out-of-range -ss produces no file at all); for thumbnail targets additionally open the output and call Sniff() to confirm it decodes as jpg/png/webp.
    Implement `func (c AVConverter) Convert(ctx, inPath, outPath string, opts map[string]any) error`: (1) `AVOptsFromMap(opts)` wrapped `"av: %w"`; (2) determine targetFormat via NormalizeFormat(filepath.Ext(outPath)) and dispatch to one of three paths, failing fast with `"av: unsupported target format %q"` on an unknown target BEFORE any subprocess (mirrors whisper.go fail-fast); (3) GUARD STAGE before any decode — call EnforceMaxDuration (reused; pass a plain-parameter ceiling — a named package const or a sensible Phase-34 default, env wiring deferred to Phase 36) and EnforceMaxResolution, wrapping errors distinctly; (4) transcode path — run `runCommand(ctx,"ffprobe",...)` for video+audio codec (prefix errors `"av: ffprobe:"`), gate `-c copy` on avStreamCopyLegal, else pick the re-encode builder (libx265 when AVOpts.Codec=="hevc", else libx264 for mp4; libvpx-vp9/libopus for webm); (5) audio-extract path — probe audio codec, use `-c:a copy` only when source aac + target m4a; (6) thumbnail path — resolve timecode (default 1.0, bounds-check against ProbeDuration per Pitfall 2), build thumbnailArgs; (7) run the ffmpeg stage wrapped `"av: ffmpeg: %w"`; (8) return validateAVOutput(outPath). Reuse runCommand unmodified. Reference the duration ceiling as a documented plain value (note: env-wiring is Phase 36, mirroring EnforceMaxDuration's own contract). Do NOT read os.Getenv. Do NOT register into convert.Default or edit converters.go (scope fence).
    Extend `internal/convert/av_test.go`: `TestAVStreamCopyLegal` (table: mp4/h264/aac->true, mp4/vp9/opus->false, webm/vp9/opus->true, webm/h264/aac->false, unknown container->false); `TestAVConverter_Transcode_LiveBinary` (mov->mp4, assert ffprobe-confirmed h264/aac output — requireLiveAVBinaries); `TestAVConverter_MP4ToWebM_LiveBinary`; `TestAVConverter_VP9SourceToMP4_ReEncodes` proving a webm(VP9/Opus)->mp4 job yields h264/aac output, NOT a silent remux (the load-bearing AVC-05 contract test); `TestAVConverter_AudioExtract_LiveBinary` including the AAC-source->m4a `-c:a copy` case; `TestAVConverter_Thumbnail_LiveBinary` (jpg/png; webp behind the libwebp skip-gate) including a `TestAVConverter_Thumbnail_OutOfRangeSS` proving an out-of-range timecode fails closed (no output file); `TestProtocolWhitelist_BlocksHTTP_Canary` building the crafted evil.m3u8 from RESEARCH.md Code Examples and asserting the ffmpeg invocation fails (non-zero) with protocol rejection (AVE-02 required offline canary); `TestAVConverter_UnsupportedTargetFailsFast` and `TestAVConverter_GarbageOpts` mirroring whisper_test.go. Use ffmpeg lavfi (testsrc+sine) to generate fixtures inside live-gated tests, same philosophy as the audio engine's live tests.
  </action>
  <verify>
    <automated>cd /Users/apaderin/dev/octoconv && go test ./internal/convert/ -run 'TestAVStreamCopyLegal|TestAVConverter|TestProtocolWhitelist' -v && gofmt -l internal/convert/av.go && go vet ./internal/convert/ && go test ./internal/convert/</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/convert/ -run 'TestAVStreamCopyLegal|TestAVConverter|TestProtocolWhitelist' -v` passes; live-binary tests either run (ffmpeg present) or Skip cleanly, but `TestAVStreamCopyLegal` and `TestAVConverter_UnsupportedTargetFailsFast`/`TestAVConverter_GarbageOpts` run unconditionally
    - `TestAVConverter_VP9SourceToMP4_ReEncodes` proves a VP9/Opus source targeting mp4 produces ffprobe-confirmed h264/aac output (no silent illegal remux) — the AVC-05 contract test
    - `TestProtocolWhitelist_BlocksHTTP_Canary` proves the crafted HLS http:// reference causes a non-zero ffmpeg exit (SSRF blocked) — AVE-02 offline canary
    - `TestAVConverter_Thumbnail_OutOfRangeSS` proves an out-of-range timecode fails closed with an "output missing or empty"-class error (Pitfall 2)
    - A guard-ordering assertion or code inspection confirms EnforceMaxDuration/EnforceMaxResolution are called before any ffmpeg encode/decode in Convert
    - `grep -c 'converters\|Default.Register\|Register(' internal/convert/av.go` returns 0 (scope fence — no registration)
    - `grep -c 'os.Getenv' internal/convert/av.go` returns 0
    - `go test ./internal/convert/` (full package) passes; `gofmt -l internal/convert/av.go` prints nothing; `go vet ./internal/convert/` exits 0
  </acceptance_criteria>
  <done>AVConverter.Convert runs the guard stage before decode, gates stream-copy on avStreamCopyLegal (never ffmpeg muxer acceptance), extracts audio/thumbnails with fail-closed output validation, applies -protocol_whitelist on every invocation (canary-proven), and stays unregistered — with the full convert package test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| uploaded file bytes → ffmpeg/ffprobe subprocess | Untrusted container is decoded/probed by an external engine invoked via runCommand |
| client opts (jobs.options) → AVConverter.Convert | Persisted opts are re-parsed strictly on the converter read path (D-10 parity) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-10 | Information Disclosure | SSRF/LFI via HLS/concat/subtitle references embedded in container content | mitigate | `-protocol_whitelist file,crypto` on every ffmpeg/ffprobe argv builder; proven by the offline HLS-http canary test (non-zero exit, no outbound connection). Concat-demuxer local-read residual is documented and scoped out (this phase uses no concat demuxer; never pass `-safe 0`) |
| T-34-11 | Tampering | Codec-mismatch silent contract violation (VP9/Opus remuxed into mp4) — AVC-05 footgun | mitigate | avStreamCopyLegal gates `-c copy` on a project-owned per-container codec allowlist checked via ffprobe BEFORE any copy attempt; never on ffmpeg muxer acceptance. TestAVConverter_VP9SourceToMP4_ReEncodes proves the re-encode |
| T-34-12 | Denial of Service | Multi-axis decode bomb (duration × resolution) | mitigate | EnforceMaxDuration (reused) AND EnforceMaxResolution run in the guard stage BEFORE any ffmpeg decode/encode; both fail-closed and independently required |
| T-34-13 | Elevation of Privilege | Opts injection (client bytes reaching ffmpeg argv / filtergraph) | mitigate | AVOptsFromMap strict-parses persisted opts; validated values select fixed argv templates (resolution enum, codec allowlist), never concatenated into `-vf`/argv strings; runCommand never invokes a shell |
| T-34-14 | Denial of Service | Runaway ffmpeg process on crafted input | mitigate | runCommand's Setpgid + SIGKILL-on-timeout process-group kill (reused unmodified) bounds every invocation |
| T-34-15 | Tampering | Thumbnail out-of-range -ss producing no/empty output silently | mitigate | Pre-flight timecode bounds-check against ProbeDuration + validateAVOutput mapping missing-file and zero-byte identically to "no output"; thumbnail output re-Sniff'd to confirm it decodes |
| T-34-SC | Tampering | npm/pip/cargo installs | accept | No package-manager dependencies added this phase; ffmpeg/ffprobe are OS binaries invoked via runCommand. Decoder-RCE version pinning (PixelSmash/CVE-2026-8461) is explicitly Phase 36 scope (Dockerfile.av-worker), no Docker surface exists in Phase 34 |
</threat_model>

<verification>
- `go test ./internal/convert/` passes (full package; live tests skip cleanly where ffmpeg/libwebp absent)
- `gofmt -l internal/convert/` prints nothing; `go vet ./internal/convert/` exits 0
- AVConverter is NOT registered: `grep -rc 'AVConverter{}' internal/convert/converters.go` returns 0 and converters.go is unchanged (scope fence)
- `grep -c 'os.Getenv' internal/convert/av.go` returns 0
- The protocol-whitelist canary test demonstrates a blocked remote reference (AVE-02)
</verification>

<success_criteria>
- ROADMAP Phase 34 success criteria 1, 2, 3 fully met (transcode mov/avi/mkv/webm->mp4 and mp4->webm; audio-extract with AAC->m4a stream-copy; thumbnail via input-side -ss default 1.0s clamped) and criterion 5's protocol-whitelist/canary + guard portion for the ffmpeg decode path
- AVC-01, AVC-02, AVC-03, AVC-04, AVC-05 satisfied and verified against a real ffmpeg binary; AVE-02's ffmpeg/ffprobe protocol-whitelist and duration+resolution guard wiring satisfied
- AVConverter remains standalone/unregistered — no queue/worker/API/converters.go changes (scope fence honored)
</success_criteria>

<output>
Create `.planning/phases/34-av-engine-foundation/34-03-SUMMARY.md` when done
</output>
