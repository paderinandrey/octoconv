---
phase: 35-queue-worker-routing-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/convert/av.go
  - internal/convert/avduration.go
  - internal/convert/av_test.go
  - internal/convert/avduration_test.go
  - internal/convert/whisper.go
  - internal/convert/whisper_test.go
  - internal/convert/pairs_disjoint_test.go
autonomous: true
requirements: [AVE-03, AVT-01]

must_haves:
  truths:
    - "A caller can tell which ffmpeg stage failed (transcode vs audio-extract vs thumbnail) using errors.Is, without string matching"
    - "A video container with zero video streams produces a typed, distinguishable error instead of an anonymous fmt.Errorf"
    - "A client can request a transcript from mp4/mov/avi/mkv/webm sources"
    - "Whisper's ffmpeg normalize stage deterministically selects the first audio stream of a multi-track container"
    - "AVConverter.Pairs() and AudioConverter.Pairs() share zero (from,to) pairs, proven by test"
  artifacts:
    - path: "internal/convert/av.go"
      provides: "ErrAVTranscodeFailed / ErrAVAudioExtractFailed / ErrAVThumbnailFailed sentinels + rewrapped call sites"
      contains: "ErrAVTranscodeFailed"
    - path: "internal/convert/avduration.go"
      provides: "ErrAVNoVideoStream sentinel (IN-01 fold-in)"
      contains: "ErrAVNoVideoStream"
    - path: "internal/convert/whisper.go"
      provides: "video source formats, video-aware minFfmpegBudget floor, -map 0:a:0"
      contains: "minFfmpegBudgetVideo"
    - path: "internal/convert/pairs_disjoint_test.go"
      provides: "AV/Audio pair-disjointness regression guard"
      contains: "TestAVAudioPairDisjointness"
  key_links:
    - from: "internal/convert/av.go convertTranscode"
      to: "ErrAVTranscodeFailed"
      via: "fmt.Errorf multi-%w wrap"
      pattern: "ErrAVTranscodeFailed"
    - from: "internal/convert/whisper.go ffmpegNormalizeArgs"
      to: "ffmpeg argv"
      via: "-map 0:a:0 inserted after -i, -protocol_whitelist retained"
      pattern: "0:a:0"
---

<objective>
Prepare the `internal/convert` layer for async wiring: give each AV ffmpeg stage its own typed sentinel error (D-01) so the worker-layer classifier can be stage-aware (D-02), give the "no video stream" rejection a sentinel too (IN-01 fold-in), extend the whisper pipeline to accept video containers (D-04/D-05), and pin AV/Audio pair disjointness with a test.

Purpose: Every downstream plan in this phase depends on these sentinels and pairs existing. `isAVTerminal` (plan 03) is structurally impossible to write correctly without D-01.
Output: 4 new sentinel errors, an extended audio source-format set, a video-aware ffmpeg budget floor, a deterministic `-map 0:a:0`, and two regression tests.
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-queue-worker-routing-integration/35-CONTEXT.md
@.planning/phases/35-queue-worker-routing-integration/35-PATTERNS.md
@.planning/phases/34-av-engine-foundation/34-SECURITY.md

<interfaces>
Existing sentinels in this package (the naming/declaration idiom to mirror):

- `internal/convert/av.go:267` — `var ErrAVOutputMissingOrEmpty = errors.New("av: output missing or empty")`
- `internal/convert/av.go:276` — `var ErrAVTimecodeOutOfRange = errors.New("av: timecode exceeds source duration")`
- `internal/convert/avduration.go:15` — `var ErrAVResolutionExceeded = errors.New("declared video resolution exceeds configured maximum")`
- `internal/convert/audioduration.go:117` — wraps `ErrAudioDurationExceeded`; AV's duration guard reuses this same sentinel via `enforceMaxDurationOf` (`av.go:395`) — deliberate reuse, not a bug.

Anonymous errors to be given sentinels (all three emit the same literal today):
- `internal/convert/av.go:443` — `fmt.Errorf("ffprobe: no video stream found")`
- `internal/convert/avduration.go:81` — same literal
- `internal/convert/avduration.go:153` — same literal

Identical wraps to be split (D-01):
- `internal/convert/av.go:481` (convertTranscode), `:528` (convertAudioExtract), `:566` (convertThumbnail) — all `fmt.Errorf("av: ffmpeg: %w", err)`

Whisper anchors:
- `internal/convert/whisper.go:68` — `audioSourceFormats = []string{"mp3", "wav", "m4a", "ogg"}`
- `internal/convert/whisper.go:90` — `const minFfmpegBudget = 30 * time.Second`
- `internal/convert/whisper.go:283-285` — the `ctx.Deadline()` budget check
- `internal/convert/whisper.go:163` — `func ffmpegNormalizeArgs(inPath, normPath string) []string`
- `internal/worker/worker.go:891` — `inPath := filepath.Join(workDir, "in."+job.SourceFormat)`, so `filepath.Ext(inPath)` is the source format
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Split AV ffmpeg-stage errors into typed sentinels (D-01) and add ErrAVNoVideoStream (IN-01)</name>
  <files>internal/convert/av.go, internal/convert/avduration.go, internal/convert/av_test.go, internal/convert/avduration_test.go</files>
  <read_first>
    - internal/convert/av.go (full — read the three convert* stage functions and the existing sentinel block around :260-280)
    - internal/convert/avduration.go (full)
    - internal/convert/av_test.go (confirm the grep-verified fact: no assertion pins the literal "av: ffmpeg:")
    - .planning/phases/35-queue-worker-routing-integration/35-PATTERNS.md §`internal/convert/av.go` — D-01 sentinel refactor
  </read_first>
  <behavior>
    - `errors.Is(convertTranscodeErr, ErrAVTranscodeFailed)` is true; `errors.Is(convertTranscodeErr, ErrAVThumbnailFailed)` is false.
    - `errors.Is(convertAudioExtractErr, ErrAVAudioExtractFailed)` is true and distinguishable from the other two.
    - `errors.Is(convertThumbnailErr, ErrAVThumbnailFailed)` is true and distinguishable from the other two.
    - The underlying `runCommand` error remains unwrappable through the sentinel (multi-`%w`), so `errors.Is(err, context.DeadlineExceeded)` still holds on a timeout.
    - A source with zero video streams yields an error satisfying `errors.Is(err, ErrAVNoVideoStream)`.
  </behavior>
  <action>
    Declare three package-level sentinels in `internal/convert/av.go` beside the existing `ErrAVOutputMissingOrEmpty` / `ErrAVTimecodeOutOfRange` block, using the same `var Err… = errors.New("av: …")` idiom: `ErrAVTranscodeFailed` ("av: transcode failed"), `ErrAVAudioExtractFailed` ("av: audio-extract failed"), `ErrAVThumbnailFailed` ("av: thumbnail failed"). Each carries a doc comment naming D-01 and stating that the worker-layer classifier (`isAVTerminal`) matches on it with `errors.Is`, and that transcode-timeout is transient while the other two stages are terminal on any failure (D-02).

    Rewrap the three `runCommand(ctx, "ffmpeg", …)` failure sites in `convertTranscode`, `convertAudioExtract` and `convertThumbnail` from the shared `fmt.Errorf("av: ffmpeg: %w", err)` to Go 1.20+ multi-verb form `fmt.Errorf("%w: %w", <stage sentinel>, err)`. Do not change any argv, guard, ordering or conversion behavior — this is an error-wrapping change only. AVE-02 invariant: every one of these invocations already carries `-protocol_whitelist file,crypto` with `file:`-prefixed paths; do not touch the argv builders.

    Declare `ErrAVNoVideoStream` in `internal/convert/avduration.go` beside `ErrAVResolutionExceeded` (idiom: `errors.New("ffprobe: no video stream found")`), with a doc comment naming IN-01 (34-REVIEW.md): a generic-brand ISOBMFF audio-only file (major brand `isom`/`mp42`) sniffs as `mp4`, routes to the av engine once `AVConverter` registers, and dies here — a typed sentinel lets the worker mark it terminal with a distinguishable `error_code` instead of retrying a hopeless job. Replace all three anonymous `fmt.Errorf("ffprobe: no video stream found")` sites (`av.go` avProbeSource, `avduration.go` x2) with `fmt.Errorf("%w", ErrAVNoVideoStream)` or a direct return of the sentinel, whichever preserves the existing call-site signature.

    Add table-driven tests in `av_test.go` (and `avduration_test.go` for the no-video-stream path) that construct each stage error via the existing live-binary test path (follow the `requireLiveAVBinaries` skip-gate precedent already in the file) or by direct unit construction where a live binary is unnecessary, and assert both positive `errors.Is` on the intended sentinel and negative `errors.Is` on the other two.
  </action>
  <verify>
    <automated>go build ./... &amp;&amp; go vet ./internal/convert/... &amp;&amp; go test ./internal/convert/ -run 'AV' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'av: ffmpeg:' internal/convert/av.go` returns no matches.
    - `grep -c 'ErrAVTranscodeFailed\|ErrAVAudioExtractFailed\|ErrAVThumbnailFailed' internal/convert/av.go` >= 6 (3 declarations + 3 call sites).
    - `grep -v '^\s*//' internal/convert/av.go internal/convert/avduration.go | grep -c 'fmt.Errorf("ffprobe: no video stream found")'` == 0.
    - `grep -c 'protocol_whitelist' internal/convert/av.go` is unchanged from its pre-task value (AVE-02 non-regression; record the before/after count in the SUMMARY).
    - `go test ./internal/convert/ -run 'AV' -count=1` passes with new assertions present for all four sentinels.
  </acceptance_criteria>
  <done>Each AV ffmpeg stage and the no-video-stream rejection are distinguishable via `errors.Is`; conversion behavior and ffmpeg argv are byte-identical to HEAD.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend whisper to video sources (D-04), raise its ffmpeg budget floor (D-05), pin -map 0:a:0</name>
  <files>internal/convert/whisper.go, internal/convert/whisper_test.go</files>
  <read_first>
    - internal/convert/whisper.go (full — `audioSourceFormats`, `minFfmpegBudget` doc comment, `Convert`, `ffmpegNormalizeArgs`)
    - internal/convert/whisper_test.go (the existing argv-pinning test style for `ffmpegNormalizeArgs`)
    - .planning/phases/35-queue-worker-routing-integration/35-RESEARCH.md §Open Questions 1-3 and §Code Examples
  </read_first>
  <behavior>
    - `AudioConverter{}.Pairs()` contains `{mp4,mov,avi,mkv,webm} × {txt,srt,vtt,json}` (20 new pairs) in addition to the existing 16.
    - `AudioConverter{}.Engine()` still returns `convert.EngineAudio` for every pair.
    - `ffmpegNormalizeArgs` output contains `-map` immediately followed by `0:a:0`, positioned after the `-i file:<inPath>` pair and before `-ar`.
    - `ffmpegNormalizeArgs` still contains `-protocol_whitelist file,crypto`, `-nostdin`, and `file:`-prefixed input and output paths.
    - With a video source extension, the pre-stage-1 budget floor is `minFfmpegBudgetVideo`; with mp3/wav/m4a/ogg it stays `minFfmpegBudget`.
  </behavior>
  <action>
    D-04: extend `audioSourceFormats` with `"mp4", "mov", "avi", "mkv", "webm"`. Update the surrounding doc comment to record D-04 and that source/target sets remain disjoint (targets are still exactly `{txt,srt,vtt,json}`), which is what keeps `AudioConverter` and `AVConverter` pair-disjoint under `Registry.Register`'s silent last-write-wins semantics.

    D-05: add `const minFfmpegBudgetVideo = 90 * time.Second` next to `minFfmpegBudget`, with a doc comment marking the value `[ASSUMED]` — provisional, grounded in RESEARCH.md's local-SSD demux measurements, revisited in Phase 36 alongside the RTF matrix — and explaining that the floor protects against upstream S3-download budget exhaustion, not raw demux cost. Add `var videoSourceFormats = map[string]bool{"mp4": true, "mov": true, "avi": true, "mkv": true, "webm": true}`. In `Convert`, replace the flat `minFfmpegBudget` in the `ctx.Deadline()` check with a `floor` local selected by `videoSourceFormats[NormalizeFormat(strings.TrimPrefix(filepath.Ext(inPath), "."))]`. Note that `process()` builds `inPath` as `in.<job.SourceFormat>` (`worker.go:891`), so the extension is the declared source format. Do NOT raise `AUDIO_ENGINE_TIMEOUT` and do NOT touch `AudioUniqueTTL` — D-05 exists specifically to avoid a class-wide recompute.

    Multi-track determinism: insert `"-map", "0:a:0"` into `ffmpegNormalizeArgs` between the `-i file:<inPath>` pair and `-ar`. Extend the existing IN-01/WR-08 comment block to record that this mirrors `AVConverter`'s own `0:a:0[?]` convention (`av.go:122,143,163`) and is a verified no-op for single-audio-track sources. Do not remove or reorder `-protocol_whitelist file,crypto` or `-nostdin` (AVE-02, T-34-10 non-regression).

    Close research Open Questions 1 and 2 with regression tests rather than code. Both were resolved by live execution and need NO new sentinel or guard: (i) a video source with ZERO audio streams already fails closed — ffmpeg exits 234, the error wraps to `"audio: ffmpeg:"`, and the existing `isAudioTerminal` classifies it terminal; (ii) a silent/tone-only audio track already fails closed — whisper exits 0 with an EMPTY transcript and `validateAudioOutput`'s existing `"audio: output is empty"` check is already in `terminalAudioSignatures`. Add two tests pinning these behaviors behind the `requireLiveAVBinaries` skip-gate precedent, generating the fixtures with ffmpeg. Their value is that both outcomes are currently EMERGENT properties of independently-motivated mechanisms, not designed guarantees. Note in the silence test's comment that STATE.md's accepted hallucination risk is NOT retired by this — moderate-confidence real-world background chatter can still yield a non-empty hallucinated transcript that an emptiness check cannot catch.

    Extend `whisper_test.go`: (a) assert the 20 new pairs are present and `Engine()` is `EngineAudio` for them; (b) extend the existing `ffmpegNormalizeArgs` argv-pinning test to assert the `-map 0:a:0` adjacency AND the continued presence of `-protocol_whitelist file,crypto` / `-nostdin` / `file:` prefixes; (c) a table-driven test over source extensions asserting the selected floor is `minFfmpegBudgetVideo` for the five video formats and `minFfmpegBudget` for mp3/wav/m4a/ogg (factor the floor selection into a small package-level helper if that is what makes it unit-testable without a live subprocess).
  </action>
  <verify>
    <automated>go build ./... &amp;&amp; go test ./internal/convert/ -run 'Whisper|Audio|Normalize|Budget|Pairs' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'minFfmpegBudgetVideo' internal/convert/whisper.go` >= 3 (const, floor selection, doc reference).
    - `grep -n '"-map", "0:a:0"' internal/convert/whisper.go` returns exactly one match.
    - `grep -c 'protocol_whitelist' internal/convert/whisper.go` is unchanged from its pre-task value.
    - A test failure is produced if `-map 0:a:0` is removed (confirm by temporarily deleting it, running the test, then restoring — report the observed failure in the SUMMARY).
    - `go test ./internal/convert/ -count=1` passes; `AudioConverter{}.Pairs()` length is 36.
    - `AUDIO_ENGINE_TIMEOUT` and `AudioUniqueTTL` appear nowhere in this task's diff.
    - Two regression tests exist for the no-audio-track and silent-audio-track video cases, both skip-gated on live ffmpeg/whisper binaries, and both assert a TERMINAL classification through the existing `isAudioTerminal` / empty-output path with no new sentinel introduced.
  </acceptance_criteria>
  <done>Video containers are transcribable through the existing audio queue; the ffmpeg normalize stage picks a deterministic audio stream and gets a larger guaranteed budget for video sources.</done>
</task>

<task type="auto">
  <name>Task 3: Pin AV/Audio pair disjointness with a regression test</name>
  <files>internal/convert/pairs_disjoint_test.go</files>
  <read_first>
    - internal/convert/avsniff_test.go (lines around TestVideoBrandDisjointness — the nested-loop-over-two-collections shape to mirror)
    - internal/convert/av.go (AVConverter.Pairs())
    - internal/convert/whisper.go (AudioConverter.Pairs(), as modified by Task 2)
    - internal/convert/convert.go (Registry.Register — the bare map assignment with silent last-write-wins)
  </read_first>
  <action>
    Create `internal/convert/pairs_disjoint_test.go` with `TestAVAudioPairDisjointness`: a nested loop over `AVConverter{}.Pairs()` × `AudioConverter{}.Pairs()` that fails on any equal `Pair`. The doc comment must state the hazard explicitly — `Registry.Register` (`convert.go:74-80`) is a bare map assignment with no collision check, so a later `Default.Register(AVConverter{})` would silently win any shared pair and route jobs to the wrong queue with no error, panic or log; this test is the ONLY guard, there is no runtime one. Also record why disjointness currently holds by construction: AV's target set is `{mp4,webm,mp3,wav,m4a,jpg,png,webp}` and audio's is always `{txt,srt,vtt,json}`, even though after D-04 both share the five video SOURCE formats.

    Add a second assertion in the same file proving the guard actually bites: build the union of both converters' pairs into a map and assert its length equals `len(AVConverter{}.Pairs()) + len(AudioConverter{}.Pairs())`, so a future overlapping target is caught by count as well as by identity.
  </action>
  <verify>
    <automated>go test ./internal/convert/ -run TestAVAudioPairDisjointness -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/convert/ -run TestAVAudioPairDisjointness -v -count=1` reports PASS.
    - The test fails if `audioTargetFormats` is temporarily given an overlapping entry such as `"mp4"` (confirm by temporary edit, then restore — report the observed failure message in the SUMMARY).
    - The test file contains no reference to `convert.Default` (it must test the converters directly, not registration order).
  </acceptance_criteria>
  <done>A future edit introducing an overlapping (from,to) pair between the two converters fails the test suite instead of silently misrouting jobs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client file bytes → ffmpeg subprocess argv | Untrusted container content is decoded by ffmpeg; embedded HLS/concat/subtitle references can reach the filesystem or network |
| client-declared source format → whisper budget selection | The extension used to pick the budget floor derives from `job.SourceFormat`, which is server-set by content sniffing, not client-supplied |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | Elevation of Privilege | `ffmpegNormalizeArgs` (whisper.go) | mitigate | `-protocol_whitelist file,crypto` + `-nostdin` + `file:`-prefixed in/out paths retained verbatim; grep-count non-regression asserted in acceptance criteria (AVE-02 / T-34-10) |
| T-35-02 | Tampering | `-map 0:a:0` insertion | mitigate | Argv element is a compile-time literal, never client-influenced; argv-pinning test asserts exact position |
| T-35-03 | Information Disclosure | new sentinel error strings | accept | Sentinels carry only stage names ("av: transcode failed"), no paths or client bytes; the wrapped `runCommand` error is already the existing disclosure surface, unchanged |
| T-35-04 | Denial of Service | video sources on the audio queue (D-04) | mitigate | Duration guard (`enforceAudioGuardBeforeConvert`, `ErrAudioDurationExceeded`) already runs for `EngineAudio` jobs regardless of container; D-05 raises only the pre-stage floor, not the attempt timeout |
| T-35-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — this phase adds zero dependencies; `git diff --stat go.mod go.sum` must show no changes |
</threat_model>

<verification>
- `go build ./... && go vet ./... && go test ./internal/convert/ -count=1` all pass.
- `git diff --stat go.mod go.sum` shows no changes.
- `grep -rn 'protocol_whitelist' internal/convert/` count is >= its pre-plan value.
</verification>

<success_criteria>
- Four new sentinels (`ErrAVTranscodeFailed`, `ErrAVAudioExtractFailed`, `ErrAVThumbnailFailed`, `ErrAVNoVideoStream`) exist and are `errors.Is`-distinguishable.
- `AudioConverter{}.Pairs()` has 36 entries covering all five video containers.
- `TestAVAudioPairDisjointness` passes and demonstrably fails on an induced overlap.
- No conversion behavior, argv, or timeout constant outside `minFfmpegBudgetVideo` changed.
</success_criteria>

<output>
Create `.planning/phases/35-queue-worker-routing-integration/35-01-SUMMARY.md` when done
</output>
