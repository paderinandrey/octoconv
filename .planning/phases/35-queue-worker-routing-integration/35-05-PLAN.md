---
phase: 35-queue-worker-routing-integration
plan: 05
type: execute
wave: 2
depends_on: ["35-02"]
files_modified:
  - internal/reconciler/reconciler.go
  - internal/reconciler/reconciler_test.go
autonomous: true
requirements: [AVE-03]

must_haves:
  truths:
    - "A stranded job with jobs.engine='av' is re-enqueued onto the av queue by the reconciler sweep"
    - "An av job is never routed to the image, document, html or audio queue"
    - "Every engine constant has a case in the reconciler routing switch, proven by test"
  artifacts:
    - path: "internal/reconciler/reconciler.go"
      provides: "enqueuer interface +EnqueueAVConvert and the EngineAV routing case"
      contains: "EnqueueAVConvert"
    - path: "internal/reconciler/reconciler_test.go"
      provides: "fakeEnqueuer.EnqueueAVConvert, TestSweepRoutesAVJobsToAVQueue, routing completeness test"
      contains: "TestSweepRoutesAVJobsToAVQueue"
  key_links:
    - from: "internal/reconciler/reconciler.go sweep"
      to: "s.enq.EnqueueAVConvert"
      via: "case convert.EngineAV in the engine routing switch"
      pattern: "case convert.EngineAV"
---

<objective>
Give the reconciler an `av` routing case so stranded `jobs.engine='av'` rows recover onto the same queue the API enqueues to, and add a completeness test asserting every engine constant is routable (D-06, second of the three seams).

Purpose: ROADMAP success criterion 1 requires reconciler recovery for av jobs. The routing switch's `default:` currently names `av` as the next engine to add.
Output: An extended `enqueuer` interface, the routing case, a routing test, and an all-engines completeness test.
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-queue-worker-routing-integration/35-CONTEXT.md
@.planning/phases/35-queue-worker-routing-integration/35-PATTERNS.md
@.planning/phases/35-queue-worker-routing-integration/35-02-SUMMARY.md

<interfaces>
Anchors verified at HEAD:

- `internal/reconciler/reconciler.go:58-65` — `type enqueuer interface { EnqueueImageConvert; EnqueueWebhookDeliver; EnqueueDocumentConvert; EnqueueHTMLConvert; EnqueueAudioConvert }`, all `func(ctx context.Context, id uuid.UUID) error`
- `internal/reconciler/reconciler.go:284-305` — the `switch j.Engine` routing block, whose `default:` records `metrics.RecordReconcilerAction("unroutable_engine")` and `continue`, and whose comment explicitly names `av` as the next engine to add
- `internal/reconciler/reconciler_test.go:89-184` — `fakeEnqueuer` with per-engine call slices and accessor helpers
- `internal/reconciler/reconciler_test.go:132` — `func (f *fakeEnqueuer) EnqueueAudioConvert(...)`
- `internal/reconciler/reconciler_test.go:276-305` — `TestSweepRoutesAudioJobsToAudioQueue` (the exact test to mirror, including the negative assertions on every other queue)
- From plan 02: `(*queue.Client).EnqueueAVConvert`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the av routing case to the reconciler</name>
  <files>internal/reconciler/reconciler.go</files>
  <read_first>
    - internal/reconciler/reconciler.go (full — the enqueuer interface at :58 and the routing switch at :284)
    - .planning/phases/35-queue-worker-routing-integration/35-PATTERNS.md §`internal/reconciler/reconciler.go`
  </read_first>
  <action>
    Add `EnqueueAVConvert(ctx context.Context, id uuid.UUID) error` to the unexported `enqueuer` interface, positioned after `EnqueueAudioConvert`.

    Add `case convert.EngineAV: enqueueErr = s.enq.EnqueueAVConvert(ctx, j.ID)` to the `switch j.Engine` routing block, after the `EngineAudio` case and before `default:`. Update the `default:` arm's comment: it currently names `av` as the next engine to add — replace that with the remaining out-of-scope engine values (cad/archive/probe) so the comment does not go stale. Do not change the fail-closed behavior of the `default:` arm or its `unroutable_engine` metric.

    Do NOT refactor the switch into a shared `queueForEngine` helper — explicitly declined for this phase (D-06); the completeness test in Task 2 is the sanctioned structural replacement.
  </action>
  <verify>
    <automated>go build ./... &amp;&amp; go vet ./internal/reconciler/...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'case convert.EngineAV' internal/reconciler/reconciler.go` returns one match inside the routing switch.
    - `grep -n 'EnqueueAVConvert' internal/reconciler/reconciler.go` returns two matches (interface declaration + call site).
    - `grep -v '^\s*//' internal/reconciler/reconciler.go | grep -c 'unroutable_engine'` is unchanged from its pre-task value (fail-closed default intact).
    - The `default:` comment no longer names `av` as pending — verify by reading and state the new wording in the SUMMARY.
    - `grep -c 'queueForEngine' internal/reconciler/reconciler.go` == 0.
  </acceptance_criteria>
  <done>Stranded av jobs are routable by the sweep instead of hitting the fail-closed default.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the av fake, routing test, and engine-routing completeness test (D-06)</name>
  <files>internal/reconciler/reconciler_test.go</files>
  <read_first>
    - internal/reconciler/reconciler_test.go lines 89-184 (fakeEnqueuer) and 270-345 (TestSweepRoutesAudioJobsToAudioQueue and its neighbours)
    - internal/reconciler/reconciler.go (as modified by Task 1)
    - internal/convert/convert.go (the Engine* const block to enumerate)
  </read_first>
  <behavior>
    - A stale job with `Engine: "av"` produces exactly one `EnqueueAVConvert` call with that job's ID.
    - The same sweep produces zero `EnqueueImageConvert` / `EnqueueDocumentConvert` / `EnqueueHTMLConvert` / `EnqueueAudioConvert` calls.
    - For every engine constant, a stale job with that engine produces exactly one call to the matching `Enqueue*` method and zero `unroutable_engine` outcomes.
  </behavior>
  <action>
    Extend `fakeEnqueuer` with `enqueueAVErr error` and `avCalls []uuid.UUID`, an `EnqueueAVConvert` method mirroring `EnqueueAudioConvert`'s mutex-guarded append, and an `avCallIDs()` accessor mirroring the existing per-engine accessors.

    Add `TestSweepRoutesAVJobsToAVQueue` mirroring `TestSweepRoutesAudioJobsToAudioQueue` exactly: one stale `jobs.StaleJob{Status: jobs.StatusActive, Engine: "av"}`, assert exactly one `EnqueueAVConvert` call carrying the right ID, and assert zero calls on all four other engine methods.

    Add `TestSweepRoutesEveryEngineConstant` — the D-06 completeness guard for the reconciler seam. Build a table mapping each `convert.Engine*` constant to an accessor returning that engine's recorded call slice, run one sweep per engine with a single stale job, and assert the matching accessor recorded exactly one call while every other accessor recorded zero. The doc comment must state that this exists because D-06 declined the `queueForEngine` refactor: a new engine constant added without a routing case must fail this test rather than silently hitting the fail-closed `default:` in production. Drive the table from the engine constants so a sixth engine class added later with no case fails here.
  </action>
  <verify>
    <automated>go test ./internal/reconciler/ -run 'TestSweepRoutes' -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/reconciler/ -count=1` passes.
    - `TestSweepRoutesAVJobsToAVQueue` fails if the `case convert.EngineAV` arm is removed from the routing switch (confirm by temporary edit, then restore — report the failure in the SUMMARY).
    - `TestSweepRoutesEveryEngineConstant` enumerates all five engine constants; `grep -c 'convert.Engine' internal/reconciler/reconciler_test.go` >= 5.
    - `go vet ./internal/reconciler/...` clean.
  </acceptance_criteria>
  <done>Reconciler recovery for av jobs is proven, and any future engine constant without a routing case fails the test suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Postgres job rows → reconciler enqueue | `jobs.engine` drives which queue a recovered job lands on; the value is server-set at create time, never client-supplied |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-17 | Denial of Service | av jobs stranded in `active` forever | mitigate | `case convert.EngineAV` added to the routing switch; `TestSweepRoutesAVJobsToAVQueue` fails if removed |
| T-35-18 | Tampering | a job routed to the wrong engine's queue | mitigate | Routing keys on the exact `convert.Engine*` constant; `TestSweepRoutesEveryEngineConstant` asserts one-and-only-one enqueue per engine |
| T-35-19 | Denial of Service | duplicate concurrent processing from reconciler re-enqueue | mitigate | Held by `AVUniqueTTL` + `asynq.ErrDuplicateTask` (plan 02), not by the reconciler; the sweep's enqueue-first recovery pattern is unchanged |
| T-35-20 | Repudiation | an unknown engine silently dropped | accept | The `default:` arm's `unroutable_engine` metric is preserved unchanged — fail-closed with an observable counter is the existing accepted design |
| T-35-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero dependencies added |
</threat_model>

<verification>
- `go build ./... && go vet ./... && go test ./internal/reconciler/ -count=1` all pass.
- `git diff internal/reconciler/reconciler.go` shows changes only in the `enqueuer` interface, the routing switch case, and the `default:` comment.
</verification>

<success_criteria>
- The reconciler routes `jobs.engine='av'` to `EnqueueAVConvert`.
- No engine constant lacks a routing case, enforced by test.
- The `queueForEngine` refactor was not performed (D-06 deferred idea respected).
</success_criteria>

<output>
Create `.planning/phases/35-queue-worker-routing-integration/35-05-SUMMARY.md` when done
</output>
