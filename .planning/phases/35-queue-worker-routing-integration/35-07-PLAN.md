---
phase: 35-queue-worker-routing-integration
plan: 07
type: execute
wave: 3
depends_on: ["35-02", "35-03"]
files_modified:
  - cmd/av-worker/main.go
autonomous: false
requirements: [AVE-03]

must_haves:
  truths:
    - "A cmd/av-worker binary builds and serves TypeAVConvert tasks from the av queue"
    - "A long in-flight transcode survives SIGTERM instead of being aborted at asynq's silent 8s default"
    - "The av worker exposes Prometheus metrics on the same endpoint shape as the other engine workers"
    - "AV retry backoff uses queue.RetryDelayFunc, not asynq's default schedule"
  artifacts:
    - path: "cmd/av-worker/main.go"
      provides: "the av engine class's worker entry point"
      contains: "queue.TypeAVConvert"
  key_links:
    - from: "cmd/av-worker/main.go"
      to: "worker.Handler.HandleAVConvert"
      via: "asynq.ServeMux HandleFunc on queue.TypeAVConvert"
      pattern: "mux.HandleFunc\\(queue.TypeAVConvert"
    - from: "cmd/av-worker/main.go asynq.Config"
      to: "AV_ENGINE_TIMEOUT"
      via: "ShutdownTimeout = AV_ENGINE_TIMEOUT + 10s override of asynq's 8s default"
      pattern: "ShutdownTimeout"
---

<objective>
Create `cmd/av-worker/main.go`, the fifth engine class's worker binary, mirroring `cmd/audio-worker/main.go` as a drop-in template.

Purpose: Without this binary nothing consumes the `av` queue — jobs would enqueue and sit. ROADMAP success criterion 1 requires end-to-end consumption (queued → active → done).
Output: A buildable, runnable av worker with the correct concurrency, queue binding, retry-delay function and graceful-shutdown window.

Scope fence: this plan produces the Go binary ONLY. `Dockerfile.av-worker`, the docker-compose service, ffmpeg version pinning, the disk-space guard and CI bake are Phase 36. Helm/KEDA is Phase 37. Do not create or edit any Dockerfile, compose file, chart, or CI workflow.
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-queue-worker-routing-integration/35-CONTEXT.md
@.planning/phases/35-queue-worker-routing-integration/35-PATTERNS.md
@.planning/phases/35-queue-worker-routing-integration/35-02-SUMMARY.md
@.planning/phases/35-queue-worker-routing-integration/35-03-SUMMARY.md

<interfaces>
Anchors verified at HEAD:

- `cmd/audio-worker/main.go` (254 lines) — THE template. Read in full.
  - `:29-68` — signal context, `db.Connect`, `storage.New`, `queue.RedisOpt`, `queue.NewClient`, `jobs.NewRepo`, `worker.NewHandler`
  - `:61` and `:113` — `envDuration("AUDIO_ENGINE_TIMEOUT", ...)` read TWICE (handler param + ShutdownTimeout). This duplication is intentional and must be mirrored, not refactored into a shared local.
  - `:87,:97` — `convert.SetAudioModelPath` / `convert.SetAudioThreads`: the env-setter happens-before boundary (setters must run before `srv.Start`). AV has NO equivalent setter — `avThreadCount()` resolves from `CgroupCPULimit()`/`NumCPU()` directly. OMIT this block and its `resolveAudioThreads`/`envDurationSeconds`/`stripInlineComment` helpers.
  - `:100-114` — `asynq.NewServeMux` + `asynq.NewServer` with `Concurrency`, `Queues`, `RetryDelayFunc`, and the `ShutdownTimeout` override (asynq's own default is 8s, which silently caps the graceful window regardless of the pod's terminationGracePeriodSeconds)
  - `:116-159` — promhttp metrics endpoint and graceful-shutdown boilerplate
  - `:180-237` — `envInt`, `envDuration`, `firstField` helpers (copy verbatim)
- `internal/worker/worker.go:360` — `NewHandler(repo, store, registry, engineTimeout, webhookRepo, deliverer, enqueuer, signingSecret, presignTTL, audioMaxDuration)`
- From plan 02: `queue.TypeAVConvert`, `queue.QueueAV`, `queue.RetryDelayFunc`
- From plan 03: `(*worker.Handler).HandleAVConvert`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create cmd/av-worker/main.go from the audio-worker template</name>
  <files>cmd/av-worker/main.go</files>
  <read_first>
    - cmd/audio-worker/main.go (FULL FILE — it is the template, not a reference)
    - cmd/document-worker/main.go or cmd/html-worker/main.go (whichever exists — read one non-audio worker to confirm which parts of the audio template are audio-specific versus engine-generic)
    - internal/worker/worker.go lines 340-390 (NewHandler's parameter list and which params HandleAVConvert actually reads)
    - .planning/phases/35-queue-worker-routing-integration/35-PATTERNS.md §`cmd/av-worker/main.go`
    - .planning/phases/35-queue-worker-routing-integration/35-RESEARCH.md §Pitfall 6
  </read_first>
  <action>
    Create `cmd/av-worker/main.go` as `package main`, mirroring `cmd/audio-worker/main.go` structurally with `AV_*` names substituted.

    Wiring: signal-notified context, `db.Connect`, `storage.New`, `queue.RedisOpt`, `queue.NewClient` (deferred close), `jobs.NewRepo`, then `worker.NewHandler(repo, store, convert.Default, envDuration("AV_ENGINE_TIMEOUT", 600*time.Second), nil, nil, qc, nil, 0, 0)`. Comment each nil/zero argument with why it is safe: `webhookRepo`/`deliverer`/`signingSecret`/`presignTTL` are webhook-delivery-only and `HandleAVConvert` never reads them; the trailing `audioMaxDuration` is 0 for every non-audio worker command because AV's duration and resolution guards are self-contained inside `AVConverter.Convert`, not spliced through this parameter.

    Mark the `600*time.Second` default `[ASSUMED]` in a comment: provisional, mirroring the precedent `AUDIO_ENGINE_TIMEOUT` set (600s Go placeholder → 742s after Phase 32's RTF measurement); Phase 36 re-derives it. Also record the coupling — `RECONCILER_ACTIVE_STALE_AFTER` is a single global 900s default that must comfortably exceed every engine's timeout, so raising `AV_ENGINE_TIMEOUT` toward 900s is a coupled decision requiring the reconciler threshold to move in the same change (this near-broke the audio engine once; the incident is documented in `docker-compose.yml`).

    Server: `mux := asynq.NewServeMux()` with `mux.HandleFunc(queue.TypeAVConvert, h.HandleAVConvert)`, then `asynq.NewServer(redisOpt, asynq.Config{ Concurrency: envInt("AV_WORKER_CONCURRENCY", 2), Queues: map[string]int{queue.QueueAV: 1}, RetryDelayFunc: queue.RetryDelayFunc, ShutdownTimeout: envDuration("AV_ENGINE_TIMEOUT", 600*time.Second) + 10*time.Second })`. Copy the audio worker's `ShutdownTimeout` comment in substance: asynq defaults it to 8s, silently capping the graceful window regardless of the pod's terminationGracePeriodSeconds; aligning it to the engine timeout plus margin lets a genuinely long in-flight ffmpeg transcode survive SIGTERM instead of being aborted and requeued. Concurrency default is 2, not audio's value — transcode is CPU-bound and the container ceiling is Phase 36's to size.

    Deliberately duplicate the `envDuration("AV_ENGINE_TIMEOUT", ...)` read at both the `NewHandler` call and the `ShutdownTimeout` field, exactly as `cmd/audio-worker/main.go:61` and `:113` do. Do not hoist it into a shared local — keeping the file a drop-in template match is the point.

    Copy the promhttp metrics endpoint and graceful-shutdown boilerplate verbatim, substituting `audio-worker`/`audio` log strings with `av-worker`/`av`. Copy `envInt`, `envDuration` and `firstField` verbatim. OMIT `resolveAudioThreads`, `envDurationSeconds`, `stripInlineComment` and the `convert.SetAudioModelPath`/`SetAudioThreads` setter block — AV has no env-driven thread or model tunable in this phase.

    Do NOT create a Dockerfile, compose service, chart, or CI entry.
  </action>
  <verify>
    <automated>go build ./... &amp;&amp; go vet ./cmd/av-worker/... &amp;&amp; gofmt -l cmd/av-worker/</automated>
  </verify>
  <acceptance_criteria>
    - `go build -o /dev/null ./cmd/av-worker` succeeds.
    - `gofmt -l cmd/av-worker/` outputs nothing.
    - `grep -c 'AV_ENGINE_TIMEOUT' cmd/av-worker/main.go` == 2 (the intentional duplication).
    - `grep -n 'ShutdownTimeout' cmd/av-worker/main.go` returns one match and the line references `+ 10*time.Second`.
    - `grep -n 'RetryDelayFunc: queue.RetryDelayFunc' cmd/av-worker/main.go` returns one match.
    - `grep -n 'queue.QueueAV' cmd/av-worker/main.go` returns one match inside the `Queues` map.
    - `grep -c 'SetAudioModelPath\|SetAudioThreads\|resolveAudioThreads\|stripInlineComment' cmd/av-worker/main.go` == 0.
    - `grep -c 'RECONCILER_ACTIVE_STALE_AFTER' cmd/av-worker/main.go` >= 1.
    - `git status --porcelain` shows no new or modified Dockerfile, docker-compose, chart or workflow file.
  </acceptance_criteria>
  <done>The av queue has a consumer: a buildable worker binary bound to `queue.QueueAV`, serving `TypeAVConvert` with the D-03 retry schedule and a graceful-shutdown window sized to the engine timeout.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm the av worker consumes a real job end-to-end</name>
  <what-built>`cmd/av-worker` binary, bound to the `av` asynq queue, serving `queue.TypeAVConvert` via `worker.Handler.HandleAVConvert`. Combined with plans 02/04/06 this completes the queued → active → done path for video jobs.</what-built>
  <read_first>
    - README.md (the local run instructions, including the `set -a && . ./.env && set +a` env-sourcing convention)
    - docker-compose.yml (the postgres/redis/minio service ports — note the non-standard host ports: Postgres 5434, MinIO 9100/9101)
  </read_first>
  <action>Pause for the developer to run the live end-to-end check below. Claude has already automated everything automatable (build, vet, gofmt); this checkpoint exists only because confirming a real ffmpeg transcode flowing queued -> active -> done through live Postgres/Redis/MinIO requires a running local stack and a human reading job status and worker logs.</action>
  <how-to-verify>
    1. Start only the infrastructure services: `docker compose up -d postgres redis minio createbucket`. Do NOT run a k8s stack simultaneously (four confirmed OrbStack daemon wedges on record).
    2. Source env: `set -a && . ./.env && set +a`. Confirm `ffmpeg -version` reports 8.1.2 on the host (the worker runs on the host in this phase — the container is Phase 36).
    3. Run `go run ./cmd/api` and `go run ./cmd/av-worker` in separate shells. Expect the av worker to log its startup line naming the `av` queue.
    4. Upload a small real video: `curl -X POST http://localhost:8090/v1/jobs -H "X-API-Key: $KEY" -F file=@<small.mp4> -F target_format=webm`. Expect HTTP 202 with a job id.
    5. Poll `GET /v1/jobs/{id}` until status is `done`. Expect the transition queued → active → done, and a downloadable presigned result URL.
    6. Repeat with `target_format=srt` on the same mp4. Expect it to route to the AUDIO queue instead — the av worker must log nothing for this job, and `cmd/audio-worker` must pick it up (start it too, or confirm the job sits `queued` when only av-worker runs, which itself proves the routing split).
    7. Check `curl -s localhost:<api metrics port>/metrics | grep -i 'queue_depth.*av'` returns an `av` series.
  </how-to-verify>
  <acceptance_criteria>
    - The mp4 → webm job reaches status `done` with a non-empty result object.
    - The mp4 → srt job does NOT appear on the av worker's log; it is consumed by the audio worker (or remains `queued` when only the av worker runs).
    - The Prometheus scrape shows a queue-depth series for `av`.
    - No orphaned `ffmpeg` process remains after the worker exits (`pgrep ffmpeg` empty).
  </acceptance_criteria>
  <verify>
    <automated>MANUAL — requires a live docker-compose infra stack and host ffmpeg 8.1.2; the automatable portion (build/vet/gofmt) is covered by Task 1</automated>
  </verify>
  <done>A real mp4 reaches status `done` via cmd/av-worker, an mp4 -> srt job demonstrably does not, and the av queue-depth series is scrapeable.</done>
  <resume-signal>Type "approved" or describe what failed (include the job status, the worker log line, and the metrics grep output)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator env → worker resource ceiling | `AV_ENGINE_TIMEOUT` / `AV_WORKER_CONCURRENCY` size the CPU and wall-clock envelope for every av job |
| Redis → worker process | Task payloads arrive from shared Redis; only a job ID is trusted, all detail is re-read from Postgres |
| SIGTERM → in-flight ffmpeg | An aborted transcode leaves a partially-written temp file and requeues the job |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-26 | Denial of Service | asynq's silent 8s `ShutdownTimeout` default aborting long transcodes | mitigate | `ShutdownTimeout: AV_ENGINE_TIMEOUT + 10s` override copied from the audio worker's precedent (Pitfall 6); grep-asserted in acceptance criteria |
| T-35-27 | Denial of Service | av retries falling through to asynq's default backoff | mitigate | `RetryDelayFunc: queue.RetryDelayFunc` set explicitly in `asynq.Config`; the `TypeAVConvert` case is covered by plan 02's test |
| T-35-28 | Denial of Service | unbounded worker concurrency exhausting CPU | mitigate | `AV_WORKER_CONCURRENCY` defaults to 2 and binds only `queue.QueueAV`; the container-level CPU/RAM/disk ceiling is Phase 36 scope (AVE-04) |
| T-35-29 | Elevation of Privilege | worker running with excess privilege | transfer | Deferred to Phase 36's `Dockerfile.av-worker`, which inherits the `USER nobody` precedent from `Dockerfile.worker`; out of scope for the host-run binary |
| T-35-30 | Tampering | orphaned ffmpeg child process after timeout | mitigate | Already closed by `internal/convert/exec.go`'s `Setpgid` + process-group SIGKILL on context cancellation; the human-verify checkpoint asserts `pgrep ffmpeg` is empty |
| T-35-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero dependencies added; the binary is built from existing imports |
</threat_model>

<verification>
- `go build ./... && go vet ./... && gofmt -l .` all clean.
- `git diff --stat go.mod go.sum` shows no changes.
- No Dockerfile, docker-compose, Helm chart or CI workflow file was created or modified (Phase 36/37 scope fence).
</verification>

<success_criteria>
- `cmd/av-worker/main.go` builds, vets and formats clean.
- It binds `queue.QueueAV`, serves `queue.TypeAVConvert` with `HandleAVConvert`, uses `queue.RetryDelayFunc`, and overrides `ShutdownTimeout`.
- A live end-to-end mp4 → webm job reaches `done`, and mp4 → srt demonstrably routes elsewhere.
</success_criteria>

<output>
Create `.planning/phases/35-queue-worker-routing-integration/35-07-SUMMARY.md` when done
</output>
