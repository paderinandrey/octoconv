---
phase: 36-containerization-rtf-measured-timeout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/convert/avdiskguard.go
  - internal/convert/avdiskguard_test.go
  - internal/convert/av.go
  - internal/convert/converters.go
  - internal/convert/av_test.go
  - cmd/av-worker/main.go
autonomous: true
requirements: [AVE-04]

must_haves:
  truths:
    - "EnforceMinFreeDisk fail-closes BEFORE the ffmpeg transcode when free scratch space is below safetyFactor*inputSize"
    - "AVConverter carries MaxSourceDuration/MaxSourceResolutionHeight as struct fields; a bare AVConverter{} still defaults to 4h/4320 so every existing caller and test is unchanged"
    - "cmd/av-worker re-registers a configured AVConverter at startup from AV_MAX_DURATION_SECONDS, overriding the init() zero-value registration"
    - "AVE-02 non-regression: every ffmpeg/ffprobe argv builder still carries -protocol_whitelist file,crypto with file:-prefixed paths after the refactor"
    - "AV target/pair set is unchanged; TestAVAudioPairDisjointness still passes after re-registration"
  artifacts:
    - path: "internal/convert/avdiskguard.go"
      provides: "EnforceMinFreeDisk disk-space guard + ErrAVInsufficientDiskSpace sentinel"
      contains: "unix.Statfs"
    - path: "internal/convert/avdiskguard_test.go"
      provides: "table-driven fail-closed tests for EnforceMinFreeDisk"
    - path: "internal/convert/av.go"
      provides: "AVConverter struct fields + disk-guard call site in Convert"
      contains: "MaxSourceDuration"
    - path: "cmd/av-worker/main.go"
      provides: "envDurationSeconds + configured re-registration + disk-safety wiring"
      contains: "AV_MAX_DURATION_SECONDS"
  key_links:
    - from: "cmd/av-worker/main.go"
      to: "convert.Default.Register(convert.AVConverter{...})"
      via: "startup re-registration with env-derived MaxSourceDuration"
      pattern: "Default\\.Register\\(.*AVConverter"
    - from: "internal/convert/av.go (Convert)"
      to: "EnforceMinFreeDisk"
      via: "guard call after duration/resolution guards, before dispatch"
      pattern: "EnforceMinFreeDisk"
---

<objective>
Deliver the pure-Go backend surface of Phase 36: a new disk-space/ephemeral-storage guard (D-06) and the AVConverter config-threading refactor (D-09/Pitfall 4) that lets `AV_MAX_DURATION_SECONDS` flow from container env into the converter. No Docker, no measurement — everything here is testable with `go test` alone, and runs before any RTF checkpoint.

Purpose: The disk guard is the phase's net-new fail-closed DoS control for untrusted video decode; the struct-field refactor is the prerequisite that makes the NO-GO lever (`AV_MAX_DURATION_SECONDS`, chosen at the Plan 04 checkpoint) pluggable without editing package consts.
Output: `avdiskguard.go` (+test), an `AVConverter` that carries `MaxSourceDuration`/`MaxSourceResolutionHeight` fields, and `cmd/av-worker` wiring that re-registers a configured instance at startup.
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-containerization-rtf-measured-timeout/36-CONTEXT.md
@.planning/phases/36-containerization-rtf-measured-timeout/36-RESEARCH.md
@.planning/phases/36-containerization-rtf-measured-timeout/36-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase 2026-07-22; use directly, no exploration. -->

From internal/convert/av.go:
  type AVConverter struct{}                                  // av.go:45 — becomes a fielded struct in Task 2
  const avMaxSourceDuration = 4 * time.Hour                  // av.go:238
  const avMaxSourceResolutionHeight = 4320                   // av.go:246
  func (c AVConverter) Convert(ctx, inPath, outPath string, opts map[string]any) error  // av.go:397
    // guard block: avProbeSource → enforceMaxDurationOf(src.duration, avMaxSourceDuration)  (av.go:421)
    //                            → enforceMaxResolutionOf(src.videoStreams, avMaxSourceResolutionHeight) (av.go:424)
    //                            → switch targetFormat { ... }  (dispatch)
  func avThreadCount() int                                   // av.go:351 — already reads CgroupCPULimit()

From internal/convert/avduration.go (structural analog for the guard shape):
  var ErrAVResolutionExceeded = errors.New("declared video resolution exceeds configured maximum")  // :15
  func enforceMaxResolutionOf(streams []avVideoStream, maxHeight int) error                          // :189

From internal/convert/converters.go:
  func init() { ... Default.Register(AVConverter{}) }        // last-write-wins re-register is documented-safe

From internal/convert/cgroup.go:
  func CgroupCPULimit() (int, bool)                          // reused verbatim, no change

From cmd/av-worker/main.go (current):
  func envInt(key string, def int) int
  func envDuration(key string, def time.Duration) time.Duration
  func firstField(s string) string
  // worker.NewHandler(...) already constructed; ShutdownTimeout already = AV_ENGINE_TIMEOUT+10s (main.go:87)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EnforceMinFreeDisk fail-closed disk guard (net-new, D-06)</name>
  <files>internal/convert/avdiskguard.go, internal/convert/avdiskguard_test.go</files>
  <read_first>
    - internal/convert/avduration.go (the `EnforceMaxResolution`/`enforceMaxResolutionOf` sentinel+guard shape to mirror — 36-PATTERNS.md §avdiskguard.go names this the structural analog)
    - internal/convert/avduration_test.go (table-driven fail-closed test style to mirror)
    - 36-RESEARCH.md §"Code Examples / Disk-space guard" (worked recommended implementation)
  </read_first>
  <behavior>
    - free >= needed → nil (needed = uint64(inputSizeBytes * safetyFactor))
    - free < needed → wraps ErrAVInsufficientDiskSpace, message includes free/needed/input/factor
    - exact boundary free == needed → nil (not an error)
    - unix.Statfs error on a bogus dir path → returns a wrapped statfs error (not ErrAVInsufficientDiskSpace)
    - errors.Is(err, ErrAVInsufficientDiskSpace) is true on the insufficient-space path
  </behavior>
  <action>
    Create `internal/convert/avdiskguard.go` (package convert): declare `ErrAVInsufficientDiskSpace` sentinel mirroring `ErrAVResolutionExceeded`'s doc-comment shape, and `func EnforceMinFreeDisk(dir string, inputSizeBytes int64, safetyFactor float64) error` using `golang.org/x/sys/unix` — `unix.Statfs(dir, &stat)`, compute `free = stat.Bavail * uint64(stat.Bsize)`, `needed = uint64(float64(inputSizeBytes) * safetyFactor)`, fail-closed when `free < needed`. Do NOT hand-roll df/du or /proc/mounts (36-RESEARCH.md §Don't Hand-Roll). Add the doc comment explaining that reading REAL free space at guard time (not a per-job reservation) automatically reflects concurrent jobs on the same container filesystem, and that this is defense-in-depth, not a hard reservation (Assumption A2). Then run `go mod tidy` so `golang.org/x/sys` promotes from indirect to direct (already resolved at v0.44.0 in go.sum — no new version resolution, verified in 36-RESEARCH.md Package Legitimacy Audit). Write `avdiskguard_test.go` covering the five behaviors above using a real temp dir (`t.TempDir()`) for the success path and a non-existent path for the statfs-error path; do not assume a specific free-space number — derive `inputSizeBytes` from a tiny value so the success assertion is stable on any host.
  </action>
  <acceptance_criteria>
    - `internal/convert/avdiskguard.go` exists, imports `golang.org/x/sys/unix`, exports `EnforceMinFreeDisk` and `ErrAVInsufficientDiskSpace`.
    - `grep -c "unix.Statfs" internal/convert/avdiskguard.go` >= 1.
    - `go test ./internal/convert/ -run 'TestEnforceMinFreeDisk' -v` passes with >= 4 sub-cases including the exact-boundary and statfs-error paths.
    - `go mod tidy` leaves `golang.org/x/sys` as a direct require in go.mod; `go build ./...` succeeds.
    - No new module version resolution: `git diff go.sum` shows no `golang.org/x/sys` version change (only go.mod require-block promotion).
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/convert/ -run 'TestEnforceMinFreeDisk' && go vet ./internal/convert/</automated>
  </verify>
  <done>EnforceMinFreeDisk fail-closes below the sized threshold, passes at/above it, and the tests pin all five behaviors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AVConverter struct-field refactor + disk-guard call site (D-09/Pitfall 4)</name>
  <files>internal/convert/av.go, internal/convert/converters.go, internal/convert/av_test.go</files>
  <read_first>
    - internal/convert/av.go lines 45, 231-246 (struct + the two consts to convert to fields), 397-430 (Convert guard block + dispatch)
    - internal/convert/converters.go (the documented last-write-wins re-register semantics — leave the zero-value registration in init())
    - internal/api/api.go (the Config struct + zero-value-defaulting-in-constructor pattern, 36-PATTERNS.md's recommended analog over whisper.go's setter)
  </read_first>
  <behavior>
    - AVConverter{} (bare, zero-value) → Convert enforces 4h duration and 4320 height, identical to today
    - AVConverter{MaxSourceDuration: 5*time.Minute} → Convert rejects a 10-minute source with the duration sentinel
    - AVConverter{MaxSourceResolutionHeight: 720} → Convert rejects a 1080p source with ErrAVResolutionExceeded
    - Convert calls EnforceMinFreeDisk after the duration+resolution guards and before the dispatch switch
    - AVE-02: transcodeToMP4Args/transcodeToWebMArgs/streamCopyArgs/thumbnailArgs/ffprobe builders still carry -protocol_whitelist file,crypto + file: prefixes
  </behavior>
  <action>
    In `av.go`: add fields `MaxSourceDuration time.Duration` and `MaxSourceResolutionHeight int` to `AVConverter`. Introduce a receiver-level resolver (small helper or inline) so a zero-value field defaults to today's consts — keep `avMaxSourceDuration = 4 * time.Hour` / `avMaxSourceResolutionHeight = 4320` as the default constants and select `c.MaxSourceDuration`/`c.MaxSourceResolutionHeight` when non-zero, else the const (mirrors `api.NewServer`'s `if cfg.X == 0 { cfg.X = default }`). Update the two guard call sites (av.go:421, av.go:424) to pass the resolved values. Insert the disk-guard call immediately after the resolution guard and before the `switch targetFormat` dispatch: `os.Stat(inPath)` for size, then `EnforceMinFreeDisk(filepath.Dir(inPath), fi.Size(), avDiskSafetyFactor)` — declare `avDiskSafetyFactor` as a package const default `3.0` (Claude's Discretion per CONTEXT; flag `[ASSUMED]` in a comment, env-overridable is wired in Task 3). Preserve the exact guard ORDER and the existing `fmt.Errorf("av: %w", err)` wrapping. Do NOT touch the argv builders or the `-protocol_whitelist` flags. In `converters.go`: leave `init()`'s `Default.Register(AVConverter{})` as the zero-value fallback; update its comment to note the configured re-registration happens in cmd/av-worker/main.go. In `av_test.go`: add tests for the three field-defaulting behaviors above (bare struct = 4h/4320; configured struct enforces lower ceilings) using the existing live-binary fixture helpers where a real probe is needed, or unit-level enforce helpers where possible.
  </action>
  <acceptance_criteria>
    - `grep -c "MaxSourceDuration" internal/convert/av.go` >= 2 (field decl + resolve/use).
    - `grep -c "EnforceMinFreeDisk" internal/convert/av.go` == 1, positioned after `enforceMaxResolutionOf` and before the `switch targetFormat` dispatch (verify by line order).
    - AVE-02 non-regression: `grep -v '^\s*//' internal/convert/av.go | grep -c "protocol_whitelist"` is unchanged from HEAD (record the HEAD count first; the refactor must not drop any).
    - `go test ./internal/convert/` passes in full, including `TestAVBuildersHardenEveryInvocation`, `TestAVAudioPairDisjointness`, and the new field-defaulting tests.
    - A bare `AVConverter{}` constructed anywhere in existing tests still compiles and behaves at 4h/4320 (no existing test edited to change ceilings).
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/convert/ && go vet ./internal/convert/</automated>
  </verify>
  <done>AVConverter carries duration/resolution as zero-value-defaulting fields, the disk guard runs before dispatch, and AVE-02 + pair-disjointness are intact.</done>
</task>

<task type="auto">
  <name>Task 3: cmd/av-worker env wiring — envDurationSeconds, configured re-registration, disk-safety factor</name>
  <files>cmd/av-worker/main.go</files>
  <read_first>
    - cmd/av-worker/main.go (current state — has envInt/envDuration/firstField; missing envDurationSeconds)
    - cmd/audio-worker/main.go lines 198-228 (envDurationSeconds to clone verbatim) and 87-98 (the before-Start happens-before boundary + threads wiring)
    - internal/convert/av.go (the AVConverter fields from Task 2; avThreadCount already reads CgroupCPULimit)
  </read_first>
  <action>
    Clone `envDurationSeconds` VERBATIM from `cmd/audio-worker/main.go:198-228` into `cmd/av-worker/main.go` (bare-integer-seconds tolerant, negative-rejecting, warn-and-fallback-on-unparseable). BEFORE `srv.Start(mux)` (the happens-before boundary — all registration must complete before the consumer starts), re-register a configured converter: `convert.Default.Register(convert.AVConverter{ MaxSourceDuration: envDurationSeconds("AV_MAX_DURATION_SECONDS", 4*time.Hour), MaxSourceResolutionHeight: 4320 })`. This overrides the init() zero-value registration (documented-safe last-write-wins). Wire the disk-safety factor from env: read `AV_DISK_SAFETY_FACTOR` (float, default 3.0) via a small `envFloat` helper (or reuse existing parsing shape) and thread it to the `avDiskSafetyFactor` used in av.go — since av.go uses a package const, expose a package-level setter `convert.SetAVDiskSafetyFactor(f float64)` (whisper.go's SetAudioThreads shape) OR make `avDiskSafetyFactor` a package var settable from main; choose the setter approach to keep the value single-write-before-reads. Log the resolved values (`AV_MAX_DURATION_SECONDS`, `AV_DISK_SAFETY_FACTOR`, threads) with the 🧵/📊-style prefix used elsewhere. Do NOT change the existing `AV_ENGINE_TIMEOUT` reads or the ShutdownTimeout line (main.go:60,87) — those are correct; only the VALUE changes later in docker-compose (Plan 03/04), not this file. Leave `avThreadCount()` as-is (it already reads CgroupCPULimit); adding an `AV_THREADS` override env is optional and out of scope unless trivial.
  </action>
  <acceptance_criteria>
    - `grep -c "AV_MAX_DURATION_SECONDS" cmd/av-worker/main.go` >= 1 and it feeds a `convert.Default.Register(convert.AVConverter{` call before `srv.Start`.
    - `grep -c "AV_DISK_SAFETY_FACTOR" cmd/av-worker/main.go` >= 1, threaded into the disk-guard config (setter or var).
    - `envDurationSeconds` exists in cmd/av-worker/main.go and is byte-identical in behavior to the audio-worker version (bare-int-seconds + duration syntax + negative-reject + warn-fallback).
    - `go build ./cmd/av-worker` and `go vet ./cmd/av-worker` succeed.
    - The re-registration call is lexically BEFORE `srv.Start(mux)` (verify by line order).
  </acceptance_criteria>
  <verify>
    <automated>go build ./cmd/av-worker && go vet ./cmd/av-worker && go test ./internal/convert/</automated>
  </verify>
  <done>cmd/av-worker reads AV_MAX_DURATION_SECONDS + AV_DISK_SAFETY_FACTOR and registers a configured AVConverter before the consumer starts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| uploaded file bytes → av scratch filesystem | Untrusted video written to container ephemeral storage before transcode; a large/many-file burst can exhaust disk |
| operator env → AVConverter config | `AV_MAX_DURATION_SECONDS`/`AV_DISK_SAFETY_FACTOR` size the fail-closed envelope; must be read before the consumer serves jobs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | Denial of Service | AVConverter.Convert scratch disk | mitigate | `EnforceMinFreeDisk` fail-closes BEFORE the ffmpeg subprocess, sized proportional to probed input size — same guard-before-expensive-work discipline as EnforceMaxDuration/Resolution (av.go call site after both, before dispatch) |
| T-36-02 | Tampering | struct-field refactor of Convert | mitigate | Guard ORDER preserved (duration → resolution → disk → dispatch); AVE-02 protocol-whitelist grep-count non-regression asserted; argv builders untouched |
| T-36-03 | Tampering | startup re-registration overriding init() | mitigate | Re-register is documented last-write-wins on identical pair set; `TestAVAudioPairDisjointness` re-run confirms no pair drop/collision (T-34-02/T-35-24 non-regression) |
| T-36-SC | Tampering | golang.org/x/sys/unix promotion | mitigate | Already resolved at v0.44.0 in go.sum (indirect via pgx) — `[VERIFIED: go.sum]` in 36-RESEARCH.md Package Legitimacy Audit; `go mod tidy` only promotes to direct, no new version resolution (git diff go.sum shows no version change) |
</threat_model>

<verification>
- `go test ./internal/convert/ ./cmd/av-worker/ 2>&1 | tail` all pass.
- `go build ./... && go vet ./...` clean.
- AVE-02 protocol-whitelist grep-count in av.go unchanged vs HEAD (record before, compare after).
- `git diff go.sum` shows no golang.org/x/sys version change.
</verification>

<success_criteria>
- Disk guard fail-closes below threshold, passes at/above (5 behaviors pinned).
- AVConverter duration/resolution are zero-value-defaulting struct fields; bare `AVConverter{}` unchanged at 4h/4320.
- cmd/av-worker re-registers a configured instance from env before serving.
- No AVE-02 regression; pair-disjointness intact.
</success_criteria>

<output>
Create `.planning/phases/36-containerization-rtf-measured-timeout/36-01-SUMMARY.md` when done.
</output>
