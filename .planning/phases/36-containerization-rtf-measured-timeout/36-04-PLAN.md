---
phase: 36-containerization-rtf-measured-timeout
plan: 04
type: execute
wave: 3
depends_on: ["36-02", "36-03"]
files_modified:
  - docker-compose.yml
  - .env.example
  - internal/convert/av.go
  - internal/convert/av_test.go
autonomous: false
requirements: [AVE-04]

must_haves:
  truths:
    - "The RTF measurement is RUN by the operator at the Docker daemon (D-05 SUPERVISED) — OrbStack wedge risk + production-safety judgment"
    - "The RTF matrix measured BOTH VP9/webm and HEVC/mp4 worst-case cells; the true worst-case cell (not assumed HEVC) drives the derivation"
    - "AV_ENGINE_TIMEOUT is derived as ceil(AV_MAX_DURATION_SECONDS × p95_RTF_worstcell × 2.0), and it stays strictly under the 900s RECONCILER_ACTIVE_STALE_AFTER cap"
    - "If the derived timeout would breach 900s, the operator picks Path A (add VP9 tuning to transcodeToWebMArgs, preserving AVE-02) OR Path B (lower AV_MAX_DURATION_SECONDS) — the plan makes BOTH executable; the measurement selects; NEITHER is baked in up front"
    - "Path A (if chosen) does NOT disturb the -protocol_whitelist file,crypto flags; its argv change is pinned by tests in a commit SEPARATE from the timeout-derivation commit (Pitfall 2)"
    - "The finalized AV_ENGINE_TIMEOUT is written IDENTICALLY to all 8 queue-client services; AV_WORKER_CONCURRENCY/AV_MAX_DURATION_SECONDS/memory reflect the measured peak-RSS observation"
    - "A full live compose E2E (upload → poll → presigned download + signed webhook) passes against the running av-worker container"
  artifacts:
    - path: "docker-compose.yml"
      provides: "finalized measured AV_ENGINE_TIMEOUT/AV_WORKER_CONCURRENCY/AV_MAX_DURATION_SECONDS/memory across services"
    - path: ".env.example"
      provides: "finalized measured values with the Phase-32-style derivation comment"
  key_links:
    - from: "scripts/av-rtf-measure.sh output (p95 per cell)"
      to: "AV_ENGINE_TIMEOUT"
      via: "ceil(max_duration × p95_worstcell × 2.0) derivation, capped under 900s"
      pattern: "AV_ENGINE_TIMEOUT"
    - from: "operator go/no-go decision"
      to: "Path A (av.go) or Path B (AV_MAX_DURATION_SECONDS)"
      via: "supervised checkpoint selecting the NO-GO lever"
      pattern: "AV_MAX_DURATION_SECONDS|libvpx-vp9"
---

<objective>
The SUPERVISED go/no-go gate (D-05). The operator runs the RTF matrix, reads the p95 per cell, and — with Claude — derives `AV_ENGINE_TIMEOUT`, decides GO or applies a NO-GO lever (Path A speed-tuning vs Path B duration-lowering), then finalizes every measured value in compose/.env and proves a live E2E. This is the ONLY non-autonomous plan; everything in waves 1-2 is already built and runnable before it starts.

Purpose: Video RTF varies across the codec × resolution space (unlike audio's content-invariant RTF), and 35-RESEARCH.md's own data shows VP9 untuned at RTF≈1.41 — likely forcing a NO-GO lever. The measured number is a production-safety judgment, not an autonomous default.
Output: A finalized, RTF-derived `AV_ENGINE_TIMEOUT` (under the 900s cap) across all services, the chosen NO-GO lever applied and documented, and a green live compose E2E.
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-containerization-rtf-measured-timeout/36-CONTEXT.md
@.planning/phases/36-containerization-rtf-measured-timeout/36-RESEARCH.md
@.planning/phases/36-containerization-rtf-measured-timeout/36-PATTERNS.md

<interfaces>
<!-- Derivation formula + levers (Phase 32 precedent, 32-03-SUMMARY.md) and Path A anchors. -->

Phase 32 derivation (audio precedent, mirror exactly):
  AUDIO_ENGINE_TIMEOUT = ceil(AUDIO_MAX_DURATION_SECONDS × p95_RTF × 2.0 safety) = 742s
  NO-GO lever fired: AUDIO_MAX_DURATION_SECONDS lowered 14400→1800 so the derived timeout stayed under 900s.
  CAP = RECONCILER_ACTIVE_STALE_AFTER = 900s (15m). Never raise the cap; lower the duration ceiling.

Worked bound (36-RESEARCH.md Pitfall 1): p95 RTF 1.4 × 2.0 safety ⇒ max_duration < 900/2.8 ≈ 321s before NO-GO must fire.

Path A anchor (internal/convert/av.go:141 transcodeToWebMArgs):
  current argv: -c:v libvpx-vp9 -b:v 1M -c:a libopus  (NO -cpu-used/-deadline/-row-mt today)
  Path A adds -row-mt 1 / -cpu-used N / -deadline good, WITHOUT touching -nostdin -protocol_whitelist file,crypto or file: prefixes.
  Path A argv tests live in internal/convert/av_test.go (TestTranscodeToMP4Args + the webm assertions at :88-138).

Path B anchor: AV_MAX_DURATION_SECONDS (av.go const default 4h; env-wired in Plan 01; compose/.env in Plan 03).

E2E precedent: the Phase 35 07-plan live end-to-end checkpoint (upload video → poll status → presigned download; signed webhook confirmed).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Operator runs the RTF matrix (SUPERVISED, D-05)</name>
  <action>Checkpoint (no code change): pause for the operator to run scripts/av-rtf-measure.sh at the Docker daemon and report the per-cell p95 RTF results. Claude does not run this — it is operator-gated per D-05 (OrbStack wedge risk + production-safety judgment).</action>
  <read_first>
    - scripts/av-rtf-measure.sh (Plan 02 — the script to run)
    - .planning/milestones/v1.7-phases/32-containerization-local-e2e-rtf-gate/32-03-SUMMARY.md (the measurement-integrity-vs-go/no-go separation + raw methodology)
  </read_first>
  <what-built>
    Plan 02 built `Dockerfile.av-worker` (from-source ffmpeg n8.1.2) and `scripts/av-rtf-measure.sh` (VP9 + HEVC × 480/720/1080 sweep, lavfi fixtures, p95 nearest-rank, measurement-integrity-only exit code). Plan 03 wired the compose service + IN-02 parity with provisional values.
  </what-built>
  <how-to-verify>
    1. Ensure the OrbStack/Docker daemon is up and NO k8s cluster is live (the script self-checks `kubectl cluster-info`; stop k8s first — 4 confirmed OrbStack wedges on record when compose + k8s ran hot, D-05).
    2. Build sequentially with a non-`latest` tag (the script does this): `AV_RTF_IMAGE_TAG=octoconv-av-worker:rtf-measure ./scripts/av-rtf-measure.sh`.
    3. Confirm MEASUREMENT INTEGRITY: the build succeeded, the container started, every lavfi fixture synthesized, and every timed pipeline run exited 0 (the script exits non-zero if any of these failed — a non-zero exit here means re-run, not a NO-GO).
    4. Paste back the full `RESULT cell=<codec>@<resolution> ... p95_RTF=... peak_bytes=... image_size=...` lines for every cell — especially the `vp9@1080` and `hevc@1080` cells (D-09: VP9 is expected to dominate; confirm which cell is actually worst).
    5. Note the container `--cpus`/`--memory` the run used (must match the compose av-worker limits) and the peak_bytes (feeds the memory/concurrency decision).
  </how-to-verify>
  <acceptance_criteria>
    - The script exited 0 (measurement integrity held) OR the operator reports the specific integrity failure to fix and re-run.
    - RESULT lines for all measured cells are captured, including both vp9@ and hevc@ at the top resolution.
    - The worst-case cell (highest p95_RTF) is identified from the data, not assumed.
    - peak_bytes for the worst cell is recorded (memory-sizing input).
  </acceptance_criteria>
  <resume-signal>Paste the RESULT lines (all cells) + the container cpus/memory used, then type "measured".</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Derive AV_ENGINE_TIMEOUT + go/no-go + Path A/B selection</name>
  <action>Checkpoint (no code change): compute the Phase-32 derivation from the measured worst-cell p95, present GO vs Path A vs Path B with exact arithmetic, and pause for the operator's selection. The chosen values feed Task 3.</action>
  <read_first>
    - The RESULT lines from Task 1
    - 36-RESEARCH.md §Common Pitfalls Pitfall 1 (the 900/2.8≈321s worked bound) and Pitfall 2 (Path A must be a separate decision, not a silent patch)
    - .env.example AUDIO_MAX_DURATION_SECONDS:54 (the documented NO-GO-lever precedent wording)
  </read_first>
  <decision>
    Given the measured worst-case p95 RTF, is the Phase-32 derivation `ceil(AV_MAX_DURATION_SECONDS × p95_worstcell × 2.0)` under the 900s cap at the current AV_MAX_DURATION_SECONDS? If not, which NO-GO lever applies?
  </decision>
  <context>
    Compute the derived timeout from the worst cell. If it is comfortably < 900s at a defensible AV_MAX_DURATION_SECONDS, that is a GO — accept the number. If it breaches 900s, a lever must fire (do NOT raise the cap, do NOT inflate the timeout toward 900s). Present the operator with the exact computed numbers for each option.
  </context>
  <options>
    <option id="option-go">
      <name>GO — accept derived timeout</name>
      <pros>No code change, no capacity reduction; matches audio's clean path if RTF is low enough</pros>
      <cons>Only valid if ceil(max_duration × p95 × 2.0) < 900s at a defensible duration ceiling</cons>
    </option>
    <option id="option-path-a">
      <name>Path A — VP9 speed tuning (capability preserved, quality tradeoff)</name>
      <pros>Keeps a larger AV_MAX_DURATION_SECONDS; lowers VP9 RTF via -row-mt 1/-cpu-used/-deadline good</pros>
      <cons>Changes mp4→webm output quality contract for every client — a product decision (Pitfall 2); needs its own tested commit separate from the timeout derivation; must preserve AVE-02 flags</cons>
    </option>
    <option id="option-path-b">
      <name>Path B — lower AV_MAX_DURATION_SECONDS (capacity lever, Phase 32 verbatim)</name>
      <pros>No quality change; identical to audio's proven NO-GO lever; keeps timeout under the cap</pros>
      <cons>Rejects longer videos at the duration guard; the accepted-residual ceiling shrinks (document it like AUDIO_MAX_DURATION_SECONDS)</cons>
    </option>
  </options>
  <acceptance_criteria>
    - The derived timeout is computed and shown with the exact formula and worst-cell p95.
    - The selected option keeps AV_ENGINE_TIMEOUT strictly < 900s (verify the arithmetic).
    - If Path A: the operator explicitly signs off on the quality/speed tradeoff (Pitfall 2), and the plan will apply it in a SEPARATE commit from the value finalization.
    - If Path B: the new AV_MAX_DURATION_SECONDS is recorded with its derivation, mirroring AUDIO_MAX_DURATION_SECONDS's documented lever.
    - AV_WORKER_CONCURRENCY + av-worker memory limit are decided from the measured peak_bytes (mirror Phase 32's measured-not-assumed provenance).
  </acceptance_criteria>
  <resume-signal>Select: option-go, option-path-a, or option-path-b — with the final AV_ENGINE_TIMEOUT, AV_MAX_DURATION_SECONDS, AV_WORKER_CONCURRENCY, and memory values.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Apply the chosen path + finalize all measured values + live E2E</name>
  <files>internal/convert/av.go, internal/convert/av_test.go, docker-compose.yml, .env.example</files>
  <read_first>
    - internal/convert/av.go:141 transcodeToWebMArgs + av_test.go webm argv assertions (:88-138) — only touched if Path A
    - docker-compose.yml av-worker service + all 8 AV_ENGINE_TIMEOUT sites (Plan 03) and .env.example AV_* block
    - 32-03-SUMMARY.md (the exact derivation-comment wording style to mirror in .env.example)
  </read_first>
  <action>
    IF Path A was selected: in a FIRST, SEPARATE commit, add the VP9 tuning flags (`-row-mt 1`, `-cpu-used N`, `-deadline good`, using the operator-approved values) to `transcodeToWebMArgs` (av.go:141), inserting them WITHOUT disturbing `-nostdin -protocol_whitelist file,crypto` or the `file:` input/output prefixes; update the webm argv-pinning assertions in `av_test.go` to expect the new flags AND re-assert the AVE-02 flags survive; run `go test ./internal/convert/`. This commit must NOT touch any timeout/env value (Pitfall 2 separation).
    THEN (all paths, a SECOND commit): write the finalized `AV_ENGINE_TIMEOUT` value IDENTICALLY into all 8 queue-client services in docker-compose.yml (grep-count parity must hold), update the av-worker `AV_WORKER_CONCURRENCY`, `AV_MAX_DURATION_SECONDS` (Path B value if chosen, else the current ceiling), `deploy.resources.limits.memory`, and `stop_grace_period` (= new AV_ENGINE_TIMEOUT + 10s + margin). Mirror the same finalized values + a Phase-32-style derivation comment (formula, p95, worst cell, N, cpus/memory, margin under the 900s cap) into `.env.example` for AV_ENGINE_TIMEOUT / AV_WORKER_CONCURRENCY / AV_MAX_DURATION_SECONDS. Confirm AV_ENGINE_TIMEOUT stays strictly under RECONCILER_ACTIVE_STALE_AFTER (900s) and that the cap is unchanged.
    THEN prove a live compose E2E (mirror the Phase 35 07-plan checkpoint): `docker compose up -d` (av-worker + deps, k8s down), upload a real video job through the API, poll to `done`, download the presigned result, and confirm a signed webhook fired. Capture the job id + terminal status + a checksum/size of the output as evidence.
  </action>
  <acceptance_criteria>
    - If Path A: `grep -c "row-mt\|cpu-used\|deadline" internal/convert/av.go` >= 1 in transcodeToWebMArgs; `grep -v '^\s*//' internal/convert/av.go | grep -c "protocol_whitelist"` unchanged vs HEAD (AVE-02 intact); `go test ./internal/convert/` passes including the updated webm argv tests; the argv change is in a commit that touches NO env/compose file.
    - `grep -c "AV_ENGINE_TIMEOUT:" docker-compose.yml` == 8 and `sort -u` of the extracted values yields exactly one line (parity preserved after finalization).
    - The finalized AV_ENGINE_TIMEOUT (seconds) < 900; RECONCILER_ACTIVE_STALE_AFTER unchanged.
    - av-worker stop_grace_period > AV_ENGINE_TIMEOUT + 10.
    - `.env.example` AV_ENGINE_TIMEOUT carries a derivation comment (formula + p95 + worst cell + margin), mirroring AUDIO_ENGINE_TIMEOUT:51.
    - `docker compose config >/dev/null` validates.
    - Live E2E evidence captured: a video job reached `done`, the presigned download succeeded, and a signed webhook was confirmed (job id + status + output size recorded in the SUMMARY).
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/convert/ && docker compose config >/dev/null && test "$(grep -c 'AV_ENGINE_TIMEOUT:' docker-compose.yml)" = "8" && test "$(grep 'AV_ENGINE_TIMEOUT:' docker-compose.yml | sort -u | wc -l | tr -d ' ')" = "1"</automated>
  </verify>
  <done>The RTF-derived AV_ENGINE_TIMEOUT is finalized under the 900s cap across all services, the chosen NO-GO lever is applied and documented, and a live compose E2E is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| measured RTF numbers → production timeout config | An accepted number that is wrong (too low) aborts long transcodes mid-flight; a supervised judgment, not an autonomous default (D-05) |
| Path A argv change → ffmpeg invocation | Editing transcodeToWebMArgs risks dropping the AVE-02 protocol-whitelist flags |
| derived timeout → 900s reconciler cap | Must stay under the cap or the reconciler re-enqueues active jobs, duplicating work |
| live E2E upload → av-worker container | First real untrusted video through the fully containerized pipeline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-10 | Denial of Service | derived AV_ENGINE_TIMEOUT accepted without judgment | mitigate | D-05 SUPERVISED checkpoint: operator runs the measurement and signs off on the number; derivation shown with the exact formula; timeout kept strictly under the 900s cap |
| T-36-11 | Tampering | Path A editing transcodeToWebMArgs regresses AVE-02 | mitigate | Path A applied in a separate commit; webm argv tests re-assert `-protocol_whitelist file,crypto` + `file:` prefixes survive; grep-count non-regression of protocol_whitelist in av.go |
| T-36-12 | Denial of Service | timeout inflated toward/over 900s instead of lowering the ceiling | mitigate | NO-GO lever discipline (Phase 32 verbatim): lower AV_MAX_DURATION_SECONDS, never raise RECONCILER_ACTIVE_STALE_AFTER; arithmetic-verified < 900s |
| T-36-13 | Tampering | env divergence introduced during finalization | mitigate | Post-finalization parity re-checked: AV_ENGINE_TIMEOUT grep-count == 8 and single unique value across all services |
| T-36-SC | Tampering | supply chain (measurement run) | accept | No package installs in this plan; the RTF run consumes the already-VERIFIED Plan 02 image; ffmpeg pin advisory-tracking remains the documented operational follow-up (D-02, not this phase) |
</threat_model>

<verification>
- Measurement integrity confirmed by the operator (script exit 0, all cells).
- Derived AV_ENGINE_TIMEOUT < 900s; arithmetic shown; cap unchanged.
- Path A (if chosen): AVE-02 flags intact, argv tests pass, separate commit.
- AV_ENGINE_TIMEOUT parity == 8 services, single value.
- Live compose E2E green (job done + presigned download + signed webhook).
</verification>

<success_criteria>
- AV_ENGINE_TIMEOUT derived from the measured worst-case cell (VP9 vs HEVC decided by data), under the 900s cap.
- The chosen NO-GO lever (GO / Path A / Path B) applied and documented like Phase 32.
- All measured values finalized identically across services; memory/concurrency from peak-RSS.
- A real video converts end-to-end through the live containerized av-worker.
</success_criteria>

<output>
Create `.planning/phases/36-containerization-rtf-measured-timeout/36-04-SUMMARY.md` when done.
</output>
