---
phase: quick-260724-26g
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true
requirements: [DOC-README-01]
must_haves:
  truths:
    - "A reader sees all five engine classes (image/document/html/audio/video) presented as shipped, not one shipped + one in-progress"
    - "The Roadmap Shipped list covers v1.0 through v1.8 and contains no '🚧 In progress — v1.2' block"
    - "The cmd/ tree lists the real 12 binaries including av-worker, audio-worker, chromium-worker, document-worker, webhook-worker, mcp-server, mcp-http, manage-presets"
    - "The Configuration table includes the per-engine timeout/concurrency/retry vars plus audio/av duration ceilings and reconciler vars actually present in .env.example"
    - "No stale framing survives: 'landing next', 'young project', the Office-🚧 block, and the v1.2-in-progress block are all gone"
    - "Every factual claim (version, service, route, env var, binary) is verifiable against the read source files"
  artifacts:
    - path: "README.md"
      provides: "Actualized project README reflecting v1.0–v1.8 shipped state"
      contains: "whisper.cpp"
  key_links:
    - from: "README.md Roadmap Shipped"
      to: ".planning/MILESTONES.md"
      via: "milestone wording mirrors MILESTONES.md v1.0–v1.8"
      pattern: "v1\\.8"
---

<objective>
Actualize README.md, which is frozen at ~v1.2 ("Document Engine in progress") while eight milestones and five engine classes have shipped (v1.0–v1.8). Bring every stale section into line with the real, verifiable codebase state.

Purpose: The README is the project's front door and is now materially wrong — it claims LibreOffice is "landing next" and describes a 4-binary system when the shipped system has 12 binaries, 5 engine classes, and full k8s/KEDA/MCP support. Anyone reading it is misled about what OctoConv actually does.

Output: An updated README.md (docs-only; no source or behavior changes).
</objective>

<execution_context>
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/apaderin/dev/octoconv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@README.md
@.planning/MILESTONES.md

<verified_facts>
<!-- All facts below were extracted directly from source during planning. The -->
<!-- executor MUST NOT invent versions, service names, routes, env vars, or     -->
<!-- binaries beyond this list. When in doubt, under-claim. Re-open the source  -->
<!-- files (paths given) only if a detail here is insufficient.                 -->

FIVE ENGINE CLASSES (internal/convert/convert.go EngineImage/Document/HTML/Audio/AV; internal/convert/converters.go registers LibvipsConverter, LibreOfficeConverter, ChromiumConverter, AudioConverter, AVConverter):
- image    -> libvips           — png/jpg/webp/heic/tiff, any-to-any
- document -> LibreOffice headless — docx/xlsx/pptx/odt/ods/odp -> pdf, PLUS 6 symmetric cross pairs (docx<->odt, xlsx<->ods, pptx<->odp)
- html     -> headless Chromium  — html -> pdf
- audio    -> whisper.cpp (ffmpeg-normalize -> whisper-cli) — transcription of mp3/wav/m4a/ogg (and video containers mp4/mov/avi/mkv/webm) -> txt/srt/vtt/json
- video    -> ffmpeg (av class)  — mov/avi/mkv/webm -> mp4; mp4 -> webm; audio-extract -> mp3/wav/m4a; thumbnail -> jpg/png/webp

REAL cmd/ BINARIES (12, verified `ls cmd/`): api, worker (image engine), audio-worker, av-worker, chromium-worker, document-worker, webhook-worker, mcp-server, mcp-http, manage-clients, manage-presets, migrate

internal/ PACKAGES (verified `ls internal/`): api, auth, clients, convert, db, e2e (test-only), jobs, mcpserver, metrics, presets, queue, ratelimit, reconciler, storage, webhook, worker
  (README's current internal/ list is MISSING: presets, mcpserver. Add those. e2e is test-only — omit or note as such.)

REAL /v1 ROUTES (internal/api/routes.go): /healthz; POST /v1/jobs; GET /v1/jobs/{id}; GET /v1/formats; /v1/presets CRUD (POST, GET, GET/{name}, PUT/{name}, DELETE/{name}); /v1/system/presets CRUD (operator-gated). — API Semantics section stays UNCHANGED.

REAL docker-compose SERVICES (verified): postgres, redis, minio, createbucket, api, worker, webhook-worker-1, webhook-worker-2, document-worker, chromium-worker, audio-worker, av-worker, asynqmon

REAL .env.example VARS to ADD to config table (verified from .env.example):
- Per-engine: WORKER_CONCURRENCY/ENGINE_TIMEOUT/IMAGE_MAX_RETRY (image); DOCUMENT_WORKER_CONCURRENCY/DOCUMENT_ENGINE_TIMEOUT/DOCUMENT_MAX_RETRY + VERAPDF_TIMEOUT; HTML_WORKER_CONCURRENCY/HTML_ENGINE_TIMEOUT/HTML_MAX_RETRY; AUDIO_ENGINE_TIMEOUT/AUDIO_MAX_RETRY/AUDIO_WORKER_CONCURRENCY/AUDIO_MAX_DURATION_SECONDS/AUDIO_MODEL_PATH/AUDIO_THREADS; AV_ENGINE_TIMEOUT/AV_MAX_RETRY/AV_WORKER_CONCURRENCY/AV_MAX_DURATION_SECONDS/AV_DISK_SAFETY_FACTOR
- Webhook worker: WEBHOOK_WORKER_CONCURRENCY, WEBHOOK_PRESIGN_TTL, WEBHOOK_ALLOW_INSECURE_HTTP (WEBHOOK_SIGNING_SECRET / WEBHOOK_ALLOW_PRIVATE_IPS already listed)
- Operator/presets: OPERATOR_CLIENT_IDS
- Reconciler: RECONCILER_QUEUED_STALE_AFTER, RECONCILER_ACTIVE_STALE_AFTER, RECONCILER_SWEEP_INTERVAL, RECONCILER_MAX_RECOVERIES (README currently collapses these as `RECONCILER_*` — acceptable to keep collapsed OR expand; do not invent new ones)
- Note: MAX_UPLOAD_BYTES is now 2 GiB (2147483648) with a second per-engine post-detection ceiling holding non-video classes at ~100 MiB — do not overstate; the existing "upload size ceiling" row remains accurate as-is.

MILESTONE WORDING SOURCE (.planning/MILESTONES.md — mirror its concise style):
- v1.0 Hardening MVP; v1.1 Tech Debt Cleanup; v1.2 Document Engine Class (docx/xlsx/pptx/odt/ods/odp -> pdf via LibreOffice); v1.3 Document Class v2 (doc<->doc cross-format, OLE-CFB rejection, HTML/Chromium engine class, webhook-worker decoupling); v1.4 CI, Presets & Debt Cleanup (first CI workflow, named presets, GET /v1/formats); v1.5 MCP Access & Document Fidelity (MCP stdio server, veraPDF ISO-19005 PDF/A validation, CFB encrypted-vs-legacy classification); v1.6 Kubernetes & KEDA (Helm chart, KEDA autoscaling, MCP-over-HTTP, operator system-presets); v1.7 Audio Engine & Hardening (whisper.cpp transcription); v1.8 AV Engine (video/ffmpeg)
- CAUTION — do NOT list as "Planned" anything already shipped: cross-format document conversions shipped in v1.3; Kubernetes+KEDA shipped in v1.6; audio/video engines shipped in v1.7/v1.8. Genuinely-future "Planned" items only (e.g. CI k8s validation K8SV2-01, is_operator column K8SV2-03, trim/crop opts AVX-01 — all deferred-to-v2 per MILESTONES.md/STATE.md).

KEEP UNCHANGED (verify still accurate, do NOT rewrite): API Semantics, Quick Start flow steps (migrations/services/issue-key/convert), auth/key-rotation, SSRF guard wording, reconciler description, both MCP sections (stdio + HTTP), Frontend, License, badges.
</verified_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Actualize product framing — intro, engines, architecture tree, compose table</name>
  <files>README.md</files>
  <action>
Update the upper README to reflect five shipped engine classes. Make four edits:

1. Intro (README lines ~5-10): Replace the parenthetical "(currently libvips for images, with LibreOffice for office documents landing next)" so it states OctoConv ships five engine classes — images (libvips), office documents (LibreOffice headless), HTML (headless Chromium), audio transcription (whisper.cpp), and video (ffmpeg). Keep the surrounding sentence structure and the existing PostgreSQL/Redis-asynq framing.

2. Features -> "Conversion engines" (README lines ~83-90): Replace the two-item list (Images ✅ + Office 🚧) with all five shipped classes, each marked shipped ✅, using ONLY the format facts in verified_facts (Images png/jpg/webp/heic/tiff any-to-any via libvips; Office docx/xlsx/pptx/odt/ods/odp -> pdf plus docx<->odt / xlsx<->ods / pptx<->odp via LibreOffice headless; HTML -> pdf via headless Chromium; Audio transcription mp3/wav/m4a/ogg and video containers -> txt/srt/vtt/json via whisper.cpp; Video mov/avi/mkv/webm -> mp4, mp4 -> webm, audio-extract to mp3/wav/m4a, thumbnail to jpg/png/webp via ffmpeg). Also fold these shipped capabilities into the most fitting existing Features subsection (do not invent a new top-level structure): named conversion presets with a GET /v1/formats capability-discovery endpoint; PDF/A export with veraPDF ISO-19005 validation; CFB encrypted-vs-legacy Office classification; Kubernetes deployment via a Helm chart with KEDA queue-depth autoscaling. Keep each bullet terse; every claim must trace to verified_facts.

3. Architecture cmd/ + internal/ tree (README lines ~110-127): Replace the 4-binary cmd/ block with the real 12 binaries from verified_facts (api; worker = image engine; audio-worker; av-worker; chromium-worker; document-worker; webhook-worker; mcp-server; mcp-http; manage-clients; manage-presets; migrate), each with a one-line role. Update the external-engine label in the ASCII data-flow diagram (lines ~99-100) from "(libvips, LibreOffice, …)" to reflect all five engines (libvips / LibreOffice / Chromium / whisper.cpp / ffmpeg) if it reads cleanly. Add the two missing internal/ packages: presets (named-preset resolution) and mcpserver (MCP tool implementations). Keep the tree representative and readable.

4. Quick Start compose service table (README lines ~140-146): The full stack is 13 services — keep the table readable as a REPRESENTATIVE table, not an exhaustive dump. Keep postgres/redis/minio/api rows. Replace the single generic "worker" row with: the image `worker`; the per-engine workers (document-worker, chromium-worker, audio-worker, av-worker) collapsed into one representative row noting one worker per engine class; the two `webhook-worker` replicas; and the `asynqmon` dashboard. Add a one-line note that engine workers are one-per-class and scale independently. Do NOT alter the presigned-URL caveat note beneath the table, nor the Quick Start step flow.

Do not touch API Semantics, MCP sections, Frontend, License, or badges. Preserve markdown formatting and keep all code fences balanced.
  </action>
  <verify>
    <automated>cd /Users/apaderin/dev/octoconv && test $(($(grep -c '```' README.md) % 2)) -eq 0 && ! grep -q "landing next" README.md && grep -q "whisper.cpp" README.md && grep -q "av-worker" README.md && grep -q "chromium-worker" README.md && grep -q "manage-presets" README.md && grep -qi "ffmpeg" README.md && grep -qi "veraPDF" README.md && echo PASS1</automated>
  </verify>
  <done>Intro names five engines; Conversion engines list shows all five as shipped plus preset/PDF-A/CFB/k8s capabilities; cmd/ tree lists 12 binaries; internal/ list includes presets and mcpserver; compose table is a readable representative table covering per-engine + webhook + asynqmon services. All grep assertions pass, code fences balanced.</done>
</task>

<task type="auto">
  <name>Task 2: Actualize Configuration table, Roadmap, and Contributing</name>
  <files>README.md</files>
  <action>
Update the lower README so config, roadmap, and framing match shipped state. Make three edits:

1. Configuration table (README lines ~219-230): Extend the existing highlights table with the per-engine and reconciler vars from verified_facts. Add rows (grouped for readability) for: the image worker vars (`WORKER_CONCURRENCY`, `ENGINE_TIMEOUT`, `IMAGE_MAX_RETRY`); the document engine (`DOCUMENT_WORKER_CONCURRENCY`, `DOCUMENT_ENGINE_TIMEOUT`, `DOCUMENT_MAX_RETRY`, `VERAPDF_TIMEOUT`); the HTML engine (`HTML_WORKER_CONCURRENCY`, `HTML_ENGINE_TIMEOUT`, `HTML_MAX_RETRY`); the audio engine (`AUDIO_ENGINE_TIMEOUT`, `AUDIO_MAX_RETRY`, `AUDIO_WORKER_CONCURRENCY`, `AUDIO_MAX_DURATION_SECONDS`, `AUDIO_MODEL_PATH`, `AUDIO_THREADS`); the video/av engine (`AV_ENGINE_TIMEOUT`, `AV_MAX_RETRY`, `AV_WORKER_CONCURRENCY`, `AV_MAX_DURATION_SECONDS`, `AV_DISK_SAFETY_FACTOR`); plus `OPERATOR_CLIENT_IDS` (operator allowlist for /v1/system/presets), `WEBHOOK_WORKER_CONCURRENCY`, `WEBHOOK_PRESIGN_TTL`, and `WEBHOOK_ALLOW_INSECURE_HTTP`. Use compact one-line purposes drawn from the .env.example comments — do NOT invent numeric defaults beyond what .env.example states, and prefer grouping several related vars into a single "e.g. per engine" row where that keeps the table readable. Keep the pointer to .env.example for the full list. Leave the existing DATABASE_URL/REDIS_ADDR/S3_*, API_KEY_SALT, MAX_UPLOAD_BYTES, MAX_IMAGE_PIXELS, MAX_DOCUMENT_UNCOMPRESSED_BYTES, rate-limit, webhook-secret, RECONCILER_*, and METRICS_ADDR rows intact.

2. Roadmap section (README lines ~327-360): This is the most stale block. Rewrite it:
   - Under "✅ Shipped", replace the two v1.0/v1.1 bullets with a concise list of ALL milestones v1.0 through v1.8, mirroring the wording style of .planning/MILESTONES.md (one line each, using the milestone names/scope in verified_facts). v1.2 Document Engine is SHIPPED — it moves into this list.
   - DELETE the entire "🚧 In progress — v1.2: Document Engine Class" block (lines ~341-352) including its sub-bullets.
   - Rewrite "🔭 Planned" to contain ONLY genuinely-future items. Remove anything already shipped (cross-format document conversion, Kubernetes+KEDA, audio/video engines are all shipped). Acceptable forward-looking items per MILESTONES.md/STATE.md deferred lists: k8s validation in CI (K8SV2-01), is_operator DB column vs env allowlist (K8SV2-03), trim/crop as validated closed-opts for video (AVX-01), and dependency-advisory tracking for pinned engine binaries. If unsure an item is truly unshipped, omit it (under-claim).

3. Contributing section (README lines ~361-364): Remove the "This is a young project — expect the architecture to still be settling in places outside the hardened v1.0/v1.1 core" sentence. Replace with a brief, accurate note that OctoConv has shipped eight audited milestones (v1.0–v1.8) and that issues/PRs are welcome. Do not overclaim stability beyond "shipped and audited".

Do not touch the reconciler description, MCP sections, Frontend, License, or badges. Preserve markdown; keep code fences balanced.
  </action>
  <verify>
    <automated>cd /Users/apaderin/dev/octoconv && test $(($(grep -c '```' README.md) % 2)) -eq 0 && ! grep -q "In progress — v1.2" README.md && ! grep -q "young project" README.md && grep -q "v1.8" README.md && grep -q "AV_ENGINE_TIMEOUT" README.md && grep -q "AUDIO_MAX_DURATION_SECONDS" README.md && grep -q "OPERATOR_CLIENT_IDS" README.md && echo PASS2</automated>
  </verify>
  <done>Config table lists the per-engine (image/document/html/audio/av), operator, and webhook-worker vars with .env.example-sourced purposes; Roadmap Shipped lists v1.0–v1.8 with no in-progress-v1.2 block; Planned contains only unshipped items; Contributing drops the "young project" framing. All grep assertions pass, code fences balanced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | Docs-only edit to README.md — no code paths, no runtime inputs, no dependency installs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26g-01 | Information Disclosure | README.md public docs | mitigate | Every claim constrained to verified_facts; no secrets, endpoints, or internal-only config values beyond what .env.example already documents publicly |
| T-26g-02 | Tampering | README factual accuracy | mitigate | Grep verification gates assert stale/over-claiming strings are removed and shipped-state markers present; under-claim rule prevents inventing capabilities |
| T-26g-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — docs-only edit; supply-chain surface unchanged |
</threat_model>

<verification>
- `grep -c '```' README.md` is even (all code fences balanced) after both tasks.
- No stale strings remain: `landing next`, `young project`, `In progress — v1.2`.
- Shipped-state markers present: `whisper.cpp`, `ffmpeg`, `av-worker`, `manage-presets`, `veraPDF`, `v1.8`, `AV_ENGINE_TIMEOUT`, `AUDIO_MAX_DURATION_SECONDS`, `OPERATOR_CLIENT_IDS`.
- Manual spot-check: no section in the KEEP-UNCHANGED list (API Semantics, MCP stdio/HTTP, Frontend, License, badges, reconciler description) was rewritten.
</verification>

<success_criteria>
README.md accurately reflects the shipped v1.0–v1.8 state: five engine classes shown as shipped, 12 real binaries in the cmd/ tree, real per-engine/reconciler config vars, a Roadmap Shipped list spanning v1.0–v1.8 with no in-progress-v1.2 block and only genuinely-future Planned items, and no "young project" framing. Every factual claim is traceable to the read source files; nothing is invented.
</success_criteria>

<output>
Create `.planning/quick/260724-26g-actualize-readme-md-to-reflect-v1-2-v1-8/260724-26g-SUMMARY.md` when done
</output>
