---
phase: quick-260724-26g
plan: 01
subsystem: docs
tags: [readme, documentation, milestones]

requires: []
provides:
  - README.md accurately reflecting v1.0-v1.8 shipped state
affects: []

tech-stack:
  added: []
  patterns: []

key-files:
  created: []
  modified: [README.md]

key-decisions:
  - "Kept Roadmap Planned section limited to genuinely-future deferred items only (K8SV2-01, K8SV2-03, AVX-01, dependency-advisory tracking), per the under-claim rule"
  - "Left RECONCILER_* collapsed in the Configuration table rather than expanding to 4 individual vars, matching the plan's 'acceptable to keep collapsed' guidance"
  - "Left the existing MAX_UPLOAD_BYTES row unchanged per plan guidance not to overstate the 2 GiB/100 MiB two-tier ceiling nuance"

requirements-completed: [DOC-README-01]

duration: 15min
completed: 2026-07-23
---

# Quick Task 260724-26g: Actualize README.md to Shipped v1.0-v1.8 State Summary

**README.md rewritten from a frozen ~v1.2 snapshot (4 binaries, 2 engine classes, one "in progress") to the real shipped v1.0-v1.8 state: 12 binaries, 5 shipped engine classes (image/document/html/audio/video), full per-engine config surface, and an accurate Roadmap/Contributing framing.**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-07-23T22:20:00Z (approx)
- **Completed:** 2026-07-23T22:43:40Z
- **Tasks:** 2 completed
- **Files modified:** 1 (README.md)

## Accomplishments
- Intro and Features "Conversion engines" list now present all five engine classes (image, document, html, audio, video) as shipped (✅), each with format facts drawn only from `internal/convert/converters.go` / verified_facts — the stale "LibreOffice landing next" framing is gone.
- Architecture `cmd/` tree now lists all 12 real binaries (`api`, `worker`, `document-worker`, `chromium-worker`, `audio-worker`, `av-worker`, `webhook-worker`, `mcp-server`, `mcp-http`, `manage-clients`, `manage-presets`, `migrate`) with one-line roles; `internal/` tree gained the missing `presets` and `mcpserver` packages.
- Quick Start compose table rebuilt as a representative (not exhaustive) table covering postgres/redis/minio/api/worker, a collapsed per-engine-worker row, both webhook-worker replicas, and asynqmon — with a one-line note that engine workers scale independently per class. The presigned-URL caveat and Quick Start step flow were left untouched.
- Configuration table extended with all per-engine (image/document/html/audio/av), `OPERATOR_CLIENT_IDS`, and webhook-worker vars sourced directly from `.env.example` comments — no invented defaults.
- Roadmap Shipped list now spans v1.0 through v1.8, mirroring `.planning/MILESTONES.md`'s concise style; the stale "🚧 In progress — v1.2" block is fully removed (v1.2 moved into Shipped); Planned trimmed to only the genuinely-future items confirmed in STATE.md's Deferred Items (K8SV2-01, K8SV2-03, AVX-01, dependency-advisory tracking) — cross-format docs, Kubernetes/KEDA, and audio/video are no longer misrepresented as future work.
- Contributing section's "young project" framing replaced with an accurate "eight audited milestones (v1.0-v1.8)" note.

## Task Commits

1. **Task 1: Actualize product framing — intro, engines, architecture tree, compose table** - `294d0e1` (docs)
2. **Task 2: Actualize Configuration table, Roadmap, and Contributing** - `50bade0` (docs)

_Both commits touch only README.md; no source or behavior changes._

## Files Created/Modified
- `README.md` - Intro, Features (Conversion engines + folded-in presets/PDF-A/CFB/k8s bullets), Architecture (ASCII diagram + cmd/internal trees), Quick Start compose table, Configuration table, Roadmap, and Contributing sections actualized to the shipped v1.0-v1.8 state.

## Decisions Made
- Folded named-presets/`GET /v1/formats`, PDF/A+veraPDF+CFB classification, and Kubernetes+KEDA deployment into the existing "Core pipeline" and "Security & reliability" Features subsections rather than inventing a new top-level structure, per plan instruction.
- Kept `RECONCILER_*` and `MAX_UPLOAD_BYTES` config rows exactly as they were (plan explicitly allowed collapsing/leaving these as-is).
- Roadmap Planned section limited strictly to items confirmed as still-open in STATE.md's Deferred Items table (K8SV2-01, K8SV2-03, AVX-01) plus the ffmpeg/whisper.cpp/LibreOffice/veraPDF pinned-binary advisory-tracking gap noted in STATE.md's Phase 36 decision log — under-claimed rather than guessed at any other roadmap item.

## Deviations from Plan

None - plan executed exactly as written. Every factual claim (versions, binaries, routes, env vars, format pairs) was verified against `internal/convert/converters.go`, `internal/convert/convert.go` (via converters.go registration), `internal/api/routes.go`, `docker-compose.yml`, `.env.example`, `.planning/MILESTONES.md`, and `.planning/STATE.md` before being written, with a fresh `ls cmd/` / `ls internal/` re-verification of the binary and package lists.

## Issues Encountered

None.

## User Setup Required

None - docs-only change, no external service configuration required.

## Next Phase Readiness

README.md is now an accurate front door for the project reflecting the real v1.0-v1.8 shipped state. No blockers. This is a standalone quick task with no downstream phase dependency.

---
*Phase: quick-260724-26g*
*Completed: 2026-07-23*
