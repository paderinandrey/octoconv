---
milestone: v1.2
audited: 2026-07-10T02:40:00Z
status: passed
scores:
  requirements: 10/10
  phases: 4/4
  integration: 10/10
  flows: 1/1
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 11-api-routing-end-to-end-document-conversion
    items:
      - "WR-02: docker-compose.e2e.yml lacks extra_hosts on the api service — validateCallbackURL DNS-resolves callback host inside the api container, so the E2E webhook pair fails with 400 on plain-Linux docker engines (works on Docker Desktop)"
      - "WR-03: engine-class string literals ('image'/'document') duplicated across api constants, converter Engine() returns, reconciler switch, and DB CHECK — should be exported constants in internal/convert"
      - "WR-04: E2E HTTP clients (API calls, presigned download) have no per-request timeout — a hung endpoint degrades the suite's 5-minute-per-job diagnostic into a go test binary-timeout panic"
  - phase: pre-existing
    items:
      - "gofmt nit in internal/queue/queue_test.go (tracked since Phase 9/10 deferred-items, unrelated to v1.2 code)"
---

# Milestone v1.2 "Document Engine Class" — Audit

**Audited:** 2026-07-10
**Status:** ✅ PASSED
**Scope:** Phases 8–11 (12 + 1 gap-closure = 13 plans total)

## Requirements Coverage (3-source cross-reference)

All 10 DOC requirements verified via VERIFICATION.md (passed) + SUMMARY.md frontmatter (`requirements_completed`) + REQUIREMENTS.md traceability (`[x]`, Done/Complete). No orphans, no partials.

| Requirement | Phase | VERIFICATION | SUMMARY | REQUIREMENTS.md | Final |
|-------------|-------|--------------|---------|-----------------|-------|
| DOC-01 structural sniff → 422 before S3 | 8 | passed | listed (08-01/08-02) | [x] Done | satisfied |
| DOC-02 zip-bomb uncompressed limit | 8 | passed | listed (08-01/08-02) | [x] Done | satisfied |
| DOC-03 macro rejection | 8 | passed | listed (08-01/08-02) | [x] Done | satisfied |
| DOC-04 LibreOffice per-job profile | 9 | passed | listed (09-01/09-02) | [x] Done | satisfied |
| DOC-05 %PDF- output validation, terminal on invalid | 9 | passed | listed (09-01/09-02) | [x] Done | satisfied |
| DOC-06 hardened exec kills soffice (integration-tested) | 9 | passed | listed (09-01/09-02) | [x] Done | satisfied |
| DOC-07 separate document-worker container | 10 | passed | listed (10-03/10-04) | [x] Complete | satisfied |
| DOC-08 DOCUMENT_ENGINE_TIMEOUT, terminal | 10 | passed | listed (10-01/10-03) | [x] Complete | satisfied |
| DOC-09 engine-aware reconciler | 10 | passed | listed (10-02) | [x] Complete | satisfied |
| DOC-10 GET/webhook/download parity | 11 | passed (re-verified 4/4 after 11-04 gap closure) | listed (11-01..11-04) | [x] Complete | satisfied |

## Phase Verifications

| Phase | Status | Score | Notes |
|-------|--------|-------|-------|
| 8 — Document Content Safety & Format Detection | passed | 4/4 | — |
| 9 — LibreOffice Converter Engine | passed | 3/3 | — |
| 10 — Document Worker & Reconciler Integration | passed | 17/17 | Deferred live smoke explicitly addressed in Phase 11 (SC#4) — closed |
| 11 — API Routing & E2E Document Conversion | passed | 4/4 | Initial gaps_found (Content-Type parity) closed by plan 11-04, re-verified |

## Cross-Phase Integration (gsd-integration-checker)

**10/10 links WIRED, 0 orphaned, 0 missing, 0 broken flows.** Highlights:

1. Safety gate ordering: sniff → zip-bomb → macro → format-mismatch → EngineFor → (image-only) dimension check, all strictly before `storage.Upload`/`repo.Create` (`internal/api/handlers.go:118-214`); fail-closed on unrecognized content.
2. Routing chain traced end-to-end: `EngineFor` → `EnqueueDocumentConvert` → `TypeDocumentConvert`/`QueueDocument` → `cmd/document-worker` mux → `HandleDocumentConvert` → registry Lookup → `LibreOfficeConverter.Convert` → `validatePDF`.
3. Reconciler switches on `jobs.engine` with fail-closed skip + metric for unknown engines.
4. Webhook/download parity: both worker success/failure branches call `EnqueueWebhookDeliver` identically to the image path; `handleGetJob` format-agnostic; Content-Type via single `convert.MIMEType` (extended in 11-04) propagates to both call sites.
5. Image pipeline regression check: dimension check still applies to images only, image routing/Dockerfile.worker (libvips-only) intact.
6. Live E2E evidence: all 6 pairs (docx/xlsx/pptx/odt/ods/odp → pdf) PASS against the real docker-compose stack, HMAC-verified webhook, human-approved checkpoint (11-03-SUMMARY.md).

Build gate at audit time: `go build ./...`, `go vet ./...`, `go test ./... -count=1` — all green (14 packages).

## E2E Flows

| Flow | Status |
|------|--------|
| upload → sniff/validate → S3 → Postgres → engine-routed enqueue → LibreOffice convert → PDF validate → S3 output → MarkDone → webhook → GET /v1/jobs/{id} → presigned download | ✅ complete (live-verified) |

## Nyquist Compliance

Skipped — `workflow.nyquist_validation: false`.

## Tech Debt (non-blocking, carried forward)

See YAML frontmatter. Summary: 3 advisory code-review findings from 11-REVIEW.md (WR-02 E2E Linux `extra_hosts`, WR-03 engine-literal duplication, WR-04 E2E client timeouts) + 1 pre-existing gofmt nit. DOC-V2-05 (active anti-DoS by document complexity) remains an accepted residual risk per v1.2 requirements definition.

---

_Audit performed by orchestrator + gsd-integration-checker, 2026-07-10_
