apiVersion: v2
name: octoconv
description: OctoConv async file-conversion service — API, workers, and stateful backbone (Postgres/Redis/MinIO) on Kubernetes.
type: application
version: 0.1.0
appVersion: "1.6"
